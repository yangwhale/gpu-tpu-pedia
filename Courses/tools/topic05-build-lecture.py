# -*- coding: utf-8 -*-
r"""专题五 · 并行策略 —— **讲义（授课稿）**。

════════════════════════════════════════════════════════════════════
⭐ 这一讲的讲法，一句话说清
════════════════════════════════════════════════════════════════════
  它是一条**接力线**：每一刀都在补前面没管到的那一块。
  （2026-09-24 R7 更正：原写「每一刀都是上一刀逼出来的」，只在 FSDP→TP、切序列→PD 分离两处成立；
    V3 不用 TP 是选型不是「逼」，长上下文是模型换了形状。课件早已改口，讲义 2026-09-25 跟上。）
  ⛔ 所以**不能跳**。每节最后那句「这一刀留下的问题」就是下一节的开场白，
    台上一定要念出来，台下才知道为什么要换刀。

════════════════════════════════════════════════════════════════════
⭐⭐ 写这份讲稿时守的三条（跟专题三、四讲义同一套）
════════════════════════════════════════════════════════════════════
  ① 讲稿不复述图上的内容，只说**图给不了的**：该看哪一格、为什么这一格重要。
  ② 这是科普：**一个数是内容才报，是证据就说「有人量过」**，具体值留给课件。
  ③ 查来的和推来的要分开说。课件里推导的数都标了「本课推导」，台上照样要说。

📌 CSS 沿用专题一讲义。⛔ 不要另写一套。
"""
import io
import os
import re

HERE = os.path.dirname(os.path.abspath(__file__))
WEB = os.path.join(HERE, "..", "WebPages")
CSS_SRC = os.path.join(WEB, "topic-01-lecture.html")
OUT = os.path.join(WEB, "topic-05-lecture.html")
DECK = "topic-05.html"


def _css():
    """⛔ 逐字稿样式必须插在 </style> **之前**。"""
    h = io.open(CSS_SRC, encoding="utf-8").read()
    i, end = h.index("<style>"), h.index("</style>")
    out = h[i:end] + "</style>"
    assert out.count("</style>") == 1
    return out


# ══════════════════════════════════════════════════════════════════
# 九节的讲稿。每节四件事：留下什么 / 讲稿 / 屏幕 / 别讲什么
# ⛔ 「屏幕」里的图 id 和小节号会被 topic02-lint-cues.py 逐个对账。
# ══════════════════════════════════════════════════════════════════
CH = [

# ── 零 ─────────────────────────────────────────────────────────────
("第零节　开场：一张卡装不下（5′）", "s零", "", """
<p>🎯 <b>这一节只留一句：用一种通信，换一份显存或一份算力。</b></p>

<p class="board"><b>🖥 屏幕</b>：课件 <code>s零</code> 最上面的「🎯 开场热身」，点开，<b>先只露第一问的表</b>，别往下滚（导语里有第一问的答案）。第二、三问是预习，题里不给答案。</p>

<p class="say">「上课先做一道题，回顾上一讲。DeepSeek-V3，六千七百一十亿参数，按常规的混合精度训练，每个参数要在显存里常驻五样东西：算的时候用的权重、梯度、优化器更新的主权重、一阶动量、二阶动量。各占几个字节？」</p>

<p>💬 <b>互动</b>：等台下报数，填进表里，点「对答案」。</p>

<p class="say">「二、二、四、四、四，一共十六个字节。前两样是半精度，算矩阵乘用；后三样是全精度，给优化器用。<br>
乘上六千七百一十亿，九点七六 TiB，大约一万 GB。<br>
十六字节是常规口径，梯度按半精度算。真用 bf16 训练时，框架常把累加梯度的那份放全精度，Megatron 默认就这样，那就是十八字节。」</p>

<p class="say">「再留两道预习题，先想，不用现在答。<br>
第二问：这十六个字节里哪块最大？要是让你削，先削哪块？<br>
第三问：V3 其实没按十六字节存，它把其中两块压成了半精度。是哪两块？主权重为什么不能压？<br>
这两道讲到第二节揭晓。」</p>

<p class="board"><b>🖥 屏幕</b>：收起热身题，往下到导语。</p>

<p class="say">「现在最大的一块卡，显存也就两三百 GB，光放下这一万 GB 就要三四十到五六十块卡。<br>
而且别以为只有大模型才要切：八十亿参数的小模型，照样十六个字节一个参数，一百二十八 GB，一张 80 GB 的 H100 就放不下。<br>
这还没算激活，就是每一层算出来、留着反向要用的中间结果，序列一长，它比这些还大。」</p>

<p class="say">「就算放得下，一块卡也算不完。V3 的报告写着，全部训练用了两百七十八万八千个 H800 卡时，换成一块卡，要算三百一十八年。<br>
所以切开，不只为了放得下，也为了算得快。<br>
V3 的报告里，这一切就写成一行：PP 16、EP 64、ZeRO-1。现在看是天书，下课前你会读懂它。」</p>

<p class="say">「可是一切开，每块卡手里就只有一部分，缺的那部分在别人那儿，只能互相传。每切一刀，卡和卡之间就多一种通信。<br>
这门课只立一句话：<b>每一刀都是拿一种通信，去换一份显存，或者一份算力。</b>」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-roadmap</code>，只指盒子标题和下面的红字；「过路费」那一行先不念，第一节就认识它们。</p>

<p class="say">「今天的剧情就这一条线：<b>切一刀，付一笔通信；切完发现还有一块没管到，再切一刀。</b><br>
每一刀我们都问同样三句：它切什么？付哪种通信？放哪根线？<br>
五刀：数据、权重、专家、序列，最后连「活」本身一起拆开。你看每个盒子底下的红字，就是这一刀留下的问题，它逼出下一刀。<br>
让你来切，第一刀切在哪？大多数人会先把数据分一分，每块卡算几条样本。没错，第一刀就从这儿开始。」</p>

<p class="say">「不过下刀之前，得先认识卡和卡之间会说哪几种话。每一刀多出来的，都是其中一种。」</p>
""", """
⛔ <b>不要在这里列并行的名字</b>（DP、TP、PP、EP、CP……）。<br>
<em>名字一上来就是一堵墙。它们在后面都是从「上一刀的问题」里长出来的，那时候再叫名字，台下才记得住。</em>
"""),

# ── 一 ─────────────────────────────────────────────────────────────
("第一节　先认识五种通信（9′）", "s一", "fig-coll-nn", """
<p>🎯 <b>这一节留两样：AllReduce 能拆成两半；AllToAll 是人人给人人发不一样的东西。</b></p>

<p class="board"><b>🖥 屏幕</b>：按右下角「折叠讲解」（或按 T），<code>fig-coll-1n</code> 整张。</p>

<p class="say">「一千张卡要把各自的结果加起来，你一定以为找个班长最省事？班长一个人要收发 999 份；大家围成一圈，每人只传 1.998 份。<br>
名字看着一大堆，其实每一种只回答两个问题：<b>谁发给谁；东西到了之后，是拼起来、加起来，还是原样放着</b>。」</p>

<p class="say">「先花十秒学会看图，后面所有图都是这个画法。四张卡，一张一个颜色：卡 0 蓝、卡 1 橙、卡 2 绿、卡 3 紫。每张卡的数据切成四小块，虚线框是空的。<br>
最要紧的一条：<b>哪块上面出现竖条纹，就是被加过了</b>，条纹里有几种颜色，就是几张卡的数加在了一起。」</p>

<p class="say">「用一个班级来想。四个同学，每人一份作业，切成四块。最简单的几种都有一个班长，就是卡 0。<br>
左上，班长把整份作业给每个人发一份，叫广播；右上，班长把一份作业拆开、一人分一块，叫分发。<br>
左下，大家把自己那块交给班长拼起来，叫收集；右下，大家把整份交给班长，班长把对应的分数加起来，叫归约。」</p>

<p class="board"><b>🖥 屏幕</b>：往下展开「看它们动起来」那四段小动画，<b>先闭嘴看一圈</b>，再点开收集、归约两段全屏对比。</p>

<p class="say">「看的时候注意一件事：<b>收集和归约只差一个动作</b>。东西都送到班长那儿，一个拼起来，一个加起来。你看归约之后班长手里那四块，条纹里四种颜色都有，就是四个人的数加在了一起。」</p>

<p class="say">「班长模式有个毛病：按最笨的做法，所有东西都要挤过班长那一条线，人一多，班长就堵死了。<br>
所以训练里真正天天在跑的，是名字前面带 All 的那几个。All 的意思就是：<b>结果人人都有一份</b>，不用再找班长要。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-coll-nn</code> 整张，再放下面三段动画。<b>它们按环一步一步走，每一步落地会自动停住</b>：停住时指一下粗框那块，再点「下一步」。</p>

<p class="say">「左上是全收集，AllGather：开始每人一块，结束每人四块都有，只拼不加。<br>
右上是归约分散，ReduceScatter：开始每人一整份，结束时卡 0 只拿第 0 块的总和、卡 1 只拿第 1 块的总和，先加，再分。<br>
左下是全归约，AllReduce：开始每人一整份，结束每人都有四块的总和。数据并行同步梯度，用的就是它。右下那个长得不一样，先放着，最后讲。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-ar-split</code>，整张。</p>

<p class="say">「这张是今天最重要的一个等式：<b>全归约，等于先「加完每人分一块」，再「拼起来人人一份」</b>，也就是 AllReduce 等于 ReduceScatter 加 AllGather。<br>
先加，每人拿到一块总和；再拼，每人拿到全部总和。结果跟一步到位完全一样。」</p>

<p class="say">「为什么要拆？因为拆开之后，这两半可以放到不同的时间去做，中间还能塞进别的动作。<br>
下一节第一刀你就会看到：中间塞进一个「更新」，每张卡就少存一大块东西，通信却一个字节都不多。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-ring</code>，<b>一步一停</b>：每步停住时讲一句「粗框那块又多加进一个人」，再点「下一步」；<b>第 1 步停住时专门指卡 3 那块，它是从右边出去、沿底下绕回卡 0 的</b>。静图 <code>fig-ring</code> 留给会后自学，台上跳过。</p>

<p class="say">「那没有班长，怎么做到人人一份？围成一个环，每人只跟右边的邻居说话。<br>
先做 ReduceScatter：每一步，每张卡同时把手里的一块发给右边，再从左边收一块，加到自己同号的那一块上。第一步之后有的块是两个人的和，第二步三个人的和；四张卡转三步，每人手里都有一块是四个人的总和，就是条纹四种颜色都齐了的那块。」</p>

<p class="say">「接着用同一个环再转三步，这次只拼不加，把各自那块总和传给所有人。这后三步就是 AllGather，合起来就是一次 AllReduce。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-ar-two-ways</code>，先左后中，最后指右边那张小表。</p>

<p class="say">「同一个 AllReduce，其实有两种拼法。左边是班长做法：先全加到卡 0，再从卡 0 发回去，卡 0 要发三份，别人各发一份。<br>
中间就是刚才的环：先 ReduceScatter 再 AllGather，四张卡每张都只发一点五份。结果一模一样，差的只是谁在干活。<br>
右边就是开头那两个数：九百九十九份对一点九九八份。所以这个等式是全课最重要的一个：它不光成立，还是赢家。」</p>



<p class="say">「环也有代价：步数跟着人数涨。数据很大的时候，比的是每个人的带宽，环几乎最省；数据很小的时候，比的是一步一步的等待，步数多反而吃亏。<br>
所以通信库会按数据大小自己挑算法，小数据改走树形。TPU 更直接：用 TPU 专用快线连在一起的一整块芯片叫一个切片，大切片上芯片之间本来就连成环。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-a2a</code>，一步一停：第 1 步寄给右边第 1 个人，第 2、3 步是隔着人直接寄。</p>

<p class="say">「最后一种最特别，叫 AllToAll。每个人手里有要给每个人的一份，而且每一份都不一样，就像每个人各做一次分发。<br>
也可以想成把一张表转置一次：原来的一行变成一列。」</p>

<p class="say">「为什么网络最怕它？每一份都是写给某一个人的私信，路上没法边走边合并；前面几种都能排成只跟邻居说话的环，它不行，任意两张卡之间都有东西要走，拼的是整个网络的横截面有多宽；而且每一份多大，要等模型算到这一层才知道。<br>
记住它。后面切专家的时候，每一层都要用它两次：动画里寄过去一次，最后字幕说的原路寄回又是一次。」</p>

<p class="say">「数一下。刚才看了八个名字，但带班长的四个只是积木。日常真正用到的就五种：AllReduce、AllGather、ReduceScatter、AllToAll，再加最朴素的一对一收发。<br>
后面把模型按层切成几段时，段和段之间传东西就用它。」</p>

<p class="say">「这一节带走两件事就够了：<b>AllReduce 能拆成两半；AllToAll 是人人给人人发不一样的东西</b>。<br>
开场那张路线图上，每个盒子那行「过路费」，都是这五种里的某一种。<br>
话认全了，第一刀：切数据。」</p>
""", """
⛔ <b>不要念 1.7 那张系数表</b>，也不要直接报 2(n−1)/n 这个式子。
<em>环形 AllReduce 那一笔账用「数」的方式讲：每步发四分之一份、走六步、一共一点五份；换成 n 张卡，每步 n 分之一、走 2(n−1) 步 —— 台下跟着数一遍就懂，不用记式子。
其他原语的系数是给会后查的。</em><br>
⚠️ 环形和树形怎么挑，只点一句「小数据改走树形」，不展开（2026-09-25 现场认可这一讲法后放宽）。
"""),

# ── 二 ─────────────────────────────────────────────────────────────
("第二节　第一刀：切数据（9′）", "s二", "fig-zero-mem", """
<p>🎯 <b>这一节留一句：越闲的越先削；前两级白送，最后一级多付一半的通信。</b></p>

<p class="board"><b>🖥 屏幕</b>：滚到课件第二节标题。</p>

<p class="say">「先卖个关子：ZeRO-3 故意多付一半的通信，大模型却几乎都在用它。多花的那一半，换回了什么？<br>
第一刀最朴素：每张卡算不同的样本，这叫数据并行。<br>
每张卡放一整份模型，各算各的。算完，每张卡都得出一份「每个参数该往哪边调、调多少」，这叫梯度；样本不同，梯度就不同，所以做一次 AllReduce 求个平均，再一起更新。」</p>

<p class="say">「它一步只说一次话，是今天所有刀里<b>说话最不勤</b>的。但不勤不等于量小：V3 这么大的模型要是纯数据并行，每张卡每一步要发出去大约 2.7 TB，只是一步才发一次。」</p>

<p class="say">「可它根本没解决「装不下」：每张卡还是一整份，9.76 TiB，一个字节没少。<br>
打个比方：把 V3 的全部权重想成一整套四库全书，存进 61 间书房，一间就是一层。V3 的配置是 128 路，就是 128 个阁，每阁都放一整套：谁也不缺，可 128 个阁存了 128 份一模一样的书。」</p>

<p>💬 <b>互动</b>：<b>先别切屏</b>（图的标题和落点带就是答案），回扣开场第二问：「128 份一模一样的书，开场那十六个字节里，你先削哪块？」等台下答「那十二个」。</p>

<p class="board"><b>🖥 屏幕</b>：这时再切 <code>fig-zero-mem</code>，先按住最上面那根最长的条，之后逐行往下指（「V3 每卡常驻」那列的红字 1.29 TiB，留到讲到「可削到这儿」那句再指）。</p>

<p class="say">「那就把重复的削掉，按大小一样一样削，这叫 ZeRO。每个参数 16 个字节：蓝的是权重，2 个；橙的是梯度，2 个；绿的这一大段是 12 个，是优化器替它记的账，叫优化器状态。<br>
最大的先削。往下看：ZeRO-1，账每人只记自己那一份，绿的没了；ZeRO-2，梯度也只留自己那份，橙的也没了；ZeRO-3，连权重也切，短到看不见。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-zero-busy</code>，先指绿色那条长长的虚线，再从下往上指。</p>

<p class="say">「为什么是这个顺序？看一步里谁在忙。绿的优化器状态最大，却只在最后更新那一下用，整整一步都在显存里干放着，削它谁也不耽误。<br>
橙的梯度到反向才一层层出现；蓝的权重从头忙到尾。<b>越闲的越先削</b>，最忙的最后削，而削了它，每层用之前就得先把别人那几段复制过来。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-zero-step</code>，从左往右一格一格指：①、②、③。</p>

<p class="say">「神奇的是前两级：通信一个字节都不多。还记得刚才那个等式吗？AllReduce 拆成两半：先 ReduceScatter，每人正好拿到自己那份梯度的总和，就地更新；再 AllGather，把更新好的权重拼回给大家。<br>
活一点没多干，每人却只用替自己那份记账。」</p>

<p class="say">「中间那一步「就地更新」为什么不用传？因为 Adam 是一个参数一个参数算的：第 i 个参数的新值，只看它自己的梯度、自己的两个动量、自己的主权重，跟别人没关系。所以四张卡各更新四分之一，跟一张卡全更新一遍，结果一模一样。<br>
只有一个例外：梯度裁剪要看全体梯度有多长，这得凑一下。每张卡先算自己那段的平方和，再把这一个数加总一次，量小到可以忽略。」</p>

<p class="board"><b>🖥 屏幕</b>：往下到第三问的绿框，展开它下面的「细一点」，放 <code>fig-bf16-beta</code> 那把刻度尺。</p>

<p class="say">「揭晓第三问。V3 把两个动量压成了半精度，主权重留在全精度。注意优化器状态里没有梯度：梯度只是每步的原料，用完就清零；要全精度的是梯度累加器。算下来，V3 的优化器状态从十二字节降到八字节，累加器另算四字节。<br>
看这把尺子：刻度是半精度在 1 附近能存的数，相邻差一百二十八分之一。绿的是 β₂ 零点九五，新值占百分之五，一步挪过了好几格，存得住；红的是默认的零点九九九，新值只占千分之一，连半格都没挪过，一存又回到原处，动量就卡住了。」</p>

<p class="say">「主权重为什么不能压？它每步只加一点点，还要一直累加下去，小于半格的更新全被舍掉。所以 V3 敢压动量，不敢压主权重。」</p>

<p class="say">「可削到这儿，看「每卡常驻」这一列：每张卡还要 1.29 TiB，一千四百多 GB，还是装不下。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-siku</code>，先指 ③ 那条红线，再指阁 3 那个红框，最后指 ④。</p>

<p class="say">「只能走到最后一级：连权重也切，也就是 PyTorch 里的 FSDP。这一级就不白送了。<br>
回到藏书楼。FSDP 切的时候根本不管书是什么：一间书房的书一页页排成一长条，按长度等分成 128 段，每阁拿一段。看阁 3：一本书的后半截，接着另一本的开头，<b>单拿这一段，谁也读不了</b>。<br>
要用这一间，就把 128 段各抄一份拼回整间，读完把抄本扔掉，只留自己那段。抄齐就是 AllGather。」</p>

<p class="say">「再看底下：后面要讲的 EP 正好相反，是整架分，一架就是一个完整专家，拿着就能用。FSDP 切成碎片，只是为了通信好切块，拼回来之前没用。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-fsdp-step</code>，整张。</p>

<p class="say">「看这两行。数据并行一层两块颜色：加一次，拼一次。FSDP 是这样：要算这一层之前，先把别人那几段复制过来拼成整层，算完扔掉复制件；到了反向，也就是倒着算梯度的时候，这一层又要用，只好再复制一次，最后再把梯度加一次。<br>
一共三块，多出来的就是中间这块蓝的。所以通信多出一半。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-fsdp</code>，<b>先闭嘴等它转完一圈</b>（14 秒）。</p>

<p class="say">「看计数器：前向每层复制一次，反向每层再复制一次、再分一次梯度，三层走完是九次。<br>
换来的是：每张卡常驻的东西从大约一万 GB 降到 84 GB 左右，这还没算激活。<br>
可 V3 用的 H800 一张才 80 GB，放进去就满了。光切数据还不够，后面几刀就是这么来的。」</p>

<p class="say">「再补一句给懂行的：一步切成好几个 micro-batch 的时候，ZeRO-2 就不再白送了。它不留整份梯度，没法把几份先攒起来，只好每一份算完就加一次。所以配流水线的时候，V3 选的是 ZeRO-1。」</p>

<p class="say">「再记一件事：数据并行一步说一次话，FSDP 一步要说成百上千次。<b>说得越勤，越只能放在快线上</b>，讲摆到机器上的时候会回来。」</p>

<p class="say">「这一刀留下什么问题？FSDP 每一步搬的是权重，搬多少只看模型多大；可是算多少，跟每张卡分到的 token 数成正比，token 大致就是一个字。<br>
卡越加越多，一次喂的总量却不能跟着无限加，加太大模型反而学不好，每张卡分到的就越来越少。<br>
算得少、搬得一样多，时间就全被搬权重吃掉了。这就逼出了第二刀。」</p>
""", """
⛔ <b>不要讲 HSDP、DiLoCo。</b><br>
<em>2.4 那两条是「这一刀能放多远」的延伸，台上不讲，被问到再说「机内 FSDP、机间数据并行」。</em><br>
⚠️ 数据并行是「说话最不勤」，不是「最省」：它一步的通信量其实很大（V3 每卡约 2.7 TB），低的是频率。<br>
⚠️ 「FSDP 白送」这句话最容易说错。<b>白送的是 ZeRO-1、ZeRO-2</b>；ZeRO-3 多付一半。<br>
<em>micro-batch 时只剩 ZeRO-1 白送，台上已经讲了；原因写在课件「ZeRO 的一步」那张图的出处里。</em>
"""),

# ── 三 ─────────────────────────────────────────────────────────────
("第三节　第二刀：切权重（9′）", "s三", "fig-intensity", """
<p>🎯 <b>这一节只留一句：FSDP 搬工具、看 batch；TP 递门板、看隐藏维。</b></p>

<p class="board"><b>🖥 屏幕</b>：滚到课件第三节标题。</p>

<p class="say">「「少」到底是多少？先问个朴素的问题：一份东西搬过来一次，能被用几回？<br>
想象一间做门的工坊：师傅干一件活两秒，小工送一趟东西要五秒，师傅再快也得等。每张卡就是师傅，网络就是小工。<br>
小工搬两样东西。一样是工具，电刨、模具，这就是权重；一样是门板，一块块正在加工的门，一道工序做完递给下一道，这就是激活，每个字一块。<br>
要看的是：小工每送来一个字节，够师傅忙多久。」</p>

<p class="say">「尺子的一头是硬件：师傅手多快，除以小工腿多快。TPU v7 一颗芯片每秒算两千三百多万亿次，往外每秒送六千亿字节左右，一除，三千八百多。<br>
就是说每送来一个字节，得有三千八百多次活等着，师傅才不用等。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-intensity</code>，整张，先指红色横虚线。</p>

<p class="say">「横轴是每张卡这一步分到多少个 token；竖轴是每送一个字节换来多少次计算。<br>
红线就是那个三千八百多，线下涂红的那片，就是师傅在等小工。」</p>

<p class="say">「蓝的斜线是 FSDP。它让小工搬的是工具，也就是权重，就是上一节每层都要抄齐拼回的那间书房。一件工具送到，这个师傅手上的每块门板都要拿它过一遍，门板越多，同一趟送货够师傅忙得越久。<br>
算出来特别干净：一个参数两个字节，每个字拿它做一次乘加，正好两次运算，一约就掉。所以每送一个字节换来的计算，正好等于每张卡分到的 token 数。所以每张卡一步分不到三千八百多个 token，FSDP 就被拖住了。这是我们按最乐观的算法自己推的。」</p>

<p class="say">「三千八百多个 token 是什么概念？V3 预训练一条样本 4K 个 token，差不多就是每颗芯片一条；再少，搬权重的时间就露出来了。」</p>

<p class="board"><b>🖥 屏幕</b>：还在 <code>fig-intensity</code>，指红线上面那条细虚线。</p>

<p class="say">「这里有个坑：红线的分母，也就是小工的腿速，不能抄规格表。规格表上的是物理带宽，可真拼一次权重，路上有各种损耗。<br>
该用的是你在自己网络上实测出来的数：真跑一次 AllGather，GPU 上就是跑 nccl-tests，看它报的 busbw，就是扣掉算法本身的账以后，每张卡每秒实际收到多少字节。<br>
实测只跑到规格的七成，门槛就从三千八百多抬到五千五左右，就是这条细虚线，七成是我假设的。FSDP 要是只占了几根链路、还跟别的并行抢线，门槛还得往上走。」</p>

<p class="say">「第二刀不再搬权重，直接把权重矩阵切开，叫张量并行，TP。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-tp-mlp</code>，整张。</p>

<p class="say">「英伟达的 Megatron 框架用了一个巧思：第一块权重竖着切一刀，第二块横着切一刀。<br>
两个人各拿一半，蓝的只跟蓝的乘，橙的只跟橙的乘，中间谁也不用问谁。<b>通信被整个推到了出口，只在最后做一次 AllReduce 加一下</b>。<br>
用工坊的话说：两个师傅合干同一批门板，各做一半工序，做完得把门板凑到一起合成，这一凑就是那次 AllReduce。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-tpsplit</code>，<b>先闭嘴看完一圈</b>（9 秒）。</p>

<p class="say">「两个容易绕晕的地方。第一，每张卡两块都有份：卡 0 拿 UP 的一半，也拿 DOWN 的一半，不是一张卡管 UP、另一张管 DOWN，那是后面要讲的按层切。UP 和 DOWN 之间不说话，DOWN 算完才加一次，传的是激活。<br>
第二，横着切还是竖着切，只是画法。我们把权重画成「输入维乘输出维」，第一刀切输出维，看着是竖的；PyTorch 把权重存成「输出维乘输入维」，同一刀在它那儿就是横的。所以别记横竖，记<b>第一块切输出维，第二块切输入维</b>。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-tp-order</code>，先左后右。</p>

<p class="say">「为什么非得先竖着切？反过来试试。先横着切，每张卡手里是同一个数的一份，要加起来才是它：卡 0 是 2，卡 1 是负 3，真值负 1。<br>
GeLU 不是一条直线。先加再过，是负 0.159；各过各的再加，是 1.950，符号都反了。所以只能先加，凭空多一次通信。<br>
竖着切，每个数整个落在一张卡上，GeLU 各算各的。」</p>

<p class="say">「数一下这一下来几次。前向，MLP 出口一次，attention 出口一次。反向倒过来：X 是两张卡共用的，两边各算出 X 的一半梯度，入口处又得加一次。<br>
一层前向两下、反向两下，每一层都来。所以它只能待在最快的那一片互联里，GPU 上一般不出一台机器。」</p>

<p class="board"><b>🖥 屏幕</b>：切回 <code>fig-intensity</code>，指两条橙色平线。</p>

<p class="say">「回到这张图，TP 这条线是平的。为什么？TP 让小工递的不是工具，是门板，也就是算到一半的中间结果，激活。每个字一块门板，字多一倍，要递的多一倍，活也多一倍，一除就约掉了。」</p>

<p class="say">「它只看两件事：每个字在模型里用多长一串数表示，这叫隐藏维，就是门板的尺寸，板子越大，每递一趟要干的活越多，线越高；再就是切几份，切得越碎，每人的活越少，送的却没少，线就往下掉。<br>
V3 切 8 份是这条实线，按最乐观的红线刚好贴线，实际已在线下；切 32 份是下面那条虚线。」</p>

<p class="say">「所以两把刀各管一边：一次喂进去的那一批数据，叫 batch，<b>batch 小的时候多用 TP，大的时候多用 FSDP</b>。<br>
图上就是：左边蓝线还没爬上来，靠橙线；右边蓝线爬过去了，交给 FSDP。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-fsdp-tp</code>，先 ① 的左边大矩阵，再右边 8 张卡；然后 ② 从左往右；最后 ③ 两个框对比。</p>

<p class="say">「那 FSDP 和 TP 能不能一起用？能，而且大模型训练里常常一起用。诀窍是两刀切在不同的维上。<br>
看这 8 张卡，排成两行四列。TP 先按输出维把权重竖着切成四条，一列一条；FSDP 再把每一条上下劈成两半。每张卡只存八分之一。<br>
一行四张卡是一组 TP，看的是同一份数据；一列两张卡是一组 FSDP，数据各不相同。」</p>

<p class="say">「算一层分三步。第一步，列里 AllGather：同一列的两张卡把上下两半拼齐。注意，拼回来的只是自己那一条，整个矩阵的四分之一，不是整个矩阵。<br>
第二步，各算各的，一行四张卡拿着同一份数据，各算四分之一。第三步，行里 AllReduce，把部分和加起来，传的是激活。」</p>

<p class="say">「最妙的是账。只用 FSDP 时，每张卡拼一整层，只给自己那些 token 用。一起用时，每张卡只拼四分之一条，却是给这一行四张卡的全部 token 用。所以 FSDP 的门槛按一行的 token 数算，摊到每张卡只要九百六十个左右。<br>
这才是「batch 小多用 TP」的真正意思：<b>TP 把一行的 token 借给了 FSDP</b>。代价是 TP 那根轴每层都要对账，得占最快的线。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-wide-deep</code>，先左后右，数红点。</p>

<p class="say">「TP 出不了最快的那一片。要横跨很多台机器、走慢线，又不想每层搬权重，就换个方向切。<br>
TP 是切宽：每一层切成四条，所以每一层都得对账，左边红点一层一个。PP 是切深：前两层给卡 0，再两层给卡 1，只在段和段的交界递一次门板，用的是第一节那个一对一收发，右边只有三个红点，所以敢走慢线。」</p>

<p class="say">「可朴素地切深，是四张卡的显存、一张卡的速度：同一时刻只有一段在干活。<br>
所以把一批数据切成几份轮着灌进去。还是那间门厂，换成流水线：刨光、开孔、上漆三道工序各守一个工位，门板一摞一摞往下传。第一摞刚进刨光时，后两个工位只能干等。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-pipeline</code>，<b>闭嘴让 4 个和 8 个 micro-batch 都放完</b>（14 秒）。静图 <code>fig-pp-bubble</code> 留给自学。</p>

<p class="say">「工位上干等的那段，就是这些灰色的格子，叫气泡：开头要等后面几段灌满，结尾要等前面排空。<br>
一批数据切得越碎，气泡越小，你看同样多的活切成 8 份，灰色短了一半。可也不是越碎越好，每一份太小，卡就吃不饱。」</p>

<p class="say">「这一刀留下的问题，要从一个反常的例子说起：V3 训练的时候，一点 TP 都没用。报告说是显存抠得够细，用不着。<br>
再看参数都在哪：几乎全在一种叫「专家」的窄网络里。切进一个很窄的矩阵，每人分到的活更少，要递的门板一块不少，跟刚才那条平线往下掉是同一个道理。<br>
那就别劈矩阵，按专家切。」</p>
""", """
⛔ <b>不要推那个「4.5 × 隐藏维 ÷ TP 度数」的式子。</b><br>
<em>只说结论：TP 的账跟 batch 无关，只跟隐藏维和切几份有关。式子在课件 3.4。</em><br>
⚠️ PP 的三种改进（VPP、Zero Bubble、DualPipe）<b>点名就够</b>，别一个个讲机制。
"""),

# ── 四 ─────────────────────────────────────────────────────────────
("第四节　第三刀：切专家（7′）", "s四", "fig-moe-params", """
<p>🎯 <b>这一节留两样：EP 最重的病是负载不均；attention 和专家可以各配各的切法。</b></p>

<p class="board"><b>🖥 屏幕</b>：滚到课件第四节标题。</p>

<p class="say">「V3 的参数，97% 都在专家里。可病人不会平均挂号：最忙的那个专家，决定所有人等多久。<br>
这类模型叫 MoE：V3 除了最前面三层，每层有 256 个并排的小网络，叫专家；每个字只挑其中 8 个去算，其余不碰。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-moe-params</code>，整张；讲完再指底下的绿框。</p>

<p class="say">「先看这根条：100 份参数里，97 份在专家身上；注意力、词表这些，还有每层一个人人都要过的共享专家，加起来只有右边那一小截。<br>
所以对 MoE 模型，最省事的切法不是把每个专家劈开，而是<b>把专家整个分给不同的卡</b>：搬病人，不劈医生。这叫专家并行，EP。<br>
回到那座藏书楼：256 个专家书架整架分给 64 个阁，每阁 4 架，拿着就能用；跟 FSDP 切碎了再拼正好相反。<br>
你看底下这行，就是 V3 训练时的真实配置：16 路流水线、64 路专家并行，TP 一路都没有。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-ep</code>，<b>先闭嘴看完一圈</b>（10 秒）；讲病的时候再放一遍，等专家 0 门口排出 7 个再指。</p>

<p class="say">「它的通信就是第一节最后那一种，AllToAll。<br>
像医院分诊：每个 token 先由路由决定去看哪几个专家，派过去，在专家门口排队；看完，再送回原来的卡。每一层两次。<br>
AllToAll 是网络最怕的那种。V3 压它用了两招。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-ep-route</code>，先左后右，最后指绿色短弧。</p>

<p class="say">「左边是不管不顾：一个 token 选了 8 个专家，要是 8 个专家各在一台别的机器上，就跨机发 8 份。<br>
右边是 V3：路由挑专家之前先挑机器，最多 4 台。每台机器跨机只发一份，先落到同编号的那张卡，再走机器里的 NVLink 转给真正的专家。派发还从两个字节压成一个。一算，是左边的四分之一。」</p>

<p class="say">「为什么敢在机器里多转几手？机器里的 NVLink 是跨机网络的 3.2 倍快。跨机送到一份，在里面转给三个专家，时间也藏得住。<br>
所以报告说，同样的通信量，其实能选到 13 个专家。账只跟去了几台机器有关，跟选了几个专家无关。」</p>

<p class="say">「可这一刀真正的病不在通信，在这根队。别的刀切得匀不匀，大多是配置定的，事先就知道。<br>
EP 不行：哪个专家忙，要等路由算完才知道，而且每一批都不一样。你看专家 0 门口排了 7 个，别的只有一两个，<b>所有卡都得等它排完</b>。」</p>

<p class="say">「更要命的是 V3 一个 token 都不丢。别家常设一个容量上限，专家门口满了就把多的扔掉；V3 不扔，所以不均就直接变成等待。」</p>

<p class="say">「而且它会越滚越大：越受欢迎的专家分到的 token 越多，学得越好，就越受欢迎，别的专家荒掉。花一个大模型的显存，训出一个小模型。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-ep-bias</code>，从左往右三张，指红虚线。</p>

<p class="say">「老办法是在训练目标里罚「分得不匀」，等于逼分诊员改诊断，会拖累模型学东西。V3 换了个办法：在门口挂个号牌。<br>
号牌就是给每个专家的分数加一点、减一点：每一步结束看一眼，谁超载就减 0.001，谁闲着就加 0.001。它只影响病人被分到哪个门口，不改医生的诊断，算出来的权重一点不动。你看这个示意模拟，最忙的从平均的两倍多，慢慢压到一点零几倍。<br>
推理的时候，再把热门的专家多复制几份，分到不同的卡上。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-fold</code>。</p>

<p class="say">「还有一件事。一层里，attention 和专家是两种完全不同的形状。<br>
你看左边：4 张卡一组，合起来算一份 attention，两组各管一批请求。右边同样这 8 张卡，每张放 32 个专家。」</p>

<p class="say">「推理那边的简称：DEP8，D 是 attention 每张卡各管一批请求，EP8 是专家铺在 8 张卡上；attention 几张卡合起来算，就叫 TEP。」</p>

<p class="say">「差别能有多大？我们在 GB300 上测过：出字那一段从 TP4 换成 DEP8，同样并发下每张卡的吞吐翻了一倍。细账第七节算。」</p>

<p class="say">「其中一笔在 attention 那一半，跟 KV cache 有关，就是每个字留下的那份笔记：这份笔记切不开，TP4 就在四张卡上各抄一遍。为什么切不开，下一刀讲。<br>
这就是这一刀留下的问题：专家摊开了，attention 那一半还有样东西跟着对话长度往上涨，前三刀谁都没碰过：序列。」</p>
""", """
⚠️ <b>图右画的是 V3</b>（256 个专家、每卡 32 个）；GB300 实测用的是 V4-Pro（384 个专家，EP8 下每卡 48 个）。台上别说「就像图右边那样」。<br>
<em>被追问 GB300 倍数时：运行手册里的「2.85 倍」是 DEP8 并发 768 对 TP4 并发 512 的总量比（60,065 ÷ 21,100），「3.09 倍」是并发 1,536 那行。同并发 512 是 2.61 倍，课件用的是这个。</em><br>
⛔ <b>不要把 TEP ／ DEP 跟 Megatron 的 ETP ／ EDP 放在一起讲。</b><br>
<em>名字只差一个字母顺序，意思完全不同。台上提了就得花三分钟解释，课件 8.6 有专门的警告框，会后让人自己看。</em><br>
⚠️ GB300 那个例子要说「每张卡」、说「同样并发」：<b>每卡 2.09 倍</b>（总量 2.61 倍）。并发拉到 1,536 每卡能到 2.47 倍，但首字要等 95 秒，别拿那个数当头条。
"""),

# ── 五 ─────────────────────────────────────────────────────────────
("第五节　第四刀：切序列（6′）", "s五", "fig-kv-dup", """
<p>🎯 <b>这一节留一句：训练切激活，推理切 KV；TP 超过 KV 头数，多出来的全是复制。</b></p>

<p class="board"><b>🖥 屏幕</b>：滚到课件第五节标题。</p>

<p class="say">「一个 128K 的请求，光笔记就 8.58 GiB。你以为切到 8 张卡上，笔记也就分成 8 份？不一定，有一种切法是原样存 8 次，白存 7 份。<br>

训练的时候，是每层要存的激活跟着序列变长；推理的时候，是这份笔记。」</p>

<p class="say">「先把账算清楚，为什么一条样本会放不下。<br>
训练这边：每一层要存的激活，至少跟序列长度成正比，序列长一倍，激活多一倍；要是不用 FlashAttention，注意力那一项还跟长度的平方成正比。<br>
推理这边算得更具体。V3 每个字存一份压缩过的笔记，512 个数，再加 64 个数的位置信息，一共 576 个数，每个数 2 个字节，61 层都要存，一个字就是七万多字节。<br>
128K 个字乘下来，8.58 GiB，差不多是一张 80 GB 显卡的九分之一。这还只是一个请求，线上同时有几十上百个。」</p>

<p class="say">「训练这边叫上下文并行，CP：一条长序列切成几段，每张卡存一段。<br>
难点只在注意力：每个 token 要看它前面所有的 token，而那些 token 在别的卡上。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-ringattn</code>，<b>先闭嘴看半圈</b>（整圈 9 秒）。</p>

<p class="say">「一种办法叫 Ring Attention。每个 token 手里有一个提问，叫 Q，拿它去翻别人的笔记。<br>
自己的 Q 不动，别人的笔记 KV 沿着环一段段传过来，来一段算一段，边算边传。转一圈，自己那一行就填满了。又是第一节那个环。」</p>

<p class="say">「它为什么藏得住通信？每一步传的是一段笔记，量跟段长成正比；算的是这一段提问对这一段笔记，量跟段长的平方成正比。<br>
段越长，算的涨得比传的快。所以恰恰是序列越长，这个环越好用。」</p>

<p class="say">「每张卡最后怎么拿到完整答案？它的提问跟每一段笔记都见过一面，每次得到一份部分结果，转一圈把几份合起来。怎么合有个坑，本节后面讲 DCP 时一起说，是同一招。」</p>

<p class="say">「另一种叫 Ulysses。先说一个词：注意力分很多个头，V3 有 128 个，各算各的。<br>
Ulysses 用第一节那个转置，把按段切换成按头切：每张卡拿到全部序列、一部分头，本卡就能算完，算完再转回去。<br>
一句话记住：Ring 是人不动、笔记转圈；Ulysses 是换个切法。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-ulysses</code>，先 ① 左边的灰表（指红色竖箭头、再指一行），再 ① 右边 Ring；然后 ② 从左往右三张表。</p>

<p class="say">「Ulysses 光听容易糊涂，换张图看。把注意力的活排成一张表：一行是一段序列，一列是一个头。<br>
先看这张表的脾气：同一列里，后面的段要看前面的段，所以跨段就得传笔记；同一行里，头与头各算各的，分开算根本不用说话。<br>
Ring 是按行分：每张卡一段、全部头。列里的依赖跨了卡，只好让笔记沿环转一圈。」</p>

<p class="say">「Ulysses 的想法是：既然行里没依赖、列里有依赖，那我按列分不就好了？<br>
进来的时候是按段分的，因为前面的逐 token 运算本来就按段切。做一次 AllToAll：卡 k 把自己那行的第 j 格发给卡 j，留 1 格，发 3 格。<br>
换完就是中间这张：每张卡一个头、全部的段。这一列要看的东西全在本卡，注意力在本地算完，一次都不用问别人。算完再 AllToAll 一次，换回按段分，接着做后面的运算。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-ulysses</code>，切手动模式，<b>一步一步点「下一步」</b>：先停在头 0 散在四张卡的那一幕，再看卡 0 单独发，最后大家同时照做（共 26 秒）。</p>

<p class="say">「动画里再看一遍，这回按卡摆：每张卡左边一列是按段，右边一列是按头。先看卡 0：头 0 留下，头 1、2、3 分别飞到卡 1、2、3 的右列。其余三张卡同时照做。换完，每张卡右列就是一个头的全部四段。」</p>

<p class="say">「两者各有长短。Ulysses 的好处是：序列长一倍、卡也多一倍，每张卡的通信量不变。<br>
它的天花板是头数：换成按头切以后，每张卡至少得拿一个完整的头，所以并行度不能超过头数；KV 头比提问头少的模型，就卡在 KV 头数上。<br>
Ring 没有这个上限，但每一步要等一段笔记传过来。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-usp</code>，先 ① 左表到右表那一步，再指右表里的红色上下箭头；最后 ② 那张对比表。</p>

<p class="say">「两种还能叠起来用，叫 USP。还是那张表，这回分成块。4 张卡、2 台机器。<br>
第一步，在机器里做 AllToAll，这就是 Ulysses，只在 2 张卡之间：机器一的卡 0 拿前两段的头 0、头 1，卡 1 拿前两段的头 2、头 3；机器二同理，拿后两段。<br>
现在每张卡是两段乘两个头。头已经分开了，剩下跨段的部分交给环：拿同一批头的上下两块互传笔记，卡 0 和卡 2 一对，卡 1 和卡 3 一对，这一步走机器之间。」</p>

<p class="say">「看下面这张对比表。USP 的总卡数 ＝ Ulysses 那一维乘环那一维。头数上限只管 Ulysses 那一维，环那一维随便加，所以头少的模型也能切到很多卡上。<br>
摆法也顺：AllToAll 最怕慢线，留在机器里；环一步只跟邻居说话、还能边算边传，放到机器之间。这是按两种通信的脾气归纳的常见摆法，不是论文的硬规定。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-usp</code>，手动模式一步一步点（共 27 秒）；在「卡 0 管头 0、1，可段 2、3 的笔记在卡 2 上」那一幕多停一会儿。</p>

<p class="say">「动画里看先后：先是机器一里卡 0、卡 1 互换头，机器二同时照做；这时候红框告诉你，卡 0 缺的是段 2、3 的笔记，在对面机器上。<br>
第二步，半透明的抄件跨机器飞过去，卡 0 和卡 2 互换、卡 1 和卡 3 互换。各自算完，抄件就扔，再在机器里换回按段分。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-cp-zigzag</code>，先只指左边的三角。</p>

<p class="say">「生成式模型还有个麻烦：每个 token 只看前面的，越靠后的段算得越多。按顺序切，卡 3 算 15 格，卡 0 只算 3 格，大家都得等卡 3。<br>
</p>

<p>💬 <b>互动</b>：盖住右半边，<b>连同标题和落点带</b>（标题里就写着「之字形切正好摆平」），问「8 块、4 个人，怎么分最公平？」等有人说出「轻的配重的」。</p>

<p class="say">「<b>把最轻的一块和最重的一块配成一对，交给同一张卡</b>。右边这样分，每张卡都是 9 格。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-kv-dup</code>。</p>

<p class="say">「推理这边更反直觉。V3 提问的头有 128 个，笔记却压成了一个头，大家共用。<br>
TP 是按头切的：提问分得开，笔记那一个头切不开，<b>只能每张卡各存一份一模一样的</b>。8 张卡，红色那 7 份是白存的。<br>
上一节 GB300 那个 V4-Pro 也是一个笔记头，TP4 白存了 3 份，毛病之一就在这儿。」</p>

<p class="say">「一般的规律是这样：TP 按头切，笔记有几个头，就最多分给几张卡。<br>
很多模型把笔记定成 8 个头，比如 Llama 2 70B，TP 8 路正好一张卡一个头，一点不浪费。TP 一旦超过笔记的头数，每个头就被复制「TP 除以头数」次。<br>
MLA 把笔记压成了一个头，这个除法最难看：TP 开几路，就存几份。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-dcp</code>，先闭嘴看完第一幕「只开 TP，四块红」，等画面换成 DCP 再开口（整圈 11 秒）。</p>

<p class="say">「解法叫 DCP：笔记按 token 轮流存到这几张卡上，还是原来那几张，一张不加。<br>
新字的提问发给每张卡，各自在自己那份笔记里查，最后合一下。每层多付三次通信：收齐提问、交换分母、合并结果，换回被复制掉的显存。<b>又是拿通信换显存</b>。」</p>

<p class="say">「两个细节。为什么按 token 轮流存，不是一段一段地存？因为笔记一直在长，每出一个字就多一条，轮流存，新来的字也自然均摊到每张卡上。<br>
DCP 开几路？最多开到 TP 除以笔记头数，也就是刚好把被复制的那几份收回来，再多就没东西可收了。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-softmax-merge</code>，先中间（错的），再右边（对的）。</p>

<p class="say">「合的时候有个坑：注意力是按分数加权平均，要除以所有分数的总和，可每张卡只看得到自己那份笔记。<br>
各除各的再平均，是 14，错了：卡 1 只有一个字，却占了一半的票。正确做法是先别除，分子加分子、分母加分母，最后只除一次，是 11。<br>
跟合并两个班的平均分一样，得带上人数。所以每张卡还得交出自己的分母，这就是中间那一次通信在交换的东西。Ring 在路上这样合并，DCP 在终点这样合并，是同一招。」</p>

<p class="say">「推理其实分两段：先一口气读完整个问题，叫 prefill；再一个字一个字往外出，叫 decode。DCP 管的是 decode。<br>
prefill 那边也有对应的一种，叫 PCP：把一个长问题切到几张卡上一起读，第一个字出得早。它跟 DCP 有两点正好相反：PCP 要另外加卡，是在 TP 之外再加一维，DCP 不加卡；PCP 只管算得快，不管笔记装不装得下。」</p>

<p class="say">「这一刀留下的问题：prefill 读一个长问题，要的是把序列切开；decode 要的是 DCP 把笔记摊开。同一批卡只能摆一种切法，硬塞在一起，谁都配不好。」</p>
""", """
⚠️ <b>被追问「V3 的 config 里 KV 头明明是 128」</b>：那是 MLA 解压之后的头数；推理时缓存的是压缩后那一份，所有头共用，vLLM 把它当一个 KV 头处理。V4-Pro 的 config 直接就是 1 个 KV 头，但它不是 MLA，别说成 MLA：V4 的注意力是 CSA 和 HCA 交错堆叠（见专题三 6.4b），两种层存的都是所有头共享的一条 KV，config 里也没有 kv_lora_rank。<br>
⚠️ Ulysses 只讲结论（通信量不随规模涨、并行度不超过头数），不推公式。<br>
⚠️ Ring Attention 那段动画画的是<b>不带因果掩码</b>的情形，别说成它自带负载均衡。<br>
<em>2026-09-26 现场：五、六两节被嫌「随便一提就过去」，已把课件里的推导（8.58 GiB 的来历、Ulysses 的上限、TP÷头数、DCP 度数、PCP）写进台词。</em>
"""),

# ── 六 ─────────────────────────────────────────────────────────────
("第六节　第五刀：不切张量，切工作（7′）", "s六", "anim-pd", """
<p>🎯 <b>这一节留一句：把性质不同的两种活拆到两批机器上，每一边才能挑自己的切法。</b></p>

<p class="board"><b>🖥 屏幕</b>：滚到课件第六节标题。</p>

<p class="say">「拆给两批机器，中间还得来回搬东西，听上去更费卡？结果反过来：3 张卡能顶 7 张。<br>
上一节留下的问题是：prefill 和 decode 要的切法不一样。其实不只切法，连吃的东西都不一样。<br>
前四刀切的都是张量。第五刀换个思路：<b>一个请求里本来就有两种很不一样的活，干脆拆到两批机器上</b>。」</p>

<p class="say">「上一节说过，推理分两段。prefill 一口气读完整个问题，几千个 token 一起算，卡算不过来，这叫吃算力；<br>
decode 一次只出一个字，每一步却要把权重和笔记从显存里读一遍，读都读不过来，这叫吃带宽。」</p>

<p>💬 <b>互动</b>：问「用聊天 AI 的时候，觉得等第一个字更烦的举手？觉得字蹦得慢更烦的？」举手就行，不点人答。</p>

<p class="say">「两种都烦，而且放在一起会互相传染。你正看着回答一个字一个字往外吐，别人丢进来一本一百页的说明书，你的字就停了。<br>
先照顾新来的，老用户的字就卡住；先照顾老用户，新来的就一直等第一个字。<br>
这两种烦各有名字：等第一个字的时间叫首字延迟，TTFT；字和字之间的间隔叫 TPOT。语音助手最明显，平均速度再快，说一句停一段，就觉得卡。<br>
挤在同一批卡上，引擎每一步都得决定先干哪个。长问题一来，它的 prefill 要占好几步，这几步里所有正在出字的请求都得等：TTFT 和 TPOT 一起变差。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-pd</code>，<b>先闭嘴</b>，看上面那条被截断（整圈 11 秒）。静图 <code>fig-pd</code> 台上不放。</p>

<p class="say">「像一个厨房：有人备菜，有人出菜。放在一起的时候，来了一桌大单，备菜要占好几分钟，这几分钟里所有桌都等着上菜。你看上面那条，长橙块落下来，那几格一个字都没出。<br>
拆开之后，备菜的在另一个灶台上做，做完把备好的料递过来，出菜那边一步不停。这盘料，就是上一节那本笔记，KV cache。」</p>

<p class="say">「下面那条多用了一批机器，比的是出字断不断。」</p>

<p class="say">「有人会问：把大单切成小块，跟出菜穿插着做，不就行了？这叫分块 prefill，确实能缓解。<br>
可它有两个问题。块切小了，备菜自己变慢，因为每一块都要把前面的笔记重读一遍；更根本的是，两拨人还在抢同一个灶台，只能用同一套切法。<br>
PD 分离干脆拆开：prefill 机器只做 prefill，算完把笔记交给 decode 机器，decode 机器只管出字。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-kv-trip</code>，先指上面那根灰条，再指下面那根细三色条。</p>

<p class="say">「这一趟要多久？我们在 TPU v7x 上搭过一套：八千 token 的问题，按带宽估大约一百毫秒，只占 prefill 的百分之五到十。这个模型笔记小，换模型得重算。<br>
它敢走慢线，是因为一个请求只传一次。记住这一点，下一节要用。」</p>

<p class="say">「一百毫秒怎么来的？主要是中间那段跨机器。八千个字的笔记：K、V 两份，62 层，8 个笔记头，每头 128 个数，每个数 1 个字节，乘出来约 1.04 GB。<br>
机器之间按每颗芯片分到 100 Gbps 保守估，也就是每秒 12.5 GB，路上八十多毫秒；两头从显存到内存各十毫秒左右，合起来一百毫秒上下。<br>
从这一刀往后，调度的中心其实是那份笔记：它跟着请求搬家。Kimi 的 Mooncake 干脆把整套推理系统叫作「以 KV cache 为中心」。<br>
反过来，请求都很短、量也不大的时候，多出来这趟传输和两套机器就不一定划算，放在一起、用分块 prefill 缓解就够了。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-pd-ratio</code>，先上排七张灰卡，再下排三张。</p>

<p class="say">「拆开到底省不省？有篇论文算过一笔很干净的账：一张卡两样都做，每秒只接得住 1.6 个请求；只做 prefill 能接 5.6 个，只做 decode 能接 10 个。<br>
两张做 prefill、一张做 decode，三张卡每秒接 10 个；混着做要接 10 个，得 7 张卡。多用一种机器，反而省卡。<br>
思路是让两边差不多同时忙满。经验上，长问题的业务配两台 prefill 一台 decode，长回答的业务反过来，一台 prefill 两台 decode。<br>
论文把配比和各自的切法一起搜，同样的延迟要求下，最多能多服务 7.4 倍的请求，比的是 DeepSpeed-MII。刚才那个三顶七，只是其中一个单卡算例。」</p>

<p class="say">「更重要的是，两边终于可以用不同的切法了。decode 这边用上一节的 DCP 把笔记摊开；prefill 这边也有一种，叫 PCP：把长问题切开几张卡一起读，第一个字出得早，它要另外加卡。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-decode-ai</code>，先指红线，再指蓝点，最后指橙点。</p>

<p class="say">「先把 decode 的脾气算清楚。它每一步都要把权重从显存里读一遍，读一次只够这一批用一次。一批里有多少个请求在一起出字，读一个字节就换来多少次计算。<br>
还是第三节那把尺子，师傅和小工，只是这回小工是从仓库，也就是显存，往工位上搬工具：每出一个字，工具都得搬一趟，一批有几块门板，这趟就用几回。v7 每秒算两千三百多万亿次，每秒从显存读七万多亿字节，一除三百一十多。一批凑不到三百来个请求，师傅就在干等。」</p>

<p class="say">「MoE 更难。每个专家只分到这一批的三十二分之一，V3 是 256 个里挑 8 个。所以一批得凑到一万个左右，每个专家才吃得饱。<br>
字节的 MegaScale-Infer 论文在 A100 上算过同一笔账：一批至少 156 个请求，Mixtral 每个专家只分到 39 个。模型这边也在帮忙凑：MLA 把笔记压小，一张卡装得下更多请求，一批就大。<br>
一张卡哪来一万个请求？只能让很多张卡把自己的 token 都送到同一批专家那儿去。这就是 DEP、Wide-EP，还有下面这个 AFD，说到底都在凑这一批。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-afd</code>，先指左边的连线，再指右边三条道。</p>

<p class="say">「decode 这边还能再拆一层：attention 跟请求绑在一起，专家只要 token 够多。把两者放两组机器，叫 AFD，专家那边同时接好几组 attention 的 token，这一批就凑大了。」</p>

<p class="say">「代价是每层来回一趟，所以切成三个小批轮着跑。盯着 A 看：算 attention 一格，去半格，专家一格，回半格，第 3 格又轮到它，两边都填满了。」</p>

<p class="say">「为什么是三个小批？MegaScale-Infer 算过：要把一来一回的通信藏住，至少得三个，通信慢的时候要四个。它报告每张 GPU 的吞吐最高提升 1.9 倍。字节的 MegaScale-Infer、阶跃的 Step-3 都是这个路子。」</p>

<p class="say">「同样的思路还能往前推一段：多模态模型的视觉编码器单独部署，算好的向量再传给语言模型，跟 PD 连起来常写作 EPD。<br>
说到底，第五刀就一个意思：<b>性质不同的活，别挤在一起，拆开以后每一边挑自己的切法</b>。」</p>

<p class="say">「这一刀留下的问题：五刀都讲完了，可真到一个集群上，它们是同时存在的。<br>
谁放在一台机器里，谁跨机器？摆错了位置，前面每一刀省下来的，都会被网络吃回去。」</p>
""", """
⚠️ <b>被问「分块 prefill 不就解决了？」</b>：能缓解，但两种活还在抢同一批卡，也只能用同一套切法。<b>被问「什么时候不该拆？」</b>：请求都很短、量也不大时，多出来的传输和两套机器不一定划算。<br>
⚠️ KV 那一趟只讲中间那段怎么算（1.04 GB ÷ 12.5 GB/s），两头各「十毫秒左右」一带而过。<br>
⚠️ 动画里的格数是示意，不是实测时序；<b>「16 步出 11 个字」别当成实测数字说</b>。
"""),

# ── 七 ─────────────────────────────────────────────────────────────
("第七节　摆到机器上（6′）", "s七", "fig-freq", """
<p>🎯 <b>这一节留一句：说得勤的放快线，说得少的才走慢线；先选对切法，再调参数。</b></p>

<p class="board"><b>🖥 屏幕</b>：课件 <code>s七</code>，停在标题上。</p>

<p>💬 <b>互动</b>：<b>先别切到 fig-freq</b>（它的标题就是答案）。先问：「把集群想成一栋写字楼。TP 像几个人合写合同、每段都要对一遍；PP 像按章节递稿；DP 像另一个组下班前对一次账。谁必须坐同一张桌？」记下台下怎么分，讲完这一段再回来对。</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-freq</code>，整张，先指下半截。</p>

<p class="say">「同样的卡、同样的切法，只换一种摆法：一个走 17 格，一个走 73 格。<br>
一个集群里的线，不是一样快的。先看图下半截：GB300 一整柜 72 块卡之间是专用快线，出了这一柜只能走网卡，慢九倍。TPU 更悬殊：一个切片里走快线，跨切片要慢几十倍。<br>
再看上半截：刀也不是一样勤的。我们按一个示意配置，数了每一刀一步要说几次话：TP、EP、FSDP 每一层都要说，一步上千次；PP 十几次；DP 一步一次。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-office</code>，由里往外指三圈。</p>

<p class="say">「刚才投的票，对一下：合写合同的必须同桌，递稿子隔层楼也行，对账在别的城市都可以。<b>说得勤的坐里圈，说得少的去外圈</b>。<br>
光数次数还不够，还要看能不能边算边传、藏到计算后面。TP 藏不住：每一块末尾那次加总不做完，下一块就没法开始，所以它必须待在快线里。<br>
EP 和 FSDP 能提前发、藏住一部分，就有人让它们跨出去：V3 的专家并行横跨了 8 台机器，Llama 3 把 FSDP 放在了最外层。」</p>

<p class="say">「数据并行那一次量虽然大，也好办：反向从最后一层往前算，后面几层的梯度一算好就能先传。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>anim-meshmap</code>，一步一停：摆法一第 1 轮停住时指同色箭头「环在机器里」；摆法二第 1 轮停住时指红箭头「这两段过慢线，其中一段从外面绕回来」。</p>

<p class="say">「8 张卡、TP4 乘 DP2，同一个颜色就是一个 TP 组。<br>
摆法一，TP 组就在一台机器里，八轮来回都走快线，只有最后那一次 DP 过慢线。摆法二，TP 组横跨两台机器，一轮要等最慢那一段，所以每一轮都按慢线算。<br>
17 格对 73 格，慢四倍多。快慢一比九是示意，不是实测。」</p>

<p class="say">「换回第三节那把尺子看：每根线都有自己那条硬件线。线慢九倍，门槛就高九倍。隐藏维跟 V3 一样宽的模型，TP 8 路在 TPU 快线上刚好贴线，放到慢一个数量级的线上就远远掉下去了。<br>
所以 TP 一般不出一台 8 卡机器；GB300 把快线扩到整柜，专家并行才敢往大了开。」</p>

<p class="board"><b>🖥 屏幕</b>：<code>fig-topo</code>。</p>

<p class="say">「切法选错了，参数调得再细也没用。切专家时说 GB300 那笔细账留到这儿，现在算，按每张卡看。<br>
原始脚本每卡 1820。TP4 上能调的都调了，prefill 从一台加到三台，总数涨了四成五，可卡也翻了一倍，每卡反而掉到 1319。<br>
最后一行换的是 decode 的切法：换成 DEP8，卡也从 4 张变成 8 张。同样的并发，每卡 2758，两倍多。」</p>

<p class="say">「钱从哪来？切序列时说过，TP4 把笔记在四张卡上各存一份；换成 DEP8，笔记不再重复，专家也摊到 8 张卡上，省下的显存都变成了更大的 batch。<br>
这 8 张卡横跨两台机器，但还在同一柜的快线里。<b>先问切法对不对，再动参数</b>。」</p>

<p class="board"><b>🖥 屏幕</b>：滚到 7.4 那张对照表。</p>

<p class="say">「把前面几节串起来，挑并行像闯五关：装得下吗？快线那一圈多大，TP、EP 别超过它；序列长吗？还不够、或者非跨慢线不可，才上 PP；还剩卡，全给数据并行。」</p>

<p class="say">「表里只看两个「不用」。V3 在 8 卡一台的 H800 上不用 TP，专家并行横跨 8 台。<br>
腾讯的混元 3 在 TPU 上小规模试过专家并行，吞吐掉了七成，那次混着别的因素，原因还没查清；最后不用 EP，专家交给 FSDP 切。<br>
同样五步，走出来几乎没有重合，因为快线长得不一样。<b>别人家的配置不能照抄，先看自己的线</b>。」</p>

<p class="say">「到这里，每一刀切什么、多出什么话、该放哪根线，都齐了。下一节把它们摊在一张图上，那些 TEP8 之类的写法就读得懂了。」</p>
""", """
⛔ <b>7.5 的 strong ／ weak scaling 和 <code>fig-scale</code> 台上跳过</b>（2026-09-25 串讲后砍掉，省约 1 分钟，也不在接力线上），留给会后自学；被问到就说「加卡时连 batch 一起加，多出来的卡优先当副本」。<br>
⚠️ <b>别把「GPU 一跳就到、TPU 环面要绕路」接在混元的例子后面讲</b>：教材自己说 4 路时绕路这笔账不大，接在一起台下会听成原因。<br>
⚠️ GB300 那套的 KV 走的是柜内 NVLink，别指着它说「这就是走慢线」；「走慢线」说的是 TPU v7x 那套 PD 分离。<br>
⛔ <b>不要逐行读 7.4 那张对照表。</b><br>
<em>挑一格讲就够：混元 3 在 TPU 上<b>不用 EP</b>（单次小规模实测掉七成，原因没查清），V3 在 H800 上<b>不用 TP</b>（8 卡一台）。
两个「不用」放在一起，这张表的意思就出来了。</em><br>
⚠️ 动画里 17 格对 73 格是示意模型，<b>不是实测</b>。
"""),

# ── 八 ─────────────────────────────────────────────────────────────
("第八节　全景与收尾（2′）", "s八", "fig-panorama", """
<p>🎯 <b>这一节只留一句：名字再多，先问它切的是什么、多出哪种通信、该放哪根线。</b></p>

<p class="board"><b>🖥 屏幕</b>：课件 <code>s八</code>，放 8.1 那张全景图 <code>fig-panorama</code>，「折叠讲解」保持打开。<b>这一节只切这一次屏</b>，不要往下滚到表格。</p>

<p class="say">「最后看一眼全景，分四列：切数据、切序列、切模型、拆工作。<br>
今天是五刀，这里四列，因为切权重和切专家都算切模型，并成了一列。」</p>

<p class="say">「怎么读？拿 EP 这一格试一次。在哪一列？切模型，来自第三刀，切专家。<br>
多出来的是哪种话？AllToAll，第一节最后那一种。该放哪根线？看右上角的圆点：实心的敢走慢线，空心的待在快线里。EP 是空心。<br>
图上每个名字，都这样问三句。」</p>

<p>💬 <b>互动</b>：「现在你来读 PP。」等台下说出「切模型、一对一收发、实心，敢走慢线」。</p>

<p class="say">「图底下这行，是 TEP8、DEP16 这种推理写法：切专家时讲过，前面的字母说 attention 怎么切，后面的 EP 就是刚读的那一格，数字是一共几张卡。」</p>

<p class="say">「回到开场那条接力线：每一刀都在补前面没管到的那一块，可每一刀都一样，拿一种通信，换一份显存或者一份算力。」</p>

<p class="say">「再读一遍开场那个 V3：PP 16、EP 64、ZeRO-1 数据并行。切深、切专家、切数据；段边界收发、AllToAll、一步一次；敢跨机器、限 4 台、最便宜。开场像天书的一行配置，现在读得懂了。」</p>

<p class="say">「所以选并行策略，说到底是在选：<b>你愿意付哪一种通信，付多频繁，放在哪根线上</b>。<br>
型号会换，数字会过期，这一句不会。」</p>
""", """
⚠️ 被问「EP 为什么画成空心，V3 的 EP 不是横跨 8 台吗」：圆点画的是默认摆法，图的副标题写了有例外，第七节讲过。<br>
⛔ <b>不要逐行讲全景表。</b><br>
<em>它是会后查的地图。台上只示范一次「怎么读一行」就够。</em><br>
⚠️ 收尾那一句念完就停，别再补「还有什么问题」之外的话。
"""),
]


def _chapter(i, title, sid, fig, body, dont):
    figline = ("　·　主图 <code>%s</code>" % fig) if fig else \
              "　·　<b>本节无图</b>（故意的）"
    return """
<h3 class="sec" id="c%d">%s</h3>
<div class="say">
%s
<div class="warn"><span class="k">⚠️ 这一节最容易跑偏的地方</span>%s</div>
<p class="board sub">🖥 本节主屏：课件 <a href="%s#%s">%s</a>%s</p>
</div>
""" % (i, title, body, dont, DECK, sid, title.split("（")[0], figline)


PLAN = [("零　开场", 5, "不能", "⭐ 开场热身题（16 字节）必须做；只立一句话：用一种通信换一份显存或算力"),
        ("一　五种通信", 9, "不能", "⭐ 那个等式必须讲；1.7 的系数表不讲"),
        ("二　切数据", 9, "不能", "⭐ 前两级白送、最后一级付 50%"),
        ("三　切权重", 9, "不能", "TP 与 FSDP 各管一边；为什么先列后行要讲（GeLU 反例）；PP 只讲气泡"),
        ("四　切专家", 7, "不能", "⭐ 负载不均是 EP 最重的病"),
        ("五　切序列", 6, "不能", "⭐ KV 复制那张图一定要放"),
        ("六　切工作", 7, "不能", "PD 分离的厨房比方；AFD 点到即止"),
        ("七　摆到机器上", 6, "不能", "⭐ 说得勤的放快线；先选切法再调参"),
        ("八　全景与收尾", 2, "收尾不能砍", "只示范读一行；回到开场那一句")]
PLAN_SUM = sum(p[1] for p in PLAN)

html = ('''<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>专题五 · 讲义（授课稿）</title>
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>&#129408;</text></svg>">
<meta property="og:title" content="并行策略 · 讲义">
<meta name="theme-color" content="#5f6368">
''' + _css() + '''
<style>
.shell > main{grid-column:1 / -1}
.plan table td:first-child, .plan table th:first-child{white-space:nowrap}
p.say{background:#fffdf3;border-left:3px solid #f9ab00;padding:10px 14px;
      margin:12px 0;line-height:1.95}
</style>
</head>
<body>

<!-- ⛔ 这个文件由 Courses/tools/topic05-build-lecture.py 生成，不要手改。 -->

<div class="top"><div class="in">
  <span class="who">专题五 · <b>并行策略</b></span>
  <nav class="tabs">
    <a href="''' + DECK + '''">课件</a>
    <a href="topic-05-lecture.html" class="on">讲义</a>
    <a href="topic-04.html">专题四（那张 16 字节的账）</a>
    <a href="index.html">课程总纲</a>
  </nav>
</div></div>

<div class="shell">
<main>

<div class="plan">
  <h2>这份讲义怎么用</h2>
  <p><b>两个用法。</b><br>
    <b>① 上台讲</b>：照着念，台词、切图、每段几分钟都写着。<br>
    <b>② 自学</b>：把这一整份丢给 AI，你会得到一个既有全部背景、
    也知道该怎么讲的助教，让它带你走一遍。</p>
  <p>内容本身在<a href="''' + DECK + '''">课件</a>那边，这一份只管「怎么讲」。<br>
    课程作者 <b>Chris Yang</b> · Google Cloud AI Infra 架构师。</p>
  <table><tbody>
    <tr><td>🎯</td><td><b>这一节要留下什么</b>　讲完之后台下脑子里该剩的那一句。</td></tr>
    <tr><td>🗣</td><td><b>讲稿</b>　<em>黄底</em>的可以照着念。</td></tr>
    <tr><td>🖥</td><td><b>屏幕</b>　该滚到课件的哪张图、放哪段动画。
      <em>⭐ 这些提示挂了 <code>class="board"</code>，构建时逐条对账，
      指了不存在的图或小节号会当场报出来。</em></td></tr>
    <tr><td>⚠️</td><td><b>别讲什么</b>　这一节最容易跑偏的方向。</td></tr>
  </tbody></table>
</div>

<div class="plan">
  <h2>⏱ 时间：主线 ''' + str(PLAN_SUM) + '''′</h2>
  <p>⛔ <b>这一讲是一条接力线，不能跳。</b>
  <em>每节最后那句「这一刀留下的问题」就是下一节的开场，时间再紧也要念。
  要压时间，压每一节中间的例子，别压首尾。</em></p>
  <table>
<thead><tr><th>段落</th><th>分钟</th><th>能不能砍</th><th>说明</th></tr></thead><tbody>
''' + "".join("<tr><td>%s</td><td>%d</td><td>%s</td><td>%s</td></tr>\n" % p for p in PLAN) + '''<tr><td><b>合计</b></td><td><b>''' + str(PLAN_SUM) + '''</b></td><td></td><td>第九节（出处台账）不进课堂，被追问时翻</td></tr>
</tbody></table>
  <p style="margin-top:10px">⛔ <b>上面那张表是计划，下面这张是地板。</b>
  <em>「地板」＝ 把讲稿一字不落念完、再加切图的时间，一句现场发挥都没有，<b>也不含等动画转完一圈的时间</b>（全讲约 3 分钟）。每次 build 现数。</em></p>
@@FLOOR@@
</div>

<h3 class="sec">🧵 上台之前：把两条暗线记在心里</h3>
<div class="say">
  <p><b>① 每一刀都付一种通信。</b>
    <em>讲每一刀时都点一次名：它多出来的是第一节那五种话里的哪一种。讲到第七节，台下自己就能说出「那它该放快线还是慢线」。</em></p>
  <p><b>② 每一刀都在补前面没管到的那一块。</b>
    <em>batch 小了 FSDP 撑不住，补上 TP；V3 的参数几乎全在专家里，补上 EP；上下文一长一条样本放不下，补上 CP；prefill 和 decode 要的切法不一样，补上 PD 分离。
    ⛔ 一旦讲成「接下来介绍另一种并行」，这条线就断了。</em></p>
  <span class="pause">⭐ <b>如果只能让台下带走一句话</b>：
  <b>选并行策略，就是在选你愿意付哪一种通信、付多频繁、放在哪根线上。</b></span>
</div>

<h3 class="sec">🖥 投屏怎么放</h3>
<div class="say">
  <p>⭐ <b>这一讲有十几段动画，全是无声循环。</b>
    <em>放动画时<b>先闭嘴看一遍</b>，等它转完一圈再开口；边放边讲，台下眼睛和耳朵会打架。</em></p>
  <p>⭐ <b>课件右下角有个「折叠讲解」按钮（或按 T），投屏前按一下。</b>
    <em>不开的话，台下会低头读字，不听你讲。</em></p>
</div>
'''
        + "".join(_chapter(i, t, sid, fig, body, dont)
                  for i, (t, sid, fig, body, dont) in enumerate(CH))
        + '''
<h3 class="sec">🚧 这份讲义还薄的地方</h3>
<div class="say">
  <ul>
    <li>[ ] <b>没真讲过，时间表是估的。</b>第一次讲完按实际用时回来改。</li>
    <li>[ ] <b>没有课前题、也没有课后题。</b>
      <em>第七节末尾很适合问一句：「如果你手上是 8 卡一台的机器，V3 那套配置里哪一刀要改？」</em></li>
  </ul>
</div>

</main>
</div>
</body></html>''')

# ── 时间账：从成品 HTML 现数，别手写常数 ────────────────────────────
_SAY_RE = r'<p class="say[^"]*">(.*?)</p>'
_say = "".join(re.sub(r"<[^>]+>", "", m) for m in re.findall(_SAY_RE, html, re.S))
_n = len(re.findall(r"[一-鿿]", _say))
_cuts = len(re.findall(r'<p class="board">', html))
_tot = _n / 200.0 + _cuts * 0.25

_secs = [(m.start(), re.sub(r"<[^>]+>", "", m.group(1)))
         for m in re.finditer(r'<h3 class="sec" id="c\d+">(第[零一二三四五六七八九十]+节.*?)</h3>', html)]
assert len(_secs) == len(CH) == len(PLAN), "节数对不上：%d / %d / %d" % (len(_secs), len(CH), len(PLAN))
_secs.append((html.index("🚧 这份讲义还薄"), ""))
_rows, _floor, _over = [], 0.0, []
for _i in range(len(CH)):
    _t = html[_secs[_i][0]:_secs[_i + 1][0]]
    _cn = len(re.findall(r"[一-鿿]", "".join(re.sub(r"<[^>]+>", "", x)
                                             for x in re.findall(_SAY_RE, _t, re.S))))
    _cc = len(re.findall(r'<p class="board">', _t))
    _f = _cn / 200.0 + _cc * 0.25
    _floor += _f
    _plan = int(re.search(r"（(\d+)′", _secs[_i][1]).group(1))
    if _f > _plan:
        _over.append((_secs[_i][1].split("　")[0], _f, _plan))
    _rows.append("<tr><td>%s</td><td>%s</td><td>%d</td><td>%.1f′</td><td>%d′</td></tr>"
                 % (_secs[_i][1].split("（")[0], format(_cn, ","), _cc, _f, _plan))
html = html.replace("@@FLOOR@@",
    '<table><thead><tr><th>节</th><th>台词汉字</th><th>切图</th><th>地板</th><th>计划给的</th></tr></thead><tbody>'
    + "".join(_rows)
    + '<tr><td><b>合计</b></td><td colspan="2"></td><td><b>%.0f′</b></td><td><b>%d′</b></td></tr>'
    % (_floor, PLAN_SUM) + "</tbody></table>")

# ⭐ 两套计划时长（节标题里的、时间表里的）必须一致 —— 专题四踩过「各自自洽、互相对不上」。
_head_min = [int(m) for m in re.findall(r'<h3 class="sec" id="c\d+">第[零一二三四五六七八九十]+节[^（<]*（(\d+)′', html)]
assert _head_min == [p[1] for p in PLAN], ("节标题 %s 与时间表 %s 对不上"
                                            % (_head_min, [p[1] for p in PLAN]))
# ⭐ 2026-09-24 L19：「可压到 N」曾经五节都写得比地板还低 —— 压不到的承诺比不写更坑讲师。
for _p, _r in zip(PLAN, _rows):
    _m = re.match(r"可压到 (\d+)", _p[2])
    _fv = float(re.findall(r"<td>([\d.]+)′</td>", _r)[0])
    assert not _m or int(_m.group(1)) >= _fv, "%s 写「可压到 %s」但地板已经 %.1f′" % (_p[0], _m.group(1), _fv)
if _over:
    print("   ⚠️  地板超过计划的节：")
    for _nm, _fl, _pl in _over:
        print("       %-16s 地板 %5.1f′ > 给 %2d′" % (_nm, _fl, _pl))

# ⭐ 2026-09-25 对照构建手册补的两条（手册 09 · 3.3）：
# ① 课件封面「讲约 N 分钟」是手打的，跟这里的计划表是同一个量的两处写法 —— 断两处之间。
_chip = re.search(r"讲约 (\d+) 分钟", io.open(os.path.join(WEB, DECK), encoding="utf-8").read())
assert _chip and int(_chip.group(1)) == PLAN_SUM, \
    "课件封面写「讲约 %s 分钟」，讲义计划表合计 %d′ —— 改一处不改另一处" % (_chip and _chip.group(1), PLAN_SUM)
# ② 台词数字密度（从专题四搬来，只报告）：一串数从耳朵进去台下留不住。
#   判据：这个数是「内容」还是「内容的证据」？证据说「有人量过」就够。
# 已判定「数就是内容」的块（2026-09-25 判）：
#   · 之字形那段：3／7／11／15 格对 9／9／9／9，这几个数就是这一格要讲的全部
#   · GB300 那段：1820／1319／2758 每卡吞吐，是第七节的论据本身，台下跟着比大小
_JUDGED = ("「生成式模型还有个麻烦", "「切法选错了，参数调得再细也没用",
           "「为什么非得先竖着切")   # 2026-09-25：GeLU 反例，数字就是论证本身
_noisy = []
for _b in re.findall(r'<p class="say">(.*?)</p>', html, re.S):
    _t = re.sub(r"<[^>]+>", "", _b).replace("&nbsp;", " ")
    _c = len(re.findall(r"[一-鿿]", _t))
    if _c < 40:
        continue
    _d = len(re.findall(r"[0-9]", re.sub(r"[A-Za-z]+-?[0-9]+", "", _t)))   # 摘掉 TP4、DEP8 这类名字里的数字
    if _d * 100.0 / _c > 12.0 and not _t.strip().startswith(_JUDGED):
        _noisy.append("%.0f/百字  %s" % (_d * 100.0 / _c, re.sub(r"\s+", " ", _t)[:40]))
if _noisy:
    print("   ⚠️  台词数字偏密（只报告，判断是内容还是证据）：")
    for _x in _noisy:
        print("       " + _x)

# ⛔ 讲稿里报的 TiB/GiB 大数，教材页面上必须也有（讲稿是手打的，漂的一定是讲稿）。
_DECKP = os.path.join(WEB, DECK)
_deck = re.sub(r"\s+", "", io.open(_DECKP, encoding="utf-8").read())
_flat = re.sub(r"\s+", "", re.sub(r"<[^>]+>", "", html))
_BIG = re.findall(r"[0-9]+\.[0-9]+(?:TiB|GiB)", _flat)
_lost = sorted({b for b in _BIG if b not in _deck})
assert not _lost, "讲稿报了教材上没有的数：%s" % "、".join(_lost)

# ⛔ 讲稿里指的动画 id，课件里必须有（topic02-lint-cues 只对 fig- 开头的 id）。
_anims = set(re.findall(r"<code>(anim-[a-z0-9-]+)</code>", html))
_miss = sorted(a for a in _anims if ('id="%s"' % a) not in io.open(_DECKP, encoding="utf-8").read())
assert not _miss, "讲稿指了课件里没有的动画：%s" % "、".join(_miss)

# ⛔ 一块台词对应一个动作：不许超过 200 汉字。
_long = [(len(re.findall(r"[一-鿿]", re.sub(r"<[^>]+>", "", s))), re.sub(r"<[^>]+>", "", s)[:30])
         for s in re.findall(r'<p class="say">(.*?)</p>', html, re.S)]
_long = [x for x in _long if x[0] > 200]
assert not _long, "有台词块超过 200 汉字：%s" % _long

# ⛔ 台上提示不许漏进台词：say 块里每一个字都默认要出口。
_STAGE_ONLY = ("不要在这里", "不要展开讲", "整段跳过", "别念", "台上不用念",
               "时间紧可以", "这一格不要", "别在这里", "讲之前停", "先只放")
_leak = [(w, re.sub(r"<[^>]+>", "", t)[:40]) for t in re.findall(r'<p class="say">(.*?)</p>', html, re.S)
         for w in _STAGE_ONLY if w in t]
assert not _leak, "台上提示漏进台词了：%s" % _leak

# ⛔ 收尾语气只许出现在最后一节，否则会「谢两次幕」。
_CURTAIN = ("数字会过期", "念完就停")
_cstart = [m.start() for m in re.finditer(r'<h3 class="sec" id="c\d+"', html)]
_early = sorted({w for m in re.finditer(r'<p class="say">(.*?)</p>', html, re.S)
                 for w in _CURTAIN if w in m.group(1) and m.start() < _cstart[-1]})
assert not _early, "收尾语气出现在最后一节之前：%s" % "、".join(_early)

io.open(OUT, "w", encoding="utf-8").write(html)
print("ok  topic-05-lecture.html  %s 字符 · %d 节 · 台词 %s 字 ＋ %d 切图 ＝ 地板 %.0f′（计划 %d′）"
      % (format(os.path.getsize(OUT), ","), len(CH), format(_n, ","), _cuts, _tot, PLAN_SUM))

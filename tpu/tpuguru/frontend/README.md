# frontend — 静态页面

三个页面：**提交** / **报告** / **历史（含两次对比）**。

控件与问号文案**全部按 [../PARAMS.md](../PARAMS.md) 生成**，不要在代码里另写一份说明。

交互要点（详见 PARAMS.md 末尾）：
1. 「粘命令」与「填表单」双向同步
2. 预设按钮：`BF16 最优` / `FP8 生产` / `FP8 峰值`
3. 改任何控件实时跑 lint，🔴 fatal 直接挡住提交
4. 问号固定三段：干什么 / 改了会怎样 / 建议

# -*- coding: utf-8 -*-
"""专题二 · 外传 **L200** —— v6e 与扩散模型，精讲版。

════════════════════════════════════════════════════════════════
⭐⭐ L100 与 L200 的分工（这一段决定了下面每一段该写多长）
════════════════════════════════════════════════════════════════
L100（topic-02x.html）= **十五分钟，以图为主，几乎不写字。**
  ⚠️ 2026-09-09 起它换了定位：从「讲芯片」改成「讲我们做了什么」——
  砍掉显微镜四张，换进三阶段部署与十模型时间线。
  正文只做三件事：接上一张、点出这一张要看什么、交到下一张。
  它给的是**结论**：屋脊点 560、强度 75,600、短板被挡在瓶颈之外。

L200（本页）= **同一个故事，但每一步都摊开。** 具体多出四样东西：

  ① **推导链**    —— L100 只给「560」，L200 给出它是怎么除出来的、
                      以及**我们拿什么外部锚点验过它**
  ② **反例与边界** —— L100 的结论都是顺的，L200 必须给出它什么时候不成立
                      （Flux.2 的 text encoder、v6e 上 SparseCore 的成熟度…）
  ③ **中间量**    —— 每张图背后的那张表：FLOP 怎么拆的、字节怎么数的
  ④ **工程落地**  —— L100 完全没有的两节：§五 流水线的形状、§六 我们真的跑过

⛔ **L200 不是 L100 加水。** 判据很硬：
   **每加一段，问它是不是①②③④之一。都不是的，就是水，删掉。**

════════════════════════════════════════════════════════════════
⛔⛔ 2026-09-09：节号不再跟 L100 一致 ——&nbsp;因为 L100 换了定位
════════════════════════════════════════════════════════════════
原来的规矩是「§零–§四 两边指同一件事」，前提是 **L100 是 L200 的子集**。
现场把 L100 从「二十分钟讲芯片」改成「**十五分钟讲我们做了什么**」之后，
那个前提没了：L100 砍掉了显微镜那四张、补进了三阶段部署与十模型时间线，
**它现在是同一批材料的另一种剪法，不是这一版的前几节。**

  ⛔ 硬凑节号只会更糟 ——&nbsp;两页的 §二会指两件不同的事，而且不报错。
  ⭐ 改成**显式映射**：L100 的收尾放一张「本讲某节 → L200 某节」的对照表，
    读者拿着它跳，不依赖号相同。
    下面的自检也跟着改成「查那张表在不在」，不再查节号一一对应。

  ⭐ 判据：**两份文档的关系变了，先改描述关系的那条规则，再改文档** ——&nbsp;
    留着一条已经不成立的规则，比没有规则更坏：它会让人以为对齐还在。

════════════════════════════════════════════════════════════════
⭐ 图从 svg 文件读，跟 L100 共用同一批源 —— 不抄一份短版过来
════════════════════════════════════════════════════════════════
理由跟专题二 L200 那条一样：**同一个职责不要两个载体。**
改了 topic02x-fig-*.py，两页下次 build 都自动跟上。

  ⚠️ 但**图注是两页各写各的** ——&nbsp;这不违反上面那条：
    图注是「这一页此刻要你看什么」，两页的教学意图本来就不同。
    L100 的图注给结论，L200 的图注指向紧接着的那段推导。

⛔ BODY 里有 % 号，**只能用普通字符串**，不要在它上面做 %-格式化。
"""
import io
import os
import re

from topic02_family import nav          # 四页互跳导航，单一来源

HERE = os.path.dirname(os.path.abspath(__file__))
WEB = os.path.join(HERE, "..", "WebPages")
CSS_SRC = os.path.join(WEB, "topic-02-L300.html")
OUT = os.path.join(WEB, "topic-02x-L200.html")

_src = io.open(CSS_SRC, encoding="utf-8").read()


def _sub(text, pattern, repl, what):
    """替换 ＋ 断言真的替换了。
    ⛔ 不要退回裸 str.replace ——&nbsp;它匹配不上时**静默原样返回**。"""
    new, n = re.subn(pattern, repl, text, count=1)
    assert n == 1, "改不动 %s —— 模板变了？（模式：%s）" % (what, pattern)
    return new


head = _src[:_src.index("</style>") + len("</style>")]
head = _sub(head, r"<title>.*?</title>",
            "<title>专题二 · 外传 L200：v6e 与扩散模型（精讲）</title>", "<title>")
head = _sub(head, r'<meta property="og:title" content="[^"]*">',
            '<meta property="og:title" content="v6e 与扩散模型 · 精讲版：'
            '从屋脊点一路推到三阶段部署">', "og:title")
head = _sub(head, r'<meta property="og:description" content="[^"]*">',
            '<meta property="og:description" content="L100 给结论，这一版给推导链、'
            '反例和中间量，再多两节 L100 没有的工程落地：流水线为什么又胖又瘦，'
            '以及我们在这条路上真跑过的十个模型。">', "og:description")
head = _sub(head, r'<meta property="og:url" content="[^"]*">',
            '<meta property="og:url" content="https://gist.higcp.com/Courses/'
            'WebPages/topic-02x-L200.html">', "og:url")
head = re.sub(r'\s*<meta property="og:image"[^>]*>'
              r'(\s*<meta property="og:image:(width|height)"[^>]*>)*', "", head)
_probe = re.sub(r"/\*.*?\*/|<!--.*?-->", "", head, flags=re.S)
assert "TPU 与 GPU" not in _probe and "topic-02-L300" not in _probe, \
    "head 里还有专题二 L300 的残留"
# ⛔ 防回归：famnav 的样式住在 L300 的 <style> 里、由 topic02-port-microscope.py 注入。
#   那段被重写过一次、把样式冲掉过一次，而**页面照样构建、导航条只是变成裸链接** ——
#   ⭐ 不报错的退化必须用断言接住。
assert "nav.famnav" in head, \
    "切下来的 head 里没有 famnav 样式 —— 先跑 topic02-port-microscope.py"

head += """
<style>
ul + p, ol + p, ul + div.note, ol + div.note, table + p { margin-top: 14px }
h3 { margin:30px 0 8px; font-size:17px }
h4 { margin:18px 0 6px; font-size:15px }
/* ⭐ L200 的版面比 L100 紧一档：字多了，图之间不必留那么大的空。
   ⛔⛔ 只能写 margin-top / margin-bottom，**绝不能用 `margin: 26px 0` 这种简写**。
   简写会把 margin-left 一并设成 0，而 `figure.fbox`（特异度 0,1,1）跟
   `figure.fwide` 同级、又排在后面，于是压掉 .fwide 的 margin-left:50%，
   只剩 translateX(-50%) 生效 ——&nbsp;**整张宽图往左平移半个屏幕**。
   ⚠️ 这个坑 topic02-port-microscope.py 白纸黑字写过，L100 又原样踩了一遍。
   ⭐ 判据：**改 margin 一律写具体方向。** */
/* ⛔⛔ `.hero` 这个类是从**专题一**继承下来的，它自带两层装饰性伪元素：
     .hero:before  —— 一张「token 之旅」插图（opacity .5）
     .hero:after   —— 一层白色蒙版，`linear-gradient(95deg, …94% → …12%)`
   而专题一是把内容包在 `<div class="wrap">` 里的，靠 `.hero .wrap{z-index:2}` 浮在上面。
   ⭐ 外传的 header 直接放 h1/p，**没有那层 .wrap**，于是拿不到 z-index ——
     标题被压在两层装饰底下，被那层蒙版糊掉。
   ⛔ 而且蒙版是 95deg 的：左侧 94% 不透明、右侧只剩 12% ——&nbsp;
     **所以症状是「左半边特别糊、右半边还能看」**，一眼看去像配色问题，其实是层级问题。
   ⭐ 判据：**看到「同一行字左右清晰度不一样」，先去找方向性渐变，别去调颜色。**
     纯粹的对比度问题不会只糊一半。

   修法：这一页本来就不需要那张插图（它画的是专题一的 token 之旅，
   跟「两颗芯片」毫无关系，而且那几个彩点正好横穿标题）。
   两层一起关掉，只留下 .hero 本身那道蓝到白的底色；再给直接子元素兜一层 z-index。 */
header.hero::before,
header.hero::after { display: none }
header.hero > *    { position: relative; z-index: 2 }
figure.fbox { margin-top: 26px; margin-bottom: 26px }
/* ⭐⭐ 图按**原生宽 1400 封顶**，不再随视口放大。
   ⛔ 2026-09-09 实测：1900px 视口下每张图都被拉到 1.22 倍 ——&nbsp;
     后果有两层，第二层才是要命的：
       ① 页面白白长了两千多像素；
       ② **图里的字跟着放大 22%**，于是「图上字太多」这个观感有一半
          其实是「图上的字被放大了」——&nbsp;而版面 lint 查的是「字号有没有
          越过正文」，1.22 倍刚好没越过，所以它一直没报。
   ⭐ 判据：**SVG 是按某个宽度设计的，就别让它超过那个宽度** ——&nbsp;
     放大不会增加信息，只会让密度看起来更高。
   ⚠️ 只作用在 svg 上，**不碰 figure 自身的 margin** ——&nbsp;
     那个 margin 简写坑（会压掉 .fwide 的 margin-left:50%）见上面那段。 */
figure.fbox > svg { max-width: 1400px; margin-left: auto; margin-right: auto;
                    display: block }
</style>
"""


def fig(name, cap):
    """插一张图。SVG 是产物，源在 topic02x-fig-*.py。
    ⛔ 别在这儿改 SVG ——&nbsp;改了下次重跑就没了。"""
    p = os.path.join(HERE, name + ".svg")
    assert os.path.isfile(p), "找不到 %s.svg —— 先跑对应的 topic02x-fig-*.py" % name
    svg = io.open(p, encoding="utf-8").read().strip()
    return ('<figure class="fbox fwide" id="%s">%s\n<figcaption>%s</figcaption></figure>'
            % (name, svg, cap))


BODY = []
a = BODY.append

# ══════════════════════════════════════════════════════════════
# 开篇
# ══════════════════════════════════════════════════════════════
a("""
<header class="hero">
  __FAMNAV__
  <div class="kicker">专题二 · 外传　·　L200 精讲版</div>
  <h1>算力强，显存弱 —— 这样一颗芯片，该配什么样的活</h1>
  <p class="lede">TPU v6e 与扩散模型。<b>L100 那十五分钟给的是结论；这一版把每一步摊开</b> ——
    每个数怎么除出来的、拿什么锚点验过、什么时候不成立，
    以及 L100 只给了结论、这里给全过程的两件事：<b>流水线为什么又胖又瘦</b>，
    还有<b>我们在这条路上真跑过的十个模型</b>。</p>
  <div class="note info"><span class="t">怎么读这一页</span>
    <b>L100 是同一批材料的另一种剪法，不是这一版的前几节</b> ——
    它十五分钟，砍掉了显微镜那几张、换进了三阶段部署与十模型时间线。
    <br>两边的节号<b>不再一一对应</b>；L100 的收尾有一张
    「本讲某节 → 本页某节」的对照表，拿着它跳。
    <br>⛔ 全篇仍然<b>不做端到端 benchmark 对比</b>，理由在 §零 说清楚。
  </div>
</header>
""")

# ══════════════════════════════════════════════════════════════
# §零
# ══════════════════════════════════════════════════════════════
a("""
<section id="s零">
  <h2><span class="secno">零</span>这一讲只回答一个问题</h2>

  <p>专题二立过一条线：<b>算力 ÷ 显存带宽</b>。它的物理含义是
    <b>「每从显存搬一个字节，这台机器本来能算多少次」</b> ——
    一个纯粹由硬件规格决定的数，跟你跑什么模型无关。</p>

  <p>那一讲量了两颗：B200 是 312，TPU v7 是 313。<b>几乎一模一样。</b>
    当时的结论是「这一代旗舰的胃口都差不多」。</p>

  <p>这一讲把同一把尺子多量两颗。<b>其中一颗立刻破了那个「都一样」。</b></p>

  <h3>零点一　为什么这个数值得单独拿出来说</h3>

  <p>因为它是一条<b>分界线</b>，而分界线的用途只有一个：<b>判某一段计算落在哪一侧。</b></p>

  <p>把你要跑的那段计算也算出一个数 —— <b>算术强度</b>，即
    「这段计算每搬一个字节，实际算了多少次」。两个数一比：</p>

  <ul>
    <li><b>强度 &lt; 屋脊点</b> —— 算力在等数据。此时提升算力毫无意义，
      要么加带宽，要么想办法让每个字节多干点活。</li>
    <li><b>强度 &gt; 屋脊点</b> —— 数据管够，算力是唯一瓶颈。
      此时加带宽也毫无意义。</li>
  </ul>

  <p><b>就这么两句话。</b>这一讲剩下的全部内容，都是在给这两句话填具体的数。</p>

  <div class="note info"><span class="t">⛔ 先说清这一讲不做什么，以及为什么</span>
    <b>不比 benchmark，不谈谁跑得快。</b>屋脊点是<b>结构量</b> ——
    它只说「这台机器的胃口有多大」，不说「这顿饭多久吃完」。
    <br>⚠️ 而且这不是回避：<b>我们手上唯一一组同模型双平台实测（HunyuanVideo-1.5），
    本身就不支持「v6e 更快」这个结论。</b>
    与其挑一组好看的数，不如把这一讲的边界说死 ——
    <b>它讲的是结构匹配，不是快慢。</b>
    <br><em>扩散模型本身的原理（加噪、去噪、latent、VAE、DiT、CFG）在<b>专题十一</b>，
    那是一小时的深潜。这里只取那些结构事实<b>对硬件的后果</b>。</em>
  </div>
</section>
""")

# ══════════════════════════════════════════════════════════════
# §一
# ══════════════════════════════════════════════════════════════
a('<section id="s一">')
a('  <h2><span class="secno">一</span>同一把尺子，量四颗芯片</h2>')

a(fig("figx-1",
      "<b>图 X-1</b>　三颗旗舰挤在 295–313，<b>v6e 是 560</b>。"
      "<em>下面三小节分别回答：这个数怎么除出来的、我们凭什么信它、"
      "以及「算力强显存弱」在这四颗上具体差多少。</em>"))

a("""
  <h3>一点一　这个数是怎么除出来的</h3>

  <p>分子是<b>稠密 bf16 算力</b>，分母是<b>HBM 带宽</b>。两个都取官方规格表上的数，
    不取任何实测值 —— 这一点很重要，它保证了这条线<b>在跑任何东西之前就能画出来</b>。</p>

  <table>
    <thead><tr><th>芯片</th><th>bf16 稠密算力</th><th>HBM 带宽</th>
      <th>屋脊点<br><span class="dim">＝ 算力 ÷ 带宽</span></th><th>出处</th></tr></thead>
    <tbody>
      <tr><td><b>H100 SXM</b></td><td>989.5 TFLOPS</td><td>3.35 TB/s</td>
        <td><b>295</b></td><td>NVIDIA 官方数据表</td></tr>
      <tr><td><b>B200</b></td><td>—</td><td>—</td>
        <td><b>312</b></td><td>专题二已推导</td></tr>
      <tr><td><b>TPU v7</b></td><td>—</td><td>—</td>
        <td><b>313</b></td><td>专题二已推导</td></tr>
      <tr><td><b>TPU v6e</b></td><td>918 TFLOPS</td><td>1,638 GB/s</td>
        <td><b>560</b></td><td>Cloud TPU v6e 官方规格页</td></tr>
    </tbody>
  </table>

  <div class="note warn"><span class="t">⚠️ H100 那个 989.5 是算出来的，不是抄的</span>
    NVIDIA 的数据表上印的是 <b>BFLOAT16 Tensor Core：1,979 TFLOPS</b>，
    但那一行下面有一句小字：<b>with sparsity</b>（结构化稀疏，2:4）。
    <br><b>稀疏算力不能跟稠密算力比</b> —— 它要求权重里每四个有两个是零，
    而我们这里比的所有负载都不满足这个前提。
    <b>稠密值是它的一半：989.5。</b>
    <br>⭐ 这是这张表里唯一一处需要动脑的地方，也是最容易被抄错的地方 ——
    <b>网上大量「H100 有 1979 TFLOPS」的说法，都漏了那三个词。</b>
  </div>

  <h3>一点二　我们凭什么信这条公式 —— 拿一个已知答案复现它</h3>

  <p>公式再简单，也得验。<b>验的办法不是再推一遍，而是找一个别人已经算过的例子，
    看我们的算法能不能把他的答案原样复现出来。</b></p>

  <p>用的是 Google 那本公开的《How to Scale Your Model》。它在讲屋顶线的那一章里，
    对 <b>TPU v5p</b> 给出了自己的屋脊点。我们把 v5p 的官方算力和带宽代进
    「算力 ÷ 带宽」，<b>得到的数跟它印出来的一致。</b></p>

  <div class="note ok"><span class="t">⭐ 这一步为什么不能省</span>
    我们要用这条公式去给 v6e 和 v7 下判断，而<b>那两个数没有人可以对照</b>。
    先在一个<b>有标准答案的输入</b>上跑通，再拿它去算没有答案的输入 ——
    这样万一 v6e 那个 560 错了，错也只可能错在<b>输入的规格数</b>上，
    <b>不会错在公式本身</b>。
    <br><em>整个错误空间被砍掉了一半，代价是十分钟。</em>
  </div>

  <h3>一点三　「算力强、显存弱」具体是多强、多弱</h3>

  <p>把 v6e 跟 H100 逐项相除，三个比值：</p>

  <table>
    <thead><tr><th>比什么</th><th>v6e</th><th>H100 SXM</th>
      <th>v6e ÷ H100</th><th>这一项算强还是弱</th></tr></thead>
    <tbody>
      <tr><td>bf16 稠密算力</td><td>918 TFLOPS</td><td>989.5 TFLOPS</td>
        <td><b>93%</b></td><td><b>基本打平</b></td></tr>
      <tr><td>HBM 带宽</td><td>1,638 GB/s</td><td>3.35 TB/s</td>
        <td><b>49%</b></td><td>只有一半</td></tr>
      <tr><td>HBM 容量</td><td>32 GB</td><td>80 GB</td>
        <td><b>40%</b></td><td>不到一半</td></tr>
    </tbody>
  </table>

  <p>三项里两项是短板，只有一项打平。<b>所以「v6e 合适」这句话，
    从这里开始就注定只能是有条件的</b> —— 条件就是那两个短板得<b>不参与</b>。</p>

  <p class="landing">⭐ <b>屋脊点 560 其实就是这三个比值的一句话总结：</b>
    分子基本没变，分母砍了一半，商自然涨到约两倍。
    <br><em>560 ÷ 295 ＝ 1.90 —— 这个 1.9 倍，跟带宽那栏的 49% 是同一件事的两种说法。</em></p>
</section>
""")

# ══════════════════════════════════════════════════════════════
# §二
# ══════════════════════════════════════════════════════════════
a('<section id="s二">')
a('  <h2><span class="secno">二</span>把两颗芯片拆开</h2>')

a('  <p>上一节的三个比值是<b>结果</b>。这一节看它们是从什么样的硅片布局里长出来的 ——'
  '<b>两颗芯片在同样的三个问题上，做了完全相反的选择。</b></p>')

a('  <h3>二点一　v6e：算力集中在两个大方阵里</h3>')
a(fig("figx-2",
      "<b>图 X-2</b>　一颗 v6e：<b>1 颗 ＝ 1 个核 ＝ 1 个 device</b>，"
      "两个 256×256 的 MXU，128 MiB 片上暂存，片外那道门 1,638 GB/s。"
      "<em>⭐ 注意右栏那三条看着像减配的规格 —— 二点四节会解释它们为什么是配套的。</em>"))

a("""
  <p>三件事值得记住：</p>
  <ol>
    <li><b>算力集中。</b>整颗芯片的稠密算力就装在两个 256×256 的方阵里。
      <b>要喂饱它，你的矩阵得足够大</b> —— 小矩阵会让方阵大半空转。</li>
    <li><b>片上有一整块暂存。</b>128 MiB 的 VMEM，比 H100 一个 SM 的
      共享内存大三个数量级。<b>但它全部由编译器安排</b>，没有硬件缓存兜底。</li>
    <li><b>通往片外的管子窄。</b>1,638 GB/s，不到 H100 的一半 ——
      这就是上一节那个 49% 的物理来源。</li>
  </ol>

  <div class="note info"><span class="t">⭐ 这一代没有「容量除以 2」那个坑</span>
    专题二反复强调过：TPU v7 上 <b>1 颗芯片 ＝ 2 个 device</b>，
    所以框架日志里按 device 报的数，换算成每芯片要乘 2、看容量要除 2。
    <br><b>v6e 是 1:1</b> —— 一颗芯片就是一个 TensorCore 就是一个 device。
    <em>跨代沿用那条换算规则会算错，这是本讲需要单独提醒的一处。</em>
  </div>
""")

a('  <h3>二点二　H100：同样的画法，三个相反的选择</h3>')
a('  <p>下面这张图<b>刻意用了跟 X-2 完全一样的画法和比例尺</b> ——'
  '尤其是那道「门」的宽度，两张图是按带宽等比画的，<b>可以直接叠着看。</b></p>')
a(fig("figx-3",
      "<b>图 X-3</b>　一颗 H100：算力摊成 <b>528 个</b>小单元（132 个 SM，每个 4 个），"
      "片上多了一整层<b>硬件自动管</b>的 L2，门宽一倍。"
      "<em>⭐ 门的宽度按带宽等比 —— 跟 X-2 直接可比。</em>"))

a("""
  <table>
    <thead><tr><th>同一个问题</th><th>TPU v6e 的答</th><th>H100 的答</th>
      <th>这个选择在防什么</th></tr></thead>
    <tbody>
      <tr><td><b>算力怎么摆</b></td>
        <td>集中成 2 个 256×256 方阵</td>
        <td>摊成 528 个小单元</td>
        <td>摊开是为了<b>不管来什么形状都有人能接</b>；
          集中是为了<b>大矩阵上把利用率吃满</b></td></tr>
      <tr><td><b>片上谁做主</b></td>
        <td>128 MiB 全归编译器，<b>CMEM ＝ 0</b></td>
        <td>50 MB L2 由<b>硬件自动</b>管，另有每 SM 256 KB</td>
        <td>硬件自动管是为了<b>应付「我不知道你要跑什么」</b>；
          交给编译器是为了<b>吃透「我早就知道你要跑什么」</b></td></tr>
      <tr><td><b>门开多宽</b></td>
        <td>1,638 GB/s</td>
        <td>3.35 TB/s</td>
        <td>门宽一倍，代价是屋脊点低一半 —— <b>它挡的是「强度不够」的活</b></td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐⭐ 两颗芯片各自都是自洽的</span>
    这三行不是「谁做得好谁做得差」，是<b>同一道题的两个解，各自配套</b>。
    <br>H100 那一列全部指向<b>「负载未知」</b>：摊开、缓存兜底、门开大 ——
    <b>三个都是为不确定性买的保险</b>。
    <br>v6e 那一列全部指向<b>「负载已知」</b>：集中、编译期写死、门只开够用 ——
    <b>三个都是把保险费省下来换算力密度</b>。
    <br>——&nbsp;<b>所以问题从来不是谁更强，是你手上的活属于哪一种。</b>
  </div>

  <div class="note warn"><span class="t">⚠️ 那个 CMEM ＝ 0 是有代价的，别只看它省下的</span>
    没有硬件缓存兜底，意味着<b>编译器排错了就没有后手</b>。
    形状是动态的、访存模式在运行时才知道的负载，在这套设计上会很难受。
    <br><b>这条恰好是 §三要用的：扩散模型为什么正好把这个前提喂得满满的。</b>
  </div>
""")

a('  <h3>二点三　一颗装得下吗 —— 那三条「减配」其实是配套的</h3>')
a('  <p>X-2 右栏有三条规格看着像减配：<b>只有 4 个 ICI 口、二维环面、'
  '一个 Pod 只有 256 颗</b>（对照 v7 是 6 口、三维、9,216 颗）。'
  '<b>把模型的体积摆出来，这三条立刻说得通。</b></p>')
a(fig("figx-6",
      "<b>图 X-6</b>　上半权重体积 ＋ 实测配置，<b>下半是同一颗芯片上的真实预算</b>。"
      "<em>⛔ 右列原来写的是「1 颗 / ≥2 颗」——&#160;那是拿体积 ÷ 32 GB 推的。"
      "换成实测之后立刻看出两处会猜错的地方（见图下第一条落点带）。</em>"))

a("""
  <p>逻辑很直接：<b>v7 那套三维环面、九千多颗的规格，是为「一个模型摊在几千颗上」
    准备的。而扩散这一族根本不打那场仗。</b></p>
  <p>不打，就<b>不用付那个成本</b> —— 少两个 ICI 口、少一个维度、Pod 小一个数量级，
    省下来的面积和功耗，全都还给了算力密度。<b>这三条不是减配，是不同的题面。</b></p>

  <div class="note bad"><span class="t">⛔⛔ 「装得下」和「该用几颗」是两个不同的问题</span>
    这张图右列以前是<b>拿权重体积除以 32 GB 推出来的颗数</b>。换成实测之后，
    两处猜错立刻显形：
    <br><b>① S3Diff 只有 6.6 GB</b>，按体积「一颗绰绰有余」——&nbsp;对，但重点不在这。
    <b>我们把它摊到 8 卡做张量并行，实测反而更慢</b>（5.46 s 对 5.28 s），预热还长 15 倍。
    <br><b>② Wan2.1 的 28 GB</b> 按体积「贴边能塞一颗」——&nbsp;
    <b>而实测从来没人这么跑</b>，它是 v6e-8 上 dp=1、tp=8 摊开跑的。
    <br>⭐ <b>前者看体积就能答，后者只能实测。</b>
    <br>⛔⛔ 更要紧的是第三条：<b>「28 GB &lt; 32 GB 所以能跑」分子分母都错。</b>
    <b>分母</b>：32 GB 是标称不是预算 —— 我们那次 OOM 时 XLA 报的是只剩
    <b>13.10 GB</b>。<b>分子</b>：要放的不只是权重，还有<b>峰值激活</b>，
    而扩散这一族<b>激活比权重大</b> —— CogVideoX-5B 权重 10 GB，
    它的 VAE 解码一步却要 <b>19 GB</b>。
    <br>⭐ 激活随分辨率与帧数涨，<b>权重一个字节不涨</b>：Wan2.1 的 480P 跑得动、
    720P OOM，用的是同一份权重。<b>权重决定装不装得进，激活决定跑不跑得动。</b>
  </div>
""")

a('  <h3>二点四　X-2 右栏那两样只标了存在的：SparseCore 与二维环面</h3>')
a(fig("figx-7",
      "<b>图 X-7</b>　SparseCore <b>有两个职责</b>：① 稀疏 / 大表（推荐模型那类）；"
      "<b>② 集合通信卸载 —— 把 all-reduce、all-gather 从 TensorCore 手里接过去，"
      "不占 MXU，跟计算真正并行。</b>"
      "<em>⛔ 这张图推翻重写过一次，图上原样保留了错在哪 —— "
      "那个错法本身值得讲给学员听。</em>"))

a("""
  <div class="note bad"><span class="t">⛔ 这个错法值得单独讲一遍</span>
    初版这张图写的是「扩散模型用不到 SparseCore，它是给推荐系统那类负载准备的」。
    <b>那是错的。</b>
    <br>错误的来源：架构文档里写着
    <b>「a primary use case is accelerating recommendation models」</b>，
    我<b>把「主用途」当成了「用途的全集」</b>。
    <br>⭐ 判据：<b>「a primary use case」这种措辞本身就在告诉你「还有别的」</b> ——
    它是一个明确的<b>不完全枚举</b>信号，而我把它读成了定义。
    <br>⚠️ 更该警惕的是：<b>我当时还给这个错误配了一条像模像样的推导链</b>
    （扩散没有大 embedding 表 → 没有稀疏聚合 → 它基本闲着）。
    <b>链子每一环都对，前提漏了一半，于是整条链推向了错的地方。</b>
    <br><em>——&nbsp;这是这门课里最该带走的一种自我怀疑：
    <b>推理顺畅从来不是前提完整的证据。</b></em>
  </div>

  <p>更正后的结论整个反过来：<b>只要跨卡，SparseCore 就在干活。</b>
    扩散确实没有大 embedding 表，所以它走的不是第 ① 条路；
    但只要模型要切开，<b>all-gather 和 reduce-scatter 就一大堆</b> —— 第 ② 条路它走得很勤。</p>

  <div class="note warn"><span class="t">⚠️ 口径：这批材料主要是第四代 SparseCore 的语境</span>
    「集合通信卸载」那批官方材料以 <b>v7x / 第四代 SparseCore</b> 为主，
    而 <b>Trillium 是第三代</b>。<b>v6e 上这条路的成熟度我们没有实测。</b>
    <br><em>如实标出来，不含糊过去 —— 这一条在图上也写着。</em>
  </div>
""")

a(fig("figx-8",
      "<b>图 X-8</b>　4 个 ICI 口连上下左右，边缘绕回成二维环面，16×16 ＝ 256 颗，"
      "<b>最远 16 跳</b>（对照 v7 的 4×4×4 是 6 跳）。"
      "<em>走不走这条路取决于你切不切模型 —— <b>v6e 的互联是「够扩散这一族用」"
      "的档位，不是「不用」。</b></em>"))

a('</section>')

# ══════════════════════════════════════════════════════════════
# §三
# ══════════════════════════════════════════════════════════════
a('<section id="s三">')
a('  <h2><span class="secno">三</span>那么扩散模型是哪一种活</h2>')

a('  <p>要判它落在轴的哪一侧，得先算出它的<b>算术强度</b>。'
  '而算强度之前，得先回答一个更基础的问题：<b>这类模型为什么是「计算密集」的？</b>'
  '——&nbsp;拿 Wan2.1-T2V-14B 的官方配置当场算一遍，一步都不跳。</p>')

a(fig("figx-9",
      "<b>图 X-9</b>　三步：像素 → latent → token，然后拆 FLOP。"
      "<em>下面三小节把图上这三列各自的算式摊开，"
      "并给出<b>一个能证伪整套公式的外部锚点</b>。</em>"))

a("""
  <h3>三点一　第一步：一段视频到底有多少个 token</h3>

  <p>输入是 <b>1280×720、81 帧</b>。它要过两道压缩才进 Transformer：</p>

  <table>
    <thead><tr><th>这一步</th><th>做了什么</th><th>形状变成</th><th>依据</th></tr></thead>
    <tbody>
      <tr><td>原始像素</td><td>—</td><td>81 × 720 × 1280 × 3</td><td>输入参数</td></tr>
      <tr><td><b>VAE 压缩</b></td>
        <td>时间 ÷4、空间 ÷8×8，通道 3 → 16</td>
        <td>16 × <b>21</b> × <b>90</b> × <b>160</b></td>
        <td><code>vae_stride = (4, 8, 8)</code></td></tr>
      <tr><td><b>Patch 化</b></td>
        <td>空间上每 2×2 合成一个 token</td>
        <td>21 × <b>45</b> × <b>80</b></td>
        <td><code>patch_size = (1, 2, 2)</code></td></tr>
      <tr><td><b>序列长度 N</b></td><td>21 × 45 × 80</td>
        <td colspan="2"><b>＝ 75,600 个 token</b>，每个 <code>dim = 5120</code> 维</td></tr>
    </tbody>
  </table>

  <p>对照一下：<b>一次 LLM 解码，一步只处理 1 个 token。</b>差了将近五个数量级 ——
    而这个差距，就是后面所有结论的源头。</p>

  <h3>三点二　第二步：这 FLOP 是怎么花掉的</h3>

  <p>一层 DiT、一个去噪步，四项开销。<b>N ＝ 75,600，d ＝ 5,120，FFN 中间层 13,824。</b></p>

  <table>
    <thead><tr><th>这一项</th><th>算式</th><th>FLOP</th><th>占比</th></tr></thead>
    <tbody>
      <tr><td><b>注意力（QKᵀ 与 AV）</b></td><td>4 · N² · d</td>
        <td><b>1.17 × 10¹⁴</b></td><td><b>72%</b></td></tr>
      <tr><td>FFN</td><td>4 · N · d · 13,824</td>
        <td>2.14 × 10¹³</td><td>13%</td></tr>
      <tr><td>自注意力的 q/k/v/o 投影</td><td>8 · N · d²</td>
        <td>1.59 × 10¹³</td><td>10%</td></tr>
      <tr><td>交叉注意力（文本只有 512 个 token）</td><td>—</td>
        <td>8.77 × 10¹²</td><td>5%</td></tr>
      <tr><td colspan="2"><b>每层每步合计</b></td>
        <td><b>1.63 × 10¹⁴</b></td><td>100%</td></tr>
      <tr><td colspan="2">× 40 层 × 50 步 ＝ <b>生成一段视频</b></td>
        <td colspan="2"><b>3.26 × 10¹⁷ FLOP</b></td></tr>
    </tbody>
  </table>

  <p>七成花在那个 <b>N²</b> 项上。<b>而 N² 意味着：token 数翻倍，这一项翻四倍。</b>
    上一小节那个 75,600 之所以是源头，原因就在这个平方上。</p>

  <div class="note info"><span class="t">这里可以顺手回答「一颗 v6e 要跑多久」</span>
    3.26 × 10¹⁷ ÷ 918 × 10¹² ＝ <b>355 秒，约 5.9 分钟</b>。
    <br>⛔ 但这是<b>算术，不是性能预测</b> —— 它假设算力 100% 吃满，而这做不到（见三点五）。
    <b>它的正确用法是当下界：任何低于 5.9 分钟的宣称，一定有别的事情发生了</b>
    （降步数、降分辨率、缓存复用、稀疏化）。
  </div>

  <h3>三点三　那一行 config 才是分水岭</h3>

  <p>上面那个「注意力占七成」<b>完全取决于一个字段</b>：</p>

  <pre><code>window_size = (-1, -1)   # ← 不开滑窗，全局注意力</code></pre>

  <p>如果它不是 −1，而是一个有限的窗口 <b>w</b>，注意力就从 <b>4·N²·d</b> 掉成
    <b>4·N·w·d</b> —— 从平方变成线性。整套结论会怎么变：</p>

  <table>
    <thead><tr><th>假如窗口是</th><th>注意力 FLOP</th><th>注意力占比</th>
      <th>每层总量</th><th>总算力变成原来的</th></tr></thead>
    <tbody>
      <tr><td><b>−1（实际情况）</b></td><td><b>1.17 × 10¹⁴</b></td><td><b>72%</b></td>
        <td><b>1.63 × 10¹⁴</b></td><td><b>1 ×</b></td></tr>
      <tr><td>8,192</td><td>1.27 × 10¹³</td><td>22%</td>
        <td>5.87 × 10¹³</td><td>1 / 2.8</td></tr>
      <tr><td>4,096</td><td>6.34 × 10¹²</td><td>12%</td>
        <td>5.24 × 10¹³</td><td>1 / 3.1</td></tr>
      <tr><td>2,048</td><td>3.17 × 10¹²</td><td>6%</td>
        <td>4.92 × 10¹³</td><td>1 / 3.3</td></tr>
    </tbody>
  </table>

  <div class="note bad"><span class="t">⛔ 所以「视频模型都很重」这句话不能当依据</span>
    开了滑窗，注意力从占七成掉到占一成，<b>整个「计算密集」的结论也就跟着塌了</b>。
    <br>⭐ 判据：<b>这个字段必须去 config 里看，不能凭直觉推。</b>
    同一族模型里，开不开窗口是设计者逐个模型做的选择 ——
    <b>它不是「视频模型」这个类别的属性。</b>
  </div>

  <h3>三点四　凭什么信这一整套算式 —— 一个能证伪它的锚点</h3>

  <p>上面那张 FLOP 表里，任何一项的系数写错 2 倍，结果看起来都一样合理。
    <b>所以必须找一个独立的、有标准答案的量来验。</b></p>

  <p>用参数量。同一份 config，把一层的权重数出来：</p>

  <table>
    <thead><tr><th>一层里有什么</th><th>参数量</th></tr></thead>
    <tbody>
      <tr><td>自注意力 q/k/v/o：4 · d²</td><td>104.9 M</td></tr>
      <tr><td>交叉注意力 q/k/v/o：4 · d²</td><td>104.9 M</td></tr>
      <tr><td>FFN：2 · d · 13,824</td><td>141.6 M</td></tr>
      <tr><td><b>每层合计</b></td><td><b>351.3 M</b></td></tr>
      <tr><td><b>× 40 层</b></td>
        <td><b>14.05 B　←　官方标称「14B」</b></td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐⭐ 这一步为什么不能省 —— 它当场抓到过一个错</span>
    参数量能对上，说明我们对<b>「这一层里到底有哪些矩阵」</b>的理解是对的；
    而 FLOP 表用的是同一批矩阵。<b>形状对了，系数才有意义。</b>
    <br>⭐ 写这一节时，我另起了一个脚本复算，得到的却是 <b>9.86 B</b>，对不上。
    查下去发现：<b>那个脚本漏掉了交叉注意力的四个投影矩阵</b>（少了 104.9 M/层）。
    ——&nbsp;<b>是锚点抓到了它，不是我看出来的。</b>
    <br><em>如果当初没设这个锚点，那个脚本会安安静静地给出一套自洽但错误的数。
    <b>孤立的、对不上任何外部量的数字，才是最该害怕的。</b></em>
  </div>

  <h3>三点五　⚠️ 强度够高 ≠ 算力自动吃满</h3>

  <p>到这里可以算强度了：这段计算的算术强度<b>约等于 N ＝ 75,600</b>，
    是 v6e 那条 560 的 <b>135 倍</b>。<b>带宽完全不是瓶颈。</b></p>

  <p>但落在算力侧，<b>不等于算力就用满了</b>。
    ⛔ <b>这里的口径要说准</b>（2026-09-09 审计改）：上面整段算的是
    <b>Wan2.1</b>，而下面这个 MFU 出自我们 <b>Wan2.2</b> 的 profile 分析
    （<code>tpu/Wan2.2/docs/wan_tpu_optimization_guide.md</code>），
    <b>而且它是单个算子的数，不是整模型的</b>：</p>

  <div class="tw"><table>
    <thead><tr><th style="width:290px">口径</th><th style="width:110px">MFU</th>
      <th>怎么来的</th></tr></thead>
    <tbody>
      <tr><td><b>Splash Attention 这一个算子</b></td><td><b>37%</b></td>
        <td>roofline 15.974 ms ÷ 实测 43.93 ms ＝ 36.4%</td></tr>
      <tr><td><b>整模型（优化后）</b></td><td><b>34%</b></td>
        <td>Xprof 总览；<b>基线是 12%</b></td></tr>
    </tbody></table></div>

  <p>两个数都说明同一件事 —— <b>六成多的算力没吃到</b>。原因不在带宽，在别处：</p>

  <div class="note warn"><span class="t">⚠️ head_dim ＝ 128，而 MXU 是 256×256</span>
    注意力是<b>按头</b>算的，每个头的维度是 128。而 v6e 的 MXU 收缩阵列是
    <b>256×256</b> —— 送进去的 K 维只有 128，<b>方阵一半的位置是空的</b>。
    <br>⭐ 这正是 §二点一那个「算力集中」的另一面：
    <b>集中成大方阵，好处是大矩阵上利用率高，代价是喂不满时浪费也成块</b>。
    <br><em>——&nbsp;屋顶线告诉你<b>瓶颈在哪一侧</b>，它不告诉你<b>那一侧用得好不好</b>。
    这是两个问题，L100 只讲了第一个。</em>
  </div>

  <h3>三点六　结构上的三条：它把编译期想知道的全提前说了</h3>

  <p>三点一到三点五讲的是<b>算术上</b>的匹配。还有<b>结构上</b>的一半 ——
    而这一半恰好对上 §二点二那个「CMEM ＝ 0」的设计前提。</p>

  <a-fig-5/>

  <div class="note ok"><span class="t">⭐ 两层匹配叠在一起，才是「合适」的全部含义</span>
    <b>算术上</b>：强度 75,600 对门槛 560，窄带宽这个短板没成为瓶颈（三点五）。
    <br><b>结构上</b>：形状全静态、没有历史要养、同一段跑五十遍 ——
    编译优先这套打法拿到了它最想要的输入（这一小节）。
    <br><em>缺任何一半，「合适」都不成立。</em>
  </div>
""".replace("<a-fig-5/>", fig(
    "figx-5",
    "<b>图 X-5</b>　没有 KV cache、形状从头到尾不变、同一段计算原样跑五十遍。"
    "<em>⭐ 对照左栏的自回归 LLM：它每一步 KV 都更长，形状步步在变 ——"
    "<b>编译好的那一份，下一步就不合用了。</b></em>")))
a('</section>')

# ══════════════════════════════════════════════════════════════
# §四
# ══════════════════════════════════════════════════════════════
a('<section id="s四">')
a('  <h2><span class="secno">四</span>把活放回那两条线上</h2>')

a("""
  <p>前面每算一种负载都要拆一遍 FLOP，太慢。<b>其实有一条心算规则，
    三十秒就能把任何一种负载放到轴上。</b></p>

  <h3>四点一　一条能心算的规则：强度 ≈ 同一份权重被多少个「位置」共用</h3>

  <p>推导只要三行。看一个权重矩阵 <b>W</b>，形状 in × out，bf16 存储：</p>

  <ul>
    <li><b>要搬的字节</b>：每个参数 2 字节 → <b>2 · in · out</b></li>
    <li><b>能算的次数</b>：batch 里有 B 个位置，每个位置每参数 2 次浮点
      （一乘一加）→ <b>2 · B · in · out</b></li>
    <li><b>相除</b>：<b>强度 ＝ B</b></li>
  </ul>

  <p class="landing">⭐ <b>就这么干净：强度直接等于「有多少个位置在共用这一次权重搬运」。</b>
    <br><em>它跟模型多大、多少层、什么架构<b>全都无关</b> —— 那些量在分子分母上同时出现，约掉了。</em></p>

  <h3>四点二　于是四类负载各自落在哪</h3>
""")

a(fig("figx-4",
      "<b>图 X-4</b>　同一根轴，四类负载。"
      "<em>⭐ 位置在轴上的左右，完全由上一小节那个 B 决定 —— 不需要跑就能画。</em>"))

a("""
  <table>
    <thead><tr><th>负载</th><th>「位置」是什么</th><th>典型强度</th>
      <th>对 v6e 的 560</th><th>瓶颈在哪</th></tr></thead>
    <tbody>
      <tr><td><b>decode，batch = 1</b></td><td>1 个 token</td><td><b>≈ 1</b></td>
        <td>差 560 倍</td><td><b>带宽</b>（算力几乎全空转）</td></tr>
      <tr><td>decode，batch = 64</td><td>64 个 token</td><td>≈ 64</td>
        <td>还差 8.75 倍</td><td>带宽</td></tr>
      <tr><td><b>prefill，8K 上下文</b></td><td>8,192 个 token 一次过</td><td>≈ 8,192</td>
        <td><b>超 14.6 倍</b></td><td><b>算力</b></td></tr>
      <tr><td><b>扩散一步</b></td><td>75,600 个 token 一起动</td><td><b>≈ 75,600</b></td>
        <td><b>超 135 倍</b></td><td><b>算力</b></td></tr>
    </tbody>
  </table>

  <div class="note info"><span class="t">⭐ 这张表能直接读出一条采购判据</span>
    要让 v6e 的算力<b>不空转</b>，decode 的 batch 得堆到 <b>560</b>；
    同一件事在 H100 上只要 <b>295</b>。
    <br>——&nbsp;<b>「屋脊点高」翻译成运维语言，就是「凑批的压力大一倍」。</b>
    <em>凑不到那个批，买来的算力就是在等数据。</em>
  </div>

  <div class="note ok"><span class="t">⭐⭐ 「v6e 适合扩散」的精确说法</span>
    <b>不是它跑扩散更快。</b>是扩散把它的短板<b>挡在了瓶颈之外</b> ——&nbsp;
    强度 75,600 对门槛 560，那根窄管子根本没参与；
    <b>于是只剩下没被挡住的那一项在起作用：算力 918 对 989.5，是 H100 的 93%。</b>
    <br><em>短板不参与，长板打平 —— 这就是「合适」的全部含义。</em>
  </div>

  <div class="note bad"><span class="t">⛔ 反过来同样成立，而且更该记住</span>
    <b>小 batch 的 decode 落在轴的最左边</b>，那里比的全是带宽 ——
    而 v6e 只有 H100 的 <b>49%</b>。
    <b>同一颗芯片，在那种活上就是最吃亏的那颗。</b>
    <br><em>这不是缺点，是同一个设计选择的另一面：
    §二点二那三个「相反的选择」，在这里一次性结清账单。</em>
  </div>
""")
a('</section>')

# ══════════════════════════════════════════════════════════════
# §五 —— L100 里没有这一节
# ══════════════════════════════════════════════════════════════
a('<section id="s五">')
a('  <h2><span class="secno">五</span>流水线的形状：又胖又瘦</h2>')

a("""
  <p>到 §四为止，这门课一直把「跑一次扩散」当成<b>一件事</b>。
    实际部署时它是<b>三件事</b>：文本编码 → 五十步去噪 → VAE 解码。</p>

  <p>而这三件事<b>可以拆到三台机器上跑</b>。这一节回答两个问题：
    <b>凭什么能拆</b>（本节前半），<b>拆完怎么摆</b>（后半）。</p>

  <div class="note info"><span class="t">⭐ 为什么这一节属于一门讲硬件的课</span>
    因为「能不能拆」<b>完全是一道硬件账</b>：拆开的代价是切口上的数据要搬一趟，
    而搬得起搬不起，取决于<b>那个切口有多细</b>。
    <br><em>这跟 §四那条「强度 ≈ 位置数」是同一类推理 ——
    <b>都是先把量算出来，再让量去决定架构。</b></em>
  </div>

  <h3>五点一　管子里每一处有多粗</h3>

  <p>下面这张图把一次生成从头到尾的<b>数据体积</b>画在一根对数轴上。
    全部以 Wan2.1-T2V-14B、1280×720、81 帧为准。</p>
""")

a("""
  <p>拿 <b>Wan 2.2 图生视频</b>当例子 —— <b>十个模型都是这套布局</b>：
    <a href="https://github.com/yangwhale/gpu-tpu-pedia/tree/main/tpu/Wan2.2">
    github.com/yangwhale/gpu-tpu-pedia/tree/main/tpu/Wan2.2</a></p>
<pre><code>tpu/Wan2.2/
├── generate_i2v_torchax.py                   <b>← ① 一体化：一个进程从头跑到尾</b>
├── generate_diffusers_i2v_torchax_staged/    <b>← ② 三阶段</b>
│   ├── stage1_encoder.py                        文本 ＋ 首帧 → embedding
│   ├── stage2_transformer.py                    五十步去噪 → latent
│   ├── stage3_vae_decoder.py                    latent → 成片
│   ├── utils.py                                 <b>落盘 / 读盘的 helper 全在这</b>
│   └── stage_outputs/                        <b>← ⭐ 切口就在这个目录里</b>
│       ├── stage1_embeddings.safetensors     <b>← 段与段之间唯一交接的，就这三个文件</b>
│       ├── stage2_latents.safetensors
│       ├── generation_config.json
│       └── output_video.mp4                     成片
├── docs/wan_tpu_optimization_guide.md        约 1,970 行迁移与优化指南
└── README.md</code></pre>
  <p class="dim">⚠️ 下面这张图上的字节数取自 <b>Wan2.1</b> 那一份同名目录
    （<code>tpu/Wan2.1/generate_diffusers_torchax_staged/stage_outputs/</code>）——
    两个模型目录结构相同，数不同。</p>
""")
a(fig("figx-10",
      "<b>图 X-10</b>　三阶段之间只交接三个文件。"
      "<em>⛔ 这张图推翻重写过一次：初版画过一根 457 GB 的「注意力分数矩阵」柱子，"
      "而<b>那个矩阵从来没有被物化过</b>（Flash / Splash 分块算，算完即弃）——"
      "把不存在的量画成「最粗处」，整张图的比例尺就锚在了虚构上。</em>"))

a("""
  <p>三个数值得单独读：</p>

  <table>
    <thead><tr><th>读什么</th><th>多少</th><th>怎么来的</th></tr></thead>
    <tbody>
      <tr><td><b>VAE 压缩比</b></td><td><b>46.3 ×</b></td>
        <td>223,948,800 个像素数 ÷ 4,838,400 个 latent 数<br>
          <span class="dim">＝ 空间 64 倍 × 时间 3.86 倍 ÷ 通道摊薄 5.33 倍</span></td></tr>
      <tr><td><b>跨机要传多少</b></td><td><b>19.4 MB</b></td>
        <td>DiT 吐出来的那份 latent。<b>只有它需要过网</b></td></tr>
      <tr><td><b>传它要多久</b></td><td><b>1.5 ms</b></td>
        <td>19.4 MB 走 100 Gbps；就算走 1 Gbps 也只要 155 ms</td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐⭐ 于是「能不能切」这个问题根本不成立</span>
    被切开的那一段本身要算 <b>229 秒</b>，而搬一趟切口数据是 <b>1.5 毫秒</b> ——
    <b>切口成本是计算量的万分之零点七</b>。
    <br>⭐ 更值得记的是<b>为什么最细的那一处正好就是该切开的那一处</b>：
    往前是 774 MB 的激活，往后是 448 MB 的像素，
    <b>而 latent 自己只有 19.4 MB —— 扩散模型的形状天然把切口摆在了明处。</b>
  </div>

  <div class="note info"><span class="t">⭐ 那个 19.4 MB 顺手把形状也验了</span>
    16 × 21 × 90 × 160 个数 × 4 字节（fp32）＝ <b>19,353,600</b>，
    加上 safetensors 的 272 字节文件头，<b>正好等于文件的 19,353,872 —— 一个字节不差。</b>
    <br><em>跟 §三点四那个 14.05 B 是同一种手法：<b>用一个独立可测的量，去验一整套推导。</b>
    这次连误差都没有，因为字节数是精确的。</em>
  </div>

  <div class="note bad"><span class="t">⛔ 这张图有两处极易读反</span>
    ① <b>它画的是体积，不是时间。</b>腰细不代表那一段轻松 ——
    恰恰相反，<b>产出那 19.4 MB 的 Stage 2 占了全程 98% 的时间。</b>
    <br>② ⛔ <b>初版这里画过一根 457 GB 的「注意力分数矩阵」柱子，已删除。</b>
    那个矩阵<b>从来没有被物化出来过</b> —— Flash / Splash Attention 分块算，算完即弃。
    <b>把一个不存在的量画成「最粗处」，整张图的比例尺就锚在了虚构上</b>；
    而旁边那句「从不落地」的注解<b>抵消不了图形本身的断言</b>。
  </div>

  <h3>五点二　三段的胃口完全不一样 —— 这才是分开部署的真正理由</h3>

  <p>切得动只是<b>可行性</b>。真正的收益来自另一件事：<b>这三段吃的根本不是同一种资源。</b></p>
""")

a(fig("figx-11",
      "<b>图 X-11</b>　上半是三段的资源画像，下半是两种部署拓扑。"
      "<em>⭐ 左边一体化那栏的问题不是「慢」，是"
      "<b>按最馋的那一段配机器，另外两段的钱就白付了</b>。</em>"))

a("""
  <ul>
    <li><b>① 文本编码</b> —— 几乎不吃算力，就是把权重读一遍。占全程 <b>1.3%</b>。</li>
    <li><b>② DiT 去噪</b> —— <b>吃算力</b>。占全程 <b>98.3%</b>。§三算过的那 3.26 × 10¹⁷ FLOP 全在这儿。</li>
    <li><b>③ VAE 解码</b> —— <b>吃显存峰值</b>。把 480 万个数展开成 2.24 亿个，占全程 <b>0.4%</b>。</li>
  </ul>

  <div class="note info"><span class="t">⭐ VAE 那一段有个特别的形状：编译极贵，运行极便宜</span>
    720P 下 <b>预热要 80 秒，之后每次只要 1 秒 —— 80 倍差</b>。
    <br>这个形状<b>天生该做成常驻服务</b>：编译好放着，谁要解码谁来调，
    那 80 秒摊到成千上万次调用上等于零。
    <b>而一体化脚本每起一次进程就得重付一遍。</b>
    <br><em>——&nbsp;注意这是一条<b>纯粹由「编译期做主」推出来的部署结论</b>。
    §二点二那个 CMEM ＝ 0 的设计，代价在这里，收益也在这里。</em>
  </div>

  <h3>五点三　⛔ 一条顺口的结论，和一个当场推翻它的反例</h3>

  <p>看完上面那张图，最容易得出的结论是：<b>「文本编码那么轻，放 CPU 就行。」</b>
    这条在 Wan2.1 上成立 —— 它用 T5，那一段只要 <b>3 秒</b>。</p>

  <p><b>但 Flux.2 立刻推翻它。</b></p>

  <table>
    <thead><tr><th>模型</th><th>① 文本编码</th><th>② 去噪（TPU）</th>
      <th>③ VAE</th><th>最慢的是哪一段</th></tr></thead>
    <tbody>
      <tr><td><b>Wan2.1</b>（T5）</td><td>3 秒</td><td><b>230 秒</b></td><td>1 秒</td>
        <td>去噪 —— 符合直觉</td></tr>
      <tr><td><b>Flux.2</b>（Mistral3，放 CPU）</td><td><b>30 秒</b></td><td>13.5 秒</td>
        <td>1.5 秒</td><td><b>文本编码 —— 反过来了</b></td></tr>
    </tbody>
  </table>

  <div class="note bad"><span class="t">⛔ 照顺口结论摆，等于把瓶颈从 TPU 挪到了 CPU 上</span>
    Flux.2 那个「轻量」的第一段，<b>反而是全程最慢的一段</b>。
    <br>⭐ 判据不是<b>「哪一段天生该放哪」</b>，而是
    <b>「先量一量它在目标硬件上要多久」</b>。
    <br><em>而分段的价值恰恰在这里 ——&nbsp;
    <b>拆开之后，每一段的账才第一次能单独算清楚。</b>
    一体化脚本里，你连「文本编码花了 30 秒」这件事都看不见。</em>
  </div>

  <h3>五点四　一个节点摆几路：先看塞不塞得下，再看它够不够忙</h3>

  <p>一台 v6e-8 是 <b>8 颗 × 32 GB ＝ 256 GB</b>。同一台机器，两种截然不同的用法：</p>

  <table>
    <thead><tr><th>模型</th><th>bf16 权重</th><th>单颗放得下吗</th>
      <th>于是怎么用这 8 颗</th></tr></thead>
    <tbody>
      <tr><td><b>Wan2.1-14B</b></td><td>28 GB</td>
        <td>⚠️ 贴边，只剩 4 GB 余量</td>
        <td><b>TP 摊到 8 颗</b>，每颗 3.5 GB —— 40 个头 ÷ 8 ＝ 每颗 5 个头</td></tr>
      <tr><td><b>SDXL</b></td><td>7 GB</td><td>✅ 轻松</td>
        <td><b>开 8 路各生成各的</b>（数据并行）—— 实测 2.40 张/秒</td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐⭐ 两行是同一句话</span>
    <b>装不下就把一个模型摊开，装得下就多放几路。</b>
    <br>⭐ 而分段之后，这个决定是<b>按段做的</b>：
    DiT 那一层摊开、VAE 那一层多放几路，<b>互不牵扯</b> ——
    一体化脚本里这两个决定被绑死成了一个。
    <br><em>这就是「分三阶段有利于部署」这句话的全部具体含义。</em>
  </div>
""")
a('</section>')

# ══════════════════════════════════════════════════════════════
# §六 —— L100 里没有这一节
# ══════════════════════════════════════════════════════════════
a('<section id="s六">')
a('  <h2><span class="secno">六</span>这套说法，我们自己验过吗</h2>')

a("""
  <p>前五节讲的全是<b>应该怎样</b>：屋脊点该怎么算、活该落在轴的哪一侧、
    流水线该在哪儿切。<b>这一节讲实际怎样。</b></p>

  <p>——&nbsp;同一批道理，在<b>十个真模型上跑了五个月</b>之后，留下了什么。</p>

  <h3>六点一　五个月，十个模型</h3>
""")

a(fig("figx-12",
      "<b>图 X-12</b>　每一行的起止与提交数都是 <b>git 提交历史直接数出来的</b>。"
      "<em>⭐ 顶部三条色带是移植方法的更替 —— 注意 12 月 10 日那道红线："
      "<b>那天手写 Flax 的文件被删除，第三代从那一刻算起。</b></em>"))

a("""
  <p>值得单独指出的是<b>提交数的走向</b>：</p>

  <table>
    <thead><tr><th>时期</th><th>代表模型</th><th>提交数</th><th>在做什么</th></tr></thead>
    <tbody>
      <tr><td><b>前两周</b></td><td>HunyuanVideo-1.5</td><td><b>73</b></td>
        <td><b>发明方法</b> —— 同时在试三条移植路线</td></tr>
      <tr><td>头一个月</td><td>CogVideoX / Wan2.1</td><td>49 / 44</td>
        <td>方法定型，并回头反哺老模型</td></tr>
      <tr><td><b>后三个月</b></td><td>SDXL / Real-ESRGAN / Flux.1</td><td><b>9 / 3 / 2</b></td>
        <td><b>套用方法</b> —— 照着 README 抄一遍</td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐⭐ 提交数掉下去，才是一条工程路线走通的标志</span>
    差别<b>不在模型难度</b> —— SDXL 和 Flux.1 都不比 CogVideoX 简单。
    差别在于<b>前面几个是在发明方法，后面几个是在套用方法</b>。
    <br>——&nbsp;<b>所以这一节的落点不是「我们做了十个模型」，
    而是「接第十一个模型的成本，已经不是前十个的量级了」。</b>
  </div>

  <div class="note warn"><span class="t">⚠️ 提交数只能这么用，别过度解读</span>
    它反映的是<b>改动次数</b>，不等于工作量，更不等于难度。
    <br>这里拿它做的<b>唯一</b>推断是「发明 vs 套用」的<b>量级差</b>（73 对 2），
    <b>不用它比较任意两个模型谁更难。</b>
  </div>

  <h3>六点二　为什么又是 JAX 版、又是 torchax 版</h3>

  <p>这是客户最常问的一条。而它<b>讲道理是讲不赢的</b> ——
    「用 PyTorch 生态」和「用 JAX 原生」，两边都能说出一串听起来对的理由。</p>

  <p><b>我们的答法是：三条都走完，把对照版原样留在仓库里。</b></p>
""")

a(fig("figx-13",
      "<b>图 X-13</b>　三代移植方法逐项对照，下半是 torchax、Flax NNX、纯 JAX 的四项实测比较。"
      "<em>⭐ 最后一项<b>生态兼容</b>才是决定性的 —— 前三项说的是快慢，"
      "它说的是<b>下一个模型接不接得进来</b>。</em>"))

a("""
  <div class="note info"><span class="t">⭐ torchax 为什么不需要重新 tracing</span>
    Flax 和纯 JAX 都要<b>把整个函数 trace 一遍</b>再交给 XLA。
    torchax 走的是 <b>PyTorch 自己的 C++ dispatcher</b>：
    调用 <code>conv3d</code> 时，dispatcher 认出算子类型，
    路由到 torchax 注册的后端，<b>直接落到对应的 JAX 实现</b>。
    <br><em>——&nbsp;它不是「又一个 tracing 框架」，它是<b>在算子这一层换了后端</b>。</em>
  </div>

  <div class="note bad"><span class="t">⛔ 但别把这张图读成「第二代是弯路」</span>
    <b>没有第二代，我们不会知道该往哪个方向优化。</b>
    分片策略、内存布局、数值对齐这些认识，全是在手写那一版里长出来的 ——
    一开始就用 torchax，这些东西会被框架挡在视野之外。
    <br>⭐ 而且那批代码<b>今天仍在服役</b>：torchax 版算出可疑结果时，
    <b>拿纯 JAX 版跑同一个输入对一遍</b>，是最快的定位手段。
    <br><em>判据：<b>一条被换掉的路线值不值得留下，取决于它还能不能当参照物 ——
    而不是取决于它现在跑得快不快。</b></em>
  </div>

  <h3>六点三　这十个模型跟前面五节是什么关系</h3>

  <p>不是「附录里的项目清单」。<b>每一个都在验证前面某一节的某一条断言</b>：</p>

  <table>
    <thead><tr><th>前面哪一节说过</th><th>哪个模型验的</th><th>验出了什么</th></tr></thead>
    <tbody>
      <tr><td><b>§二点三</b>　权重体积决定要几颗</td><td>SDXL vs Wan2.1</td>
        <td>7 GB 的开 8 路，28 GB 的摊到 8 颗 —— 同一台机器两种用法</td></tr>
      <tr><td><b>§三</b>　扩散是计算密集</td><td>Wan2.1 720P</td>
        <td>强度 75,600；<b>Wan2.2</b> 上实测算子 MFU 37% ／ 整模型 34%
          —— <b>落在算力侧，但没吃满</b></td></tr>
      <tr><td><b>§三点五</b>　MXU 喂不满会浪费成块</td><td>Wan2.1</td>
        <td>head_dim 128 对 MXU 256 —— 一半位置空着</td></tr>
      <tr><td><b>§五点一</b>　腰在 latent 那一处</td><td>全部七个三阶段模型</td>
        <td>无一例外，中间产物都是全程最小的那一份</td></tr>
      <tr><td><b>§五点三</b>　编码器放哪要先量</td><td>Flux.2</td>
        <td><b>反例</b>：Mistral3 放 CPU 30 秒，比它的 DiT 还慢</td></tr>
      <tr><td>整套框架对非 Transformer 成不成立</td><td><b>Real-ESRGAN</b></td>
        <td>纯卷积网，8.8M 参数 —— <b>切块之后比 L40S 快 1.9 倍</b></td></tr>
    </tbody>
  </table>

  <div class="note ok"><span class="t">⭐ 最后一行是特意留的</span>
    Real-ESRGAN 是十个里唯一一个<b>非 Transformer、非扩散</b>的模型
    （纯卷积，只有 8.8M 参数）。
    <br>放它进来的目的<b>不是多凑一个</b>，是<b>看这套分析框架在完全不同的架构上还成不成立</b>。
    <br><em>——&nbsp;答案是成立的：它同样落在算力侧，同样靠切块提高每字节的计算量，
    <b>只是「位置」从 token 变成了 tile。</b></em>
  </div>
""")
a('</section>')

# ══════════════════════════════════════════════════════════════
# 收尾 —— ⛔ 非数字。
# ══════════════════════════════════════════════════════════════
a("""
<section id="s收尾">
  <h2><span class="secno">收尾</span>落点</h2>
  <p class="landing">—— <b>选芯片不是选「更强的那颗」，是先看你的活落在那根轴的哪一边。</b>
    <br><em>落右边（扩散、prefill、大 batch）比的是算力，v6e 打平；
    落左边（小 batch decode）比的是带宽，v6e 吃亏。</em>
    <br><b>而这两件事，从两颗芯片的布局图上就能提前看出来 —— 不用等跑完。</b></p>
  <div class="note warn"><span class="t">⚠️ 这一讲刻意留白的地方</span>
    <b>没有端到端性能数。</b>本讲讲结构匹配，不讲快慢 —— 两件事需要的证据不一样，
    后者要实测，而实测要连口径一起给。
    <br><em>想看扩散模型本身：<b>专题十一</b>。想看这套分析框架怎么来的：<b>专题二</b>。</em>
  </div>

  <div class="note ok"><span class="t">⭐⭐ 这一版比 L100 多给了什么</span>
    L100 给的是<b>结论</b>：560、75,600、短板被挡在瓶颈之外。
    这一版多的是<b>四样东西</b> ——
    <br>① <b>推导链</b>（§一点一、§三点二）：每个数怎么除出来的；
    <br>② <b>验证</b>（§一点二、§三点四、§五点一）：拿什么外部锚点验过它，
    以及<b>锚点当场抓到过什么错</b>；
    <br>③ <b>反例</b>（§三点三的 window_size、§三点五的 MFU 37%、§五点三的 Flux.2）：
    这些结论什么时候不成立；
    <br>④ <b>落地</b>（§五、§六）：从「该怎么切」到「真的切了，十个模型五个月」。
    <br><em>⭐ 如果只带走一条，带走 ③ ——&nbsp;
    <b>一套只有顺例的说法，第一次遇到边界就会碎；
    而知道边界在哪的人，才敢把它用在没见过的模型上。</b></em>
  </div>
</section>
""")

html = head + '\n<main class="wrap">\n' + "\n".join(BODY).replace("__FAMNAV__", nav("topic-02x-L200.html")) + "\n</main>\n"

# ── 写盘前自检 ──────────────────────────────────────────────────
import sys                                                        # noqa: E402
sys.path.insert(0, os.path.join(HERE, "tpu-micro"))
from gate import lint_public                                      # noqa: E402

bad = lint_public(html)
assert not bad, "公开页面里出现内部词，已中止写盘：%s" % bad

# ⛔ 图数断言 ——&nbsp;每加一节要顺手改这个数。
#   它的作用不是防少图，是防**图悄悄多出来**（复制粘贴节的时候最容易）。
# ⛔ 防回归：只要页面用了 class="hero"，就必须带上中和它两层装饰的那段 CSS，
#   否则标题会被那层 95deg 白蒙版糊掉（左侧 94% 不透明）。这个坑不报错、只是变糊。
if 'class="hero"' in html:
    assert "header.hero::after { display: none }" in html or \
           "header.hero::after { display:none }" in html or \
           "header.hero::before,\nheader.hero::after { display: none }" in html, \
        "用了 .hero 却没中和它的装饰伪元素 —— 标题会被白蒙版糊掉"

n_fig = html.count('<figure class="fbox fwide"')
EXPECT_FIG = 13
assert n_fig == EXPECT_FIG, "图数不对：%d（应为 %d）" % (n_fig, EXPECT_FIG)

# ⭐ 两页靠**显式映射**相连，不再靠节号相同（见文件头 09-09 那段）。
#   ⛔ 所以自检查的是「L100 里那张对照表还在不在」——&nbsp;
#     表没了，读者就没有从 L100 跳到这一页的任何路径，而**页面照样构建**。
l100 = io.open(os.path.join(WEB, "topic-02x.html"), encoding="utf-8").read()
assert "这一讲的每一节在 L200 的哪儿" in l100, \
    "L100 的收尾少了那张「→ L200」对照表 —— 两页之间会断掉"
for sid in ("s零", "s一", "s六", "s收尾"):
    assert 'id="%s"' % sid in l100, "L100 里没有 %s" % sid

io.open(OUT, "w", encoding="utf-8").write(html)
print("ok  topic-02x-L200.html  %s 字符  %d 图" % (format(len(html), ","), n_fig))

# 控制面（CPU 机器，无 GPU 依赖）
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
PyJWT>=2.9
livekit-api>=0.8          # 只用 AccessToken 铸票

# worker 额外需要（GPU 机器）
# livekit-agents>=1.5
# livekit>=0.17
# numpy, pillow

# 专题五 · 推理

> **训练把模型造出来，推理把它用起来 —— 而这两件事的约束几乎处处相反。
> 推理系统所有的复杂性，都来自一个事实：它内部有两个瓶颈相反的阶段。**

> 🚧 **大纲，未展开。** 2026-08-21 起草。
>
> **前置**：[专题一](专题01-拆开-DeepSeek-V3.md)（会算账、知道 KV cache 是什么）+
> [专题二](专题02-TPU-与-GPU.md)（知道静态形状是 TPU 的硬约束）。

---

## 这个专题要回答的一个问题

前四个专题讲的都是训练：一个目标（吞吐最大化）、一种负载、跑几个星期。

**推理不一样，而且不一样在两个层面：**

1. **目标从一个变成三个**：延迟、吞吐、成本，**三者互相打架**
2. **负载内部是分裂的**：一次请求里有两个阶段，**它们的瓶颈完全相反**

第二点是这个专题的主线。理解了它，后面所有的技术
（连续批处理、PD 分离、投机解码……）都是它的自然推论。

---

## 一 · 两个阶段，两种性质 ⭐

一次请求分成两段：

| | **Prefill** | **Decode** |
|---|---|---|
| 在干什么 | 把整段 prompt 一次吞进去，算出 KV cache | 一个 token 一个 token 往外吐 |
| 一次算多少 token | **几千甚至十几万** | **1 个** |
| 矩阵形状 | 大而方正 | **瘦长**（一行乘一个大矩阵） |
| 每算一次要读多少权重 | 同样多 | 同样多 |
| **瓶颈** | **算力受限** | ⭐ **带宽受限** |
| 决定哪个指标 | **TTFT**（首 token 延迟） | **TPOT**（每 token 时间） |

> ⭐ **同样的模型、同样的权重，两个阶段落在屋顶线的两侧。**
> （回到[专题六](专题06-性能调优与工具链.md)那张图：prefill 在平顶下，decode 在斜坡上。）

**decode 为什么那么亏**：为了算 1 个 token，要把**全部权重**从 HBM 读一遍。
671B 的模型、bf16 存，读一遍就是 1.3 TB。**算术强度低到离谱。**

**这一条推出了后面几乎所有的技术**：
- 想办法让 decode 一次多算几个 token → **批处理**、**投机解码**
- 想办法让每次读的字节少一点 → **量化**、**MoE**（只读激活的那部分）
- 既然两阶段性质不同 → **干脆分开部署**

---

## 二 · KV cache：推理的头号显存消耗

- 训练时 KV cache 不存在（一次算完整句）；**推理时它是主角**
- 它随 **序列长度 × 层数 × 并发数** 三重增长
- ⭐ **它直接决定你能同时服务多少人** —— 这是推理服务最硬的容量约束

**要算的**：拿[专题一](专题01-拆开-DeepSeek-V3.md)的 MLA 数字，
算出一个 128K 请求占多少、一张卡能放几个并发。

**管理它的技术**：
- **PagedAttention** —— 像操作系统的虚拟内存那样分页管理 KV，
  解决碎片和过度预留。**这是 vLLM 的立身之本**
- **前缀缓存（prefix cache）** —— 相同的系统提示词只算一次
- **KV cache 量化** —— 直接砍显存

> 这一节跟[专题七](专题07-注意力演进.md)接上：
> **MLA、DSA、线性注意力那一整排变体，主要就是为这一节的问题发明的。**

---

## 三 · 批处理：把 decode 的浪费摊掉

既然 decode 读一次权重只算一个 token，那就**让它一次算很多个请求的 token**。

- batch 越大 → 同一次权重读取服务的 token 越多 → **算术强度线性上升**
- 但 batch 越大 → 单个请求的延迟越差，KV cache 也吃得越多

**静态批处理的问题**：一批里有的请求早早生成完了，
却要等最长的那个 —— **卡空转**。

**连续批处理（continuous batching）**：
谁生成完了谁就走，空出的位置立刻放新请求进来。
⭐ **这是现代推理引擎吞吐提升最大的单项来源。**

---

## 四 · PD 分离：既然性质相反，为什么放在一起

**一个很自然但很晚才被普遍接受的想法：**

prefill 算力受限、decode 带宽受限 —— 把它们放在同一批卡上，
就意味着这批卡的配置对两边都不是最优，而且两个阶段还互相抢资源
（一个长 prompt 的 prefill 会卡住所有人的 decode）。

**PD 分离（Prefill-Decode Disaggregation）**：拆成两个池子，各自配置、各自扩缩容。

- 代价：**KV cache 要从 prefill 池传到 decode 池** —— 传输量多大？走什么通路？
- 收益：两边都能按自己的瓶颈优化；长 prompt 不再干扰 decode
- 什么时候值得：跟请求的长度分布强相关

> ⭐ 这跟[专题十](专题10-后训练与-RL.md)里 RL 把训练和推理拆池
> 是**同一个思想**：**性质不同的负载就该分开放。**

---

## 五 · 投机解码：用富余的算力换步数

**逻辑链条很漂亮，值得完整讲一遍：**

1. decode 是带宽受限 → **算力是有富余的**
2. 那能不能用算力换点什么？
3. 能 —— **一次猜好几个 token，然后一次性验证**
4. 猜对了就赚了几步；猜错了就退回，损失的只是那些富余的算力

**几种猜法**：
- 小模型当草稿（draft model）
- **MTP** —— ⭐ [专题一](专题01-拆开-DeepSeek-V3.md)站 6 埋的伏笔在这里回收：
  V3 训练时用的多 token 预测头，推理时正好拿来做投机
- n-gram / 前缀匹配

⚠️ **压测陷阱**：验证投机解码效果时**必须固定采样随机性**（temperature=0），
否则接受率会崩到个位数，你会得到一个完全错误的结论。**我们踩过。**

---

## 六 · 量化：直接砍访存

decode 是带宽受限 → **读的字节少一半，就快一倍**（近似）。
所以量化在推理侧的收益比训练侧直接得多。

- 权重量化：FP8 / FP4 / INT4/8
- KV cache 量化
- 激活量化（更难）
- ⚠️ 哪些层不能量化、精度掉在哪里

> 详细的精度原理放在[专题八](专题08-精度与量化.md)，
> 这里只讲**推理场景下的取舍**。

---

## 七 · TPU 上的推理：静态形状这道坎 ⭐

**这一节是我们最有话说的部分。**

推理天生是动态的：prompt 长度不同、生成长度不同、并发数在变。
而 XLA 要求**编译期形状确定**（[专题二](专题02-TPU-与-GPU.md)）。

**这个矛盾怎么解：**

- **分桶（bucketing）** —— 把长度归到有限几档，padding 到桶边界
- **预编译** —— 每个桶编译一份，缓存起来
- **编译缓存** —— 否则冷启动要等十几分钟
- 桶怎么分：太粗浪费算力（padding 多），太细编译爆炸
- ⚠️ 连续批处理 + 静态形状 = **两个方向相反的需求**，这是移植的核心难点

**引擎现状**：vLLM 和 SGLang 都起于 GPU，往 TPU 移植时**卡的全是这一条**。
⭐ 我们部署过六个模型，踩过的坑可以直接摆出来。

---

## 八 · 服务化：指标与调度

- **指标**：TTFT / TPOT / 端到端延迟 / 吞吐 / **P99**（平均值会骗人）
- **调度**：优先级、抢占、长请求怎么不饿死短请求
- **容量规划**：给定 SLA，一张卡能服务多少 QPS —— **这是所有账的落点**
- 多租户与隔离

---

## 九 · 训练与推理的全面对照（收尾）

一张表把这个专题和前四个专题接起来：

| | 训练 | Prefill | Decode |
|---|---|---|---|
| 瓶颈 | 算力 | 算力 | **带宽** |
| batch | 大而固定 | 变 | **越大越好但伤延迟** |
| 形状 | 静态 | 变长 | 变长 |
| KV cache | 无 | 在产生 | **在反复读** |
| 优化方向 | 吞吐 | TTFT | TPOT |
| 并行怎么选 | 看显存和通信 | 类似训练 | **TP 更受青睐**（减少每卡要读的权重） |

---

## 素材

| 要什么 | 在哪 |
|---|---|
| **六个模型的 TPU 部署** | `tpu/tpu-inference/`（DeepSeek R1 / V3.2、GLM-5.1、Qwen3.5、Qwen3-Coder…），每份 900-1600 行 |
| vLLM on TPU | `tpu/vllm-torchtpu/`（含 397B 的 16 芯片实测） |
| SGLang on JAX | `tpu/sglang-jax/` |
| GPU 侧对照 | `gpu/inference/a4x-max/`（含 GB300 上的 runbook） |
| 静态形状与分桶 | 内部术语页 |
| KV cache / PagedAttention / 前缀缓存 | 内部术语页 |

---

## 还没想清楚的

- [ ] **篇幅**：内容量跟训练那几个专题相当，可能要拆成
      「两个阶段与 KV cache」+「工程与服务化」
- [ ] 跟[专题七](专题07-注意力演进.md)的边界 ——
      那边讲注意力变体怎么省 KV，这边讲运行时怎么管 KV。倾向这么分
- [ ] 要不要讲 GPU 侧的推理栈（TensorRT-LLM 等）—— 倾向只做对照，不展开
- [ ] 是否要包含多模态推理（图像输入、视频）

---

> 本目录随 `Courses/` 采用 **CC BY-NC-SA 4.0**（[全文](LICENSE.txt)）。

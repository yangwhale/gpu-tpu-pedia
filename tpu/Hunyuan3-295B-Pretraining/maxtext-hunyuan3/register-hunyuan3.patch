diff --git a/src/MaxText/common_types.py b/src/MaxText/common_types.py
index fd7185a83..da6c3f653 100644
--- a/src/MaxText/common_types.py
+++ b/src/MaxText/common_types.py
@@ -86,6 +86,7 @@ class DecoderBlockType(enum.Enum):
   GEMMA3 = "gemma3"
   QWEN3 = "qwen3"
   QWEN3_MOE = "qwen3_moe"
+  HUNYUAN3 = "hunyuan3"
   GPT3 = "gpt3"
   GPT_OSS = "gpt_oss"
   SIMPLE = "simple"
diff --git a/src/MaxText/configs/base.yml b/src/MaxText/configs/base.yml
index a07f3421b..91230c009 100644
--- a/src/MaxText/configs/base.yml
+++ b/src/MaxText/configs/base.yml
@@ -195,6 +195,9 @@ shared_experts: 1
 routed_scaling_factor: 1.0 # scaling factor for routing scores
 routed_score_func: "" # scoring function for routing
 routed_bias: False # a flag if a learnable bias is added for routing
+moe_router_dtype: "" # dtype for computing router logits. Inherits from dtype if unset.
+                     # DeepSeek V3 / Hunyuan 3 compute the router in fp32 for numerical
+                     # stability of the sigmoid scores across a large expert count.
 mlp_bias: False # a flag if a learnable bias is added for MLP matmul
 n_routing_groups: -1 # number of groups for routing, disabled by default
 topk_routing_group: -1 # number of top groups to route inputs. For EP,
diff --git a/src/MaxText/layers/decoders.py b/src/MaxText/layers/decoders.py
index 3d6f9503b..dcde347ae 100644
--- a/src/MaxText/layers/decoders.py
+++ b/src/MaxText/layers/decoders.py
@@ -48,6 +48,7 @@ from MaxText.layers import (
     gemma3,
     gpt3,
     gpt_oss,
+    hunyuan3,
     llama2,
     llama4,
     mistral,
@@ -382,6 +383,8 @@ class Decoder(nn.Module):
         return [mixtral.MixtralDecoderLayer]
       case DecoderBlockType.DEEPSEEK:
         return [deepseek.DeepSeekDenseLayer, deepseek.DeepSeekMoELayer]
+      case DecoderBlockType.HUNYUAN3:
+        return [hunyuan3.Hunyuan3DenseLayer, hunyuan3.Hunyuan3MoELayer]
       case DecoderBlockType.GEMMA:
         return [gemma.GemmaDecoderLayerToLinen]
       case DecoderBlockType.GEMMA2:
@@ -447,6 +450,7 @@ class Decoder(nn.Module):
         DecoderBlockType.GEMMA3,
         DecoderBlockType.QWEN3,
         DecoderBlockType.QWEN3_MOE,
+        DecoderBlockType.HUNYUAN3,
         DecoderBlockType.GPT_OSS,
         DecoderBlockType.SIMPLE,
         DecoderBlockType.SIMPLE_MLP,
@@ -486,7 +490,7 @@ class Decoder(nn.Module):
     """get pipeline stage module"""
 
     def get_layer_to_pipeline(blocks, cfg):
-      if cfg.decoder_block == DecoderBlockType.DEEPSEEK:
+      if cfg.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
         return blocks[1]  # return the sparse block
       else:
         return blocks[0]
@@ -667,8 +671,8 @@ class Decoder(nn.Module):
         )
       else:
         partition_spec = None  # This partition spec is only used for the fsdp_ag_once feature.
-      if cfg.decoder_block == DecoderBlockType.DEEPSEEK:
-        assert len(RemattedBlockLayers) == 2, "Scanned layers must have a length of 2 using deepseek."
+      if cfg.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
+        assert len(RemattedBlockLayers) == 2, "Scanned layers must have a length of 2 using deepseek/hunyuan3."
         dense_layer = RemattedBlockLayers[0]
         moe_layer = RemattedBlockLayers[1]
         num_moe_layers = cfg.num_decoder_layers - cfg.first_num_dense_layers
@@ -713,8 +717,8 @@ class Decoder(nn.Module):
             )(y, *broadcast_args)
     else:
       if cfg.scan_layers:
-        if cfg.decoder_block == DecoderBlockType.DEEPSEEK:
-          assert len(RemattedBlockLayers) == 2, "Scanned layers must have a length of 2 using deepseek."
+        if cfg.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
+          assert len(RemattedBlockLayers) == 2, "Scanned layers must have a length of 2 using deepseek/hunyuan3."
           layer_call_kwargs = {
               "page_state": page_state,
               "previous_chunk": previous_chunk,
@@ -776,7 +780,7 @@ class Decoder(nn.Module):
               **layer_kwargs,
           )(y, *broadcast_args)
       else:
-        if cfg.decoder_block == DecoderBlockType.DEEPSEEK:
+        if cfg.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
           assert len(RemattedBlockLayers) == 2, "Unscanned layers must have a length of 2 using deepseek."
           dense_layer = RemattedBlockLayers[0]
           moe_layer = RemattedBlockLayers[1]
diff --git a/src/MaxText/layers/moe.py b/src/MaxText/layers/moe.py
index 04744c696..6c44bf168 100644
--- a/src/MaxText/layers/moe.py
+++ b/src/MaxText/layers/moe.py
@@ -243,7 +243,7 @@ class GateLogit(nnx.Module):
 
     if self.score_func:
       output = linears._convert_to_activation_function(self.score_func)(output)
-      if self.model_name.startswith("deepseek3"):
+      if self.model_name.startswith(("deepseek3", "hunyuan3")):
         pre_bias_logits = output
 
     if self.use_bias:
@@ -308,7 +308,11 @@ class RoutedMoE(nnx.Module):
         in_features_shape=self.config.emb_dim,
         out_features_shape=self.num_experts,
         model_name=self.config.model_name,
-        dtype=self.dtype,
+        # Router logits may need higher precision than the activations: with
+        # 192 experts the sigmoid scores that drive top-k are too close together
+        # to resolve reliably in bf16. Upstream Hy3 does F.linear(h.float(),
+        # w.float()); Megatron exposes the same thing as moe_router_dtype.
+        dtype=getattr(self.config, "moe_router_dtype", None) or self.dtype,
         weight_dtype=self.weight_dtype,
         quant=self.quant,
         kernel_init=self.kernel_init,
@@ -410,12 +414,15 @@ class RoutedMoE(nnx.Module):
       top_k_weights, top_k_indices = random_routing(rng, gate_logits, self.num_experts_per_tok)
       return top_k_weights, top_k_indices
 
-    if self.config.model_name.startswith("deepseek3"):
+    if self.config.model_name.startswith(("deepseek3", "hunyuan3")):
       top_k_weights, top_k_indices = self.deepseek_routing(gate_logits, pre_bias_logits)
     else:
       top_k_weights, top_k_indices = jax.lax.top_k(gate_logits, self.num_experts_per_tok)
 
-    if self.config.decoder_block == ctypes.DecoderBlockType.DEEPSEEK:
+    if self.config.decoder_block in (ctypes.DecoderBlockType.DEEPSEEK, ctypes.DecoderBlockType.HUNYUAN3):
+      # Hy3 shares DeepSeek's sigmoid routing: normalize the top-k scores then
+      # apply routed_scaling_factor. Falling through to the softmax branch below
+      # would silently discard routed_scaling_factor and change the routing math.
       top_k_weights = self.deepseek_scale_weights(top_k_weights)
     elif self.config.decoder_block != ctypes.DecoderBlockType.LLAMA4:
       top_k_weights = jax.nn.softmax(top_k_weights.astype(jnp.float32), axis=-1).astype(self.dtype)
@@ -883,10 +890,10 @@ class RoutedMoE(nnx.Module):
       wo_bias_pspec = nn.logical_to_mesh_axes(("exp", "activation_embed"))
 
     gate_logits_pspec = nn.logical_to_mesh_axes((batch_logical_axis, "activation_norm_length", None))
-    if self.config.model_name.startswith("deepseek3"):
+    if self.config.model_name.startswith(("deepseek3", "hunyuan3")):
       pre_bias_logits_pspec = nn.logical_to_mesh_axes((batch_logical_axis, "activation_norm_length", None))
     else:
-      # pre_bias_logits is None for non-DeepSeek v3 models
+      # pre_bias_logits is None for models that do not use pre-bias routing weights
       pre_bias_logits_pspec = None
 
     # w0, w1, wo needs to be un sharded on fsdp / fsdp_transpose axis, so use
@@ -1421,8 +1428,8 @@ class RoutedMoE(nnx.Module):
     """Dense matrix multiplication."""
     # gate_logits: batch, length, expert
     gate_logits = nn.with_logical_constraint(gate_logits, ("activation_batch", "activation_norm_length", None))
-    if self.config.model_name.startswith("deepseek3"):
-      # pre_bias_logits is None for non-DeepSeek v3 models
+    if self.config.model_name.startswith(("deepseek3", "hunyuan3")):
+      # pre_bias_logits is None for models that do not use pre-bias routing weights
       pre_bias_logits = nn.with_logical_constraint(pre_bias_logits, ("activation_batch", "activation_norm_length", None))
     top_k_weights, top_k_indices = self.get_topk(gate_logits, pre_bias_logits, self.rngs)
     is_llama4_decoder_layer = self.config.decoder_block == ctypes.DecoderBlockType.LLAMA4
diff --git a/src/MaxText/maxtext_utils.py b/src/MaxText/maxtext_utils.py
index e0fd0743c..c3d25f3f2 100644
--- a/src/MaxText/maxtext_utils.py
+++ b/src/MaxText/maxtext_utils.py
@@ -305,7 +305,7 @@ def calculate_routed_and_shared_ffn_tflops_per_device(config):
 
 def get_dense_moe_layers(config):
   """Helper function to calculate number of dense and moe layers"""
-  if config.decoder_block == DecoderBlockType.DEEPSEEK:
+  if config.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
     num_dense_layers = config.first_num_dense_layers
     num_moe_layers = config.num_decoder_layers - config.first_num_dense_layers
     return num_dense_layers, num_moe_layers
@@ -313,7 +313,7 @@ def get_dense_moe_layers(config):
     num_moe_layers = config.num_decoder_layers // config.interleave_moe_layer_step
     num_dense_layers = config.num_decoder_layers - num_moe_layers
   else:
-    raise ValueError("Currently we only support DeepSeek and Llama4 calculation.")
+    raise ValueError("Currently we only support DeepSeek, Hunyuan3 and Llama4 calculation.")
 
   return num_dense_layers, num_moe_layers
 
@@ -458,7 +458,16 @@ def calculate_tflops_training_per_device(config, log=True):
   # MLP flops
   if config.num_experts > 1:
     # calculation based on dropless implementation
-    if config.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.LLAMA4):
+    if config.decoder_block in (
+        DecoderBlockType.DEEPSEEK,
+        DecoderBlockType.LLAMA4,
+        DecoderBlockType.HUNYUAN3,
+    ):
+      # Hy3 has the same routed + shared + leading-dense structure as DeepSeek.
+      # The generic branch below would size the experts with mlp_dim (13312, the
+      # dense width) instead of moe_mlp_dim (1536) and skip the shared expert,
+      # inflating the reported TFLOP/s by ~5x. Nothing about training changes --
+      # this is the analytic FLOP model used for the MFU number only.
       total_ffn_flops = calculate_routed_and_shared_ffn_tflops_per_device(config)
     else:
       gate_flops = 2 * config.per_device_batch_size * config.max_target_length * config.emb_dim * config.num_experts
@@ -521,7 +530,9 @@ def calculate_tflops_training_per_device(config, log=True):
     learnable_weight_tflops = (
         (total_ffn_flops + (qkv_flops + projection_flops) * config.num_decoder_layers + embedding_flops) * 3 / 10**12
     )
-  elif config.decoder_block == DecoderBlockType.DEEPSEEK:
+  elif config.decoder_block in (DecoderBlockType.DEEPSEEK, DecoderBlockType.HUNYUAN3):
+    # total_ffn_flops is already summed over layers by the helper above, so it
+    # must not be multiplied by num_decoder_layers a second time.
     learnable_weight_tflops = (
         (total_ffn_flops + (qkv_flops + projection_flops) * config.num_decoder_layers + embedding_flops) * 3 / 10**12
     )
diff --git a/src/MaxText/pyconfig.py b/src/MaxText/pyconfig.py
index 69e63d791..df428ea1c 100644
--- a/src/MaxText/pyconfig.py
+++ b/src/MaxText/pyconfig.py
@@ -367,6 +367,8 @@ def validate_model_name(s: str) -> bool:
       "deepseek3-671b",
       "deepseek3-test",
       "kimi-k2-1t",
+      "hunyuan3-295b",
+      "hunyuan3-smoke",
       "gemma-7b",
       "gemma-2b",
       "gemma2-2b",
@@ -694,6 +696,7 @@ class _HyperParameters:
     raw_keys["grad_dtype"] = jax.numpy.dtype(raw_keys["grad_dtype"])
     raw_keys["weight_dtype"] = jax.numpy.dtype(raw_keys["weight_dtype"])
     raw_keys["mu_dtype"] = set_mu_dtype(raw_keys)
+    raw_keys["moe_router_dtype"] = set_moe_router_dtype(raw_keys)
     raw_keys["logical_axis_rules"] = _lists_to_tuples(raw_keys["logical_axis_rules"])
     raw_keys["data_sharding"] = _lists_to_tuples(raw_keys["data_sharding"])
 
@@ -812,6 +815,13 @@ def set_mu_dtype(raw_keys):
     return jax.numpy.dtype(raw_keys["mu_dtype"])
 
 
+def set_moe_router_dtype(raw_keys):
+  """Router logits dtype. Defaults to the activation dtype when unset."""
+  if raw_keys["moe_router_dtype"] == "":
+    return raw_keys["dtype"]
+  return jax.numpy.dtype(raw_keys["moe_router_dtype"])
+
+
 def validate_and_set_hlo_dump_defaults(raw_keys):
   if not raw_keys["dump_hlo"]:
     return raw_keys

# 专题四 · 反向与优化器

> **把账补全 —— 最大的一块既不是权重也不是激活，是优化器状态。**

> 🚧 **大纲，部分已展开。** 2026-08-21 起草；2026-08-23 补齐 §1.2（激活账单）、§2.1–2.4（重算判据）与 §3.2b 尾部。
>
> **前置**：[专题一](专题01-一个-Token-的一生.md)（只算前向，结论是「装不下」）。
> **后续**：[专题五](专题05-并行策略.md)（有了完整的账，才有东西可切）。

---

## ⏱ 一小时怎么讲

**✅ 6 个内容节，一小时够**

| 分钟 | 讲什么 |
|---|---|
| 0-6 | **反向要付什么**：3× 算力、激活为什么不能算完就扔 |
| 6-16 | ⭐ **不开重算这笔账有多大** —— 4.15 TiB；以及 **FSDP 为什么帮不上忙** |
| 16-22 | **全量 recompute** —— 全课第一个「决策」：三分之一算力换掉四十分之三十九的显存 |
| 22-32 | ⭐ **选择性重算：判据只有一个数** —— 每字节付多少 FLOPs；以及**同一判据在 2022 年给出相反答案** |
| 32-46 | ⭐ **优化器状态是最大的一块** —— 每参数 16 字节，其中 12 字节是 fp32 |
| 46-54 | **这张账单直接决定并行策略长什么样**（含「加无穷多卡也装不下一条 128K」那条） |
| 54-58 | **一个完整 step 的总账** |

> 3.2b / 3.2c 那两个小节偏细，**讲的时候合成一段**：
> 「切到哪一级差别是数量级的，而且 bf16 那份是流动的不是常驻的」。

---

## 这个专题要回答的一个问题

专题一里我们跟着一个 token 走完了前向，算出「装不进任何一块卡」。

**但那只是账单的一小半。**

真正训练一步，你要同时在显存里放着：权重、梯度、优化器状态、
还有一大堆**不能扔掉的中间激活**。

这个专题把这张账单补全。补完之后会发现一件事：

> **最大的那一块，既不是权重也不是激活 —— 是优化器状态。**
>
> 而这，正是所有并行策略要解决的第一个问题。

---

## 一 · 反向传播要付什么

### 1.1 算力：大约是前向的两倍

- 每一个矩阵乘，反向要算**两个**梯度：对输入的、对权重的
- 所以反向 FLOPs ≈ 2× 前向 → **一个完整 step ≈ 3× 前向**
- 这个 3 倍是后面所有 MFU 计算的基准，要讲清楚它从哪来

**算**：拿专题一第 7 步的前向 FLOPs，乘出一个完整 step 的算力

### 1.2 显存：激活为什么必须留着

**这就是专题一第 5 步立而未答的那个问题。**

- 链式法则要用到前向的中间结果 —— 算权重梯度时需要那一层的输入
- 所以前向过程中产生的激活，**不能算完就扔，得留到反向用到它为止**
- 于是显存占用不是"某一层的峰值"，而是**一路累加**

#### 专题一那个 106.75 GiB，其实已经是打过折的

专题一第 5 步算过：残差流每层一份 1.75 GiB，61 层 **106.75 GiB**。

**但那个数字有个隐含前提 —— 它假设每层只留入口那一份。**
而「每层只留入口那一份」恰恰就是**开了重算之后**的样子：层里面的东西全扔掉，
反向用到时拿入口那份重新算一遍。

所以 106.75 GiB 不是「激活的账」，是**已经做过一次交易之后的账**。
原始账单在下面 —— 而这个顺序很重要，**先看到原始账单，第二节那个决策才有分量**。

#### 不开重算，一层还要留什么

以 V3 为例，序列 131,072、batch 1、bf16。
基准单位：一份 hidden 宽的张量 = 131,072 × 7,168 × 2 B = **1.75 GiB**。

| MLA 子层留什么 | 宽度 | 大小 |
|---|---|---|
| norm 的输入（= 残差入口，重算模式下唯一留的那份） | 7,168 | 1.75 GiB |
| norm 的输出 | 7,168 | 1.75 GiB |
| Q 降维结果（layernorm 前后各一份） | 1,536 | 0.75 GiB |
| KV 降维结果（含 rope 那 64 维） | 576 / 512 | 0.27 GiB |
| **Q 展开后** | 128 头 × 192 = **24,576** | **6.00 GiB** |
| **K、V 解压后** | 128 头 × 256 = **32,768** | **8.00 GiB** |
| attention 输出 | 128 头 × 128 = 16,384 | 4.00 GiB |
| logsumexp（fp32） | 128 | 0.06 GiB |
| **小计** | | **约 22.6 GiB** |

| MoE 子层留什么 | 份数 × 宽度 | 大小 |
|---|---|---|
| norm 的输入 / 输出 | 2 × 7,168 | 3.50 GiB |
| 路由分数 | 256 | 0.06 GiB |
| **派发出去的激活** | **9 份** × 7,168 | **15.75 GiB** |
| gate / up / SwiGLU 乘积 | 3 × 9 份 × 2,048 | 13.50 GiB |
| **专家输出（合并前）** | **9 份** × 7,168 | **15.75 GiB** |
| **小计** | | **约 48.6 GiB** |

> 「9 份」= 被激活的 8 个路由专家 + 1 个共享专家。
> **每个 token 的那份激活被复制了九遍**，进去一次、出来一次，就是 31.5 GiB。
> 这是 MoE 在显存上最贵的地方 —— 而它在专题一那张参数量的表上**完全看不出来**。

**一层 MoE 块 ≈ 71 GiB，一层 dense 块 ≈ 40 GiB。**
58 层 MoE + 3 层 dense ≈ **4,245 GiB ≈ 4.15 TiB**。

> ⭐ **对照：开了重算 106.75 GiB，不开 4.15 TiB —— 约 40 倍。**
>
> 这不是「优化一下」级别的差距。**在这个规模上，重算不是选项，是前提。**

⚠️ **这张表的三条边界，讲的时候必须说清楚：**

- 这是**按算子逐项推出来的估算**，不是从哪份报告抄的。不同框架的算子融合程度、
  是否顺手重算便宜算子、MoE 派发是否真的物化九份副本，差别都很大 —— **当量级看，别当准数**
- **注意力分数矩阵没算在里面。** 专题一算过它在 128K 上是 4 TiB／层。
  FlashAttention 压根不把它写进显存，所以它不出现在这张表上。
  **换成不带 flash 的朴素实现，这张表整个作废**
- batch 和序列长度是**直接乘进去**的。上面全部是 batch=1、128K 的数字 ——
  长上下文在这一项上最痛，这也是[专题五](专题05-并行策略.md)里上下文并行存在的理由

#### ⭐ 一个反直觉的发现：MLA 的压缩，在训练前向里不生效

回头看 MLA 那张表里最大的两项：Q 展开的 6 GiB、K/V 解压的 8 GiB。

MLA 的卖点是把 KV 压到 512 维 —— 专题一第 2 步算过，128K 上省了 **56.9 倍**。
但那省的是**推理时的 KV cache**。训练的前向里，attention 依然要拿
完整的 128 头 × 256 维去算，**压缩表示必须先解压回来**。

这一条是从官方参考实现确认的，不是推断：`kv_b_proj` 作用在每个位置的
`compressed_kv` 上，输出宽度就是 `num_heads × (qk_nope_head_dim + v_head_dim)` = 32,768。

> ⭐ **同一个设计，在推理的账本上是省，在训练激活的账本上不省。**
>
> 这是全课反复出现的那个主题的又一个实例：**参数量、算力、显存是三本账，
> 不能拿一本的结论去推另一本。**

#### ⚠️ 一个非常常见的误解：FSDP 不分摊这笔账

很自然会想：都上 FSDP 了，这些激活应该被摊到几百张卡上了吧。**不会。**

**FSDP 的契约是模型状态 —— 参数、梯度、优化器状态这三样。**
ZeRO 论文把显存分成两类：model states（就是这三样）和 residual states
（激活、临时缓冲、内存碎片）。**FSDP / ZeRO-3 处理的是前者；激活属于后者，不归它管。**

容易混淆的地方在于：FSDP 下每张卡确实只持有自己那几条样本的激活，看起来像是「分掉了」。
但那是**批次被分了**，不是激活被切了 —— **每条样本的激活，在它所在的那张卡上是完整的。**

上面那 4.15 TiB（或者开了重算的 106.75 GiB）算的是**一条 128K 序列**的账。
这条序列整个待在一张卡上，**加多少张卡都不会让它变小**。

> ⭐ **要把这笔账切开，得动另外一条轴：**
>
> | 沿什么切 | 叫什么 | 切掉的是 |
> |---|---|---|
> | token | 序列并行 / 上下文并行 | 那 1.75 GiB 里的序列维 |
> | hidden | 张量并行 | 子层内部的中间张量 |
> | 专家 | 专家并行 | MoE 那九份副本 |
>
> 这几条跟 FSDP 是**正交的**，可以同时开 —— 这正是[专题五](专题05-并行策略.md)的主题。

补一句精确性：DeepSpeed 的 ZeRO-R 里**确实**有一项 activation partitioning，
但它沿的是**张量并行那条轴**，不是数据并行。所以结论不变 ——
**FSDP 那条轴不切激活。**

### 1.3 通信：梯度要汇总

- 数据并行下每张卡算出的是**部分梯度**，要 all-reduce
- 通信量正比于**参数量**，与 batch 无关
- 这一条留给[专题五](专题05-并行策略.md)展开，这里只把它列进账单

---

## 二 · Recompute：第一个真正的「决策」

专题一刻意只讲事实。到这里，**第一个需要做选择的地方出现了**。

- **想法**：激活占太多显存，那就**不留了** —— 反向要用的时候现算一遍
- **代价**：多一次前向，算力 3× → **4×**（约 +33%）
- 这是一笔明确的交易：**用算力换显存**

**粒度有三档**，从粗到细：

| 档 | 怎么切 | 特点 |
|---|---|---|
| **全量** | 每层只留入口，层内全扔 | 一个开关，省得最多，也最贵 |
| **按层** | 只 checkpoint 一部分层 | 粒度受层数限制，模型一大就不够细 |
| **按算子（选择性）** | 逐个张量决定留还是扔 | 粒度最细，**但需要一个判据** |

下面 2.2 就是那个判据。它比想象中简单 —— **只有一个数**。

### 2.1 全量重算：这笔交易到底划不划算

先把量级说清楚 —— **这才是要记住的东西**：

| | | V3 在 128K 上具体是多少 |
|---|---|---|
| **省下什么** | 激活那一大块 —— 显存里**随 batch × 序列长度增长**的部分 | **4.15 TiB → 106.75 GiB**，约 **40 倍** |
| **付出什么** | 多跑一次前向：总算力 **3× → 4×** | 大约 **多三分之一** |

> ⭐ 一句话：**用三分之一的算力，换掉四十分之三十九的激活显存。**
>
> 这个兑换比例夸张到不像是个「权衡」。所以在大模型训练里，
> **默认就是开着的** —— 值得讨论的从来不是开不开，而是开到哪一档。
>
> 反过来说：显存本来就宽裕的时候（小模型、短序列），它就是纯亏。

### 2.2 ⭐ 选择性重算只需要一个指标：每省一字节要付多少 FLOPs

对每一个中间张量问同一个问题：**把它扔掉，反向时重算回来，每省一个字节要付多少次浮点运算？**

比值越小越该扔。就这一个数，整张排序表自己就出来了。

**对线性层，这个数有闭式解，而且漂亮得出乎意料：**

```
一个 [S,k] × [k,n] 的矩阵乘
  重算代价 = 2·S·k·n  FLOPs
  产出张量 = S·n·2    bytes（bf16）
  ────────────────────────────
  每字节代价 = 2·S·k·n ÷ (S·n·2) = k
```

> ⭐ **重算一个线性层的输出，每字节要付的 FLOPs 恰好等于它的输入宽度。**
>
> `S` 和 `n` 全约掉了 —— **跟序列长度无关，跟输出多宽也无关。**
> 所以「哪个线性层最该重算」这个问题，答案就是**看谁的输入最窄**，
> 不用算，扫一眼 config 就知道。

**但 attention 不服从这条规则** —— 它不是线性层，代价随 S² 涨而产出只随 S 涨：

```
代价 = S²·nh·(qk_dim + v_dim)   ← causal 已折半
产出 = S·nh·v_dim·2  bytes
────────────────────────────
每字节代价 = S·(192+128) / (128·2) = 1.25 · S      ← 随序列长度线性增长
```

> ⭐ **两条线，一条是常数（输入宽度），一条随序列长度线性上升。所以它们必然相交。**
>
> 对 V3：`1.25·S = 7168` → **S ≈ 5,734**。
>
> - **短于 ~5.7K**：attention 比大多数线性层还便宜，是个好的重算对象
> - **长于 ~5.7K**：它一路变成最贵的那个，而且是线性地越来越贵

⚠️ **5,734 是个粗略交叉点，跟专题一那个 98,688 是同一种东西 —— 不是工程阈值。**
它依赖 causal 折半的口径、依赖 V3 的头维度（192/128）、依赖你拿哪个线性层做对照。
**要记的是那句「两条线斜率不同所以必然相交」，不是这个数。**

### 2.3 V3 在 128K 上排出来的名次

一层 MoE 块、序列 131,072、batch 1、bf16。按每 GiB 的重算代价升序：

| 张量 | 省显存 | 重算代价 | **每 GiB 付** | 结论 |
|---|---|---|---|---|
| MoE 派发（复制成 9 份） | 15.75 GiB | ~0 | **0** | 白捡 |
| RMSNorm 输出（每层 2 个） | 3.50 GiB | 0.01 TFLOP | **~0** | 白捡 |
| SwiGLU 乘积 9 份（逐元素） | 4.50 GiB | 0.01 TFLOP | **~0** | 白捡 |
| K/V 解压（输入宽 512） | 8.00 GiB | 4.40 TFLOP | **0.55** | 划算 |
| Q 展开（输入宽 1,536） | 6.00 GiB | 9.90 TFLOP | **1.65** | 划算 |
| 专家输出 9 份（输入宽 2,048） | 15.75 GiB | 34.63 TFLOP | **2.20** | 划算 |
| gate / up / 路由 / 降维（输入宽 7,168） | 9.14 GiB | 39.08 TFLOP | **7.70** | 边际 |
| **attention 输出** | **4.00 GiB** | **703.69 TFLOP** | **175.92** | **绝不** |

> ⭐ **回答那个直觉问题：「有没有占显存很小、算力却很大的东西？」**
>
> **有，就是 attention，而且极端。** 它只占 4 GiB，重算一次要 703.69 TFLOP ——
> 比排在它前面那一档贵 **23 倍**，比最便宜的那三项贵到没法比（除数是 0）。
>
> **但结论是反过来的：正因为如此，它是最该「留着」的那一个，不是最该重算的。**
> 判据只有一条比值，比值高的留、比值低的扔。

**把比值小于 3 的全收下来**：一层省 **53.5 GiB**，付 **48.9 TFLOP**。
而一层前向总共 **857.1 TFLOP**。

> ⭐ **约 6% 的算力，换掉约 77% 的激活显存。**
>
> 对比全量重算：**33% 的算力，换掉 97%**。
> **选择性重算的性价比高一个数量级** —— 这就是为什么它值得单独配。

**为什么 attention 会这么极端**：一层前向 857.1 TFLOP 里，
attention 一项就占 **82.1%**。

> 📌 **这个 82.1% 是个交叉验证，不是新数字。** 专题一第 2 步那条曲线在 128K 处报的是
> **81.8%** —— 两边算法完全独立，对上了。
> **这是全课反复用的那招：找一个外部锚点，一次性验证整套公式。**

### 2.4 ⭐ 同一条判据，2022 年给出的是相反的答案

「选择性重算」这个概念出自 NVIDIA 的
*Reducing Activation Recomputation in Large Transformer Models*
（Korthikanti 等，arXiv **2205.05198**，2022-05）。

**它的判据跟 2.1 一模一样** —— 论文原话是：挑那些
「占显存不少、但重算起来不贵」（*take up a considerable amount of memory
but are not computationally expensive to recompute*）的部分。

**但它选出来要重算的，恰恰就是 attention** —— QKᵀ、softmax、softmax dropout、
以及 attention over V。理由写得很清楚：这些算子
「输入很大因而激活很大，然而每个输入元素上的浮点运算次数非常低」。

论文的收益：GPT-3 省 **70%** 激活显存，只付 **2.7%** 算力；MT-NLG 省 **65%**，付 **1.6%**。

**为什么结论会反过来？因为它的序列长度是 2,048。**

| | 序列长度 | attention 每字节代价 | 占一层算力 |
|---|---|---|---|
| GPT-3（论文） | 2,048 | 2,560 FLOP/byte | 约 2.7% |
| **V3（我们）** | **131,072** | **163,840 FLOP/byte** | **82.1%** |

代入 2.2 那条公式：`1.25 × 2048 = 2,560`，比 V3 任何一个线性层的输入宽度都小 ——
**在 2K 上，attention 真的是最划算的重算对象。** 论文没错，是前提变了。

> ⭐ **同一条判据、同一个模型族、判据一个字没改，结论翻转。变的只有序列长度。**

⚠️ **一处口径要说明白，不然数字对不上**：论文那个 2.7% 用的是
`(s/6h) / (1 + s/6h)` 这个 Megatron 传统公式，**没有为 causal 折半**。
换成本课统一的 causal 口径，GPT-3 的 attention 占比是 **1.37%**。
两个都对，只是约定不同 —— **跨文献比这类比例前，先确认对方折没折半。**

**顺带一句：这件事今天已经不用你操心了。** FlashAttention 天生就不把分数矩阵
写进显存、反向时现算，等于把论文那条建议**内建成了默认行为**。
所以现代框架的选择性重算候选名单里，attention 根本不出现。

### 2.5 ⚠️ 但收益不能照抄别人的

**我们在混元 3 上做过完整消融，最值得记住的一条不是某个数字，
而是这个现象：**

> **同一个模型、同一个开关，换一个规模，收益可能从正的变成负的。**

原因不神秘：重算改变的是**计算与访存的配比**，
而这个配比在不同的并行配置、不同的芯片数下本来就不同。

**由此推出一条通用规则**（这条要记住，它不只对 remat 成立）：

> ⭐ **凡是会改变数据分片形状的参数，都不能跨规模照抄。**
> 小规模上验过的结论，到目标规模上必须重验。

**2.4 那个例子说明这条规则比听起来更狠**：序列长度也在这个名单里，
而且它翻转的不是收益的大小，**是收益的正负号** ——
在 2K 上最该重算的那一项，到 128K 上变成最不该动的那一项。

**另一个值得知道的量级**：在真实的训练 profile 里，
**重算掉的那部分 FLOP 可以占到已执行算力的三分之一左右**。

> 这顺带解释了一个常见困惑：**为什么 MFU 看起来那么低** ——
> 分母里有一大块被重算吃了，它做了功但不算进"有效算力"。

**顺带**：host offload —— 把激活挪到 CPU 内存，
用 PCIe/带宽换 HBM。什么时候值得

---

## 三 · 优化器状态：真正的大头 ⭐

**这一节是整课的重心。**

### 3.1 一个参数到底要占多少字节

标准的 bf16 混合精度 + AdamW，**每个参数**在显存里的分身：

| 存的什么 | 精度 | 字节 | 为什么必须有 |
|---|---|---|---|
| 权重 | bf16 | 2 | 前向/反向实际用的那份 |
| 梯度 | bf16 | 2 | 反向算出来的 |
| **主权重（master weight）** | **fp32** | **4** | bf16 精度不够做累加更新，小的更新量会被直接吃掉 |
| **一阶动量 m** | **fp32** | **4** | AdamW 的动量项 |
| **二阶动量 v** | **fp32** | **4** | AdamW 的自适应缩放项 |
| **合计** | | **16 字节 / 参数** | |

> ⭐ **注意这三行 fp32**：主权重 + m + v = **12 字节**，
> 是那 2 字节 bf16 权重的**六倍**。
> **模型本身反而是这张账单上最小的一项。**

### 3.2 落到 V3 上

- 671B 参数 × 16 字节 = **约 10.7 TB**
- 其中纯优化器相关（master + m + v）= 671B × 12 = **约 8 TB**
- 对照一个 TPU v7 device 的 **94.74 GB**

> **光是把权重、梯度和优化器状态放下，就需要一百多个 device。
> 激活还一个字节都没算。**

这个数字就是**为什么必须并行**的最直接答案 ——
比专题一那个前向的账更硬。

### 3.2b ⭐ 但那 16 字节是「每张卡都有全套」的账 —— 切一刀就完全变了

上面那 16 字节，说的是**不分片**的情况：每张卡都自带完整的一套。
一旦开始切，账就完全不一样了 —— 而且**切到哪一级差别是数量级的**。

**671B、每参数 16 字节，看 N 张卡分摊之后每卡还剩多少：**

| 每卡常驻 | 公式 | 128 卡时 |
|---|---|---|
| **纯 DP** | 16 B × 全量 | 约 **10.7 TB** —— 荒谬 |
| **ZeRO-1**（只切优化器状态） | 4 B 全量 + 12 B ÷ N | 约 **2.7 TB** |
| **ZeRO-2**（再切梯度） | 2 B 全量 + 14 B ÷ N | 约 **1.4 TB** |
| **ZeRO-3 / FSDP**（全切） | 16 B ÷ N | **不到 100 GB** |

> ⭐ **看公式那一列，不用记数字。**
>
> ZeRO-1 和 ZeRO-2 的公式里都有一项**不带 ÷N** —— 那是全量常驻的部分。
> **加再多卡也压不下去，它是个地板。**
> 只有 ZeRO-3 把所有项都除掉了，才真正随卡数线性下降。
>
> **这就是为什么光有 ZeRO-1 不够，必须一路切到 ZeRO-3。**

**再往下一步**：ZeRO-3 下模型状态摊薄之后，对着单卡的 **94.74 GB** 一减，
看看还剩多少给激活：

| 卡数 | 模型状态 / 卡 | 单卡剩余余量 |
|---|---|---|
| 128 | 83.9 GB | **10.1 GiB** |
| 256 | 41.9 GB | 49.2 GiB |
| 512 | 21.0 GB | 68.7 GiB |
| 1024 | 10.5 GB | **78.5 GiB** |

> **所以「至少一百多张卡」是个假答案。**
> 真正的下限由**激活**决定，而激活随 batch × 序列长度涨。
> 这就把[专题一](专题01-一个-Token-的一生.md)那笔前向的账和这里接上了。

#### ⭐ 而且把这两个数放在一起看，会得到一个更硬的结论

- 1.2 节算过：**一条 128K 序列，即使开了重算，激活也要 106.75 GiB**
- 上表最后一行：卡加到 1024 张，**每卡余量最多也就 78.5 GiB**

余量的上限是单卡 HBM 本身，**88.23 GiB —— 永远小于 106.75。**

> ⭐ **也就是说：卡加到无穷多，这条 128K 序列也装不进任何一张卡。**
>
> 这不是估算，是个上界比较：106.75 > 88.23，中间没有可以调的旋钮。
> **FSDP 这条轴到此为止 —— 想跑 128K，就必须把序列本身切开。**
>
> 上下文并行不是「一种可选的优化」，它是长上下文训练的**准入条件**。
> 这条结论直接把[专题五](专题05-并行策略.md)从「还有哪些花样」变成「非做不可」。

### 3.2c ZeRO-3 下 bf16 权重根本不常驻 —— 它是流动的

上表 ZeRO-3 那一行写「16 ÷ N」其实还偏保守。
**bf16 那 2 字节在 FSDP 下连分片都不常驻，是用一层转一层的。**

这一条我们查过 PyTorch 源码，机制很干净：

- **常驻的是 `_local_shard`** —— 分片，且是**原始精度（fp32）**
- **`_mp_shard`**（低精度分片）在初始化时**立刻被 `_free_storage` 释放**，
  只有轮到这一层才 `_alloc_storage` + 一次 `copy_()` 隐式转成 bf16
- all-gather 的落点 `_full_param_padded` 是 bf16 的**完整一层**
- **all-gather 一完成，`post_unshard()` 立刻把 `_mp_shard` 释放掉**

官方对 `FULL_SHARD` 的描述逐字印证了这个生命周期：

> unshards (via all-gather) **before the forward**, reshards **after the forward**,
> unshards **before the backward** computation, and reshards **after the backward**.

也就是说 —— **前向走到某层：fp32 分片 → 转 bf16 → all-gather 成完整一层 → 用 → 全扔。
反向回到这一层：再转一次、再 all-gather 一次。**

任意时刻显存里的 bf16，只有**当前那一两层**的量，不是 61 层的量。

**代价是什么**（天下没有免费的）：
- 每层每方向多一次 fp32→bf16 的转换，纯访存
- 反向要**再 all-gather 一遍**（`SHARD_GRAD_OP` 可以不 reshard，
  用显存换掉这次通信 —— 又一个决策点）

> 📌 **讲课时要点破的一句**：
> 「每参数 16 字节」是个**教学用的朴素账**，用来砸出量级。
> 真实峰值要看**时间线**，不是把各项简单相加 ——
> 有些项是常驻的，有些项是流动的。**这个区分本身就是[专题五](专题05-并行策略.md)的入口。**

### 3.3 为什么主权重必须是 fp32

这一条值得单独讲，因为它反直觉：

- 训练后期，单步更新量相对权重本身**非常小**
- bf16 只有 8 位尾数 —— 小的更新量加上去会被**直接舍掉**，等于没更新
- 所以要留一份 fp32 的"真身"来累加，bf16 那份只是它的投影
- 这也解释了为什么 **FP8 训练难**：精度往下压的时候，
  哪些量可以压、哪些必须保留高精度，是有讲究的（那是另一课）

### 3.4 换个优化器，账单就变了

| | 状态大小 | 每参数字节（含权重与梯度） |
|---|---|---|
| **AdamW** | 2× 参数量（m + v） | 16 |
| **Muon** | **1× 参数量**（只有动量） | **约 12** |
| SGD + momentum | 1× 参数量 | 约 12 |

- **Muon** 用 Newton-Schulz 迭代对整个梯度矩阵做正交化，
  代替 AdamW 逐元素的二阶矩缩放 → **省掉了 v 这一整份**
- 671B 上这一份就是 **2.7 TB** 的差别
- ⚠️ 但它多了正交化的计算开销，而且不是所有参数都适用
  （embedding / head 通常还是走 AdamW）
- 待核实：Muon 实现里主权重是否仍需 fp32

> **优化器的选择是一个显存决策，不只是收敛速度的决策。** 这一点常被忽略。

---

## 四 · 这张账单直接决定了并行策略长什么样

**这一节是通向专题五的桥。**

把上面的账按大小排一下：

```
优化器状态（12 B/参数）  ████████████  最大
激活（随 batch × 序列长度涨）  ████████?   长上下文下可以超过它
权重（2 B/参数）        ██
梯度（2 B/参数）        ██
```

**ZeRO 的三级设计，正是照着这个顺序来的：**

| 级别 | 切什么 | 为什么是这个顺序 |
|---|---|---|
| ZeRO-1 | **优化器状态** | 它最大，而且**只在更新时用一次**，切了几乎不增加通信 |
| ZeRO-2 | 再切梯度 | 次大，反向结束时才需要完整的 |
| ZeRO-3（≈ FSDP） | 再切参数 | 最后才动它，因为**每一层前向都要用**，切了就得每层 all-gather |

> ⭐ **顺序不是随意的，是「谁最大、谁最少被用到，就先切谁」。**
> 理解了这张账单，ZeRO 的分级就不用背了。

同理：
- **重算**针对的是激活那一项
- **offload** 针对的是"暂时用不到"的那些项
- **CP** 针对的是激活里随序列长度涨的那部分

---

## 五 · 一个完整 step 的总账

把前向（专题一）+ 反向 + 更新合成一张表：

| 阶段 | FLOPs | 峰值显存贡献 | 通信 |
|---|---|---|---|
| 前向 | 1× | 激活一路累加 | （并行策略带来的） |
| 反向 | 2× | 激活逐步释放 + 梯度产生 | 梯度 all-reduce |
| 更新 | 极小 | 优化器状态常驻 | （ZeRO 下有额外通信） |
| **合计** | **3×**（重算则 4×） | | |

要回答：

1. 一个 step 总共多少 FLOPs
2. **峰值出现在哪个时刻** —— 这个问题比"总共多少"更有用
3. 全部加起来要多少 device

**顺带讲**：梯度累积 —— 用多个 micro-batch 攒一次更新，
在不增加显存的前提下放大有效 batch。它改变的是**时间线**不是总量

---

## 六 · 这个专题明确不讲

> ⭐⭐⭐ **2026-09-16 现场把范围收紧了，原话记在这儿：**
> 「我们主要是想讲那个**反向计算**，跟所有的**优化器**相关的东西。
> 因为推理里边它就没有反向计算 —— 你说反向计算它就是训练。
> 但是你也要**限制一下范围**。」
>
> ⭐ 这个划法比原来那条更准：**这一讲的边界不是「训练的账」，
> 而是「推理里没有的那些东西」**。判断某个题目该不该进来，
> 就问一句：**推理侧有没有对应物？** 没有的才归这一讲。
>
> ⛔ 判据（收范围时的通用做法）：**范围收紧之后，第一件事不是删，
> 是回头看界内还有什么没写完。** 这次一回头就发现 §1.8（梯度的一生）
> 是界内最正中间的一块，而它在 1.3 之后就断了。

- 具体的并行怎么切 → [专题五](专题05-并行策略.md)
- FP8 / 量化训练 → 精度那个专题
- **Scaling law / 该训多大多久 / 数据配比 —— ⛔ 明确不讲。**
  2026-09-16 现场：「这种东西不是我们关心的东西」。
  ⭐ 它们确实是训练的第一个决策，但**推理侧也有对等的规模问题**，
  而且它们跟反向、跟优化器都不沾边 —— 不在这条线上
- **节点容错 / 千卡运维** —— 跟反向和优化器无关，属于工程运维
- 收敛性本身、模型好不好 —— 这里只关心**资源账**与**能不能训下去**
  （⭐ 注意「能不能训下去」在 §六 是**在范围内**的：
  spike 和数值稳定性直接由反向与优化器的数值行为决定）

---

## 素材

| 要什么 | 在哪 |
|---|---|
| **ZeRO（训练主线论文，已进正课 §四）** | Rajbhandari 等，*ZeRO: Memory Optimizations Toward Training Trillion Parameter Models*，arXiv **1910.02054**（2019-10）。**§3.1 那个「每参数 16 字节」的经典口径出自它，§四 的三级切法也出自它** —— 同一篇既给了账单、也给了切法，所以它不是一条参考文献，是这一讲的骨架 |
| 优化器 | AdamW / Muon / per-head Muon 原论文 + 内部术语页 |
| **学习率那一节（§3.5）的全部数字** | 三处公开出处，逐条核过，**没有一个是凭印象写的**：① **GPT-3** 表 2.1 的七档峰值 LR ＋ batch、375M token 线性 warmup、2600 亿 token 余弦降到 10% —— arXiv **2005.14165**；② **DeepSeek-V3** 的 WSD 形状（2K 步 → 10T 恒定 → 4.3T 余弦 → 末段两级）、梯度裁剪 1.0、batch 3072→15360、**bf16 的一/二阶矩 ＋ fp32 主权重和梯度**、**重算 RMSNorm 与 MLA 上投影** —— arXiv **2412.19437** §3.2.3（重算）/ **§3.3.3**（bf16 一二阶矩 ＋ fp32 主权重与梯度，2026-09-17 订正，原写 §3.2.3）/ §4.2（超参）；③ warmup 的真实机制（**不是「样本太少估不准」**）—— arXiv **2406.09405**（NeurIPS 2024）；WSD 形状的系统分析 —— arXiv **2410.05192** |
| 混合精度与主权重 | 混合精度、loss scaling、matmul 精度 —— 内部术语页 |
| 重算与 offload | recompute / remat / host offload —— 内部术语页 |
| 选择性重算的出处 | Korthikanti 等，*Reducing Activation Recomputation in Large Transformer Models*，arXiv **2205.05198**（2022-05）。§2.4 引的判据原文、GPT-3 / MT-NLG 的 70%/2.7% 与 65%/1.6%，都出自这篇 |
| §2.2 那条闭式解与 §2.3 名次表 | **自己推的**，跟 §1.2 同一套输入。闭式解可以当场验证（约分而已），名次表沿用 §1.2 的估算前提 |
| 梯度累积 | 内部术语页（含 MaxText 的 GA loss 行为那个坑）。⭐ §1.8 已写完整五条 |
| **§6 数值稳定性的四条出处** | 全部公开、逐条核过原文：PaLM 的 spike 与消融 **arXiv 2204.02311** §5.1；router z-loss 与 Table 4 **arXiv 2202.08906**；QK-norm 与 8B 处发散 **arXiv 2302.05442** §2；Post-LN/Pre-LN 与 warmup **arXiv 2002.04745**（ICML 2020）。⛔ 这一节**刻意一个自己的实测都没有** |
| 我们的实测 | `tpu/Hunyuan3-295B-Pretraining/TUNING-v7.md`（remat 消融、显存实测） |
| §1.2 那两张激活表 | **自己按算子推的**，输入只有两样：V3 的 `config.json`（层数/宽度/专家数）+ 官方参考实现的 MLA 前向（确认 K/V 在训练里真的解压）。**没有第三方数字，所以也没有第三方背书 —— 讲的时候按估算讲** |

---

> ## 📌 2026-09-18 11:30 收口
>
> **停在这里。** cron 已撤（`9ae233ac0497`）。这一段一共推了 **18 次提交**。
>
> ✅ **已闭环**：R01–R22＋R22b（重画 ＋ 素材落地 ＋ figink 清账）、
> RQ1–RQ4a（质感改造的低风险四条）、**R28 讲稿同步**、**R30 收尾**。
> ——&nbsp;**教材与讲稿现在是一致的，31 个 figure 全有 cue，全站 build 绿。**
>
> ⬜ **剩下的全是锦上添花，随时可做也可以不做**：
> `R22c` 9 格重画（其中 2 格我自己判过「可以不改」）／
> `R23–R24` 4 段动画／`R25` 入口新图／`R26` 串故事线／`R27` 历史进讲义／
> `R29` 全量截图复看／`RQ4b` 字号收三档（**唯一会触发大量版面重排的一步**）／
> 讲稿那 2 处既有的顺序差异。
>
> ⭐ 判据：**一个没有终点的清单会一直长** ——&nbsp;
> 这一段最后是靠「把『必须的』和『锦上添花的』分开」才收住的，不是靠做完。

---

> ## 📌 2026-09-18 后记 · 「降维阶梯」动画 ＋ 顺手挖出四支片子都在跳
>
> 起因是那句「抄他的故事线、讲法、绘图代码，然后画自己会动的图」。
> 骨架取自 3b1b 神经网络系列 `part2.py` 的类名序列：
> **一维讲通 → 升到二维 → 主动承认想不出高维 → 于是不再画空间，改画一列数**。
> 落成 `topic04-anim-descend.py`（三幕 15.3 秒，数据全部当场算），挂在 §梯度下降那一格。
>
> ⭐⭐⭐ 但这一轮真正的收获是**验证方式本身出了错**，而且连错三次：
>
> | 我用的判据 | 为什么它是瞎的 |
> |---|---|
> | 首末帧**平均像素差** 1.26 → 判「一致」 | 画面 98.5% 是白底，差异被稀释两个数量级 |
> | 换成**差异像素 ÷ 墨水** | `saddle` 只转了半度就量出 51.2%，跟「差着一整幕」同档 |
> | 再换成**墨水总量相对差** | `memtime` 那条恒定灰带占 19 万像素，把真变化稀释成 17.4% |
>
> ⭐⭐⭐ 三次是**同一个病换皮**：**全局聚合指标被不变的大多数主导。**
> 结论不是再换一个指标 ——&nbsp;**一个标量分不开「末帧是另一幕」和「末帧偏了一像素」，
> 而人眼一秒就能分开。** 所以工具最终做成**基线回归 ＋ 必落一张上下拼接图**：
> 只问「有没有比记录的更糟」，判定交给人，判完的结论写进 `loop-baseline.json` 的 `note`。
>
> ⛔ 顺带发现：**当时在线的三支动画全都在跳，而从没人量过** ——
> 因为「构建全绿」一直只覆盖单帧的几何与文字，**从没覆盖过产物的时间维度**。
>
> 三支的根因各不相同，但修法指向同一条判据：
> - `reverse` 32.7% → 22.7%：复位**在四个绘制函数里各写了一份**，漏了两份。
>   ⭐ **同一条不变量复制 N 份，N 越大越必然漏** ——&nbsp;改成在时间轴上做一次映射，
>   复位段直接把时钟拨回 0，「末帧 ≡ 首帧」就**按定义成立**。
> - `memtime` 18.2% → **1.7%**：内容是累积的，**根本没有复位段**。
>   清零只会把那一跳从接缝挪进片内，所以改成**时钟倒着走回去**，山退潮、扫描线滑回左边。
> - `descend`：`FadeOut` 会把 mobject **移出 scene**，`set_opacity(1)` 救不回来；
>   而 `self.add` 是**追加到最上层**，加回来反倒让曲线盖住了球。
>   ⭐ **「加回 scene」和「加回原来的图层」是两件事** ——&nbsp;按最初那一行原样重新
>   `add` 一遍即可（`Scene.add` 会先去重再追加），比去找 `add_to_back` 稳。
> - `saddle` 51.2% 原样保留：**数字最高，问题最小**（半度旋转 × 棋盘格纹理）。
>   ⭐ 判为良性局限，不改。
>
> ⭐⭐ 另一条顺手固化的：图注里那句「N 秒无声循环」也会过期 ——
> 给 memtime 加回退段之后片长从 12.8 变成 14.6，图注一声不响就错了。
> 已做成 lint。⛔ 第一版正则写成「左括号紧挨着数字」，
> 于是两条过期只抓出一条：**锚点要选语义短语，别选标点**，标点的位置随排版漂。
>
> 全部接进 `build-all.sh`（1.4 秒，只读 mp4 不重渲）。

## ⭐⭐⭐ T 轮：十份评审驱动的二十轮打磨（2026-09-19 02:55 HKT 起）

> ⭐ 现场：「用 5 个麻瓜去学习你的课程，再用 5 个专家，分别提修改意见。
> 然后根据这个修改意见，**逐轮的去迭代，再来个 20 轮**。精益求精嘛，
> **要抓住自己的故事线，要做到讲述的跌宕起伏。**
> 这个**前世今生**，要让大家彻底了解 —— 是怎么实现？这个 **loss 到底是怎么算**？」
>
> ⚠️ 原话里有半句我没听清（语音转写成「大模型训练的医生是钱龙」）。
> **按「训练的前世今生 ＝ 一部把钱变成模型、又一步步少烧点钱的历史」来理解**，
> 并以此作为全课主轴。⛔ 这是我的假设，不是他说的原话 —— 等他确认。

### ⭐⭐ 两条贯穿全部二十轮的主线

1. **故事线要跌宕起伏**：每一节都要有「抛出问题 → 给出答案 → 留下钩子」。
   钩子断掉的地方就是要修的地方。
2. **「loss 到底怎么算」要串成一条主线**，不能散落在几个小节里。

### ⛔ 一条已定的编辑约束：基准统一到 4K

> 原话：「一直都是拿 **4K** 这个 sequence length 来讲，因为现在训练大家都是用
> 4K 来训的，之后再扩训那部分我们不管。**以 4K 长度为基准去讲解。**」

⚠️ **但 128K 在本讲里是承重的**，不能一刀切删。开工前先看清这三处：

| 现在用 128K 的地方 | 4K 上会怎样 |
|---|---|
| recompute「每 GiB 付多少 TFLOP」排名表（S=131072 算的） | **排序会变**，需要重算 |
| 「2K 上最该重算的，到 128K 变成最不该动的」 | **需要两端对比才成立** —— 这是 128K 该留的地方 |
| 「`6ND` 在 128K 上错，因为 attention 占 82.1%」 | 按 attention ∝ S 外推：4K 时 att/lin = 4.59 × 4096/131072 = 0.143 → **attention 只占约 12.5%**，这一段的性质变了 |

⭐ 所以方向是：**默认视角切到 4K，128K 只保留在「对比点」上并标明是扩训场景。**

### ⭐⭐ 新加的一整块内容：batch 大小 ↔ 收敛与稳定性

> 现场原话（语音）：「还有 **sequence loss 跟训练稳定性的关系**，就是那个 TPS，
> 那个 **global batch size 为什么大了就不行**。就是相当于这个 **TP 开得太大**的时候，
> TPS 就会很大，那样的话**对于收敛有什么影响**？GPS 如果开小了的话，对于收敛有什么影响？
> 对这个 **micro batch** 这种，就是还有什么 **GA** 呀这种。
> **为什么 batch 开大了训练就会稳定**？等等，这里的理论也都讲讲。」

⛔ **两个缩写语音转写不清，不替他猜 —— 按「两种读法都覆盖」来写，落地前当面确认：**

| 听到的 | 我的判断 | 怎么处理 |
|---|---|---|
| **TPS** | 三种可能：① tokens per step ② TP size ③ tokens per sequence。他说「TP 开大 → TPS 就会很大」，而 **TP 开大在固定卡数下会让 DP 路数变少、global batch 变小**，所以「TP 大 → TPS 大」在 ①③ 两种读法下都不成立 | ⛔ **这一条不要照着写。** 先写清「TP / DP / micro-batch / GA 四个旋钮各自怎么决定 global batch」，让他自己指认哪个是 TPS |
| **GPS** | 几乎可以确定是 **GBS = global batch size**（同一句里他前面刚说过 global batch size） | 按 GBS 写 |

⚠️ **他这段话里有一处表面矛盾，而这恰好是这一块最值得讲的东西**：
前半句问「global batch **为什么大了就不行**」，后半句问「为什么 batch **开大了训练就会稳定**」。
**两句都对，讲的是同一条曲线的两段** —— 这就是这一节的故事线：

1. **往大了开：稳。** 梯度是一批样本的平均，batch 越大梯度噪声越小
   （标准误 ∝ 1/√B），loss 曲线越平滑、越不容易被一个坏批带飞。
   ⭐ 这一条接得住 §1.2 的 `fig-vote`「一屋子人各提各的要求，最后取平均」——
   **人越多，平均越不受一个偏激分子影响。**
2. **但开过头：每个 token 的收益开始塌。** 存在一个「临界 batch」——
   超过它之后再加样本，梯度已经够准了，噪声不再是瓶颈，
   **多出来的算力换不回等比例的进步**。这时候增大 batch 只是在烧钱。
3. ⭐⭐ **所以「大了就不行」和「大了才稳」不矛盾：稳定性单调受益，样本效率有拐点。**
   两条曲线在同一张图上，交点就是你该选的 batch。

**必须讲到的机制（每一条都要落到本讲已有的账上）：**

- **micro-batch × GA（梯度累积）× DP 路数 ＝ global batch** —— 三个旋钮乘出同一个数。
  ⭐ 本讲 §1.8 第④道泳道已经讲了 GA，但**没说它跟 batch 的关系**，
  也**没说累积时要不要除以 K**（E2 报的：不除等于把学习率乘了 K —— 这是 GA 最经典的 bug）。
- **TP 为什么会牵动 batch**：固定卡数下 `DP = 总卡数 ÷ (TP × PP × EP)`，
  TP 开大 → DP 变小 → **同样的 micro-batch 下 global batch 变小**。
  所以「并行策略」和「收敛」不是两件事，**TP 那个旋钮的另一头连着学习率**。
- **batch 和学习率要一起动** —— 本讲 §3.5 已有 GPT-3 那八档表（batch 越大 LR 越小）
  和「batch-LR 上界」那段，但**没有把它和稳定性接起来**。这一块正好是那段的下半截。
- **sequence loss 这个量本身**：一个 batch 的 loss 是「所有 token 的 −ln p 取平均」，
  ⭐ 而**按 token 平均还是按序列平均，在变长 batch 上给出不同的梯度** ——
  这条 E2 也报了，正好是「sequence loss 跟稳定性的关系」最直接的一个落点。

⭐ **放哪儿**：跟 §3.5（学习率）和 §6（训不崩）是同一条线，
E4 的重排方案已经建议把这两节合并成「幕七 · 真的坐在机器前面」——
**这一块就是幕七缺的那一节**，不用另起炉灶。

### ⭐ 十份评审已全部落盘 → `Courses/reviews/`

⛔ **T01 是一个全新的 turn，看不到当时的对话** —— 所以评审原文必须从磁盘读，不能靠回忆。

| 先读 | 说明 |
|---|---|
| **`reviews/_CONVERGENT.md`** | ⭐⭐⭐ **从这里开始。** 被 2 位以上互不通气的评审各自撞到的问题，按 A/B/C/D/E 分级，A 级四条我已独立 grep 复核过 |
| `reviews/README.md` | 为什么有这个目录、怎么用这批材料 |
| 其余 16 份 | 原始输出，未加工。⛔ **认它报的现象，不认它给的根因** |

⚠️ **文件名是按内容起的，不是按当初派活时的眼镜名** ——
我试过用关键词把 agent 日志映射回眼镜名，**四份里错了三份**
（"KV cache"、"梯度消失"这类词在别的专题里一样高频，匹配到了专题三的评审）。
所以改成按每份自己的标题命名，`X-` 前缀表示它是这一批里的额外视角。

### 轮次表

### ✅ A 级已在 T01 之前先修掉（2026-09-19 03:1x–03:4x HKT）

这三条都是**多位评审各自撞到 ＋ 我独立 grep 复核成立**，且改动机械、零判断，
所以没等轮次直接做了。⛔ **T01 别再排它们。**

| 做了什么 | commit |
|---|---|
| 全课落点 `<h3>` 开标签整个丢了（h3 开 40 闭 41）→ 补上，框架自动补出 `id="s6-7"` | `e0681d4` |
| 七处交叉引用指错小节（6.4→3.7、6.7→6.6、1.2→1.7 ×3），顺带把节级锚点升成精确小节 | `e0681d4` |
| §4.1 空支票 → 由常量生成的账本表（81 字符 → 781），接住 §4.2 的「照着这个顺序」 | `934c853` |

⭐ **每一条都配了断言，防的是整类问题不是这几处**：
标签开闭配对（少一个开标签会**静默渲染成正文、构建不报错**）；
站内锚点必须落地（**查成品不查中间变量** —— 小节 id 是 `P.finish()` 才补的）；
§4.1 的表与散在 §5.1 的手写数必须对得上。

⛔ **一条没修、留给 T 轮**：E5 报的 20+ 处**论文节号被自动链成本页锚点**
（`arXiv 2005.14165 §2.3` 点下去跳到本讲 §2.3）。这些锚点**有效**，
所以上面那条断言抓不到 —— 要改的是自动链接器的边界，不是链接本身。
最讽刺的一处：正文正在说「原文分节是…不是 §3.2.3 那个重算小节」，
而这句话里那个 §3.2 本身就是错链。

- [x] **T01** ✅ 2026-09-19 收齐十份评审 → 合并去重 → **重写下面 T02–T20 的具体内容**
      （这一轮的产出就是一张真正的清单；在它做完之前，下面几条是种子）
      ⭐ 起手先读 `reviews/_CONVERGENT.md`，A 级四条可以直接进 T02
> ⭐ **排序原则**：不改就会让读者**记住一个错误事实**的排最前；
> 结构塌陷次之；叙事重排再次之；图和讲义垫后（因为它们要等内容定稿）。
> ⛔ 每一轮开工前**先 grep 复核**评审报的那条还在不在 —— 前面几轮可能已经顺手改掉了。

#### 第一批 · 不改就会教错（T02–T05）

- [x] **T02** ✅ 2026-09-19 **4K 基准 · 宣布 ＋ 激活账**
      §0 加一行基准声明（V3 预训练真实配置：序列 4,096、global batch 3,072 条，
      长上下文扩训不在本课范围）；§1.7 那两张激活表按 4K 重算
      （每层 71 GiB → **约 2.2 GiB**，全模型 4.15 TiB → **约 133 GiB**，÷32）；
      §5.1 的分水岭改用 4K 表述。
      ⛔ **推导链要写出来**：激活只认 token 总数（§5.1 自己的结论），所以线性缩放。
      ⭐ 三人各自算出同一批数，见 `reviews/_CONVERGENT.md` C 级表。
      ⛔ **§1.7 边界②「注意力分数矩阵 4 TiB/层」在 4K 上是 4.0 GiB/层，
      仍比该层其余全部激活（2.22 GiB）大 —— 这条论点照样承重，不要顺手删掉。**

- [x] **T03** ✅ 2026-09-19 **4K 基准 · attention 重算的结论翻面**（全讲最值钱那张图）
      ⛔⛔ **T01 这条写过头了，T03 落地时当场更正**：4,096 确实落在交点 5,734 左侧，
      attention 从 175.92 降到 **5.50 TFLOP/GiB**、排名从第 8 升到第 7、比 gate/up（7.70）
      便宜 29% —— **但 5.50 仍然高于本讲自己那条「小于 3 全收下」的切线**，
      所以它**并没有变成「该重算」**，只是从「绝不」变成「边际」。
      ⭐ 变的是「它还是不是极端离群值」（事实），不是「它该不该重算」（看你把刀切在哪）。
      判据已写成断言：`_att_base > CUT`，这条前提变了就会炸。
      §2.3/2.4/2.5/7.0 全部要标场景。
      ⭐⭐ **最好的改法（M3 提的，改完反而更强）**：`fig-per-byte` 把 V3 工作点竖线
      从 131,072 挪到 **4,096**，另加一根虚线标「长上下文扩训 → 128K」——
      于是那个翻转变成**同一个模型在扩训阶段内部发生的**，比跨模型比更有说服力。
      ⛔ **`fig-per-byte` 里 `assert S_V3 > CROSS` 会炸，要改成 `<`。**
      ⛔ §2.3 表前七行是线性层、闭式解里 S 约掉了，**这份名单在 4K 上一字不变**，
      唯一会挪的是 attention 那一行。补一句就能让「1.9% 换 77%」从 128K 专用变普适
      （4K 上那个数字是 **9.3%**，「性价比高一个数量级」只剩 2.8 倍，措辞要改）。

- [x] **T04** ✅ 2026-09-19 **峰值漏了「正在重算的那一层」**
      `106.75 GiB` ＝ 61 × 1.75，只有存档点；全量重算峰值还要加上当前在算那一层
      的完整激活（一层 MoE 块 71.14 GiB）→ **177.89 GiB**。
      ⭐ **这不是外加口径 —— §5.4 的 125M 例子和 §2.2 图 Ⓒ 都用的是正确口径**，
      只有 671B 这个头号数字没做。连带：40 倍 → **23.9 倍**、2.5% → **4.2%**、
      94 条 → **56 条**。三人算式一致。
      ⛔ 这是全篇唯一一个**照抄会把集群配小 67%** 的数。
      ⛔ **连带 §5.1**：T02 已把分水岭改成 4K 优先（1,230 万 token ＝ 3,000 条），
      并补上了「V3 自己的 batch 从第一步就坐在线上、后期越过 5.1 倍」那个反转。
      T04 把 106.75 改成 177.89 之后，**那几个数要一起重算**
      （分水岭降到约 733 万 token ＝ 约 1,790 条 4K）—— ⭐ 反转只会更强，但数字必须对。

- [x] **T05** ✅ 2026-09-19 **§5.4 那句「比例跟 671B 上那笔几乎一样」是错的**
      小例子 6.7×，671B 那边 40×（修完 T04 是 23.9×），差 3.6–6 倍。
      两个原因：(a) 两个模型的「一层激活 ÷ hidden 宽度」比不同（40.7 vs 17）；
      (b) 两处口径不一致（同 T04）。
      ⭐ 「多付 33%」那半句是对的且尺度无关，只有「省 6.7 倍 ≈ 40 倍」那半句不对。
      ⛔ 这句话正是 §5.4 存在的理由，而它被同一份材料上一节的数字当场证伪。

#### 第二批 · 现场点名的两条主线（T06–T09）

- [x] **T06** ✅ 2026-09-19 **loss 主线 · 新开 §1.0「loss 到底是一个什么数」**
      放在 §0 和 §1.1 之间，约 800 字 ＋ 1 图。四步，每一步都是后面某节的挂钩：
      ① **loss ＝ 惊讶程度**，−ln p。⭐ 立刻给两个可验锚点：随机初始化的模型对
      V3 那个 **129,280** 词表均匀猜 → ln(129280) ＝ **11.77**；训好 → 2.x。
      ✅ 11.77 已核（专题一里 129,280 出现 31 次，含「词表矩阵 129,280 × 7,168」）。
      ⛔ **「训好 2.x」我没有 V3 的官方数字，所以没把它写成实测值** ——
      改用 `困惑度 ＝ e^loss` 这个纯恒等式做刻度尺（不需要引任何人的数），
      表里那一行明写「公开模型的大致量级，当参照看」。
      ⭐ 意外收获：刻度尺上端 loss 11.77 的困惑度**正好等于词表 129,280** ——
      「在整个词表里犹豫」就是瞎猜的定义，这把尺自己闭合了。**这两个数是全课的 loss 刻度尺**，治三处：
      §6.1「飞多少算飞」、§6.3 那张表的分母、§3.5「loss 下得慢」到底多慢。
      ② 一个 token 一个 loss，一个 batch 取平均 → 接 §1.2 的 `fig-vote`。
      ③ **loss 是一个数** → 交棒 §1.5（「几千亿进、一个出」现在有一整格来立，不再是旁白）。
      ④ 单位是 **nats/token** → 交棒 §3.3 的量纲推导。

- [x] **T07** ✅ 2026-09-19 **新内容：batch 大小 ↔ 收敛与稳定性** → 落成 §3.5b
      ⛔ 现场新加，**位置不动**。两句表面矛盾的话是同一条曲线的两段：
      稳定性单调受益（噪声 ∝ 1/√B），样本效率有拐点（临界 batch）。
      必须落到本讲已有的账上：micro-batch × GA × DP ＝ global batch；
      TP 开大 → DP 变小 → global batch 变小（**并行那个旋钮的另一头连着学习率**）；
      接 §3.5 的 GPT-3 八档表。⛔ **TPS 那个缩写标着待确认，不要替他猜。**

- [x] **T08** ✅ 2026-09-19 **loss 主线 · 递进 ＋ logits 进账**
      §1.2 加「对 −ln p 求导正好得到 p − y」（"预测减真值"从此有来处）；
      §3.3 量纲那段钉死单位；§3.5 第③条判据加绝对刻度；§6.1 加一把尺
      （「跳回 ln(V) 就是模型忘光了」）；§6.3 加脚注（−1.755 是对数似然不是 loss）。
      ⭐⭐ **同时把 logits 那一行补进账**：§1.7 两张表里**没有词表这一维**。
      4K × 129,280 × 2 B ＝ **1.01 GiB/序列**（fp32 softmax 翻倍）；
      §5.4 的 125M 例子里 8,192 token × 50,257 × 2 B ＝ **0.77 GiB**，
      **是它自报总量 0.32 GiB 的 2.4 倍** —— 而那一节的卖点就是「你能自己算一遍」。
      ⭐ 真实实现必须分块算 loss，这恰好是个该讲的工程点。
      ⛔ 还要交代**归一化按 token 还是按序列**（变长 batch 上给出不同梯度），
      以及 **GA 累积要不要除以 K**（不除等于把学习率乘了 K —— GA 最经典的 bug）。

- [x] **T09** ✅ 2026-09-19 **事实批一：数字对不上自己**
      ① GPT-3「最宽的线性层 12,288」→ 按本讲自己的判据是**输入宽度**，
      MLP 降维输入是 4h ＝ **49,152**。⛔ 典型的「老常识套新架构」，
      而且正好出现在教读者**别这么干**的那一格（`fig-per-byte.py:45` 注释写着 `＝ d_model`）。
      结论方向不变（2,048 离得更远），但那个数和那条线画错了。
      ② GPT-3 表 2.1 是 **8 档**，§7.1 和 `fig-lr-curve` 图注写「七档」。
      ③ `fig-underflow`「33 个数量级」→ 实际画的那对端点是 **30.7**
      （33.7 是拿 fp16 正规数下界量的，但同句自己列了次正规数到 5.96e−8）。
      ④ bf16「一格」两个值：§3.2 图 Ⓑ 是 1/128（ulp，对），§6 判据①写 ≈1/256（半格）。
      ⑤ ⛔ **实际比评审报的更严重**：整张表八行合计 66.64 GiB，而一层 MoE 是 71.14 ——
      缺 4.50，改成评审说的 10.08 也只补上一部分。用 §1.7 的宽度表重导之后闭合了：
      候选池 284,224 ＋ 入口 7,168 ＝ 291,392 ＝ 一层，那一行的正解是 **9.70**。
      ⭐ 而池子在 128K 上正好 69.39 GiB，跟正文早就写着的「可重算池 69.4」对上 ——
      **分母一直是对的，只是表少列了三行**（MLA 的两个 norm 输出 ＋ Q 降维那份 LN 输出）。

#### ⛔⛔⛔ 2026-09-19 06:57 现场改口径 —— **后面所有轮次以这条为准**

> 原话：「那个**瘦身很重要**，对于故事线没有帮助的，就给它折叠起来，或者是把它删了，
> **你自己决定**。然后**前面的专题讲过的内容，也不用再深入地讲，就引一下**。
> 注意**控制深度**，我们是要讲：**① 反向传播的原理 ② 梯度下降的原理 ③ loss 怎么算**。
> **一切关于能不能放得下、能不能以并行的方式之类的，那都是后面的专题要讲的东西。
> 别跑题了，专注。**」

**⭐ 先量了一遍再动手（成品 78.7K 字）：**

| 归属 | 小节 | 字数 | 占比 |
|---|---|---|---|
| ★ 核心三件事 | §1.0–1.6（反向 ＋ loss）、§3.3–3.6（梯度下降/优化器） | 34.0K | 43% |
| ⛔ **放得下 / 并行** | §2 重算 11.2K、§5 总账 6.8K、§1.7 激活账 2.7K、§4 并行 2.2K、§3.1 16 字节 1.5K、§3.7 checkpoint 1.2K | **25.5K** | **32%** |
| △ 边界 | §1.8 通信/GA、§3.2 精度、§3.8 LoRA | 6.0K | 8% |
| 其余 | §0、§6 训不崩、§7 附录 | 13.1K | 17% |

**⭐⭐⭐ 我的判断（他说「你自己决定」）——&nbsp;三条：**

1. **主线要换。** 现在的脊梁是「**账**」（§0 就是「哪个最大」），而账正是他说的
   「能不能放得下」。⭐ 换成「**一个 loss 值怎么变成三千亿个参数更新**」——
   ⛔ 这跟 E4 的重排提议**是同一个方向**（`reviews/E4-narrative.md` 第七节），
   两边独立得出同一个结论，可信度高。
2. **折叠，不删。** 这批内容质量高、数都核过，而且**正是专题五要用的**。
   用 `details` 折叠 ＋ 一句指针（「这笔账专题五会摊开，这里只要结论」）。
   ⛔ 唯一真删的是纯重复和口径辨析那类。
3. **核心要更深不是更浅。** §3.3「梯度是怎么变成更新的」是「梯度下降的原理」的正主，
   现在 8,917 字塞了四个独立主题，读者会糊。**拆开讲透，字数可以不降反升。**

**⛔ 判据：一节留不留，问「它服务于① 反向 ② 梯度下降 ③ loss 这三件事的哪一件」。
答不上来就折叠。**

#### 第三批 · 换主线与瘦身（T12–T16）

- [x] **T12** ✅ 2026-09-19 **换主线 ＋ 立范围**
      ① hero ＋ §0 从「四样东西哪个最大」改成「**一个 loss 怎么变成三千亿个更新**」。
      ⛔ 现在 §0 的悬念本身就是个显存问题 —— 它把读者的注意力从第一屏就带偏了。
      ② §0 加一格**明确本讲边界**：这一讲只讲那三件事；
      「装不装得下、怎么切到多少张卡上」**专题五**，并给指针。
      ⭐ 这一格同时解决 T13 原来要修的「hero 拿前作当开场白」。
      ③ ✅ 扫过了 —— **对前作的引用本来就已经是一句话**（「专题一核过」「专题一独立算出 81.8%」），
      唯一在展开的是 §5 开头「把前向（专题一）＋反向＋更新合成一张表」，已改成指针。
      ⭐ 真正的问题不在引用长度，在**整条脊梁**：改完了。

- [x] **T13** ✅ 2026-09-19 **§2 重算：正文可见 11.2K → 4.5K（折掉 62%）**
      ⭐ **保留**（它是 §1.6「激活扔不掉」的直接答案，属于反向的因果链）：
      「反向要激活 → 激活扔不掉 → 那就扔了再算回来 → 这是本课第一个真正的取舍」
      ＋ 那条判据（每字节代价 ＝ 输入宽度）—— **判据是可迁移的思维方式，留**。
      ⛔ **折叠进 `details`**：八行排序表、4K/128K 翻转的完整推演、causal 折半口径、
      §2.5b 三条副作用、§2.6 MFU/HFU。折叠块开头写一句
      「⭐ 这些是专题五的预习，本讲不要求」。
      ⚠️ `fig-per-byte` **留在正文**（它讲的是判据本身，不是账）。

- [x] **T14** ✅ 2026-09-19 **§4 并行 ＋ §5 总账：可见 9.8K → 5.1K**
      ① §4 整节压成 3–4 句 ＋ 指向专题五。⛔ 现在它只有 2.2K 却占着一个大节的位置，
      而 hero 承诺的「ZeRO 三级不用背」本身就是并行的事 —— **承诺要撤掉**。
      ② §5 折叠掉总账、6ND、两个峰。
      ⭐ **但 §5.4 那个 125M 小例子要留，而且前移** —— 它是全讲唯一能手算的例子，
      服务的是「loss 怎么算 / 梯度下降在真机器上长什么样」，不是显存账。
      ⛔ 前移后要把它的落点从「省多少倍」改成「**这就是你笔记本上那个训练循环**」。

- [x] **T15** ✅ 2026-09-19 **§1.7 / §3.1 / §3.7 折叠 ＋ §1.8 拆半**
      §1.7（作者自己写着「可以整段跳过」）、§3.1（16 字节）、§3.7（checkpoint）
      → 全部折叠，正文各留一句结论。
      §1.8 拆开：**五道泳道留在正文**（它是「梯度算出来之后还要过什么」，属于反向），
      通信量与 batch 那半段折叠（并行）。

- [x] **T16** ✅ 2026-09-19 **§3.3 拆开讲透 → 3.3 ＋ 3.3b**
      现在 8,917 字塞了四个独立主题，而标题承诺的「折算系数」要到 55% 处才出现。
      拆成：**3.3 为什么顺着梯度走就能变好**（fig-descent / 鞍点 / 动量 / Polyak 1964）
      ＋ **3.4 那一步该迈多大**（一个学习率不够 → 量纲 → 0.2294 → Kimi K2 当接口）。
      ⭐ 这是「梯度下降的原理」的正主，**要比现在更深，不是更浅**。
      ⭐⭐ 顺带把 T14 原计划的三处高潮提级并进来：16＝16 的反转、0.2294、fig-prepost。

#### 第四批 · 收尾（T17–T20）

- [ ] **T17** ⭐ **收尾回环 ＋ 前世今生**
      落点前加回环（开篇那个问题 → 现在能回答了）＋ loss 收口那一句。
      补两段历史：**反向传播自己的前世**（§1.5 的正向/反向模式就是自动微分那条线，
      ⛔ 年份归属必须查原文）、**Adam 被扳倒过那次**。
      ⛔ 混合精度 fp16→bf16 那段**降级成一句**（它属于数值，不是这三件事）。

- [ ] **T18** ⭐⭐ **讲义对账**（✅ 最急那条 T11 已修）
      剩下三处：Adam update「≈1」应为 0.2；「七档/五百倍/差五倍」vs「八档/1,400 倍/10 倍」；
      时间表说四条判据在第六节、实际在第五节且第五节明写「这是全讲真正的收尾」。
      ⭐ 回填两段讲义强于课件的：WSD 为什么赢、12 字节的根子在 `fig-onelr`。
      ⛔ **讲义要跟着新主线重排**，不能只改数字。

- [ ] **T19** ⭐ **图 ＋ 那条链接 bug**
      ⛔ 先做一条老账：**20+ 处论文节号被自动链成本页锚点**
      （`arXiv 2005.14165 §2.3` 点下去跳到本讲 §2.3）。这些锚点有效，
      所以 T01 加的锚点断言抓不到 —— 要改的是自动链接器的边界。
      §0 新悬念图（配合 T12 换主线，**不再是四根条**）；
      `fig-step` Ⓑ 的量名错（同时在场的是 DP × micro-batch 不是 global batch）；
      `fig-recompute` Ⓒ 主视觉与结论相反 ＋ √L 前提在 V3 上不成立；
      三处内部图 id 泄漏；七段动画 `aria-label` 与图注核一遍。
      ⛔ 折叠掉的那几节，它们的图要跟着进折叠块。

> ⛔ **改完必须回头看产物**：T16 里我用了一个不带断言的 `s.replace()` 改标题，
> 它**一声不响地没匹配上**，而 build 照样全绿 —— 是回头查渲染结果才发现的。
> ⭐ 判据：**每一次字符串替换都要 assert 命中次数。**「回执说会发生」不等于发生了。

> ⛔ **量折叠比例时别用 `<details.*?</details>` 非贪婪正则** —— foldfig 里嵌着 figsrc，
> 内层先收尾，外层剩下的会被算成「可见」。我因此一度以为 §4.3 没折进去。
> ⭐ 按 `<details` / `</details>` 数深度扫。

- [ ] **T20** 收尾：全链 build ＋ 逐张截图复看 ＋ `index.html` 同步 ＋
      附录 §七进吸顶目录 ＋ `cron-tool.py remove 02dbdc11bf42` ＋ 汇总

#### ⭐ 没进轮次表、但记下来的（够不上一轮，顺手做掉）

- CSS 里约 100 行死代码（`.console` 滑块 / `.journey` / `.guess` / `body.batched`），
  正文一个对应元素都没有。⭐ M4 指出：那套 CSS 暗示的恰恰是一个
  「可以调序列长度、页面上的数跟着变」的控件 —— **真做出来的话，整个 4K/128K 问题会自动消失**。
  （⚠️ 但那是一个新功能，不是一轮打磨。想做要单独立项。）
- 「贵」全程只按 FLOPs 算，**一个字没提访存** —— 而那才是反向在墙钟上常超 2× 的原因。
- `fig-vanish` Ⓒ「ReLU 治好了梯度消失」是 2012 年的答案；§1.7 表里是 SwiGLU，
  §6.5 说真正扛住深度的是残差＋归一化位置，两处隔 4000 行没有一句话连起来。
  ⭐ 一句回指就够：「fig-vanish 那条红线每层乘 0.8；Post-LN 那条每层乘 1/√2 ＝ 0.707。
  **它不是像那张图，它就是那张图。**」
- 「既然优化器状态最大，为什么不用 SGD？」全讲没回答（SGD ＝ 0 份状态，省 75%）。
  答案其实在 `fig-onelr`，但两张图隔 1000 行。
- 「重算就是拿时间换空间」漏了最反直觉的一半：省下显存可以开更大 batch / 更少切分，
  **重算经常是净赚时间的**。
- ⛔ 「重算」跟推理侧的 `recompute`（KV 满了驱逐请求重新 prefill）不是一回事，全文 60+ 次零消歧。
- §3 开场缺一句「**推理侧这一整节可以当成零**」：部署 671B 要 1.22 TiB，
  训练常驻 9.76 TiB —— 多出来的 8 倍推理里一个字节都不存。这个 8 倍全文没出现过。
- §4.3 对 CP 的定位跟 §5.1 打架（§5.1 刚证明上了 FA 之后激活账里没有 S² 项）。
  ⛔ 4K 上 CP 基本没有存在理由，而 §5.5 的对策表把它列成了激活那一栏的标准答案。
- §1.8「通信跟 batch 无关、越大摊得越薄」对 DP 梯度成立，但对本讲主角 V3 的
  EP all-to-all 差 30 万倍/token 且**完全不摊薄**。⭐ 这是通向专题五最重要的一个钩子。
- 968 KB 里**没有一个美元数字、没有 GPU 小时、没有墙钟天数**，只有一处卡数。
  而它已经引了 arXiv 2412.19437 六次 —— 那篇给了 2.788M H800 GPU-hours。
  ⭐ M5 的一个好发现：全文「美元」只出现 3 次，**全部是讲链式法则的比喻**
  （人民币→港币→美元）—— 课程教了读者「一串换算怎么从一个单位到另一个单位」，
  却从没把这条链从 TFLOP 跑到美元。
- §6「训不崩」不在主线上（全讲脊梁是「账」，§6 一笔账都没有）。
  ⭐ 接法：spike 也是一本账 —— 每崩一次回滚 100 步、扔 200–500 批，**可以换算成 GPU 小时**。
- V3 报告摘要原话「we did not experience any irrecoverable loss spikes」——
  全讲拿 V3 当跑例，§6 却完全建立在 PaLM 上，不提这条同源反例。
  ⭐ 它其实**支持** §6.5 的论点，白白浪费了。
- 「DeepSeek-V4 用 Muon」是全讲唯一一条没给 arXiv 号的引用（E5 核到是 **2606.19348**）。
- `80 GiB 一张卡 → 125 张`：市面标 "80GB" 是十进制 ＝ 74.5 GiB，可用约 70 GiB，
  照算是 134–143 张。⭐ 低优先，但既然全篇在讲「口径要报清楚」，这条自己犯了。

## ⭐⭐⭐ S 轮：风格统一 ＋ 清尾（2026-09-18 14:55 HKT 起）

> ⭐ 现场：「当然是都做了呀。然后**按轮次来搞**，一次都搞的话你可能搞不明白。」
> ⛔ **不挂 cron**。上一批挂了半天跑了十个小时，现场明确嫌长。这批按需一轮一轮来。

### ⛔ 每轮开工前先问这一句

房规松绑之后最容易犯的错，是把「松绑」理解成「全都要变长变花」。
⭐⭐ **房规①（文字可进画面）和房规③（长度与循环）是独立的两条。**
所以升级每一段之前先判：

| 它缺的是 | 那就 |
|---|---|
| **解释**（读者看得懂画面，但不知道这在说什么概念） | 加公式／字幕，**时长不动、`loop` 不动** |
| **铺陈**（一个动作太快，或者有起承转合讲不完） | 才放长到 20 秒以上，改 `controls` 不给 `loop` |

⛔ 判据：**「不够 fancy」的解药是信息密度，不是时长。**
一段 15 秒循环片配一条公式，可能比拉成 30 秒更好看也更好懂。

### 轮次表

- [x] ~~**S1** `anim-descend` 升级~~ ——&nbsp;**2026-09-18 完成**。三幕各一行字幕、第一幕一条更新式 `w ← w − η∇L`、第三幕竖轴顶端加 `+`／`−` 编码图例。**时长与 `loop` 都没动**（判它缺的是「解释」不是「铺陈」）。<br>      ⛔⛔ 这一轮最贵的教训跟图无关：我以为「加字之后渲染变慢了」，先怪 updater 每帧重设属性、改了一版；再怪 `Text` 每帧光栅化、跑了个三分半的对照实验 ——&nbsp;**把文字对象照建但不加进 scene，还是三分半，两个假设都不成立。**真相是那三分半本来就是这个场景的固有成本（540 条等高线段）。<br>      ⭐⭐⭐ 问题出在**前提**：我从来没记录过它原来要多久。**「感觉比以前慢」在没有旧基线数字时不是可靠信号。**已经把实测渲染成本写进 skill 的成本表 ——&nbsp;那张表存在的意义不只是预估等多久，**更是给下一次「是不是变慢了」一个可比的数**。<br>      ⭐ 那轮 updater→幕间 add/remove 的改动仍然保留：**幕与幕之间是离散事件，不该做成每帧都要问一次的连续量**，而且复位要恢复什么变得明摆着。<br>      ⭐ 顺手修了 `render.sh` 的基线路径（原来只找 mp4 旁边的，于是走 render.sh 时永远说「没有基线」，而直接跑 check-loop 却找得到）——&nbsp;**同一件事有两个入口时，它们对「配置在哪」必须有同一个答案。**
- [x] ~~**S2** `anim-reverse` 升级 ——&nbsp;「一趟 vs N 趟」需要一句话点破~~ ——&nbsp;**2026-09-18 完成（并行）**。<br>      两行角色抬头（`∂L/∂wᵢ` vs `∇L`，长短差本身在说话）＋ 三幕字幕 ＋ 两条图例 ＋ 末端方块标 `L`。幕界从动画自己的节拍算出来，不是挑的时间点；最后一次字幕切换钉在 `T_RESET`，于是**末帧按定义 ≡ 首帧**，不用在结尾补恒等 play。
- [x] ~~**S3** `anim-vanish` 升级 ——&nbsp;两条轨各缺一个刻度说明；`0.8^k` / `1.2^k` 该出现~~ ——&nbsp;**2026-09-18 完成（并行）**。<br>      只补四处：`对数刻度` / `线性刻度` ＋ 贴着两条曲线的 `1.2^k` / `0.8^k`。⭐ 位置是**搜出来的**（按画出来的折线采样占位，再挪到最近的合规空位，`MIN_GAP` 断言兜底），不是手调的。⛔ 并且横向让开扫描线的两个**驻留位** —— **放字要避开的不是「线经过的地方」，是「线停下来的地方」**。
- [x] ~~**S4** `anim-onelr` 升级 ——&nbsp;门槛 `2/A₁` 该写出来，标尺该有单位~~ ——&nbsp;**2026-09-18 完成（并行）**。<br>      标尺有了名字和量程、门槛写出闭式 `η = 2/A₁ = 0.08`、左上三行前提（更新式 ＋ A₁ ＝ 25 / A₂ ＝ 1）、随 η 分三段的字幕（颜色与轨迹同源）。字幕是 η 的**纯函数**，回扫时自动归位，三角波复位不受影响。
- [x] ~~**S5** `anim-memtime` 升级 ——&nbsp;三段（前向攒／反向放／更新后释放）该有名字~~ ——&nbsp;**2026-09-18 完成（并行）**。<br>      四幕字幕 ＋ 每幕一句「为什么」＋ 图例 ＋ 灰带里那笔账 ＋ 轴注；落点做成**两条虚线**（竖的说「是这一刻」，横的说「此后谁也没再碰到它」）。⭐ 落点当场验：扫 argmax 断言峰必须落在 `T_FWD`。⛔ 第一版采样网格 `linspace` **不含折点**，峰量错了 —— **分段线性函数求极值，网格必须含折点**。
- [x] ~~**S6** `anim-saddle` 升级 ——&nbsp;两条剖面该标出「一个方向往上、一个往下」~~ ——&nbsp;**2026-09-18 完成（并行）**。<br>      六块 `add_fixed_in_frame_mobjects` 静态文字：主句（「谷」染绿、「峰」染红）＋ 命名「鞍点」＋ 左右两组图例 ＋ 黑点说明 ＋ 曲面方程。⭐ 图例**用色块不用指向箭头** —— 相机转满一圈，钉位置的标注寿命是 0。黑点半径从 0.085 抬到 0.15（原来被两条剖面压在底下）。
- [ ] **S7** R22c **必改 3 格**：`reverse` Ⓓ／`recompute` Ⓒ／`lr-curve` Ⓑ
- [ ] **S8** R22c 中等 5 格
- [ ] **S9** 两条**事实待核**（这批里唯一关乎对错的）：峰值显存出现在哪一刻要不要在那里讲清；Muon 主权重是否仍需 fp32 ——&nbsp;⛔ 必须扒实现，不许凭印象
- [ ] **S10** R25 入口新图「你要找的是一个函数」
- [ ] **S11** R26 串故事线：每一格「上一格留下什么、这一格接什么」
- [ ] **S12** R27 历史统一进讲义 ＋ 出处栏
- [ ] **S13** RQ4b 字号收三档（分批，每批全量 lint ＋ 截图）
- [ ] **S14** R29 全量 lint ＋ 逐张截图复看 ＋ 修
- [ ] **S15** 收尾：index.html／讲义同步／全链体检／动画基线复核

## ⭐⭐⭐ 三十轮重做（2026-09-17 21:40 HKT 起；09-18 04:25 由二十轮扩到三十轮）——&nbsp;一条故事线，画到能看

> **现场原话**：「你写的文档，现在那些图画的实在是太粗糙了，根本就没有用心。
> 好好去学习一下台大李宏毅老师的讲义 —— 从 loss 怎么算开始，怎么反向传播、
> 怎么链式法则、怎么梯度下降，梯度下降的精度、梯度消失、梯度爆炸，
> 每一代优化器要解决的问题、带来的好处，**都画图画得清清楚楚，
> 沿着一个完整的故事线讲下来**。每发生一件事，**背后的故事、他怎么发现这个方法的、
> 那些有趣的历史**，也都收集过来写进去。
> **不要一次都写完，量力而为，一次只写一小部分，按二十次迭代，
> 边收集、边写、边画、边优化。**」

---

### ⛔ 这一轮的画图判据（新标准，比之前严）

**把这一格里所有文字删掉，还剩下什么？剩下的东西能不能让人猜出这一格在讲什么？**

- 剩不下东西的 ——&nbsp;**那就是一块写字的板子，不是图**。
- 现有的图按这条一量，`fig-descent` / `fig-onelr` / `fig-saddle` Ⓐ / `fig-slider` Ⓑ
  能过（有曲线、有轨迹、有地形、有推子）；
  **而相当一部分格子过不了 ——&nbsp;它们只是带彩色边框的文字。那正是「粗糙」的所指。**

⭐ 配套三条：
1. **能让它自己跑出来的，绝不手摆**（`fig-descent` 的轨迹、`fig-onelr` 的锯齿、
   `fig-precision` 的 bf16 舍入都是这么来的）。
2. **一格只画一件事。** 要说两件就分两格。
3. **历史故事进讲义和「出处与口径」，不进图面。** 图面只留画。

---

### 📖 故事线（不按节号，按讲述顺序）

| # | 这一步在讲什么 | 现状 |
|---|---|---|
| 1 | 你要找的是一个函数；怎么衡量它好不好 ＝ loss | 有正文，⛔ 无图 |
| 2 | loss 是一个数 → 第一个梯度 ＝ 预测 − 真值 | ✅ `fig-vote` Ⓐ |
| 3 | 导数 / 偏导数 / 链式法则 ＝ 兑换率 | ✅ `fig-slider` |
| 4 | 为什么必须从后往前 | ✅ `fig-reverse` |
| 5 | 梯度 ＝ 所有样本诉求的平均 | ✅ `fig-vote` Ⓒ |
| 6 | 顺着梯度走为什么就能变好 | ✅ `fig-descent` |
| 7 | 会不会卡住 ——&nbsp;鞍点与垭口 | ✅ `fig-saddle` |
| 8 | **⛔ 链式法则一路乘 → 梯度消失 / 梯度爆炸** | **全讲几乎没讲**（`fig-slider` Ⓒ 已埋伏笔） |
| 9 | **⛔ 动量 ——&nbsp;惯性能冲过去** | **无** |
| 10 | 一个学习率伺候不了所有参数 | ✅ `fig-onelr` |
| 11 | 最优步长 ＝ 一阶导 ÷ 二阶导 | ✅ `fig-beststep` |
| 12 | **⛔ Adagrad → RMSProp → Adam：每一代解决什么、又带来什么新问题** | 只有一根字节条，**没有因果链** |
| 13 | 学习率调度：warmup 与 decay | ✅ `fig-lr-curve` |
| 14 | 精度：大数吃小数 | ✅ `fig-precision` |
| 15 | 换一把尺 ＝ 换一个优化器（Muon） | ✅ `fig-muon` |

---

### 🗓 二十轮的排法（每轮一小块，做完就勾）

- [x] ~~**R01** 定判据 ＋ 路线图 ＋ 第一批已核的历史素材~~ ——&nbsp;**2026-09-17 21:50 完成**。
      判据定在「删掉文字还剩什么」；素材库首批 8 条全部读过一手原文；
      驱动这几轮的 cron job_id ＝ `9ae233ac0497`（每 30 分钟一轮，**R30** 后自撤）。
- [x] ~~**R02** 新图 `fig-vanish`~~ ——&nbsp;**2026-09-17 22:05 完成**，排在 §1.4 末尾（正文最后一句正是「把 61 个小导数按顺序乘起来」，图接着问「那乘起来会怎样」）。<br>      ⭐ 三格都是真画的：Ⓐ 三条真连乘出来的曲线（60 层）、Ⓑ 一根跨 10 个数量级的对数轴、Ⓒ sigmoid 与 ReLU 两条真画的导数曲线。落点：**被指数放大的不是梯度，是「每层偏离 1 多少」那个微小偏差** —— 0.8 与 1.2 只差两成，两头差 10 个数量级。<br>      ⛔ 画的时候栽了两次，都记进脚本头了：① 曲线越界写成 `break`，而 ×1.2 那条**第一个点就越界**，于是整条一个像素没画出来。**判据：越界要「分段跳过」，不是「遇到就停」。** ② 「冲出画面」的箭头指反了 —— **箭头指的是它往哪儿去，不是它从哪儿来。**<br>      ⚠️ 还抓到自己一处不一致：图注写「11 个数量级」而脚本算出来是 10（60 层 ＝ 59 个间隔），已改齐。
- [x] ~~**R03** `fig-vanish` 的历史 ＋ 讲稿~~ ——&nbsp;**2026-09-17 22:35 完成**。历史全部当轮读过一手来源，进「出处与口径」和讲义，**一个字没进图面**。<br>      · **1991**：Hochreiter 硕士论文《Untersuchungen zu dynamischen neuronalen Netzen》（慕尼黑工业大学）——&nbsp;⭐ **德文写的，从没在任何英文期刊发表过**<br>      · **1994**：Bengio / Simard / Frasconi《Learning long-term dependencies with gradient descent is difficult》，IEEE Trans. Neural Networks **5(2):157–166** ——&nbsp;标题就是结论<br>      · ⭐⭐⭐ **2001 年的结尾**：Hochreiter、Bengio、Frasconi、Schmidhuber **四个人合写了一章**《Gradient flow in recurrent nets》——&nbsp;**当年分头发现同一件事的两拨人，十年后坐到一起把它总结了。**<br>      · ⚠️ 归属至今有争议，讲法固定为「1991 年那篇论文正式指出」，不卷进优先权之争<br>      · ⚠️ 补了一条口径：**这个问题最早是在循环网络上发现的**，而 RNN 按时间展开就是「层数 ＝ 序列长度」的超深网络 —— 当年实验跨度 **1000 步 ＝ 一千层**<br>      ⛔ 本轮栽了一次：讲稿**插错位置**，塞进了 `fig-3x` 那段的中间（屏幕提示会前后矛盾：刚说「放大 fig-3x 的 Ⓐ」，中间插一整段 fig-vanish，然后又说「然后跳到 Ⓑ」）。已挪到 §1.4「你永远不用把它展开」之后。<br>      ⭐ 判据：**插一段讲稿之前，先读它上下各一段的屏幕提示** ——cue 的 lint 只检查「那张图还在不在」，**检查不了「这时候屏幕上该是哪张」**。
- [x] ~~**R04** 新图 `fig-momentum`~~ ——&nbsp;**2026-09-17 23:05 完成**，排在 §3.3 的 `fig-saddle` 之后 —— 那一格的落点正是「卡不住，**但走得慢**」，动量是那半句的第一个答案。<br>      ⭐ 三格都是真画的：Ⓐ 一条自造 loss 上**两条真滚出来的轨迹**（没动量那条停在浅坑、梯度归零；有动量那条冲过去落进深谷）、Ⓑ **按真实数值画长度和方向**的三个箭头、Ⓒ 一排真按 β 衰减的柱子。<br>      ⭐⭐⭐ **跑出来的比设计的还好。** 原本想讲「坑底梯度是 0 可它还在动」，而真跑出来那一步更狠：它刚越过坑底，**梯度这一项是在把它往回拉**，惯性以 **13 倍**的优势压过去。图改成画这个对抗。<br>      ⭐ 判据：**让它自己跑出来，然后照着跑出来的讲** —— 比照着自己预想的讲更有说服力，而且那才是真的。<br>      ⛔ 落点刻意收着写：**动量冲得过去是因为那个坑够浅**，它降低的是「起点决定一切」的程度，**不是消除它**，更不是「能找到全局最优」。<br>      ⚠️ 坑在哪、多深，是**让脚本自己找**出来的（扫梯度变号），不是我标上去的；四条 assert 盯着。
- [x] ~~**R05** `fig-momentum` 历史 ＋ 讲稿~~ ——&nbsp;**2026-09-17 23:35 完成**，讲义 72′。<br>      ⭐⭐⭐ 历史读到了 **Polyak 1964 原文**（《Some methods of speeding up the convergence of iteration methods》，比反向传播那篇 Nature **早 22 年**），比预期好得多：<br>      · 他给这方法起的名字就是「**小重球法**」（the method of a small heavy sphere）——&nbsp;**我们图上那个球，名字是从这儿来的**<br>      · ⭐⭐⭐ 我们 Ⓑ 画的那个对抗，**原文逐字写过**：「The motion proceeds **not in the direction of the force (i.e. antigradient) because of the presence of inertia**」，还说那一项让它「沿着**谷底**走」<br>      · ⭐⭐ 他 1964 年给的经验区间是 **ρ ＝ 0.8–0.99**，而今天 Adam 的默认 β₁ ＝ 0.9 ——&nbsp;**六十年过去还在这个区间里**。已把这个区间做成脚本里的一条 assert，图上用的两个 β 都得落进去（**这条不是装饰，它真能抓错**）<br>      · 他连调参顺序都写了：先把 ρ 设 0 调好学习率，收敛慢下来再加动量<br>      ⛔ **有一条没能核实，所以不写**：常有人说 1986 年那篇反向传播的 Nature 论文里就带动量项 ——&nbsp;这一轮拿不到原文（Toronto 那个链接 404），**本讲不提这一条**。<br>      ⛔⛔ 本轮按 R03 那条判据自查插入点，抓到**两处顺序错**：① 动量那段插在了「而那一整列数是从哪来的」（回指 `fig-descent` Ⓒ）前面；② 更大的一处 —— **讲稿里 `fig-onelr` 排在 `fig-descent` 之前，而教材里它在 `fig-momentum` 之后**。已把 `fig-onelr` 整块挪到教材那个位置，并把随之重复的一段压缩掉。<br>      ⭐ 判据：**教材改了图序，讲稿的图序必须跟着改** ——&nbsp;而 board-cue 的 lint 只验「图还在不在」，**验不了顺序**。<br>      ⚠️ 顺带发现：**讲稿里完全没有 `fig-beststep` 的 cue**（教材里它在 onelr 和 muon 之间）。留给 R18。
- [x] ~~**R06** 新图 `fig-lineage`~~ ——&nbsp;**2026-09-18 00:05 完成**，排在 §3.4 那张字节表之前。<br>      ⭐⭐⭐ 手法：**喂同一串恒定梯度**，看各家给出的「有效学习率」——&nbsp;梯度不变时，健康的优化器应当给出**一条平线**；不平的就是它自己的毛病，赖不到数据头上。于是「谁解决了谁的什么」变成了**四条真跑出来的曲线**，而不是四个方框。<br>      · **AdaGrad 的病在后期**：分母是累加的平方和（只增不减），有效学习率单调掉到稳态的 **4.1%**<br>      · **RMSProp 的病在初期**：滑动平均从 0 起步，第一步被放大 **32 倍**<br>      · **Adam**：恒定梯度下**从第一步起就是平的**；而它若不做校正，峰值只有 **6.6 倍** —— 因为动量那一项的偏差**部分抵消**了 v 的偏差<br>      ⭐⭐ 每一环都出自 **Adam 原文**（arXiv 1412.6980，已读原文）：AdaGrad 的更新式逐字是 `θ − α·g/√(Σg²)`（**累加不除 t**，所以那个病是从式子里长出来的）；偏差校正的理由是「从 0 起步 → biased towards zero，especially during the initial timesteps」；而且原文点名 **without bias correction, like in RMSProp**。<br>      ⭐⭐⭐ 链最强的证据也来自原文：**AdaGrad 就是 Adam 的一个特例**（β₁＝0、(1−β₂) 无穷小、α 按 t^(−1/2) 退火）—— **后面那个能把前面那个含进去，这才叫链。**<br>      ⚠️ 记了一条口径差异：**李宏毅课上那页写的 Adagrad 是「均方根」形式**（分母除了 t＋1），跟原版不同；本讲按原版画，因为「单调衰减」只在原版上成立。两边对不上时别以为自己算错。<br>      ⛔ 画的时候修了两处，都记成判据：<br>      ① Ⓑ 原来把 RMSProp 也画进来当对照 —— 可它那条是**初期偏差的尾巴**，放在「后期」这一格里会被读成「RMSProp 也在后期衰减」，**正好读反**。换成 SGD 平线。<br>      ⭐ 判据：**一格只讲一件事；对照组要选「没有这个病」的那个，不要顺手放一个「有另一种病」的进来。**<br>      ② 三条线在右端都收敛到 1 附近，标签叠成一团。⭐ 判据：**曲线的标签贴在「它跟别人最不一样」的那一段，不要一律贴右端。**
- [x] ~~**R07** `fig-lineage` 历史三则 ＋ 讲稿~~ ——&nbsp;**2026-09-18 00:35 完成**，讲义 76′。<br>      ⭐⭐⭐ 三则并排着讲，**反差全在「出版物的规格」上**，两条引用串本轮当场核过：<br>      · **AdaGrad**：正经 JMLR 长文（Duchi / Hazan / Singer，**12(61):2121–2159, 2011**，整整 39 页）。⭐ 而题目是《Adaptive Subgradient Methods for **Online Learning** and Stochastic Optimization》——&nbsp;**它根本不是为深度学习写的**<br>      · ⭐⭐⭐ **RMSProp 没有论文**。唯一的「出版物」是 Hinton 那门 Coursera 网课的**一页 slide**，标准引用写作 **Tieleman & Hinton (2012), Lecture 6.5-rmsprop**。而那页 slide 的**副标题本身就是算法的完整定义**：「Divide the gradient by a running average of its recent magnitude」<br>      · **Adam**（ICLR 2015）两处都印在论文里：首页脚注「作者顺序由 Google Hangout 上掷硬币决定」；致谢「特别感谢 Ivo Danihelka 和 Tom Schaul 起了 Adam 这个名字」——&nbsp;**名字不是作者自己起的**<br>      ⭐ 落点：**一条改变了整个领域的技术链，中间那一环连篇论文都没有。**<br>      ⭐ 讲稿插在「按字节排的谱系」之后，明说「按字节排只是一种看法，再换一条线看 —— 因果」；按 R05 的判据先读过上下两段的屏幕提示才插的。
- [x] ~~**R08** 按新判据重审全部图，列出「写字板子」清单~~ ——&nbsp;**2026-09-18 01:05 完成**。<br>
      ⭐⭐⭐ 没有靠眼睛扫二十张，而是**把判据做成了一个工具**：
      新增 `tools/topic02-lint-figink.py`（已接进 `build-all.sh`，**只报告不断言**）。
      它解析成品 SVG，数「删掉所有 `<text>` 之后还剩多少承载信息的形状」。<br>
      ⛔⛔ **工具本身返工了三次，每一次都是一条判据**：<br>
      · 第一版用**尺寸**区分「柱子」和「文字框」——&nbsp;失败，两者尺寸重叠，
        把 `fig4-slider`、`fig4-precision` 这种真画了东西的判成板子。
        ⭐ 改用「**同组之间变不变**」：同宽**异高**＝柱状图，同宽**同高**＝表格。
        **判据：分类要抓「它为什么在那儿」，不是「它多大」。**<br>
      · 第二版按**整张图**量 ——&nbsp;于是 21 张全过。可现场原话是「把**这一格**里的字删掉」。
        ⭐ **判据：量化一条判据时，粒度必须跟判据原话一致** ——&nbsp;
        粒度放粗一档，指标自动变好看，而问题一个没少。改成**按格量、报最差的那一格**。<br>
      · 第三版漏了两样：落点带被当成一格（按高度卡掉了），
        以及**横平竖直但长度各异的线**（`fig4-momentum` Ⓑ 那三根按真实数值画的箭头全是水平的）
        ——&nbsp;跟柱子同一个判据：**同向而长度在变 ＝ 数据；同向同长 ＝ 分隔线。**<br>

**📋 重画清单（R09–R14 按此顺序，每轮一到两格）**

| 优先 | 图 · 格 | 那一格现在是什么 | 判断 |
|---|---|---|---|
| 1 | `fig4-stability` Ⓐ | 26 字、零形状 | **重画** —— 它是「一条河」的主视觉，却没画河 |
| 2 | `fig4-reverse` Ⓑ | 22 字、零形状 | **重画** —— 讲「为什么从后往前」，该有两条方向相反的链 |
| 3 | `fig4-3x` Ⓐ | 19 字、零形状 | **重画** —— 数「几次矩阵乘」，该画成三块真的乘法 |
| 4 | `fig4-zero` Ⓑ | 19 字、零形状 | **重画** —— ZeRO 的通信量，该是条 |
| 5 | `fig4-spike` Ⓑ | 18 字、零形状 | **重画** —— 救火三步，该是时间线 |
| 6 | `fig4-circuit` Ⓑ | 17 字、零形状 | **重画** —— 三个门，该画成三个真的门 |
| 7 | `fig4-slider` Ⓐ | 17 字、一个推子 | 边界 —— 推子在，但只有一个；**加读数随推子变化的小曲线** |
| 8 | `fig4-saddle` Ⓑ / `fig4-vote` Ⓑ / `fig4-descent` Ⓑ / `fig4-onelr` Ⓑ | 三行文字条 | ⚠️ **多数是「合理的表」** —— 它们在做三路对照，硬画反而更糟。<br>⛔ 判据：**先问「这一格要传的是量还是分类」** —— 是量才必须画 |
| — | `fig4-precision` Ⓒ | 三类量三种命运 | ⚠️ 同上，**合理的表**，不改 |

⭐ 所以真正要重画的是**前 6 张 ＋ 1 张加料**，不是 19 张 ——&nbsp;
工具的作用是把它们排到前面，**判断仍然是人做的**。
- [x] ~~**R09** 校准那把尺子 ＋ 重排清单~~ ——&nbsp;**2026-09-18 01:35 完成**。<br>
      ⛔⛔ 本轮**一格都没重画**，因为一动手就发现 **R08 那张清单是错的**：
      排在第 1 位的 `fig4-stability` Ⓐ **明明画了河**（三个填充的梯形），
      排在第 2 位的 `fig4-reverse` Ⓑ **明明是一张连线图**。<br>
      ⭐ 于是这一轮的活变成了「**先把尺子校准**」——&nbsp;又抓出两类假阳性：<br>
      · **填了色的 `path`** ——&nbsp;它是一个形状，**跟它有几个绘图命令无关**。
        原来的「≥8 个命令才算曲线」把只有 3 个 `L` 的填充多边形整个漏掉了。<br>
      · **连线图** ——&nbsp;一条链上的节点是「同宽同高的矩形」，
        被我的柱子判据判成了**表格**。⭐ 而**连线图跟表格的区别不在方框，在箭头**：
        表格的格子之间没有箭头。于是加了一条：一格里 ≥ 3 个 `marker-end` ＝ 在画关系。<br>
      ⭐⭐⭐ **元级判据（这一轮最值钱的一条）：**
      **一把新尺子，必须拿「已知好」和「已知坏」两边的样本都回归一遍。**
      R08 只拿了「我知道它好」的那一边，而那批恰好都能过 ——&nbsp;
      于是漏掉的正是**本仓库最常用的两种画法**（填充图形、连线图）。
      ⛔ 「样本全过」不是尺子准的证据，**很可能只是样本选偏了**。<br>
      ⚠️ 顺带记一条留给 R10+ 的观察：`fig4-stability` Ⓐ 那条河
      **虽然画了，但画得太弱**（整段只加宽 14 px，读起来还是三个彩色方块）。
      工具看得见它有形状，看不见它**弱** ——&nbsp;这一类只能靠人看截图。

**🎬 插曲（2026-09-18 02:10，现场加的）：试了 3Blue1Brown 那套工具**

现场：「你看看三蓝一棕的绘图工具好不好用，要是好用的话你就多用用，把一些必要的图替换一下。」
⭐ 装了 manim CE 0.21 实测，结论**跟预期不一样，分两半**：

| | 实测 |
|---|---|
| **输出 SVG** | ⛔ **不支持**。`--format` 只接受 `png / gif / mp4 / webm / mov` |
| 中文 | ✅ 完美，无豆腐块（走 Pango） |
| 静态 PNG | 1.2 秒 |
| 3D 动画 | 1080p60 / 8 秒 / 渲染 **2 分半** / 直出 2.9 MB，压到 960 宽 **276 KB** |

⛔ **所以不能拿它替换静态图。** 我们那套是内联 SVG，换来的不只是好看：
文字可选可搜、读屏能读（每张都有 aria）、投屏无损缩放、进 git 能 diff，
**而且全部 lint 都建立在「它是 SVG」上**（字号 / 撞车 / 图内节号 / 墨水体检）。换成 PNG 全丢。

⭐⭐ **但它在另一头无可替代：它本来就是拍动画的**，而 3B1B 的杀手锏从来是「动起来」。
于是定了用法：**静态图继续用 `topic03_draw`，manim 只在「动起来才讲得清」的地方加短片。**

- [x] ~~第一段：`anim-saddle`（鞍点的 3D 曲面）~~ ——&nbsp;**已上线**，排在 `fig-saddle` 之后。
      理由很具体：静态图只能切**两个剖面**，而「一个方向上翘、另一个方向下沉」**天生是三维的**。
      红绿两条剖面曲线**真画在曲面上**，转满整圈无缝循环，**一个字都没有**（字由图注承担）。
      规矩和渲染命令写进了 `tools/manim/README.md`。
- [x] ~~**第二段：`anim-memtime`（显存随时间）**~~ ——&nbsp;**已上线**，排在 `fig-step` 之后。<br>
      ⭐⭐⭐ 这是扫完 25 张图之后排**第一位**的候选，理由很具体：
      **「峰值出现在哪一刻」本身就是一个过程。** 静态图画得出那座山的形状，
      画不出「它怎么被堆起来、又怎么塌下去」。
      ⭐ 而「还没想清楚的」里一直挂着的那条
      「要不要讲清楚峰值在哪一刻 —— 这需要时间线图，可能比想象中难讲」，**它就是答案**。<br>
      画的是**一个完整的 step**：前向（激活一层层堆高）→ 反向（激活释放、梯度长出来）
      → 更新（梯度清零）→ 回到起点，首尾同状态所以 loop 是真循环。
      三条带的高度**按真实字节数算**（14 B 基座 / 2 B 梯度 / 4.15 TiB 激活），
      跟 `fig-step` 同一套口径，脚本里 assert 了。85 KB。<br>
      ⛔ 修过两处：① 末尾停留太短，loop 读起来像「画到一半突然跳回去」，加长到约 2 秒；
      ② **没有图例分不清灰蓝红** —— ⭐ 而修法**不是往视频里加字**，
      是把图例放进 `figcaption`：**那儿的字可以选中、可以被读屏，放进视频就两样都没了**。

**📋 动画候选排名（2026-09-18 扫完 25 张，判据：增量只来自「时间」或「第三个维度」）**

| 排 | 图 | 动起来多的是什么 | 状态 |
|---|---|---|---|
| — | `fig-saddle` | 三维形状（二维只能切两个剖面） | ✅ **已上线** |
| 1 | `fig-step` / `fig-act-bill` | 「峰值在哪一刻」是个过程 | ✅ **已上线** |
| 2 | `fig-reverse` | 「你得**重复**几遍」——&nbsp;重复本身就是时间 | ✅ **已上线**（2026-09-18） |
| 3 | `fig-vanish` | 「传」是个动作 ＋ **对数轴自己的代价** | ✅ **已上线**（2026-09-18，双轨：对数 vs 线性） |
| 4 | `fig-onelr` | **学习率本身成了一根能走的轴** | ✅ **已上线**（2026-09-18，参数扫描） |
| 5 | `fig-momentum` | 惯性冲过浅坑 ＋ **冲过头**（动量的副作用） | ✅ **已上线**（2026-09-18，**松绑后第一支**：中文字幕 ＋ 公式变形 ＋ 镜头推拉） |
| 6 | `fig-descent` | 两个球同时滚，落进不同的谷 | ✅ **已上线**（2026-09-18，骨架取自 3b1b 的降维阶梯） |

> ## 📌 2026-09-18 下午 · 房规松绑

> 六段做完后收到一句话：「为啥没有 3b1b 那么 fancy 的图？」
> ⭐⭐⭐ 答案不是能力，是**我自己定的四条房规里有两条在压表现力**：
> 「一个字都不放」砍掉了**公式与图形同步变形**（他最强的手法）；
> 「15 秒 ＋ 首尾同帧」把一个概念压到十五秒，还得留一段复位。
> ⭐ 现场拍板：「必须得松，按最漂亮的风格来，**一次搞一个，多轮迭代，别着急**。」
>
> **松绑后的新房规**（都实测过才写）：
> ① 文字可以进画面（`MathTex` 公式、`Text` 中文走 Pango 不需要 CJK 宏包），
> 但**必须为讲解服务**，且 `aria-label` 要复述画面里每一句话；
> ② 静态图仍排在动画上面；
> ③ **长度与循环二选一** ——&nbsp;≤20 秒的短片仍要首尾同帧挂 `loop`；
> >20 秒的叙事片给 `controls` 不给 `loop`，baseline 里写 `"loop": false`。
> ⭐ 判据：**守卫要守的是「承诺」，不是「形状」。**
> ④ **数据全部当场算，这条永远不松** ——&nbsp;而且**参数也算数据**。
>
> 全部写回了 skill `manim-teaching-figures`。
> ⛔ 顺带一个教训：松绑写进 SKILL.md 之后，`traps.md` 里那句
> 「动画里一个字都不放」**又独立活了半天** ——&nbsp;
> ⭐ **改规矩要把所有复述它的地方一起找出来**，不然新旧两套同时生效。

⛔ **其余 19 张不做。** 它们传的是**分类 / 对照 / 账单 / 比例** ——&nbsp;
静态能并排比，动起来反而看不全。（比如 `fig-precision` 虽讲「连加一千次」，
可结论是**一动不动**，动画反而没画面。）
⭐⭐ **2026-09-18 现场推翻了下面这条**：「三蓝一棕画的动图超级好用，多借鉴一下。」——&nbsp;3～6 继续做，不再攒着。<br>⭐ 同一轮还补了一处：`fig-saddle` 和它底下那段动画之间**没有过渡句**，读者看完静态图很可能就翻过去了。**动画不会自己招手，得有人指路。**<br>~~⚠️ 而且**别一次做完**：每段渲染两三分钟，以后每改一次都得重渲 ——&nbsp;
**多一段视频就多一份维护。**~~

**📋 重排后的清单（R10–R14，每轮一到两格）**

| 优先 | 图 · 格 | 那一格现在是什么 | 该画成什么 |
|---|---|---|---|
| ~~1~~ | ~~`fig4-3x` Ⓐ~~ | **R10 已重画** | ✅ 三次**真的矩阵乘**，形状按真实维度成比例。⭐ 于是两件事一眼看出来：**三块积的面积一样大**（所以「1＋2＝3」不是比喻，是数出来的，脚本 assert 了三次乘加次数相等）；**② 那块操作数是虚线框 ＋ 一根箭头指回 ①** —— 它不是新算的，是前向存下来的。**激活扔不掉，那根箭头就是全部答案。** |
| 2 | `fig4-zero` Ⓑ | 19 字、零形状 | ZeRO 三级的**通信量**，该是条不是字 |
| 3 | `fig4-spike` Ⓑ | 18 字、一个箭头 | 救火三步，该是**时间线** |
| 4 | `fig4-circuit` Ⓑ | 17 字、零形状 | 三个门，该画成三个真的门（分发 / 交换 / 路由） |
| 5 | `fig4-reverse` Ⓒ | 17 字、零形状 | 「换个看法」那一格，待定 |
| 6 | `fig4-slider` Ⓐ | 17 字、一个箭头 | 加一条「读数随推子变化」的小曲线 |
| — | `precision` Ⓒ / `saddle` Ⓑ / `vote` Ⓑ | 三路对照 | ⚠️ **合理的表，不改**（传的是分类不是量） |

⭐ 而 `fig4-stability` Ⓐ 从清单上**退下来了** ——&nbsp;它是被坏尺子冤枉的。

- [x] ~~**R10** 重画 `fig4-3x` Ⓐ~~ ——&nbsp;**2026-09-18 完成**。原来是三张卡片，每张里**写着**「一次矩阵乘」——&nbsp;一格在数矩阵乘，却一个矩阵都没画。
      现在是**三次真的矩阵乘**，形状按真实维度成比例：**三块积面积一样大**（「1＋2＝3」是数出来的，脚本 assert 了三次乘加次数相等），**② 那块是虚线框 ＋ 一根箭头指回 ①** ——&nbsp;激活扔不掉，那根箭头就是全部答案。
- [x] ~~**R11** 重画 `fig4-zero` Ⓑ~~ ——&nbsp;**2026-09-18 完成**。原来是三张卡片，每张列「占多少字节 / 多久用一次 / 所以第几个切」。<br>      ⭐⭐ 可这一格的论证**本来就是二维的**：一个轴「多大」，一个轴「多久用一次」——&nbsp;三样东西落在平面上的**位置**，自己就把顺序排出来了。**卡片把二维压成三段文字，白扔一个维度。**<br>      现在是散点：泡泡大小也按字节数（重复编码「多大」），一条虚线折线走出**先往下（按大小）、再往右（按频率）**的 L 形 —— 那就是切的顺序；右上角专门标出**那一角是空的：又大又忙的东西**。<br>      ⭐ 脚本 assert 了两条：权重的 122 次 ＝ 61 层 × 2（不是随手写的）；**按「先大后忙」排出来的顺序必须正好等于 ZeRO 的三级**。<br>      ⭐⭐⭐ 判据：**当一件事是「按两个指标排序」时，它就该是一张二维图，而不是三张卡** ——&nbsp;卡片只能告诉你结论，位置能让你自己看出结论。<br>      ⛔ 顺带一条画法判据：**散点图的标签要各自朝「自己这一侧最空」的方向放** ——&nbsp;统一朝上或统一朝下，一定会撞到轴。
- [x] ~~**R12** 重画 `fig4-spike` Ⓑ~~ ——&nbsp;**2026-09-18 完成**。<br>      ⛔ **先看实物，别照着清单动手**：路线图写的是「救火三步，该是时间线」，而实物 Ⓑ 画的根本不是救火三步，是「裁剪开着 → 还是飞了 → 论文说不知道」三个文字框。<br>      ⭐⭐⭐ 真正的病比「字太多」深一层：它**陈述了矛盾却没有化解矛盾**。「保险开着还是飞了」读起来像悖论，于是读者要么觉得裁剪没用，要么觉得自己没看懂。<br>      而它一点都不悖 ——&nbsp;**裁剪摁住的是「梯度范数」，飞掉的是「loss」，保险装在 A 上，事故发生在 B 上。**<br>      ⭐⭐ 判据：**当一句话听起来像悖论时，图的任务不是复述它，是把那两个被读者当成同一个的量分开画。**<br>      新 Ⓑ ＝ 把 Ⓐ 中段放大，上下两条对齐同一批时刻的曲线：上面梯度范数（虚线＝裁剪前会冲过阈值，红实线＝裁剪后顶被削平、一次都没越），下面同一时段的 loss（六个尖峰一个不落）。<br>      ⭐ 输入曲线仍是示意，**但「裁剪」这一步是真算的** ——&nbsp;`clipped = min(raw, 阈值)` 是裁剪的*定义*不是实测，所以那条 assert 有牙齿。判据：**示意图里也能有硬骨头 ——&nbsp;把「按定义必然成立」的那段真算出来，别连它一起手画。**<br>      ⛔⛔ **同一个坑第二次栽**：「裁剪阈值」四个字被右栏盖掉了一半，而 `fig4-saddle` 那一轮记的正是「右侧文字落笔前先算起点＋字数×字号」。已把这条改成脚本里的 assert ——&nbsp;**判据只写在注释里，它就只在写它的那个文件生效。**<br>      ⛔ 另一处：第一版 loss 直接套 Ⓐ 的整段纵轴，结果那一小段主干几乎是平的，像「一条直线插几根刺」，反而把「它在飞」讲弱了。⭐ 判据：**放大一段来看时，纵轴要跟着一起放大** ——&nbsp;横轴放大了纵轴没放大，那一段只会比原来更平。<br>      ⚠️ 顺带一条自警：核 lint 报告时把专题三的 `fig-sink` 当成了本图（编号都是 `#26`），**差点去改一张不相干的图**。
- [x] ~~**R13** 重画 `fig4-circuit` Ⓑ~~ ——&nbsp;**2026-09-18 完成**。<br>      旧版是三张卡片：每张一个符号、一个比喻（分发器／交换器／路由器）、两行规则。把字删掉只剩三个空框。<br>      ⭐⭐⭐ 而这三个门的差别**本来就是线的形状**：分发＝一进两出两条等粗｜交换＝两条交叉｜路由＝一条通一条断。<br>      ⭐⭐ 判据：**当三样东西的差别可以画成三种连线形状时，写成三段文字就是把图形信息翻译成文字、再让读者翻译回去。**<br>      ⭐ 画法直接用了刚归档的 Olah 那条（见素材库 🅒）：**一张骨架重复三次，只换红线怎么走。** 读者只需读一次骨架，之后注意力全给差异 ——&nbsp;**而且这是降成本的画法，一套骨架用三遍比画三张卡片还省事。**<br>      ⭐ 另外让**线宽直接编码梯度大小**（`1.8 + 4.8·|g|/4`，带 assert 保证粗细差 >1.9 倍），负梯度画虚线 ——&nbsp;数值不写在旁边也看得出谁大谁小。<br>      ⛔ 两处自己栽的：①「换」字放在了门圆心上，跟 × 叠成「×换」——&nbsp;已挪到两条虚线**真正交叉的那个点**上（那里才是交换发生的地方）；② 标题拼成了「max max 门」，因为符号本身就是名字。
- [x] ~~**R14** `fig4-reverse` Ⓒ ＋ `fig4-slider` Ⓐ ＋ `fig4-circuit` Ⓒ~~ ——&nbsp;**2026-09-18 三件全完成**。<br>      ① **`fig-reverse` Ⓒ**（见上一轮记录）：三行对照表 → 两张骨架逐点相同的网络图。<br>      ② **`fig-slider` Ⓐ**：中间那个纯文字框（`5.00 ↓ 5.03`）换成**两把共用同一种格子的竖尺**，参数走 1 格、loss 走 3 格。<br>      ⭐⭐⭐ 先试过李宏毅那张「小人站曲线上、脚下一条切线」（素材库 🅑①），**放弃了**：**斜率 3 要求纵轴的像素比例尺是横轴的 3 倍**，否则读者用眼睛量出来是 0.63 不是 3，而面板是宽扁的放不下。<br>      ⭐⭐ 判据：**一张请读者「用眼睛量比例」的图，两个轴的比例尺必须相同；做不到就别用坐标系，换一个不需要两个轴的画法。**（换成共用格子的竖尺之后，比例就是格数，不用换算。）<br>      ⛔ 栽了一次：红条终点定在从下数第 2 格，往下退 3 格**戳穿了尺底**。⭐ 判据：**凡是「从某点往回退 n 步」的画法，都要先问退得出去吗** ——&nbsp;已做成 assert。<br>      ③ **`fig-circuit` Ⓒ**：两个文字框 → 左边**一个**乘法门＋它挂着的那个方块，右边 **175 个同样大小的方块**。<br>      ⭐⭐⭐ 判据：**当论点是「这东西多得吓人」时，唯一能传达它的是「多到一眼看不完」的画面本身**，不是那个数字。⭐ 而且左右两边的方块**用同一组尺寸**（跟 slider 两把尺共用格子是同一条判据）。<br>      <br>      ⚠️⚠️ **本轮最要紧的发现，跟这三张图无关**：`figink` 实际报 **16 个 ❌ / 8 个 ✅**，而我前几轮每轮都写「剩下的正好是下一轮要做的」——&nbsp;**那句话是看 `head` 截断后的前 3 行说的**，后面还有 13 行。<br>      ⭐⭐⭐ 判据：**截断的输出不等于完整的清单。**（这是「空结果不等于没有」的变体 ——&nbsp;`head` 把「还有很多」显示成了「就这些」。）<br>      ⛔ 16 个里**未必都要重画**：figink 是分诊工具不是判官，像 `fig-saddle` Ⓑ 那种缩放行很可能是误报。**R19 逐个过，别照着数字批量动手。**
> ⭐⭐⭐ **2026-09-18 04:25 现场：「再加十轮。」**
> ⛔ 关键不是「加十轮」，是**加在哪里**。原 R15–R20 全是**收尾流程**
> （串故事线／讲稿重读／全量 lint）——&nbsp;新内容轮要是排在它们后面，
> 等于**串完故事线又改内容，收尾白做一遍**。
> ⭐ 所以收尾整体顺延到 **R25–R30**，新的十轮插在前面，按
> **内容 → 质量 → 动画** 排：素材落地 5 轮、figink 逐张过 3 轮、剩余动画 2 轮。
> ⛔ 顺延改的是**调度器的输入**（cron 找「下一个未勾的 `- [ ] **Rxx**`」），
> 已按它的读法自己走了一遍：下一个未勾的是 **R15**。

**📋 第二批（R15–R24）——&nbsp;素材落地 ＋ figink 清账 ＋ 补完动画**

⛔ R15–R17 用的全是**素材库里已核过原文**的料，但那批是 **subagent 读的**：
**每条在真正落地之前，我要自己再打开那篇文章核一遍原话。**

- [x] ~~**R15** warmup 的第三个解释~~ ——&nbsp;**2026-09-18 完成**。<br>      ⭐⭐⭐ **这一轮最值钱的不是内容，是「自核原文」这条规矩当场兑现了。**<br>      subagent 的转述是「warmup 治的不是更新量太小，是更新量全是噪声」——&nbsp;听上去像**又一个互相竞争的说法**。<br>      而我打开 8747 核了一遍，发现转述**漏了最要紧的那半句**（原文原话）：<br>      　　「这里的讨论前提是梯度消失，如果是 Pre Norm 之类的结构，**没有明显的梯度消失现象，那么不加 Warmup 往往也可以成功训练。**」<br>      ⭐⭐⭐ 这半句把「三个解释怎么调和」直接解决了 ——&nbsp;**它们不是在抢同一个位置，是不同前提下的不同机制。**<br>      ⛔ 只看转述就落地，我会把一个**有适用边界的解释写成普适的** ——&nbsp;这正是「二手转述不能直接进课件」要防的那类错，而且它**不会报错**。<br>      落地内容：Adam 的更新量 `O(η)` → 梯度消失不再是「学不动」而是「乱学」→ 四步推理链 → **可对日志的现象：loss 先快速收敛到一个常数，再发散到 NaN** → 三个说法各有前提的对照表 → 落点「该问的不是哪个对，是在什么前提下」。<br>      ⛔ 顺手栽的一个：我给这段用了 `<h5>` 和 `<table class="tbl">`，**两个都是全站唯一、且都没有样式定义** ——&nbsp;HTML 合法，但会落到浏览器默认样式。⭐ 判据：**别为一段内容引入全站唯一的标签或 class**，先看这份文档里同类内容已经怎么写了（现有 13 个表格全是裸 `<table>`）。
- [x] ~~**R16** Post-LN 下 LN 是梯度消失的「元凶」之一~~ ——&nbsp;**2026-09-18 完成**。<br>      §6.5 原来只讲了 2020 那篇的**现象**（Post-LN 初始化时靠近输出梯度大），**没讲机制，也没讲「那为什么还留着它」**。<br>      补上的机制一句话：Post-LN 初始化那刻 `x` 和 `F(x)` 方差各 1，加起来是 2，Norm 拉回 1 ⇒ **相当于除以 √2**；递归下去 **x₀ 在第 l 层的系数就是 `2^(−l/2)`**。<br>      ⛔⛔ **本轮避开的陷阱**：那是**前向**的残差通道被削，**不等于「梯度每层乘 1/√2」**。两者相关但不是一回事 ——&nbsp;混起来就是 CLAUDE.md 第一原则点名的那种「听起来像常识的架构关系」。⭐ 所以新图**画的、标的、算的全是前向直通项**，「因此梯度消失」那一步交给正文，图上不冒充；出处栏也写明了这条区别。<br>      🆕 **新图 `fig4-prepost`**：⭐⭐⭐ **同一条曲线画两遍，只换价值判断。**Ⓐ Ⓑ 的曲线是同一个函数、同一组坐标（有 assert），变的只有底下那个结论框：Ⓐ「残差名存实亡，前面的层学不到东西」／Ⓑ「这正好是你要的，它替你把前面的层按住了」。<br>      ⭐⭐ 判据：**又一次用 Olah 那招，但这次移动的不是高亮，是结论** ——&nbsp;**事实没变，评价变了**，而这正是这一格要讲的事。<br>      ⭐ 三段论证结构直接照搬苏剑林（素材库 🅒 那条「推翻之后要回头平反」）：**① LN 是元凶 → ② 那为什么还加它（去掉方差会炸，问题是加在哪儿）→ ③ 甚至对 finetune 是好处（本来就该优先调靠近输出的层）**。<br>      ⭐ 顺带把 R15 那三个 warmup 解释的**前提坐实了**：第三条要的正是这条指数衰减，Pre-LN 没有这条线，所以那条对它不适用。<br>      ⛔ 两处自己栽的：① 落点那行字**压在左边结论框上**（只差 2px，lint 验不出 text↔box）；② 纵轴标签 `anchor="end"` 放在 `gx0−8`，而左半的 `gx0` 只有 54，**文字一直顶到画布边缘**。
- [x] ~~**R17** 动量 β 与 η 联动 ＋ batch 的 Surge 现象~~ ——&nbsp;**2026-09-18 完成**，两篇原文都自核了。<br>      **① `fig-momentum` 新增 Ⓓ。** 原文给的不只是那条公式，还有**为什么**：β ＝ (1−λγ/2)/(1+λγ/2) ——&nbsp;**λ 才是摩擦系数（物理量），β 只是它和步长凑出来的数**。<br>      ⭐⭐⭐ 于是那条调参建议有了根：**你只把学习率调小、不动 β，等于偷偷把摩擦力拧大了。**画成三根长度 ∝ λ 的条：原来 0.158 ／ 只降 η 不动 β **0.500（涨 3.16 倍）** ／ 同时把 β 提到 0.953 **回到 0.158**。三条 assert 兜着（调完 λ 必须纹丝不动、涨的倍数和窗口拉长的倍数都必须正好是 1／√r）。<br>      **② Surge 现象只进正文，刻意不画图。** ⭐ 判据：**一条带条件的规律画成图，条件就掉了** ——&nbsp;它是「取决于 β_noise、不一定出现」的，而图天然会把它画成一条确定的曲线。<br>      ⚠️⚠️ **这是本批第二次自核救回一条被过度概括的结论。**subagent 转述是「超过 B_noise 后最优学习率反而要降」，而原文加了限制：**β_noise ≥ 1 时依旧单调递增**；苏剑林还**批评了原论文**把 Surge 说成几乎总会出现，他自己的结论是「**也不一定会出现**」。<br>      ⭐⭐⭐ 两次漏的是同一类东西：**转述保留了结论，丢掉了限定词** ——&nbsp;而限定词恰恰是那条结论能不能用的全部。<br>      ⭐ 顺带收下一条更基本的：OpenAI 那个 `η* ≈ η_max/(1+B_noise/B)` **单调递增但有上界** ——&nbsp;光这一条就够破「batch 翻倍学习率就该翻倍」了。
- [x] ~~**R18** 混合精度：FP16 下界竖线~~ ——&nbsp;**2026-09-18 完成**，🆕 新图 `fig4-underflow`。<br>      ⛔ 两件不能做的事：原图不能复制（版权），直方图数据我们没有（编了就是造数据）。⭐ **但竖线的位置是 IEEE 754 的定义，可以精确算** ——&nbsp;所以这张图**没有直方图**，只有一根真数轴和几条真边界，每个数都从 `numpy.float16` 的位模式当场算，「归零」也**真舍了一遍**验过。<br>      ⭐⭐⭐ 算出来之后发现一个**比原图更好的画面**：bf16 下界 1.18e−38 和 fp16 下界 6.10e−5 之间隔着 **33 个数量级**，那一大片空白本身就是论点 ——&nbsp;比「指数位 5 位 vs 8 位」直观得多。<br>      Ⓑ 画 loss scaling：×1024 ＝ 在这根对数轴上**整体右移 3.01 格**。⚠️ 诚实标了放大后落在**次正规区**（存得下但精度打折）。<br>      <br>      ⚠️⚠️⚠️ **本轮最值钱的是顺带挖出来的一个全站级 bug。**我在落点带标题里写了 `<u>`，**整条落点带在浏览器里凭空消失** ——&nbsp;而 SVG 良构、XML 自检通过、几何 lint 全绿。<br>      根因跟当年 `<em>` 那次一模一样：`<u>` 是 HTML 解析器的**foreign-content 逃逸标签**，图是 inline 进 HTML 的，浏览器读到它**当场退出 SVG 模式**，后面全丢。绘图基元本来拦了 `<em> <i> <b> <p> <br> <font`——&nbsp;**唯独漏了 `<u>`**。<br>      ⭐⭐⭐ 判据：**这种黑名单要么枚举干净，要么改白名单，漏一个等于没有。**（SVG 里唯一允许的内联标签就是 `tspan`。）<br>      ⭐ 已补进拦截，并做了 **negative test** 确认真生效（带 `<u>` 的被拦、带 `tspan` 的放行）——&nbsp;「加了断言」不等于「断言有效」。全仓库复扫，0 个 SVG 含 `<u>`。<br>      ⭐ 顺带记了 figink 的**第三个盲区**：数轴类图认不出来（三条竖线只有两种长度、色块只有一个 rect）。跟盲区之二同源：**它数不出「少而承重」的形状**。仍不放宽阈值。
- [x] ~~**R19** update-to-data 比值曲线 ＋ 那条 −3 的黑线~~ ——&nbsp;**2026-09-18 完成**，🆕 新图 `fig4-updratio`。<br>      ⛔ 素材库这条是 **Ⓒ 级**，落地前自核了 CS231n 原文。原话：「A rough heuristic is that this ratio should be somewhere around **1e-3**…」⭐ 还捡到转述里没有的一句：**"Note: updates, not the raw gradients"** ——&nbsp;看的是**更新量**不是梯度（对 Adam 这两个差得远）。<br>      ⭐⭐⭐ **本轮最值得记的：跑出来的结果先把我要画的论点驳了。**<br>      我本来想画「太大 → loss 更差」。⛔ 可真跑完发现**太大那条的末 loss 反而最低**。顺着查才看清代价在别处：**权重撑到 4.2 倍、抖动大约 9 倍、学习率再乘 2.1 就 NaN**。<br>      ⭐⭐⭐ 于是论点被改成了真话：**这个比值不是「loss 最低」的指标，是「你离悬崖多远」的指标** ——&nbsp;判据：**诊断指标不是优化目标。**<br>      ⚠️ 而且**没有把那个反常结果藏掉**：脚本里专门有一条 `assert L_HI < L_OK` 钉住它，落点带也写明这是玩具问题（随机数据、无验证集），「loss 更低」在这儿本来就不算数。<br>      ⭐ 全是跑出来的：两层 tanh MLP 纯 numpy 手写前向反向、固定种子；「刚好」那个学习率是**扫出来的**（让稳态比值最贴 1e−3），「再乘 2.1 就 NaN」是**二分搜出来的**。<br>      ⛔ 三处过程中的自我纠正，都值得记：<br>      ① 抖动断言写了 `>10×`，实测 8.6× 挂了 ——&nbsp;⭐ **正确反应是先打印实际值，不是把 10 改成 8**。最后定 5（论点成立的最低要求）。<br>      ② docstring 里抄了手测另一个学习率时的「20 倍」——&nbsp;⭐ 判据：**注释里的数也是一种断言**，凡是脚本能算的，注释就只说方向、不说数。<br>      ③ Ⓑ 里「太小」和「刚好」两条**真的几乎重合**，标签撞了 4px。⭐ 判据：**两条线真的重合时就合并标注并说出来**，而不是拉开标签假装分得开。
- [x] ~~**R20** figink 清账（一／三）~~ ——&nbsp;**2026-09-18 完成**：前 6 张**逐张判完**，动手改了 1 张。<br>      ⭐⭐⭐ **判定结果：4 个误报、2 个真该改。**（所以这批的**假阳性率是 2/3** ——&nbsp;⛔ 更坐实了「照着数字批量动手」是错的。）<br>      · `fig-underflow` Ⓐ ——&nbsp;**误报**，数轴类（盲区之三）<br>      · `fig-slider` Ⓐ ——&nbsp;**误报**，两根长度对比条（盲区之二）<br>      · `fig-momentum` Ⓓ ——&nbsp;**误报**，三根摩擦条（同盲区之二）<br>      · `fig-3x` Ⓒ ——&nbsp;**误报**，是 **unit chart**（1／3／4 个等大方块，**数量本身就是数据**）：盲区之一。而且这一格画得很对，「推理买一遍、训练买三遍」就是靠数方块看出来的。<br>      · `fig-saddle` Ⓑ ——&nbsp;**真该改**（三个等高框，字删完只剩三个一样的框）。⭐ 它要讲的是「方向数 2 → 几万 → 几千亿，而要求是**全都**朝上」，**形状应该是箭头的数量爆炸**，跟 `fig-circuit` Ⓒ 那个 scale-up 同一招。**留给 R21。**<br>      · `fig-precision` Ⓒ ——&nbsp;**真该改，本轮已改。**<br>      ⭐⭐⭐ 改法：这一格的判据「**老的贡献会不会永远不走**」**本身就是一条衰减曲线的形状**。于是换成同一根对数横轴上的四条真曲线：主权重**一直平着**、二阶矩 v（β₂＝0.999）半衰期 **693 步**、动量 m（β₁＝0.9）**6.6 步**、梯度／激活**下一步就没**。<br>      ⭐ 半衰期是算的、不是标的；还 assert 了「十万步后连 v 也忘光」。⛔ 横轴必须用对数 ——&nbsp;线性轴上 β₂ 跟主权重看着一样平，论点就画没了。<br>      ⭐ 落点变成一句能一眼验证的话：**只有那条一直平着的必须 fp32；忘得掉的就存得粗。**<br>      ✅ 专题四 ❌ 计数 18 → 17；`fig-precision` 从「写字板子」降到「⚠️ 偏板子」（现在最差的那格换成了 Ⓑ）。
- [x] ~~**R21** figink 清账（二／三）~~ ——&nbsp;**2026-09-18 完成**：还了 R20 欠的 `fig-saddle` Ⓑ，又判完中间 5 张。<br>      **① `fig-saddle` Ⓑ 重画完毕。** 旧版三个等高框。⭐⭐⭐ 这一格讲的是一个**合取条件**（方向数从 2 涨到几千亿，而要求是**全都**朝上）——&nbsp;**合取条件的形状就是一排箭头**：箭头越多「全都朝上」越难，而**只要有一个朝下就有路**（那一个画成刺眼的红色反向箭头）。<br>      ⭐ 三行用**同样的箭头大小和间距**，所以行的**长度**就等于方向的**个数**，比例诚实；最后一行画到边缘加省略号 ——&nbsp;**「画不下」本身就是论点**。<br>      ⛔ 栽了一次：左边标签 `anchor="end"` 顶出画布切掉两个字。⭐ 这是 `fig4-saddle` 那轮记的「右侧先算起点＋字数×字号」的**镜像版** ——&nbsp;**左对齐的反面是右对齐，两头都要算**。已做成 assert。<br>      **② 中间 5 张判定：2 个误报、3 个真该改。**<br>      · `fig-step` Ⓑ ——&nbsp;**误报**。代码里本来就有 `frac = tib/RESIDENT`，**它一直在画条形图**，只是三根条数不出来（盲区之二）。<br>      · `fig-onelr` Ⓑ ——&nbsp;**误报**。是对 Ⓐ 那张图的数值解读，推导链本来就该是文字。<br>      · `fig-descent` Ⓑ ——&nbsp;**真该改**：「一个数 → 一个向量」本身就是维度的形状。<br>      · `fig-recompute` Ⓑ ——&nbsp;**真该改**：有两个数值维度（1.9% 算力换 77% 显存 ／ 33% 换 97%），该是二维图，跟 R11 的 `fig-zero` Ⓑ 同一招。<br>      · `fig-vote` Ⓑ ——&nbsp;**真该改**：三种做法作用在**三个不同的位置**上，是形状不是分类。<br>      <br>      ⚠️⚠️ **本轮发现一个我一直理解错的度量。** 改完 `fig-saddle` Ⓑ 之后，❌ 计数**没有降**（还是 17）——&nbsp;因为**同一张图的 Ⓒ 顶上来了**。<br>      ⭐⭐⭐ 原因：figink **每张图只报最差的那一格**。所以「❌ 计数」是「**有问题的图数**」，**不是「问题数」**。（R20 那次 18→17，是因为 `fig-precision` 整张从 ❌ 掉到了 ⚠️。）<br>      ⛔ 判据：**一个「每个对象只报最差项」的报告，修好一项不一定让计数降 ——&nbsp;它的计数不是工作量。****真实待改的格数比 17 多**，R22 要按「格」重新点一遍，不按「图」。
- [x] ~~**R22** figink 清账（三／三）——&nbsp;判定部分~~ ——&nbsp;**2026-09-18**。按「判定优先、重画顺延」做，**重画整体顺延**（见下面新加的 R22b）。<br>      **① 给工具加了 `--per-panel`（按格点，不按图）。**⛔ 重构时先存了一份旧输出，改完 **diff 逐字节相同** ——&nbsp;⭐ 判据：**重构共用逻辑，先存旧输出做回归**。<br>      ⭐⭐⭐ **真实数字：73 格里 24 格不及格**，而按图点是 17。两个数差了 7，差的就是「一张图里不止一格有问题」的那些。<br>      **② 本轮判了 9 格（累计 24 格里判过 18 格）。**误报：`slider` Ⓑ（9 个推子只有一个动）／`act-bill` Ⓑ（注释写明已修成「两框按比例」）／`saddle` Ⓒ（minimum ratio 数轴）／`muon` Ⓓ（时间轴分段）／`batch` Ⓐ（坐标图）／`batch` Ⓑ（对 Ⓐ 的分段解读）。<br>      真该改：`recompute` Ⓒ（k＝1／4／16「隔几层留一个」，**间隔是形状**）／`beststep` Ⓒ（「每两个参数之间都有一个」，**N² 的形状**）／`lr-curve` Ⓑ（「依赖终点 vs 可随时截断」是两条曲线的形状）。<br>      ⛔ **剩 6 格没判**（`descent` Ⓒ／`beststep` Ⓑ／`reverse` Ⓓ／`muon` Ⓑ／`stability` Ⓑ／`batch` Ⓒ）——&nbsp;⭐ 判据：**判定要有依据，宁可标「未判」也不要凭标题猜。**<br>      **③ 累计 6 格真该改**：`descent` Ⓑ／`recompute` Ⓑ／`vote` Ⓑ／`recompute` Ⓒ／`beststep` Ⓒ／`lr-curve` Ⓑ。<br>      **④ 盲区总结（比前三条更根本）**：24 格里 23 格的墨水是 **0**，而我判出的误报（数轴、两三根对比条、unit chart、时间轴、坐标图、一排推子）**墨水也全是 0**。<br>      ⭐⭐⭐ 所以问题**不是阈值太严，是形状词汇表太小** ——&nbsp;工具认「成组的」（曲线／柱子组／向量组／箭头组），**不认「少量的、承重的、非成组的」**。已写进工具。<br>      ⭐⭐ 而且现在手上有了一个现成的**回归集**：**18 格已判样本（12 误报 ／ 6 真改）**。要校准这把尺子，就拿它两头都跑一遍 ——&nbsp;收进 R22b。

- [x] ~~**R22b** figink 清账 · 收尾~~ ——&nbsp;**2026-09-18**：**24 格全判完**，改了优先级最高的 1 格，其余按价值重排（见 R22c）。<br>      **① 剩下 6 格判完**：真该改 4（`descent` Ⓒ 分量条／`beststep` Ⓑ 两条抛物线／`reverse` Ⓓ 两个反向漏斗／`muon` Ⓑ 方阵 vs 拍平条）；低优先 2（`stability` Ⓑ、`batch` Ⓒ ——&nbsp;都是实验结论／补充说明，文字尚可）。<br>      ⭐⭐ **24 格总账：12 格误报、10 格真该改、2 格低优先。**（跟按判定经验估的「10 格左右」吻合。）<br>      **② 改了 `fig-muon` Ⓑ。** 旧版两个文字框。⭐⭐⭐ 「拍平」本身就是一个**形状动作**：方阵里对角线特殊（迹／特征值只认对角），拍平成一条之后所有格子长得一样。<br>      ⭐ 判据：**当一个操作会「丢掉结构」时，最好的画法是把「丢之前」和「丢之后」用同一批格子画两遍** ——&nbsp;两边格子数和大小都相同（有 assert），不然读者看到的是「大小变了」而不是「结构没了」。拍平条下面用淡紫小竖线标出原对角线那六个：**散落无规律，正是「看不出来了」**。<br>      ⚠️ **改完 figink 仍判 ❌** ——&nbsp;因为我画的**正是它不认的 unit chart**（等大格子、数量才是数据）。上一轮刚写下这条盲区，这一轮就撞上了。<br>      ⭐⭐⭐ 判据：**按「工具能认」去选画法是本末倒置。**尺子是用来提示复看的，不是用来迎合的 ——&nbsp;**为了让它变绿而改画法，那张图就是为工具画的，不是为读者画的。**<br>      **③ 工具校准没做**，连同剩下 9 格一起重排进 R22c。

> ⭐⭐⭐ **2026-09-18 09:00 现场：「内容挺好，但图非常廉价……在保持正确内容的情况下，能不能把图画得更有质感。」**
> ⛔ **这批插在 R22c 之前** ——&nbsp;它是现场直接指示，而且我上一轮已经答应要做。
> （判据 [[feedback_turn-ends-i-stop]]：说了要做又不做，等于什么都没发生。）

**📋 质感改造（RQ 批）——&nbsp;先诊断后动手，绝不为了好看动内容**

⭐ 诊断有数据（专题四 29 张图）：**613 个 emoji**（光 ⭐ 就 408）、**1520 段文字**（每张平均 52 段）、每个框一块底色、字号挤了 8 档。
⛔ **结论：廉价感来自视觉噪音，不是内容。**对比样张见 https://cc.higcp.com/pages/fig-quality-20260918-090204.html

- [x] ~~**RQ1** 图内装饰符归零~~ ——&nbsp;**2026-09-18 完成**。<br>      改在**绘图基元的文字出口**（一处），不是改 613 处字符串 ——&nbsp;⭐ 而且删字符只会让文本**变短**，不会撑出边界，所以风险低。<br>      ⭐⭐ 语义**没丢**：「重要」本来就由字重和位置表达，「负面／正面」由红／绿表达，「注意」由灰色表达。⛔ 保留 `✓ ✕ ↑ ↓ → ← ⋮` ——&nbsp;**它们是几何记号，不是装饰**。<br>      ✅ 做了 negative test（装饰全删、几何记号全留）；✅ 改动前后**全量版面 lint 逐行对比，无新增告警**（原有那两条 `s012-fig1-2` / `fig-sink` 改动前就在）。<br>      ⛔⛔ **本轮一个方法错误值得记**：我先 grep 了 `topic03_draw.py` 里的 `<text`，看到只有一个出口，就宣布「所有文字都走一个出口」。⚠️ 可我 grep 的是**那一个文件**，不是整个目录 ——&nbsp;结果全站 emoji 只从 613 降到 **59**，剩下的全在**另一条生产线**（`topic02-inject-s012.py` 这类**自己拼 SVG、不 import 基元**的脚本）产出的老图里。<br>      ⭐⭐⭐ 判据：**要做「全站生效」的改动，先数清楚有几条生产线。**我验证的是「这个文件里只有一个出口」，不是「整个目录只有一条管线」——&nbsp;跟 CLAUDE.md 里「MCP 有三个配置源」是同一类错。

- [x] ~~**RQ2** 另一条生产线的装饰符~~ ——&nbsp;**2026-09-18 完成**，**全站装饰 emoji 613 → 0**。<br>      ⭐ 先按 RQ1 立的判据**数清楚了范围**：第二条生产线有 **22 个脚本**（`topic02-fig-*.py` / `topic02-figs-*.py`），各自拼 SVG 字符串、不 import 基元，产出 `fig0-*` / `fig1-*` / `fig2-*` / `fig3-*` / `figC` 和被专题四借用的 `fig4-1`。<br>      ⭐⭐ **做成一个后处理，而不是改那 22 个脚本**：22 处改动 ＝ 22 个出错机会，一个后处理只有一处；而且它**幂等**（走基元那条线的图早没有了，扫过去不变），已接进 `build-all.sh` 图生成之后那一步。<br>      ⛔ **代价说清楚：这是给产物打补丁，不是改根因。** 源码里仍有 emoji，靠每次 build 后剥一遍。哪天那条生产线要重构，应顺手给它也做一个共用文字出口。（已写进脚本头部。）<br>      ⭐⭐⭐ **一处自查**：我在新脚本里**重写了一遍剥离逻辑**，没复用基元的。判据：**两份独立实现的同一逻辑，要各自测** ——&nbsp;已做逐条对拍，11 个用例结果完全一致；产物里 `✓ ✕ ↑ ↓ → ← ⋮` 共 **297 处完好**。<br>      ✅ 全量版面 lint 与基线逐行一致，无新增告警。
- [x] ~~**RQ4a** 去掉卡片填充块~~ ——&nbsp;**2026-09-18 完成**。<br>      ⭐ 先拆开看：**字号收档一定触发版面重排，去填充块不改几何** ——&nbsp;所以先做后者。<br>      ⛔⛔ **填充色在好几张图里是数据**，不能一刀切：`fig-underflow` 的绿色区间带 ＝ 33 个数量级的**范围**；`fig-circuit` Ⓒ 的 175 个粉方块 ＝ **数量**；`fig-muon` Ⓑ 对角线那几格 ＝ **位置**。（⭐ 判据「**改一个视觉属性之前，先问它在哪些地方承载着语义**」第二次用上。）<br>      ⭐⭐ 区分规则（都做了单测）：<br>      　① `fill == stroke` → **染色区域**，保留（区间带就是这种，它压根没有「框」）<br>      　② **中性灰**（`#f1f3f4`／`#f6f7f8`／`#edeff1`）→ 一律转白，**不看尺寸** ——&nbsp;灰不承载正／负，只可能是装饰<br>      　③ 语义色（红绿蓝紫黄）→ 按尺寸判：w ≥ 200 且 h ≥ 60 才算卡片<br>      ⛔ 第一版只有 ①③，漏掉了 `fig-circuit` 那几个 **104×52 的灰底输入框** ——&nbsp;而那恰恰是诊断里点名「像表单输入框」的那一类。补了 ② 才收干净。<br>      ✅ 验证：全量 lint 与基线逐行一致；抽检两张 ——&nbsp;`fig-circuit` 的灰底框全变白底细框；`fig-prepost` 的**红／绿结论框转白后，正负对比仍由边框色和标题色承载，没丢**。

- [ ] **RQ4b** 字号收到三档（11 辅助 / 15 正文 / 22 强调）——&nbsp;**这是 RQ 批里唯一会触发大量版面重排的一步**。⛔ 必须分批，每批跑全量 lint ＋ 逐张截图。
- [ ] **R22c** 待改的 9 格 ——&nbsp;⭐ **按价值排过序，不再一锅端**：<br>      **必改 3 格**（形状极清晰、论点最强）：`reverse` Ⓓ（**两个方向相反的漏斗**，「从窄的那头起步」本来就是几何的）／`recompute` Ⓒ（k＝1／4／16，**间隔就是形状**）／`lr-curve` Ⓑ（**依赖终点 vs 可随时截断** ＝ 两条曲线）。<br>      **中等 5 格**：`descent` Ⓑ／`descent` Ⓒ／`vote` Ⓑ／`beststep` Ⓑ／`beststep` Ⓒ／`recompute` Ⓑ。<br>      **低优先 2 格**：`stability` Ⓑ／`batch` Ⓒ ——&nbsp;**可以不改**。<br>      ⛔ **可选**：用 18 格回归集校准工具的形状词汇表（两头都要跑：12 个误报转绿 ＋ 6 个真改仍判红）。<br>      ⭐ 判据：**清单超预期时要重排优先级，不是让它无限拖** ——&nbsp;必改 3 格做完就往下走，中等和低优先**不阻塞 R23–R30**。
- [x] ~~**R23** 动画 ③：`fig-vanish`~~ ——&nbsp;**2026-09-18 上线**。<br>      ⛔ **第一版被我自己否了**：只画对数轴上那把扇子，而**指数取完对数就是直线** ——&nbsp;跟它上面那张静态图几乎一模一样，动起来只多给了「谁先谁后」。<br>      ⭐⭐⭐ 判据：**一段动画要活下来，得给出静态图给不了的东西。**「同样的内容播一遍」不算 ——&nbsp;那只是替读者把眼睛移了一次。<br>      ⭐⭐ 第二版找到的增量是**对数轴自己的代价**：**它让你看得见十个数量级，也正好让你感觉不到十个数量级有多大。**于是画两条轨、共用一根横轴和一根扫描线：上轨对数（三条才张开三成），下轨线性（**第 8 层蓝线顶出画框、第 18 层红线贴地消失**）。两个层号和那个 31% 都是脚本当场算的，并各有一条断言钉住。<br>      ⭐ 这个反差**只有对齐播才成立** ——&nbsp;静态图并排画两幅，读者不会自己去对同一层。<br>      ④ `fig-onelr` 顺延到 R24。
      ＋ `fig-onelr`（锯齿越弹越大 vs 一路蹭）
- [x] ~~**R24** 动画 ④~~ ——&nbsp;`fig-onelr` **2026-09-18 上线**；`fig-descent` 已在前面做掉。**只剩 `fig-momentum` 一段。**<br>      ⭐⭐⭐ 这一段刻意**换了手法**：前四段的增量都来自「时间」（谁先谁后、重复几遍、一步步降）。⛔ 而静态图已经把 η 大和 η 小各画了一条轨迹，再让它们同时跑一遍，增量就只剩「谁先谁后」——&nbsp;`vanish` 刚在这儿栽过一次。<br>      ⭐⭐ 所以改用**第三个维度：学习率自己**。把 η 做成一根能走的轴，看到的不是两条轨迹，是**一条轨迹的形态在连续演变**：贴着谷底蹭 → 走得刚好 → 开始横跳 → 炸出画面。**静态图只能挑两三个 η 各画一条，「中间发生了什么、拐点在哪」它给不了。**<br>      ⛔⛔ **叙事被自己的数打过一次脸**：初稿写「能用的区间窄得吓人 ——&nbsp;从开始横跳到炸只有一条线宽」，实测那段占标尺 **57%**，一点都不窄 ——&nbsp;我把「横跳区」和「门槛带」混成了一件事。⭐⭐⭐ 判据：**叙事跟着数走，不是调参数去迁就叙事。**改成标门槛 ±5%（标尺上只隔 10%，一边收敛一边发散），这条是真的。<br>      ⭐ 顺带一个结构收获：**参数扫描天然首尾闭合** ——&nbsp;三角波，不用像累积式那两段另造复位段。

**📋 第三批（R25–R30）——&nbsp;收尾（原 R15–R20 顺延）**

- [ ] **R25** 新图：§1 开头那张「你要找的是一个函数」——&nbsp;故事线的入口
- [ ] **R26** 把整条故事线串一遍：检查每一格的「上一格留下什么、这一格接什么」
- [ ] **R27** 历史故事统一收进讲义 ＋ 出处栏，图面清干净
- [x] ~~**R28** 讲稿同步~~ ——&nbsp;**2026-09-18 完成**。教材 31 个 figure，讲稿原缺 **9 个 cue**（3 段动画 ＋ R16/R18/R19 新加的 3 张图 ＋ 3 张老图）。已全部补上并写了对应台词，**31/31 齐全**。<br>      ⛔ 插入时栽了一次：锚点用「该 id 第一次出现」找，而它常常落在 board 之前的**引导句**上 ——&nbsp;结果 `anim-saddle` 被插到了 `fig-saddle` 自己的 board **前面**。⭐ 判据：**要定位一段的位置，锚点必须是那段的结构标记（这里是 board cue），不是它的名字第一次出现的地方。**<br>      ⚠️ 顺序检测也误报过一轮：按「名字第一次出现」比对会把**先预告后详讲**算成乱序。改用「屏幕 cue 首次位置」后，只剩 2 处既有差异（`fig-muon`↔`fig-lineage`、`fig-stability`↔`fig-spike`）——&nbsp;**非本轮引入，留待以后**。<br>      ⭐ 讲稿 76′ → **84′**。~~原：全量重读（⭐ **已知要补**：§3.5 的 warmup 第三段〔R15〕、§6.5 的 LN 机制与 `fig-prepost`〔R16〕，讲稿里都还没有），补齐新图、删掉与图重复的话（⚠️ **已知：`fig-beststep` 至今没有任何 cue**；另外核一遍每张图的 cue 顺序跟教材一致）
- [ ] **R29** 全量 lint ＋ 截图逐张看 ＋ 修<br>      ⚠️ **R17 留的一个信号**：`fig-momentum` 加到四格后是 **1914px，专题四最高**，投屏读数排进了全站最矮 8 张。⛔ 那份报告自己写着「**是不能整张投的读数，不是缺陷**」（117 张里 87 张都这样），所以**没有因此做重构** ——&nbsp;⭐ 判据：**工具自己标了「不是缺陷」的读数，别当缺陷处理**，但可以当信号。这一轮判一下 Ⓓ 该不该拆成独立一张。（⭐ figink 那 16 个已在 R20–R22 清完，这轮只做最后确认）
- [x] ~~**R30** 收尾~~ ——&nbsp;**2026-09-18 完成**：索引页那行从「8 图 · 80′」改成**「28 图 ＋ 3 段动画 · 84′」**（数字是从产物里数出来的，不是手填）；全站 build 绿，专题四版面 0 处告警。

---

### 📚 历史素材库（**已核一手来源**，随轮次取用）

| 事 | 已核到的原文 | 出处 |
|---|---|---|
| **梯度消失是谁发现的** | Hochreiter **1991 年的硕士论文**（慕尼黑，Schmidhuber 指导）**正式指出**了深层与循环网络训练失败的原因。⭐ 故事点：**一篇德文硕士论文，没在英文期刊发过**，却是整个深度学习「第一性难题」的源头 | Wikipedia「Vanishing gradient problem」＋ Schmidhuber 本人主页 `idsia.ch/~juergen/fundamentaldeeplearningproblem.html`。⚠️ **归属有争议**（Schmidhuber 2025 还在为此写文章），⛔ 引用时只说「1991 年那篇硕士论文正式指出」，**不卷进优先权之争** |
| **RMSProp 没有论文** | Adam 论文把它引作 **(Tieleman & Hinton, 2012)** ——&nbsp;那是 **Hinton 在 Coursera 网课上的一页 slide**，从未正式发表 | Adam 原文 arXiv 1412.6980 §1 与 §5，**已读原文核过** |
| **Adam 是两者的合并** | 原文：「combine the advantages of two recently popular methods: **AdaGrad**（擅长稀疏梯度）and **RMSProp**（擅长在线与非平稳设定）」 | 同上，§1 原句 |
| **Adam 的作者顺序是掷硬币定的** | 原文脚注逐字：**"Equal contribution. Author ordering determined by coin flip over a Google Hangout."** | 同上，首页脚注 |
| **Adam 这个名字不是作者起的** | 致谢逐字：**"special thanks to Ivo Danihelka, and Tom Schaul for coining the name Adam."** | 同上，Acknowledgments |
| **李宏毅：训练卡住不一定是梯度小** | 投影片原句 **"Training stuck ≠ Small Gradient"**；以及实测「收敛到的临界点 **never reach a real local minima**」 | `ml2021-course-data/small-gradient-v7.pdf`、`optimizer_v4.pdf`，已读原文 |
| **warmup 的老解释** | 李宏毅投影片给的理由是 **"At the beginning, the estimate of σ has large variance"** | `optimizer_v4.pdf`。⛔ **本讲 §3.5 引 2024 年那篇把这个解释证伪了** ——&nbsp;写的时候要并排呈现，**不能只写一边** |
| **李宏毅的收尾比喻** | **"If the mountain won't move, build a road around it." → "Can we change the error surface? Directly move the mountain!"** | `optimizer_v4.pdf` 末页。⭐ 正好对应本讲 §6「治数值 vs 治结构」两条治法 |

### 🔬 3Blue1Brown 神经网络系列 —— **从源码读出来的**（2026-09-18）

⭐⭐⭐ 现场：「你抄他的故事线、讲法、绘图代码，然后自己画可以动的图。」
⛔ 这一批的可信度是**最高的一级**：不是 subagent 转述，也不是看视频记的，
是**把 `3b1b/videos` 的 `_2017/nn/` 整个拉下来读的源码**
（`part1/2/3.py` 共 **12951 行、151 个 Scene 类**）。

⚠️⚠️ **许可先说清楚**：`3b1b/videos` 的内容是 **CC BY-NC-SA 4.0**
（署名／非商业／相同方式共享）——&nbsp;跟 manim 库本身的 MIT **不是一回事**。
⛔ 我们 `Courses/` 是公开仓库，**照抄代码或画面会被这个许可传染**。
⭐ 所以下面记的全是**结构和手法**，不含任何可复制的内容。

#### 🅐 故事线的骨架（从 `part2.py` 的 61 个类名**顺序**读出来的）

⭐⭐ 类名本身就是一句话，排下来就是叙事线。梯度下降那一集：

| 段 | 类名（原文） | 他在干什么 |
|---|---|---|
| 开场 | `ShowLastVideo` → `ShowPlan` | **先接上一集**，再说这一集的计划 |
| 破题 | `NotSciFi` | ⭐ **先破一个误解**，再开始讲 |
| 归约 | `FunctionMinmization` → `IntroduceCostFunction` | 把问题**归约成一个数学问题** |
| 认怂 | `ThisIsVeryComplicated`／`EmphasizeComplexityOfCostFunction` | **承认它复杂**，不假装简单 |
| ⭐⭐⭐ **降维阶梯** | `SingleVariableCostFunction` | ① 先在**一维**上讲通 |
| | `TwoVariableInputSpace` → `CostSurface` | ② 升到**二维**看形状 |
| | `ConfusedAboutHighDimension` | ③ **主动承认**：再往上想不出来了 |
| | `NonSpatialGradientIntuition` | ④ 于是**不再画空间** |
| 实证 | `TestPerformance` → `ReactToPerformance` → `NoticeWhereItMessesUp` → `WrongExamples` | ⭐⭐ **跑真模型，然后专门看它错在哪** |
| 证伪 | `InputRandomData`／`RandomlyLabeledImageData` → `CompareLearningCurves` | ⭐⭐⭐ **喂随机标签，对比学习曲线** —— 证明它在记忆不是学习 |
| 留白 | `PauseAndPonder` | ⭐ 老师说「停下来想想」，然后**真的 `wait(6)`** |

#### 🅑 六条可复制的手法（读代码读出来的，不是看视频猜的）

1. ⭐⭐⭐ **「不再画空间」是有具体做法的。** `NonSpatialGradientIntuition` 里
   他把 W 画成**一根竖着的列向量**（`w_0 … ⋮ … w_13,000`），
   然后分两步讲：`show_sign_interpretation()`（**颜色＝符号**，蓝正红负）、
   `show_magnitude_interpretation()`（**长度＝大小**）。<br>
   ——&nbsp;**「想象三千亿维」不可能，「看一列条在动」可能。**
2. **一个 Scene 内部也有节奏。** 上面那个类的 `construct()` 只有四行，
   全是 `self.add_vector()` / `self.add_gradient()` / … ——&nbsp;
   **每个方法只讲一件事**，顺序即讲法。
3. **拟人化**：`YellAtNetwork`（对着网络吼）、`NetworkGrowthMindset`。
4. **主动承认困惑**：`ConfusedAboutHighDimension` 是一个**独立场景**，
   不是一句带过 ——&nbsp;⭐ 他把「你现在应该懵了」当成一个要专门处理的节点。
5. **真的留白**：`PauseAndPonder` 里 `teacher_says("Pause and ponder!")` 之后
   直接 `self.wait(6)`。⭐ 六秒在视频里是很长的。
6. **先讲算法再给名字**：`GradientDescentAlgorithm` 排在 `GradientDescentName` **前面**。

#### 🅒 对照专题四：我们缺的两条（这是最值钱的发现）

| | 他有 | 我们 |
|---|---|---|
| 降维阶梯 | 一维 → 二维 → 承认想不出来 → 非空间 | ⭐ 有（`fig-descent`／`fig-saddle`），但**没动起来** |
| ⛔ **跑真模型看它错在哪** | `TestPerformance` → `NoticeWhereItMessesUp` | **完全没有** |
| ⛔ **证伪式反例** | 喂随机标签，比学习曲线 | **完全没有** |
| 拟人化 | 多 | 少 |
| 显式留白 | 有 | 少 |

⭐⭐⭐ 后两条的共同点：**他不只讲原理，他让模型当众失败一次。**
一次失败比十句「要注意」有用 ——&nbsp;而这正是我们整讲都缺的那个动作。

---

### 🔬 李宏毅 ＋ 其他高手专场（2026-09-18，另两个 subagent）

⛔ **先分级 —— 「谁看过什么」决定这条能信到什么程度：**

| 级 | 含义 | 本批哪些 |
|---|---|---|
| **Ⓐ 我自己看过原图** | 最高。可以直接据此重画 | 李宏毅 `opt-12`（我打开看了） |
| **Ⓑ agent 渲染成 PNG 看过原图** | 现象可信，**细节方向要自核** | 李宏毅其余 7 条、Olah、Alammar、GroupNorm、loss-landscape |
| **Ⓒ agent 只读了源码 / 网页文字** | 只当线索，配色比例一律不可引用 | Karpathy 三个 notebook 的全部条目 |

⭐⭐⭐ **Ⓐ 级那一张当场就抓到 Ⓑ 级的一处错**：agent 说 AdaGrad 轨迹
「突然**朝上**喷起一道竖线」——&#160;我打开 `opt-12` 看到的是**向下扎出去再弹回来**，
**方向反了**；而且那一页是**三张共用同一底图的 contour**（两小一大），不是一张。
（「幅度一次比一次小」这条它说对了。）
⛔ 这跟我自己在 `fig4-vanish` 上把箭头画反是**同一类错**：
**方向是最容易在转述里翻面的属性，因为读的人心里已经有了「该往哪走」的预期。**

⭐ 本地参考图在 **`~/ref-figs/hylee/`**（26 个 PNG/PDF）——&#160;
⛔ **刻意放在仓库外**：那是他人版权材料，我们的做法是
**用自己的基元重画 ＋ 标出处**，不是搬运。

---

#### 🅐 对本讲最有行动价值的四条（按「补的是哪个洞」排）

**① Olah：同一张计算图画两遍，只换标注方向** —— 补 §1「为什么从后往前」
`e=(a+b)(b+1)`，第一遍每个节点标 `∂·/∂b`（从下往上），第二遍标 `∂e/∂·`（从上往下）。
⭐⭐ **这个洞是两个 agent 合起来填上的** ——&#160;
扒李宏毅那个明确报「ml2021 系列里**没有**反向传播的独立投影片，
它放在延伸影片里，没核到原文的我没写」；而 Olah 那篇恰好整篇都在讲这个。
⭐ 判据：**反向传播的关键不是「算得快」，是「换了一个合并方向」——&#160;
所以它必须用「同一张图的两种遍历」来呈现，任何只画一遍的图都说不出这件事。**
出处：https://colah.github.io/posts/2015-08-Backprop/

**② 混合精度：梯度对数直方图 ＋ 一条 FP16 下界竖线** —— 补 §6.6 loss scaling
竖线**左边一大片**标「换成 FP16 后会被抹成 0」。
⭐ 判据：**「精度不够」是形容词，「这一竖线左边全没了」是可数的损失。**
⚠️ 我们 `fig4-precision` Ⓐ 已经有位段条（跟下面 Raschka 那张撞了），
**但没有这条竖线** ——&#160;而位段条讲的是「能表示多细」，竖线讲的是「能表示多小」，
**这正是 §6.6 那个误解的分界**。这是真缺口。
出处：Lilian Weng 转 Narang & Micikevicius 2018，https://lilianweng.github.io/posts/2021-09-25-train-large/

**③ update-to-data 比值曲线 ＋ 一条画在 −3 的黑线** —— 我们完全没有
纵轴 `log10(std(lr·grad)/std(param))`，每个参数一条线，贴着黑线就说明学习率对了。
⭐ 判据：**把一个抽象超参变成一条可以用眼睛对齐的参考线。**
判据 1e-3 的出处是 CS231n《Neural Networks Part 3》"Ratio of weights:updates"。
⚠️ Ⓒ 级（agent 读的是 notebook 源码），**引用前必须自己跑一遍或核 CS231n 原文**。

**④ Lili Jiang：给球挂一个「会长大的方块」** —— 深化 `fig-lineage`
方块边长 ∝ 累积平方梯度，动量画成箭头。AdaGrad 的方块一路胀大到球几乎停住。
⭐⭐ 判据：**优化器的差别全在「内部状态」，而内部状态平时不可见 ——&#160;
把它画成一个挂在球身上的实体，「Adam ＝ 箭头 ＋ 方块」就成了图上的一句话。**
原作是 3D 动画，静态化：等高线俯视 ＋ 3–4 个快照，保住「方块随步数变大」这条线索。
出处：https://github.com/lilipads/gradient_descent_viz

---

#### 🅑 李宏毅的画法通则（八条，agent 每条都附了页码证据）

1. **一张底图用到底，每次只换一个变量。** `optimizer_v4` 第 4/12/13 页是**同一张** contour，
   同刻度、同起点、同一个橙色 `×`，只换轨迹。读者认一次图，之后注意力全给差异。
2. **坏的先画，好的紧跟，并排同尺寸，方向固定「左坏右好」。**
3. **标注写成因果短语链，不写名词。** 例：`+Δw₂ → +Δy large → +Δe large → +ΔL large`。
   ⭐ **没有一处标注只是给对象命名。**
4. **颜色绑对象，不绑好坏；红色只留给「看这里」**（圈公式关键项、画运动箭头、圈异常段）。
5. ⭐⭐ **同一页同时放「学习者看得见的」和「他看不见的」。**
   小人只有脚下一块踏板，但曲线右边的深谷读者看得见。
   **信息差是画出来的，不是说出来的。**
6. **真实数字不抹平成示意图** ——&#160;保留 matplotlib 原始刻度，标 `100,000 updates`。
7. **抽象概念配一张实物照片当锚，并标来源**（高维配石棺、BN 配青霉素 + `serendipitous`）。
8. ⭐ **一页只新增一个元素。** PDF 里的「重复页」其实是 build ——&#160;
   `normalization_v4` 第 2 页只有左半，第 3 页才补右半。**从不一次性铺满。**

#### 🅒 Olah 的「少写字」是怎么做到的（这条最可复制）

《Understanding LSTM Networks》全篇**只有一张 LSTM 单元底图**，原样重复四次，
每次只把其中一条通路加粗描黑（遗忘门／输入门／更新／输出）。
配套两个动作：① 先发一张**图例**，把「粉圆＝逐元素、黄方块＝一层带权网络、
线合并＝拼接、线分叉＝复制」一次讲完，此后不再用文字解释；
② 变体章节继续复用同一套记号，**差别由图承担，文字只说「为什么要这么改」**。

⭐⭐⭐ 判据：**他的路子不是「把解释塞进图注」，也不是「塞进悬停交互」，
而是建立一套一致的图形记号，然后用「同一张图 ＋ 移动高亮」代替反复叙述。**
⭐ 而且这是**降成本**的画法 ——&#160;一套底图复用四次，画图工作量反而更小。

#### 🅓 其余可直接重画的（Ⓑ/Ⓒ 级，用前按分级自核）

| 装置 | 讲什么 | 出处 |
|---|---|---|
| Olah 二部图 `(α+β+γ)(δ+ε+ζ)` | 为什么反向一趟能拿到对所有输入的导数 | colah 同上 |
| micrograd 节点里塞 `data`／`grad` 两个数 | 前向填左边、反向填右边，autograd 就这点事 | github.com/karpathy/micrograd（**原文件就是 SVG**） |
| 五层激活/梯度直方图叠在一张坐标上 | 「越往前越小」的**量感在曲线间距里** | nn-zero-to-hero makemore part3（Ⓒ） |
| `dlogits` 当成 32×27 灰度照片 | 交叉熵梯度到底在「要求」什么：每行一个暗像素、行和为 0 | 同上 part4（Ⓒ） |
| Alammar：误差画成黄色竖条 | 「That's quite a bit of yellow. Yellow is bad.」损失从数变成**面积** | jalammar.github.io |
| Raschka 显存「下楼梯」七连图 | **每次只改一个变量**，因果关系锁死 | sebastianraschka.com/blog/2023/pytorch-memory-optimization |
| GroupNorm Fig.2 四个立方体涂蓝 | 四种归一化的唯一差别＝沿哪几个轴取均值 | arxiv.org/pdf/1803.08494 |
| loss-landscape 有无 skip 两张曲面 | 残差让优化变容易 ＋ **损失曲面不总是光滑山谷** | github.com/tomgoldstein/loss-landscape |
| Distill 动量：按特征值拆成 3 个一维问题 | **步长只能迁就最差的那个方向** | distill.pub/2017/momentum |

⛔ **一条诚实的负面结论，省下以后重搜**：agent 明确报
**中文圈没找到敢交的原创视觉装置** ——&#160;知乎/CSDN 的高热文章内容不差，
但视觉部分要么是 d2l/CS231n 图的转载，要么只有公式和表格。
苏剑林那一支是**推导型写作，图本来就少**（所以他那批收的是「框架」不是「画法」）。
⚠️ 还有一个洞没人填上：**梯度爆炸**没找到够格的装置，只有通用的梯度范数监控曲线。

---

### 🔬 苏剑林专场（2026-09-18 派 subagent 扒的，七条全读过全文、原话逐字）

⛔⛔ **这一批的规矩比素材库更严一档**：它们是**别人（subagent）读的**，不是我读的。
⭐ 判据：**二手转述不能直接进课件** ——&#160;
每条在**真正用进图或讲稿之前，我要自己再打开那篇文章核一遍原话**。
下面记的是「值得去核什么、去哪儿核」，不是「已经可以用的结论」。
（memory 里那条 `feedback_summary-is-secondhand-narrative` 说的就是这个。）

| # | 框架（一句话） | 它推翻/收紧了什么 | 出处 | 跟本讲哪儿对撞 |
|---|---|---|---|---|
| 1 | **有限学习率本身就是一个正则项** ——&nbsp;离散化误差 ≈ 往 loss 里加梯度惩罚 | 「学习率小＝解得准只是慢」的前提是**你要 ODE 的精确解**；而深度学习要的不是 | [7787](https://kexue.fm/archives/7787) | §3.5 学习率曲线 |
| 2 | ⭐⭐⭐ **Post-LN 下 LN 是梯度消失的「元凶」之一** ——&nbsp;残差通道被按 `2^(l/2)` 指数衰减 | 「残差＋LN 解决梯度消失」的前提是 **Pre-LN**。而且他不停在这儿：既然是元凶为什么还留着 | [8747](https://kexue.fm/archives/8747) | **`fig-vanish` ＋ §6.5 Pre/Post-LN** |
| 3 | ⭐⭐⭐ **warmup 治的不是「更新量太小」，是「更新量全是噪声」** ——&nbsp;Adam 把微弱信号和纯噪声一起放大成 O(η)，于是梯度消失从「学不动」变成「乱学」；warmup 主要在**踩刹车压住后面的层** | 「有 Adam 就不怕梯度消失」的前提是你担心**幅度**。⭐ 还给了可证伪判据：不 warmup 会**先快速收敛到一个常数、再发散到 NaN** | [8747](https://kexue.fm/archives/8747) | **§3.5 warmup —— 那儿已经有两个解释了，这是第三个** |
| 4 | **自适应学习率的原始动机是误差控制**，不是「每个参数配一个学习率」；而**逐分量**那一支是「理论较差、实验赢了」的那个 | 「自适应 ＝ per-parameter」被当成定义，其实原始推导只要一个**标量** | [6234](https://kexue.fm/archives/6234) | **`fig-lineage`** |
| 5 | ⭐⭐ **β 和 η 不是两个独立超参** ——&nbsp;学习率从 α 降到 rα 时，β 该提到 `1−(1−β)√r` | 「decay 学习率」等于**偷偷加大了摩擦力** | [5655](https://kexue.fm/archives/5655) | **`fig-momentum` Ⓒ** |
| 6 | **Weight Decay 是「记忆周期」旋钮** ——&nbsp;模型顶多记住 `O(1/λη)` 步 | 「WD ＝ L2 正则防过拟合」的前提是**多轮训练**。LLM 单轮预训练根本没有过拟合可防 | [11459](https://kexue.fm/archives/11459) | AdamW 那一段 |
| 7 | ⭐⭐ **batch 变大，最优学习率可能要「降」**（Surge 现象）——&nbsp;`η* = η_max/(1+B_noise/B)`，有上界 | 「batch 大 → 梯度准 → 学习率可以大」隐含了**无上界外推**；而 Adam 下 `B→∞` 收敛到 `sign(g)`，**而 sign(g) 本身就不是最优方向** | [10542](https://kexue.fm/archives/10542) | **`fig-batch`** |
| 8 | （备用）**线性衰减是理论最优的学习率调度**，终点损失 `O(1/√T)` 达信息论下界 | 「α/√t 这类不需预知总步数的更优雅」——&nbsp;换成「终点损失」口径它们必输 | [11494](https://kexue.fm/archives/11494) | §3.5 cosine vs WSD |

⭐⭐⭐ **另外四条是「怎么写」的方法论，比上面任何一条都通用：**

1. **反问句直接当小节标题，一节翻一面。** 《BERT 0.02》全篇骨架就是四个反问：
   梯度消失说的是什么 → **LN 真能缓解梯度消失？**（推翻）→ **那为什么还加 LN？**（平反）→
   **我们真的担心梯度消失吗？**（把问题取消掉）。
   ⭐ **关键在第三步：推翻之后一定要回头说清「那它为什么还在」——&nbsp;不做这步就成了抬杠。**
2. **先把常识答案喂饱，再问它覆盖不到的角落。** 不否定常识，而是给它划一条边界线，只在线外做文章。
3. **替读者先说出反对意见。** 他会专设一节自我诘难，甚至承认「这个假设本质上是错误的」，
   再「挣扎一下」。⭐ **认错不削弱说服力，反而是最强的说服力 ——&nbsp;它证明作者比你更早想到了你的反驳。**
4. **把一个「因为太简单而没人怀疑」的默认设定拎出来当问题。**
   例：「为什么梯度下降一定要是一阶的？二阶的行不行？」——&nbsp;整篇动量就从这一问长出来。

---

⛔ **素材库的规矩**：**只放已经读过原文的**。道听途说的历史一条都不进 ——&nbsp;
历史故事最容易讲得绘声绘色而全是假的，而它一旦假了，整份材料的可信度跟着赔进去。

---

## 待办清单（2026-09-17 04:50 HKT 记，接力用）

> ⭐ 这一份是**给下一轮自己看的**。`turn 一结束我就不存在了`，
> 所以每收一批就把剩下的写回这里 —— 不写，下一轮要重新把上下文挖一遍。

**已闭环（本轮四个 commit）**
- 红队甲类 A1/A2/A3/A4/A7–A12 全部核过原文后修正
- 乙类 B2/B3/B4/B6/B8/B11/B12/B16 修正；判据 ① ③ 重写
- §2.3 排序表改成**由闭式解生成**（不再手填）
- 新图 `fig4-spike`（§6.1 那张欠着的「画面」）＋ 讲义 3 条 cue
- 两条 lint 自身的 bug：readability 没挖 `<style>`；xref 的计数标记侧全是噪声
- `topic03_draw.save()` 加 `.svg` 断言

**社区讲法的料在哪（2026-09-17 现场再次强调：多图多彩、字尽量减少、以画图为主、话放讲义）**

| 来源 | 位置 | 已用在哪 |
|---|---|---|
| **苏剑林**（科学空间） | 网上 | Adam update RMS ＝ 0.2 的闭式解（§3.3）；Muon 的「迹」与范数视角（`fig4-muon` Ⓑ Ⓒ）。<br>⭐ **2026-09-17 第二轮**：《为什么我们偏爱各向同性？基于最速下降的理解》里那句**「『梯度反方向是下降最快的方向』有前提，最关键的前提是度量取了欧氏范数」**→ 补进 `fig4-descent` Ⓑ 末尾一行。<br>⛔ **刻意只留一句指针，不在那儿再画一遍** —— 「F 范数 → SGD／谱范数 → Muon」的圆与方 `fig4-muon` Ⓒ 已经画了。⭐ 判据：**同一个画面全书只画一次，别处只留指针。** |
| **zartbot（渣B）** 公众号 | 本地已扒 `~/learning/zartbot/`，**360 篇 md** | 归属链核对（Muon → Keller Jordan，苏剑林分析，MuonClip → Kimi K2）。<br>⚠️ 这批偏**时评**不偏教学，直接可搬的比喻不多 |
| **李宏毅**（台大） | 投影片 `speech.ee.ntu.edu.tw/~tlkagk/.../DNN backprop.pdf` | ✅ **2026-09-17 已用两处**：<br>① 「backward pass 就是另一张 neural network」——同拓扑、边反向、权重转置，每个节点从「套非线性」换成 **multiply a constant**（他投影片原词），而那个常数在前向就定死了 → `fig4-reverse` 新 Ⓒ。<br>② **「最优的一步 ＝ |一阶导| ÷ 二阶导」**，以及「用一阶导去估二阶导」——这是「**为什么要除**」最深的那个答案 → 新图 `fig4-beststep`（§3.3）。<br>④ **2026-09-17 第二轮**：「导数为零的点分 local minima 与 saddle point，判据是 Hessian 特征值全正与否；实测 **never reach a real local minima**」→ **新图 `fig4-saddle`（§3.3）**。⭐⭐⭐ 这是「梯度下降**为什么可以**」最深的那半个答案 —— 上一格 `fig-descent` 老实说了「落哪个谷看起点」，这一格负责拆掉它留下的恐慌：**维度一多，坡度为零的地方绝大多数只是垭口。**<br>⛔ 三条克制写进了脚本头：不画他那张实测散点（手上只有定性结论）、不给「罕见到什么程度」配概率（特征值符号不独立，那个数是编的）、三体典故只转述投影片原话不展开情节。<br>③ batch：「有平行运算时大小 batch 单次时间差别不大，而一个 epoch 反过来」→ 新图 `fig4-batch`（§1.8）。⛔ **但这条结论我们没照抄** —— 它成立的前提是并行度没吃满，而大模型基本在吃满之后那一段，所以图画的是**结论的适用区间**而不是结论 |
| CS231n | 网上 | 电路图 ＋ 三个门（`fig4-circuit`） |
| **3Blue1Brown** | 本地 `~/3b1b-videos/` **只有 2024 transformer 系列，没有梯度下降那集**；官方讲义 `3blue1brown.com/lessons/gradient-descent` | ✅ **2026-09-17 已用**，四个讲法逐条核过原文 → 新图 `fig4-descent`：球滚下山、步长 ∝ 斜率所以自动刹车、落哪个谷取决于起点、高维改看那一列数的正负与相对大小。<br>⭐⭐⭐ **2026-09-17 第二轮又扒了第二部**《What is backpropagation really doing?》→ **新图 `fig4-vote`（§1.2）**，四个装置逐条核过原文：nudge（推多少 ∝ 差多远）／想让一个数变大的三条路／**改权重要按上游亮度成比例**／**众口难调，只听那张 2 的网络会把所有图都判成 2**。<br>⭐⭐ 它一口气接上本讲三处（**接头都是本讲自己的合题，原文没有**）：Ⓐ ＝ §1.2 那颗种子的人话版；**Ⓑ「上游越亮越划算」＝ §1.6「反向必须用前向的值」的人话版，激活扔不掉的根子**；Ⓒ 那个「取平均」＝ §1.8 跨卡汇总真正在干的事 |

⭐ **本讲已经在用的生活比喻**（新加的往这儿记，免得重复）：
一条河（稳定性三类治法）｜ 存档点密度条（√L）｜ 种子插在哪一头（正向 / 反向模式）｜
**同一张网倒着走**（反向的形状，李宏毅）｜ **三条泳道的流水线**（梯度的一生）｜ **两条深浅不同的谷**（为什么要除以二阶导，李宏毅）｜ **球滚下山 ＋ 两个起点两个谷**（梯度下降为什么可以，3B1B）｜ **一条折线的两段**（batch 何时白赚，李宏毅）｜
**垭口**（鞍点 —— 沿路是最高点，横过来沿山脊是最低点，坡度为零却有路可走，李宏毅 + 本讲自己的比喻）｜ **一屋子人各提各的要求，最后取平均**（梯度是什么，3B1B）｜ **把连过来的线加粗，上游越亮越划算**（权重梯度为什么要乘前向输入，3B1B）｜
同一根条被削三次（ZeRO）｜ 分发器 / 交换器 / 路由器（三个门）｜ 岔路一进两出（反向两笔）

---

**⭐ 正文瘦身：已开工，判据定了**

现场：「字尽量减少，以画图为主，有想说的话都放到讲义里去」。
⭐ 判据：**一段正文该不该留，问「图和讲义都有了之后，它还剩什么是别处没有的」**
——&nbsp;什么都不剩就删，剩一句就只留那一句。
- [x] §3.3（最胖的一节，近 2000 字）—— **2026-09-17 第一刀**：
      顺带发现一个更该先修的：`fig-descent` / `fig-beststep` 原来排在
      这一节**所有正文之后**，讲「为什么」的图排在讲「是什么」的后面。
      已调序 —— descent 提到全节最前（它是本源），
      beststep 紧跟在「把尺度除掉」那句之后（它回答「凭什么是除」）。
      ⭐ 判据：**一张回答「为什么」的图，要排在提出那个「什么」的段落之前或紧邻。**
- [x] §3.5 —— **2026-09-17**：同一个毛病（`fig-lr-curve` 也在全节 1700 字之后），
      已提到 lead 之后；warmup 那块自曝压缩。
- [x] §2.4 —— **2026-09-17**：`fig-per-byte` 从 851 字之后提到最前，
      它就是那一节的全部画面。
- [x] **做成工具**：新增 `topic02-lint-figpos.py`，每次 build 量一遍
      「每张图前面已经放了本节百分之多少的字」。
      ⚠️ **刻意只报告不断言** —— 量完全书才发现末尾的图多数是对的
      （收束型），真错的只有那四张（先决条件型）。
      判断要按**图的作用**，不能只看位置；只看位置的断言会把
      四条真问题和七条正常情况一起报，然后人就不看它了。
- [x] §1.8 —— **2026-09-17**：开头那个五条编号列表换成新图 `fig4-gradlife`
      （三条泳道：做什么 / 能不能藏 / 推理里有没有），后面两段被图取代的正文删掉，
      净省 600+ 字符。⭐ 判据：**一个「依次经过若干道」的过程，
      列表只表达顺序，流水线图能同时表达顺序、并行和缺席。**
- [x] ~~§3.2（1195 字）~~ ——&nbsp;**2026-09-17**：那段 <ul> 本质上是一次算术
      （「小于 1/256 的部分会被舍掉」），已换成 `fig-precision` 把那次算术真做一遍，净减四百余字。
      ⭐ 判据：**一段正文如果本质上是一次计算，就让图把那次计算做出来，正文只留结论。**
- [x] ~~§3.4（1123 字）~~ ——&nbsp;**2026-09-17**。删的不是「写得啰嗦」的那部分，
      是**跟 `fig-muon` Ⓓ 逐字重复**的那两段（FLOP<1%、「FLOP 口径不是墙钟口径」）。
      ⛔⛔ 而对照时撞出一处**自相矛盾**：正文写「单步时间那栏加了」，
      图上写「它花的是本来就闲着的算力」——&nbsp;两句各有出处、各自都对，
      可放在同一页上，读者只会觉得其中一句是错的。
      已改成一句调和：**「更慢」说的是方向，「塞进空窗」说的是幅度，
      而幅度是你的配置说了算的 —— 真要用自己量。**
      ⭐ 判据：**同一件事在正文和图上都出现时，「各自正确」不够 ——
      还得让读者看得出这两句在说同一件事的哪两个侧面。**

---

**⭐⭐⭐ 2026-09-17 现场新指令：「反向传播、梯度下降、微积分、偏导数都讲得明明白白，
到底 loss 怎么变成梯度、怎么更新；所有的精度问题也写明白。画图讲。」**

按这条清点了一遍，四块里三块已经有图了：

| 要讲明白的 | 现状 |
|---|---|
| **loss 怎么变成梯度** | ✅ `fig-vote`（人话版：一屋子人提要求取平均）＋ `fig-circuit`（小电路当场验）＋ `fig-3x`（两次矩阵乘）＋ `fig-reverse`（为什么从后往前） |
| **梯度怎么变成更新** | ✅ `fig-descent`（本源）→ `fig-saddle`（高维里卡不死）→ `fig-onelr`（一个学习率不够用）→ `fig-beststep`（凭什么是除）→ `fig-muon` |
| **精度** | ✅ **2026-09-17 新** `fig-precision` —— §3.2 原来是全讲**唯一一节上千字零图**的。核心画面就一个：**大数吃小数**。脚本里有真的 bf16 舍入函数，`1.0` 每步加 `3e-4` **连加一千次还是 1.0**（三条 assert 盯着）。Ⓐ 顺带解掉 6.6：fp16 要 loss scaling 跟「16 位不够」无关，是它把 3 位从指数挪给了尾数 |
| **微积分 / 偏导数本身** | ✅ **2026-09-17 补上** `fig-slider`（排在 §1.1 最前，就在「求导是高中的事」那句后面 —— 那句话正是麻瓜掉队的地方）。主比喻两个：**调音台的推子** ＋ **换汇**。<br>· 导数 ＝ **你动一格，它动几格**（⛔ 不是「现在是多少」——&nbsp;那是值，这是兑换率）<br>· 偏导数 ＝ 「偏」字全部含义就三个字：**按住不动**；三千亿个推子各自那个数排成一列 ＝ **梯度**<br>· 链式法则 ＝ **换汇**，人民币→港币→美元，总汇率当然是乘出来的<br>⭐ 「兑换率」这说法自带两个钩子（都是本讲自己的）：**从哪头开始乘代价差一万年**（接 §1.5）、**单位对不上所以必须再乘一个折算系数**（接 §3.3）。<br>⚠️ 刻意不碰极限 —— 画的是差商，而 §1.1 那个「笨办法」用的正是差商，不严格反而接得更顺 |

---

**还欠着的，按价值排**

1. **缺图**（「少说话多画图」这条最直接的落点）
   - [x] ~~§1.5 从后往前的漏斗~~ ——&nbsp;**2026-09-17 完成**（`fig4-reverse`，画的是「种子插在哪一头」）
   - [x] ~~§2.1 存档点密度条~~ ——&nbsp;**2026-09-17 完成**（做成 `fig4-recompute` 的新 Ⓒ：17 / 8 / 17，两头一样高）
   - [x] ~~§4.2 ZeRO 四行条图~~ 2026-09-17 完成（`fig4-zero`） —— 不切 / 切优化器 / 再切梯度 / 再切权重
   - [x] ~~§5.1 常驻块 vs 激活随序列长度的两条线~~ ——&nbsp;**2026-09-17 判定不画**。
         动手前先验了关系：激活账里**没有 S² 项**（分数矩阵不落显存），
         所以它随 token 总数线性 —— 沿 S 轴的交点落在 1,230 万 token，没意义；
         而有意义的那个交点 `fig4-step` Ⓑ 已经沿 batch 轴画了（94 条），是同一个点。
         ⭐ 核查过程翻出一件更值钱的事，已写成 §5.1 正文：
         **显存只认 token 总数，算力还要看你怎么切** —— 正好接上 §5.2 的 6ND 低估。
         ⛔ 判据：**一条「该补图」的待办，动手前先验它要画的关系成不成立。**
2. **逐图审的改法**（已想好，未动手）
   - [x] ~~`fig4-stability` A → 一条河~~ ——&nbsp;**2026-09-17 完成**。
         原来是三张并排的卡片，读者记住的是「有九个技巧」，不是「它们各治一段」。
         换成河之后，「上游 / 中游 / 下游」这三个词自己就把分工说完了；
         而且顺带带出一件卡片版讲不出来的事：
         **上游的活是一次性的，下游的活每出一次事就得干一次。**
   - [x] ~~`fig4-recompute` B → 存档点密度条~~ ——&nbsp;**2026-09-17**：B 保留（它讲的是「一层之内留哪些」），密度条新开一格 C（「隔几层留一个」）—— 两个不同的轴，不该互相替换
   - [x] ~~`fig4-muon` C → 圆形与方形的约束区域~~ ——&nbsp;**2026-09-17 完成**。
         画在**奇异值平面**上：F 范数是圆、谱范数是方，同一个梯度方向，
         圆的最远点在延长线上（保持比例 → SGD），方的最远点是那个角
         （两个奇异值都变成 1 → 正是 msign）。**Muon 是从方角上掉出来的。**
   - [x] ~~`fig4-3x` B → 岔路图~~ ——&nbsp;**2026-09-17 完成**（A 保留，它数的是「几次乘法」，没问题）。
         B 原来是两张并排的问题卡，读者看到的是「有两件事」，看不到「为什么正好两件」。
         改成一进两出的岔路：上支乘前向输入 → 权重梯度 → **到此为止**（没有出口箭头）；
         下支乘这层权重 → 新敏感度 → **接着往左走**。
         ⭐ 判据：**「为什么是 N 个」要用图形结构回答，不要用并列的卡片** ——
         并列只表达「有 N 个」，结构才表达「为什么是 N 个」。
   - [x] ~~`fig4-optimizers` B → 加分组括号~~ ——&nbsp;**2026-09-17 完成**，
         但分的不是原计划那两组。那根条上真正该划的一刀是**全讲的主轴**：
         **2 B 推理也要 ｜ 14 B 训练才要**。
         ⭐ 判据：一根条上如果有一刀是全讲的主轴，那一刀就该画在条上，不是写在条下。
3. **讲义缺口**
   - [x] ~~讲稿~~ ——&nbsp;**2026-09-17 补齐，41 条 cue / 64′**（起点 12 条 / 31′）。
         已补：`fig-circuit`、LoRA、`fig-slider`、`fig-vote`、`fig-saddle`、
         `fig-onelr`、`fig-precision`、**`fig-muon`**。
         ⭐ `fig-muon` 这条**刻意只补了一分钟版、且只放 Ⓒ** ——
         本节的讲法约束原本就写着「Muon 控制在一分钟，展开 Newton-Schulz 会带偏节奏」，
         所以补的不是一整段，是**那一分钟里最该上屏的一格**（圆与方），
         其余三格明确标「这次别放」。
         ⛔ 判据：**补讲稿要服从这一节原有的时间约束，不能因为「图好看」就撑开它。**
         ⭐ **3.6 / 3.7 也补了**，各一分钟：β 是「记多久」的旋钮（窗口 ≈ 1/(1−β)）、
         checkpoint 的大小正好就是那 12 字节（＋ 步数 / 数据位置 / 随机数状态三样）。
         ⛔⛔ 而补的时候撞出一处**讲稿与教材的漂移**，比缺讲稿严重得多：
         **教材的全课落点是四条判据，讲稿收尾只念了三条，而且第三条
         （「改变数据分片形状的参数不能跨规模照抄」）在教材那四条里根本没有** ——
         讲义自己的时间表却还写着「最后那四条判据是全讲的落点」。已按教材改齐。
         ⭐ 判据：**board-cue 的 lint 只对得上图的 id，对不上「讲的内容是不是同一套」** ——
         这类漂移只能靠人把两边并排读一遍。
   - [x] ~~时间重配~~ ——&nbsp;**2026-09-17 重配**，按内容实际长出来的样子：
         零3 / 一15（＋circuit）/ 二15 / 三22（＋LoRA）/ 四5（有 fig-zero 后更快）/
         五11 / 六9 ＝ 80，附录不进课堂。地板从 31′ 涨到 **40′**（每次 build 现数）。
4. **麻瓜天团点名、还没答的三条**
   - [x] ~~Q1 小规模手算例子~~ ——&nbsp;**2026-09-17 完成**，新 §5.4：
         125M、一张卡、batch 8×1024，从 16 字节一路算到「激活 2.11 GiB 反而大过常驻 1.86 GiB」。
         落点是**换了 5,000 倍规模，比例基本没变** —— 这一讲教的是比例不是那些数。
   - [x] ~~Q2 LoRA / 微调零覆盖~~ ——&nbsp;**2026-09-17 完成**，新 §3.8。
         ⭐ 我原来以「范围收紧了」为由跳过它，但回头对现场那条界
         （「推理里没有的那些东西」）—— **LoRA 完全在界内**，它只是被漏了。
         判据：一个话题该不该讲，看它能不能用已讲的东西讲完；能，就该讲，而且会很短。
   - [x] ~~Q6 框架里那个开关叫什么~~ ——&nbsp;**2026-09-17 完成**，新 §7.0 对照表：
         Megatron / PyTorch / JAX / DeepSpeed / bitsandbytes，**每一个名字都当场 grep 源码核过**，
         并标了「从哪个文件查到的」。⭐ 顺带发现两个 API 名字里就写着本讲的判据
         （`dots_with_no_batch_dims_saveable`、`core_attn` 是默认）。
5. **剩余乙类**
   - [x] ~~B1 warmup 那篇自陈小规模~~ ——&nbsp;**2026-09-17**。查了原文 Limitations：
         「相对小规模的数据集和模型，能否推广到更大规模还需进一步研究」；
         用的 Transformer 只有 4 层、上下文 64。已把「明确说这不是主因」收成
         「在那个尺度上证伪了，能不能搬到千亿参数上他们自己没说能」，并自曝这一处很讽刺 ——
         §2.6 的判据正是「小规模验过的到目标规模必须重验」，我转述时自己没守。
   - [x] ~~B5 「预测减真值」的求导对象~~ ——&nbsp;**2026-09-17**。说明它是对 **logits** 求导，
         不是对概率；并点出这个区别的后果：约掉的正是 softmax 那一步，
         所以梯度不会在 softmax 饱和处消失 —— 这对组合总是成对出现，一半原因在这儿。
   - [x] ~~B10 那个 3 哪来的~~ ——&nbsp;**2026-09-17**。承认是我定的、不是算的：
         它落在 2.20 和 7.70 中间的空当里，放 2.5 或 5 结果一样。
         真正定这条线的是**你的兑换率**（一 GiB 显存值多少 TFLOP）——
         排序是客观的，切在哪是配置说了算。
   - [x] ~~B18 / B19~~ ——&nbsp;**2026-09-17 判定不可恢复，结案**。
         查了全仓库和 git 全历史：这一条**从建清单的第一次提交（`e3021f6`）起
         就写着「内容待重查」** —— 给它编号的那份评审报告本身没有进仓库，已经找不到。
         ⛔ 一个指向已丢失文档的编号**不是可执行的待办**，留着只会永远挂在清单上。
         ⭐ 同批的 B1 / B5 / B10 都已逐条落地；真要重新体检这块，
         走 `muggle-review` 重新生成一份带内容的清单，比追这两个号有意义。
         ⛔ 判据：**查不到就明说查不到，然后结案 —— 不要把「我不知道」伪装成「待办」。**
6. **对标报告里还没搬进来的借鉴**
   - [x] ~~CS231n 的「三个门」＋ 带数字的小电路图~~ ——&nbsp;**2026-09-17**，新图 `fig4-circuit`。
         f = (a+b)×c，a=2 b=−3 c=4：同一条线上两个数，黑的前向、红的反向。
         三个门：加法是**分发器**、乘法是**交换器**、max 是**路由器**。
         ⭐ 而 Ⓒ 才是它在这一讲里的用处：乘法门反向要用前向的值 ——
         **这就是整讲那张激活账单的最小例子**，激活不是框架顺手缓存的，
         是反向的数学要求它在场。落点带顺带把「重算」也在这张小电路上讲通了。
   - [x] ~~混合精度不省显存~~ ——&nbsp;**2026-09-17**，写进 §3.1：
         纯 fp32 是 4+4+4+4=16，混合精度是 2+2+4+4+4=16，**常驻一个字节都没省**。
         权重和梯度各省的 2 字节，正好被多出来的 4 字节主权重吃掉。
         它换的是**速度**和**激活**（激活确实省一半）。
         判据：说「省显存」之前，先问省的是哪一块 —— 常驻和激活是两本账。
   - [x] ~~AllReduce ＝ AllGather ＋ ReduceScatter~~ ——&nbsp;已落进 `fig4-zero` 的落点带
   - [x] ~~根号 L 存档点的来历~~ ——&nbsp;已落进 `fig4-recompute` Ⓒ，并引了 ZeRO §3.2 原话
   - [x] ~~重算的静默正确性风险~~ ——&nbsp;**2026-09-17**，新 §2.5b。三条：
         ① 随机数 —— 框架挡住了（`preserve_rng_state` 默认 True，已核源码），
            而这正好解释了 checkpoint 里为什么要存随机数状态；
         ② 副作用 —— **没人替你挡**，被重算的那段最好是纯函数；
         ③ 逐比特不一致 —— 通常无害，但它让「逐比特可复现」变难，
            而那正是 §6.2 那个救火办法的前提。
         判据：一个「重做一遍」的优化，必须问一句「重做出来的一样吗」。
   - [x] ~~PyTorch 的 SAC `policy_fn` 与 `jax.remat`、`no_sync()`、bucketing~~ ——&nbsp;
         **2026-09-17 完成**。SAC / `jax.remat` 原来就在 §7.0 表里；这轮补了缺的两行，
         都当场 grep 过源码并记了行号：
         **`ddp.no_sync()`**（`torch/nn/parallel/distributed.py:1601`，文档原语：
         「进去之后梯度只在本地累加，退出后的第一次 forward-backward 才同步」）、
         **`bucket_cap_mb`**（默认 **25 MiB**，同文件）。
         ⭐ 两行放一起才有落点：**`no_sync()` 干的正是把「梯度的一生」第 ② 道整个关掉** ——
         所以「梯度累积顺带省通信」不是副作用，就是这个 API 的用途；
         而 `bucket_cap_mb` 那个桶，正是第 ② 道「✅ 能藏」的实现。
         ⚠️ 顺手自曝一处**本讲少讲的**：`CheckpointPolicy` 其实有 **6** 个枚举值不是 2 个 ——
         还有 `{MUST,PREFER}_CPU_OFFLOAD`。框架眼里「这块激活怎么办」是**三选一**
         （存 / 重算 / 卸到 CPU），本讲只讲了前两个。已写进 §7.0。
7. **两位专家评审没跑完**
   - [ ] E（训练框架工程师）、F（优化理论）两个 subagent 都 Stream idle timeout，无结果

---

## 还没想清楚的

- [ ] **要不要在这里就讲清楚"峰值显存出现在哪一刻"** ——
      这需要画时间线图，可能比想象中难讲
- [x] ~~「累加就要 fp32」这条判据~~ —— **2026-09-16 收紧**：真正的分界是**会不会「老的贡献永远不走」**。滑动平均（m / v）有遗忘，不是累加，bf16 扛得住；V3 就是这么配的。已写进 §3.2
- [ ] Muon 的主权重是否仍需 fp32 —— 待核实实现

---

> 本目录随 `Courses/` 采用 **CC BY-NC-SA 4.0**（[全文](LICENSE.txt)）。

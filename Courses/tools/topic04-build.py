# -*- coding: utf-8 -*-
r"""专题四 · 反向与优化器 —— **主线**（要拿上讲台的那一份）。

════════════════════════════════════════════════════════════════════
⭐⭐⭐ 这个专题在整门课里干什么：**讲清一个 loss 值怎么变成 __NPARAM_CN__次更新**
  ⛔⛔ 2026-09-19 T12 换主线。原来的脊梁是「把账补全」，而现场明确：
    「一切关于**能不能放得下**、能不能以**并行**的方式，那都是后面的专题要讲的。
      控制深度，我们是要讲 ① 反向传播的原理 ② 梯度下降的原理 ③ loss 怎么算。」
  ⭐ 判据：**一节留不留，问它服务于那三件事的哪一件。答不上来就折叠。**
════════════════════════════════════════════════════════════════════
专题一跟着一个 token 走完了前向，算出「装不进任何一块卡」。
**但那只是账单的一小半。**

真正训练一步，显存里同时压着四样东西：权重、梯度、优化器状态，
外加一大堆**不能算完就扔的中间激活**。这一讲把这张账单补全 ——
补完之后会发现一件多数人想不到的事：

  ⭐⭐⭐ **最大的那一块，既不是权重也不是激活 ——&#160;是优化器状态。**

而这，正是所有并行策略要解决的第一个问题（→ 专题五）。

════════════════════════════════════════════════════════════════════
⛔ 写这一份之前先读的三条（都是专题三用血换来的）
════════════════════════════════════════════════════════════════════
① **HTML 就是源。** 正文直接写在这个文件里，`.md` 只是大纲，
   **不做 md → HTML 的转换**。改内容改这里。
② **要人「看」的留图，要人「读」的留字。** 图上只放非画不可的东西；
   一句话能说清的对照，不值一整格面板。
③ **判据⑩：两处说同一件事，挨着图的那一处赢。** 正文不要复述图注／图内文字，
   `build-all.sh` 的查重会当场抓。

📌 脚手架（head / 图装配 / 锚点 / 吸顶目录 / 折叠开关）复用
  `topic03_page.py`，画法基元复用 `topic03_draw.py` ——&#160;
  ⛔ **别另起一套**，那是专题二、三、二x 已经共用的同一份。
"""
import io
import math
import os
import re

import topic03_page as P

HERE = os.path.dirname(os.path.abspath(__file__))
WEB = os.path.join(HERE, "..", "WebPages")
OUT = os.path.join(WEB, "topic-04.html")

head = P.make_head(
    "专题四 · 反向与优化器",
    "反向与优化器 · 一个 loss 值，怎么变成 __NPARAM_CN__个参数的更新",
    "训练一步里到底发生了什么：loss 是怎么算出来的、"
    "这一个数怎么变成 __NPARAM_CN__个导数、以及拿到导数之后每个参数该往哪儿挪多少。"
    "三件事，一条链。",
    "topic-04.html")

# ⭐ 节号与标题写死在这儿，跟正文里的 <section id> 对齐。⛔ 改一处要改两处。
# ⭐⭐ 标题说的是这一节**在故事里干什么**，不是它讲哪个名词。
SECTIONS = [
    ("s零", "零", "一个数，和 __NPARAM_CN__个参数"),
    ("s一", "一", "一个数怎么变成 __NPARAM_CN__个导数 —— 反向传播的原理"),
    ("s二", "二", "代价：那些中间结果扔不掉 —— 以及第一个真正的取舍"),
    ("s三", "三", "有了导数，那一步该迈多大 —— 梯度下降与优化器"),
    ("s四", "四", "这张账交给专题五（本讲不展开）"),
    ("s五", "五", "把这三件事，放到一台你摸得到的机器上"),
    ("s六", "六", "训不崩 —— loss 飞了怎么办，以及怎么让它别飞"),
    ("s七", "七", "这一讲的数是怎么核的"),
]


def todo(what, figs, asks):
    """一章的「🚧 待写」块。⛔ 这一章写完就把对它的调用整段删掉。"""
    return ('<div class="note warn"><p>🚧 <b>本章待写 ——&nbsp;%s</b></p>'
            '<p><b>这一章在故事里干什么：</b>%s</p>'
            '<p><b>打算用的图：</b><code>%s</code></p></div>'
            % (what, asks, "</code> · <code>".join(figs)))


BODY = '''<section id="s零"><div class="wrap"><div class="stn"><span class="badge">第 零 节</span><h2>一个数，和 __NPARAM_CN__个参数</h2></div>

<p class="lead">专题一跟着一个 token 走完了前向 ——&nbsp;<b>模型吐出了它的猜测。</b>
  <em>这一讲从那一刻接着往下走：<u>猜错了，然后呢？</u></em></p>

<p>「猜得多离谱」会被压成<b>一个数</b>。<em>不是一张表，不是一个向量 ——&nbsp;
  <b>一个数。</b></em></p>

<p class="landing">⭐⭐⭐ <b>而训练这件事，从头到尾就是在做一件事：</b>
  <em>拿这<u>一个</u>数，去决定<u>__NPARAM_CN__个</u>参数各自该往哪边挪、挪多少。</em></p>

<div class="note warn"><p>✋ <b>这里台下一定会有人举手：<u>一个数怎么可能决定
  __NPARAM_CN__个东西？信息量根本不够。</u></b></p>
<p class="landing">⭐⭐⭐ <b>问得对，而这一问的答案就是整讲的主线：
  那 __NPARAM_CN__个方向不是从这个数里<u>读</u>出来的，是从它<u>算</u>出来的。</b></p>
<p><em>信息不在这个数里 ——&nbsp;它在<b>网络的结构</b>里，
  在<b>那一次前向留下的每一个中间值</b>里。
  loss 只是个起点，链式法则负责把它展开成 __NPARAM_CN__份。</em>
  <span class="sub">⭐ 记住这句话，第二节那笔「激活为什么扔不掉」的账
  就不用解释了 ——&nbsp;<u>要展开，就得留着展开时要用的东西</u>。</span></p></div>

<div class="note ok"><p>⭐ <b>所以这一讲就三件事，顺着这条链走一遍：</b></p>
<ul>
  <li><b>① 这个数怎么来的</b> ——&nbsp;<em>loss 到底怎么算（<a href="#s1-0">1.0</a>）。</em></li>
  <li><b>② 一个数怎么变成 __NPARAM_CN__个导数</b> ——&nbsp;<em>反向传播的原理（<a href="#s一">第一节</a>）。
    ⭐ 这一段是全讲最该讲透的：<b>笨办法要算 __NAIVE_YEARS_CN__年，而实际只多花两倍前向的时间</b>。</em></li>
  <li><b>③ 有了导数，那一步该迈多大</b> ——&nbsp;<em>梯度下降与优化器（<a href="#s三">第三节</a>）。</em></li>
</ul>
<p class="landing">⭐⭐ <b>三件事，一条链。</b>
  <em>——&nbsp;链的一头是一个数，另一头是 __NPARAM_CN__次「挪一点点」。</em></p></div>

<div class="note danger"><p>⛔⛔ <b>这一讲<u>不讲</u>什么 ——&nbsp;先说清楚，免得你等一个不会来的答案。</b></p>
<ul>
  <li>⛔ <b>装不装得下。</b><em>显存够不够、哪一块最大、怎么省 ——&nbsp;
    这一讲会顺手报几个数字让你有量级感，<b>但不展开</b>。</em></li>
  <li>⛔ <b>怎么切到多少张卡上。</b><em>张量并行、流水线、ZeRO 的三级分法 ——&nbsp;
    <b>整个是<a href="index.html">专题五</a>的事。</b></em></li>
</ul>
<p class="landing">⭐ <b>具体说：<u>第二、四、五节里那些细账</u>，
  以及全讲所有点得开的折叠块 ——&nbsp;都属于上面这两类。</b>
  <em>它们是专题五的预习，<u>跳过去不影响这一讲的三件事</u>。</em></p>
<details class="foldfig"><summary><b>为什么这条线要划在这儿</b>
  <span class="why">——&nbsp;一句话的方法论，跟这一讲的三件事无关，可以直接跳过</span></summary>
<p><span class="sub">⛔ 反向传播和梯度下降是<u>原理</u>，它们几十年不变；
  而「放不放得下」是<u>当下这一代硬件的处境</u>，换一代机器就变。
  ⭐ 把两者混在一起讲，读者会以为原理也是会过期的。</span></p>
</details></div>

<div class="note"><p>📐 <b>先把场景钉住，不然后面每个数你都不知道它在说哪一档。</b></p>
<p><em>这一讲所有的账，默认都按 <b>DeepSeek-V3 预训练的真实配置</b>算：
  <b>序列长度 4,096</b>，global batch 从 3,072 条爬到 15,360 条。</em></p>
<p class="sub">⚠️ V3 对外说的是 128K 上下文，但那是预训练<u>之后</u>
  两段各一千步的扩展；真正跑掉 14.8T token 的那一段，<b>是在 4K 上</b>。
  ⭐ 本讲凡是出现 128K 的地方，都会明说那是<b>扩训场景</b>，
  并给出跟 4K 的换算 ——&nbsp;<b>不标场景的数字没有意义</b>，这也是全讲第③条判据。</p></div>

</div></section>


<section id="s一"><div class="wrap"><div class="stn"><span class="badge">第 一 节</span><h2>一个数怎么变成 __NPARAM_CN__个导数 ——&nbsp;反向传播的原理</h2></div>

<p class="lead">反向传播不是「再跑一遍」。<em>它要付两样东西：
  <b>大约两倍于前向的算力</b>，以及 ——&nbsp;更要命的 ——&nbsp;
  <b>一路累加、不能提前扔掉的中间激活</b>。</em></p>

<div class="note warn"><p>📌 <b>先把「激活」这个词钉死 ——&nbsp;它在这一讲出现六十多次，
  而它有三个都很常见的意思。</b></p>
<ul>
  <li><b>本讲一律指第一个</b>：<em>前向算出来、<u>还没被扔掉</u>的
      <b>中间结果</b>（每层的输入、norm 的输出、注意力的分数……）。
      整个第二节都在算它占多少显存。</em></li>
  <li>⛔ <b>不是「激活参数」。</b><em>上一讲说 V3「每 token 激活 37B」——&nbsp;
      那是 MoE 里<u>这一步被选中参与计算的那部分权重</u>，
      跟这里说的中间结果完全是两样东西。
      本讲只在讲 MoE 派发时用到那个意思，用到时会写成
      「<b>被激活的专家</b>」。</em></li>
  <li>⛔ <b>也不是「激活函数」。</b><em>SwiGLU、GELU 那一类算子，
      本讲一律写全「<b>激活函数</b>」，不简称。</em></li>
</ul>
<p class="landing">⭐ <b>一句话：本讲看到「激活」两个字单独出现，指的就是
  <u>前向留下来、反向还要用的那堆中间结果</u>。</b></p></div>

<p><span class="sub">⭐ 不过在算这笔账之前，得先把<b>起点</b>说清楚 ——&nbsp;
  <em>整条链是从一个叫 loss 的数开始往回走的，
  <b>而那个数本身，值得先花两分钟。</b></em></span></p>

<!-- ⭐⭐⭐ 2026-09-16 现场原话：「loss 是怎么变成梯度的？这个反向计算是怎么回事？
     这些好多人都难以理解，这么多年了都没有人真正讲明白。就是关于那个偏导数，
     **有了偏导数怎么算反向梯度它就容易了呢？没有偏导数那简直就是噩梦。**」
     ⭐ 这一问把这一节的写法整个换掉了。原来的写法是「先给公式、再算三倍」——
       那等于跳过了唯一值得讲的东西。
     ⛔ 判据：**先问「难在哪」，再讲「怎么解」。**
       大部分教程一上来写链式法则，是在回答一个还没被提出的问题。 -->

<h3>1.0　⭐⭐⭐ 先说清楚：<u>loss 到底是一个什么数</u></h3>

<!-- ⭐⭐⭐ 2026-09-19 T06 新开。现场原话：「**这个 loss 到底是怎么算？**」
     ⛔ 动手前先 grep 了一遍：全讲「负对数」「对数似然」「困惑度」**各 0 次**，
       「交叉熵」3 次而且只是提了个名字。也就是说这一讲从
       「loss 是一个数」直接跳到了「它的梯度」——&#160;中间那一步从来没讲过。
     ⛔ 后果不止是缺一段，它让三个地方悬空：
       ① §3.3 的量纲推导（「梯度的单位是 loss ÷ 参数」）站在一个没定义的量上；
       ② §1.5 的全部论证靠「loss 只有一个数」，而这句话原来是顺口说的；
       ③ §6 整节在讲「loss 飞了」，读者手上却**没有刻度尺**。
     ⭐ 判据：**一个贯穿全讲的量，必须在第一次用它之前就定义好、并且给出可验的锚点。** -->

<p class="lead">⭐ 这一讲后面所有的账，都是为了让一个数变小。
  <em>——&nbsp;所以先花两分钟，把<b>这个数本身</b>说清楚。</em></p>

<h4>第一步 · loss 就是「<u>模型有多意外</u>」</h4>

<p>模型读完前面的字，要猜下一个字。<b>它不是只报一个答案，而是给词表里每一个词打一个概率。</b>
  <em>——&nbsp;然后我们翻开正确答案，只看<b>它给对的那个词打了多少分</b>。</em></p>

<p><span class="sub">📌 <b>那一列分数是怎么变成概率的：softmax。</b>
  <em>每个分数取指数（保证为正），再除以全部之和（保证加起来等于 1）。
  ——&nbsp;<b>本讲后面 softmax 出现十几次，说的都是这一个动作。</b>
  <u>专题三那张注意力图里的「权重加起来等于 1」，用的也是它。</u></em></span></p>

<p class="landing">⭐⭐ <b>loss ＝ −ln p</b>　
  <em>（p ＝ 模型给正确那个词的概率）</em></p>

<div class="note ok"><p>⭐ <b>为什么是这个式子 ——&nbsp;它其实很讲道理。</b></p>
<ul>
  <li><em>模型很确信而且猜对了（p → 1）：<b>−ln 1 ＝ 0</b>，不罚。</em></li>
  <li><em>模型给对的那个词几乎没打分（p → 0）：<b>−ln p → ∞</b>，罚到天上去。</em></li>
  <li><em>⭐ 关键是<b>取了对数</b>：这让「从 1% 提到 2%」和「从 50% 提到 100%」
    得到<u>一样的奖励</u> ——&nbsp;都是概率翻倍，都降 <code>ln 2</code>。</em></li>
  <li><em>⭐⭐ 而对数真正带来的是另一样东西：
    <code>d(−ln p)/dp ＝ −1/p</code> ——&nbsp;<b>概率越低的位置，梯度越大</b>。
    所以模型会<u>先去救它错得最离谱的那些</u>。
    <span class="sub">⛔ 这里原来写的是「不然模型会只顾着把已经很有把握的地方
    再推高一点点」——&nbsp;<b>那句推不出来</b>：换成线性的 <code>1−p</code>，
    导数恒为 −1，模型对「已经有把握的」和「完全没把握的」是<u>无差别</u>的，
    并不会偏向前者。方向正好说反了。</span></em></li>
</ul>
<p><span class="sub">📌 这个东西的正式名字叫<b>交叉熵</b>，也叫<b>负对数似然</b>。
  ——&nbsp;三个名字，同一个式子。</span></p></div>

<h4>第二步 · ⭐⭐⭐ 一把可以自己验的尺</h4>

<p><b>−ln p 听起来抽象，但它有一个非常具体的换算</b>：<em>把它取回指数，
  <code>e<sup>loss</sup></code> 就是<b>困惑度</b> ——&nbsp;
  直观说就是「<u>模型这一步大概在几个词之间犹豫</u>」。</em></p>

__TBL_SCALE__

<div class="note danger"><p>⭐⭐⭐ <b>看最上面那一行 ——&nbsp;它不是随手填的。</b></p>
<p><em>一个<b>刚初始化、什么都没学</b>的模型，对每个词的概率都一样，
  也就是 <code>p ＝ 1 / 129,280</code>。代进去：</em></p>
<p class="landing"><b>loss ＝ −ln(1 / 129,280) ＝ ln 129,280 ＝ <u>11.77</u></b></p>
<p><em>⭐ 而它的困惑度正好就是 <b>129,280</b> ——&nbsp;<u>整个词表</u>。
  <b>「在 129,280 个词之间犹豫」，这不就是瞎猜的定义吗。</b></em></p>
<p><span class="sub">⭐⭐ 所以这把尺的上端<b>不是估的，是算出来的</b>
  ——&nbsp;你换个词表大小，上端就跟着换（词表 129,280 这个数在专题一核过）。
  ⛔ 而下端我<b>没有 V3 的官方数字</b>：表里「2.0 上下」是公开模型的大致量级，
  <b>当参照看，别当某个具体模型的实测值。</b></span></p></div>

<p class="landing">⭐ <b>这把尺后面要用三次</b>：
  <em><a href="#s3-5">3.5</a> 判断「loss 下得够不够快」、
  <a href="#s六">6.1</a> 判断「飞了多少算飞」、
  <a href="#s六">6.3</a> 读那张表里的数。
  ——&nbsp;<b>没有尺，那三处都只能靠感觉。</b></em></p>

<h4>第三步 · 一个 token 一个 loss，<u>一个 batch 取平均</u></h4>

<p>上面算的是<b>一个位置</b>的 loss。<em>而一条 4K 的序列有 4,096 个位置，
  每个位置都在猜它的下一个字 ——&nbsp;<b>所以一条序列就有 4,096 个 loss。</b></em></p>

<p class="landing">⭐ <b>把它们全部<u>取平均</u>，就得到这一批的 loss。</b></p>

<div class="note"><p>⭐⭐ <b>「取平均」这三个字，是后面很多事情的根。</b></p>
<p><em>你马上会在 <a href="#s1-2">1.2</a> 的图里看到这个比方：
  <b>一屋子人各提各的要求，最后取平均</b>。那屋子里坐的就是这些 token。</em></p>
<p class="landing">⛔ <b>所以 batch 首先不是为了喂饱卡 ——&nbsp;<u>是为了别只听一个人的</u>。</b>
  <em>——&nbsp;人越多，平均越不容易被一个古怪的样本带偏。
  这条线一直通到 <a href="#s六">6</a> 那节「训不崩」。</em></p></div>

<h4>第四步 · ⭐⭐ 它<u>只有一个数</u>，而且它有单位</h4>

<p>几千亿个参数进去，<b>出来的是一个数</b>。<em>不是一个向量，不是一张表 ——&nbsp;<b>一个数。</b></em></p>

<p class="landing">⭐⭐⭐ <b>记住这件事。</b>
  <em>——&nbsp;<a href="#s1-5">1.5</a> 会告诉你，<u>整个反向传播之所以划算，全靠这个「一」</u>。</em></p>

<div class="note"><p>📐 <b>顺带说一句单位，后面 <a href="#s3-3b">3.3b</a> 要用。</b></p>
<p><em>取的是自然对数，所以 loss 的单位是 <b>nats / token</b>
  ——&nbsp;「每个 token 平均有多少奈特的意外」。</em></p>
<p><span class="sub">⭐ 别觉得这是学究气：<a href="#s3-3b">3.3b</a> 会靠它推出
  <b>学习率的单位</b>，那是全讲最漂亮的一段推导之一 ——&nbsp;
  而它需要 loss <u>确实有单位</u>这件事。</span></p></div>

<div class="note ok"><p>⭐⭐⭐ <b>那么这一讲要干的事，现在可以用一句话说完了。</b></p>
<p class="landing"><em>把这个数，<b>从 11.77 挪到 2 附近</b>。</em></p>
<p><em>⛔ 而挪动它的唯一办法，是知道<b>每一个参数该往哪边动一点点</b>
  ——&nbsp;<u>也就是这一个数对 __NPARAM_CN__个参数的偏导数</u>。</em></p>
<p class="landing">⭐ <b>问题来了： __NPARAM_CN__个导数，怎么算得过来？</b></p></div>

<h3>1.1　先把难点问对 ——&nbsp;难的不是求导，是<u>求 __NPARAM_CN__个导数</u></h3>

<p class="lead">⭐ 求导是高中的事。
  <em>这里难的是：要求<b>快 __NPARAM_CN__个</b>导数，而且<b>每一步都要重求一遍</b>。</em></p>

<p>⛔ <em>不过「求导是高中的事」这句话会劝退一批人 ——&nbsp;
  <b>所以先花一格，把这一讲要用到的三个词说完。</b>
  没有极限、没有公式，<b>三句话，一张图。</b></em></p>

__FIG_SLIDER__

<p>先看看笨办法长什么样 ——&nbsp;<em>这一步不能跳过，
  不知道笨办法有多笨，就不会觉得反向传播有多神。</em></p>
<div class="note danger"><p>⛔ <b>笨办法（有限差分）：</b>
  我想知道某个参数对 loss 有多大影响，就把它动一丁点，
  整个网络重跑一遍前向，看 loss 变了多少。</p>
<p><em>那 __NPARAM_CN__个参数，就是<b>__NPARAM_CN__次前向</b>。</em></p>
<p class="landing"><code>671e9 秒 ÷ 3.156e7 秒/年 ＝ <b>__NAIVE_YEARS__ 年</b></code>
  <em>（一次前向按一秒算 ——&nbsp;已经极度乐观了）</em></p>
<p><em>⛔ <b>而这还只是<u>一步</u></b>。训练要走几十万步。</em></p></div>

<p class="landing">⭐⭐⭐ 所以反向传播真正解决的问题，不是「怎么求导」，
  是<u>怎么把 __NPARAM_CN__次前向压成一次</u>。</p>

<h3>1.2　种子：第一个梯度长什么样</h3>

<p>起点就是 <a href="#s1-0">1.0</a> 那个数 ——&nbsp;<b>loss</b>。
  <em>（它只有一个，这一点后面是全部关键。）</em></p>

<p>那第一个梯度是什么？<em>拿最常见的交叉熵来说：网络最后吐出一个概率分布
  ——&nbsp;下一个字是「的」的概率 0.3、是「了」的概率 0.2……
  而正确答案是一个<b>只有一格是 1、其余全是 0</b> 的东西。</em></p>

<div class="note ok"><p>⭐⭐ 第一个梯度 ＝ 你猜的，减去正确答案。</p>
<p><em>猜高了的地方是正数，该高没高的地方是负数。
  就这么简单。这个差，就是整条反向链的<b>种子</b>。</em></p>
<p class="landing">⭐⭐⭐ <b>而它不是凑巧长这样 ——&nbsp;它是 <a href="#s1-0">1.0</a>
  那个 <code>−ln p</code> 求一次导的结果。</b></p>
<p><em>对 logits 求导（不是对概率求导），
  <code>−ln p</code> 那一串恰好约化成 <b>p − y</b>
  ——&nbsp;<u>模型给的概率，减去那个只有一格是 1 的正确答案</u>。</em></p>
<div class="note ok"><p>⭐⭐ <b>跟 <a href="#s1-0">1.0</a> 那一段接上：
  同一个 <code>−ln p</code>，<u>换个求导对象，给出两件不同的东西</u>。</b></p>
<ul>
  <li><b>对<u>概率</u> p 求导</b> ——&nbsp;<em><code>−1/p</code>，
      <b>概率越低梯度越大</b>。这解释的是<u>动机</u>：模型为什么会先去救错得最离谱的。</em></li>
  <li><b>对 <u>logits</u> 求导</b> ——&nbsp;<em>整串约化成 <code>p − y</code>。
      这给出的是<u>实现</u>：反向那条链的种子，一个减法就完事。</em></li>
</ul>
<p class="landing">⭐ <b>一个解释「为什么想动」，一个给出「怎么动」——&nbsp;
  同一个 loss 的两张脸。</b></p>
<p><span class="sub">⛔ 也正因为约化得这么干净，框架里它俩总是
  <u>融合成一个算子</u>：分开算要先出概率再求导，合起来一步就到。
  <em>不是所有 loss 都这么好看，但这一对是今天的默认组合。</em></span></p></div>
<p><span class="sub">⚠️ 一个说准了才不会误导的细节：
  <em>那个「减」<b>不是对概率求导，是对 <u>logits</u> 求导</b>
  ——&nbsp;也就是 softmax 之前那一层的输出。<br>
  ⭐ 这个区别有实际后果：正因为约掉的是 softmax 那一步，
  <b>这个梯度才不会在 softmax 饱和的地方消失</b>
  ——&nbsp;softmax 和交叉熵总是成对出现，一半的原因就在这儿。</em></span></p></div>

__FIG_VOTE__

<h3>1.3　往回传的<u>不是一个数</u> ——&nbsp;这里是最多人卡住的地方</h3>

<p>⛔ <em>很多人脑子里的画面是「一个梯度值一层一层传下去」。不是这样。</em></p>

<p>往回传的是一整个张量，形状跟这一层的输出<u>一模一样</u>。
  <em>它的含义是：「loss 对我这一层输出的每一个位置，各有多敏感」
  ——&nbsp;这一层输出有多少个数，它就有多少个数。</em></p>

<div class="note"><p>⭐⭐ 每一层拿到这个上游传来的敏感度之后，只做两件事：</p>
<ul>
  <li><b>① 算出自己那块权重的梯度</b> ——&nbsp;
    <em>用这个敏感度，乘上<b>前向时存下来的输入</b>。</em></li>
  <li><b>② 算出该继续往下游传的敏感度</b> ——&nbsp;
    <em>用这个敏感度，乘上<b>自己的权重</b>。</em></li>
</ul>
<p class="landing">⭐ 这两件事各是一次矩阵乘。前向一次、反向两次
  ——&nbsp;<u>三倍算力就是从这儿来的</u>。</p>
<p><span class="sub">⛔ <b>但「数次数」还不够 ——&nbsp;三次矩阵乘如果规模不一样，
  1 ＋ 2 根本不等于 3。</b> <em>真正让它成立的是：这三次乘的
  <u>形状是同一组</u>。设这一层是 <code>[B,in] × [in,out]</code>：
  前向 <code>X·W</code>、① <code>dYᵀ·X</code>、② <code>dY·Wᵀ</code>
  ——&nbsp;<b>三个都是 B × in × out 这三个数相乘</b>，
  所以三块积的<u>体积一样大</u>，加起来才正好是 3 倍。
  （下面那张图把这三块按真实比例画了出来。）</em></span></p>
<p><span class="sub">⭐ <b>知道理由，才换得了情境</b>：
  <em>最后一层不用往下游传 ——&nbsp;省掉 ②，就是 <b>2 倍</b>；
  冻住的层不更新权重 ——&nbsp;省掉 ①，也是 <b>2 倍</b>（<a href="#s三">3.8</a> 的 LoRA 走的是这条）。
  ⛔ <u>只记住「3×」这个数的人，这两问都答不了。</u></em></span></p></div>

__FIG_3X__

__FIG_CIRCUIT__

<h3>1.4　⭐ 为什么偏导数一出现，这件事就变容易了</h3>

<p class="lead">⭐⭐⭐ 因为偏导数是<u>局部</u>的。</p>

<div class="note ok"><p>❓ <b>「偏」这个字，就是全部的关窍。</b></p>
<p><em>普通的导数问的是：这个东西变了，结果变多少。<br>
  偏导数问的是：<b>其它全部按住不动</b>，只动这一个，结果变多少。</em></p>
<p class="landing">⭐ 而正因为其它都按住了，<b>这个问题就缩到了一个算子身上</b>
  ——&nbsp;<em>它不需要知道外面的世界长什么样。这就是「局部」的意思。</em></p></div>

<p><em>每一个算子只需要知道<b>「我自己是怎么把输入变成输出的」</b>，
  就能写出自己的反向规则。
  它完全不需要知道前面是什么、后面是什么、整个网络长什么样。</em></p>

<div class="note ok"><p>⭐⭐ 这就是为什么自动微分能做成一个<u>通用库</u>。</p>
<p><em>矩阵乘写一个 backward，softmax 写一个 backward，加法写一个 backward
  ——&nbsp;然后框架只干一件事：按顺序倒着把它们串一遍。</em></p>
<p class="landing">⭐ 没有任何人需要手推整个网络的导数。</p></div>

<p>反过来想才知道这有多可怕：
  <em>如果你非要写出「loss 对第一层某个权重」的<b>解析式</b>，
  那个式子要穿过 <b>61 层</b>展开 ——&nbsp;项数是天文数字，写不出来。</em></p>
<p class="landing">⭐⭐ 链式法则真正的意思是：<u>你永远不用把它展开。</u>
  <em>你只要把 61 个<b>局部的小导数</b>，按顺序乘起来。</em></p>

<div class="note ok"><p>⭐⭐⭐ <b>⚠️ 这里要把 <a href="#s一">1.3</a> 那两笔账跟「乘起来」这三个字焊在一起 ——&nbsp;它们说的是同一件事。</b></p>
<p><em>1.3 说每一层拿到上游的敏感度之后做两笔矩阵乘，其中第 ② 笔是
  「<b>用这个敏感度，乘上自己的权重</b>」，结果传给下游。</em></p>
<p class="landing">⭐⭐ <b>那一乘，就是链式法则的一环；
  而「自己的权重」，就是这个算子的<u>局部导数</u>。</b></p>
<p><em>——&nbsp;所以「61 个局部导数按顺序乘起来」不是一个比喻，
  <b>它就是反向那一趟里，第 ② 笔被连做了 61 次</b>。
  ⛔ 第 ① 笔（算权重梯度）不在这条链上：它是<u>从链上取值</u>，
  每一层各取各的，取完就交给优化器，不往下传。</em></p>
<p><span class="sub">⭐ 这个区分后面一直有用：「梯度」这个词在本讲同时指两样东西 ——&nbsp;<b>第 ① 笔算出来的<u>权重梯度</u></b>（要 all-reduce、要进优化器、每参数 2 字节）和<b>第 ② 笔往回传的<u>敏感度</u></b>（用完就扔，不进任何账）。§1.8 那道 all-reduce 传的是前者。</span></p></div>

<p class="lead">❓ <em>——&nbsp;<b>那「乘起来」之后会怎样？</b></em>
  <em>这一问有一个非常出名的答案，而它是<b>链式法则最直接的后果</b>。</em></p>

__FIG_VANISH__

<h4>⭐⭐⭐ 那深层网络凭什么还能训得动 ——&nbsp;残差的全部秘密就一个加号</h4>

<p><b>先把病说准。</b><em>一个参数的梯度 ＝ 61 个局部导数<u>连乘</u>。
  连乘对「每一项平均是多少」极其敏感：</em></p>
<ul>
  <li><em>每层平均 <b>0.9</b> ——&nbsp;61 层乘下来是 <b>0.0016</b>，梯度到不了前面（消失）；</em></li>
  <li><em>每层平均 <b>1.1</b> ——&nbsp;61 层乘下来是 <b>335</b>，一步就炸（爆炸）。</em></li>
</ul>
<p class="landing">⛔ <b>注意这两个数离 1 都只有<u>一成</u> ——&nbsp;
  比上面那张图用的 ±20% 还近一半，塌得照样彻底。</b>
  <em>——&nbsp;<u>连乘没有中间地带</u>：不是指数塌，就是指数涨。
  <b>而且越靠近 1，越说明问题</b>：连「每层只偏 10%」都扛不住 61 层，
  那就不存在「小心一点就没事」这种可能。
  这就是 2015 年之前「层一深就训不动」的全部原因。</em></p>

<div class="note ok"><p>⭐⭐⭐ <b>残差做的事，数学上只有一步：把每一层从
  <code>y ＝ F(x)</code> 改成 <code>y ＝ x ＋ F(x)</code>。</b></p>
<p><em>于是这一层的局部导数从 <code>∂F/∂x</code> 变成了
  <b><code>1 ＋ ∂F/∂x</code></b>。</em></p>
<p class="landing">⭐⭐⭐ <b>这就是全部，剩下的都是它的推论。</b></p>
<ul>
  <li><b>连乘里多了一条恒等通路。</b><em>把 61 层的
      <code>∏ (1 ＋ ∂F<sub>i</sub>/∂x)</code> 展开，里面有一项是
      <b>所有括号都取 1</b> ——&nbsp;而 <code>1<sup>61</sup> ＝ 1</code>。
      <u>不管中间那些权重是大是小，梯度总有一条不衰减的路直达任何一层。</u></em></li>
  <li><b>它把「乘」换成了「加」。</b><em>没有残差时，第 3 层收不收得到梯度，
      取决于第 4 到 61 层那一整串乘积；有残差之后，各层的贡献是<b>加</b>起来的
      ——&nbsp;某一层学坏了只是少加一项，<u>不会把整条路乘没</u>。</em></li>
  <li><b>初始化时它天生就是 1。</b><em>F 那一支初始化很小（不少实现直接把
      最后一个投影置零），所以刚开始每层的局部导数就是 <code>1 ＋ 很小</code>
      ——&nbsp;<b>网络一开始等价于一个浅网络</b>，训练过程再一层层把 F 学起来。</em></li>
</ul>
<p class="landing">⭐⭐ <b>所以「残差让模型能做深」不神秘：<u>它把每一层的局部导数
  从「某个数」变成了「1 ＋ 某个数」</u>。</b>
  <em>——&nbsp;而 <a href="#s六">6.0</a> 那张表里，爆炸和消失的根因正是
  「局部导数偏离 1」。残差是从<b>结构</b>上把它钉在 1 附近；
  <a href="#s六">6.5</a> 那些手段（Pre-LN、初始化缩放）是在同一件事上再拧一道。</em></p>
<p><span class="sub">📌 出处：He 等，<b>arXiv 1512.03385</b>（ResNet）。
  恒等通路那条更干净的推导在同一组人的
  <a href="https://arxiv.org/abs/1603.05027" target="_blank" rel="noopener">arXiv 1603.05027</a>
  《Identity Mappings in Deep Residual Networks》——&nbsp;
  它把「梯度可以不经过任何权重矩阵直达浅层」写成了一个式子。
  ⛔ 本讲不展开那个式子：上面那句「1 的 61 次方还是 1」已经是它的全部内容。</span></p></div>

<!-- ⭐⭐⭐ 动画候选排名表第 3 名。第一版我自己否了：只画对数轴上那把扇子，
     而指数取完对数就是直线 —— 跟上面那张静态图几乎一模一样，不值一段视频。
     ⭐ 判据：一段动画要活下来，得给出静态图给不了的东西。
     ⭐⭐ 这一版的增量是**对数轴自己的代价**：它让你看得见十个数量级，
     可也正好让你感觉不到十个数量级有多大。两条轨对齐播才有这个反差。 -->
<figure class="fbox fwide" id="anim-vanish">
<video src="media/topic04-vanish.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="画面分上下两条轨，共用同一根横轴：最右边是输出层，往左是越来越靠近输入的层；一根竖直的扫描线从右往左走，同时穿过两条轨，表示梯度正在往回传到第几层。上轨是对数刻度，三条线从右端同一点出发向左张开成一把扇子 —— 红色是每层乘零点八，灰色是每层乘一点零，蓝色是每层乘一点二。下轨是线性刻度，有一条地面线和一条表示原始幅度等于一的参考线，上方还有一条画框上沿。扫描线刚走出几层，下轨的蓝色曲线就急速上冲、越过上沿后直接断掉，表示它已经冲出了画面；红色曲线则一路下坠贴在地面上，几乎看不见。而这个时候，上轨那把扇子才刚刚张开三成。画面上另有四处文字：上轨右上角标着「对数刻度」，下轨左上角标着「线性刻度」；左侧贴着蓝色曲线标着数学式「一点二的 k 次方」，贴着红色曲线标着「零点八的 k 次方」，其中 k 是往回传了几层。"></video>
<figcaption>⭐⭐⭐ <b>对数轴让你看得见十个数量级，也让你感觉不到十个数量级有多大。</b>
  <em>两条轨共用一根横轴、一根扫描线 ——&nbsp;所以它们永远在说<b>同一层</b>。<br>
  上轨是<b>对数刻度</b>：三条线才刚刚张开，看着「还差不多」。<br>
  ⭐ 下轨是<b>线性刻度</b>，也就是这些数<b>真实的样子</b>：
  <b>第 8 层</b>蓝线就顶出了画框，<b>第 18 层</b>红线已经贴在地上看不见了
  ——&nbsp;而同一层，上轨那把扇子<b>才张开三成</b>。<br>
  ⛔ 每层只差两成的兑换率，十几层就已经<b>没法放进同一张图里</b>了。
  <span class="sub">（14 秒无声循环，Manim 渲染，脚本在 <code>tools/manim/</code>。
  出框层与消失层都是脚本当场算的，并有断言钉住。）</span></em></figcaption>
</figure>

<h3>1.5　⭐⭐ 最深的一条：为什么是<u>从后往前</u></h3>

<p>数学上<b>两个方向都成立</b>，算出来的结果<b>一模一样</b>。
  <em>⭐ 区别只有一个 ——&nbsp;<b>你得把整条链走多少遍。</b></em></p>

__FIG_REVERSE__

<!-- ⭐⭐⭐ 2026-09-18 现场：「三蓝一棕画的动图超级好用，多借鉴一下。」
     这段是动画候选排名表的第 2 名。为什么是它：
     上面那张图能画出「一边 N 条线、一边 1 条线」，
     ⛔ 但读者得自己把 N 条线换算成「跑 N 趟有多累」——&nbsp;而**重复本身就是时间**。
     动起来就不用换算：下面那条早早跑完停着，上面那条还在一趟一趟吭哧。
     ⛔ 原来这里写着「同样一个字都不放」—— 2026-09-18 房规松绑后已作废。
     现在画面上有角色抬头、两个公式和三幕字幕；aria-label 逐句复述了它们。
     ⚠️ 静态图排在它上面，视频加载不出来也不影响读懂。 -->
<figure class="fbox fwide" id="anim-reverse">
<video src="media/topic04-reverse.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="上下两排同时开跑，共用同一套画面。上排左端标着「正向模式，每趟只到手，偏 L 偏 w i」，下排左端标着「反向模式，一趟就到手，nabla L」，也就是整个梯度。每排是一条灰色的链，链上串着八个圆圈代表八个参数，链的右端各有一个方块，方块里写着 L，也就是 loss。画面中间有两行灰色小字：「8 个参数」和「走过的趟数」，后者指向右边那两摞计数方块。画面顶端的字幕分三幕。第一幕写「同一条链，两个方向同时开跑」。这时下排一颗绿球从右端的 L 出发往左跑，沿途把八个圆圈一次全部点亮，右边只堆起一块绿色计数方块。第二幕字幕换成「下面这条从 loss 出发，一趟就跑完了」，此时下排已经全亮并停住不动。而上排那颗红球还在一趟一趟地跑：每一趟从一个参数出发跑到右端的 L，只点亮那一个圆圈，右边就多堆一块红色计数方块。第三幕字幕换成「上面这条还在跑 —— 八个参数，八趟」，红色计数方块一路堆到八块，而绿色那摞从头到尾只有一块。最后画面复位，回到第一幕重新开始。"></video>
<figcaption>⭐⭐⭐ <b>两条链同时开跑 ——&nbsp;看谁先跑完。</b>
  <em>上面是正向模式：小球<b>一次只能为一个参数跑一趟</b>，跑完才点亮那一个，
  于是八个参数就得跑八趟（右边堆了八块）。<br>
  下面是反向模式：小球从 loss 出发<b>往回走一趟</b>，沿途所有参数一起点亮
  ——&nbsp;<b>一趟就完事，右边永远只有一块。</b><br>
  ⭐ 注意它<b>早早就停在那儿了</b>，而上面那条还在吭哧
  ——&nbsp;<b>那份等待就是 __NPARAM_CN__倍代价的样子。</b>
  <span class="sub">（9 秒无声循环，Manim 渲染，脚本在 <code>tools/manim/</code>。）</span></em></figcaption>
</figure>

<div class="note ok"><p>⭐⭐⭐ 所以反向传播成立的<u>全部理由</u>就一句话：
  参数有几千亿个，而 loss 只有一个。</p>
<p><em>多输入、单输出。
  ⛔ 如果 loss 不是一个数、而是一百万维的输出，<b>这套就不划算了</b>
  ——&nbsp;那时候反倒该用从前往后。</em></p>
<p class="landing">⭐⭐⭐ 所以 1.1 那个问题的答案是：那 __NPARAM_CN__次前向，
  <b>压成的是<u>一次反向</u></b>。</p>

<p><span class="sub">⭐ <b>这条判据可以直接迁移</b>：
  看到任何一个「求一大堆偏导」的问题，先数一下<b>输入多、还是输出多</b>
  ——&nbsp;它决定了你该从哪头开始。</span></p></div>

<div class="note"><p>🕰️ <b>而这件事第一次被写下来，<u>据公开资料</u>是
  一篇没人读的硕士论文。</b></p>
<p><em>1970 年，赫尔辛基大学，一个叫 <b>Seppo Linnainmaa</b> 的学生在硕士论文里
  提出了<b>反向模式的自动微分</b> ——&nbsp;也就是上面这整套「从输出往回推」的做法。</em></p>
<p class="landing">⛔ <b>而那篇论文里<u>一次都没提神经网络</u>。</b>
  <em>——&nbsp;他解决的是「一个复合函数怎么高效求导」这个纯数学问题。</em></p>
<p><em>⭐ 神经网络那边要再等十六年，才有人把它接上去（1986 年那篇 Nature）。</em></p>
<p><span class="sub">⭐⭐ <b>记住这个形状，它一会儿还会再出现一次</b> ——&nbsp;
  <a href="#s1-4">1.4</a> 那个「梯度消失」，第一次被说清楚也是在
  <b>一篇硕士论文</b>里（1991 年，慕尼黑，德文，从没在英文期刊发表）。<br>
  ⛔ <b>两次改变一切的东西，第一次出现时都是没人读的硕士论文。</b></span></p>
<details class="figsrc"><summary>这两条史料各查到什么程度</summary>
<p><span class="sub">⚠️ <b>1970 那条我核的是公开的传记与综述资料，
  <u>没有读到论文原件</u></b>（芬兰语，未数字化）——&nbsp;当量级可靠的二手事实看，
  所以正文写的是「据公开资料」而不是断言。<br>
  ⭐ 1991 那条（Hochreiter 的德文硕士论文）同样没读原件，
  但它有 2001 年那篇四人合写的综述作为一手转述，可信度高一档。<br>
  ⛔ <b>两条都不要当成「我读过」来引用。</b></span></p></details></div>

<h3>1.6　闭环：反向<u>必须</u>用到前向的值 ——&nbsp;这就是激活扔不掉的根本原因</h3>

<p>回头看 1.3 那两件事里的第①件：
  算自己权重的梯度，要用「前向时存下来的输入」。</p>

<div class="note danger"><p>⛔⛔ 这就是激活必须留着的根本原因 ——&nbsp;
  不是谁设计得不好，是<u>反向的数学本身要求它在场</u>。</p>
<p><em>前向每算出一个中间结果，都得<b>一直挂在那儿</b>，
  等反向走回来的时候用。整条网络走完，它们全都还在。</em></p></div>

<p class="landing">⭐⭐ <b>这一节的两笔账到此都立住了：</b>
  <em>算力 <b>3×</b>（前向 1 ＋ 反向 2）；
  显存里多出<b>一整条从头挂到尾的激活</b>。</em>
  而下一节要做的，就是拿第一样去换第二样。</p>



__FIG_ACT_BILL__

<div class="note"><p>📌 <b>图 Ⓑ 那两张卡片的读法：</b>
  <em>它们是<b>同一条序列的两种配置</b>，不是两个模型。</em></p>
<p><span class="sub">⚠️ 两个数都是<b>自己按算子推的</b>（输入是 V3 的 config
  ＋ 官方参考实现的 MLA 前向），<b>没有第三方背书</b>。
  <em>⭐ 但就算差一倍，结论也不变：这个规模上，装不下。</em></span></p></div>


<div class="note"><p>📌 <b>那「一堆中间结果」到底有多少？——&nbsp;一句话给你个量级。</b></p>
<p><em>按 V3 的真实配置（4K 序列、一条）逐项数下来：<b>一层约 2.2 GiB，
  61 层合起来约 133 GiB</b>。</em></p>
<p><em>对照一下：V3 的权重按 bf16 是 <b>__W_BF16__ GiB</b> ——&nbsp;所以一条序列的激活
  只有权重的<b>约九分之一</b>。</em></p>
<p class="landing">⭐ <b>但真正要记住的是这两样东西涨得不一样：权重是固定的，激活每多跑一条序列就多一份。</b>
  <em>batch 到约 <b>__ACT_XOVER__ 条</b>，激活就追平权重；而训练的 batch 从来不止十条。
  ——&nbsp;这才是下一节那笔交易存在的理由。</em></p>
<div class="note ok"><p>❓ <b>那这 2.2 GiB 里，最大的是哪一项？</b>
  <em>——&nbsp;这个问题必须在这儿答，因为<u>下一节整节都在挑「该扔哪一个」</u>。</em></p>
<p class="landing">⛔⛔ <b>不是 attention ——&nbsp;是 MoE 的「九份复制」。</b></p>
<ul>
  <li><b>派发出去的激活 __MOE_D_PCT__ ＋ 专家输出 __MOE_O_PCT__ ＋ SwiGLU 乘积 __MOE_S_PCT__</b>
      ——&nbsp;<em>三项是<u>同一件事的后果</u>：每个 token 要发给 9 个专家
      （8 个路由 ＋ 1 个共享），于是这几个张量各存九份。
      <b>合起来 __MOE_ALL_PCT__，一层的一半。</b></em></li>
  <li><em>而 <b>attention 输出</b>只占 <b>__ATT_PCT__</b>
      ——&nbsp;<u>比多数人猜的小一个档</u>。</em></li>
</ul>
<p><span class="sub">⭐ <b>这一条直接决定了下一节的名单</b>：
  要省激活，先看的不是 attention，是那几份复制。
  <em>⛔ 它也解释了一件反直觉的事 ——&nbsp;MoE 在<u>参数</u>账上很省
  （每 token 只选中一小部分权重），在<u>激活</u>账上却要付九份的复制费。</em></span></p></div>

<p><span class="sub">⛔ <b>逐项怎么数的、换到 128K 是多少</b>
  ——&nbsp;都在下面这个折叠块里。<em>属于「装不装得下」，本讲不要求。</em></span></p></div>

<details class="foldfig"><summary><b>把那张激活账单逐项摊开：一层里到底挂了哪些张量</b>　<span class="why">⭐ 作者自己写着「这一小节可以整段跳过」—— 给要自己算的人看的，属于「装不装得下」</span></summary>
<h3>1.7　把那张原始账单摊开 ——&nbsp;<u>一层里到底挂了些什么</u></h3>

<!-- ⭐⭐ 2026-09-16 补。这一小节是**给要自己算的人**看的，讲课时可以整段跳过。
     ⛔ 但它不能不写：上面那两个数（原始激活 / 重算后峰值）如果只给结论不给拆解，
       它们就是两个**没法被质疑**的数字 —— 而这一讲从头到尾都在要求「说清是查的还是推的」。
     ⭐ 判据：**一个自己推出来的数，必须连着推导一起给，否则它冒充的是事实。** -->

<p class="lead">⭐ 这一小节可以整段跳过。
  <em>它是给要自己动手算的人看的 ——&nbsp;上面那两个数从哪来，全在这儿。</em></p>

<p><b>基准单位先立住：</b><em>一份 hidden 宽的张量
  ＝ <code>4,096 × 7,168 × 2 B</code> ＝ <b>56 MiB</b>。
  下面所有数都是它的倍数。</em>（序列 <b>4,096</b>、batch 1、bf16。）</p>

<div class="note"><p>⭐ <b>为什么是 4,096 而不是 128K</b> ——&nbsp;
  <em>V3 是在 <b>4K</b> 上做的预训练（14.8T token 全在这个长度上），
  128K 是预训练<u>之后</u>两段各一千步的扩展。
  <b>本讲所有的账都按 4K 算</b>；128K 只在需要对比的地方出现，并会标明它是扩训场景。</em></p>
<p class="sub">⚠️ 换算很简单：<b>激活只认 token 总数</b>（这一点 <a href="#s5-1">5.1</a> 会专门论证），
  所以 128K 的数就是把下面每个数 <b>×32</b>（131,072 ÷ 4,096）。
  ⛔ <b>唯一的例外是注意力分数矩阵</b> ——&nbsp;它带 S²，要 ×1024。见下面第二条边界。</p></div>

<h4>MLA 子层留什么</h4>
__TBL_ACT_MLA__

<h4>MoE 子层留什么</h4>
__TBL_ACT_MOE__

<div class="note ok"><p>⭐⭐⭐ 「9 份」是这张表里最值得停一下的地方。</p>
<p><em>9 ＝ 被激活的 8 个路由专家 ＋ 1 个共享专家。
  <b>每个 token 的那份激活被复制了九遍</b> ——&nbsp;进去一次、出来一次，
  光这两项就 <b>__ACT_NINE__</b>。</em></p>
<p>⛔ 而它在专题一那张参数量的表上<u>完全看不出来</u>。
  <em>——&nbsp;MoE 在参数账上很划算（每 token 只<b>选中</b>一小部分权重参与计算），
  可在激活账上，它要付九份的复制费。</em></p></div>

<p class="landing">⭐ 合起来：一层 MoE 块约 <b>__ACT_LAYER__</b>，一层 dense 块约 <b>__ACT_DENSE__</b>；
  58 层 MoE ＋ 3 层 dense ≈ <u>__ACT_TOTAL__</u>。</p>

<div class="note"><p>📐 <b>换到扩训那一档（128K）</b>：<em>×32 ＝ 一层 MoE 块约 71 GiB，
  全模型约 <b>4.15 TiB</b>。</em></p>
<p class="sub">⛔ 这个 4.15 TiB 是很多人记住的那个数，但它是<b>扩训阶段</b>的账 ——&nbsp;
  真正跑了 14.8T token 的那段预训练，这一项是 <b>__ACT_TOTAL__</b>。</p></div>

<div class="note danger"><p>⚠️ 这张表的三条边界，讲的时候必须说清楚。</p>
<ul>
  <li>它是按算子逐项推的，不是从哪份报告抄的。
    <em>不同框架的算子融合程度、是否顺手重算便宜算子、
    MoE 派发是否真的物化九份副本，差别都很大 ——&nbsp;当量级看，别当准数。</em></li>
  <li>注意力分数矩阵没算在里面。
    <em>它带 <b>S²</b> ——&nbsp;<b>全表唯一一个不跟着 token 总数走的量</b>。
    4K 上是 <b>__ATTN_SCORE__／层</b>（128 头 × 4,096² × 2 B），
    128K 上是 <b>4 TiB／层</b>（×1024，不是 ×32）。
    FlashAttention 压根不把它写进显存，所以它不出现在这张表上
    ——&nbsp;⛔ 换成不带 flash 的朴素实现，这张表整个作废。</em>
    <p class="sub">⭐⭐ <b>别因为「才 4 GiB」就放过它</b>：上面整整一层 MoE 块的全部激活
      加起来才 __ACT_LAYER__ ——&nbsp;<b>这一项自己就比那一整层还大</b>。
      在 128K 上它是压倒性的，在 4K 上它<u>依然是单个最大的张量</u>。
      ⛔ 这条论点没有因为换基准而失效，只是从「吓人」变成「仍然第一」。</p></li>
  <li>那个 <b>__PEAK__</b> 的峰值是「已经做过一次交易之后」的账。
    <em>它假设每层只留入口那一份，<b>再加上当前正在重算的那一层的完整激活</b>
      （__CKPT__ ＋ __INFLIGHT__）——&nbsp;而那恰恰就是<b>开了重算之后的样子</b>。
      ⛔ <b>后面那一项很容易漏</b>：漏掉它，峰值会低估 __UNDER__，
      照着配显存会少配三分之二。
      ⚠️ <b>它还压着两个前提</b>：一是<u>存档点正好切在层边界上</u>；
      二是<u>同一时刻只有一层在重算</u> ——&nbsp;流水线并行的气泡里可能不止一层，
      那时峰值要按同时在算的层数乘上去。<b>两条都成立时这个数才是对的。</b></em><em>
    ⭐ 所以先看原始账单、再看下一节，<b>顺序不能颠倒</b>。</em></li>
</ul></div>

<div class="note danger"><p>⛔⛔ <b>还有一项，它不在上面两张表里 ——&nbsp;因为它不属于任何一层。</b></p>
<p><em>最后那个投影把 hidden 变成<b>词表上的分数</b>（logits），形状是
  <code>[序列长度, 词表]</code>。V3 的词表是 <b>129,280</b>：</em></p>
<p class="landing"><code>4,096 × 129,280 × 2 B</code> ＝ <b>__LOGITS__</b>　
  <em>（一条 4K 序列）</em></p>
<div class="note"><p>⭐ <b>把它跟上面那两张表放一起比一下：</b></p>
<ul>
  <li><em>表里最大的单个张量是「K/V 解压后」，<b>256 MiB</b>。</em></li>
  <li><em>logits 是 <b>__LOGITS__</b> ——&nbsp;<u>差不多是它的四倍</u>。</em></li>
  <li><em>而整整一层 MLA 子层加起来才 <b>722.5 MiB</b>
    ——&nbsp;<b>logits 一项就比一个完整的 MLA 子层还大。</b></em></li>
</ul>
<p class="landing">⛔ <b>它是整个模型里最大的单个张量</b>，
  <em>而且 softmax 通常还要升到 fp32 去做 ——&nbsp;<u>那就再翻一倍</u>。</em></p></div>
<p><em>⭐⭐ <b>所以真实实现几乎都不会把它整个物化出来</b>：
  把序列切成若干块，<u>算一块的 loss、回一块的梯度，再算下一块</u>。
  ——&nbsp;这跟 <a href="#s二">2</a> 那节的重算是同一个思路，
  <b>只是换到了词表这一维上。</b></em></p>
<p><span class="sub">⚠️ 正因为主流实现都分块，<b>它没有被算进上面的总账</b>
  ——&nbsp;这是个口径选择，不是忘了。但你要是自己写训练循环、
  又老老实实一次性算完整个 logits，<b>这一块会突然出现在你的显存里</b>。
  ⭐ 扩训到 128K 那一档它是 <b>31.6 GiB</b>，届时它<u>不分块就根本跑不动</u>。</span></p></div>


</details>
<h3>1.8　⭐ 梯度算出来之后，<u>还没完</u></h3>

<!-- ⭐⭐⭐ 2026-09-16 补。现场把这一讲的范围收紧了：
     「我们主要是想讲**反向计算**，跟所有**优化器**相关的东西。
       因为推理里边它就没有反向计算 —— 你说反向计算它就是训练。」
     ⭐ 这个划法比我原来的排法**更准**：这一讲的边界不是「训练的账」，
       而是**「推理里没有的那些东西」**。梯度这条链整条都在界内，
       而它在 1.3 之后就断了 —— PLAN 里写着「梯度通信」，我没写。
     ⛔ 判据：**范围收紧之后，第一件事不是删，是回头看界内还有什么没写完。** -->

<p class="lead">⭐ 1.3 只讲到<b>梯度算出来</b>那一刻就停了。
  <em>可它离「被用掉」还隔着好几道 ——&nbsp;
  而这几道每一道都是推理里不存在的。</em></p>

__FIG_GRADLIFE__

<details class="foldfig"><summary><b>那笔通信有多大、为什么跟 batch 无关</b>　<span class="why">⭐ 通信是并行的事 —— 专题五整讲在算它</span></summary>
<h4>⭐⭐ 第 ② 步那笔通信，有个很反直觉的性质</h4>

<p><em>纯数据并行下，每一步要把<b>整份梯度</b>在所有卡之间汇总一遍。
  量级很好算：<b>参数量 × 每参数字节数</b>。</em></p>
<p class="landing">⭐⭐⭐ 关键在这儿：这笔通信量<u>跟 batch 完全无关</u>。
  <em>——&nbsp;参数有多少就传多少，你喂一条序列和喂一千条，<b>传的是同样多的字节</b>。</em></p>

<div class="note ok"><p>⭐⭐ 而计算量是随 batch 线性涨的。所以：</p>
<p><em>batch 越大，这笔通信被摊得越薄。</em></p>
<p>⭐ <em>这一下就解释了两件原本看着不相干的事：</em></p>
<ul>
  <li>为什么大模型都在爬 batch。
    <em>GPT-3 从 32K token 一路爬到 3.2M，PaLM 从 1M 翻到 4M，
    V3 从 3072 条序列爬到 15360 条（4K 上下文，折合 <b>12.6M →&nbsp;62.9M token</b>）
    ——&nbsp;<b>它们全都在往上爬</b>，而且都写着「越到后面越大」。</em></li>
  <li>为什么梯度累积除了省显存还顺带便宜。
    <em>累积 K 个 micro-batch 才更新一次，就是<b>把通信次数除以了 K</b>
    ——&nbsp;它省的不只是激活。</em></li>
</ul></div>
</details>

__FIG_BATCH__

<h4>⭐ 那条「能藏」的泳道，值得单独说一句</h4>
<p class="landing">⭐⭐ 「算」和「传」能叠在一起，<b>是训练里最重要的一类优化</b>
  ——&nbsp;<em>而推理侧<b>没有对应物</b>：它没有反向，
  也就没有这条可以边算边传的长尾巴。</em></p>

<p><span class="sub">⭐ 裁剪那一道为什么藏不了、以及「逐层裁剪」为什么是另一件事，
  <em>图下面的落点带写了。这一节只留一句：
  <b>「这一步总共迈多大」才是跟 3.3 对话的那个量。</b></em></span></p>

<h4>📌 顺带把 ④ 说清楚：梯度累积到底省什么</h4>
<ul>
  <li><b>省激活</b> ——&nbsp;<em>同时在场的只有一个 micro-batch 的那份。这是主要收益。</em></li>
  <li><b>省通信</b> ——&nbsp;<em>K 个 micro-batch 只汇总一次。</em></li>
  <li>⛔ <b>不省常驻那块</b> ——&nbsp;<em>权重、梯度、优化器状态一个字节都不少。</em></li>
  <li>⛔ <b>不省算力</b> ——&nbsp;<em>总 FLOPs 一点没变，还是那么多要算。</em></li>
  <li>⚠️ <b>反而可能变贵一点</b> ——&nbsp;<em>累加要在 fp32 里做（见 <a href="#s三">3.2</a>），
    梯度那一栏从 2 字节变成 4 字节。</em></li>
</ul>
<p class="landing">⭐ 一句话：梯度累积改的是<u>时间线</u>，不是<u>总量</u>。</p>

<div class="note"><p>⭐ <b>这一节的边界要说清楚：</b>
  <em>上面只讲了「这一步存在、它有多大、能不能藏起来」。
  至于在不同的切法下它究竟是 all-reduce 还是别的形态、量各自差多少
  ——&nbsp;那是<b>专题五</b>整讲的事。</em></p>
<p><span class="sub">⚠️ 通信量那个「参数量 × 每参数字节」是<b>量级口径</b>：
  真实的环形汇总要来回搬，<b>实际搬运量大约是它的两倍</b>；
  而 MoE 的专家部分走的又是另一套。当量级看。</span></p></div>

</div></section>


<section id="s二"><div class="wrap"><div class="stn"><span class="badge">第 二 节</span><h2>代价：那些中间结果扔不掉 ——&nbsp;以及第一个真正的取舍</h2></div>

<p class="lead">这是全课第一次出现<b>真正的取舍</b>：
  <em>不是「有没有更好的办法」，而是两样东西只能选一样，你选哪个。</em></p>

<p><span class="sub">⭐ <b>这一节留在主线上，是因为它接着 <a href="#s1-6">1.6</a>
  那个结论</b>（反向要用中间结果，所以扔不掉）——&nbsp;
  <em>「扔了再算回来」是这条因果链的下一步，<b>而那条判据很值钱</b>。
  ⛔ 但<u>具体省多少 GiB、哪一档划算</u>属于「装不装得下」，那是
  <a href="index.html">专题五</a> 的事 ——&nbsp;本节相关的细账都收进折叠块了。</em></span></p>


<h3>2.1　重算是什么 ——&nbsp;一句话：<u>不存过程，只存存档点</u></h3>

<p><em>前向每算出一个中间结果就留着，是因为上一节那条
  ——&nbsp;反向要用。重算说：我不留了。</em></p>
<p>反向要用的时候，我从这一层的入口<u>再往前跑一遍</u>，现算出来。
  <em>⭐ 就像游戏存档 ——&nbsp;不存整个过程，只存一个存档点，
  要用的时候从那儿重打一遍。</em></p>

<h3>2.2　全量重算：这笔交易到底划不划算</h3>

<p class="lead">⭐ 先把量级说清楚 ——&nbsp;这才是要记住的东西。</p>
<ul>
  <li><b>省下什么：</b><em>激活那一大块 ——&nbsp;显存里<b>随 batch × 序列长度增长</b>的部分。
    V3 在基准的 4K 上具体是 <b>__ACT_TOTAL__</b> →&nbsp;<b>__PEAK__</b>，
    约 <b>__RECOMP_X__ 倍</b>。</em>
    <span class="sub">⭐ 这个倍数<b>跟序列长度无关</b>（分子分母同比例缩），
    128K 上也是 __RECOMP_X__ 倍 ——&nbsp;4.15 TiB →&nbsp;177.89 GiB。</span></li>
  <li><b>付出什么：</b><em>多跑一次前向，总算力 <b>3× →&nbsp;4×</b>，
    也就是<b>多三分之一</b>。</em></li>
</ul>

<div class="note ok"><p>⭐⭐⭐ 这个兑换比例，<b>夸张到不像是个「权衡」</b>。</p>
<p><em>所以在大模型训练里，它<b>默认就是开着的</b> ——&nbsp;
  值得讨论的从来不是开不开，<b>而是开到哪一档</b>（图 Ⓑ 那三档）。</em></p></div>

__FIG_RECOMPUTE__

<h3>2.3　⭐ 选择性重算：判据只有<u>一个数</u></h3>

<p>对每一个中间张量问<b>同一个问题</b>：
  <em>把它扔掉、反向时重算回来，每省一个字节要付多少次浮点运算？</em></p>
<p>比值越小越该扔。就这一个数，整张排序表自己就出来了。</p>

<div class="note ok"><p>⭐⭐ 对线性层，这个数有闭式解，而且漂亮得出乎意料：</p>
<p><em>一个 <code>[S,k] × [k,n]</code> 的矩阵乘 ——&nbsp;
  重算代价 <code>2·S·k·n</code> FLOPs，产出张量 <code>S·n·2</code> 字节（bf16）。
  一除，每字节代价 ＝ k。</em></p>
<p><span class="sub">⛔ <b>这个闭式解压着一个前提：重算它只付它自己那一个算子。</b>
  <em>成立的条件是<u>它的输入还在</u>。一旦上游那个张量也被扔了，
  就得先把上游重算出来 ——&nbsp;<b>代价要沿链累加</b>，不再等于这一个 k。
  ⭐ 本讲的选择性重算名单都挑在「输入还在」的位置上，所以这条一直成立；
  但你自己排名单时要盯着它。</em></span></p>
<p class="landing">⭐⭐⭐ 重算一个线性层的输出，每字节要付的 FLOPs
  <u>恰好等于它的输入宽度</u>。
  <em><code>S</code> 和 <code>n</code> <b>全约掉了</b> ——&nbsp;
  跟序列长度无关，跟输出多宽也无关。</em></p>
<p>⭐ <em>所以「哪个线性层最该重算」这个问题，<b>答案是看谁的输入最窄</b>
  ——&nbsp;不用算，扫一眼 config 就知道。</em></p>

<div class="note ok"><p>⭐⭐ <b>这里要跟 <a href="#s一">1.6</a> 那个排名对一下
  ——&nbsp;「最占地方的」和「最该扔的」是<u>两个不同的排序</u>。</b></p>
<p><em>1.6 排的是<b>占多少</b>，这一节排的是<b>每省一字节付多少</b>。
  一个看绝对量，一个看比值 ——&nbsp;<u>它们没有任何理由一致</u>。</em></p>
<p class="landing">⭐⭐⭐ <b>而这一次，它们碰巧一致了 ——&nbsp;
  1.6 里最占地方的那三项，正好都在最便宜的那一批。</b></p>
<ul>
  <li><em><b>MoE 派发</b>（占 __MOE_D_PCT__）——&nbsp;逐元素，每字节代价 <b>≈ 0</b></em></li>
  <li><em><b>SwiGLU 乘积</b>（占 __MOE_S_PCT__）——&nbsp;同上，<b>≈ 0</b></em></li>
  <li><em><b>专家输出</b>（占 __MOE_O_PCT__）——&nbsp;输入宽 2,048，
      每 GiB 付 <b>2.20</b>，仍在「小于 3 全收下」那条线之下</em></li>
</ul>
<p><span class="sub">⛔ <b>这是运气，不是规律</b> ——&nbsp;
  <em>反例就在同一张表上：<u>最贵的那一项（o_proj 的输出，每 GiB 付 17.59）
  只占一层的 2.5%</u>，扔它既不省地方又很贵。
  所以<b>下次换个模型，两个排序完全可能打架</b> ——&nbsp;
  真打架的时候，<u>按比值挑，不按大小挑</u>：判据是「每省一字节付多少」，
  从来不是「谁最胖」。</em></span></p></div></div>

<h3>2.4　但 attention 不服从这条 ——&nbsp;而且它<u>必然</u>会跟线性层交叉</h3>

<!-- ⛔ 2026-09-17：这张图原来排在本节 850 字之后，而**它就是这一节的全部画面**。
     ⭐ 判据（今天第三次用）：**一节的核心画面要先出来，字是给它做注脚的。**
       量了一遍全书的图位置才发现只有这一处真错，其余末尾的图都是
       「讲完再看」的收束 ——&#160;所以这条判据要**按图的作用判**，不能只看位置。 -->

<p><em>attention 不是线性层：代价随 S² 涨，产出只随 S 涨。
  一除，<b>每字节代价随序列长度<u>线性上升</u></b>。</em></p>

__FIG_PER_BYTE__

<p class="landing">⭐⭐⭐ 图上那两条<b>斜率不同</b>的线，就是这一节的全部内容
  ——&nbsp;<em>一条平的（线性层，代价 ＝ 输入宽度），一条在爬的（attention）。
  <b>斜率不同，就必然相交。</b></em></p>

<div class="note"><p>⭐⭐⭐ 一条是常数（输入宽度），一条在往上爬
  ——&nbsp;<u>那它们必然相交</u>。</p>
<p><em>拿来对照的那条平线，要用<b>最宽的那个线性层</b>。对 V3 它是
  <b>o_proj</b>（把 128 个头拼起来投回残差流），输入宽 <code>128 × 128 = __WIDEST__</code>
  ——&nbsp;<u>不是 d_model 的 7,168</u>。</em></p>
<p><em>于是：<code>1.25·S = __WIDEST__</code> →&nbsp;<b>S ≈ __CROSS__</b>。</em></p>
<ul>
  <li><b>短于 ~__CROSS_K__</b> ——&nbsp;<em>attention 还没爬到最贵那一档，
    <b>扔掉它不是这一层里最亏的选择</b>。</em></li>
  <li><b>长于 ~__CROSS_K__</b> ——&nbsp;<em>它越过最宽的线性层，从此一路最贵，
    <b>而且线性地越来越贵</b>。</em></li>
</ul>
<p><span class="sub">⚠️ __CROSS__ 是个粗略交叉点，不是工程阈值。
  它依赖 causal 折半的口径、依赖 V3 的头维度、依赖你拿哪个线性层做对照。
  ⭐ 要记的是那句「两条线斜率不同所以必然相交」，不是这个数。</span></p>
<p><span class="sub">⛔ <b>这里我自己踩过一次，值得你顺手学走。</b>
  这个交点早先写的是 <b>5,734</b>，因为我拿 d_model（7,168）当了「最宽的线性层」。
  <em>在标准 MHA 上「头数 × 每头维度 ＝ hidden」，所以那个等式成立；
  <u>MLA 把这条约束解除了</u> ——&nbsp;V3 是 128 头 × 128 ＝ 16,384，比 d_model 宽一倍。
  <b>反例一直印在 1.7 那张表上</b>（「attention 输出　128 头 × 128 ＝ 16,384」），
  只是隔了半节没对上账。</em>
  ⭐ 判据：<b>凡是「A ＝ B」这种听起来像常识的架构关系，换了架构就要重新验一遍</b>
  ——&nbsp;它不会报错，只会让后面每一个数都偏。</span></p></div>

<details class="foldfig"><summary><b>把一层里每个张量按这条判据排一遍 —— __NROWS__ 行名次表、该切在哪、省多少付多少</b><span class="why">⭐ 判据本身上面已讲完；这里是跑一遍的细账，属于「装不装得下」</span></summary>
<p>把 V3 在<b>基准的 4K</b> 上排一遍 ——&nbsp;一层 MoE 块，按「每 GiB 要付多少 TFLOP」升序：</p>

__TBL_PER_BYTE__

<p><span class="sub">⭐ 这张表不用背，它是<u>上面那条闭式解直接排出来的</u> ——&nbsp;
  除了逐元素那几行（代价≈0）和 attention 那一行（随 S 动），
  其余每一行的「每 GiB 付」就是各自的<u>输入宽度</u>换了个单位，
  你拿 config 自己也能排一遍。
  <em>⛔ 顺带自曝：这张表早先有<b>一行</b>是手填的，
  「省显存 9.14 × 每 GiB 7.70」算出来是 70 TFLOP，那一栏却写着 39.08。
  <b>而这张表的卖点正是「你自己也能排一遍」</b> ——&nbsp;
  照着排的人会正好撞上它。<b>现在整张表由闭式解生成，手改不了。</b></em></span></p>

<div class="note danger"><p>⭐⭐ 回答那个直觉问题：「有没有占显存很小、算力却很大的东西？」
  ——&nbsp;<em>有，就是 attention ——&nbsp;<b>但「有多极端」完全取决于你在哪个序列长度上问。</b></em></p>
<p><em>判据只有一条比值 ——&nbsp;<b>比值高的留，比值低的扔</b>。
  而这张表里<u>只有 attention 这一行会随序列长度动</u>，其余各行是输入宽度，跟 S 无关。</em></p>
<table><thead><tr><th>attention 每 GiB 付</th><th>4K（本讲基准）</th>
  <th>128K（扩训）</th></tr></thead><tbody>
<tr><td>绝对值</td><td><b>__ATT_PB_BASE__</b></td><td><b>__ATT_PB_LONG__</b></td></tr>
<tr><td>跟最宽的线性层（o_proj，__WIDEST_PB__）比</td>
  <td><b>只有它的 __ATT_VS_W__</b></td><td><b>贵 __TIMES_LONG__ 倍</b></td></tr>
<tr><td>在这张表里排第几</td><td>第 __RANK_BASE__ / __NROWS__（<b>o_proj 和 gate/up 都更贵</b>）</td>
  <td>第 __RANK_LONG__ / __NROWS__（<b>最贵</b>）</td></tr>
<tr><td>结论那一栏</td><td><b>边际</b></td><td><b>绝不</b></td></tr>
</tbody></table>
<p class="landing">⭐⭐⭐ <b>同一个模型、同一条判据，只把序列长度从 4K 推到 128K
  ——&nbsp;attention 就从「还排在两个线性层后面」变成「比谁都贵 __TIMES_LONG__ 倍」。</b></p>
<p><span class="sub">⛔ <b>别把话说过头</b>：4K 上 attention 的比值是 __ATT_PB_BASE__，
  <u>仍然高于下面那条「小于 3 全收下」的线</u> ——&nbsp;
  所以按本讲自己的切法，它<b>依然不进「收下」那一批</b>。
  <em>变的是「它还是不是那个极端离群值」，不是「它突然该重算了」。
  ⭐ 这个区别很重要：前者是事实，后者要看你把刀切在哪。</em></span></p></div>

<div class="note"><p>❓ <b>「小于 3」这个 3 是哪来的？——&nbsp;<u>我定的，不是算出来的。</u></b></p>
<p><em>它在表上的位置很清楚：专家输出那一行是 2.20，下一行 gate/up 是 7.70
  ——&nbsp;<b>3 就落在这两行中间那个空当里</b>，放 2.5 或者 5 结果完全一样。</em></p>
<p class="landing">⭐⭐⭐ 而真正定这条线的东西<b>不在这张表上</b>：
  <em>它是你现在的<b>兑换率</b> ——&nbsp;<u>一 GiB 显存对你值多少 TFLOP</u>。<br>
  显存快爆了就往右挪（多扔几项，多付点算力），算力是瓶颈就往左挪。</em></p>
<p><span class="sub">⛔ 所以别把 3 记成阈值 ——&nbsp;
  <em>要记的是「表是按比值排的，<b>你只需要决定在哪儿切一刀</b>」。
  ⭐ 排序是客观的，切在哪是你的配置说了算。</em></span></p></div>

<p class="landing">⭐⭐ 照这条线切（把比值小于 3 的全收下来）：一层省 <b>__CHEAP_GIB__</b>，付 <b>__CHEAP_TF__</b>。
  <em>换算到<b>一个 step</b>（一层，前向 ＋ 反向 ＝ 3 遍 ＝ <b>__STEP_TF__</b>）：
  <u>多付 __SEL_PCT__ 的算力，换掉可重算池的 <b>__SEL_SAVE__</b></u>。</em></p>
<p><em>对比全量重算 ——&nbsp;同一个 step 口径下是「多付 33%，换掉 97%」。
  ⭐ <b>选择性重算的性价比还是高 __EDGE__ 倍</b>，这就是它值得单独配的原因。</em></p>

<div class="note"><p>⚠️ <b>这两个数在 128K 上长得很不一样，而漂亮的是那一档。</b></p>
<p><em>128K 上同样切法是「<b>只多付 1.9%</b> 换 __SEL_SAVE__」，性价比高 <b>14.5 倍</b>
  ——&nbsp;确实够得上「一个数量级」。</em></p>
<p class="landing">⭐ <b>为什么差这么多：分子（重算代价）∝ S，
  分母（一个 step）在 128K 上被 S² 的 attention 撑着。</b>
  <em>——&nbsp;所以序列越长，同一批重算看起来越便宜。
  ⛔ 那个「1.9%」是<u>长上下文专属的漂亮数字</u>，别拿到 4K 上引用。</em></p>
<p><span class="sub">⭐ 但方向没变：两个基准上选择性都明显划算。
  <b>而全量重算那个「多付 33% 换 97%」是尺度无关的</b> ——&nbsp;
  它只取决于「前向跑两遍而不是一遍」，跟 S 一点关系都没有。</span></p></div>
</details>

<details class="foldfig"><summary><b>口径收紧：FlashAttention 下 attention 的反向是 2.5 倍不是 2 倍</b>　<span class="why">⚠️ 精确口径，不改变任何结论的方向</span></summary>
<div class="note danger"><p>⛔ <b>顺带收紧一个口径：attention 那一部分的反向<u>不是 2 倍，是 2.5 倍</u>。</b></p>
<p><em><a href="#s一">1.3</a> 那个「反向 ≈ 前向两倍」对<b>带权重的矩阵乘</b>是严格的。
  但这一讲<u>处处假设用了 FlashAttention</u>（<a href="#s1-7">1.7</a> 边界②）——&nbsp;
  而 FlashAttention 为了省显存，<b>反向时要把注意力分数重新算一遍</b>。</em></p>
<p class="landing">📌 FlashAttention-2 论文原话：<em>「To get the FLOPs of the backward pass,
  we multiply the forward pass FLOPs by <b>2.5</b>
  (since there are 2 matmuls in the forward pass and 5 matmuls in the backward pass,
  <b>due to recomputation</b>).」</em>（arXiv <b>2307.08691</b> §4.1）</p>
<p><em>⭐⭐ <b>这正是这一节主题的又一个实例</b>：FlashAttention 省下的显存，
  代价就是<u>它自己内部也在重算</u>。
  ——&nbsp;<b>「没有免费的午餐」在这里有一个非常具体的价码。</b></em></p>
<p><span class="sub">⚠️ 代进本讲的占比：128K 上 attention 占一层前向 82.1%，
  一个 step 是 <b>3.41×</b> 而不是 3×（低估 14%）；
  4K 上 attention 只占 12.5%，是 <b>3.06×</b> ——&nbsp;<b>基本无碍</b>。
  ⛔ 本讲其余各处仍按 3× 的近似口径写，<u>这是有意的简化，不是漏了</u>。</span></p></div>
</details>
<p><span class="sub">⚠️ <b>「选择性重算多付 __SEL_PCT__」和「全量重算多付三分之一」这两个百分比，早先分母不一样</b>（一个除一遍前向、一个除整个 step），
  并排比是不能比的 ——&nbsp;<em>而这一讲自己在 2.5 就写着「跨文献比这类比例前，
  先确认对方折没折半」。<b>已统一到 step 口径。</b></em></span></p>

<div class="note ok"><p>⭐⭐ <b>一条第一方证据：上面那条判据（每省一字节要付多少 FLOPs，＝ 产生它的那个算子的输入宽度）不是我们自己推着玩的。</b></p>
<p><em>DeepSeek-V3 的报告里写得很直白 ——&nbsp;他们
  「重算全部 RMSNorm 运算和 MLA 上投影」，
  理由是「开销很小，却显著降低了存激活的显存需求」。</em></p>
<p class="landing">⭐ <b>RMSNorm 输出和 MLA 上投影，正是这条判据算出来最便宜的那两类</b>——&nbsp;逐元素的代价≈0，上投影的输入宽只有 512 / 1,536。
  <em>判据是我们自己推的，但<b>推出来的名单跟人家实际配的对上了</b>
  ——&nbsp;这比多算一遍更有说服力。</em></p>
<p><span class="sub">📌 出处：arXiv <b>2412.19437</b> §3.2.3。</span></p></div>

<h3>2.5　⭐⭐ 同一条判据，2022 年给出的是<u>相反</u>的答案</h3>

<p>「选择性重算」这个概念出自
  <em>Korthikanti 等，Reducing Activation Recomputation in Large
  Transformer Models，arXiv <b>2205.05198</b>（2022-05）。</em></p>
<p><b>它的判据跟 2.3 一模一样</b> ——&nbsp;<em>论文原话是：挑那些
  「占显存不少、但重算起来不贵」的部分。</em></p>
<p>⛔ 但它选出来要重算的，<u>恰恰就是 attention</u> ——&nbsp;
  <em>QKᵀ、softmax、softmax dropout、以及 attention over V。
  收益是：GPT-3 省 <b>70%</b> 激活显存只付 <b>2.7%</b> 算力；
  MT-NLG 省 <b>65%</b> 付 <b>1.6%</b>。</em></p>

<div class="note ok"><p>⭐⭐⭐ 为什么结论会反过来？因为它的序列长度是 2,048。</p>
<p><em>⛔ 但要用 <b>GPT-3 自己的斜率</b>算。它是标准 MHA，<b>qk ＝ v ＝ 128</b>，
  所以斜率是 <b>1.00</b>，不是 V3 那个 1.25 ——&nbsp;
  代进去是 <code>1.00 × 2048 = 2,048</code>。
  而 <b>GPT-3 自己最宽的线性层是 49,152</b>（＝ 4h，MLP 降维的输入宽；V3 是 7,168）。</em></p>
<p class="landing">⭐ <b>2,048 远小于 49,152 ——&nbsp;它落在自己交点的左边很远处。
  在 2K 上，attention 真的是最划算的重算对象。论文没错。</b></p>

<div class="note ok"><p>⭐⭐⭐ <b>判据一个字没改，结论却翻转了。但有一句要说准：</b></p>
<p>⛔ <em><b>变的不只是序列长度。</b>两个模型的斜率不同（1.00 vs 1.25）、
  对照的线性层也不同（49,152 vs 7,168）——&nbsp;<b>两条线都得重画。</b></em></p>
<p><span class="sub">⛔ <b>而那个 49,152 我自己就写错过</b>：原来写的是 12,288，
  也就是 GPT-3 的 <code>d_model</code>。可这条判据要的是
  <u>产生那个张量的矩阵乘的<b>输入宽度</b></u> ——&nbsp;MLP 降维层的输入是 <b>4h</b>。
  ⭐⭐ 「最宽的线性层 ＝ d_model」在 V3 上<b>碰巧成立</b>（MoE 专家内部只有 2,048），
  在稠密 FFN 上不成立。<b>这正是这一格自己在讲的那件事，我照样踩了一遍。</b></span></p>
<p class="landing">⭐⭐ 换句话说：能迁移的是<b><u>那个形状</u></b>，不是那些数。
  <em>——&nbsp;换个模型，你要<b>重画两条线</b>，而不是重记一组数字。</em></p>
<p><span class="sub">⛔ 顺带自曝一处：这张图早先把 V3 的 1.25 直接套在 GPT-3 上，
  标成了 2,560。<em>——&nbsp;而这一讲从头到尾在讲「别拿 A 的常数套 B」。
  <b>判据：同一张图里出现两个模型时，每个模型的每一条线都要用它自己的常数。</b></em></span></p></div>
<p><span class="sub">⚠️ 一处口径要说明白，不然数字对不上：论文那个 2.7%
  用的是 Megatron 传统公式，<b>没有为 causal 折半</b>。换成本课统一的 causal 口径，
  GPT-3 的 attention 占比是 <b>1.37%</b>。两个都对，只是约定不同 ——&nbsp;
  跨文献比这类比例前，先确认对方折没折半。</span></p></div>

<p>顺带一句：这件事今天已经不用你操心了。
  <em>FlashAttention 天生就不把分数矩阵写进显存、反向时现算
  ——&nbsp;等于把论文那条建议<b>内建成了默认行为</b>。</em></p>

<details class="foldfig"><summary><b>去翻 Megatron 源码：那个默认值今天还在，而维护者在旁边钉了一条警告</b>　<span class="why">⭐ 很好玩，但属于框架实现细节</span></summary>
<div class="note danger"><p>⛔ <b>这里我原来写「所以现代框架的候选名单里 attention 根本不出现」——&nbsp;
  <u>去翻了源码，完全说反了。</u></b></p>
<p><em>Megatron-LM 的选择性重算开关 <code>--recompute-modules</code>，
  候选项里第一个就是 <code>core_attn</code>，
  <b>而且它是<u>默认值</u></b>（<code>recompute_modules = ["core_attn"]</code>）。</em></p>
<div class="note danger"><p>⛔⛔ <b>先把话说准 ——&nbsp;这里有三层，我原来只讲了一层。</b></p>
<ul>
  <li>① <b>「默认」不等于「默认开着」。</b><em><code>recompute_granularity</code>
    的默认值是 <code>None</code>，也就是<u>什么都不重算</u>。
    <code>core_attn</code> 是你<b>主动打开 selective 之后</b>，默认选中的那个模块。</em></li>
  <li>② <b>而在 FlashAttention / 融合 attention 下，重算它基本没用。</b>
    <em>FlashAttention 压根不把注意力分数写进显存 ——&nbsp;
    <u>没有物化的东西，谈不上「扔掉再算回来」</u>。</em></li>
  <li>③ ⭐⭐⭐ <b>最有意思的是：Megatron 自己知道这件事，而且把警告写进了源码。</b></li>
</ul>
<p class="landing"><em>「If you are using transformer_engine as the transformer implementation,
  the core_attn is from transformer_engine and <b>may be the fused version</b>.
  <b>For fused attention, you have no need to set 'core_attn' to recompute.</b>
  Please check that the core_attn recompute is really needed.」</em></p>
<p><span class="sub">📌 <code>transformer_config.py</code> 里
  <code>if "core_attn" in self.recompute_modules: warnings.warn(...)</code>，2026-09 主干。</span></p></div>

<p class="landing">⭐⭐⭐ <b>所以这不是「一个过期的默认值没人管」——&nbsp;
  而是<u>有人发现了，但他没改默认值，他在旁边钉了一条警告</u>。</b></p>

<div class="note ok"><p>⭐⭐ <b>为什么这样处理，比「直接改掉」更值得琢磨。</b></p>
<p><em>改默认值是要付代价的：<b>别人照着旧默认跑出来的结果，从此复现不了</b>。
  ——&nbsp;所以框架宁可留着一个不再最优的默认，也不愿意让历史结果失效。</em></p>
<p class="landing">⛔ <b>于是这个默认值就成了一枚化石</b>：
  <em>它记录的是 2,048 那个年代的最优解 ——&nbsp;在那个长度上它<u>对</u>，
  在长上下文上按我们这张表它是最不该动的那一个，
  而在<b>今天的融合 attention 下它干脆没什么可做</b>。</em></p>
<p><span class="sub">⭐ 判据不在于「这个开关该不该开」，而在于：
  <b>看到任何一个默认值，先问它是哪一年选出来的、当时的前提还在不在。</b>
  ——&nbsp;而这一条对我自己也成立：<u>我最初写的是「它还在那里当默认」，
  那句话把三层揉成了一层，读者会以为一打开训练它就在偷偷重算 attention。</u></span></p></div>
</details>
<p><span class="sub">⭐ <b>更值得看的是名单上的其余几项</b>：
  <code>layernorm</code>、<code>moe_act</code>、<code>mla_up_proj</code>
  ——&nbsp;<em>分别对应 RMSNorm 输出、SwiGLU 乘积、K/V 解压与 Q 展开，按 <a href="#s二">2.3</a> 那条判据算出来都很便宜。
  <b>方向完全一致 ——&nbsp;都落在这张表最便宜的那一批里。</b>
  <em>⛔ 但别说成「一个不多一个不少」：名单上还有 <code>mlp</code> / <code>moe</code> /
  <code>shared_experts</code>，其中 <code>mlp</code> 对应的正是表里最贵的 gate/up。</em>
  我们那条闭式解排出来的名单，和框架实际提供的选项对上了。</em></span></p>
<p><span class="sub">📌 <code>megatron/core/transformer/transformer_config.py</code>
  的 <code>recompute_modules</code> 字段（2026-09 主干）。
  ⛔ <b>判据：说「现代框架都不这么干了」之前，去 grep 一下那个框架。</b>
  <em>这类断言听起来像常识，而它正好是最容易过期的一类。</em></span></p></div>

<p class="landing">⭐⭐ <b>这一节的落点，一句话：</b>
  <em>反向要用中间结果，所以它们扔不掉；
  <u>但「扔掉再算回来」是一笔可以谈的交易</u> ——&nbsp;
  而谈不谈得拢，靠的是<b>一条能自己排出来的判据</b>，不是背别人的结论。</em></p>

<p><span class="sub">⛔ <b>还有两件事这一节没展开</b>，因为它们不属于本讲那三件事：
  <em>重算<u>可能算出不一样的东西</u>（随机数、副作用、逐比特），
  以及<u>收益不能跨规模照抄</u>（MFU / HFU 那套口径）。
  ——&nbsp;都收在下面这个折叠块里。</em></span></p>

<details class="foldfig"><summary><b>重算的两笔「不在账上」的代价：结果可能不一样、收益不能照抄</b>　<span class="why">⭐ 工程上很重要，但属于「怎么把它跑起来」，不是反向/梯度下降/loss 的原理</span></summary>
<h3>2.5b　⛔ 重算还有一笔<u>不在账上</u>的代价：它可能算出不一样的东西</h3>

<!-- ⭐⭐⭐ 2026-09-17 补。整个第二节都在算「省多少显存、付多少算力」——
     那是**两本能算的账**。⛔ 而重算还有第三样东西，它不在任何一本账上：
       **重跑一遍前向，跑出来的东西不保证跟第一遍一样。**
     ⭐ 判据：**一个「重做一遍」的优化，必须问一句「重做出来的一样吗」。**
       这一类问题不报错、不慢、只是结果悄悄不对 —— 而前面那两本账
       一个字都不会提到它。 -->

<p class="lead">⭐ 前面算的是两本能算的账。<em>可重算还有第三样代价，
  <b>它不在任何一本账上</b> ——&nbsp;而且它不报错。</em></p>

<p><b>重算的前提是：把同一段前向<u>再跑一遍</u>，跑出来的要一模一样。</b>
  <em>——&nbsp;有三种情况会让它不一样。</em></p>

<div class="note"><p>⭐ <b>① 随机数 ——&nbsp;这一条框架替你挡住了。</b></p>
<p><em>前向里有 dropout。如果重算时抽到的是另一套掩码，
  那反向用的就不是前向那张网络了 ——&nbsp;<b>梯度直接是错的，而且没有任何报错。</b></em></p>
<p class="landing">⭐⭐ <b>PyTorch 的做法很干脆：把前向那一刻的随机数状态<u>存下来</u>，
  重算前先恢复。</b>
  <em>——&nbsp;<code>torch.utils.checkpoint</code> 的
  <code>preserve_rng_state</code>，<b>默认就是 True</b>。</em></p>
<p><span class="sub">⭐ 而这一条正好解释了 <a href="#s3-7">3.7</a> 那个清单里的第三项：
  <em>checkpoint 里除了权重和优化器状态，<b>还得存随机数状态</b> ——&nbsp;
  原因是同一个：随机数是训练结果的一部分，不是「运行时的临时东西」。</em></span></p></div>

<div class="note danger"><p>⛔ <b>② 副作用 ——&nbsp;这一条<u>没人替你挡</u>。</b></p>
<p><em>如果那段前向除了返回结果，还<b>顺手改了别的东西</b>
  ——&nbsp;更新了一个滑动统计量、累加了一个计数器、写了一行日志
  ——&nbsp;<b>重算会把它再改一遍。</b></em></p>
<p class="landing">⛔ 框架不会替你回滚这些，因为它根本不知道你改了什么。
  <em>——&nbsp;判据很简单：<b>被重算的那一段，最好是个纯函数。</b></em></p></div>

<div class="note"><p>⚠️ <b>③ 逐比特不一致 ——&nbsp;通常无害，但它有个具体的受害者。</b></p>
<p><em>同一段计算跑两遍，结果可能<b>差在最后几位</b>
  ——&nbsp;归约的顺序不同、挑的 kernel 不同，都会这样。</em></p>
<p><em>⭐ 对训练本身，这个量级的差别<b>基本没影响</b>。
  ⛔ <b>但它让「逐比特可复现」变难</b> ——&nbsp;
  而那正是 <a href="#s6-6">6.6</a> 里 PaLM 做到的那件事，
  也是 <a href="#s6-2">6.2</a> 那个「回滚 ＋ 跳数据」的救火办法所依赖的前提。</em></p></div>

<p class="landing">⭐⭐ 一句话：重算把「算力 ↔ 显存」这笔交易谈成了，
  <b>但它同时悄悄引入了一个「结果要可重现」的要求</b>。
  <em>——&nbsp;前两本账会告诉你划不划算，这一条不会：<b>它只在出事的时候现身。</b></em></p>

<h3>2.6　⚠️ 本章落点：收益不能照抄别人的</h3>

<div class="note danger"><p>⛔⛔ 同一个模型、同一个开关，换一个规模，
  收益可能<u>从正的变成负的</u>。</p>
<p><em>原因不神秘：<b>重算改变的是计算与访存的配比</b>，
  而这个配比在不同并行配置、不同芯片数下本来就不同。</em></p></div>

<p class="landing">⭐⭐⭐ 由此推出一条通用规则（它不只对 remat 成立）：
  凡是会改变数据分片形状的参数，都不能跨规模照抄。
  <em>小规模上验过的结论，到目标规模上必须重验。</em></p>

<p>⭐ <em><b>2.5 那个例子说明这条规则比听起来更狠</b>：
  <b>序列长度也在这个名单里</b>，而且它翻转的不是收益的大小，
  <b>是收益的正负号</b> ——&nbsp;在 2K 上最该重算的那一项，
  到 128K 上变成最不该动的那一项。</em></p>

<div class="note"><p>📌 <b>另一个值得知道的量级 ——&nbsp;而这个数可以直接算出来：</b></p>
<p><em>有用的活是 <b>3 份</b>（前向 1 ＋ 反向 2），全量重算再加 <b>1 份</b>
  ——&nbsp;总共执行了 4 份。<b>所以重算占已执行算力的<u>四分之一</u>。</b></em></p>
<p><span class="sub">⛔ <b>这里我原来写的是「三分之一」</b> ——&nbsp;
  <em>那是把「比原来多付三分之一」（1 ÷ 3）和「占执行总量的多少」（1 ÷ 4）
  搞混了。<b>同一个 1 份，分母不同。</b>
  📌 顺带：那个「多三分之一」不是我们推的，
  <b>ZeRO 论文原话就是「33% re-computation overhead」</b>（arXiv 1910.02054 §3.2）。</em></span></p>
<p>⭐ <em>这顺带解释了一个常见困惑 ——&nbsp;<b>为什么 MFU 看起来那么低</b>：
  <b>分子</b>里那一大块重算做了功，但按定义<u>不算进「有效算力」</u>。</em></p>

<div class="note"><p>⭐⭐ <b>说准一点：这里有<u>两个</u>指标，差的正好就是重算。</b></p>
<ul>
  <li><b>MFU</b>（Model FLOPs Utilization）：<em>分子用
    <b>不含重算</b>的模型 FLOPs（也就是 <code>6ND</code> 那一套）。</em></li>
  <li><b>HFU</b>（Hardware FLOPs Utilization）：<em>分子用
    <b>机器真的算了</b>的 FLOPs，<u>含重算</u>。</em></li>
</ul>
<p class="landing">⭐ <b>全量重算时 <code>HFU ／ MFU ＝ 4 ／ 3</b></code>
  ——&nbsp;<em>就是上面那个「4 份里有 1 份是重算」。</em></p>
<p><span class="sub">⛔ <b>所以看到一个 MFU 数字，第一件事是问它的分子取的哪一套。</b>
  ——&nbsp;两篇论文报的 MFU 差 30%，可能只是一篇开了重算、一篇没开。</span></p></div>

<div class="note danger"><p>⛔⛔ <b>而在长上下文上，「MFU 低」的<u>主因不是重算</u>。</b></p>
<p><em>重算最多让 MFU 变成 HFU 的 3/4（<b>1.33 倍</b>）。可 <a href="#s五">5.2</a> 会算给你看：
  128K 上 <code>6ND</code> 只数到真实算力的 <b>17.9%</b> ——&nbsp;
  光这个口径差就是 <b>5.6 倍</b>。</em></p>
<p class="landing">⭐⭐ <b>两个因子的大小<u>随序列长度换位</u>：</b>
  <em>4K 上 6ND 只低估 1.14 倍，<b>重算那 1.33 倍反而成了主因</b>；
  128K 上正好倒过来。</em></p>
<p><span class="sub">⛔ 这条很实际：在长上下文上看到「MFU 只有 20%」就去关重算，
  <b>最多捞回四分之一</b> ——&nbsp;真正该做的是先把 FLOP 口径对齐。</span></p></div></div>



</details>
</div></section>


<section id="s三"><div class="wrap"><div class="stn"><span class="badge">第 三 节</span><h2>有了导数，那一步该迈多大 ——&nbsp;梯度下降与优化器</h2></div>

<p class="lead">前面两节都在跟激活较劲。<em>可把账摊开一看 ——&nbsp;
  那个从头到尾一言不发的角色，才是最大的一块。</em></p>

<h3>3.1　一个参数到底要占多少字节</h3>

<div class="note"><p>❓ <b>先花两句话把前提说清楚，不然下面那张账单看不懂。</b></p>
<p><em>训练的时候，<b>同一个参数在显存里同时存着两份</b>：<br>
  ① 一份<b>省地方的</b>（bf16，2 字节）——&nbsp;前向和反向都拿它算；<br>
  ② 一份<b>精确的</b>（fp32，4 字节）——&nbsp;专门用来记账，<b>每一步的更新加在它身上</b>。</em></p>
<p class="landing">⭐ <b>这套做法就叫「混合精度」。</b>
  <em>——&nbsp;为什么非得存两份，<b>下一小节（3.2）整节都在回答</b>。</em></p></div>

<p>混合精度训练下，<b>每一个参数身上挂着五样东西</b> ——&nbsp;
  <em>而其中只有第一样是「模型本身」。</em></p>

__FIG_OPTIMIZERS__

<div class="note ok"><p>⭐⭐ 这一节只要记住一句：权重 2 字节，优化器那边 12 字节。</p>
<p><em>⭐ 所以「显存里最大的一块是优化器状态」不是一个修辞 ——&nbsp;
  它就是 2 比 12 这个比。</em></p>
<p>⛔ <em>而且这 12 字节<b>全是 fp32</b>。下一小节说为什么它们非 fp32 不可。</em></p></div>

<details class="foldfig"><summary><b>那 16 字节摊开：混合精度到底省没省常驻显存</b>　<span class="why">⭐ 结论「一个字节都没省」很反直觉，但它是显存账 —— 本讲只要记住「Adam 每个参数要记两个矩」</span></summary>
<div class="note danger"><p>⛔⛔ <b>顺手拆掉一个最顽固的误解：「用了混合精度，显存不就省一半吗？」</b></p>
<p class="landing">⭐⭐⭐ <b>常驻这一块，它<u>一个字节都没省</u>。</b>
  <em>——&nbsp;两边算一遍，答案都是 16。</em></p>
<table>
<thead><tr><th></th><th>权重</th><th>梯度</th><th>动量 m</th><th>二阶矩 v</th><th>主权重</th><th>合计</th></tr></thead>
<tbody>
<tr><td><b>纯 fp32</b></td><td>4</td><td>4</td><td>4</td><td>4</td><td>—</td><td><b>16 B</b></td></tr>
<tr><td><b>混合精度</b></td><td>2</td><td>2</td><td>4</td><td>4</td><td><b>4</b></td><td><b>16 B</b></td></tr>
</tbody></table>
<p><em>⭐ <b>看那一列</b>：权重和梯度各省了 2 字节，
  <b>可换来的是多出一份 4 字节的主权重</b> ——&nbsp;<b>2 ＋ 2 正好被 4 吃掉。</b></em></p>
<p class="landing">⭐⭐ <b>那混合精度到底换来了什么？——&nbsp;两样，都不是常驻显存。</b></p>
<ul>
  <li><b>速度。</b><em>矩阵乘走 bf16，用的是芯片上那套专门的单元
    ——&nbsp;这才是它的主要目的。</em></li>
  <li><b>激活。</b><em>激活那一大块是 bf16 存的，<b>这里确实省了一半</b>
    ——&nbsp;而按 <a href="#s五">5.4</a> 那个小例子，激活经常比常驻还大。</em></li>
</ul>
<p><span class="sub">⛔ <b>判据：说「省显存」之前，先问省的是<u>哪一块</u>。</b>
  <em>常驻和激活是两本账，一句「省一半」把它们混在一起，
  <b>结论就会在你最需要它的时候是错的</b>（比如估一张卡装不装得下）。</em></span></p></div>

</details>
<h3>3.2　哪些量必须高精度 ——&nbsp;判据只有一句：<u>看它累不累加</u></h3>

<!-- ⭐⭐⭐ 2026-09-16 现场问：「为啥梯度是 bf16 的，它不是 fp32 的呢？」
     ⭐ 这一问逼出了一条比原来那节更一般的判据。原来这一节只解释了
       「主权重为什么必须 fp32」——&#160;那只回答了五格里的一格。
     ⭐⭐ 换成「看它累不累加」之后，**五格全解释掉了**，而且这条判据
       出了这一讲还能用（FP8 训练里哪些量能压，问的是同一句）。
     ⛔ 判据（元级）：**一条只解释得了一个例子的规则，多半不是规则，
       是那个例子的复述。** -->
<p class="lead">图上那五格，为什么有三格是 fp32、两格是 bf16？
  <em>⭐ 不用一格一格记 ——&nbsp;<b>根子上只有一个画面。</b></em></p>

__FIG_PRECISION__

<p class="landing">⭐⭐⭐ 所以那 12 个 fp32 字节，装的正好是「老的贡献不走」的那几个；
  两个 bf16 字节，装的正好是「用完就扔」的。
  <em>——&nbsp;这不是巧合，这是同一条判据画出来的线。</em></p>

<div class="note"><p>❓ 那梯度能不能是 fp32？——&nbsp;能，而且有些实现就是。</p>
<p><em>最常见的场景是<b>梯度累积</b>：几个 micro-batch 的梯度要相加。
  一直在 bf16 里加，小的会被大的吃掉 ——&nbsp;累几次就掉精度了。
  所以不少实现专门开一个 <b>fp32 的 accumulator</b>。</em></p>
<p>⛔ <em>那样这一栏就是 <b>4 字节</b>，总账从 <b>16 变 18</b>。</em></p>
<p><span class="sub">📌 <b>我们图上那个 16 是哪来的：</b>它是
  <b>ZeRO 论文的经典口径</b> ——&nbsp;2 字节权重 ＋ 2 字节梯度 ＋
  12 字节 fp32 那三份，原文写作 <code>2Ψ ＋ 2Ψ ＋ 12Ψ ＝ 16Ψ</code>。
  <b>arXiv 1910.02054</b>。
  <em>⭐ 所以这个数有出处，不是我们自己定的；但它是一种配置，不是定律
  ——&nbsp;报这个数的时候要把口径一起报。</em></span></p>
<p><span class="sub">⚠️ <b>一处小订正</b>：那两个 2 字节，论文里是 <b>fp16</b>，不是 bf16
  ——&nbsp;2019 年那会儿大家用的是 fp16。
  <em>今天普遍换成了 bf16，<b>字节数一样，所以这张账一个字不用改</b>；
  但两者不是一回事（见 6.6 「bf16 要不要 loss scaling」那条）。</em></span></p></div>

<!-- ⭐⭐⭐ 2026-09-16 补：核 DeepSeek-V3 报告（arXiv 2412.19437）时撞上一条
     **第一方反证**，它把这条判据逼得更准了。原来写的是「累加的就要 fp32」——
     可 V3 明确把 m 和 v 放成了 bf16。
     ⛔ 判据（元级）：**一条判据被反例打中的时候，先别扔它 ——
       多半是它的措辞比它的机制粗。** 这里粗的就是「累加」那两个字。 -->
<div class="note danger"><p>⚠️ 一条第一方反证，把上面那句话逼得更准了。</p>
<p><em>DeepSeek-V3 的报告里明说：AdamW 的<b>一阶矩和二阶矩都用 bf16</b>，
  「没有可观察到的性能退化」；而<b>主权重和梯度仍然保 fp32</b> ——&nbsp;
  梯度保 fp32 的理由，恰恰就是「要用于累积」。</em></p>
<p>⭐⭐ <em>方向没错，措辞太粗。真正的分界不是「累不累加」，是
  <u>会不会<b>无限</b>累加</u>：</em></p>
<ul>
  <li><b>主权重</b> ——&nbsp;<em>跨十万步不断加进微小增量，<b>老的贡献一直在里面</b>。
    这是真的无限累加，必须 fp32。</em></li>
  <li><b>动量 m / 二阶矩 v</b> ——&nbsp;<em>它们是<b>滑动平均</b>，
    每步都乘一个小于 1 的系数再掺新值 ——&nbsp;<b>老的贡献会被衰减掉</b>。
    ⭐ 滑动平均<b>不是</b>累加，所以 bf16 扛得住。</em></li>
  <li><b>梯度</b> ——&nbsp;<em>本来是一次性的；一旦要做梯度累积，它就变成累加量，
    于是又得升回 fp32。</em></li>
</ul>
<p class="landing">⭐ 收紧后的判据：看它是不是「老的贡献永远不走」。
  <em>——&nbsp;永远不走的必须 fp32；会被衰减掉的、用完就扔的，低精度就够。</em></p>

<div class="note"><p>⚠️ <b>「必须 fp32」这句话要留个口子 ——&nbsp;有人真的把它省掉了。</b></p>
<p><em>主权重之所以要 fp32，是因为<b>四舍五入会把小更新整个抹掉</b>。
  ⭐ 而如果把它换成<b>随机舍入</b> ——&nbsp;按余数的大小决定进位的概率
  ——&nbsp;那么被抹掉的部分，<b>在多步之后期望上会被找回来</b>。</em></p>
<p class="landing">⭐ 那么判据其实比「累不累加」还要细一层：
  <em>真正致命的不是累加，是<b>累加 ＋ 每次都朝同一个方向抹零</b>
  ——&nbsp;把这个偏差去掉，bf16 主权重就是可行的。</em></p>
<p><span class="sub">⛔ 这不是推荐做法 ——&nbsp;它要额外的支持，而 fp32 主权重便宜又省心。
  <em>放在这里是因为：<b>知道一条规则「为什么成立」，才知道它什么时候可以不成立。</b></em></span></p></div>
<p><span class="sub">📌 出处：DeepSeek-V3 技术报告，arXiv <b>2412.19437</b>
  <b>§3.3.3</b>（原文分节是「低精度存储与通信」，不是 §3.2.3 那个重算小节）。
  ⭐ <b>原文这句话几乎是逐条印证上面那张清单</b>：
  <em>「用 BF16 而非 FP32 追踪 AdamW 的一阶和二阶矩，未观察到性能下降；
  但主权重、以及<b>用于 batch 累积的梯度</b>，仍保留 FP32。」
  ——&nbsp;<b>连「梯度因为要累积所以升回 fp32」这一条都写在里面。</b></em>
  ⛔ <b>顺带说明这张 16 字节的表不是定律</b> ——&nbsp;
  V3 这套配下来，每参数的账跟经典口径已经不是一个数了。</span></p></div>

<p><span class="sub">⭐ 这条判据出了这一讲还能用：<b>FP8 训练为什么难</b>，
  问的就是同一句 ——&nbsp;精度往下压的时候，<b>哪些量能压、哪些必须留住</b>，
  看的还是它累不累加。<em>那是另一课的事。</em></span></p>

<h3>3.3　⭐⭐ 为什么<u>顺着梯度走</u>，loss 就会变小</h3>

<!-- ⭐⭐⭐ 2026-09-16 现场原话：「梯度是怎么变成权重更新的？**它那个折算关系是啥样？**」
     ⭐ 「折算」这个词问得极准 —— 这里真的有一个**量纲**问题，
       而几乎没有教程点破它。点破之后，从 SGD 到 Adam 到 Muon 那条线
       就不再是「又一个新优化器」，而是**同一个问题的三种答案**。 -->

<!-- ⛔⛔ 2026-09-17 调序。两张「讲为什么」的图原来排在这一节**所有正文之后** ——
     读者先被灌完 SGD / Adam / AdamW，最后才看到「为什么顺着梯度走就能变好」
     和「那一步凭什么该这么大」。**解释排在被解释的东西后面。**
     ⭐ 判据：**一张回答「为什么」的图，要排在提出那个「什么」的段落<u>之前或紧邻</u>。**
       排在后面它就从「解释」退化成了「补充材料」，而多数人读不到那儿。 -->

<p class="lead">⭐ 拿到了 __NPARAM_CN__个偏导数。<em>——&nbsp;<b>然后呢？往哪边挪。</b></em></p>

<div class="note ok"><p>⭐⭐⭐ <b>先把这件事的「为什么」说死，它其实只有一行。</b></p>
<p><em>每个参数挪一点点，loss 会变多少？<b>把每个参数的挪动量乘上它自己的偏导数，再全加起来。</b>
  ——&nbsp;这就是偏导数的定义，没别的。</em></p>
<p class="landing">⛔ <b>那想让 loss 变小，就要让这个总和是<u>负的</u>。</b></p>
<p><em>⭐ 最省事的办法：<b>让每个参数都朝着自己偏导数的反方向挪</b>。
  于是每一项都是「负 × 正」或者「正 × 负」——&nbsp;
  <u>每一项都是负的，加起来必然是负的</u>。</em></p>
<p class="landing">⭐⭐ <b>所以「沿负梯度走，loss 一定下降」不是经验之谈，是<u>算出来的</u>。</b>
  <em>——&nbsp;而且在所有「走同样远」的方向里，<b>它降得最快</b>
  ——&nbsp;这就是「梯度是上坡最快的方向」那句话的另一面。</em></p></div>

<div class="note danger"><p>⛔⛔ <b>但上面那段话里藏着一个词：<u>「挪一点点」</u>。</b></p>
<p><em>「挪动量 × 偏导数」这个算法，<b>只在挪得很小的时候才准</b>。
  ——&nbsp;偏导数说的是「<u>就在你脚下这一点</u>，坡有多陡」，
  它<b>不保证一米之外那里还是这个坡</b>。</em></p>
<p class="landing">⭐ <b>所以步子一大，「保证下降」就失效了</b>：
  <em>你可能冲过谷底，落到对面<b>比出发点还高</b>的地方。</em></p>
<p><span class="sub">⭐⭐ 记住这条裂缝 ——&nbsp;<b>方向是确定的，步长是没有答案的。</b>
  这一小节剩下的部分、以及下一小节<u>整节</u>，都在跟它较劲。</span></p></div>

__FIG_DESCENT__

<!-- ⭐⭐⭐ 2026-09-18：这一段的**骨架**是从 3Blue1Brown 那一集的类名序列读出来的
     （`3b1b/videos` 的 `_2017/nn/part2.py`，61 个 Scene 排下来就是一条叙事线）：
       SingleVariableCostFunction → TwoVariableInputSpace/CostSurface
       → ConfusedAboutHighDimension → NonSpatialGradientIntuition
     即「先一维讲通 → 升二维看形状 → **主动承认高维想不出来** → 不再画空间」。
     ⛔ 那个仓库是 CC BY-NC-SA 4.0，所以**一行代码、一个画面都没有复制** ——
       借的是这条思路；曲线、等高线、分量条全是我们自己算的。
     ⭐ 第三幕最值得学：高维不画成空间，画成**一列条**，颜色＝符号、长度＝大小。 -->
<figure class="fbox fwide" id="anim-descend">
<video src="media/topic04-descend.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="动画分三幕，画面顶端每一幕都有一行字幕。
  第一幕，字幕写着「一维：落进哪个谷，看你从哪儿出发」，下面还有一行公式：w 更新为 w 减去 eta 乘以 L 的梯度。画面里是一条一维的 loss 曲线，红蓝两个小球从曲线两端各自往下滚，最后落进两个深浅不同的谷 —— 同一条曲线、同一个规则，落点却不同。
  第二幕，字幕换成「二维：换成等高线俯视，形状还看得见」。画面换成二维的等高线俯视图，一条绿色轨迹从外圈一路绕进中心的最低处。
  第三幕，字幕换成蓝色的「 __NPARAM_CN__维：画不出来了 —— 改画一列数」。画面中间出现一根竖轴和十四条横向的色条，竖轴顶端两侧各有一个记号：右边一个加号、左边一个减号，说明向右为正、向左为负。蓝色向右表示正、红色向左表示负，长度表示大小；随着训练推进，这些条一根根地缩短 —— 这是把高维参数画成一列数，而不是画成空间。"></video>
<figcaption>⭐⭐⭐ <b>一维 → 二维 → 不再画空间。</b>
  <em>第一幕：同一条曲线、同一个规则，<b>两个球落进了不同的谷</b>。<br>
  第二幕：升到二维，换成等高线俯视 ——&nbsp;形状还看得见。<br>
  ⭐ 第三幕是关键：<b>__NPARAM_CN__维想象不出来，所以干脆不画空间了</b>
  ——&nbsp;把参数画成<b>一列条</b>，颜色是符号、长度是大小，
  <b>它们一根根缩短，就是在下降。</b><br>
  <span class="sub">（15 秒无声循环，Manim 渲染。骨架取自 3Blue1Brown 神经网络系列的
  叙事结构（<a href="https://github.com/3b1b/videos/tree/master/_2017/nn">源码</a>，
  CC BY-NC-SA 4.0）——&nbsp;<b>只借思路，画面与数据全部自算</b>。
  脚本在 <code>tools/manim/</code>。）</span></em></figcaption>
</figure>

<p class="lead">⛔ 那张图末尾留了一句不太好听的话：<b>落在哪个谷，取决于你从哪儿出发。</b>
  <em>一维的图上这件事看着很吓人 ——&nbsp;满眼都是坑，随便掉哪个都出不来。
  <b>下面这张就是来拆这个画面的。</b></em></p>

__FIG_SADDLE__

<p class="lead">⛔ 可这张图是<b>切了两刀给你看剖面</b> ——&nbsp;
  <em>而「一个方向上翘、另一个方向下沉」<b>本来是个三维的形状</b>。
  ⭐ <b>下面让它转起来。</b></em></p>

<!-- ⭐⭐⭐ 2026-09-18：这一格是全讲第一段动画。理由很具体 ——
     上面那张图只能画**两个剖面**，而「一个方向上翘、另一个方向下沉」
     **天生是三维的**：二维讲得出结论，讲不出那个形状。
     ⛔ 视频里原本一个字都没有（2026-09-18 房规松绑后已加解释文字） —— 文字由这段图注和上面那张静态图承担。
       视频里的字既不能选中也不能被读屏，放进去等于把可访问性往回退。
     ⚠️ 静态图排在它上面，所以视频加载不出来也不影响读懂。
     ⛔ 实测结论：manim **不能**替换我们的静态图（它不出 SVG），
       它的价值只在「动起来才讲得清」的那几格。见 tools/manim/ 脚本头。 -->
<figure class="fbox fwide" id="anim-saddle">
<video src="media/topic04-saddle.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="一个鞍面在缓慢自转，画面上打着四组说明文字。顶上一行大字写着「同一个点，一个方向是谷，另一个方向是峰」，下一行小字写着「这样的点叫鞍点 —— 翻山的那个垭口」。左边是红色图例：「沿红线走：往上」「这条路是上坡，loss 变大」。右边是绿色图例：「沿绿线走：往下」「顺着它接着走，loss 变小」。底下一行写着「黑点：这里梯度为零 —— 可它既不是谷底，也不是山顶」，左下角标着曲面的方程：z 括号 u 逗号 v 括号 等于 零点三五 乘以 括号 u 平方 减 v 平方 括号。画面中央那张蓝色棋盘格曲面上，红色曲线沿一个方向往上翘，绿色曲线沿另一个方向往下沉，两条在中间那个黑点处交叉。"></video>
<figcaption>⭐⭐⭐ <b>同一个鞍点，转起来看。</b>
  <em>上面那张图只能切两刀给你看剖面；<b>而它真正的形状是这样的。</b>
  红线沿一个方向<b>往上翘</b>（走过去 loss 变大），
  绿线沿另一个方向<b>往下沉</b> ——&nbsp;<b>两条在中间那个点交叉。</b><br>
  ⭐ 站在那个黑点上，脚下确实是平的 ——&nbsp;<b>可绿色那条路一直都在。</b>
  <span class="sub">（8 秒无声循环，用 Manim 渲染 ——&nbsp;
  就是 3Blue1Brown 那套工具的社区版。脚本在 <code>tools/manim/</code>。）</span></em></figcaption>
</figure>

<p class="lead">⛔ 上面那张图欠了半句：<b>卡不住，可垭口附近坡太平，它会在那儿磨很久。</b>
  <em>——&nbsp;<b>下面这张是那半句的第一个答案。</b></em></p>

__FIG_MOMENTUM__

<!-- ⭐⭐⭐ 2026-09-18 现场：「为啥没有 3b1b 那么 fancy？」——
     答案不是能力，是我自己定的房规：「一个字都不放」砍掉了公式与图形同步变形，
     「15 秒 + 首尾同帧」把一个概念压到十五秒还得留复位段。
     现场拍板松绑：「按最漂亮的风格来，一次搞一个，多轮迭代。」
     ⭐ 这是松绑后的第一支：中文 Text（走 Pango）、MathTex 公式变形、镜头推拉。
     ⭐ 2026-09-20 改成了能循环的动图（结尾复位回第 0 帧）—— 现场：「能放一个动图搞定的，就别放视频。」
     ⛔ 文字进画面之后多一条义务：aria-label 必须复述画面里出现过的每一句话。 -->
<figure class="fbox fwide" id="anim-momentum">
<video src="media/topic04-momentum.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="二十四秒半的分幕动画（无缝循环），画面是一条起伏的灰色曲线，从左上往右下走，中间有一个浅坑，右边有一个更深的谷。
  第一幕，画面顶端出现一行灰字「没有动量：停在第一个坑」。一颗灰球从曲线左端往下滚，滚进那个浅坑之后就停住不动了，画面闪烁一下强调它卡在那里。
  第二幕，顶端换成一行蓝字「带动量：把上一步的速度留下来」。一颗蓝球从同样的起点出发，它头顶有一支水平的蓝色箭头，箭头长度代表当前速度、方向代表往哪边走。蓝球滚到浅坑附近时，镜头推近，可以看到它带着速度直接越过了卡在坑里的那颗灰球，冲出浅坑。镜头拉回全景，蓝球继续往右，一路冲过了深谷、跑到画面右侧很远的地方，此时出现一行红字「冲过了头」。随后它又荡回来，最终稳定落在深谷底部，画面闪烁一下强调。
  第三幕，公式登场。先写出「v t 等于 beta 乘以 v t 减一，加上 g t」，其中 beta 和 v t 减一 两项被高亮，下方出现一行蓝字「上一步的速度，留下 β 那么多」。随后这条公式变形成「w t 等于 w t 减一，减去 eta 乘以 v t」，下方换成一行蓝字「走的不是这一步的梯度，是攒下来的速度」。整段结束时，曲线和两颗球仍留在画面下方。"></video>
<figcaption>⭐⭐⭐ <b>同一条曲线、同一个起点，差别只在「上一步的速度留不留」。</b>
  <em>灰球没有动量，滚进第一个浅坑就<b>停住了</b>；
  蓝球带着攒下来的速度<b>直接冲过去</b>，落进了更深的谷。<br>
  ⭐ 注意它<b>冲过了头</b>才荡回来 ——&nbsp;那不是 bug，是动量真实的副作用，
  也正是下一段要治的东西。<br>
  ⛔ 两颗球跑的是<b>同一份代码</b>，唯一的差别是 β 从 0 变成 0.9。
  <span class="sub">（24.6 秒，Manim 渲染，带播放控件、不循环。
  地形与这两条轨迹是脚本当场跑出来的：「先遇到的坑更浅」「灰球停在浅坑」
  「蓝球落进深谷」「速度峰值高出近三倍」各有一条断言钉住。
  脚本在 <code>tools/manim/</code>。）</span></em></figcaption>
</figure>

<p class="lead">⭐ 这三张图把<b>方向</b>说完了 ——&nbsp;顺着负梯度走 loss 就在降，维度一多基本卡不死，平地上还能靠惯性滑过去。
  <em>这一节只剩一个问题：<b>那一步，到底该迈多大？</b></em></p>

<p>最朴素的那一版只有一行：<b>新权重 ＝ 老权重 −&nbsp;学习率 × 梯度</b>。
  <em>⭐ 但你仔细想 ——&nbsp;<b>这一行凭什么成立？</b></em></p>

<div class="note ok"><p>⭐⭐⭐ <b>前半程收一下 ——&nbsp;到这儿为止，「往哪边走」已经解决了。</b></p>
<ul>
  <li><em>负梯度方向<b>保证</b>让 loss 下降（<a href="#s3-3">上面那张图</a>）——&nbsp;
    只要那一步<b>足够小</b>。</em></li>
  <li><em>高维里挡路的<b>不是碗底，是鞍点</b> ——&nbsp;而动量能把你推过去。</em></li>
</ul>
<p class="landing">⛔ <b>但这两条都带着同一个漏洞：<u>「足够小」是多小？</u></b></p>
<p><em>⭐ 一阶近似只在一小步之内成立。<b>步子一大，那个「保证下降」就失效了</b>
  ——&nbsp;你会冲过谷底，冲到对面更高的地方去。</em></p>
<p class="landing">⭐⭐ <b>所以真正难的从来不是方向，是<u>步长</u>。</b>
  <em>——&nbsp;下面整整一小节都在回答这一个问题。</em></p></div>

<h3>3.3b　⭐⭐⭐ 那一步<u>该迈多大</u> ——&nbsp;从一个量纲问题说起</h3>

<!-- ⭐⭐⭐ 2026-09-19 T16 从 §3.3 里切出来。
     ⛔ 原来 §3.3 一节 8,900 字塞了四个独立主题（为什么能下降 / 鞍点 / 动量 /
       步长怎么定），而**标题承诺的「折算系数」要到 55% 处才出现** ——
       读者带着「要讲折算」的预期先读了四千多字别的东西。
     ⭐ 现场定的三件事之一就是「梯度下降的原理」，所以这里是**拆开讲透，不是砍**。
     ⛔ 用 3.3b 而不是重新编号：3.4 及以后的锚点全都有人引，改号会全断。 -->

<p class="lead">⭐ 上一小节说清了「往哪边走」。<em>这一小节只回答一个问题：
  <b>迈多大一步。</b></em></p>

<p>⭐ <b>先看一行几乎所有人都写过的代码：</b>
  <em><code>参数 ← 参数 − 学习率 × 梯度</code></em></p>

<div class="note"><p>⭐⭐ 两边的单位对不上。</p>
<p><em>梯度的含义是「这个参数变一点，loss 变多少」——&nbsp;
  <b>单位是 loss ÷ 参数</b>
  <span class="sub">（<a href="#s1-0">1.0</a> 说过，loss 的单位是 <b>nats/token</b>
  ——&nbsp;所以这里是 nats/token ÷ 参数）</span>。
  <span class="sub">⚠️ <b>说准一点：「参数」不是物理单位，权重本身是无量纲的数。</b>
  这里是把<u>参数自身的尺度</u>当一个单位在用 ——&nbsp;它在这套推导里守恒，
  所以推得动；但它跟「米」「秒」不是一回事。
  ⛔ 另外这一段推的是 <b>SGD</b>（更新量 ＝ 学习率 × 梯度）。
  Adam 除掉二阶矩之后更新量本身就是<u>参数尺度</u>，
  所以它的学习率量纲跟这里不同 ——&nbsp;下面 fig-beststep 的落点会说这一条。</span>
  可你要的是「参数该挪多少」——&nbsp;<b>单位是参数本身</b>。</em></p>
<p>⛔ 所以中间<u>必须</u>乘一个东西把它折过来 ——&nbsp;那就是学习率。
  <em>反推一下它的单位：<b>参数² ÷ loss</b>，
  也就是 <b>参数² · token / nat</b>。</em></p>
<p><span class="sub">⭐ 把这个单位念出来就明白它为什么不能跨模型抄了：
  <b>它里面同时含着「你的参数有多大」和「你的 loss 有多大」</b>
  ——&nbsp;而 loss 的尺度<u>跟词表大小有关</u>（<a href="#s1-0">1.0</a> 那把尺的上端就是 ln V）。
  换个词表，连它都动。</span></p>
<p class="landing">⭐⭐⭐ 所以学习率<u>不是一个纯数字</u>。
  <em>它跟你的 loss 有多大、你的权重有多大，<b>全绑在一起</b>
  ——&nbsp;这就是为什么换个模型、换个 batch，学习率就得重调。
  它压根不是个通用常数。</em></p></div>

<h4>这个量纲问题一暴露，SGD 的根本毛病也就出来了</h4>

<p class="lead">全网络共用<b>一个</b>学习率，可不同参数的梯度尺度能差好几个数量级。
  <em>——&nbsp;<b>下面两条轨迹用的是同一个谷、同一套规则，只改了学习率。</b></em></p>

__FIG_ONELR__

<!-- ⭐⭐⭐ 动画候选第 4 名。刻意不用前四段那个套路（「两个东西同时跑比谁快」）——
     上面那张静态图已经把 η 大和 η 小各画了一条，再动一遍只多给「谁先谁后」。
     ⭐ 这段的增量来自**第三个维度：学习率自己**。η 做成一根能走的轴，
     于是看到的不是两条轨迹，是一条轨迹的形态在连续演变。
     ⭐⭐ 副产物：参数扫描天然首尾闭合（三角波），不用另造复位段。 -->
<figure class="fbox fwide" id="anim-onelr">
<video src="media/topic04-onelr.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:900px;display:block;margin:0 auto;border-radius:10px"
  aria-label="画面顶端写着「同一个谷、同一个起点 —— 只把学习率 eta 从小扫到大」。左上角三行前提：更新式「w 更新成 w 减 eta 乘 A 乘 w」；「陡方向，纵向，A 一 等于 25」；「平方向，横向，A 二 等于 1」。画面上方是一个又扁又长的二次谷，用几圈灰色椭圆等高线表示，中心那个灰点是谷底。下方是一根学习率标尺，标着「学习率 eta」和量程「零点零一六 到 零点零九三」，上面有一个滑块。标尺中间偏右有一条红色竖线，正上方标着「发散门槛，eta 等于 2 除以 A 一，等于零点零八」；红线两侧紧挨着两条细灰线，右下角说明「两侧细线等于门槛正负百分之五」「左边收敛，右边发散 —— 只差一成」。滑块从左往右移动，也就是学习率从小调到大，上方的下降轨迹随之连续变形，字幕跟着分三段。绿色阶段字幕是「eta 太小：贴着谷底慢慢蹭」「平方向 26 步只走掉三分之一」，轨迹是一长串密密麻麻的绿色小点，贴着谷底往左蹭，二十六步还没走到中心。蓝色阶段字幕是「eta 够大了：陡方向左右横跳」「可幅度一跳比一跳小，还在收敛」。滑块越过红线后进入红色阶段，字幕是「eta 越过门槛：每步在放大」「几步就冲出画面 —— 是性质变了」，轨迹变成一路放大的红色大锯齿，很快冲出画框。随后滑块原路退回，整个过程反向重演。"></video>
<figcaption>⭐⭐⭐ <b>把学习率本身做成一根能走的轴。</b>
  <em>滑块从左往右 ＝ η 从小调到大，上面那条轨迹跟着连续变形：<br>
  <b>贴着谷底慢慢蹭 → 走得刚好 → 开始左右横跳 → 炸出画面。</b><br>
  ⭐ 注意标尺上那三条挨在一起的线：中间红的是<b>发散门槛</b>
  （闭式的，<b>2 ÷ 陡方向曲率</b>），两侧细线是它的 ±5% ——&nbsp;
  <b>只隔标尺的一成，而跨过去就从收敛翻成发散。</b><br>
  ⛔ 而左边那头也不是好消息：η 小到不横跳时，
  <b>平缓方向二十六步只走掉三分之一</b>。<b>两头都不满意，这才是「一个学习率」的困境。</b>
  <span class="sub">（15 秒无声循环，Manim 渲染，脚本在 <code>tools/manim/</code>。
  门槛、±5% 的收敛/发散、以及左端那「三分之一」都是脚本当场跑出来并有断言钉住的。）</span></em></figcaption>
</figure>

<h4>⭐ 自适应那条线，核心想法只有一句</h4>

<p class="landing">别用梯度的大小，只用梯度的方向 ——&nbsp;
  把每个参数<u>自己的尺度</u>先除掉。</p>

<p>❓ <b>可凭什么是「除」？</b>
  <em>——&nbsp;这个问题有一个标准答案，而且小到能当场验。</em></p>

__FIG_BESTSTEP__

<p class="landing">⭐⭐ 看完上面那张图，下面这三个名字就<b>不是三个技巧，是同一件事的三次改良</b>
  ——&nbsp;<em>它们都在<b>凑那个分母</b>。</em></p>

<ul>
  <li><b>AdaGrad</b> 第一个这么干：<em>除以历史梯度平方和的根号。
    ⛔ 但有个致命问题 ——&nbsp;<b>那个分母只增不减</b>，
    走着走着步子越来越小，<b>最后停住不动了</b>。</em></li>
  <li><b>RMSProp</b> 只改一个地方：<em>把「累加」换成「滑动平均」，
    <b>分母不再单调涨</b>。</em></li>
  <li><b>Adam</b> 再加一样：<em>把梯度本身也做个滑动平均（动量），
    <b>让方向别抖</b>。</em></li>
</ul>

<div class="note ok"><p>⭐⭐⭐ 现在关键的一步：Adam 的更新量大约是
  「动量 ÷ 二阶矩的根号」——&nbsp;<u>分子分母的量纲互相抵消</u>。</p>
<p><span class="sub">📌 <b>完整一点</b>：真实的式子是
  <code>m̂ ／ (√v̂ ＋ ε)</code>，那个 <b>ε</b>（常见 1e−8）是防止除零的。
  ⛔ <b>而「量纲互相抵消」严格说只在 ε ＝ 0 时成立</b> ——&nbsp;
  ε 带着梯度的量纲，<u>它正是这条「跟梯度尺度无关」的破口</u>。
  ⭐ 平时它小到无所谓；但梯度真的很小的时候（比如 <a href="#s六">6.5</a> 讲的深层消失），
  <b>是 ε 而不是 √v̂ 在当分母</b> ——&nbsp;那一刻 Adam 就退回成了普通的 SGD。</span></p>
<p><em>也就是说，<b>Adam 把梯度的量纲给除掉了</b>
  ——&nbsp;这一步的大小，变成了一个<b>跟梯度尺度无关的常数</b>。</em></p>

<div class="note danger"><p>⚠️ 那个常数是多少？——&nbsp;<u>不是 1，是 0.2。</u>
  <em>这一条我原来写错了，而且错得很典型：「量纲抵消了」只说明它是常数，
  没说明它等于 1。</em></p>
<p>⭐ <em>实测它<b>稳定在 0.2～0.3</b>，而且<b>不同尺寸的模型都一样</b>。</em></p>
<p class="landing">⭐⭐⭐ <b>而它有一个漂亮到不像话的闭式解 ——&nbsp;
  在「梯度基本是噪声」这个近似下：</b><br>
  <code>update RMS ≈ √[(1−β₁) / (1＋β₁)]</code><br>
  <em>代入 β₁ ＝ 0.9：<b>0.2294</b>。跟实测对上了。</em></p>
<p><span class="sub">📌 出处：苏剑林《为什么 Adam 的 Update RMS 是 0.2？》（科学空间
  <code>kexue.fm/archives/11267</code>）——&nbsp;
  文中给了数值模拟（纯高斯梯度模拟出 <b>0.225</b>）与平均场近似两条路，
  两边都落在同一个数上。</span></p></div>

<p class="landing">⭐⭐ 注意这个式子里<u>只有 β₁，没有 β₂</u>。
  <em>——&nbsp;两个 β 就此分工清楚：<b>β₂ 管「记多久」，β₁ 管「每步迈多大」</b>
  （3.6 接着讲）。</em></p>
<p class="landing">⭐⭐ 所以学习率在 Adam 里的身份<u>变了</u> ——&nbsp;
  它不再是折算系数，而是直接乘在一个常数上：
  <em>每一步每个参数大约挪 <code>0.2 × 学习率</code>。</em></p>
<p>⭐ <em>这才是「Adam 的学习率好调」的真正原因：
  <b>它跟 loss 的尺度、跟梯度的大小都脱钩了</b>，
  只剩下一个由 β₁ 定死的系数。</em></p>
</div>

<h4>AdamW 改的那一处很小，但很重要</h4>

<p><em>原来 Adam 把<b>权重衰减混进梯度里</b>一起算，
  于是它也被那个自适应分母除了一遍 ——&nbsp;
  <b>结果梯度大的参数，正则反而弱。</b></em></p>
<p class="landing">⭐ AdamW 就是把它从梯度里<u>拿出来</u>，直接加到更新那一步上。
  <em>论文标题里那个 <b>decoupled</b>，说的就是这件事
  ——&nbsp;整篇论文主要就干了这一件事。</em></p>

<h4>⭐ 而 0.2 这个常数，顺手解决了「换优化器要不要重调学习率」</h4>

<p><em>既然 Adam 每步挪的是 <code>0.2 × 学习率</code>，
  那<b>别的优化器只要也把自己的 update 对齐到 0.2</b>
  ——&nbsp;Adam 那套调好的学习率和权重衰减就能直接搬过去用。</em></p>
<p class="landing">⭐⭐ 这不是纸上谈兵：Kimi K2 从 Adam 迁到 Muon，用的就是这一招
  ——&nbsp;<em>把 Muon 的 update RMS 统一成 <b>0.2</b>，其余超参照抄。</em></p>
<p><span class="sub">📌 同一篇（苏剑林，科学空间）。
  ⭐ <b>这是「量纲」这条线最实用的一个落点</b>：
  <em>先把某个量做成常数，常数就能当接口用。</em></span></p>

__FIG_MUON__

<div class="note danger"><p>🕰️ <b>这条「一路往上加」的线上，有一次很少被提的<u>翻车</u>。</b></p>
<p><em>Adam 原始论文（2014）是带收敛性证明的。
  ⛔ <b>2018 年，有人指出那个证明是错的。</b></em></p>
<p class="landing">📌 <em>Reddi、Kale、Kumar，<b>《On the Convergence of Adam and Beyond》</b>
  （ICLR 2018，arXiv <b>1904.09237</b>）——&nbsp;摘要原话：
  给出了「a simple convex optimization setting where <b>Adam does not converge</b>」，
  并「describe the <b>precise problems with the previous analysis</b> of Adam algorithm」。</em></p>
<p><em>⭐ 他们指出病根在<b>那个指数滑动平均本身</b>：
  <code>v</code> 会忘掉很久以前那些大梯度，于是某些方向上的步子可以一直不收敛。
  他们的补丁叫 <b>AMSGrad</b> ——&nbsp;让 <code>v</code> 只增不减。</em></p>
<p class="landing">⭐⭐⭐ <b>而最值得记的是后来发生的事：<u>大家继续用 Adam。</u></b></p>
<p><em>AMSGrad 在实践里没比 Adam 好多少，于是那个「证明是错的」的算法
  ——&nbsp;<b>被引用了十万次，训出了今天所有的大模型</b>。</em></p>
<p><span class="sub">⛔ 这件事跟本讲的中心思想完全同频：
  <b>一个东西能用，和它被证明过，是两回事。</b>
  ——&nbsp;而这一讲从头到尾在教的，就是<u>别信结论，去看它的前提</u>。</span></p></div>

<h4>Muon 更激进一层</h4>

<p class="lead">⭐ 上面那张图基本讲完了。
  <em>这里只补图上没说的三句。</em></p>

<ul>
  <li>它不是「又一个调参技巧」。
    <em>Ⓒ 那个视角说明：<b>SGD 和 Muon 是同一个问题的两个答案</b>，
    差别只在<u>拿哪把尺子量「这一步迈多大」</u>。</em></li>
  <li>它跟 3.1 那张账单直接对上。
    <em>不存二阶矩 ——&nbsp;<b>每参数 16 字节掉到 12</b>。</em></li>
  <li><b>而它的算力几乎是白拿的</b>（Ⓓ）——&nbsp;
    <em>⭐ 这是个可以迁移的思路：先找一段本来就闲着的时间，再看它能干什么。</em></li>
</ul>

<p><span class="sub">⭐ Ⓐ 那三列里，中间那一列值得单独想一下：
  <em>Muon 作用在对角阵上会<b>退化成逐元素取 sign</b>
  ——&nbsp;也就是说，<b>「向量做法」本来就是「矩阵做法」的一个特例</b>。
  ⛔ 我们以前把它们当两种东西讲，那是把特例和一般情形讲反了。</em></span></p>

<h4>📌 公式里不写、但真实存在的那几道</h4>

<p><em>梯度算完，到真正更新之间，还要过：</em>
  多卡 all-reduce 求平均 →&nbsp;按全局范数裁剪一次（防止偶发大梯度炸掉）
  →&nbsp;按 schedule 取当前学习率 →&nbsp;权重衰减 →&nbsp;才是那一步更新。</p>

<div class="note ok"><p>⭐⭐⭐ 这一整条线可以用一句话收：
  梯度只告诉你<u>往哪走</u>，优化器决定<u>走多远</u>。</p>
<p><em>而几十年的演化，全都在回答同一个问题 ——&nbsp;
  <b>这个「多远」该由谁说了算：</b></em></p>
<ul>
  <li><b>SGD</b> 说 ——&nbsp;<em>由<b>你</b>说了算，你去调。</em></li>
  <li><b>AdaGrad / Adam</b> 说 ——&nbsp;<em>由<b>梯度自己的历史尺度</b>说了算。</em></li>
  <li><b>Muon</b> 说 ——&nbsp;<em>由<b>矩阵的几何</b>说了算。</em></li>
</ul>
<p class="landing">⭐ 说到底就这么一条线索。</p></div>

<h3>3.4　换个优化器，账单就跟着变</h3>

<p class="lead">⭐ 优化器的选择是一个显存决策，不只是收敛速度的决策。
  <em>——&nbsp;这一点常被忽略，而它恰恰是这一讲要立的那条判据。</em></p>

<!-- ⭐⭐⭐ 2026-09-16 现场：「你这个不得把主流优化器先捋一下？像什么 Adam、
     AdamW，还有 Muon，还有一个最开始的叫 G 什么来着？」
     ⭐ 那个是 SGD。（现场说的「GDN」是 Gated DeltaNet，上一讲的**架构**，
       不是优化器 ——&#160;两个 G 撞车了，值得在正文里替读者点一句。）
     ⛔ 判据：这一节**不按算法怎么算讲，按账单讲** ——&#160;
       更新公式和收敛曲线在这一讲里都不承重。 -->


<div class="note danger"><p>⛔ Muon 那一份不是白省的 ——&nbsp;三条限制都是作者自己写明的。</p>
<ul>
  <li>只管二维参数。<em>标量、向量，以及
    <b>embedding 和最后那个输出头</b>，仍然走 AdamW ——&nbsp;
    作者明说输入输出层用 AdamW 效果才最好。
    所以整模型省不到那四分之一。</em></li>
  <li><b>每一步更慢</b>（原文写明）。<em>⛔ 所以它<b>不是「又快又省」</b> ——&nbsp;
    <b>它把一笔账挪到了另一笔账上</b>：显存那栏减了，单步时间那栏加了。</em></li>
</ul>
<p class="landing">⚠️ <b>这跟<u>上面那张 Muon 图</u>的 Ⓓ 那句「它塞进了一段本来就空着的时间」<u>不矛盾</u>，
  但两句话一定要一起读：</b>
  <em><b>「更慢」说的是方向</b>（多做的那几轮矩阵乘不是免费的），
  <b>「塞进空窗」说的是幅度</b>（作者按 FLOP 算下来低于 1%）。<br>
  ⛔ 而<b>幅度是你的配置说了算的</b> ——&nbsp;那几轮是一串互相依赖的小矩阵乘，
  <b>FLOP 少不代表墙钟时间短</b>。<b>真要用，在目标配置上自己量一遍。</b></em></p>
<p>⭐⭐ <em>而它的证据方式值得单独一提：Muon 在 NanoGPT 那个刷速度的公开竞赛里
  把记录提了 <b>35%</b>，此后<b>十二次破纪录、七个不同的人</b>，全都还在用它。
  ——&nbsp;要是有人能把 AdamW 调到一样好，换回去就能破纪录，可没人换。</em></p>
<p><span class="sub">⭐ 顺带一句跟上一讲连起来：DeepSeek-V4 就是用 Muon 训的
  ——&nbsp;论文摘要里列的三大升级之一。</span></p></div>





<p class="lead">⛔ 在比账单之前，先把这几个名字<b>串成一条线</b> ——&nbsp;
  <em>不然它们只是几个并列的选项，只能靠背。</em></p>

<div class="note ok"><p>⭐⭐⭐ <b>这条线只有一句话：每一代都在治上一代<u>没治住的那种不稳</u>。</b>
  <em>——&nbsp;对照 <a href="#s六">6.0</a> 那张失败模式表看，它就不用背了。</em></p>
<ul>
  <li><b>SGD</b> ——&nbsp;<em>只有方向，没有记忆。
      沿陡方向来回弹，沿平方向几乎不动。</em></li>
  <li><b>＋ 动量</b> ——&nbsp;<em>治的是<b>来回弹</b>：把历史梯度做滑动平均，
      相反的分量互相抵消，同向的累加起来。</em></li>
  <li><b>Adagrad / RMSProp</b> ——&nbsp;<em>治的是<b>各参数尺度差太远</b>：
      全网共用一个学习率，可不同参数的梯度能差几个数量级
      （<a href="#s3-5">3.5</a> 那两条轨迹就是这件事）。
      办法是给每个参数各除以自己梯度的均方根。</em></li>
  <li><b>Adam</b> ——&nbsp;<em>把上面两样<b>合体</b>：一阶矩管方向、二阶矩管尺度。
      代价就是那多出来的 8 字节（<a href="#s3-1">3.1</a>）。</em></li>
  <li><b>AdamW</b> ——&nbsp;<em>治的是一个副作用：
      <b>权重衰减被二阶矩缩放了</b> ——&nbsp;梯度大的参数反而被正则得更弱。
      办法是把衰减从梯度里摘出来，直接作用在权重上。</em></li>
  <li><b>Muon</b> ——&nbsp;<em>治的是<b>更新的方向本身</b>：
      Adam 逐元素地定每一步，整块矩阵的更新可能被少数几个方向主导。
      Muon 对更新矩阵做正交化，让各方向的步子更均匀。</em></li>
</ul>
<p class="landing">⭐ <b>所以「换优化器」从来不是换个名字 ——&nbsp;
  是换一个你打算治的病。</b>
  <em>⛔ 而代价永远落在同一个地方：<u>每个参数要额外记几个字节</u>。</em></p></div>

__FIG_LINEAGE__

<h4>⭐ 省状态的三条岔路 ——&nbsp;它们各砍掉了那 16 字节里的不同部分</h4>

<!-- ⭐⭐ 2026-09-16 补。原来这三条只写在 fig4-optimizers 的出处栏里，
     正文一个字都没有 —— 而「每参数几字节」正是这一讲的主轴。
     ⛔ 判据：**图的出处栏是给人查的，不是给人读的。**
       一件事只写在那儿，等于没写。 -->

<p>⭐ 都是在动那 16 字节里的某一格，但动的格子不一样
  ——&nbsp;<em>下面的字节数按 3.1 那套口径当场算：</em></p>

<table>
<thead><tr><th>做法</th><th>动了哪一格</th><th>每参数字节</th><th>代价</th></tr></thead>
<tbody>
<tr><td><b>AdamW</b>（基准）</td><td>——</td><td>2＋2＋4＋4＋4 ＝ <b>16</b></td><td>——</td></tr>
<tr><td><b>Muon</b></td><td>把<b>二阶矩</b>整份拿掉</td><td>2＋2＋4＋4 ＝ <b>12</b></td>
    <td>只管二维参数；改用正交化补回来</td></tr>
<tr><td><b>Lion</b></td><td>同样只留一份动量</td><td>2＋2＋4＋4 ＝ <b>12</b></td>
    <td>更新只用<b>梯度的符号</b></td></tr>
<tr><td><b>Adafactor</b></td><td>把二阶矩<b>分解成一行 ＋ 一列</b></td>
    <td>那一份 ≈ <b>0</b>；不带动量时总共 <b>8</b></td>
    <td>丢掉了 v 的逐元素信息</td></tr>
<tr><td><b>8-bit Adam</b></td><td>两份状态<b>各量化到 1 字节</b></td>
    <td>2＋2＋4＋1＋1 ＝ <b>10</b></td>
    <td>算法不变，换的是存法</td></tr>
</tbody></table>

<p class="landing">⭐⭐ 注意这三条走的是<b><u>三个不同的思路</u></b>：
  <em>Muon 和 Lion 是「不要那一份」；Adafactor 是「换个更省的表示」；
  8-bit 是「同一份东西存得更小」。
  ——&nbsp;<b>所以 8-bit 可以跟前两者叠加，而前两者互斥。</b></em></p>

<div class="note"><p>📌 8-bit Adam 那条值得多说一句，因为它的做法很干净。</p>
<p><em>它把状态张量<b>切成小块、逐块独立量化</b>，
  再加上一种<b>对大值小值都精确</b>的非线性量化。
  论文报告：在一系列任务上保持 32 位的效果，而且不用改任何优化器超参
  ——&nbsp;两行代码的 drop-in 替换。</em></p>
<p><span class="sub">📌 Dettmers 等，<b>arXiv 2110.02861</b>（ICLR 2022 spotlight）。</span></p>
<p>⚠️ 但「不改算法只改存法」这句话<b><u>不完全成立</u></b>，摘要里就写着。
  <em>论文把三件事绑在一起才达到 32 位的效果：分块量化、非线性量化，
  以及第三件 ——&nbsp;<b>一个「稳定 embedding 层」</b>。</em></p>
<p class="landing">⛔ <b>第三件是<u>动网络结构</u>的，不是存法。</b>
  <em>它存在的理由很具体：语言模型的输入 token 分布极不均匀，
  embedding 那一层的梯度方差特别大 ——&nbsp;
  <b>而量化最怕的就是方差大。</b></em></p>
<p><span class="sub">⭐ 这一条本身就是个判据：
  <em>一个号称「drop-in 替换」的东西，先去摘要里数一数它到底改了几处
  ——&nbsp;<b>「两行代码」说的是调用方改两行，不是它内部只改了一处。</b></em></span></p></div>

<h4>⚠️ Muon 要在大模型上真跑起来，还得补两样</h4>
<p><em><a href="#s3-3b">3.3b</a> 那段讲的是 Muon 的<b>想法</b>。
  可想法好用不等于能直接上规模 ——&nbsp;
  2025 年有一篇专门做这件事的论文，识别出两条必需的补丁：</em></p>
<ul>
  <li>① 得加 weight decay。<em>原版没有。</em></li>
  <li>② 得仔细调每个参数的<u>更新幅度</u>。
    <em>——&nbsp;正交化之后各方向尺度是齐了，但整体该迈多大还得单独定。</em></li>
</ul>
<p class="landing">⭐ 补上这两样之后，它才能「开箱即用」地跑大规模训练。
  <em>论文的规模律实验报告：在算力最优的设定下，Muon 的计算效率约为 AdamW 的两倍。</em></p>
<p><span class="sub">📌 <b>arXiv 2502.16982</b>（Moonlight，3B/16B MoE，5.7T token）。
  ⚠️ 「两倍计算效率」是论文自己的规模律结论，不是我们的实测 ——&nbsp;
  <em>而且回想 <a href="#s二">2.6</a> 那条判据：这类结论换规模要重验。</em>
  ⛔ <b>至今仍未核实的一条</b>：Muon 下主权重是否仍需 fp32。</span></p>

<h3>3.5　⭐ 学习率：那条曲线到底怎么画</h3>

<!-- ⭐⭐⭐ 2026-09-16 现场原话：「学习率这个是个非常高深的手艺，该怎么调？
     有哪些方法？下降的速度是多少？一开始是多少？**这些都得讲。**」
     ⭐ 这一节的数**一个都没凭印象写** —— 逐条核过 GPT-3 的表 2.1、
       DeepSeek-V3 报告的训练超参段、以及那篇 warmup 机制论文。
     ⛔ 判据：**「高深的手艺」往往只是「没人把公开配置列在一起」。**
       列出来之后，规律是肉眼可见的。 -->

<p>⭐ <em>在钻进「曲线怎么画」之前，先给一把<b>随时能用的尺子</b>
  ——&nbsp;<b>它不告诉你最优解，但能告诉你「有没有离谱」。</b></em></p>

__FIG_UPDRATIO__


<!-- ⛔ 2026-09-17：这张图原来也在**全节 1700 多字之后**，跟 3.3 那两张一个毛病。
     ⭐ 同一条判据：**先给整体，再拆局部。**
       曲线的形状是这一节的骨架，骨架要先立住，后面三段才有地方挂。 -->

<p class="lead">⭐ 整条学习率曲线只有三段：<b>升上去、稳住、降下来</b>。</p>

__FIG_LR_CURVE__

<p class="landing">⭐ 形状就这样。<em>下面把三段拆开 ——&nbsp;
  <b>每一段都有公开配置可以抄，不用猜。</b></em></p>

<h4>第一段 · warmup ——&nbsp;流行的那个解释是错的</h4>

<p>⛔ <em>大部分教程说：Adam 一开始样本太少、二阶矩估不准，所以得慢慢来。</em></p>
<div class="note danger"><p>⚠️ 2024 年有一篇专门做系统实验的论文，明确说这不是主因。</p>
<p><em>原文的判断是：warmup 压倒性的好处，在于
  <b>让网络能承受一个更大的目标学习率</b> ——&nbsp;
  它把网络推到 loss 曲面上<b>条件更好</b>的区域去。
  而「样本太少估不准」这个说法，他们直接反驳了：
  真正的问题是预条件曲率一开始就很高，大 batch 也一样高。</em></p>
<p class="landing">⭐⭐ 所以 warmup 真正的价值是<b>让你敢把峰值设高</b>
  ——&nbsp;<em>顺带让这个超参<b>更好调</b>（可选区间更宽）。</em></p>
<p><span class="sub">📌 出处：Why Warmup the Learning Rate? Underlying
  Mechanisms and Improvements，arXiv <b>2406.09405</b>（NeurIPS 2024）。</span></p>

<p>⛔⛔ <b>但「明确说这不是主因」这句<u>说得太满</u>，得收一格。</b>
  <em>那篇论文的 Limitations 自己写着：实验是在
  <b>相对小规模</b>的数据集和模型上做的，能否推广到更大规模还需进一步研究。</em></p>
<p><em>⚠️ 具体有多小：<b>Transformer 只有 4 层、上下文长度 64</b>，
  另一组是 CIFAR-10 上的 WRN。<b>——&nbsp;这跟我们整讲在谈的尺度差着好几个数量级。</b></em></p>

<!-- ⭐⭐⭐ 2026-09-18 R15：补上第三个解释。
     ⛔ 这一段的由来值得记：subagent 扒回来的转述是「warmup 治的不是更新量太小，
       是更新量全是噪声」——&nbsp;听上去像是又一个互相竞争的说法。
     ⭐ 但我自己打开原文核了一遍，发现转述**漏了最要紧的那半句**：
       「这里的讨论前提是梯度消失，如果是 Pre Norm 之类的结构，
        没有明显的梯度消失现象，那么不加 Warmup 往往也可以成功训练。」
     ⭐⭐⭐ 这半句把「三个解释怎么调和」直接解决了 ——&nbsp;
       它们**不是在抢同一个位置，是不同前提下的不同机制**。
     ⛔ 只看转述就落地的话，我会把一个**有适用边界的解释**写成普适的。 -->
<p>⭐⭐⭐ <b>而还有第三个说法 ——&nbsp;<u>它自带一个前提</u>。</b></p>

<p><em>苏剑林在讨论「Transformer 怎么解决梯度消失」时，顺带回答了这个问题。
  他的切入点跟上面两个都不同：<b>Adam 让「梯度消失」这件事换了个意思。</b></em></p>

<div class="note"><p>⭐ 先看 Adam 的更新量长什么样（原文的写法）：</p>
<p style="text-align:center"><code>Δθ ＝ −η · E[g] ⁄ √E[g²]</code></p>
<p><em>分子分母<b>同量纲</b>，所以那个分式是 <b>O(1)</b>，更新量就是 <b>O(η)</b>。
  ——&nbsp;这跟 SGD 完全不同：SGD 的更新量正比于梯度，<b>梯度小，步子就小</b>。</em></p>
<p class="landing">⭐⭐ 所以在 Adam 底下，梯度消失<b>不再意味着「学不动」</b>
  ——&nbsp;<em>只要梯度还大于随机误差，参数照样拿到常数量级的更新。</em></p></div>

<p>⛔⛔ <b>可这恰恰是麻烦的开始。</b>
  <em>原文的推理链是这样的（前提：模型确实有明显的梯度消失）：</em></p>
<ol>
<li><em>不做 warmup，一上来就快学。<b>越靠后的层越敏感，学得越快。</b></em></li>
<li><em>可后面的层<b>是拿前面的层的输出当输入的</b>，而前面的层还没学好
  ——&nbsp;它是在<b>糟糕的输入上</b>快速前进。</em></li>
<li><em>很快它到达一个<b>糟糕的局部最优</b>，学习放缓，
  <b>反传回前面的梯度进一步变弱</b>。</em></li>
<li>⛔ <em>梯度不准了，<b>可 Adam 的更新量还是常数量级</b>
  ——&nbsp;于是那就是<b>一个常数量级的随机噪声</b>。
  方向开始乱，前面崩，后面跟着崩。</em></li>
</ol>

<div class="note danger"><p>⭐⭐⭐ 这条推理给了一个<b>可以拿去对日志的现象</b>
  ——&nbsp;比任何机制解释都好用：</p>
<p class="landing"><b>Post-LN 的模型不做 warmup，你会看到：
  loss 先快速收敛到一个常数附近，再训一段，开始发散，直至 NaN。</b></p>
<p><em>⭐ 而 warmup 做的事，用原文的话说是
  <b>「抑制了后面的层的学习速度，并且给了前面的层更多的优化时间，
  以促进每个层的同步优化」</b>。</em></p></div>

<p>⛔⛔⛔ <b>但这一段有一个<u>必须一起记住的前提</u>，原文自己写得很清楚：</b></p>
<blockquote><em>「这里的讨论前提是梯度消失，如果是 Pre Norm 之类的结构，
  <b>没有明显的梯度消失现象，那么不加 Warmup 往往也可以成功训练。</b>」</em></blockquote>

<div class="note ok"><p>⭐⭐⭐ 于是三个说法各就各位了
  ——&nbsp;<b>它们不是在抢同一个位置。</b></p>
<table><thead><tr>
<th>说法</th><th>前提</th><th>现在怎么看它</th></tr></thead><tbody>
<tr><td>二阶矩样本太少、估不准</td><td>没说前提</td>
    <td>⛔ 已被 2024 那篇系统实验<b>驳掉</b></td></tr>
<tr><td>让网络能承受<b>更大的峰值学习率</b></td><td>一般情况</td>
    <td>⭐ 成立 ——&nbsp;但那篇的实验<b>只到 4 层</b></td></tr>
<tr><td><b>压住后面的层，等前面的层跟上</b></td>
    <td><b>有明显梯度消失<br>（也就是 Post-LN）</b></td>
    <td>⭐ 成立，而且<b>给了可观察的现象</b></td></tr>
</tbody></table>
<p class="landing">⭐⭐⭐ <b>所以该问的不是「哪个解释对」，是「在什么前提下」。</b>
  <em>你的模型是 Pre-LN 还是 Post-LN，决定了哪一条在起作用 ——&nbsp;
  现代 LLM 基本都是 Pre-LN（见 §6.5），所以第二条更相关；
  <b>但如果你在训 Post-LN 的东西、又看到「loss 先平后 NaN」，那就是第三条。</b></em></p></div>

<p><span class="sub">📌 出处：苏剑林《模型优化漫谈：BERT 的初始标准差为什么是 0.02？》，
  <a href="https://kexue.fm/archives/8747">kexue.fm/archives/8747</a>
  ——&nbsp;上面引号里的两句为原文原话。
  ⚠️ 这是<b>作者本人的分析文章</b>，不是同行评议论文；
  <b>我们按「一个有前提的解释」收下它，不当定论。</b></span></p>
<p class="landing">⭐⭐⭐ <b>所以准确的说法是</b>：
  <em>在<u>那个尺度</u>上，他们把「估不准」这个解释证伪了，
  并给出了一个更有解释力的机制。<b>而这个结论能不能搬到千亿参数上，
  他们自己没说能。</b></em></p>
<p><span class="sub">⛔ 顺带自曝一处，而且很讽刺：
  <em>这一讲在 <a href="#s二">2.6</a> 立了一条判据 ——&nbsp;
  <b>小规模上验过的结论，到目标规模必须重验</b>。
  结果我转述这篇论文的时候自己就没守。
  ——&nbsp;<b>判据写在纸上容易，用在自己引的那篇论文上才难。</b></em></span></p></div>

<p><b>实际用多长？</b><em>GPT-3 是<b>头 3.75 亿个 token</b> 线性升上去
  ——&nbsp;总训练量 3000 亿，占 <b>0.125%</b>。DeepSeek-V3 是<b>头 2000 步</b>。</em></p>
<p class="landing">⭐ 换句话说，<b>这一段短到可以不当成超参看</b>。</p>

<h4>第二段 · 峰值设多少 ——&nbsp;有一张现成的表可以抄</h4>

<p><em>GPT-3 论文的<b>表 2.1</b> 把<b>八个</b>规模的峰值学习率和 batch 一起列了出来
  ——&nbsp;这是一张免费的先验，不用自己扫：</em></p>

<div class="note"><p>📌 GPT-3 全系列（同一套数据、同一套训练流程，只有规模在变）：</p>
<ul>
  <li><b>125M</b> →&nbsp;6.0×10⁻⁴，batch 0.5M token　|
      <b>350M</b> →&nbsp;3.0×10⁻⁴，batch 0.5M　|
      <b>760M</b> →&nbsp;2.5×10⁻⁴，batch 0.5M</li>
  <li><b>1.3B</b> →&nbsp;2.0×10⁻⁴，batch 1M　|
      <b>2.7B</b> →&nbsp;1.6×10⁻⁴，batch 1M</li>
  <li><b>6.7B</b> →&nbsp;1.2×10⁻⁴，batch 2M　|
      <b>13B</b> →&nbsp;1.0×10⁻⁴，batch 2M</li>
  <li><b>175B</b> →&nbsp;0.6×10⁻⁴，batch 3.2M</li>
</ul>
<p class="landing">⭐⭐⭐ 规律肉眼可见：模型越大，学习率越小、batch 越大。
  <em>而且幅度很温和 ——&nbsp;<b>1,400 倍的规模差，学习率只差 10 倍</b>（125M 的 6.0e-4 → 175B 的 0.6e-4）。</em></p>
<p>⭐ <em>论文自己的话是：「更大的模型通常能用更大的 batch，
  <b>但需要更小的学习率</b>」。他们用<b>梯度噪声尺度</b>来定 batch。</em></p>
<p><span class="sub">⛔ 注意这张表里 batch 和学习率是<u>一起</u>变的 ——&nbsp;
  这是最容易漏的一条：你不能只调学习率不看 batch，它俩绑在一起。
  GPT-3 还额外把 batch 从 32K token 线性爬到目标值（头 40–120 亿 token 内）。
  📌 出处：arXiv <b>2005.14165</b> 表 2.1 与<b>附录 B</b>；
  「更大的模型要用更小的学习率」那句在 §2.3。</span></p></div>

<!-- ⭐⭐⭐ 2026-09-18 R17：batch 与学习率的关系不是单调的。
     ⛔ 这一条**刻意不画图**，理由值得记：它是一条「取决于 β_noise、不一定出现」
       的规律，而图天然会把它画成一条确定的曲线 ——
       ⭐ 判据：**一条带条件的规律画成图，条件就掉了。**
       文字反而能承载「什么时候出现、什么时候不出现」。
     ⚠️ subagent 的转述是「超过 B_noise 后最优学习率反而要降」，
       而原文明确加了限制，而且苏剑林还批评了原论文把它说成几乎总会出现。
       这是本批第二次自核救回一条被过度概括的结论（第一次是 R15 的 warmup）。 -->
<p>⛔⛔ <b>而「batch 越大、学习率就越能调大」这句，还有一层<u>很多人不知道的天花板</u>。</b></p>

<div class="note"><p>⭐ 先把常见的那两条摆出来：</p>
<ul>
<li><b>平方根缩放</b>：batch 扩大 n 倍、学习率扩大 √n 倍。
  <em>——&nbsp;推导的出发点是<b>让更新量的噪声强度保持不变</b>
  （增量协方差 ∝ η²/B，要它不变就得 η ∝ √B）。</em></li>
<li><b>OpenAI 用梯度噪声尺度</b> B<sub>noise</sub> 给出
  <code>η* ≈ η<sub>max</sub> /(1 ＋ B<sub>noise</sub>/B)</code>
  <em>——&nbsp;注意这个形式是<b>单调递增且有上界</b>的：
  batch 再大，学习率也只能逼近 η<sub>max</sub>，不会一直涨。</em></li>
</ul>
<p class="landing">⭐⭐ 光这一条就够破一个想当然了：
  <b>「batch 翻倍，学习率就该翻倍」是错的</b>
  ——&nbsp;<em>它有上界，而且越往后收益越小。</em></p></div>

<p>⛔⛔⛔ <b>但在 Adam 底下，还可能更反直觉一点：</b>
  <em>超过某个 batch 之后，<b>最优学习率不升反降</b>
  ——&nbsp;这就是所谓的 <b>Surge 现象</b>。</em></p>

<div class="note danger"><p>⭐⭐⭐ <b>为什么会这样？</b>
  <em>直观的说法是：<b>这本质上是「自适应学习率」本身次优的体现。</b></em></p>
<p><em>把 Adam 粗略看成 <code>sign(g)</code>：batch 越大，估出来的
  <code>sign(g̃)</code> 就越接近真的 <code>sign(g)</code>。
  ⛔ <b>可 sign(g) 就是最好的更新方向吗？——&nbsp;不一定</b>，
  尤其到了训练后期。<br>
  ⭐ 于是 batch 取中等的时候，<b>那点噪声反而在替你修正这种次优</b>；
  batch 再大，噪声没了，<b>修正的机会也没了</b>，
  所以只好更谨慎地把学习率降下来。</em></p>
<p>⛔⛔ <b>但这条<u>有前提，而且原作者自己特意收了一格</u>：</b></p>
<blockquote><em>「（当然这里还有一个限制，β 是始终小于 1 的，
  <b>如果 β<sub>noise</sub> ≥ 1，那么最优学习率与 Batch Size 的关系依旧是单调递增的。</b>）」</em></blockquote>
<p class="landing">⭐ 苏剑林还<b>顺手批评了那篇原论文</b>：
  <em>原论文在一个较强的近似下得出「Surge 几乎总会出现」，他认为<b>不大科学</b>；
  他自己换了个更合理的近似，结论是
  <b>「即使 Hessian 的非对角元不可忽略，Surge 现象也不一定会出现」</b>。</em></p>
<p><span class="sub">📌 苏剑林《当 Batch Size 增大时，学习率该如何随之变化？》
  <a href="https://kexue.fm/archives/10542">kexue.fm/archives/10542</a>；
  所评论的原论文为《Surge Phenomenon in Optimal Learning Rate and Batch Size Scaling》。
  ⚠️ 引号里为原文原话。<b>这是作者本人的分析，不是同行评议结论</b>
  ——&nbsp;<b>我们收的是「别想当然地外推」这条警告，不是一个可以照抄的公式。</b></span></p></div>

<p>一个现代参考值，它看着完全不合上面那条规律：
  <em>DeepSeek-V3 是 <b>671B</b> 的 MoE，峰值 <b>2.2×10⁻⁴</b>
  ——&nbsp;比 GPT-3 的 175B 大得多，<b>学习率却高出近四倍</b>。</em></p>

<div class="note danger"><p>⛔ <b>我先给过一个解释，而它<u>被上面那张表当场推翻了</u>。</b></p>
<p><em>那个解释是：「V3 每个 token 只激活 37B，所以该按 37B 看」。
  ⛔ 可你往上一行看：GPT-3 的 13B 是 1.0×10⁻⁴，175B 是 0.6×10⁻⁴，
  <b>37B 夹在中间，按那张表应该是 0.8×10⁻⁴ 上下</b> ——&nbsp;而 V3 是 2.2×10⁻⁴。<br>
  换个口径<b>没有把差距抹平</b>，只是从「差 3.7 倍」变成「差 2.8 倍」。</em></p>
<p class="landing">⭐⭐⭐ <b>真正的答案是：这两个数<u>本来就不该放在一起比</u>。</b></p>
<ul>
  <li><b>batch 完全不是一个量级。</b><em>GPT-3 175B 是 3.2M token，
    V3 是 12.6M 起、爬到 62.9M ——&nbsp;<b>差二十倍</b>。
    而这一节反复说：<b>学习率和 batch 是绑着动的。</b></em></li>
  <li><b>形状不一样。</b><em>GPT-3 走余弦、早早就开始降；
    V3 是 WSD，<b>峰值那个数要一路恒定扛过 10T token</b>。
    ——&nbsp;<b>「峰值」在两条曲线上不是同一个身份的东西。</b></em></li>
  <li><b>连参数都不是同一批。</b><em>MoE 里绝大多数参数每一步只被一小部分
    token 碰到。<b>它挨的更新次数和稠密模型不是一个数量级。</b></em></li>
</ul>
<p><span class="sub">⭐⭐ 所以这一格的教训，跟 2.5 那张图是同一条：
  <em>GPT-3 那张表的价值，在于它内部八个规模是<b>同一套配方、只有一个旋钮在动</b>
  ——&nbsp;而这既是它能当先验的原因，<b>也是它出了那张表就不能当先验的原因</b>。<br>
  ⛔ <b>判据：一条趋势只在「产生它的那次受控实验」内部可用。</b></em></span></p>
<p><span class="sub">⛔ 顺带自曝：我原来那句「看激活规模不看总参数」听起来很顺，
  <em>而它正是<b>最危险的那一类断言 ——&nbsp;听着像常识，还刚好能圆上场</b>。
  它错在<b>拿一个没验过的换算去救一个跨实验的比较</b>，
  而跨实验的比较本身就不成立。</em></span></p></div>

<h4>第三段 · 怎么降 ——&nbsp;两个流派，终点却撞在同一个数上</h4>

<div class="note"><p>⭐ 老派 · cosine（GPT-3 / Llama / Mistral 一系）</p>
<p><em>GPT-3 的做法：在 2600 亿 token 内按余弦曲线降到峰值的 10%，
  之后<b>就一直保持在这个十分之一不动</b>。</em></p></div>

<div class="note"><p>⭐ 新派 · warmup-stable-decay（DeepSeek-V3 就是这个形状）</p>
<p><em>V3 的完整曲线：<b>2000 步</b>线性升到 2.2×10⁻⁴ →&nbsp;
  <b>一直恒定</b>，跑到 <b>10T token</b> 才开始降 →&nbsp;
  用 <b>4.3T token</b> 余弦降到 2.2×10⁻⁵ →&nbsp;
  最后 500B token 里，前 333B 恒定 2.2×10⁻⁵，后 167B 换成 7.3×10⁻⁶。</em></p>
<p><span class="sub">📌 arXiv <b>2412.19437</b> §4.2；梯度裁剪范数 1.0，
  batch 从 3072 爬到 15360（头 469B token）后保持。
  这个形状的系统分析见 arXiv <b>2410.05192</b>。</span></p></div>

<p class="landing">⭐⭐⭐ 两派的差别<b>不在「哪个收敛更好」</b>，
  而在<u>它们各自要求你什么时候做决定</u> ——&nbsp;<em>图 Ⓑ 那两栏讲的就是这件事。</em></p>

<p class="landing">⭐⭐ 而两派的<b>终点</b>撞在同一个数上 ——&nbsp;
  <em>图里那条横贯全场的虚线。</em></p>

<h4>⭐ 落点：怎么调，三条就够</h4>

<div class="note ok"><ul>
  <li>① 别从零猜。<em>找一个<b>规模和架构都接近</b>的公开配置抄过来当起点
    ——&nbsp;上面那张表和 V3 的报告都是公开的，<b>这是免费的先验</b>。</em></li>
  <li>② 跨规模不能直接照抄。<em>回到 <a href="#s3-3b">3.3b</a> 那条：
    <b>学习率带量纲</b>。小规模上扫出来的值，要么用可迁移的参数化，
    <b>要么在目标规模上小步重验</b>。</em></li>
  <li>③ 判据看两条曲线。<em>loss 炸了、或者 grad norm 出突刺 ——&nbsp;<b>太大</b>；
    loss 下得慢、grad norm 一直贴着地板 ——&nbsp;<b>太小</b>。
    <br>⭐ <b>「慢」有绝对刻度可参照</b>（<a href="#s1-0">1.0</a> 那把尺）：
    <u>从 11.8 一路往下掉是正常开局</u>；
    <b>掉到 2 附近之后，每 0.01 都很贵</b>。</em></li>
</ul>
<p class="landing">⛔ 梯度裁剪是安全带，不是调参手段。
  <em>V3 设的是 <b>1.0</b>。靠调裁剪阈值来压住一个太大的学习率，
  是在掩盖问题不是在解决问题。</em></p></div>

<div class="note danger"><p>⛔⛔ 最后一条，也是最容易被忽略的：
  单报一个学习率数字，是没有意义的。</p>
<p><em>它必须连着 batch、schedule 形状、总 token 数 一起报
  ——&nbsp;换掉其中任何一个，这个数就不再适用。
  ⭐ 上面那两套配置之所以能抄，正是因为它们<b>四样都写全了</b>。</em></p></div>

<h3>3.5b　⭐⭐⭐ 那 batch 本身该开多大 ——&nbsp;<u>为什么大了稳，大过头又不行</u></h3>

<!-- ⭐⭐⭐ 2026-09-19 T07 新开。现场原话（语音）：
     「还有 sequence loss 跟**训练稳定性**的关系，就是那个 TPS，那个 **global batch size
       为什么大了就不行**。…… **为什么 batch 开大了训练就会稳定**？等等，这里的理论也都讲讲。」
     ⭐⭐ 他这段话里有一处**表面矛盾**（大了不行 / 大了才稳），而那正是这一节的故事线：
       **稳定性单调受益，样本效率有拐点。** 两句都对，讲的是同一条曲线的两段。
     ⛔ 原话里「TPS」这个缩写语音转写不清，三种读法（tokens per step / TP size /
       tokens per sequence）都跟他那句「TP 开大 TPS 就会大」对不上 ——
       **所以没有替他猜**，改成把四个旋钮各自怎么决定 global batch 讲清楚。
       「GPS」按 GBS ＝ global batch size 处理（同一句前文他刚说过 global batch size）。
     ⛔ 判据：**听不清的缩写不要替人补全** ——&#160;补错了，读者学到的是一条不存在的关系。 -->

<p class="lead">上一小节最后那句是：<b>单报一个学习率没有意义，它必须连着 batch 一起报。</b>
  <em>——&nbsp;那 batch 自己呢？<b>它该怎么定，凭什么。</b></em></p>

<h4>第一步 · ⭐ 先把「batch」这个词拆开 ——&nbsp;它是<u>四个旋钮乘出来的</u></h4>

<p>很多混乱来自「batch」被当成一个数在说。<em>真跑起来，你手上是四个独立的旋钮：</em></p>

<table>
<thead><tr><th>旋钮</th><th>它是什么</th><th>拧大了，谁跟着变</th></tr></thead>
<tbody>
<tr><td><b>micro-batch</b><br><em>m</em></td>
    <td>一张卡一次前向真正塞进去几条序列</td>
    <td>⛔ <b>激活</b>直接跟着涨（<a href="#s1-7">1.7</a> 那张表整体 ×m）</td></tr>
<tr><td><b>梯度累积</b><br><em>K</em></td>
    <td>攒几次再更新一次（<a href="#s一">1.8</a> 第 ④ 道）</td>
    <td>⭐ <b>激活不变</b> ——&nbsp;同时在场的永远只有一份</td></tr>
<tr><td><b>DP 路数</b><br><em>d</em></td>
    <td>多少份模型副本各吃各的数据</td>
    <td>激活按卡摊薄，但<b>常驻那 16 字节每份都要一套</b></td></tr>
<tr><td><b>序列长度</b><br><em>S</em></td>
    <td>本讲基准 4,096</td>
    <td>激活 ∝ token 总数（<a href="#s5-1">5.1</a>）</td></tr>
</tbody></table>

<p class="landing">⭐⭐ <b>global batch（一次更新看了多少 token）　＝　m × K × d × S</b></p>

<div class="note danger"><p>⛔⛔ <b>而这里藏着一条最容易被忽略的连线：<u>TP 开大，global batch 会变小。</u></b></p>
<p><em>卡的总数是定的，<b>凡是切模型的那几维都在跟 DP 抢卡</b>：</em></p>
<p class="landing"><b>d　＝　总卡数 ÷ (TP × PP × CP)</b></p>
<p><em>所以你把 TP 从 8 开到 16，<b>DP 路数当场减半</b> ——&nbsp;
  别的都不动的话，<b>global batch 也跟着减半</b>。</em></p>
<p><span class="sub">⛔ <b>这条式子里<u>不该有 EP</u>，这是我改过一次的地方。</b>
  <em>Megatron-Core 里其实有<b>两个</b> data-parallel 尺寸：注意力那张网格的
  <code>data_parallel_size = world_size ÷ (TP × PP × CP)</code>，
  和专家那张网格自己的
  <code>expert_data_parallel_size = world_size ÷ (ETP × EP × PP)</code>。
  <b>EP 只切后者</b>；global batch 看的是前者 ——&nbsp;
  MoE 层只是把同一批 token 在专家维上重新分了一次，没有让这一步看的 token 变少。
  （出处：<code>megatron/core/parallel_state.py</code> 里
  <code>model_size = TP × PP × CP × gtp_remat</code> 那一段。）
  ⭐ 顺带补上原来漏掉的 <b>CP</b> ——&nbsp;上下文并行确实在这个分母里。</em></span></p>
<p class="landing">⭐⭐⭐ <b>而 batch 一变，学习率就得跟着变（下面第三步）。</b>
  <em>——&nbsp;也就是说：<u>并行策略那个旋钮的另一头，连着的是学习率</u>。</em>
  <em>它看起来是「怎么把模型切到卡上」的工程问题，<b>结果落在了收敛上。</b></em></p>
<p><span class="sub">⭐ 这也是为什么换一套并行配置之后，
  「代码没改、数据没改，loss 曲线却不一样了」——&nbsp;<b>因为 batch 悄悄变了。</b></span></p></div>

<h4>第二步 · ⭐⭐⭐ 为什么 batch 开大了会<u>稳</u></h4>

<p>回到 <a href="#s1-0">1.0</a> 那件事：<b>一个 batch 的 loss 是所有 token 的 loss 取平均。</b>
  <em>梯度也一样 ——&nbsp;它是<b>一批样本各自意见的平均</b>。</em></p>

<div class="note ok"><p>⭐ <b>那张「一屋子人取平均」的图（<a href="#s1-2">1.2</a>），
  现在可以再用一次。</b></p>
<p><em>屋里坐 8 个人，混进一个特别偏激的，平均值会被他拽走一大截。<br>
  屋里坐 800 个人，同一个人的影响<b>被稀释到几乎看不见</b>。</em></p>
<p class="landing">⭐⭐ <b>梯度的「抖动」大致按 <u>1/√B</u> 缩小</b>
  ——&nbsp;<em>batch 翻 4 倍，噪声减半。</em></p>
<p><span class="sub">📐 这是样本平均的标准结论（独立样本的均值，标准误 ∝ 1/√n）。
  ⚠️ <b>前提是样本之间大致独立</b> ——&nbsp;同一条序列里相邻的 token 显然不独立，
  所以真实的有效样本数比 token 数少。<b>当量级看，别当精确公式。</b></span></p></div>

<p class="landing">⭐ <b>所以「大 batch 更稳」不是玄学，就是平均的老道理：</b>
  <em>参与平均的样本越多，<u>单个坏样本越带不动它</u>。</em></p>

<p><em>⛔ 反过来说，<b>小 batch 的每一步都在被噪声推着走</b>。
  loss 曲线会毛、grad norm 会跳 ——&nbsp;而
  <a href="#s六">6</a> 那节讲的 spike，很多就是在这种时候被一小撮坏样本点着的。</em></p>

<h4>第三步 · ⭐⭐ 那为什么<u>不能一直往大开</u></h4>

<p>既然大了更稳，为什么不把 batch 开到天上去？<em>——&nbsp;<b>两个各自独立的理由。</b></em></p>

<div class="note"><p><b>理由一 · 噪声降下去了，但「每个样本的收益」也降下去了。</b></p>
<p><em>batch 小的时候，梯度很糙，<b>加样本能实实在在让方向变准</b> ——&nbsp;
  这时候加倍 batch，大致能少走一半的步数。</em></p>
<p><em>可一旦梯度已经够准了，<b>噪声不再是瓶颈</b>。这时候再加样本，
  方向也不会更对多少 ——&nbsp;<b>步数几乎不减，算力却实打实翻倍。</b></em></p>
<p class="landing">⭐⭐⭐ <b>中间那个转折点，通常叫「临界 batch」。</b>
  <em>——&nbsp;过了它，<u>你花的是算力，买不到等量的进步</u>。</em></p>
<div class="note ok"><p>⭐⭐ <b>这件事这一讲其实已经提过一次了 ——&nbsp;只是没展开。</b></p>
<p><em><a href="#s3-5">3.5</a> 引 GPT-3 那八档配置时有一句：
  <b>「他们用<u>梯度噪声尺度</u>来定 batch」</b>。</em></p>
<p class="landing">⭐ <b>那就是上面这段话的量化版本</b>：
  <em>先量一量「这一批梯度里有多少是噪声」，
  <u>噪声还占大头 ——&nbsp;加样本划算；噪声已经很小 ——&nbsp;加样本是浪费</u>。</em></p>
<p><span class="sub">⭐ 同一篇论文还写着「更大的模型通常能用更大的 batch，但需要更小的学习率」
  ——&nbsp;<b>这半句就是下面「理由二」</b>。两条其实一直摆在那张表旁边。</span></p></div>

<p><span class="sub">⛔ 它<b>不是一个固定的数</b>：模型越大、训练越往后，它越大。
  所以真实配置里 batch 是<b>一路往上爬</b>的 ——&nbsp;
  V3 就是从 3,072 条爬到 15,360 条（上一小节那张表）。
  <b>爬 batch 不是为了更快，是因为临界点在往上挪。</b></span></p></div>

<div class="note"><p><b>理由二 · batch 一大，学习率就得跟着抬 ——&nbsp;而抬学习率是有上限的。</b></p>
<p><em>batch 翻倍，梯度噪声小了，<b>你可以也应该迈更大的步</b>
  ——&nbsp;不然等于白拿了准度却不用。<a href="#s3-5">3.5</a> 那八档 GPT-3 配置
  就是这条关系的实物：<b>模型越大、batch 越大，学习率反而在降</b>
  ——&nbsp;那是因为规模在同时往下压它，两股力量叠在一起。</em></p>
<p class="landing">⛔ <b>但学习率不能无限抬</b>：抬过头，
  <em>loss 直接飞（<a href="#s六">6</a>）。所以 batch 增大能换来的加速，
  <u>被「学习率能抬到多高」这条硬边界卡住了</u>。</em></p></div>

<div class="note ok"><p>⭐⭐⭐ <b>所以那两句听起来矛盾的话，其实是同一条曲线的两段。</b></p>
<table>
<thead><tr><th></th><th>batch 往大开</th></tr></thead>
<tbody>
<tr><td><b>稳定性</b></td>
    <td>✅ <b>单调受益</b> ——&nbsp;噪声 ∝ 1/√B，越大越稳，没有拐点</td></tr>
<tr><td><b>样本效率</b></td>
    <td>⛔ <b>有拐点</b> ——&nbsp;过了临界 batch，多花的算力换不回等量的进步</td></tr>
</tbody></table>
<p class="landing">⭐ <b>「大了就不行」说的是第二行，「大了才稳」说的是第一行。</b>
  <em>——&nbsp;两句都对。<u>选 batch 就是在这两条之间找那个刚好够稳、又还没浪费的位置。</u></em></p></div>

<h4>第四步 · ⛔ 两个会<u>静默</u>咬人的实现细节</h4>

<div class="note danger"><p>⛔⛔ <b>① 梯度累积忘了除以 K ——&nbsp;等于把学习率乘了 K。</b></p>
<p><em><a href="#s一">1.8</a> 说过「累积改的是时间线，不是总量」。
  <b>那句话成立的前提是你真的取了平均。</b></em></p>
<p><em>K 个 micro-batch 的梯度加起来之后，<u>必须除以 K</u>
  ——&nbsp;忘了除，梯度整整大 K 倍，<b>而这跟把学习率乘 K 是一回事</b>。</em></p>
<p class="landing">⭐ <b>症状很有迷惑性</b>：<em>代码跑得通、不报错、loss 一开始还在降，
  然后突然飞掉 ——&nbsp;而你会去怀疑数据、怀疑初始化。
  <u>它其实是个学习率问题，只是伪装成了别的样子。</u></em></p></div>

<div class="note danger"><p>⛔ <b>② 「按 token 平均」还是「按序列平均」，在变长 batch 上不是同一件事。</b></p>
<p><em>一批里有的序列 4,096 个 token、有的只有 500 个（padding 掉的不算）。</em></p>
<ul>
  <li><b>按 token 平均</b>：<em>所有有效 token 一视同仁 ——&nbsp;
    <b>长序列因为 token 多，在这一步里说话更响。</b></em></li>
  <li><b>按序列平均</b>：<em>每条序列先各自平均，再对序列取平均 ——&nbsp;
    <b>长短序列话语权相同，于是短序列里每个 token 的权重更高。</b></em></li>
</ul>
<p class="landing">⛔ <b>两种算法给出<u>不同的梯度</u></b>，
  <em>而它们的代码差别只有一行。⭐ 预训练几乎都用按 token 平均
  ——&nbsp;但重点不是选哪个，是<b>知道自己选的是哪个</b>。</em></p></div>

<div class="note"><p>⚠️ <b>一个听不清的缩写，我没有替他补全。</b></p>
<p><em>现场提到一个念作「TPS」的量，说「TP 开得太大的时候，TPS 就会很大」。
  ⛔ 但按上面第一步那个式子，<b>TP 开大会让 DP 变少、global batch 变小</b>
  ——&nbsp;三种可能的读法（tokens per step / TP size / tokens per sequence）
  <u>都跟这句话对不上</u>。</em></p>
<p class="landing">⭐ <b>所以这里写的是「四个旋钮各自怎么决定 global batch」这个骨架</b>
  ——&nbsp;<em>等指认了 TPS 是哪一个，再把那条具体关系补上。</em>
  <span class="sub">（判据：<b>听不清的缩写不要替人补全</b> ——&nbsp;
  补错了，读者学到的是一条不存在的关系。）</span></p></div>

<p class="landing">⭐⭐ <b>这一节的落点：batch 不是「能塞多大塞多大」。</b>
  <em>它是四个旋钮的乘积，它决定学习率，它被并行策略牵着，
  而它的上限来自<u>样本效率</u>而不是显存。</em>
  <em>——&nbsp;<b>显存只决定你能不能塞下，塞不塞得对是另一回事。</b></em></p>

<h3>3.6　⭐⭐ 另外那两个超参：β₁ 和 β₂ 到底在干什么</h3>

<!-- ⭐⭐⭐ 2026-09-16 补。核 PaLM 的超参段时撞到一条特别好的东西，
     而且它**顺手接上了 3.2 和 3.5 两处**：
       · β₂ 决定「老的贡献能活多久」—— 正是 3.2 那条判据的量化版
       · PaLM 的 parameter scaling 把 GPT-3 手调的那张表**自动化了** —— 接 3.5
     ⛔ 判据：**核一条超参的原文，常常会捡到两条别处的接口。**
       只查数字就会把这两条都错过。 -->

<p class="lead">学习率讲完了，<b>可 Adam 还有两个数没讲</b>：<em>β₁ 和 β₂。</em>
  <em>⭐ 它们通常被当成「用默认值就行」的东西
  ——&nbsp;但它们的含义其实特别清楚。</em></p>

<div class="note ok"><p>⭐⭐ 一句话：β 决定「老的贡献能活多久」。</p>
<p><em>滑动平均每步乘一个 β 再掺新值，所以<b>有效窗口大约是 <code>1/(1−β)</code> 步</b>。</em></p>
<ul>
  <li><b>β₁ ＝ 0.9</b> →&nbsp;<em>动量的窗口约 <b>10 步</b>。它只负责把方向抖动抹平。</em></li>
  <li><b>β₂ ＝ 0.95</b> →&nbsp;<em>约 <b>20 步</b>；<b>β₂ ＝ 0.99</b> →&nbsp;约 <b>100 步</b>。</em></li>
</ul>
<p class="landing">⭐ 所以 β₂ 调大 ＝ 分母更平稳但反应更慢；调小 ＝ 跟得紧但噪声大。
  <em>——&nbsp;这不是玄学，<b>就是一个「记多久」的旋钮</b>。</em></p></div>

<h4>⭐⭐⭐ 而 PaLM 在这里做了一件反常的事：<u>让 β₂ 随步数变</u></h4>

<p><em>它用的是 <code>β₂ ＝ 1 − k<sup>−0.8</sup></code>，<b>k 是当前步数</b>。</em></p>
<p><b>代几个数进去看看它在干嘛：</b></p>
<ul>
  <li><em>第 100 步：β₂ ≈ <b>0.975</b>，窗口约 40 步。</em></li>
  <li><em>第 1 万步：β₂ ≈ <b>0.9994</b>，窗口约 1,600 步。</em></li>
  <li><em>第 10 万步：β₂ ≈ <b>0.9999</b>，窗口约 1 万步。</em></li>
</ul>
<p class="landing">⭐⭐ 也就是说，<u>窗口是跟着训练一起长的</u>（大约按 <code>k<sup>0.8</sup></code>）。
  <em>而固定的 β₂ ＝ 0.99，意味着<b>从头到尾都只记 100 步</b>。</em></p>

<div class="note"><p>❓ 为什么要这么做？论文给的理由很具体，而且很好懂。</p>
<p><em>因为<b>罕见的 embedding token</b> ——&nbsp;
  一个生僻字可能几千步才出现一次。在一个 100 步的窗口里，
  它的二阶矩估计基本是垃圾：大部分时候是 0，偶尔来一个大值。</em></p>
<p class="landing">⭐ 而窗口跟着训练一起长，这个问题就自己消失了。
  <em>论文原话：「我们发现这比标准的 β₂ ＝ 0.99 更稳定」。</em></p>
<p><span class="sub">📌 arXiv <b>2204.02311</b> 的超参段。同段还有：
  <b>β₁ ＝ 0.9</b>、学习率前 1 万步固定 10⁻²、之后按 <code>1/√k</code> 衰减。</span></p></div>

<div class="note danger"><p>⚠️ 这里有个顺带推出来的结论，而它跟 3.2 那条判据打架 ——&nbsp;
  值得说清楚。</p>
<p><em>3.2 说：动量和二阶矩是滑动平均，老的贡献会被衰减掉，所以 bf16 扛得住。<br>
  可如果 β₂ 一路涨到 0.9999，<b>那个「衰减」就慢得快要不衰减了</b>
  ——&nbsp;v 越来越像一个真的累加器。</em></p>
<p class="landing">⭐⭐ 所以那条判据要带上前提：它成立的条件是「β 不随步数趋近 1」。
  <em>⛔ 这一条是<b>我们自己推的</b>，不是论文说的
  ——&nbsp;PaLM 用的是 Adafactor 系、也没报精度配置，
  能不能真观察到这个影响，没有证据。当猜想看。</em></p></div>

<h4>⭐ 还捡到一条：PaLM 把 GPT-3 手调的那张表<u>自动化了</u></h4>
<p><em>PaLM 用的其实是 Adafactor，但关掉了分解
  ——&nbsp;论文说这<b>等价于「带参数缩放的 Adam」</b>：
  按参数矩阵的均方根去缩放学习率。</em></p>
<p class="landing">⭐⭐⭐ 而论文自己点破了这跟 3.5 那张表的关系：
  <em>「这么做的效果，类似于 GPT-3 那样手工把学习率随规模调小」。</em></p>
<p><span class="sub">⭐ 换句话说：3.5 那八档配置是<u>手动</u>解法，参数缩放是<u>自动</u>解法。
  <em>而且论文说自动那版还多一个好处 ——&nbsp;
  尺度本来就不同的那些参数（embedding、layer norm 的缩放）
  不会被按同一个比例一起压下去。</em></span></p>

<details class="foldfig"><summary><b>checkpoint 要存什么、为什么正好是那 12 字节</b>　<span class="why">⭐ 存储的事，不是这三件事</span></summary>
<h3>3.7　⭐ checkpoint 要存的，正好就是那 12 字节</h3>

<p class="lead">⭐ 这一节能用 3.1 那张账单直接推出来，
  不用查任何文档。</p>

<p>那 16 字节里，<b>哪些必须进 checkpoint？</b><em>一格一格问「丢了能不能重建」：</em></p>
<ul>
  <li><b>bf16 权重（2 B）</b> ——&nbsp;<em>⛔ <b>不用存</b>：它是 fp32 主权重的投影，<b>转一下就有</b>。</em></li>
  <li><b>bf16 梯度（2 B）</b> ——&nbsp;<em>⛔ <b>不用存</b>：下一步重新算一份。</em></li>
  <li><b>fp32 主权重（4 B）</b> ——&nbsp;<em>⭐ <b>必须存</b>，而且<b>不能只存 bf16 版</b>
    ——&nbsp;存成 bf16 再读回来，等于把训练到现在累出来的低位全扔了。</em></li>
  <li><b>动量 m、二阶矩 v（各 4 B）</b> ——&nbsp;<em>⭐ <b>必须存</b>：
    它们是<b>历史</b>，丢了就只能从零重新积累。</em></li>
</ul>

<div class="note ok"><p>⭐⭐⭐ 所以 checkpoint 的大小 ＝ 那 12 字节 × 参数量。
  <em>对 671B：约 <b>7.3 TiB</b> 一份。</em></p>
<p><span class="sub">⚠️ <b>但别把这个数安到 V3 头上</b> ——&nbsp;
  <em>3.2 刚说过它的一二阶矩是 bf16，那就是 <b>4＋2＋2 ＝ 8 字节</b>，
  一份 <b>4.9 TiB</b>，比经典口径小三分之一。
  <b>——&nbsp;省状态的那些做法，省的同时也在省 checkpoint。</b></em></span></p>
<p>⭐ <em>这一下解释了两件工程上的事：</em></p>
<ul>
  <li>为什么 checkpoint 这么大、存这么慢。
    <em>——&nbsp;它存的不是模型，是优化器。模型本身只占其中的三分之一。</em></li>
  <li>为什么「发布的模型权重」比 checkpoint 小得多。
    <em>——&nbsp;发布只要那 2 字节的推理权重，而 checkpoint 里那 <b>12 字节全是训练用的 ——&nbsp;一比六</b>。</em></li>
</ul></div>

<div class="note danger"><p>⛔⛔ 但只存这 12 字节还不够 ——&nbsp;还有三样，漏一样都会让「恢复」变成「重来」。</p>
<ul>
  <li><b>① 步数</b> ——&nbsp;<em>因为学习率、β₂、weight decay <b>都可能是步数的函数</b>（见 3.5、3.6）。</em></li>
  <li><b>② 数据读到哪儿了</b> ——&nbsp;<em>⭐ 这一条最容易漏，而且漏了最隐蔽：
    恢复之后又把同一批数据重训一遍，<b>loss 曲线看着完全正常</b>。</em></li>
  <li><b>③ 随机数状态</b> ——&nbsp;<em>dropout、数据打散都要用。</em></li>
</ul>
<p class="landing">⭐⭐ 而 <a href="#s6-2">§6.2</a> 那个「回滚 ＋ 跳数据」的救火办法，前提正是 ①②。
  <em>——&nbsp;数据位置说不清，你连「跳掉哪几批」都没法讲。</em></p></div>

<p><span class="sub">⭐ <b>顺带一个真实的省法</b>：优化器那部分<b>不一定要留在加速器上</b>。
  <em>DeepSeek-V3 就把权重的<b>指数滑动平均放在 CPU 内存里</b>、每步异步更新
  ——&nbsp;<b>既不占显存也不占训练时间</b>，用途是提前估计「学习率衰减之后会有多好」。
  📌 arXiv <b>2412.19437</b> §3.2.3。</em></span></p>

</details>
<h3>3.8　⭐⭐⭐ LoRA：<u>大多数人真正会碰到的那种训练</u></h3>

<!-- ⭐⭐⭐ 2026-09-17 补。麻瓜审最狠的一条：
     **LoRA / 微调在这一讲里零覆盖，而那恰恰是绝大多数人唯一会亲手跑的训练。**
     ⛔ 我原来的理由是「范围收紧了」。⭐ 但回头对一遍现场定的那条界
       ——「推理里没有的那些东西」—— **LoRA 完完全全在界内**：
       它有反向、有优化器状态、有激活，三本账一本不少。
       所以它不在界外，它只是被我漏了。
     ⭐⭐ 而它放在这里（§3 末尾、账单讲完之后）刚刚好：
       **它不需要新概念，它就是同一张 16 字节的账换一个算法。**
       ⛔ 判据：**一个话题该不该讲，看它能不能用已讲的东西讲完。**
         能，就该讲，而且会很短；不能，才是真的出界。 -->

<p class="lead">⭐ 前面整讲算的都是<b>从零开始训</b>。
  <em>——&nbsp;可绝大多数人这辈子跑的训练，是<b>拿别人训好的模型接着调</b>。</em></p>

<p>⭐⭐ <b>好消息是：这件事不需要任何新概念</b>，
  <em>它就是上面那张 16 字节的账，<b>换一个算法重算一遍</b>。</em></p>

<div class="note"><p>📌 <b>LoRA 一句话：底座冻住不动，只在旁边挂一对很瘦的小矩阵。</b></p>
<p><em>原来那个 4096 × 4096 的大矩阵不训了；
  旁边加一个 <code>4096 × 16</code> 和一个 <code>16 × 4096</code>，<b>只训这两个</b>。</em></p>
<p class="landing">⭐ <b>关键在那个 16</b>：<em>两个瘦矩阵加起来
  <code>2 × 4096 × 16</code>，只有原矩阵的 <b>0.8%</b>。</em></p></div>

<h4>那三本账各自怎么变</h4>

<table>
<thead><tr><th>账</th><th>全量微调</th><th>LoRA</th><th>省了吗</th></tr></thead>
<tbody>
<tr><td><b>常驻</b><br><span class="sub">权重 ＋ 梯度 ＋ 优化器状态</span></td>
    <td>16 B × <b>全部</b>参数</td>
    <td><b>2 B × 全部</b>（冻住，只读）<br>＋ 16 B × <b>那 0.1%</b></td>
    <td>⭐⭐⭐ <b>省得最狠</b></td></tr>
<tr><td><b>算力</b></td><td>3×（前向 1 ＋ 反向 2）</td>
    <td><b>约 2×</b></td><td>⭐ 省了约三分之一</td></tr>
<tr><td><b>激活</b></td><td>整条链都要在场</td>
    <td><b>整条链还是都要在场</b></td><td>⛔ <b>基本不省</b></td></tr>
</tbody></table>

<div class="note ok"><p>⭐⭐⭐ <b>第一行拿 70 亿参数代进去，数字很夸张。</b>
  <span class="sub">（形状按 Llama-2-7B 一档：<b>32 层、隐藏维 4,096</b>。
  下面那个 839 万是这么来的：<code>32 层 × 2 处（Q、V）× 2 块（A、B）× 4,096 × 16 ＝ 8,388,608</code>
  ——&nbsp;⭐ 原来只给了结果，照着算的人凑不出来，因为「32 层」一个字都没写。）</span></p>
<ul>
  <li><b>全量微调</b>：<em>6.74e9 × 16 B ＝ <b>100.4 GiB</b>。</em></li>
  <li><b>LoRA</b>（每层给 Q 和 V 各挂一对，秩 16）：
    <em>冻结底座 <b>12.55 GiB</b> ＋ adapter <b>0.125 GiB</b> ＝ <b>12.68 GiB</b>。</em></li>
</ul>
<p class="landing">⭐⭐ <b>相差 7.9 倍</b> ——&nbsp;
  <em>而 adapter 一共只有 <b>839 万</b>个参数，占全模型 <b>0.124%</b>。</em></p>
<p><span class="sub">⭐ <b>为什么差这么多</b>：<em>那 16 字节里有 14 字节
  （梯度 ＋ 主权重 ＋ m ＋ v）是<b>只有「要被更新的参数」才需要的</b>。
  冻住的参数<b>只留 2 字节权重</b> ——&nbsp;它退回成了推理的样子。</em></span></p></div>

<div class="note"><p>⭐⭐ <b>第二行那个「3× 变 2×」值得单独说，因为它直接来自 1.3。</b></p>
<p><em><a href="#s一">1.3</a> 说过反向要付<b>两笔</b>乘法，这里沿用它的编号：
  ① 算<b>权重</b>的梯度（给优化器用），② 算<b>输入</b>的梯度（往下一层传）。</em></p>
<p class="landing">⭐⭐⭐ <b>而冻住的那些层，<u>第 ① 笔可以整个不算</u></b> ——&nbsp;
  <em>它的权重根本不更新，算出梯度也没人用。</em></p>
<p><em>于是：前向 1 ＋ 只剩 ② 那一笔 1 ＝ <b>约 2×</b>，而不是 3×。</em></p>
<p><span class="sub">⛔ <b>反过来的情形别搞混</b>：只微调<u>最后一层</u>时，省掉的是 ②（不用再往前传）；而 LoRA 每层都挂 adapter，② <b>一笔都省不掉</b>，省的是 ①。</span></p>
<p><span class="sub">⭐ 这一条是<b>从 1.3 那两笔账直接推出来的</b>，不是新知识
  ——&nbsp;<em>这正是把 LoRA 放在这里讲的理由。</em></span></p></div>

<div class="note danger"><p>⛔⛔ <b>而第三行才是最容易被误解、也最值钱的一行。</b></p>
<p><em>很多人以为「只训 0.1% 的参数 ＝ 训练变得很轻」。<b>显存上不是。</b></em></p>
<p class="landing">⭐⭐⭐ 因为 adapter 挂在<b><u>每一层</u></b>上。
  <em>——&nbsp;最前面那层的 adapter 也要梯度，
  而梯度只能从 loss 一路传回去（<a href="#s一">1.5</a>）。
  <b>所以整条链的激活，该在场的还得在场。</b></em></p>
<p><em>⭐ 结论很具体：<b>LoRA 之后，激活那一块往往成了新的大头</b>
  ——&nbsp;<b>所以 LoRA 还是要开重算</b>（<a href="#s二">第二节</a>）。
  <u>两件事不是替代关系，是叠加关系。</u></em></p>
<p><span class="sub">⚠️ 严格说激活会<b>少一点</b>：冻结的线性层不再需要为「权重梯度」
  留住它的输入。<em>但非线性那些算子照样要，<b>所以只是少一点，不是少一个数量级。</b>
  ⛔ 这一句是<b>按 1.3 那两笔账推的</b>，我们没有实测。</em></span></p></div>

<p class="landing">⭐⭐ <b>一句话收口：LoRA 改的是「有多少参数带着那 16 字节」，
  <u>它没有改变「反向要穿过整个网络」这件事</u>。</b></p>
<p><span class="sub">⭐ <b>而这正好是这一讲的主轴在小尺度上的一次复现</b>：
  <em>参数那一块可以按比例砍，<b>而链条本身砍不动</b>
  ——&nbsp;因为它来自反向传播的数学，不来自你训多少参数。</em></span></p>

</div></section>


<section id="s四"><div class="wrap"><div class="stn"><span class="badge">第 四 节</span><h2>这张账交给专题五（本讲不展开）</h2></div>

<p class="lead">这一节是通向<b>专题五</b>的桥。
  <em>⭐ 把上面几项按大小排一遍，你会发现 ——&nbsp;
  ZeRO 那三级，正是照着这个顺序来的。</em></p>

<p class="lead">⛔ <b>这一节是<u>专题五的预习</u>，不是本讲那三件事的一部分。</b>
  <em>——&nbsp;三句话说完，细的都收在下面那个折叠块里。</em></p>

<div class="note ok"><p>⭐ <b>训练要用的东西，按大小排下来是这个顺序：</b></p>
<p class="landing"><b>优化器状态（12 字节／参数）　＞　权重 ＝ 梯度（各 2 字节）　＞　激活（看 batch）</b></p>
<ul>
  <li><em>卡装不下的时候，<b>就按这个顺序往外切</b> ——&nbsp;先切最大的、而且最少被用到的。</em></li>
  <li><em>这正好就是 <b>ZeRO 那三级</b>的顺序：先优化器状态，再梯度，最后权重。</em></li>
  <li><em>⛔ 但「怎么切、切给谁、切完通信涨多少」<b>整个是
    <a href="index.html">专题五</a> 的事</b>。</em></li>
</ul>
<p><span class="sub">⭐ 这里只要你带走一句：<b>并行策略不是凭空设计出来的，
  它是被这张账的大小顺序逼出来的。</b>
  ——&nbsp;至于账本身怎么算，本讲第二节那个折叠块和下面这个折叠块里都有。</span></p></div>

<details class="foldfig"><summary><b>这张账本身：四项各多大、ZeRO 三级怎么从它推出来、其它几招各在切哪一项</b>　<span class="why">⭐ 专题五的预习 —— 本讲不要求</span></summary>
<h3>4.1　先把账按大小排一遍</h3>

<p>把前面三节的东西摆在一起，<b>按占多少排个序</b> ——&nbsp;
  <em>这个顺序本身就是这一节的全部内容。</em></p>

__TBL_LEDGER__

<div class="note"><p>⭐ <b>前三行都是「每参数几字节 × 参数量」，所以它们的比例<u>永远是 12 : 2 : 2</u></b>
  ——&nbsp;<em>换多大的模型都不变，换多少张卡也不变。</em></p>
<p><em>优化器状态一项就占常驻的 <b>75%</b>。
  ⭐ 而它是三项里<b>唯一一个前向反向都用不上</b>的
  ——&nbsp;<a href="#s3-1">3.1</a> 说过，它只在<b>更新那一瞬间</b>被读一次、写一次。</em></p></div>

<div class="note danger"><p>⛔ <b>但第四行不一样 ——&nbsp;它是唯一一个<u>你自己能调</u>的。</b></p>
<p><em>前三项跟着参数量走，<b>你没得选</b>；激活跟着
  「这一步同时在场多少 token」走 ——&nbsp;而那是 micro-batch、梯度累积、
  DP 路数、序列长度<b>四个旋钮乘出来的</b>。</em></p>
<p class="landing">⭐⭐ <b>所以「谁最大」不是模型的属性，是你这次训练配置的属性。</b>
  <em>——&nbsp;batch 开到某个程度，第四行就会翻到第一行上面去。
  那个分水岭具体在哪，<a href="#s5-1">5.1</a> 会算给你看。</em></p></div>

<p class="landing">⭐⭐⭐ <b>记住这个顺序 ——&nbsp;下一小节讲的 ZeRO，
  三级分法就是照着它一级一级往下切的。</b></p>



<h3>4.2　ZeRO 的三级，正是照着这个顺序来的</h3>

<!-- ⭐⭐⭐ 2026-09-16 现场：「把那个 ZeRO 的论文 ——&#160;其实是训练的主线、
     很重要的一个论文 ——&#160;也放到我们讲课的内容列表里。」
     ⭐ 所以它不只是「素材表里的一条出处」，而是**正课内容**：
       §四整节就是在讲它，而且是<u>从账单推出来</u>，不是介绍一个工具。
     ⛔ 判据：**一个名词值不值得进正课，看它能不能被前面的内容推出来。**
       ZeRO 能 ——&#160;所以它不是「再介绍一个框架」，是这张账单的必然结论。 -->
<p class="lead">⭐ ZeRO 的分级不用背。
  <em>把上面那个顺序倒过来读一遍，你就把它推出来了。</em></p>

__FIG_ZERO__

<div class="note ok"><p>⭐⭐ 切的顺序只有一条原则：谁最大、谁最少被用到，就先切谁。</p>
<ul>
  <li>ZeRO-1 切优化器状态。<em>它<b>最大</b>，而且<b>只在更新时用一次</b>
    ——&nbsp;切了几乎<b>不增加通信</b>。先切它，性价比最高。</em></li>
  <li>ZeRO-2 再切梯度。<em>它跟权重一样大，但<b>反向结束前它可以是散着的</b>
    ——&nbsp;在那之前它可以是散着的。</em></li>
  <li>ZeRO-3（≈ FSDP）最后才切参数。<em>因为<b>每一层前向都要用它</b>
    ——&nbsp;一切开，每层都得先 all-gather 把它拼回来。
    通信代价是三级里最高的。</em></li>
</ul>
<p class="landing">⭐⭐⭐ 所以顺序不是随意的，也不是历史巧合 ——&nbsp;
  它是「大小」和「被用到的频率」这两个量排出来的。</p>
<p><span class="sub">📌 出处：Rajbhandari 等，ZeRO: Memory Optimizations
  Toward Training Trillion Parameter Models，<b>arXiv 1910.02054</b>。
  <em>⭐ 上一节那个「每参数 16 字节」也出自它 ——&nbsp;
  同一篇论文既给了账单，也给了切法。</em></span></p></div>

<h3>4.3　同一张账单，其它几招各在切哪一项</h3>

<p>把 ZeRO 之外那几个常听见的名词摆回这张账单上，<b>它们各自的位置立刻就清楚了</b>：</p>
<ul>
  <li><b>重算</b>（<a href="#s二">第二节</a>那个决策）——&nbsp;<em>针对<b>激活</b>那一项。</em></li>
  <li><b>offload</b> ——&nbsp;<em>针对<b>「暂时用不到」</b>的那些项
    ——&nbsp;注意它切的不是「哪一类」，是<b>「哪一段时间」</b>。</em></li>
  <li><b>CP（上下文并行）</b>——&nbsp;<em>针对激活里<b>随序列长度涨</b>的那一部分
    ——&nbsp;也就是长上下文下反超优化器状态的那一块。</em></li>
</ul>
<p class="landing">⭐⭐ 所以这几个名词不是「几种优化技巧」，
  是<u>同一张账单上的四个不同栏目各自的对策</u>。
  <em>——&nbsp;而<a href="#s五">专题五</a>整讲要做的，就是把「切」这件事本身摊开算。</em></p>

</details>
</div></section>


<section id="s五"><div class="wrap"><div class="stn"><span class="badge">第 五 节</span><h2>把这三件事，放到一台你摸得到的机器上</h2></div>

<p class="lead">前面讲的都对，<b>但全是 6,710 亿参数、TiB 级的数</b>。
  <em>——&nbsp;它们没有分母。这一节把同一件事缩到<b>一个你能自己算一遍的模型</b>上。</em></p>

<p class="lead"><b>⛔ 这一节是<u>专题五的预习</u>，不是本讲那三件事的一部分</b>
  ——&nbsp;<em>顺手给个量级就走，<b>跳过去不影响前面任何一节</b>。</em><br>
  把前向（专题一讲过）＋ 反向 ＋ 更新合成一张表。
  <em>⭐ 而其中最有用的一问不是「总共多少」，是
  <b>「峰值出现在哪一个时刻」</b>。</em></p>

__FIG_STEP__

<!-- ⭐⭐⭐ 2026-09-18 第二段动画。扫完 25 张图之后它排第一，理由很具体：
     **「峰值出现在哪一刻」本身就是一个过程。** 上面那张静态图画得出山的形状，
     画不出「山是怎么被堆起来、又怎么塌下去的」。
     ⭐ 而 md 的「还没想清楚的」里一直挂着一条「要不要讲清楚峰值在哪一刻 ——
       这需要时间线图，可能比想象中难讲」。这段就是那条的答案。
     ⛔ 视频里原本一个字都没有（2026-09-18 房规松绑后已加解释文字）；图例放在下面的 figcaption 里 ——
       那儿的字**可以选中、可以被读屏**，放进视频里就两样都没了。 -->
<figure class="fbox fwide" id="anim-memtime">
<video src="media/topic04-memtime.mp4" autoplay loop muted playsinline
  style="width:100%;max-width:1000px;display:block;margin:0 auto;border-radius:10px"
  aria-label="一个完整训练步里显存的涨落，画面上带解说文字。灰色的基座标着「常驻：一个 step 从头到尾都不动」，以及「权重 2 加优化器状态 12 等于 14 字节每参数，8.54 TiB」。图例给出三块颜色：常驻是权重加优化器，激活是前向堆、反向放，梯度是反向才长出来。底部注明「横轴等于一个 step 的时间，下面 61 格等于 61 层，亮着的是此刻还挂着激活的层」。第一幕字幕「前向：激活一层层攒起来」，副行「每过一层就多挂一份中间结果 —— 一份都不能提前扔，反向还要用它」，蓝色的激活从左往右堆高。前向结束时打出红色标注「峰值就在这一刻 —— 前向刚结束」，并给出「八点五四 加 四点一五 等于 十二点六九 TiB」，同时拉出一条贯穿全宽的峰值虚线。第二幕字幕「反向：激活逐层放掉，梯度一份份长出来」，副行「红的在长，可它比蓝的小得多 —— 所以总量从峰值那一刻起只降不升」。第三幕字幕「更新完：梯度也清空，只剩常驻那一块」，副行「回到 8.54 TiB —— 下一个 step 从这条灰带上重新堆」。第四幕字幕「下一个 step，从头再来一遍」，副行「训练就是把这座山堆起来、再拆掉 —— 重复几十万次」，画面随后倒放回起点。"></video>
<figcaption>⭐⭐⭐ <b>同一笔账，让它跑一遍。</b>
  <em>上面那张图画的是这座山的<b>形状</b>；这段画的是它<b>怎么被堆起来、又怎么塌下去</b>。</em><br>
  <span class="sub">
  <b style="color:#9aa0a6">■</b> 常驻（权重 ＋ 优化器状态，14 B/参数）——&nbsp;<b>始终不动</b>　｜　
  <b style="color:#4285f4">■</b> 激活 ——&nbsp;<b>前向堆高，反向释放</b>　｜　
  <b style="color:#d93025">■</b> 梯度（2 B/参数）——&nbsp;<b>反向才出现，反向结束时最全</b>　｜　
  最下面那排小格 ＝ 61 层，<b>走到哪一层就点到哪一格</b></span><br>
  <em>⭐⭐ 盯住那根竖线：<b>不开重算时，最高点出现在前向刚走完、反向还没开始的那一刻</b>
  （<u>开了重算就不是了</u> ——&nbsp;那时峰值挪到反向的第一个瞬间：61 份入口 ＋ 正在重算的那一层，见 fig-act-bill 的放大格）
  ——&nbsp;<b>不是训练的某个阶段，是一个瞬间。</b>
  <span class="sub">（三条带的高度按真实字节数算，跟上面那张图同一套口径。
  14.6 秒无声循环，Manim 渲染，脚本在 <code>tools/manim/</code>。）</span></em></figcaption>
</figure>

<details class="foldfig"><summary><b>完整的一步总账：四项加起来多少、6ND 为什么低估、峰值出现在哪一刻</b>　<span class="why">⭐ 全是「装不装得下」—— 专题五的预习，本讲不要求</span></summary>
<h3>5.1　显存：四项加起来是多少</h3>

<p class="lead">⭐ 前面四节各算各的，<b>现在把它们摆到一起</b>。
  <em>这一节全部按<b>全局总量</b>算 ——&nbsp;不分卡，怎么切是下一讲的事。</em></p>

<div class="note"><p>📌 常驻那一块的算式，短到可以口算：</p>
<p><em>671e9 参数 × 16 字节 ÷ 1024⁴ ＝ 9.76 TiB。
  拆开是：权重 1.22 ＋ 梯度 1.22 ＋ 优化器状态 <b>7.32</b>。</em></p>
<p>⭐ <em>换算一下就知道这是个什么量级 ——&nbsp;
  按每张卡 80 GiB 算，光这一块就要 125 张，还没算激活、没算任何冗余。</em>
  <span class="sub">⚠️ <b>下面那个激活峰值按「全量重算」算</b> ——&nbsp;
  而 <a href="#s二">2.4</a> 说过 V3 自己配的是<u>选择性</u>重算（RMSNorm ＋ MLA 上投影）。
  <b>两个口径不是一回事</b>：这里取全量是为了给一个干净的下界，真机上激活会比它高。</span>
  <span class="sub">（80 GiB 是这里<b>随手设的换算基准</b>，不是在说某款具体的卡。
  ⭐ <b>专题一按 v7 的 94.74 GiB／device 算同一笔账，得到的是<u>约 105 张</u></b>
  ——&nbsp;TiB 那个数两边一致，差的只是除数。）</span></p></div>

<p class="landing">⭐⭐ 注意这三项里，只有优化器状态那 7.32 TiB 是<u>纯开销</u>
  ——&nbsp;<em>它既不参与前向，也不参与反向，
  只在每一步的最后那一瞬间被用一次。</em></p>

<!-- ⭐⭐⭐ 2026-09-17 夜间续轮 R2。
     接力清单上原本有一条待办：「画常驻块 vs 激活随序列长度的两条线，交点在哪」。
     ⛔ 动手前先验了一下，**这张图不该画**：
       ① 激活账里**没有 S² 项**（1.7 那三条边界的第二条写着：
          注意力分数矩阵不落显存，FlashAttention 不写它）。
          所以激活是**随 token 总数线性**的 —— 两条线在约 737 万 token
          才相交，那不是任何人会遇到的配置。
       ② 而**有意义的那个交点，fig-step 的 Ⓑ 已经画了** —— 沿 batch 轴。
          再画一张沿 S 轴的，画的是同一个点。
     ⭐⭐ 但这次核查本身翻出了一件更值钱的事，就是下面这一格：
       **显存只认 token 总数，算力不只认 token 总数。** 写成正文。
     ⛔ 判据：**一条「该补图」的待办，动手前先验它要画的关系成不成立。**
       画出来的图看上去都像对的 —— 尤其是坐标轴画对了的那种。 -->

<div class="note ok"><p>⭐⭐⭐ <b>顺着这个数往下问一句，会撞上一件反直觉的事。</b></p>
<p><em>常驻那 9.76 TiB 是<b>定值</b> ——&nbsp;跟你喂多长、喂多少条都无关。
  那激活要涨到多大才追得上它？</em></p>
<p class="landing">⭐ <b>答案是 <u>约 __WS_TOK__ token</u></b>
  ——&nbsp;<em>按本讲的 4K 基准，就是 <b>约 __WS_SEQ__ 条序列</b>。</em>
  <span class="sub">（一条 4K 序列开了全量重算之后峰值 __PEAK__；换成扩训那一档，
  一条 128K 是 177.89 GiB，<b>约 56 条</b>，token 总数一模一样。）</span></p>

<div class="note danger"><p>⛔⛔ <b>停一下 ——&nbsp;这里有个数特别容易代错，我自己代错过一版。</b></p>
<p><em><a href="#s3-5">3.5</a> 说过，V3 的 global batch 从 <b>3,072 条 4K</b> 起步
  （＝ <b>1,258 万 token</b>），后期爬到 <b>15,360 条</b>。
  看上去远远越过了 __WS_TOK__ 那道分水岭。</em></p>
<p class="landing">⛔⛔⛔ <b>可 global batch 代不进来 ——&nbsp;分水岭比的是<u>同时在场</u>。</b></p>
<p><em><code>global batch ＝ DP 路数 × micro-batch × 梯度累积 K × 序列长度</code>，
  而那 <b>K 份从来不同时存在</b>（<a href="#s一">1.8</a> 第 ④ 道、
  <a href="#s3-5b">3.5b</a> 那张旋钮表都写着同一句：
  <u>梯度累积改的是时间线，不是总量</u>）。
  <b>显存里同时在场的只有前两项</b> ——&nbsp;上面那张图的 Ⓑ 用的正是这个口径。</em></p>
<p><em>给个稳的上界：V3 用 2,048 张卡、PP16 × EP64，
  就算把 DP 放到最宽松的 <code>2048 ÷ 16 ＝ 128</code> 路、micro-batch 取 1，
  同时在场也只有 <b>128 条 ＝ 52 万 token</b> ——&nbsp;
  <u>不到那道分水岭的十四分之一</u>。</em></p>
<p class="landing">⛔ <b>所以「激活从第一步就反超优化器状态」这个结论不成立 ——&nbsp;已撤回。</b>
  <em>在 V3 自己的配置上，<a href="#s3-1">3.1</a> 那句
  「最大的一块是优化器状态」<b>仍然成立</b>。</em></p>
<p class="sub">⛔ 这一处原来写的是「V3 从第一步起就越过分水岭 1.7 倍，训到后期 8.5 倍」，
  还顺势说本讲第一个答案被自己的例子推翻了 ——&nbsp;<b>那是把 global batch 当成了同时在场</b>。
  ⭐ 而<b>同一页的图一直是对的</b>：Ⓑ 上印着「同时在场 ＝ DP 路数 × micro-batch」。
  <b>判据：同一个量在图里和正文里各写一遍时，先确认两边用的是同一个定义</b> ——&nbsp;
  这一次图对文错，而图注和正文都能自圆其说，谁都不会报错。</p></div>

<p>⭐⭐ <b>还有个容易被跳过的前提：<u>那 __WS_TOK__ token 怎么凑出来的，显存不在乎。</u></b></p>
<ul>
  <li><b>约 1,800 条 4K</b>，还是 <b>约 56 条 128K</b> ——&nbsp;
    <em>对显存<b>是同一件事</b>。</em></li>
  <li><b>为什么</b>：<em>激活账里<b>没有随 S² 涨的项</b>
    ——&nbsp;注意力分数矩阵根本不落显存（<a href="#s1-7">1.7</a> 那三条边界的第二条）。
    剩下的每一项都是「每个 token 一份」，<b>所以它只认 token 总数。</b></em></li>
</ul>

<div class="note danger"><p>⛔ <b>但算力<u>不是</u>这样。</b></p>
<p><em>同样 __WS_TOK__ token，<b>拆成长序列</b>和<b>拆成短序列</b>，
  要算的浮点数<u>差很多</u> ——&nbsp;因为 attention 那部分随 <b>S²</b> 涨，
  而它<b>不在显存账上、却在算力账上</b>。</em></p>
<p class="landing">⭐⭐⭐ 所以序列长度这个旋钮，<b>在两本账上的行为不一样</b>：
  <em>显存那本<b>只看总量</b>，算力那本<b>还要看你怎么切</b>。
  ——&nbsp;下一节那个「6ND 严重低估」，讲的就是这件事。</em></p>
<p><span class="sub">⚠️ 前提说清楚：以上都建立在<b>用了 FlashAttention</b>
  这一条上。<em>换成把分数矩阵写进显存的朴素实现，
  <b>S² 就回到显存账里，上面整段都不成立。</b></em></span></p></div></div>

<h3>5.2　算力：那个人人都在用的 <code>6ND</code>，在长上下文下<u>严重低估</u></h3>

<!-- ⭐⭐⭐ 2026-09-16。这一小节是写 §5 的时候自己撞出来的：
     本来只想「把 6ND 代进去报个数」，代完发现它跟 §2.3 那个 82.1% 直接打架。
     ⭐ 判据：**一份材料里两个独立算出来的数对不上，不是麻烦，是线索。**
       这里对不上的原因很干净 —— 6ND 数的是「跟权重相乘」那部分，
       而 attention 的 S² 项里没有权重，它根本不在 6ND 的账里。
     ⛔ 所以这一节的价值不在那个数，在**那个公式什么时候会骗你**。 -->

<p>估训练算力最常用的一条是 <b><code>6ND</code></b>：
  <em>每个参数、每个 token，大约 <b>6 次浮点运算</b>（前向 2、反向 4
  ——&nbsp;正好对上<a href="#s一">第一节</a>那个 1＋2）。</em></p>

<p>代进去（MoE 要用<u>激活</u>参数，不是总参数）：
  <em><code>6 × 37e9 × 131,072 ≈ <b>29.1 PFLOP</b></code>，一条 128K 序列。</em></p>

<div class="note danger"><p>⛔⛔ 但这个数在 128K 上是错的 ——&nbsp;而且错得很多。</p>
<p><em><code>6ND</code> 数的是<b>「跟权重相乘」</b>那部分。
  而 attention 里 QKᵀ 和 AV 这两步<b>没有权重参与</b>
  ——&nbsp;它们根本不在这个公式的账里。</em></p>
<p>⭐ <em>而<a href="#s七">附录 7.2</a> 那一行说：在 128K 上，
  attention 占一层前向算力的 82.1%。
  也就是说 <code>6ND</code> 数到的只是剩下那 <b>17.9%</b>。</em></p>
<p class="landing">⭐⭐⭐ 反推一下：真实算力大约是 <code>6ND</code> 的
  <u>五到六倍</u> ——&nbsp;<em>29.1 PFLOP 实际接近 <b>160 PFLOP</b>。</em></p>
<p><span class="sub">⚠️ 这个倍数是<b>由 82.1% 反推的</b>（1 ÷ 0.179），
  而 82.1% 本身是自己按算子推的 ——&nbsp;<b>当量级看</b>。
  ⭐ 要记的不是这个倍数，是<b>那个公式什么时候会骗你：序列一长就会</b>。</span></p></div>

<p class="landing">⭐ 反过来说，<code>6ND</code> 在短序列上是很好用的
  ——&nbsp;<em>GPT-3 那个 2,048，attention 只占百分之一点几，
  漏掉它完全无所谓。又是同一件事：不是公式错了，是前提变了。</em></p>

<h3>5.3　峰值：<u>先问「哪一项的峰」</u></h3>

<p>⛔ <em><a href="#s一">第一节</a>那张山形图给过一个答案：<b>峰值在前向末尾</b>。
  那句话只在谈激活的时候成立。</em></p>
<p>把四项一起画到时间轴上（就是上面那张图），
  <em>会看到一件当时看不出来的事：不同的项，峰在不同的时刻。</em></p>

<div class="note ok"><p>⭐⭐⭐ 推广出去，这是一条到处能用的判据：</p>
<p><em>把几条<b>形状不同</b>的曲线叠起来之后，
  总和的峰，未必落在任何一条单独的峰上。
  ——&nbsp;所以「什么时候最挤」这个问题，
  必须连着「挤的是哪一项」一起问。</em></p></div>

<p><span class="sub">📌 <b>工程上的直接后果：</b>
  你去查 OOM，<b>光知道「峰值 xx GiB」没用</b> ——&nbsp;
  要知道<b>是哪一项在那一刻最大</b>，才知道该去动哪个开关。
  <em>激活大就开重算 / 上 CP；优化器状态大就上 ZeRO ——&nbsp;
  这两条路走反了，一点用都没有。</em></span></p>

</details>
<h3>5.4　⭐⭐⭐ 一个 <u>1.25 亿参数</u>的模型，一步里发生了什么</h3>

<!-- ⭐⭐⭐ 2026-09-17 补。麻瓜审的第一条意见：
     **全讲从头到尾都是 6710 亿参数、TiB 级的数，没有一个能自己算一遍的例子。**
     ⛔ 那些数没错，但它们对没跑过训练的人是**没有分母的** ——
       听完只记住「很大」，记不住「怎么来的」。
     ⭐ 判据：**一讲里至少要有一个小到能手算、而且结论还成立的例子。**
       它不是为了简单，是为了**让人能自己验一遍**。
     ⭐ 挑 125M 是因为：它是 GPT-3 论文表 2.1 的第一行（所以学习率有出处），
       而且这个尺寸真的能在一张普通卡上跑起来。 -->

<div class="note ok"><p>⭐⭐ <b>先把这一讲那三件事，在这台机器上各落一句：</b></p>
<ul>
  <li><b>① loss 怎么算</b>：<em>8,192 个位置，<u>每个位置一个 <code>−ln p</code></u>，取平均。
    刚初始化时它是 <code>ln(词表)</code>，跟 <a href="#s1-0">1.0</a> 那把尺一模一样
    ——&nbsp;<b>只是词表小一点，所以起点低一点</b>。</em></li>
  <li><b>② 一个数变成多少个导数</b>：<em><b>1.25 亿个</b>。
    而代价是「多跑两遍前向」，不是 1.25 亿遍（<a href="#s1-1">1.1</a>）。</em></li>
  <li><b>③ 那一步该迈多大</b>：<em>Adam 要为<u>每一个</u>参数记两个滑动平均
    ——&nbsp;<b>那是 12 字节里的 8 字节</b>；剩下 4 字节是 fp32 主权重，
    来自混合精度，跟 Adam 无关（<a href="#s3-1">3.1</a>）。</em></li>
</ul>
<p class="landing">⭐ <b>下面这张账之所以值得算一遍，是因为它能把③那句话变成一个你能验的数。</b></p></div>

<div class="note"><p>📌 <b>设定：一个 1.25 亿参数的模型，一张卡，batch 8 × 序列 1,024。</b></p>
<p><em>12 层、宽度 768、MLP 宽 3,072 ——&nbsp;
  <b>这就是 GPT-3 论文表 2.1 的第一行</b>（<a href="#s三">3.5</a> 那张八档表最上面那档）。</em></p></div>

<h4>第一步 · 常驻那一块</h4>
<p><em>照搬 <a href="#s三">3.1</a> 那个 16 字节：</em></p>
<table>
<thead><tr><th>项</th><th>每参数</th><th>1.25 亿参数</th></tr></thead>
<tbody>
<tr><td>bf16 权重</td><td>2 B</td><td>0.23 GiB</td></tr>
<tr><td>bf16 梯度</td><td>2 B</td><td>0.23 GiB</td></tr>
<tr><td><b>优化器状态</b>（fp32 主权重 ＋ m ＋ v）</td><td><b>12 B</b></td><td><b>1.40 GiB</b></td></tr>
<tr><td><b>小计</b></td><td><b>16 B</b></td><td><b>1.86 GiB</b></td></tr>
</tbody></table>
<p class="landing">⭐ <b>第一个可以自己验的结论</b>：
  <em>这个模型的权重只有 0.23 GiB，<b>而它要占掉 1.86 GiB</b> ——&nbsp;
  <u>八倍</u>。多出来的全是训练才要的。</em></p>

<h4>第二步 · 激活那一块</h4>
<p>一层里每个 token 留下的东西，<b>一项一项数</b>（宽度 768）：</p>
<table>
<thead><tr><th>留下的张量</th><th>宽度</th></tr></thead>
<tbody>
<tr><td>两个 norm 的输出</td><td>2 × 768 ＝ 1,536</td></tr>
<tr><td>Q / K / V</td><td>3 × 768 ＝ 2,304</td></tr>
<tr><td>attention 输出</td><td>768</td></tr>
<tr><td>MLP 第一层输出</td><td>3,072</td></tr>
<tr><td>激活函数的输出</td><td>3,072</td></tr>
<tr><td>MLP 第二层输出</td><td>768</td></tr>
<tr><td><b>一层合计</b></td><td><b>11,520 个数 ＝ 22.5 KiB</b>（bf16）</td></tr>
</tbody></table>
<p><em>× 12 层 ＝ 每个 token <b>270 KiB</b>。
  × 8,192 个 token ＝ <b>2.11 GiB</b>。</em></p>

<div class="note danger"><p>⛔⛔ <b>停一下 ——&nbsp;这里出了一件值得吃惊的事。</b></p>
<p class="landing">⭐⭐⭐ <b>激活 2.11 GiB ＞ 常驻 1.86 GiB。</b>
  <em>一个<u>只有 1.25 亿参数</u>的小模型，
  <b>激活已经比权重、梯度、优化器状态加起来还大了。</b></em></p>
<p><em>⭐ 而它<b>不需要长上下文</b>，序列才 1,024，光是 batch 开到 8 就够了。
  ——&nbsp;<b>所以重算不是大模型的专利。</b></em></p></div>

<h4>第三步 · 开了重算之后</h4>
<p><em>照 <a href="#s二">2.2</a> 那一档：每层只留入口那一份（768 宽），
  外加<b>当前正在重算的那一层</b>的完整一份。</em></p>
<p class="landing">⭐ <b>0.32 GiB</b> ——&nbsp;<em>2.11 掉到 0.32，<b>省了 6.7 倍</b>，
  代价是总算力从 3× 变 4×。</em></p>

<div class="note danger"><p>⛔ <b>⚠️ 这个 0.32 GiB 有一个<u>故意的省略</u>，照着验的人会撞上它。</b></p>
<p><em>它只算了 12 层里的激活 ——&nbsp;<b>没算最后那个 logits</b>。
  GPT-3 那一代沿用 GPT-2 的 BPE 词表，<b>约 5.03 万</b>：</em></p>
<p class="landing"><code>8,192 token × 50,257 × 2 B</code> ＝ <b>0.77 GiB</b>
  ——&nbsp;<em><u>是那 0.32 GiB 的 2.4 倍</u>。</em></p>
<p><em>⭐ 也就是说：<b>如果你一次性把整个 logits 算出来，这台机器上最大的一块
  既不是权重也不是激活 ——&nbsp;是那张「词表上的分数表」。</b></em></p>
<p class="landing">⛔ <b>而这一小节的卖点正是「你能自己算一遍」</b>
  ——&nbsp;<em>所以这一条必须写出来。上面那 <a href="#s1-7">1.7</a> 的注解说了，
  真实实现<b>分块算 loss</b>，所以它不进总账；但<u>这是个口径选择，不是它不存在</u>。</em></p>
<p><span class="sub">⚠️ 5.03 万这个词表大小我是按「GPT-3 沿用 GPT-2 的 BPE」写的，
  <b>没有在这一轮回原文核过</b> ——&nbsp;换成 3 万或 6 万，结论（它比激活总量还大）不变。</span></p></div>

<table>
<thead><tr><th></th><th>不开重算</th><th>开了重算</th></tr></thead>
<tbody>
<tr><td>常驻</td><td>1.86 GiB</td><td>1.86 GiB</td></tr>
<tr><td>激活</td><td>2.11 GiB</td><td><b>0.32 GiB</b></td></tr>
<tr><td><b>合计</b></td><td><b>3.97 GiB</b></td><td><b>2.18 GiB</b></td></tr>
<tr><td>算力</td><td>3×</td><td>4×（＋33%）</td></tr>
</tbody></table>

<div class="note ok"><p>⭐⭐ <b>这张小表值得停一下 ——&nbsp;它就是你笔记本上那个训练循环。</b></p>
<ul>
  <li><b>16 字节那张账</b>（<a href="#s三">3.1</a>）——&nbsp;<em>1.86 GiB 是这么来的。</em></li>
  <li><b>激活随 token 总数长</b>（5.1，就在本节）——&nbsp;
    <em>2.11 GiB 里唯一的变量就是那 8,192 个 token。</em></li>
  <li><b>拿算力换显存</b>（<a href="#s二">2.2</a>）——&nbsp;
    <em>多付 <b>33%</b>，省 <b>6.7 倍</b>。</em></li>
</ul>
<p class="landing">⭐⭐ <b>「多付 33%」跟 671B 那边<u>一模一样</u> ——&nbsp;
  但「省 6.7 倍」跟那边的 __RECOMP_X__ 倍<u>差了三倍半</u>。</b></p>
<p><em>⛔ 这一处早先写的是「比例跟 671B 那笔几乎一样」。<b>不一样，而且差得不小。</b>
  ——&nbsp;⭐ 不过它为什么不一样，本身比那句话值钱。</em></p></div>

<div class="note ok"><p>⭐⭐⭐ <b>为什么同样是「全量重算」，一个省 6.7 倍、一个省 __RECOMP_X__ 倍？</b></p>
<p><em>把账写成一个式子就看明白了。设 <b>r ＝ 一层激活 ÷ 入口那一份</b>、<b>L ＝ 层数</b>：</em></p>
<p class="landing"><b>省多少倍　＝　r ÷ (1 ＋ r/L)</b></p>
<p><em>分子 <b>r</b> 是「一层里挂了多少东西」——&nbsp;层越胖，扔掉能省越多。<br>
  分母那一项是<b>当前正在重算的那一层</b>摊到每层头上 ——&nbsp;层越多，它越不碍事。</em></p>
<table>
<thead><tr><th></th><th>r ＝ 一层激活 ÷ 入口</th><th>L ＝ 层数</th><th>省多少倍</th></tr></thead>
<tbody>
<tr><td>125M 小模型</td><td><b>__R_SMALL__</b>（11,520 ÷ 768）</td><td>12</td><td><b>6.7</b></td></tr>
<tr><td colspan="4"><span class="sub">⚠️ <b>这两行的 r 口径略有不同</b>：小模型那一行只数了两个 norm 的<u>输出</u>，V3 那一行（1.7 的表）把 norm 的<u>输入</u>（＝残差入口）也数了进去。把入口补上，小模型的 r 约 16、省的倍数约 7.1 ——&nbsp;<b>量级和结论都不变，但严格说这两个数不是同一把尺子量的。</b></span></td></tr>
<tr><td>V3 671B</td><td><b>__R_V3__</b>（一层 MoE 块 ÷ 7,168）</td><td>61</td><td><b>__RECOMP_X__</b></td></tr>
</tbody></table>
<p class="landing">⭐ <b>小模型<u>两项都吃亏</u>：r 小 2.7 倍，L 小 5 倍。</b>
  <em>——&nbsp;所以它省得少<b>是必然的，不是哪儿算错了</b>。</em></p>
<p><em>⭐⭐ 而 r 大，根子在 <b>MoE</b>：<a href="#s1-7">1.7</a> 那张表里
  「派发出去的激活」和「专家输出」各复制九份 ——&nbsp;
  <b>一层里挂的东西多了，重算能省的自然也多。</b>
  <u>MoE 在参数账上省，在激活账上贵，而在重算账上又因此格外划算</u>
  ——&nbsp;同一个设计，在三张账上是三个方向。</em></p>
<p><span class="sub">⚠️ 这个式子给 V3 算出来是 <b>24.4</b>，实测 __RECOMP_X__
  ——&nbsp;差在那 3 层 dense 比 MoE 薄。<b>当量级看，别当精确值。</b></span></p></div>

<div class="note"><p>⭐⭐⭐ <b>那到底什么东西是<u>真的</u>跟规模无关的？</b>
  ——&nbsp;<em>这一小节存在的理由，得换成这三条。</em></p>
<ul>
  <li><b>多付 33%。</b><em>它只取决于「前向跑两遍而不是一遍」，跟模型多大、序列多长都无关。</em></li>
  <li><b>每参数 16 字节。</b><em>1.25 亿和 6,710 亿，拆法一字不差（<a href="#s3-1">3.1</a>）。</em></li>
  <li><b>激活只认 token 总数。</b><em>8,192 个 token 和 737 万个 token，规律是同一条。</em></li>
</ul>
<p class="landing">⛔ <b>而「省多少倍」<u>不在这个名单里</u>。</b>
  <em>——&nbsp;它跟着 r 和 L 走，<b>换个架构就得重算一遍</b>。
  ⭐ 这正好是第③条判据的又一个实例：<u>会变的和不会变的，得分清楚</u>。</em></p></div>

<div class="note"><p>⚠️ <b>三条边界，跟 1.7 那张表同一套。</b></p>
<ul>
  <li><em>这些数是<b>按算子逐项推的</b>，不是从哪份报告抄的
    ——&nbsp;框架的算子融合会让它小一些。<b>当量级看。</b></em></li>
  <li><em>没算注意力分数矩阵（假设用了 FlashAttention），
    也没算<b>临时缓冲、碎片、通信缓冲</b> ——&nbsp;真跑起来还要留富余。</em></li>
  <li><em>「开了重算 0.32 GiB」假设<b>每层都设了存档点</b>
    ——&nbsp;<a href="#s二">2.2</a> 那张图的 Ⓒ 说了，这不一定是最省的密度。</em></li>
</ul></div>

<details class="foldfig"><summary><b>这张账交给专题五的是什么</b>　<span class="why">⭐ 交棒段，属于并行</span></summary>
<h3>5.5　这张账单交给下一讲的是什么</h3>

<p class="lead">专题一算完前向，结论是<b>「装不下」</b>。
  <em>这一讲把账补全之后，那句话变得<b>具体</b>了 ——&nbsp;
  不是「装不下」，是「四项各自装不下，而且各有各的治法」。</em></p>

<div class="note ok"><p>⭐⭐ 四项 ＋ 四种对策，一一对上：</p>
<ul>
  <li><b>优化器状态</b>（最大，最少被用到）→&nbsp;<em><b>切</b>（ZeRO-1）</em></li>
  <li><b>梯度</b>（跟权重一样大，但反向结束前可以散着）→&nbsp;<em><b>切</b>（ZeRO-2）</em></li>
  <li><b>权重</b>（每层都要用）→&nbsp;<em><b>最后才切</b>（ZeRO-3／FSDP），因为通信最贵</em></li>
  <li><b>激活</b>（随 batch × 序列长度涨）→&nbsp;<em><b>重算</b>，或者按序列<b>切</b>（CP）</em></li>
</ul>
<p class="landing">⭐⭐⭐ 你会注意到四条里有三条写着同一个字：<u>切</u>。
  <em>——&nbsp;那就是<b>专题五</b>整讲要做的事：切这件事本身，也是要算账的。</em></p></div>



</details>
</div></section>

<section id="s六"><div class="wrap"><div class="stn"><span class="badge">第 六 节</span><h2>训不崩 ——&nbsp;<u>loss 飞了怎么办，以及怎么让它别飞</u></h2></div>

<!-- ⭐⭐⭐ 2026-09-16 加。这一节是**扫完十四份大纲之后补的**：
     「loss spike」「z-loss」「scaling law」三个关键词在整套课程里**零命中**，
     而前两样是真训练时最痛的东西。
     ⛔ 判据（这次盘点本身的方法）：**别凭印象说「还缺什么」——
       把关键词扫一遍，零命中的那几个才是真洞。**
       扫之前我以为最大的洞是数据，扫完发现数值稳定性比它更干净地缺着。
     ⭐ 本节**一个自己的实测都没有**，四条全部来自公开论文并逐条核过原文 ——
       理由见 §七：这一讲已经有太多「自己推的」数了。 -->

<p class="lead">前面整整一讲都在算<b>账</b>。<em>可账算得再清楚，也回答不了训练现场最常见的那一句：
  「loss 突然飞上去了，怎么办？」</em></p>

__FIG_STABILITY__

<h3>6.0　⭐⭐⭐ 先给这几种「坏」起个名字 ——&nbsp;一张对照表</h3>

<p>这一讲从头到尾讲过<b>五种</b>不同的坏，可它们散在五个章节里，
  各叫各的名字。<em>先把它们并排摆一次 ——&nbsp;
  <b>训练现场真正难的不是治，是先认出这是哪一种。</b></em></p>

<table><thead><tr><th>叫什么</th><th>现场长什么样</th><th>根因</th>
  <th>怎么确认是它</th><th>治法在哪一节</th></tr></thead><tbody>
<tr><td><b>梯度爆炸</b></td>
    <td>loss 变 NaN／Inf，或梯度范数一步之内跳几个数量级</td>
    <td>反向那条连乘里，一段的局部导数持续<b>大于 1</b>，
        61 层复合下来指数放大（<a href="#s一">1.4</a>）</td>
    <td>每步打印 global grad norm，看它是不是在某一步跳了几个数量级</td>
    <td>裁剪（<a href="#s六">6.4</a>）· 换 Pre-LN（<a href="#s六">6.5</a>）</td></tr>
<tr><td><b>梯度消失</b></td>
    <td>靠近输入那几层几乎不动，训了很久还是初始化的样子</td>
    <td><u>同一条连乘</u>，只是局部导数持续<b>小于 1</b> ——&nbsp;
        跟爆炸是一枚硬币的两面</td>
    <td>按层打印梯度范数，看是不是随深度单调塌下去</td>
    <td>Pre-LN 与初始化（<a href="#s六">6.5</a>）</td></tr>
<tr><td><b>更新被舍入吞掉</b><br><span class="sub">（「训不动」）</span></td>
    <td>loss 不降，可梯度明明不是 0；换上 fp32 主权重立刻就好</td>
    <td>单步的<u>相对</u>挪动幅度，小于尾数能分辨的门槛
        （bf16 约 <code>1/256</code>）</td>
    <td>算 <code>update RMS ÷ 参数 RMS</code>，跟 <code>1/256</code> 比</td>
    <td>fp32 主权重（<a href="#s3-2">3.2</a>）</td></tr>
<tr><td><b>步长过大发散</b></td>
    <td>loss 一路上翘；或来回弹，<u>每弹一次幅度更大</u></td>
    <td><code>η</code> 超过 <code>2 ÷ 陡方向的曲率</code>，
        沿那个方向冲过头并被放大</td>
    <td>把 <code>η</code> 砍一半重跑 ——&nbsp;立刻正常就是它</td>
    <td>学习率上限与 warmup（<a href="#s3-3b">3.3b</a> · <a href="#s3-5">3.5</a>）</td></tr>
<tr><td><b>loss spike</b><br><span class="sub">（不稳定）</span></td>
    <td>平稳训练里突然一根尖峰，之后可能自愈、也可能一路崩</td>
    <td>⛔ <b>不是数据坏</b> ——&nbsp;这一条有消融（<a href="#s六">6.2</a>）</td>
    <td>回滚重跑<u>同一批</u>数据，看它还飞不飞</td>
    <td>本章 6.1–6.6</td></tr>
</tbody></table>

<div class="note ok"><p>⭐⭐ <b>前两行值得单独看一眼：爆炸和消失不是两个问题，是<u>同一条连乘</u>
  的两个方向。</b></p>
<p><em><a href="#s一">1.4</a> 说过，一个参数的梯度是 61 个局部导数<b>乘起来</b>。
  这串乘积里每一项平均大于 1，结果就指数涨；平均小于 1，就指数塌。
  <b>所以它们的治法也是同一类</b> ——&nbsp;让每一层的局部导数<u>不偏离 1 太远</u>，
  这正是 <a href="#s六">6.5</a> 那些结构手段在干的事。</em></p>
<p><span class="sub">⛔ 中间那两行（舍入吞掉、步长发散）常被一起叫成「训不动」，
  可它们的判据正好相反：<b>前者要把步子放大或提精度，后者要把步子调小</b>。
  认错了会越治越糟。</span></p></div>

<h3>6.1　先看一个真实的画面</h3>

<div class="note"><p>📏 <b>动手之前，先把 <a href="#s1-0">1.0</a> 那把尺拿出来。</b></p>
<p><em>「飞了」是个感觉词，而这一节要讲的是判断。<b>换成数就清楚了：</b></em></p>
<ul>
  <li><em>预训练中后期 loss 在 <b>2 上下</b>晃 ——&nbsp;正常。</em></li>
  <li><em>突然跳到 <b>6</b>：困惑度从 7 变成 400
    ——&nbsp;<b>模型退回到「只会字频」那一档</b>，但还认得字。</em></li>
  <li>⛔ <em>跳回 <b>11.8</b>：那是 <code>ln 129,280</code>
    ——&nbsp;<b>它把学过的东西<u>全忘光了</u>，退回到瞎猜。</b></em></li>
</ul>
<p class="landing">⭐⭐ <b>所以「飞多高」不是修辞，它直接告诉你还救不救得回来。</b>
  <em>——&nbsp;跳到 6 通常回滚几十步就能过去；<u>跳回 ln V 基本就是这一段白跑了</u>。</em></p></div>

<div class="note danger"><p>⛔⛔ PaLM 训 540B 的时候，loss 飞了<u>大约 20 次</u>。</p>
<p><em>而且 ——&nbsp;<b>梯度裁剪是开着的</b>。
  这些 spike 出现在<b>极不规则</b>的时刻，有时候训到很晚才来；
  而更小的模型上根本没观察到。</em></p>
<p><span class="sub">📌 arXiv <b>2204.02311</b> §5.1。
  「大约 20 次」「尽管梯度裁剪是开着的」都是论文原话。</span></p></div>

__FIG_SPIKE__

<p class="landing">⭐ 先记住这个规模效应：<u>小模型上训得好好的，不代表大模型上不会飞</u>。
  <em>——&nbsp;这跟 <a href="#s二">2.6</a> 那条「不能跨规模照抄」是同一件事，
  只不过这次翻车的不是收益，是训练本身。</em></p>

<h3>6.2　⭐⭐⭐ 那个最反直觉的消融：<u>不是数据坏</u></h3>

<p>第一反应都一样：肯定是那批数据有问题。
  <em>PaLM 团队去验了这件事，做法很干净：</em></p>
<p><em>把 spike 前后那几批数据<b>单独拎出来</b>，从<b>另一个更早的 checkpoint</b>
  重新喂一遍 ——&nbsp;结果不飞。</em></p>

<div class="note ok"><p>⭐⭐⭐ 所以 spike 不是「坏数据」造成的，
  是<u>这批数据</u> 和 <u>当时那个参数状态</u> 撞在一起才出的事。</p>
<p><em>换个时刻喂同样的数据，什么都不会发生。</em></p>
<p class="landing">⭐ 这一下就解释了他们那个看起来很土的办法为什么管用：
  <em>回滚到 spike 之前约 100 步的 checkpoint，跳掉那 200–500 批数据，继续跑
  ——&nbsp;之后同一个点就不再飞了。</em></p>
<p><span class="sub">⛔ 注意这不是「修好了」，是「绕过去了」。
  <em>论文自己也写着：由于训练成本太高，他们没能找到一个有原则的缓解办法。
  ⭐ 这句坦白值得原样转述 ——&nbsp;这一行至今仍是开放问题。</em></span></p></div>

<h3>6.3　⭐⭐ 这一节真正的难点：<u>稳住很容易，难的是稳住而不掉质量</u></h3>

<p><em>「让它别飞」有一堆办法：学习率调到极小、裁剪收到极紧……
  都能稳，代价是模型变差。</em></p>
<p>⭐ <b>ST-MoE 那篇论文把这件事量出来了</b>
  ——&nbsp;<em>同一个配置换不同随机种子反复跑，看几次能跑完、以及跑完的质量：</em></p>

<table>
<thead><tr><th>做法</th><th>稳定性</th><th>质量（越大越好）</th><th>结论</th></tr></thead>
<tbody>
<tr><td>基线</td><td>4 / 6</td><td>−1.755</td><td>六次里两次训崩</td></tr>
<tr><td colspan="4"><span class="sub">⚠️ <b>这一列是<u>对数似然</u>，不是 loss</b>
  ——&nbsp;所以它是负的，而且<b>越大越好</b>（−1.755 好过 −4.206）。
  ⭐ 换成 <a href="#s1-0">1.0</a> 那把尺就是：loss ＝ 1.755 与 4.206，
  <u>困惑度 5.8 对 67</u>。<b>别把这两栏当成同一个量读。</b></span></td></tr>
<tr><td>收紧 update clipping（0.1）</td><td><b>3 / 3</b></td><td><b>−4.206</b></td>
    <td>⛔ 稳了，可质量被打穿了</td></tr>
<tr><td><b>router z-loss</b></td><td><b>3 / 3</b></td><td><b>−1.741</b></td>
    <td>⭐ 稳了，质量还略好一点</td></tr>
</tbody></table>

<p class="landing">⭐⭐⭐ 中间那一行是这张表的全部价值：
  「稳定 3/3」看着完美，可它是拿质量换来的。
  <em>——&nbsp;评价一个稳定性手段，必须同时看这两栏。</em></p>

<div class="note"><p>⚠️ <b>两个口径要说清楚，不然这张表会被读得太重。</b></p>
<ul>
  <li><b>分母不一样。</b><em>基线跑的是 <b>6 个种子</b>，每个稳定性手段各跑
    <b>3 个种子</b> ——&nbsp;所以「4/6」和「3/3」<b>不是同一个分母下的比较</b>。
    ⛔ <b>3/3 也就是三次，样本小到不该当成「保证稳定」。</b></em></li>
  <li><b>这个实验是<u>刻意挑</u>出来的。</b><em>作者写得很直白：小模型很少不稳定，
    大模型又贵到跑不起足够的种子。<b>于是他们特意选了一个约三分之一概率会崩的配置，
    还特意换到多语种数据上 ——&nbsp;因为那会让不稳定更频繁。</b></em></li>
</ul>
<p class="landing">⭐ 这不是黑点，<b>是好的实验设计</b> ——&nbsp;
  <em>研究一个罕见故障，你必须先把它变得不罕见。
  但读数的时候要记住：<b>这个概率是被调高过的。</b></em></p></div>
<p><span class="sub">📌 arXiv <b>2202.08906</b>（ST-MoE）Table 4。
  论文自己的小标题就是「很多方法能稳住稀疏模型，但代价是质量变差」。</span></p>

<h3>6.4　治法一 · 治数值：<u>不让某些量长太大</u></h3>

<h4>z-loss ——&nbsp;摁住 softmax 前面那个 logits</h4>
<p><em>做法出人意料地简单：在总 loss 上加一项，惩罚 softmax 归一化因子的对数的平方。
  ——&nbsp;它逼着那个 <code>log Z</code> 待在 0 附近，<b>也就是不让 logits 长得太大</b>。</em></p>
<p>⭐ 两个地方都能加，而且都有人用：</p>
<ul>
  <li><b>输出 softmax 上</b> ——&nbsp;<em>PaLM 用的是 <code>10⁻⁴ · log²Z</code>，
    论文说它<b>提高了训练稳定性</b>。</em></li>
  <li><b>MoE 路由器上</b> ——&nbsp;<em>就是上面那个 router z-loss。
    ⭐ <b>MoE 训练几乎必配</b>：路由器那一步也是个 softmax，而它的 logits 一旦爆掉，
    路由结果会剧烈抖动。</em></li>
</ul>
<p><span class="sub">⭐ <b>为什么 z-loss 特别划算</b>：ST-MoE 的解释是 ——&nbsp;
  logits 后面接的是指数函数，而指数对输入误差极度敏感。
  <em>把输入范围压小，等于直接降低了这一步的数值风险，
  而模型并不真的需要那么大的动态范围。</em></span></p>

<h4>梯度裁剪 ——&nbsp;它是安全带，不是调参手段</h4>
<p><em>按<b>全局范数</b>裁：把所有参数的梯度当成一个大向量，
  范数超过阈值就整体等比例缩回去。<b>常见设 1.0</b>（PaLM、DeepSeek-V3 都是 1.0）。</em></p>
<p>⛔ 但 6.1 那句话要记住：PaLM 开着裁剪，照样飞了 20 次。
  <em>——&nbsp;裁剪拦得住单步的爆炸，拦不住「参数状态已经走到了一个坏地方」。</em></p>

<h3>6.5　治法二 · 治结构：<u>让它天生不容易飞</u></h3>

<h4>⭐⭐ 归一化放在哪儿 ——&nbsp;这件事顺带回答了 3.5 那个悬案</h4>
<p><em>原始 Transformer 把 LayerNorm 放在<b>残差块之间</b>（Post-LN）。
  后来大家改成放在<b>残差块里面</b>（Pre-LN）。</em>
  这不是风格问题。</p>

<div class="note ok"><p>⭐⭐⭐ 2020 年有一篇论文用理论证明了这件事，
  而它的结论直接接上了<a href="#s三">3.5</a>：</p>
<p><em>Post-LN 在初始化的那一刻，靠近输出层的梯度就很大 ——&nbsp;
  这时候用大学习率必然不稳。而 warmup 正是在实践中压住这个问题的办法。<br>
  反过来，<b>Pre-LN 在初始化时梯度就是良态的</b> ——&nbsp;
  所以论文说：Pre-LN 可以把 warmup 那一段去掉。</em></p>
<p class="landing">⭐⭐ 所以「为什么要 warmup」这个问题，历史上有过两个答案：
  <em>2020 年那篇说<b>是被 Post-LN 逼的</b>；
  <a href="#s三">3.5</a> 引的 2024 年那篇说<b>它让网络能承受更大的峰值学习率</b>。
  ⭐ 两个都对，而且不矛盾 ——&nbsp;
  前者解释了「为什么当年非要不可」，后者解释了「为什么今天还留着」。</em></p>
<p><span class="sub">📌 arXiv <b>2002.04745</b>（ICML 2020），用平均场理论证的。</span></p></div>

<!-- ⭐⭐⭐ 2026-09-18 R16：上面那篇给的是**现象**（Post-LN 初始化时靠近输出梯度大），
     ⛔ 没讲机制，也没讲「那为什么还留着它」。下面这段补的就是这两块。
     ⛔⛔ 一个必须避开的陷阱：原文公式给的是 x₀ 在第 l 层输出里的系数 2^(−l/2)，
       那是**前向**的残差通道被削，**不等于「梯度每层乘 1/√2」** ——
       两者相关但不是一回事，混起来就是「听起来像常识的架构关系」。
       所以图上画的、算的全是前向直通项，「因此梯度消失」这一步由正文说。 -->
<p>⛔⛔ <b>不过上面那段只说了「Post-LN 的梯度不好」，没说<u>为什么</u>。</b>
  <em>而机制其实一句话就能讲完，讲完之后还能顺手回答一个更扎人的问题：
  <b>既然它不好，那为什么不干脆去掉？</b></em></p>

<div class="note danger"><p>⭐⭐⭐ Post-LN 是
  <code>x<sub>t+1</sub> ＝ Norm(x<sub>t</sub> ＋ F<sub>t</sub>(x<sub>t</sub>))</code>。</p>
<p><em>初始化那一刻，<b>x 和 F(x) 可以看成两个独立随机向量</b>，
  各自方差是 1，加起来方差就是 2 ——&nbsp;而 Norm 要把方差拉回 1，
  <b>所以它在这一刻相当于「除以 √2」。</b></em></p>
<p class="landing">⭐⭐ 一层一层递归下去：
  <b>最初那个输入 x₀，在第 l 层输出里的系数就是 <code>2<sup>−l/2</sup></code>。</b>
  <em>——&nbsp;<b>残差那条「直通路」被指数地削掉了，越靠近输入削得越狠。</b></em></p>
<p><em>⭐ 所以苏剑林那句话说得很重，但它有依据：
  <b>「在 Post Norm 的 BERT 模型中，LN 不仅不能缓解梯度消失，
  它还是梯度消失的『元凶』之一。」</b></em></p></div>

__FIG_PREPOST__

<p>⛔⛔⛔ <b>那为什么不去掉它？</b>
  <em>——&nbsp;因为去掉之后 <b>x ＋ F(x) 的方差就是 2</b>，残差越多方差越大。
  <b>问题从来不是「加不加 Norm」，是「加在哪儿」</b>：
  Pre-LN 改成 <code>x ＋ F(Norm(x))</code>、最后总输出再加一个 Norm，
  这样每个残差分支<b>是平权的</b>，就没有那条指数衰减了。</em></p>

<div class="note ok"><p>⭐⭐⭐ 而最反直觉的一条是：
  <b>这个「毛病」，换个场合就变成了功能。</b></p>
<p><em>Finetune 的时候，我们<b>本来就希望优先调靠近输出的参数</b>，
  不要过度动靠近输入的 ——&nbsp;免得把预训练学到的东西破坏掉。
  而梯度消失的意思恰恰是「越靠近输入，它对最终输出的影响越弱」，
  <b>这正好是 finetune 想要的。</b></em></p>
<p class="landing">⭐⭐ 所以预训练好的 Post-LN 模型，
  <b>往往比 Pre-LN 有更好的 finetune 效果</b>。
  <em>⛔ 同一条曲线，预训练时是毛病，微调时是功能
  ——&nbsp;<b>这就是上面那张图左右两半在说的事。</b></em></p>
<p><span class="sub">📌 出处：苏剑林《模型优化漫谈：BERT 的初始标准差为什么是 0.02？》
  <a href="https://kexue.fm/archives/8747">kexue.fm/archives/8747</a>。
  ⚠️ 这是<b>作者本人的分析文章</b>，不是同行评议论文
  ——&nbsp;我们按「一个讲得通的机制解释」收下，不当定论。</span></p></div>

<p>⭐ <em>顺带把 <a href="#s三">3.5</a> 那三个 warmup 解释的<b>前提</b>坐实了：
  第三条说「压住后面的层、等前面的层跟上」，
  <b>它要的正是这里这条指数衰减</b> ——&nbsp;
  Pre-LN 没有这条线，所以那一条对它也就不适用。</em></p>

<h4>QK-norm ——&nbsp;注意力分数长疯了</h4>
<p><em>ViT 往上做到<b>约 80 亿参数</b>的时候，训了几千步就发散。
  根因查出来很具体：注意力 logits 变得极大，
  于是 softmax 之后的注意力权重<b>塌成几乎 one-hot</b>（熵接近 0）。</em></p>
<p class="landing">⭐ 治法也很直接：在做点积之前，给 Q 和 K 各做一次 LayerNorm。
  <em>——&nbsp;这就是今天很多大模型标配的 QK-norm 的来历。</em></p>
<p><span class="sub">📌 arXiv <b>2302.05442</b>（ViT-22B）§2。
  ⚠️ <b>出处要说准：QK-norm <u>不是</u> ViT-22B 发明的。</b>
  <em>它的原文写的是「我们<b>采用</b> Gilmer 等（2023）的做法」
  ——&nbsp;ViT-22B 是<b>把它用到 220 亿参数上并留下那张对照图</b>的地方，
  这才是它值得引的原因。</em>
  ⭐ 注意这又是一次「只在大规模上才出现」的故障 ——&nbsp;
  跟 6.1 那条是同一个模式。</span></p>

<h4>初始化 ——&nbsp;一句话带过，但别漏</h4>
<p><em>深网络里，如果每一层的残差分支都按同样的尺度初始化，
  <b>信号会随层数累积放大</b>。常规做法是<b>按深度把残差分支的初始化缩一下</b>
  ——&nbsp;它不解决 spike，但它决定了你从一个多好的起点出发。</em></p>

<h3>6.6　顺带清掉两个常见误解</h3>

<div class="note"><p>❓ 「bf16 是不是也要 loss scaling？」——&nbsp;不用。</p>
<p><em>loss scaling 是 <b>fp16 时代</b>的必需品：fp16 的<b>指数位只有 5 位</b>，
  小梯度会直接下溢成 0，所以要先把 loss 放大若干倍再算、更新前再缩回去。<br>
  ⭐ 而 bf16 的指数位跟 fp32 一样是 8 位 ——&nbsp;
  动态范围没缩，缩的是尾数精度。所以它不下溢，也就不需要 loss scaling。</em></p>

__FIG_UNDERFLOW__

<p class="landing">⭐ 这跟 <a href="#s三">3.2</a> 那条判据是配套的：
  bf16 换来的是「范围够、精度差」，所以怕的是<u>累加</u>，不是<u>下溢</u>。</p></div>

<div class="note"><p>❓ 「训练是不是应该做到可复现？」——&nbsp;能做到，而且很值。</p>
<p><em>PaLM 做到了<b>逐比特可复现</b>：从任何一个 checkpoint 重启，
  后续每一步的结果<b>跟原来那次一模一样</b>。</em></p>
<p>⭐ <em>靠的是两件事：<b>确定性的计算框架</b>，
  以及<b>确定性的数据管线</b> ——&nbsp;
  后者的关键是<b>「第几批数据」只由步数决定</b>，跟进程数、恢复次数都无关。</em></p>
<p class="landing">⛔ 而 6.2 那个「回滚 ＋ 跳数据」的办法，前提就是这个。
  <em>——&nbsp;数据管线不确定，你连「跳掉哪几批」都说不清。</em></p>
<p><span class="sub">📌 同样出自 arXiv <b>2204.02311</b>。</span></p></div>

<h3>6.7　⭐⭐⭐ 全课落点：四条判据</h3>

<div class="note ok"><p>⭐⭐⭐ <b>先回到开头那句话。</b></p>
<p><em>第零节我说：训练这件事，就是拿<u>一个</u>数，去决定<u>__NPARAM_CN__个</u>参数各自该往哪边挪。</em></p>
<p class="landing">⭐ <b>现在这句话的每一个字都有着落了：</b></p>
<ul>
  <li><b>那个数</b> ——&nbsp;<em><code>−ln p</code>，模型对正确答案有多意外。
    刚开始是 <b>11.77</b>（在整个词表里瞎猜），训到后来落在 <b>2</b> 附近。
    （<a href="#s1-0">1.0</a>）</em></li>
  <li><b>一个数怎么变成 __NPARAM_CN__个导数</b> ——&nbsp;<em>因为<u>进去的多、出来的少</u>，
    所以从输出往回推最划算。<b>__NPARAM_CN__次前向，压成了一次反向。</b>
    （<a href="#s1-5">1.5</a>）</em></li>
  <li><b>那一步该迈多大</b> ——&nbsp;<em>方向是算出来的，<b>步长不是</b>。
    半个世纪的优化器史，都在跟这一个没有答案的问题较劲。
    （<a href="#s3-3b">3.3b</a>）</em></li>
</ul>
<p class="landing">⭐⭐⭐ <b>而中间所有那些账 ——&nbsp;三倍算力、扔不掉的中间结果、
  每参数 16 字节 ——&nbsp;都只是为了把那一个数，<u>从 11.8 挪到 2</u>。</b></p>
<p><em>⛔ 到了 2 附近之后，<b>每往下 0.01 都很贵</b>
  ——&nbsp;而那时候你付的，就是你刚才算过的那整张账。</em></p></div>

<p class="lead">这一讲还报了几十个数字。<em>但真正想留下的不是那些数。</em></p>

<div class="note ok"><ul>
  <li>① 要不要高精度，比的是<b>两个数</b>：
    <em>「这一项<u>每一步挪动的相对幅度</u>」，对上「<u>尾数能分辨的最小格</u>」
    ——&nbsp;bf16 相邻两个数差 <b>1/128</b>（尾数 7 位，2⁻⁷），
    <u>所以「加上去还是被舍掉」的门槛是它的一半，约 <b>1/256</b></u>。<br>
    <span class="sub">⛔ 这两个数都对，但<b>不是同一个量</b>：
    1/128 是<b>格宽</b>（<a href="#s3-2">3.2</a> 那张精度图的 Ⓑ 画的 0.0078125 就是它），
    1/256 是<b>舍入门槛</b>（差得比半格还小就原地不动）。当判据用的时候看后者。</span><br>
    滑动平均每步都在遗忘，挪得动；主权重跨十万步累加、单步只挪千分之几，<b>挪不动</b>。
    （<a href="#s三">3.2</a>）</em></li>
  <li>② 该不该重算，看「每省一字节要付多少 FLOPs」。
    <em>线性层的这个数恰好等于输入宽度；attention 的随序列长度线性上升
    ——&nbsp;两条斜率不同，必然相交。（<a href="#s二">2.3</a>）</em></li>
  <li>③ 两个量如果<b>对同一个变量的增长速度不同</b>，
    那它们的排序<b>必然在某一点翻转</b>。
    <em>——&nbsp;所以任何「A 比 B 划算」的结论，<b>都得带上它成立的那一段区间</b>；
    脱离区间的排序是没有意义的。<br>
    ⭐ 本讲里它翻转过好几处，而且每处的那个变量都不一样：
    序列长度（<a href="#s二">2.4</a>）、模型规模（6.1，就在本节）、
    以及同一个开关换规模连收益的正负号都变（<a href="#s二">2.6</a>）。</em></li>
  <li>④ 评价任何一个稳定性手段，要同时看「稳没稳」和「质量掉没掉」。
    <em>只看前者的话，把学习率设成零是最优解。（本节 6.3）</em></li>
</ul>
<p class="landing">⭐ 数字会过期，这四条不会。</p></div>

<p><span class="sub">⛔ <b>这一节刻意<u>一个我们自己的实测都没有</u></b> ——&nbsp;
  四条全部来自公开论文并逐条核过原文。
  <em>理由写在 <a href="#s七">§七</a>：这一讲已经有太多「自己推的」数了，
  稳定性这一块不该再加。</em></span></p>

</div></section>

<section id="s七"><div class="wrap"><div class="stn"><span class="badge">附　录</span><h2>这一讲的数是怎么核的</h2></div>

<!-- ⭐⭐⭐ 2026-09-16 加。这一节是**给这一讲上保险**用的。
     ⛔ 这一讲报了几十个数字，其中相当一部分是自己推的。
       把两类混在一起报，读者没有任何办法分辨 —— 而他们会抄回去用。
     ⭐ 判据（CLAUDE.md 第一原则的落地）：
       **推出来的必须写清推导链；查来的必须给得到出处。两类不能长得一样。** -->

<p class="lead">⭐⭐ 这一讲报了几十个数字。它们<u>不是同一种东西</u>。
  <em>有的能查到论文原文，有的是我们自己按算子推的
  ——&nbsp;后者可能差一倍，前者不会。</em></p>

<h3>7.0　⭐⭐ 这些东西在框架里<u>叫什么</u></h3>

<!-- ⭐⭐⭐ 2026-09-17 补。麻瓜审的另一条：
     **全讲讲了一堆概念，可没有一处告诉你「这东西在代码里叫什么」。**
     ⛔ 后果很具体：听完了，回去打开框架，**不知道该搜哪个词**。
     ⭐ 判据：**一个概念要么给它在真实工具里的名字，要么承认这一讲不管落地。**
       含含糊糊地讲概念、不给名字，等于把最后一公里留给读者自己撞。
     ⚠️⚠️ 这张表最容易腐坏 —— 名字会改。所以：
       **每一行都标了是从哪个文件 grep 出来的**，读者可以自己再验一遍。
       表里每一个名字都是 2026-09 主干上**当场 grep 到的**，不是凭记忆写的。 -->

<p class="lead">⭐ 这一讲讲了一堆概念。<b>可回去打开框架，该搜哪个词？</b></p>

<table>
<thead><tr><th>这一讲里的说法</th><th>在框架里叫什么</th><th>核的地方</th></tr></thead>
<tbody>
<tr><td><b>全量重算</b>（<a href="#s二">2.2</a>）</td>
    <td><code>--recompute-granularity full</code></td>
    <td rowspan="3">Megatron-LM<br><code>training/arguments.py</code><br>
        <code>core/transformer/transformer_config.py</code></td></tr>
<tr><td><b>选择性重算</b>（<a href="#s二">2.3</a>）</td>
    <td><code>--recompute-granularity selective</code> ＋
        <code>--recompute-modules</code><br>
        <span class="sub">候选项：<code>core_attn</code>（<b>selective 打开后的默认选项</b>）/ <code>layernorm</code> /
        <code>moe_act</code> / <code>mla_up_proj</code> / <code>mlp</code> …</span></td></tr>
<tr><td><b>梯度裁剪</b>（<a href="#s一">1.8</a>）</td>
    <td><code>--clip-grad</code>（默认 <b>1.0</b>，正好是 V3 用的那个值）</td></tr>
<tr><td><b>重算</b>，手写的那种</td>
    <td><code>torch.utils.checkpoint.checkpoint(...)</code></td>
    <td rowspan="2">PyTorch<br><code>torch/utils/checkpoint.py</code></td></tr>
<tr><td><b>选择性重算</b>，按张量挑</td>
    <td><code>create_selective_checkpoint_contexts</code> ＋ <code>CheckpointPolicy</code><br>
        <span class="sub">⭐ 这就是 2.3 那条判据在 PyTorch 里的落点：
        <b>你自己写规则决定哪个算子存、哪个重算</b></span></td></tr>
<tr><td><b>重算</b>（JAX 那边）</td>
    <td><code>jax.checkpoint</code>（别名 <code>jax.remat</code>）</td>
    <td rowspan="2">JAX<br><code>jax/_src/ad_checkpoint.py</code></td></tr>
<tr><td><b>选择性重算</b>的现成策略</td>
    <td><code>jax.checkpoint_policies.dots_with_no_batch_dims_saveable</code><br>
        <span class="sub">⭐ 名字直接说出了判据：<b>「矩阵乘的结果留着」</b>
        ——&nbsp;正是 2.3 那张表最贵的那几行</span></td></tr>
<tr><td><b>梯度累积</b>（<a href="#s一">1.8</a> 第 ④ 道）</td>
    <td><code>ddp.no_sync()</code>　——&nbsp;上下文管理器<br>
        <span class="sub">进去之后梯度<b>只在本地累加、不跨卡同步</b>；
        <b>退出后的第一次 forward-backward 才同步一次</b>（原文档语）</span></td>
    <td rowspan="2">PyTorch<br><code>torch/nn/parallel/distributed.py</code></td></tr>
<tr><td><b>「跨卡汇总能藏进计算里」</b>（<a href="#s一">1.8</a> 第 ② 道）</td>
    <td><code>bucket_cap_mb</code>（默认 <b>25 MiB</b>）<br>
        <span class="sub">⭐ 梯度<b>攒够一桶就发一次</b> ——&nbsp;
        所以前面的层还在算，后面的桶已经在路上了。
        <b>那一道「✅ 能藏」，藏在这个桶里</b></span></td></tr>
<tr><td><b>ZeRO 的三级</b>（<a href="#s四">4.2</a>）</td>
    <td>配置里的 <code>zero_optimization.stage</code> ＝ <b>1 / 2 / 3</b></td>
    <td>DeepSpeed<br><code>runtime/zero/config.py</code></td></tr>
<tr><td><b>8-bit 优化器</b>（<a href="#s三">3.4</a>）</td>
    <td><code>bitsandbytes.optim.Adam8bit</code> / <code>AdamW8bit</code></td>
    <td>bitsandbytes<br><code>optim/__init__.py</code></td></tr>
</tbody></table>

<div class="note ok"><p>⭐⭐ <b>这张表里有两行值得单独看一眼 ——&nbsp;它们是这一讲的判据<u>被写进 API 名字里</u>了。</b></p>
<ul>
  <li><code>dots_with_no_batch_dims_saveable</code> ——&nbsp;
    <em>「矩阵乘的结果<b>存下来</b>」。<b>为什么是矩阵乘？</b>
    因为它每省一字节要付的 FLOPs 最多 ——&nbsp;正是 <a href="#s二">2.3</a> 那条闭式解。</em></li>
  <li><code>ddp.no_sync()</code> ——&nbsp;
    <em>⭐⭐ 把这两行放一起看就清楚了：<b><code>no_sync()</code> 干的事，
    正是把第 ② 道整个关掉。</b>
    累积期间一次不通信，攒完再一次性同步 ——&nbsp;
    <b>所以「梯度累积顺带省通信」不是副作用，它就是这个 API 的用途。</b></em></li>
  <li><code>core_attn</code> 是 Megatron 打开 selective 之后的<b>默认选项</b>
    （⛔ 不是「默认开着」——&nbsp;<code>recompute_granularity</code> 默认是 <code>None</code>），
    而且源码里就带着一条「融合 attention 下你不需要设它」的警告 ——&nbsp;
    <em>而按 <a href="#s二">2.4</a>，长上下文下它恰恰是<b>最不该重算</b>的那一个。
    ⭐ <b>默认值是有年代的</b>，这条在 <a href="#s二">2.5</a> 展开过。
    ⛔ <b>顺带消一个看起来矛盾的地方</b>：<a href="#s二">2.2</a> 说「大模型训练里重算默认就开着」讲的是<u>业界实践</u>，
    这里说 <code>recompute_granularity</code> 默认 <code>None</code> 讲的是<u>框架出厂值</u> ——&nbsp;<b>两句都对，但不是同一个「默认」。</b></em></li>
</ul>
<p><span class="sub">⚠️ <b>顺带承认这一讲少讲了一档。</b>
  <em>PyTorch 的 <code>CheckpointPolicy</code> 其实有<b>六个</b>枚举值，
  不是两个：<code>{MUST,PREFER}_SAVE</code>、<code>{MUST,PREFER}_RECOMPUTE</code>、
  还有 <code>{MUST,PREFER}_CPU_OFFLOAD</code>。<br>
  ⭐ 也就是说，框架眼里「这块激活怎么办」是<b>三选一</b>：
  <b>存在显存里 / 扔了重算 / 卸到 CPU 再搬回来</b> ——&nbsp;
  <b>而本讲只讲了前两个</b>。第三条是拿 PCIe 带宽换显存，
  跟本讲那条「拿算力换显存」是同一个形状、不同的货币。<br>
  ⛔ 至于 <code>MUST</code> 和 <code>PREFER</code> 的区别，文档写得很直接：
  <b><code>MUST_*</code> 表示这条不许被 <code>torch.compile</code> 那类子系统覆盖</b>。</em></span></p>

<p class="landing">⭐ 所以这张表不只是「查词表」，<b>它是个反向检查</b> ——&nbsp;
  <em>如果你理解的判据跟框架提供的选项对不上，
  <b>多半是你的判据错了，或者那个默认过期了。</b></em></p></div>

<div class="note danger"><p>⚠️ <b>这张表<u>最容易过期</u>，所以用法要说清楚。</b></p>
<p><em>上面每一个名字都是 <b>2026 年 9 月</b>在各自主干上<b>当场 grep 到的</b>，
  不是凭印象写的 ——&nbsp;<b>而框架改名是常事。</b></em></p>
<p class="landing">⛔ <b>判据：给了名字，就得给「在哪个文件里能查到」。</b>
  <em>——&nbsp;否则名字一改，这张表就从「帮忙」变成「误导」，而且<u>不会有任何东西报错</u>。</em></p></div>

<h3>7.1　查得到出处的（放心抄）</h3>
<table>
<thead><tr><th>数字 / 说法</th><th>出处</th></tr></thead>
<tbody>
<tr><td>每参数 <b>16 字节</b>（2＋2＋12）的经典口径；ZeRO 三级的切法</td>
    <td>Rajbhandari 等，<b>arXiv 1910.02054</b></td></tr>
<tr><td>GPT-3 <b>八档</b>峰值学习率与 batch；375M token 线性 warmup；
    2,600 亿 token 内余弦降到 <b>10%</b>；weight decay 0.1；上下文 2,048</td>
    <td><b>arXiv 2005.14165</b> 表 2.1 与<b>附录 B</b>（§2.3 只写「细节见附录 B」）</td></tr>
<tr><td>DeepSeek-V3 的 <b>完整 LR schedule</b>（2K 步 →&nbsp;10T 恒定 →&nbsp;4.3T 余弦
    →&nbsp;末段两级）；梯度裁剪 <b>1.0</b>；batch 3072→15360；
    bf16 的一/二阶矩 ＋ fp32 主权重和梯度；<b>重算 RMSNorm 与 MLA 上投影</b></td>
    <td><b>arXiv 2412.19437</b> §3.2.3（重算）/ §3.3.3（低精度）/ §4.2（超参）</td></tr>
<tr><td>选择性重算的判据原文；GPT-3 省 70% 付 2.7%；MT-NLG 省 65% 付 1.6%</td>
    <td>Korthikanti 等，<b>arXiv 2205.05198</b></td></tr>
<tr><td><b>warmup 的真实机制</b>（让网络能承受更大的目标学习率；
    「样本太少估不准」<b>不是</b>主因；warmup 拉长收益边际）</td>
    <td><b>arXiv 2406.09405</b>（NeurIPS 2024）</td></tr>
<tr><td>Adam / AdamW / Adafactor / Lion 的原始定义</td>
    <td><b>1412.6980</b> / <b>1711.05101</b> / <b>1804.04235</b> / <b>2302.06675</b></td></tr>
<tr><td>Muon 只作用于二维参数、embedding 与输出头仍用 AdamW、
    per-step 墙钟比 AdamW 慢</td>
    <td>作者 writeup（<b>三条都是原文</b>）</td></tr>
</tbody></table>

<h3>7.2　⚠️ 自己推的（当量级看，别当阈值）</h3>
<table>
<thead><tr><th>数字</th><th>推导链</th><th>它可能错在哪</th></tr></thead>
<tbody>
<tr><td>激活 <b>__ACT_TOTAL__</b> → 重算后峰值 <b>__PEAK__</b>（4K）</td>
    <td>按算子逐项推（<a href="#s一">1.7</a> 那两张表）。输入只有 V3 的 config
        ＋ 官方参考实现的 MLA 前向</td>
    <td>框架的算子融合程度、MoE 派发是否真的物化九份</td></tr>
<tr><td>attention 占一层算力 82.1%</td>
    <td>按 causal 折半口径算。<b>⛔ 这里早先写「专题一用完全独立的另一套算法算出
        81.8%，两边对上了」——&nbsp;那句话是错的，已撤回。</b>
        <em>两边分子是<u>同一条公式</u>（S²·H·(d<sub>qk</sub>+d<sub>v</sub>)、同一份 config、
        同样 causal 折半），只有分母不同：专题一除整模型（含 lm_head），
        这里除一层的激活参数。差的那 0.463% 正是 lm_head 的稀释
        ——&nbsp;<code>81.8 ÷ (1−0.00463) = 82.18</code>。</em></td>
    <td>口径（折没折半）；换头维度就变。
        ⛔ <b>它是一次算术自检，不是外部锚点</b> ——&nbsp;两个数同源，对上是必然的。
        这个量目前<b>没有第三方实测可验</b></td></tr>
<tr><td>交叉点 <b>S ≈ __CROSS__</b></td>
    <td><code>1.25·S = __WIDEST__</code>（o_proj 的输入宽 ＝ 128 头 × 128）。
        1.25 来自 V3 头维度 192/128、causal 折半。
        ⛔ 早先这里分母写的是 d_model 7,168、交点报 5,734 ——&nbsp;
        <b>「最宽线性层 ＝ d_model」在 MLA 上不成立</b></td>
    <td>⛔ 换一条线性层做对照，交点就不同 ——&nbsp;<b>它不是工程阈值</b></td></tr>
<tr><td><b>6ND 低估五到六倍</b></td>
    <td>由上面那个 82.1% 反推（1 ÷ 0.179）</td>
    <td>它继承了 82.1% 的全部不确定性；<b>目前没有第三方实测佐证</b></td></tr>
<tr><td><b>9.76 TiB</b> 常驻块 · <b>约 __WS_SEQ__ 条</b> 4K 序列的分水岭</td>
    <td>671e9 × 16 B ÷ 1024⁴；再除以重算后峰值 __PEAK__。
        ⛔ <b>那个峰值必须含「正在重算的那一层」</b> ——&nbsp;
        只算存档点会把分水岭报成 __WS_SEQ_OLD__ 条，偏高 67%。<b>构建时带 assert</b></td>
    <td>16 B 是经典口径 ——&nbsp;<b>V3 自己就不是这么配的</b>（见 3.2）</td></tr>
<tr><td>「按每张卡 80 GiB 算要 125 张」</td>
    <td>9.76 TiB ÷ 80 GiB</td>
    <td>⛔ 80 GiB 是随手设的换算基准，不是某款具体的卡；
        真实部署还要算冗余、通信缓冲、碎片</td></tr>
</tbody></table>

<div class="note ok"><p>⭐⭐⭐ 为什么要专门写这一节。</p>
<p><em>因为这两类数字<b>长得一模一样</b> ——&nbsp;都是几位有效数字加一个单位。
  而读者会把它们<b>一起抄走</b>。</em></p>
<p class="landing">⭐ 判据：一个自己推出来的数，必须连着推导一起给；
  否则它冒充的是事实。</p>
<p><span class="sub">📌 顺带一条方法：上面 82.1% 那一行是这一讲最该学的做法
  ——&nbsp;它之所以可信，不是因为算得仔细，
  而是因为专题一用完全独立的另一套算法给出了 81.8%。
  <em>⭐ 孤立的估算没法验证；能对上一个外部锚点的估算，一次就把整套公式验了。</em></span></p></div>

</div></section>
'''

# ⛔ 每写完一章，把对应这一行整段删掉（连同正文里的占位符）。
PLAN = {
    # ✅ 第一节已写（2026-09-16）——&nbsp;loss→梯度的机制 ＋ 3× 的出处 ＋ 激活账单。
    #    ⛔ 还欠 fig4-3x / fig4-act-bill。
    # ✅ 第二节已写（2026-09-16）——&nbsp;重算全节（含 2022 年那个反转）。
    #    ⛔ 还欠 fig4-recompute / fig4-per-byte。
    # ✅ 第三节已写（2026-09-16）——&nbsp;3.1 / 3.2 / 3.3 / 3.4 ＋ fig4-optimizers。
    # ✅ 第四节已写（2026-09-16）——&nbsp;ZeRO 进正课：从账单推出三级顺序。
    # ✅ 第五节已写（2026-09-16）——&nbsp;总账 ＋ 两个峰 ＋ 6ND 的失效边界。
    #    ⭐ 五节全部写完，PLAN 已空 —— 这个 dict 留着是给下一轮加章用的。
}

# ⚠️ `make_head()` 只吐到 </style> 为止 ——&#160;`</head>` / `<body>` / 封面区
#   要自己接。（第一次没接，`add_figonly_toggle` 当场 assert 挂掉，
#   报的是「页面结构不对」——&#160;看着像装配问题，其实是缺标签。）
HERO = '''
</head>
<body>

<!-- ⛔ 这个文件由 Courses/tools/topic04-build.py 生成。
     **正文写在那个脚本的 BODY 常量里** ——&nbsp;改内容改那里，别改这个产物。 -->

<div class="hero"><div class="wrap">
  <div class="crumb"><a href="index.html">加速器系统课程</a> ／ 主线 ／ 专题四
    ／ <b>反向与优化器</b></div>
  <h1>反向与优化器</h1>
  <div class="en">Completing the Bill: Backward, Recompute, and the Optimizer</div>
  <div class="hook">
    模型读完一句话，猜错了下一个字。<b>这份「错得有多离谱」是<u>一个数</u>。</b><br>
    <em>——&nbsp;而训练要做的，是拿这<b>一个</b>数，
    去决定<b>__NPARAM_CN__个</b>参数各自该往哪边挪多少。</em>
  </div>
  <p style="max-width:820px;color:var(--gray)">
    这一讲只讲三件事：<b>这个数怎么算出来</b>（loss）、
    <b>一个数怎么变成 __NPARAM_CN__个导数</b>（反向传播）、
    <b>拿到导数之后那一步该迈多大</b>（梯度下降与优化器）。
    <em>——&nbsp;一条链，从一个数走到一次更新。</em>
  </p>
  <div class="chips">
    <span class="chip">前置 <b>专题一</b></span>
    <span class="chip">部分小节需要 <b>专题三</b></span>
    <span class="chip">后续 <b>专题五 · 并行策略</b></span>
    <span class="chip">读法 顺着一个 loss 值走一遍</span>
  </div>
  <p class="author">课程作者　<b>Chris Yang</b><span class="sep">·</span>Google Cloud
    AI Infra 架构师</p>
</div></div>

'''

FOOT = '''
<div class="wrap" style="padding:32px 0 64px">
  <p style="color:var(--gray)">
    ← 回 <a href="index.html">课程总纲</a>　·
    前向那半张账在 <a href="topic-01.html">专题一 · 一个 Token 的一生</a>　·
    这张账怎么切，在 <b>专题五 · 并行策略</b>（未上线）<br>
    本页由 <code>Courses/tools/topic04-build.py</code> 生成 ——&nbsp;
    <b>正文写在那个脚本里</b>。
  </p>
</div>

</body></html>'''

FIGS = {
    "__FIG_MUON__": ("fig-muon", "fig4-muon.svg", 'topic04-fig-muon.py',
        '⭐⭐⭐ <b>Ⓐ 的三列要<u>并排看</u></b> ——&nbsp;'
        '<em>分开讲是三个知识点，并排放你自己就看出来了：它们是同一个动作。</em><br>'
        '⭐⭐ <em>Ⓑ 用「迹」回答了「矩阵和向量到底差在哪」；'
        'Ⓒ 说明<b>换一把量尺就换一个优化器</b>；'
        'Ⓓ 解释了那十几次矩阵乘<b>为什么几乎不要钱</b>。</em><br>'
        '📌 <em>四条讲法都取自<b>苏剑林《Muon 优化器赏析》</b>（科学空间）——&nbsp;'
        '图是我们自己重画的。</em>'),
    "__FIG_ZERO__": ("fig-zero", "fig4-zero.svg", 'topic04-fig-zero.py',
        '⭐⭐⭐ <b>Ⓐ 是<u>同一根条被削掉三次</u></b> ——&nbsp;'
        '<em>虚线框就是切走的部分，右边那个 GB 是每张卡真正还要装下的量。</em><br>'
        '⭐⭐ <em>Ⓑ 解释了顺序：<b>「多大」乘以「多久用一次」</b>。'
        '优化器状态最大、又只在更新那一瞬间用 ——&nbsp;所以它先被切。</em><br>'
        '⭐ <em><b>能算的就别抄</b> ——&nbsp;抄来的数对不上时，'
        '你不知道是自己抄错了还是人家印错了；算出来的对不上，当场就报错。</em>'),
    "__FIG_GRADLIFE__": ("fig-gradlife", "fig4-gradlife.svg",
        'topic04-fig-gradlife.py',
        '⭐⭐ <b>看三条泳道，别只看第一条</b> ——&nbsp;'
        '<em>第一条是「做什么」，第二条是「<b>能不能跟计算叠在一起</b>」，'
        '第三条才是落点。</em><br>'
        '⭐⭐⭐ <em>第三条泳道<b>全是紫的</b> ——&nbsp;'
        '这一格是现场那条界（<b>推理里没有的那些东西</b>）最直观的一次落地。</em><br>'
        '⛔ <em>而五道里<b>只有裁剪那一道会把所有卡钉在一起等</b>，'
        '偏偏它传的只是一个数。</em>'),
    "__FIG_BATCH__": ("fig-batch", "fig4-batch.svg", 'topic04-fig-batch.py',
        '⭐⭐⭐ <b>先看那条线是<u>折的</u></b> ——&nbsp;'
        '<em>整张图的全部信息就在那个拐点上。</em><br>'
        '⭐ <em>看图的顺序建议：<b>先 Ⓐ 找到拐点，再 Ⓑ 对照两边各自成立什么</b>，'
        '最后 Ⓒ 看通信那条<b>为什么不长这样</b>。</em><br>'
        '⚠️ <em>这张图<b>一个实测数字都没有</b>，只画形状 ——&nbsp;'
        '<b>拐点落在哪，取决于你的模型、卡和并行配置，得自己量。</b></em>'),
    "__FIG_DESCENT__": ("fig-descent", "fig4-descent.svg",
        'topic04-fig-descent.py',
        '⭐⭐⭐ <b>这是全讲的本源那一格</b> ——&nbsp;'
        '<em>前面讲了梯度<b>怎么算出来</b>，这里讲<b>为什么顺着它走就能变好</b>。</em><br>'
        '⭐⭐ <em>Ⓐ 那两串球<b>是真跑了一遍梯度下降算出来的</b>，不是摆上去的：'
        '步子一步比一步小 ——&nbsp;<b>没人让它慢，是坡自己变平了</b>。</em><br>'
        '⛔ <em>而 A 那一串<b>落进了更浅的谷</b> ——&nbsp;'
        '<b>只因为它从左边出发。</b>看图时先比两个落点的高低，再回头看起点。</em>'),
    "__FIG_UPDRATIO__": ("fig-updratio", "fig4-updratio.svg",
        'topic04-fig-updratio.py',
        '⭐⭐⭐ <b>「学习率设对了吗」有一条能用眼睛对齐的线。</b>'
        '<em>量的是<b>更新量 ÷ 参数本身</b>（是更新量，不是梯度 ——&nbsp;原文特意强调），'
        'CS231n 给的经验值是 <b>1e−3</b>。</em><br>'
        '⭐⭐ <em>Ⓐ 三个学习率跑同一个网络：太小那条贴在黑线下、'
        '「刚好」那条<b>骑在黑线上</b>、太大那条远在黑线上方。</em><br>'
        '⛔ <em>Ⓑ 是这张图最该看的一格：<b>太大那条的 loss 反而更低</b> ——&nbsp;'
        '可它把权重<b>撑到 4.2 倍</b>、抖动大了约 9 倍，而学习率<b>再乘 2.1 就 NaN</b>。<br>'
        '⭐⭐⭐ 所以这条比值<b>不是「loss 最低」的指标，是「你离悬崖多远」的指标</b>'
        '——&nbsp;<b>诊断指标不是优化目标。</b></em><br>'
        '⚠️ <em>曲线是我们自己跑的两层 MLP（纯 numpy、固定种子）；'
        '「刚好」那个学习率是<b>扫出来的</b>，「再乘 2.1 就 NaN」是<b>二分搜出来的</b>。</em>'),
    "__FIG_UNDERFLOW__": ("fig-underflow", "fig4-underflow.svg",
        'topic04-fig-underflow.py',
        '⭐⭐⭐ <b>fp16 的毛病不是「不够细」，是「下不去」。</b><br>'
        '⭐⭐ <em>Ⓐ 一根对数轴上三条真边界：fp16 正规数下界 <b>6.10e−5</b>、'
        'fp16 归零线 <b>5.96e−8</b>（再往左就真是 0）、bf16 下界 <b>1.18e−38</b>。'
        '中间那一大片绿 ——&nbsp;<b>30 个数量级</b>，全是 bf16 存得下、fp16 存不下的。</em><br>'
        '⭐ <em>Ⓑ loss scaling 就是<b>在这根轴上整体右移</b>：×1024 ＝ 平移 3.01 格，'
        '一个本来归零的梯度就跨过红线活了。</em><br>'
        '⛔ <em>每个边界都是从 <code>numpy.float16</code> 的位模式当场算的，'
        '「归零」也是<b>真舍了一遍</b>验的；<b>没有直方图</b>'
        '——&nbsp;那份数据我们没有，不编。</em>'),
    "__FIG_PREPOST__": ("fig-prepost", "fig4-prepost.svg",
        'topic04-fig-prepost.py',
        '⭐⭐⭐ <b>左右两边是同一张图 ——&nbsp;连坐标都没动。</b>'
        '<em>变的只有底下那个结论框。</em><br>'
        '⭐⭐ <em>红线是 Post-LN：最初那个输入在第 l 层输出里只剩 '
        '<b>2<sup>−l/2</sup></b>，到第 24 层是 <b>1／4096</b>；'
        '绿线是 Pre-LN，一直是 1。</em><br>'
        '⭐ <em>Ⓐ 说这叫「残差名存实亡」，Ⓑ 说这正好替你按住了前面的层'
        '——&nbsp;<b>同一条曲线，预训练时是毛病，微调时是功能。</b></em><br>'
        '⛔ <em>画的是<b>前向</b>的残差直通项，<b>不是梯度倍率</b>'
        '——&nbsp;两者相关，但不是一回事。</em>'),
    "__FIG_VANISH__": ("fig-vanish", "fig4-vanish.svg",
        'topic04-fig-vanish.py',
        '⭐⭐⭐ <b>链式法则是连乘，而连乘是指数的。</b>'
        '<em>同一个 60 层网络，每层兑换率 0.8 还是 1.2 ——&nbsp;'
        '<b>只差两成，最靠输入那层拿到的梯度差了 10 个数量级。</b></em><br>'
        '⭐⭐ <em>Ⓐ 那条贴在轴上的红线<b>不是画漏了</b>，是真的小到画不出来；'
        'Ⓑ 得换一根对数轴才装得下这两头。</em><br>'
        '⭐ <em>Ⓒ 是这一格唯一能<b>当场算清楚</b>的那截原因：'
        'sigmoid 的导数最大只有 <b>0.25</b>（σ(1−σ) 的闭式最大值），'
        '<b>光这一下每层就先乘了个 ≤ ¼ 的数</b> ——&nbsp;'
        '而 ReLU 正半轴恒为 1。<b>深网络忽然训得动，就是这么来的。</b></em>'),
    "__FIG_SLIDER__": ("fig-slider", "fig4-slider.svg",
        'topic04-fig-slider.py',
        '⭐⭐⭐ <b>整讲的入口那一格</b> ——&nbsp;'
        '<em>导数、偏导数、链式法则，其实是同一个词：<b>兑换率</b>。</em><br>'
        '⭐⭐ <em>Ⓐ <b>导数问的不是「现在是多少」，是「你动一格，它动几格」。</b>'
        'Ⓑ「偏」字的全部含义就三个字 ——&nbsp;<b>按住不动</b>；'
        '而 __NPARAM_CN__个旋钮各自那个数排成一列，<b>那一列就叫梯度</b>。</em><br>'
        '⭐ <em>Ⓒ 链式法则就是<b>换汇</b>：人民币→港币→美元，'
        '每步一个汇率，<b>总汇率当然是乘出来的</b>。'
        '——&nbsp;而「从哪一头开始乘」代价差 __NAIVE_YEARS_CN__年，那是 1.5 的事。</em>'),
    "__FIG_VOTE__": ("fig-vote", "fig4-vote.svg",
        'topic04-fig-vote.py',
        '⭐⭐⭐ <b>全讲最口语的一张</b> ——&nbsp;'
        '<em>「梯度」这个词唬人，本意却只是<b>一屋子人各提各的要求，最后取个平均</b>。</em><br>'
        '⭐⭐ <em>Ⓑ 那句「<b>上游越亮的那根线，加粗越划算</b>」，'
        '就是 §1.6「反向必须用到前向的值」的人话版 ——&nbsp;'
        '<b>激活扔不掉，根子在这一句上。</b></em><br>'
        '⭐ <em>Ⓒ 只听一条样本的，网络会学会把什么都答成同一个字。'
        '<b>所以要平均 ——&nbsp;而多卡训练里那道「跨卡汇总」，干的就是这个平均。</b></em>'),
    "__FIG_LINEAGE__": ("fig-lineage", "fig4-lineage.svg",
        'topic04-fig-lineage.py',
        '⭐⭐⭐ <b>这不是四个并列的选项，是一条因果链</b> ——&nbsp;'
        '<em>每一环补上一环的洞，同时留下一个新的。</em><br>'
        '⭐⭐ <em>喂<b>同一串恒定的梯度</b>进去，健康的优化器应当给出<b>一条平线</b>；'
        '不平的就是它自己的毛病。<b>AdaGrad 的病在后期</b>（分母只增不减，'
        '有效学习率自己掉到稳态的 4%）；<b>RMSProp 的病在初期</b>'
        '（滑动平均从 0 起步，头一步被放大 32 倍）；'
        '<b>Adam 从第一步起就是平的</b>。</em><br>'
        '⭐ <em>这条链最强的证据来自 Adam 原文自己：'
        '<b>AdaGrad 就是 Adam 的一个特例</b>'
        '（β₁ 取 0、(1−β₂) 取无穷小、学习率按 t 的负二分之一次方退火）。</em>'),
    "__FIG_MOMENTUM__": ("fig-momentum", "fig4-momentum.svg",
        'topic04-fig-momentum.py',
        '⭐⭐⭐ <b>同一条 loss、同一个起点，只多给它一点惯性。</b>'
        '<em>灰的那条滚进半路那个浅坑就停了（<b>梯度真的归零，动不了</b>）；'
        '绿的那条冲过去，落进了后面更深的谷。<b>两条都是真滚出来的。</b></em><br>'
        '⭐⭐ <em>Ⓑ 是机关所在，而且比预想的更狠：在它刚越过坑底那一刻，'
        '<b>梯度其实是在把它往回拉</b> ——&nbsp;'
        '<b>是惯性以十几倍的优势把它带出去的。</b>箭头长度和方向都按真实数值画。</em><br>'
        '⛔ <em>但别读成「能找到全局最优」——&nbsp;'
        '<b>它冲得过去是因为那个坑够浅</b>。它降低的是「起点决定一切」的程度，不是消除它。</em>'),
    "__FIG_SADDLE__": ("fig-saddle", "fig4-saddle.svg",
        'topic04-fig-saddle.py',
        '⭐⭐⭐ <b>这张图拆掉上一张留下的那个阴影</b> ——&nbsp;'
        '<em>「掉进浅坑出不来」<b>是一维特有的画面</b>。</em><br>'
        '⭐⭐ <em>Ⓐ 记住<b>垭口</b>这一个词就够了：沿着翻山的路它是最高点，'
        '横过来沿山脊走它又是最低点 ——&nbsp;<b>坡度为零，却照样有路可走。</b></em><br>'
        '⭐ <em>Ⓑ 要当真谷底，<b>所有</b>方向都得朝上；'
        '几千亿个参数就是几千亿个方向，<b>漏一个它就只是垭口</b>。'
        'Ⓒ 实测也是这样：训练停下来的那些点，<b>一个真谷底都没遇到过</b>。</em><br>'
        '⛔ <em>但别读成「所以随便训」——&nbsp;<b>卡不住不等于走得快</b>，'
        '垭口附近坡太平，后面那些优化器一多半是在治这个。</em>'),
    "__FIG_ONELR__": ("fig-onelr", "fig4-onelr.svg",
        'topic04-fig-onelr.py',
        '⭐⭐⭐ <b>这张图替掉的是一句纯文字的断言</b> ——&nbsp;'
        '<em>「梯度尺度差好几个数量级」听着像句套话，'
        '<b>画出来就是红色那条弹着弹着飞出了画面。</b></em><br>'
        '⭐⭐ <em>Ⓑ 才是重点：<b>中间那个「折中」根本不存在。</b>'
        '在不发散的前提下能用的<b>最大</b>学习率，'
        '让平的那个方向走完一半也要几百步 ——&nbsp;'
        '<b>这不是调参没调好，是谷的形状决定的。</b></em><br>'
        '⭐ <em>两条轨迹都是<b>真跑出来的</b>，三个数都有 assert 盯着。</em>'),
    "__FIG_BESTSTEP__": ("fig-beststep", "fig4-beststep.svg",
        'topic04-fig-beststep.py',
        '⭐⭐⭐ <b>这张图回答的是「<u>为什么要除</u>」</b> ——&nbsp;'
        '<em>Ⓐ 在抛物线上，<b>一阶导 ÷ 二阶导正好等于「离底还有多远」</b>，'
        '所以最优的一步是算得出来的。</em><br>'
        '⭐⭐ <em>Ⓑ 最反直觉的一格：<b>梯度大不代表离得远</b> ——&nbsp;'
        '陡谷那个点梯度是 2.0，却只差 0.5。<b>不除以二阶导，两个参数根本不可比。</b></em><br>'
        '⭐ <em>Ⓒ 收回现实：二阶导在几千亿参数上算不起，'
        '所以自适应优化器<b>拿一阶导的历史去估它</b> ——&nbsp;'
        '<b>Adam 分母上那一坨就是这么来的。</b></em>'),
    "__FIG_CIRCUIT__": ("fig-circuit", "fig4-circuit.svg",
        'topic04-fig-circuit.py',
        '⭐⭐⭐ <b>这是整讲唯一一处「小到能用眼睛跟着数走」的地方</b> ——&nbsp;'
        '<em>同一条线上两个数：黑的是前向，红的是反向。</em><br>'
        '⭐⭐ <em>Ⓑ 那三个门不用背：<b>加法是分发器、乘法是交换器、max 是路由器</b>。'
        '记住脾气就能手推任何一张电路。</em><br>'
        '⛔ <em><b>Ⓒ 别跳过</b> ——&nbsp;乘法门反向要用前向的值，'
        '<b>而这就是整讲那张激活账单的源头</b>：'
        '激活不是框架顺手缓存的东西，是反向的数学要求它在场。</em>'),
    "__FIG_REVERSE__": ("fig-reverse", "fig4-reverse.svg",
        'topic04-fig-reverse.py',
        '⭐⭐⭐ <b>两排只差一件事：<u>种子插在哪一头</u></b> ——&nbsp;'
        '<em>而这一件事，就把代价从「 __NPARAM_CN__遍」压到「一遍」。</em><br>'
        '⭐⭐ <em>Ⓑ 里每个算子底下那个 ✓ 是关键：<b>回来的路上顺手就收下了</b>'
        ' ——&nbsp;不是走完再统一算。</em><br>'
        '⭐⭐⭐ <em><b>Ⓒ 是另一个看法</b>：反向那一遍<b>就是同一张网倒着走</b> ——&nbsp;'
        '同样的形状，只有三处换了。<b>第三处最要紧：每个节点从「套一个非线性」'
        '换成「乘一个常数」，而那个常数在前向就定死了。</b></em><br>'
        '⛔ <em>Ⓓ 的反例一定要看：<b>「反向传播更好」是条件成立的</b>，'
        '条件就是输入多输出少。</em>'),
    "__FIG_SPIKE__": ("fig-spike", "fig4-spike.svg", 'topic04-fig-spike.py',
        '⭐⭐⭐ <b>Ⓐ 只要看两条线的<u>形状差别</u></b> ——&nbsp;'
        '<em>上面那条扎满了尖，下面那条是平的。'
        '<b>而下面那条是同一套配方的小模型。</b></em><br>'
        '⭐⭐ <em>Ⓑ 是这一节存在的理由：<b>保险是开着的，事故照样发生</b>。'
        '——&nbsp;所以后面那些办法全是「绕过去」，没有一条是「根治」。</em><br>'
        '⚠️ <em>Ⓐ 那条曲线<b>纵轴上一个刻度都没有</b>，这是故意的 ——&nbsp;'
        '<b>没有刻度的曲线是示意，有刻度的才是数据。</b>'
        '论文给了前者的描述，没给后者的数。</em>'),
    "__FIG_STABILITY__": ("fig-stability", "fig4-stability.svg",
        'topic04-fig-stability.py',
        '⭐⭐ Ⓐ 按「治在链条的哪一步」排，不按「有哪些技巧」排 ——&nbsp;'
        '<em>技巧清单列完了，读者还是不知道<b>该先动哪个</b>。</em><br>'
        '⭐⭐⭐ <em>Ⓑ 是 PaLM 那个消融：同一批数据换个时刻喂就不飞，'
        '所以 spike 是数据和参数状态撞出来的，不是数据本身坏。</em><br>'
        '⛔ <em>Ⓒ 中间那根条看着完美（稳定 3/3），<b>可它把质量打穿了</b>。</em>'),
    "__FIG_STEP__": ("fig-step", "fig4-step.svg", 'topic04-fig-step.py',
        '⭐⭐⭐ <b>全专题的收口图</b> ——&nbsp;'
        '<em>前面四节各自那一块，第一次摆到<b>同一条时间轴</b>上。</em><br>'
        '⭐ <em>Ⓐ 要看的是<b>两条形状相反的带子</b>：'
        '蓝的前向堆、橙的反向堆 ——&nbsp;<b>它们的高点差了大半个 step</b>。</em><br>'
        '⭐⭐ <em>Ⓑ 三根条<b>全是当场算的</b>，脚本里带 assert ——&nbsp;'
        '改任何一个输入，对不上会自己报错。</em><br>'
        '📌 <em>梯度累积到底省什么、不省什么，<b>在 1.8 有完整的五条</b>。</em>'),
    "__FIG_3X__": ("fig-3x", "fig4-3x.svg", 'topic04-fig-3x.py',
        '⭐⭐ <b>Ⓑ 是这张图的钥匙</b> ——&nbsp;'
        '<em>反向不是「比较慢」，是它要回答<b>两个不同的问题</b>：'
        '我这块权重该怎么改、上游该收到什么。两个问题，两次乘法。</em><br>'
        '⛔ <em>Ⓐ 第②步那句「用前向存下来的输入」是整个专题的枢纽 ——&nbsp;'
        '激活扔不掉、以及下一节那笔交易，全挂在这一句上。</em>'),
    "__FIG_ACT_BILL__": ("fig-act-bill", "fig4-act-bill.svg",
        'topic04-fig-act-bill.py',
        '⭐⭐⭐ 横轴是时间，不是层号 ——&nbsp;'
        '<em>换成层号，「什么时候最挤」这个问题就提不出来了。</em><br>'
        '⭐ <em>山顶那一竖同时回答了第五节要问的「峰值在哪一刻」：'
        '前向刚算完、反向还没开始。</em>'),
    "__FIG_RECOMPUTE__": ("fig-recompute", "fig4-recompute.svg",
        'topic04-fig-recompute.py',
        '⭐⭐ <b>要看的是两根条的<u>不对称</u></b> ——&nbsp;'
        '<em>付出那边只多出一小截，省下那边短到几乎看不见。</em><br>'
        '⚠️ <em>两根条<b>都归一化了</b>，所以可以直接并排看 ——&nbsp;'
        '单位不同的量并排放，读者第一反应是去比长度，而那个比较没有意义。</em><br>'
        '⭐⭐⭐ <em>Ⓒ 换了<b>另一个轴</b>：Ⓑ 问的是「一层之内留哪些」，'
        'Ⓒ 问的是「<b>隔几层留一个</b>」。'
        '看那三个「同时在场」——&nbsp;<b>17 / 8 / 17，两头一样高。</b></em><br>'
        '⛔ <em><b>所以最优点在中间，而它落在 √L 上</b> ——&nbsp;'
        '这个结果的出处是 <b>Chen et al. 2016</b>（arXiv 1604.06174，'
        '标题就是结论：<i>Training Deep Nets with Sublinear Memory Cost</i>）：'
        '「an algorithm that costs <b>O(sqrt(n)) memory</b> to train a n layer network, '
        'with only the computational cost of <b>an extra forward pass</b>」。'
        '⚠️ ZeRO 论文（1910.02054 §3.2）也说过「降到大约总量的平方根、代价 33% 重算」，'
        '<b>但它是在转述这个结果，不是它的出处</b>。</em>'),
    "__FIG_PER_BYTE__": ("fig-per-byte", "fig4-per-byte.svg",
        'topic04-fig-per-byte.py',
        '⭐⭐⭐ <b>全专题最值钱的一张</b> ——&nbsp;'
        '<em>「两条斜率不同的线必然相交」这句话，图讲一秒，字讲一段。</em><br>'
        '⭐ <em>双对数不是为了好看：只有在这个坐标上，'
        '「常数」才是水平线、「正比于 S」才是直线。</em><br>'
        '⛔ <em>两根竖线就是那场翻转的全部原因 ——&nbsp;'
        '判据一个字没改，变的只有序列长度。</em>'),
    "__FIG_LR_CURVE__": ("fig-lr-curve", "fig4-lr-curve.svg",
        'topic04-fig-lr-curve.py',
        '⭐⭐ 这张图比的是形状，不是高度 ——&nbsp;'
        '<em>两个模型的峰值差 3.7 倍，<b>不归一化根本叠不到一起</b>。'
        '峰值的绝对值另有一份八档对照，在正文里。</em><br>'
        '⭐ <em>看两件事：<b>V3 那条长平台</b>，'
        '以及<b>两条曲线都落在同一根「10%」虚线上</b>。</em>'),
    "__FIG_PRECISION__": ("fig-precision", "fig4-precision.svg",
        'topic04-fig-precision.py',
        '⭐⭐⭐ <b>Ⓑ 是这一节的全部</b> ——&nbsp;'
        '<em>权重 1.0、每步加 0.0003，在 bf16 里连加<b>一千次</b>还是 1.0。'
        '<b>不是慢，是一步都没动。</b>换 fp32 同样一千次：1.0 → 1.3。</em><br>'
        '⭐⭐ <em>这些数<b>不是引来的，是脚本当场算的</b> ——&nbsp;'
        '里面有一个真的 bf16 舍入函数，三条 assert 盯着。你可以自己跑一遍。</em><br>'
        '⭐ <em>Ⓐ 顺带解掉一个常见误解：<b>fp16 要 loss scaling 跟「16 位不够」无关</b>'
        '——&nbsp;它跟 bf16 一样是 16 位，只是把 3 位从指数挪给了尾数，'
        '<b>于是范围小了，小梯度直接掉到 0。</b></em>'),
    "__FIG_OPTIMIZERS__": ("fig-optimizers", "fig4-optimizers.svg",
        'topic04-fig-optimizers.py',
        '⭐⭐ <b>Ⓐ 的形状是这张图的全部</b> ——&nbsp;'
        '<em>三十年一路往上加，<b>2024 年有人往回走了一步</b>。</em><br>'
        '⭐ <em>而 Ⓑ 把「最大的一块是优化器状态」翻译成了一根尺子：'
        '权重只占 2 B，优化器那边占 12 B。</em>'),
}

# ══════════════════════════════════════════════════════════════════
# §2.3 的排序表：**由闭式解生成，不手填**
# ⛔ 2026-09-17 红队抓到：这张表原来八行全是手打的，其中一行
#    「省显存 9.14 GiB × 每 GiB 付 7.70」算出来是 70 TFLOP，
#    「重算代价」那一栏却写着 39.08 —— 另外七行都对得上，就它不对。
# ⭐ 而这张表的卖点正是它下面那句「你拿 config 自己也能排一遍」。
#    照着排的人**会正好撞上那一行**，然后开始怀疑整条闭式解。
# ⭐⭐⭐ 判据：**一张宣称「按某条公式排出来」的表，就得真的用那条公式生成。**
#    手填的表里，「大部分行能对上」不构成任何保证 —— 错的那行长得跟对的一样。
# ══════════════════════════════════════════════════════════════════
GIB_F = 2 ** 30 / 1e12        # FLOPs/字节 → TFLOP/GiB
DTYPE_B = 2                   # bf16
S_BASE = 4096                 # ⭐ 本讲基准：V3 预训练真实序列长度（14.8T token 全在这档）
S_LONG = 131072               # 长上下文扩训（预训练之后各一千步）
S_RATIO = S_LONG // S_BASE    # ＝ 32，激活线性缩放的倍数
D_V3 = 7168                   # V3 的模型宽度（＝ 残差流的宽度，gate/up 的输入）
# ⛔ D_V3 原来的注释写的是「最宽的那个线性层的输入」——&#160;**在 MLA 上不成立**。
#    o_proj 吃的是 128 头 × 128 ＝ 16,384，比 d_model 宽一倍。
#    这正是「老约束在新架构上已被解除」的标准案例：MHA 里 heads × head_dim ＝ hidden，
#    MLA 里不是。交点、倍数、名次全都建在这个常数上，所以它单列并跟 §1.7 的表对账。
W_WIDEST_V3 = 128 * 128       # o_proj 的输入宽 ——&#160;全层最宽的线性层
N_ACT_L = 585.3e6             # 一层的激活参数量（MLA ＋ 被选中的 9 个专家）
H_V3, DQK_V3, DV_V3 = 128, 192, 128

def _step_tf(S):
    """一层跑完一个 step（前向 ＋ 反向 ≈ 3 遍）要多少 TFLOP。
    ⛔ 2,571 这个数原来是**空降的** —— 而「只多付 1.9%」整个结论拿它当分母。
       三位评审都点名了。现在它由 S 算出来，换基准自动跟着走。
    ⭐ 两项的 S 次数不同（线性 ∝ S，attention ∝ S²），**这正是翻转的来源**。"""
    lin = 2 * N_ACT_L * S
    att = S * S * H_V3 * (DQK_V3 + DV_V3)
    return 3 * (lin + att) / 1e12

def _att_share(S):
    lin, att = 2 * N_ACT_L * S, S * S * H_V3 * (DQK_V3 + DV_V3)
    return att / (lin + att)

# (名称, 省显存 GiB, 每字节 FLOPs, 结论)
#   线性层：每字节 FLOPs ＝ **输入宽度**（2·S·k·n ÷ (S·n·2)，S 和 n 全约掉）
#   逐元素：每元素约四五次运算，bf16 折下来 ≈ **2 次/字节**。
#     ⚠️ 这是个估算，所以两行**取同一个数** —— 它们之间分不出先后，
#     而且取 2 还是取 3，显示出来都是「~0」，排序也不变。
#     ⛔ 不要为了让表看起来精确而给它们编两个不同的值。
#   attention：随序列长度线性上升，V3 那套头维度下约 1.25·S
# (名字, 128K 上省多少 GiB, 每字节代价 FLOPs/B)
# ⛔ GiB 一列原来是**手填的 128K 常量**，而这一段正文的卖点是「你拿 config 自己也能排一遍」。
#    现在按基准缩放；每字节代价里只有 attention 带 S，其余是输入宽度、与 S 无关。
# ⭐⭐ 而这正是翻转的全部机制：**七行不动，一行在动。**
# ⛔⛔ 2026-09-19 T09：这张表原来**加起来对不上一层**。
#   八行合计 66.64 GiB，而一层 MoE 块是 71.14 ——&#160;缺 4.50。
#   评审报的是「9.14 那一行应该是 10.08」，但改完仍差 3.56 ——&#160;**缺的不止一行**。
#   ⭐ 根因：这张表原来是**手写的**，没有跟 §1.7 那张宽度表对过账。
#     漏掉的是 MLA 的两个 norm 输出和 Q 降维那一份 layernorm 输出（都是逐元素、pb≈2）。
#   ⭐⭐ 重导之后总和自动闭合：候选池 284,224 ＋ 入口 7,168 ＝ 291,392 ＝ 一层。
#     而候选池在 128K 上正好 69.39 GiB ——&#160;**跟正文早就写着的「可重算池 69.4」对上了**，
#     说明分母一直是对的，只是表少列了几行。
#   ⛔ 判据：**一张「把某个整体拆开」的表，必须断言它加回去等于那个整体。**
#
# (名字, 等效宽度, 每字节代价 ＝ 产生它的那个算子的输入宽度)
PER_BYTE_W = [
    ("MoE 派发（复制成 9 份）",                  64512, 0.0),
    ("logsumexp（attention 副产物，fp32）",        256, 0.0),
    ("RMSNorm 输出（3 份，逐元素）",              15872, 2.0),
    ("SwiGLU 乘积 9 份（逐元素）",                18432, 2.0),
    ("K/V 解压（输入宽 512）",                    32768, 512.0),
    ("Q 展开（输入宽 1,536）",                    24576, 1536.0),
    ("专家输出 9 份（输入宽 2,048）",              64512, 2048.0),
    ("gate / up / 路由 / 降维（输入宽 7,168）",    39744, float(D_V3)),
    # ⛔ 这一份原来跟另外三个 RMSNorm 输出捆在一起按「逐元素、白捡」计价（2026-09-19 红队查出）。
    #    它是 MLA 之后的残差流 ＝ o_proj 的输出 ——&#160;要重算它就得跑 o_proj，
    #    而 o_proj 的输入是 128 头 × 128 ＝ 16,384，**是这一层最宽的线性层输入**。
    #    ⭐ 反例一直印在 §1.7 那张表上（「attention 输出　128 头 × 128 ＝ 16,384」），
    #      隔了 100 行没对上账 ——&#160;所以下面那条 assert 直接跟 §1.7 的表绑死。
    ("MLA 后的残差流（＝ o_proj 输出，输入宽 16,384）", 7168, float(W_WIDEST_V3)),
    ("attention 输出",                           16384, None),   # None ＝ 1.25·S
]
ENTRY_W = 7168          # 入口那一份 ——&#160;重算模式下留的就是它，不在候选池里
_pool_w = sum(w for _, w, _ in PER_BYTE_W)
assert _pool_w + ENTRY_W == 291392, \
    "候选池 %d ＋ 入口 %d 加不回一层 MoE 块的 291,392" % (_pool_w, ENTRY_W)
CUT = 3.0        # 「比值小于 3 全收下」——&#160;正文里说清了这是我定的，不是算出来的


def _per_byte(S):
    """按序列长度 S 排一遍。⭐ 返回已按「每 GiB 付」升序排好的行。"""
    return sorted([(nm, w * DTYPE_B * S / 2 ** 30, 1.25 * S if pb is None else pb)
                   for nm, w, pb in PER_BYTE_W], key=lambda r: r[2])


def _verdict(ratio):
    """⛔ 原来「白捡/划算/边际/绝不」是**手写在每一行上**的，
       换个基准就会留下一行说谎的标签。现在从比值算出来。"""
    if ratio < 0.05:  return "白捡"
    if ratio < CUT:   return "划算"
    if ratio < 20.0:  return "边际"
    return "绝不"


_POOL_B = _pool_w * DTYPE_B * S_BASE

# ⭐ §1.6 主线要回答「一层里最大的是哪一项」——&#160;由这张表直接算，不手填。
#   （2026-09-20 第 8 轮开讲时补：原来主线把这一问整个推给了折叠块，
#    可下一节整节都在挑「该扔哪个」，不先说哪几项占地方就没法挑。）
#   ⛔ 我一开始以为大头是 attention，一算是 MoE 的九份复制 ——&#160;查了才没写错。
_L1 = {nm: w * DTYPE_B * S_BASE for nm, w, _ in PER_BYTE_W}
_L1["入口"] = ENTRY_W * DTYPE_B * S_BASE
_L1_TOT = sum(_L1.values())
_pc = lambda k: "%.0f%%" % (100.0 * _L1[k] / _L1_TOT)
_MOE_D_PCT = _pc("MoE 派发（复制成 9 份）")
_MOE_O_PCT = _pc("专家输出 9 份（输入宽 2,048）")
_MOE_S_PCT = _pc("SwiGLU 乘积 9 份（逐元素）")
_ATT_PCT   = _pc("attention 输出")
_MOE_ALL   = sum(_L1[k] for k in ("MoE 派发（复制成 9 份）",
                                  "专家输出 9 份（输入宽 2,048）",
                                  "SwiGLU 乘积 9 份（逐元素）"))
_MOE_ALL_PCT = "%.0f%%" % (100.0 * _MOE_ALL / _L1_TOT)
assert 48 < 100.0 * _MOE_ALL / _L1_TOT < 53, "九份复制不再占一半：%s" % _MOE_ALL_PCT
assert _L1["attention 输出"] < _L1["MoE 派发（复制成 9 份）"], "大头变成 attention 了？"

_ROWS_BASE = _per_byte(S_BASE)
_ROWS_LONG = _per_byte(S_LONG)

# ⭐⭐⭐ 这一讲最值钱的那个论断，写成断言：**同一条判据，两个基准给出不同的名次。**
_att_base = [r for r in _ROWS_BASE if r[0] == "attention 输出"][0]
_att_long = [r for r in _ROWS_LONG if r[0] == "attention 输出"][0]
assert _ROWS_LONG[-1][0] == "attention 输出", "128K 上 attention 应该排最后（最贵）"
assert _ROWS_BASE[-1][0] != "attention 输出", \
    "4K 上 attention 不该再是最贵的那一个 —— 翻转没发生，去查斜率"
assert abs(_att_base[2] * GIB_F - 5.50) < 0.01 and abs(_att_long[2] * GIB_F - 175.92) < 0.01
# ⛔ 但**别把话说过头**：5.50 仍然高于 CUT ＝ 3，所以按本讲自己那条线，
#    4K 上 attention 依然不进「收下」那一批。它从「绝不」变成「边际」，不是变成「该重算」。
assert _att_base[2] * GIB_F > CUT, "4K 上 attention 仍在切线之上 —— 这条前提变了就要改正文"
assert _verdict(_att_base[2] * GIB_F) == "边际" and _verdict(_att_long[2] * GIB_F) == "绝不"

_rows, _prev = [], -1.0
for _name, _gib, _pb in _ROWS_BASE:
    _cost, _ratio = _gib * _pb * GIB_F, _pb * GIB_F
    assert _ratio >= _prev, "排序坏了：%s" % _name
    _prev = _ratio
    _v = _verdict(_ratio)
    _c = "~0" if _cost < 0.005 else "%.2f TFLOP" % _cost
    _r = "0" if _pb == 0 else ("~0" if _ratio < 0.05 else "%.2f" % _ratio)
    _b = (lambda x: "<b>%s</b>" % x) if _v in ("绝不", "边际") else (lambda x: x)
    _rows.append("<tr><td>%s</td><td>%s</td><td>%s</td><td><b>%s</b></td><td>%s</td></tr>"
                 % (_b(_name), _b("%.3f GiB" % _gib), _b(_c), _r, _b(_v)))

_TBL = ('<table>\n<thead><tr><th>张量</th><th>省显存</th><th>重算代价</th>'
        '<th>每 GiB 付</th><th>结论</th></tr></thead>\n<tbody>\n'
        + "\n".join(_rows) + "\n</tbody></table>")

# ⭐ 正文里引用这张表的三个数，也回头对一遍 —— 表改了正文没跟着改，
#    是这类「生成表 + 手写正文」最典型的下一个坑。
_cheap = [r for r in _ROWS_BASE if r[2] * GIB_F < CUT]
_CHEAP_GIB = sum(r[1] for r in _cheap)
_CHEAP_TF = sum(r[1] * r[2] * GIB_F for r in _cheap)
_CROSS = W_WIDEST_V3 / 1.25       # attention 追平**最宽线性层**的序列长度
# ⭐ 正文里凡是「交点是多少、表有几行、attention 排第几」，一律从这里取 ——
#   这一节反复出问题的模式就是「表是生成的、正文是手写的」，所以不再留手写的数。
_NROWS      = len(PER_BYTE_W)
_WIDEST_PB  = W_WIDEST_V3 * GIB_F
_RANK_BASE  = [r[0] for r in _ROWS_BASE].index("attention 输出") + 1
_RANK_LONG  = [r[0] for r in _ROWS_LONG].index("attention 输出") + 1
assert _RANK_LONG == _NROWS, "128K 上 attention 该排最后一名"
assert _RANK_BASE < _NROWS, "4K 上 attention 不该排最后 —— 翻转没发生"

# ⭐ 选择性 vs 全量：两个百分比与它们的性价比之比，全部由基准算出来。
_STEP_TF   = _step_tf(S_BASE)
_SEL_PCT   = 100.0 * _CHEAP_TF / _STEP_TF          # 选择性重算多付百分之几
# ⛔ _FULL_SAVE 原来写死 97.0，而同一个脚本自己算出来的是 95.81 —— 这是全篇唯一一个
#    「C 类手填常量」真的填错的地方（2026-09-19 审计查出）。它还撑着对外的「性价比 __EDGE__ 倍」。
#    改成从峰值占比推，跟 fig-recompute 用同一个口径。
_FULL_PCT  = 100.0 / 3.0          # 全量重算多付：在 fwd+bwd=3 上再加一次 fwd
# _FULL_SAVE / _EDGE 依赖 _PEAK_PCT，在下面峰值那一块算完之后才定义。
_SEL_SAVE = 100.0 * _CHEAP_GIB / (_POOL_B / 2 ** 30)   # 选择性换掉了池子的百分之几
# _EDGE 见下方峰值块之后（它依赖 _FULL_SAVE）。
# attention 跟它前一档的倍数 —— 128K 上最贵，4K 上它已经不在最后。
# ⛔ 这两个数原来是 22.857 / 0.714，因为分母用了 7,168。分母换成真正最宽的 16,384 之后
#    是 10.0 / 0.3125 ——&#160;代码本来就是算出来的，错的只有常数和正文。
_TIMES_LONG = _ROWS_LONG[-1][2] / _ROWS_LONG[-2][2]
_ATT_VS_WIDEST = _att_base[2] / float(W_WIDEST_V3)   # 4K 上 attention ÷ 最宽线性层
assert _ROWS_LONG[-2][2] == float(W_WIDEST_V3), \
    "128K 上第二贵的那行应该就是最宽线性层，现在是 %.0f" % _ROWS_LONG[-2][2]

for _nm, _got, _want in (("128K attention ÷ 最宽线性层", _TIMES_LONG, 10.0),
                         ("4K attention ÷ 最宽线性层", _ATT_VS_WIDEST, 0.3125),
                         ("4K 选择性占比", _SEL_PCT, 9.30),
                         ("一层一步 TFLOP", _STEP_TF, 16.45),
                         ("4K attention 占前向", 100 * _att_share(S_BASE), 12.53),
                         ("128K attention 占前向", 100 * _att_share(S_LONG), 82.11)):
    assert abs(_got - _want) < 0.02, "%s 算出 %.3f，期望 %.3f" % (_nm, _got, _want)

# ══════════════════════════════════════════════════════════════════
# ⭐⭐⭐ §1.7 的两张激活表 —— 由「张量宽度」生成，序列长度只是一个变量。
#
# ⛔ 原来两张表是**手写的 GiB 数**，钉死在 S ＝ 131,072 上。现场定的基准是
#    **4K**（V3 预训练真实配置），一改就要手工改十几个数、还要改小计和总计 ——
#    那正是「改一处忘三处」的产地。
# ⭐ 判据：**一个数如果是算出来的，就让它在构建时算，不要让它在正文里躺着。**
#    改成这样之后，切基准只动 S_BASE 一个数，而 128K 的旧值成了回归锚点。

# (说明, 宽度怎么来的, 等效宽度, 每元素字节)
_MLA_ROWS = [
    ("norm 的输入（＝ 残差入口，<b>重算模式下唯一留的那份</b>）", "7,168", 7168, 2),
    ("norm 的输出", "7,168", 7168, 2),
    ("Q 降维结果（layernorm 前后各一份）", "2 × 1,536", 3072, 2),
    ("KV 降维结果（含 RoPE 那 64 维）", "576 ＋ 512", 1088, 2),
    ("<b>Q 展开后</b>", "128 头 × 192 ＝ <b>24,576</b>", 24576, 2),
    ("<b>K、V 解压后</b>", "128 头 × 256 ＝ <b>32,768</b>", 32768, 2),
    ("attention 输出", "128 头 × 128 ＝ 16,384", 16384, 2),
    ("logsumexp（fp32）", "128", 128, 4),
]
_MOE_ROWS = [
    ("norm 的输入 / 输出", "2 × 7,168", 14336, 2),
    ("路由分数", "256", 256, 2),
    ("<b>派发出去的激活</b>", "<b>9 份</b> × 7,168", 64512, 2),
    ("gate / up / SwiGLU 乘积", "3 × 9 份 × 2,048", 55296, 2),
    ("<b>专家输出（合并前）</b>", "<b>9 份</b> × 7,168", 64512, 2),
]
_DENSE_FFN_W = 3 * 18432 + 14336      # dense 层的 FFN 部分（MLA 那半边共用）
N_MOE_L, N_DENSE_L = 58, 3

def _bytes(rows, S):
    return sum(w * b for _, _, w, b in rows) * S

def _sz(nbytes):
    """⭐ 4K 上很多项不到 1 GiB —— 硬写 GiB 会变成一串 0.02，读者读不出量级。"""
    g = nbytes / 2 ** 30
    if g >= 1:
        return "%.2f GiB" % g
    m = nbytes / 2 ** 20
    # ⛔ 整数 MiB 会把 8.5 显示成 8（差 6%）—— 小数点后那一位在小项上是承重的
    return ("%.1f MiB" % m).replace(".0 MiB", " MiB") if m < 100 else "%.0f MiB" % m

def _act_tbl(rows, S, head2, total_label):
    body = "".join('<tr><td>%s</td><td>%s</td><td>%s</td></tr>' % (n, w, _sz(ww * bb * S))
                   for n, w, ww, bb in rows)
    return ('<table><thead><tr><th>留下的张量</th><th>%s</th><th>大小</th></tr></thead>'
            '<tbody>%s<tr><td><b>%s</b></td><td></td><td><b>约 %s</b></td></tr>'
            '</tbody></table>' % (head2, body, total_label, _sz(_bytes(rows, S))))

_MLA_B   = _bytes(_MLA_ROWS, S_BASE)
_MOE_B   = _bytes(_MOE_ROWS, S_BASE)
_LAYER_B = _MLA_B + _MOE_B
_DENSE_B = _MLA_B + _DENSE_FFN_W * DTYPE_B * S_BASE
_TOTAL_B = N_MOE_L * _LAYER_B + N_DENSE_L * _DENSE_B

# ⛔ 回归锚点：同一套宽度在 128K 上必须复现页面发布过的那些数。
#   ⭐ 这才是「换基准」和「算错了」的分水岭 —— 没有它，改完只能靠感觉。
for _nm, _got, _want in (
        ("MLA 小计",   _bytes(_MLA_ROWS, S_LONG) / 2 ** 30,                       22.58),
        ("MoE 小计",   _bytes(_MOE_ROWS, S_LONG) / 2 ** 30,                       48.56),
        ("一层 MoE 块", (_bytes(_MLA_ROWS, S_LONG) + _bytes(_MOE_ROWS, S_LONG)) / 2 ** 30, 71.14),
        ("全模型",     (N_MOE_L * (_bytes(_MLA_ROWS, S_LONG) + _bytes(_MOE_ROWS, S_LONG))
                        + N_DENSE_L * (_bytes(_MLA_ROWS, S_LONG)
                                       + _DENSE_FFN_W * DTYPE_B * S_LONG)) / 2 ** 40, 4.145)):
    assert abs(_got - _want) < 0.01, \
        "128K 锚点对不上：%s 算出 %.3f，发布过的是 %.3f —— 宽度表被改坏了" % (_nm, _got, _want)

# 注意力分数矩阵是 S² 项，**不跟着 token 总数走** —— 它是唯一一个换基准时
# 要按平方缩的量。⭐ 这正是 §1.7 边界②该讲清楚的地方。
# ⛔⛔ 全量重算的峰值 ＝ 存档点 ＋ **当前正在重算的那一层的完整激活**。
#   原来只算了存档点（61 × 入口那一份 ＝ 106.75 GiB@128K），漏掉后面那一项 ——
#   而 §5.4 的 125M 例子和 §2.2 图 Ⓒ 用的都是**正确**口径，只有 671B 这个头号数字没做。
#   ⭐ 三位评审各自算出同一批数。这是全篇唯一一个**照抄会把集群配小 67%** 的数。
_CKPT_B     = (N_MOE_L + N_DENSE_L) * 7168 * DTYPE_B * S_BASE   # 每层只留入口那一份
_INFLIGHT_B = _LAYER_B                       # 在算的那一层，取 MoE 块（最坏情况）
_PEAK_B     = _CKPT_B + _INFLIGHT_B
_RECOMP_X   = _TOTAL_B / _PEAK_B             # 全量重算省多少倍
_PEAK_PCT   = 100.0 * _PEAK_B / _TOTAL_B
_FULL_SAVE = 100.0 - _PEAK_PCT    # 全量重算省掉的比例 —— 跟 fig-recompute 同一口径
assert abs(_FULL_SAVE - 95.81) < 0.05, "全量重算省的比例变了：%.2f" % _FULL_SAVE
_EDGE = (_SEL_SAVE / _SEL_PCT) / (_FULL_SAVE / _FULL_PCT)   # 选择性 vs 全量 的性价比之比
RESIDENT_B  = 671e9 * 16                     # 常驻块（每参数 16 字节）
_WS_SEQ     = RESIDENT_B / _PEAK_B           # 激活追平常驻需要多少条序列
_WS_TOK     = _WS_SEQ * S_BASE

# §1.6 的对照。⛔ 这里原来写的是「133 GiB 比整个模型的权重还大一截」——
#   方向反了：权重 bf16 是 1,250 GiB，133 GiB 只有它的九分之一（2026-09-19 红队查出）。
#   真正成立、而且更有力的说法是「两者涨得不一样」：权重固定，激活按 batch 线性叠。
#   所以这里算的是交叉点，不再拿两个孤立的数硬比大小。
_W_BF16_B  = 671e9 * DTYPE_B                 # 只有权重，不含梯度和优化器状态
_ACT_XOVER = _W_BF16_B / _TOTAL_B            # 激活追平**权重**要多少条 4K 序列
assert _W_BF16_B > _TOTAL_B, "激活反而比权重大了？先查 _TOTAL_B 的口径"
assert 9.0 < _ACT_XOVER < 10.0, "激活追平权重的 batch 变了：%.2f" % _ACT_XOVER

for _nm, _got, _want in (("存档点@128K",  _CKPT_B * S_RATIO / 2 ** 30,     106.75),
                         ("在算层@128K",  _INFLIGHT_B * S_RATIO / 2 ** 30,  71.14),
                         ("峰值@128K",    _PEAK_B * S_RATIO / 2 ** 30,     177.89),
                         ("省多少倍",     _RECOMP_X,                        23.86),
                         ("峰值占比",     _PEAK_PCT,                         4.19),
                         ("分水岭条数",   _WS_SEQ,                        1798.6),
                         ("分水岭 token", _WS_TOK / 1e6,                     7.37)):
    assert abs(_got - _want) < max(0.02, abs(_want) * 0.001), \
        "%s 算出 %.3f，期望 %.3f" % (_nm, _got, _want)

# ⭐⭐ §5.4 那句「比例跟 671B 上那笔几乎一样」是错的（6.7 倍 vs 23.9 倍）。
#   ⛔ 但光改掉它是浪费 ——&#160;这两个数为什么不一样，本身是条能带走的规律：
#        省多少倍 ＝ r ÷ (1 + r/L)     r ＝ 一层激活 ÷ 入口那一份，L ＝ 层数
#   分子是「一层里挂了多少东西」，分母那一项是「在算的那一层摊到每层头上」。
#   ⭐ 小模型**两项都吃亏**：r 小 2.7 倍，L 小 5 倍。所以它省得少是必然的，不是算错。
SMALL_L, SMALL_D = 12, 768
SMALL_LAYER_W = 2 * 768 + 3 * 768 + 768 + 3072 + 3072 + 768      # ＝ 11,520
_R_SMALL = SMALL_LAYER_W / float(SMALL_D)                         # ＝ 15.0
_R_V3    = (_LAYER_B / (DTYPE_B * S_BASE)) / 7168.0               # ＝ 40.7
_x_small = _R_SMALL / (1 + _R_SMALL / SMALL_L)
_x_v3    = _R_V3 / (1 + _R_V3 / (N_MOE_L + N_DENSE_L))
for _nm, _got, _want in (("小模型 r", _R_SMALL, 15.0), ("V3 r", _R_V3, 40.66),
                         ("小模型省几倍", _x_small, 6.67), ("V3 闭式估", _x_v3, 24.395)):
    assert abs(_got - _want) < 0.02, "%s 算出 %.3f，期望 %.3f" % (_nm, _got, _want)
# ⚠️ 闭式给 V3 是 24.4，实测 23.9 ——&#160;差在那 3 层 dense 比 MoE 薄。
#   写进正文时说「约」，不要拿闭式冒充精确值。
assert abs(_x_v3 - _RECOMP_X) < 0.6

# ⭐⭐⭐ §1.0 的「loss 刻度尺」——&#160;现场点名要讲清「loss 到底是怎么算的」。
#   ⛔ 全讲原来一次都没出现过「困惑度」「负对数」「对数似然」，
#      而 §6 整节在讲「loss 飞了」——&#160;读者手上**没有尺**，不知道飞到多少算飞。
#   ⭐ 刻度尺只用一个恒等式：困惑度 ＝ e^loss，不需要引任何人的实测值。
V_VOCAB = 129280                      # V3 词表（专题一核过：词表矩阵 129,280 × 7,168）
_L_RANDOM = math.log(V_VOCAB)         # 均匀瞎猜时的 loss ＝ ln V ＝ 11.77
assert abs(_L_RANDOM - 11.7697) < 1e-3

_SCALE = [
    (_L_RANDOM, "刚初始化，还没学任何东西",
     "在整个词表里均匀瞎猜 ——&nbsp;<b>这就是 loss 的上限</b>"),
    (6.0,  "学会了最基本的字频",  "相当于在几百个词里犹豫"),
    (4.0,  "语法大致对了",        "几十个词"),
    (3.0,  "像模像样的文本",      "二十个词上下"),
    (2.0,  "现代大模型预训练末期<b>大致落在这一档</b>", "七八个词"),
    (1.0,  "几乎能背出来了",      "两三个词 ——&nbsp;<b>正常预训练到不了这里</b>"),
]
_TBL_SCALE = ('<table><thead><tr><th>loss</th><th>困惑度 ＝ e<sup>loss</sup></th>'
              '<th>大概是什么水平</th><th>「在几个词之间犹豫」</th></tr></thead><tbody>'
              + "".join(
                  # ⭐ 只有最上面那一行是**算出来的**（ln 129,280）；其余四档是按
                  #   公开模型的量级填的。原来它们长得一模一样，而这一讲自己的
                  #   招牌就是「算出来的和估出来的要分得清」——&#160;所以标出来。
                  ('<tr><td><b>%s</b>%s</td><td>%s</td><td>%s</td>'
                   '<td><em>%s</em></td></tr>')
                  % ("%.2f" % v if abs(v - _L_RANDOM) < 1e-6 else "%.1f" % v,
                     ' <span class="sub">算出来的</span>'
                     if abs(v - _L_RANDOM) < 1e-6
                     else ' <span class="sub">量级</span>',
                     format(int(round(math.exp(v))), ","), what, feel)
                  for v, what, feel in _SCALE)
              + '</tbody></table>')

# ⛔⛔ §1.7 那两张表是**逐层**的，所以它们漏了一样不属于任何一层的东西：
#   最后那个投影吐出的 **logits**，形状 [S, 词表]。三位评审里有一位专门点了它。
#   ⭐ 它在 4K 上比表里最大的单个张量（K/V 解压 256 MiB）还大四倍。
_LOGITS_B      = S_BASE * V_VOCAB * DTYPE_B
_LOGITS_B_LONG = S_LONG * V_VOCAB * DTYPE_B
assert abs(_LOGITS_B / 2 ** 30 - 0.986) < 0.002

_ATTN_SCORE_B = 128 * S_BASE ** 2 * DTYPE_B
_ATTN_SCORE_LONG = 128 * S_LONG ** 2 * DTYPE_B

# ⭐ §4.1 那张「按大小排一遍」的表 —— 由常量生成，不手填。
#   ⛔ 这一节原来是张空支票：正文写着「这个顺序本身就是这一节的全部内容」，
#      然后**一张表一张图都没有**；而 4.2 第一句就是「正是照着这个顺序来的」。
#      四位评审各自撞到它。
N_PARAM = 671e9               # V3 总参数（激活参数是 37B，但显存按总量算）
# ⛔ 正文原来一律写「三千亿个参数」——&#160;源文件 29 处、渲染后 45 处，
#   而 V3 是 6,710 亿（激活 37B），**哪个口径都对不上**（2026-09-19 审计查出）。
#   它还坐在页面大标题和 §1 的标题里。⭐ 这一讲自己的招牌就是
#   「不标场景的数字没有意义」，所以不能拿一个约数当标题用。
#   现在由 N_PARAM 驱动，换模型就自动跟着走。
_NPARAM_CN = format(round(N_PARAM / 1e8), ",") + " 亿"
# ⭐ 笨办法要算多少年 ——&#160;原来是手算完手填「两万年」，跟「所有数由脚本算」
#   这条规矩不一致（2026-09-20 第 3 轮开讲时发现：讲的时候我当场又算了一遍）。
#   一次前向按 1 秒；一年 3.156e7 秒（365.25 天）。
_SEC_PER_YEAR  = 3.156e7
_NAIVE_YEARS   = N_PARAM / _SEC_PER_YEAR
_NAIVE_YEARS_CN = "两万" if 15000 < _NAIVE_YEARS < 25000 else "%.0f" % _NAIVE_YEARS
assert 21000 < _NAIVE_YEARS < 21500, "笨办法年数变了：%.0f" % _NAIVE_YEARS
assert _NPARAM_CN == "6,710 亿", _NPARAM_CN
TIB = 1024 ** 4
# 每参数的字节数 —— 拆法与 §3.1 一致（bf16 权重 2、bf16 梯度 2、
# fp32 主权重 4 ＋ 一阶矩 4 ＋ 二阶矩 4 ＝ 12）
LEDGER = [
    ("优化器状态", 12, "fp32 主权重 4 ＋ 一阶矩 <i>m</i> 4 ＋ 二阶矩 <i>v</i> 4",
     "只在<b>更新那一瞬间</b>用一次"),
    ("权重",        2, "bf16 那一份，前向反向都拿它算", "每一层都要读"),
    ("梯度",        2, "bf16，跟权重同形", "反向写、更新后就能扔"),
]
_LED_B = sum(r[1] for r in LEDGER)
_pct = lambda v: ("%.1f" % v).rstrip("0").rstrip(".")   # 12.5 要保留，75.0 要收掉
_rows = "".join(
    '<tr><td><b>%s</b></td><td class="num">%d B</td>'
    '<td class="num"><b>%.2f TiB</b></td><td class="num">%s%%</td>'
    '<td>%s</td><td><em>%s</em></td></tr>'
    % (nm, b, N_PARAM * b / TIB, _pct(100.0 * b / _LED_B), how, when)
    for nm, b, how, when in LEDGER)
_TBL_LEDGER = (
    '<table class="tbl"><thead><tr>'
    '<th>这一项</th><th class="num">每参数</th><th class="num">671B 上</th>'
    '<th class="num">占常驻</th><th>它是什么</th><th>什么时候用得上</th>'
    '</tr></thead><tbody>%s'
    '<tr class="sum"><td><b>常驻小计</b></td><td class="num"><b>%d B</b></td>'
    '<td class="num"><b>%.2f TiB</b></td><td class="num">100%%</td>'
    '<td colspan="2"><em>⭐ 跟 batch、跟序列长度<b>都无关</b> ——&nbsp;'
    '只要模型定了，这个数就定了。</em></td></tr>'
    '<tr><td><b>中间激活</b></td><td class="num">——</td>'
    '<td class="num"><b>看 batch</b></td><td class="num">——</td>'
    '<td>反向要用的那一堆中间结果（<a href="#s1-6">1.6</a>）</td>'
    '<td><em>⛔ 唯一一个<b>你自己能调</b>的 ——&nbsp;'
    '也是唯一一个能反超上面三项的</em></td></tr>'
    '</tbody></table>'
    % (_rows, _LED_B, N_PARAM * _LED_B / TIB))

_html = head + HERO + BODY + FOOT
_html = _html.replace("__TBL_PER_BYTE__", _TBL)
_SUBS = {}          # ⭐ 记下每一次替换，供 place_figs 之后补跑一遍
for _ph, _val in (("__ATT_PB_BASE__", "%.2f" % (_att_base[2] * GIB_F)),
                  ("__ATT_PB_LONG__", "%.2f" % (_att_long[2] * GIB_F)),
                  ("__ATT_VS_W__",    "%.0f%%" % (100 * _ATT_VS_WIDEST)),
                  ("__TIMES_LONG__",  "%.0f" % _TIMES_LONG),
                  ("__CHEAP_GIB__",   "%.2f GiB" % _CHEAP_GIB),
                  ("__CHEAP_TF__",    "%.2f TFLOP" % _CHEAP_TF),
                  ("__STEP_TF__",     "%.1f TFLOP" % _STEP_TF),
                  ("__SEL_PCT__",     "%.1f%%" % _SEL_PCT),
                  ("__SEL_SAVE__",    "%.0f%%" % _SEL_SAVE),
                  ("__EDGE__",        "%.1f" % _EDGE),
                  ("__W_BF16__",      format(_W_BF16_B / 2 ** 30, ",.0f")),
                  ("__ACT_XOVER__",   "%.0f" % _ACT_XOVER),
                  ("__WIDEST__",      format(W_WIDEST_V3, ",")),
                  ("__CROSS__",       format(int(round(_CROSS)), ",")),
                  ("__CROSS_K__",     "%.0fK" % (_CROSS / 1024)),
                  ("__NROWS__",       str(_NROWS)),
                  ("__WIDEST_PB__",   "%.2f" % _WIDEST_PB),
                  ("__RANK_BASE__",   str(_RANK_BASE)),
                  ("__RANK_LONG__",   str(_RANK_LONG)),
                  ("__NPARAM_CN__",   _NPARAM_CN),
                  ("__NAIVE_YEARS__", format(int(round(_NAIVE_YEARS)), ",")),
                  ("__NAIVE_YEARS_CN__", _NAIVE_YEARS_CN),
                  ("__MOE_D_PCT__",   _MOE_D_PCT),
                  ("__MOE_O_PCT__",   _MOE_O_PCT),
                  ("__MOE_S_PCT__",   _MOE_S_PCT),
                  ("__MOE_ALL_PCT__", _MOE_ALL_PCT),
                  ("__ATT_PCT__",     _ATT_PCT)):
    assert _ph in _html, "正文里没有 %s" % _ph
    _html = _html.replace(_ph, _val)
    _SUBS[_ph] = _val
_html = _html.replace("__TBL_LEDGER__", _TBL_LEDGER)
_html = _html.replace("__TBL_SCALE__", _TBL_SCALE)
_html = _html.replace("__LOGITS__", _sz(_LOGITS_B))
for _ph, _val in (("__R_SMALL__", "%.0f" % _R_SMALL), ("__R_V3__", "%.1f" % _R_V3)):
    assert _ph in _html, "正文里没有 %s" % _ph
    _html = _html.replace(_ph, _val)
    _SUBS[_ph] = _val
for _ph, _val in (("__PEAK__",        _sz(_PEAK_B)),
                  ("__CKPT__",        _sz(_CKPT_B)),
                  ("__INFLIGHT__",    _sz(_INFLIGHT_B)),
                  ("__RECOMP_X__",    "%.1f" % _RECOMP_X),
                  ("__UNDER__",       "%.0f%%" % (100 * (1 - _CKPT_B / _PEAK_B))),
                  ("__WS_SEQ_OLD__",  "%.0f" % (RESIDENT_B / _CKPT_B)),
                  ("__WS_SEQ__",      "%.0f" % _WS_SEQ),
                  ("__WS_TOK__",      "%.0f 万" % (_WS_TOK / 1e4))):
    assert _ph in _html, "正文里没有 %s" % _ph
    _html = _html.replace(_ph, _val)
    _SUBS[_ph] = _val
_html = _html.replace("__TBL_ACT_MLA__",
                      _act_tbl(_MLA_ROWS, S_BASE, "宽度", "小计"))
_html = _html.replace("__TBL_ACT_MOE__",
                      _act_tbl(_MOE_ROWS, S_BASE, "份数 × 宽度", "小计"))
for _ph, _val in (
        ("__ACT_NINE__",   _sz(2 * 64512 * DTYPE_B * S_BASE)),   # 派发 ＋ 专家输出
        ("__ACT_LAYER__",  _sz(_LAYER_B)),
        ("__ACT_DENSE__",  _sz(_DENSE_B)),
        ("__ACT_TOTAL__",  _sz(_TOTAL_B)),
        ("__ATTN_SCORE__", _sz(_ATTN_SCORE_B))):
    assert _ph in _html, "正文里没有 %s" % _ph
    _html = _html.replace(_ph, _val)
    _SUBS[_ph] = _val

assert abs(_CROSS - 13107) < 1, "交叉点变了：%.0f" % _CROSS
# ⛔ 这两条原来钉的是 5,734（分母用了 d_model）。钉住一个错的值，assert 反而变成了保险箱 ——
#    所以现在改成钉「交点 ＝ 最宽线性层 ÷ 1.25」这条**关系**，外加宽度本身跟 §1.7 的表对账。
assert abs(_CROSS * 1.25 - W_WIDEST_V3) < 1e-6, "交点和最宽线性层脱钩了"
assert "5,734" not in _html.replace("早先写的是 <b>5,734</b>", "").replace(
    "交点报 5,734", ""), "正文里还留着旧交点 5,734"
# ⛔ 128K 锚点。⚠️ 省显存那个数 T09 从 53.50 变成 55.69 ——&#160;**这是修正不是漂移**：
#   旧表漏了 MLA 的两个 norm 输出和 Q 降维那份 layernorm 输出（都是 pb≈2 的白捡项）。
#   ⭐ 算力代价 48.95 TFLOP **一点没变**，因为补上的几行每字节代价几乎为零。
# ⛔⛔ 2026-09-19 再修一次：55.69 → 53.94。少掉的 1.75 GiB 就是「MLA 后的残差流」那一份 ——
#   它原来跟三个 RMSNorm 输出捆在一起按逐元素计价（白捡），实际重算它要跑 o_proj（输入宽 16,384），
#   比值 17.59 远在切线之外，所以它不该进「收下」那一批。算力代价 48.95 依旧没变（同样因为
#   它旧价几乎为零）。⭐ 这两次都是**修正**：省显存那一栏两次都在动，算力那一栏两次都没动。
assert abs(_CHEAP_GIB * S_RATIO - 53.94) < 0.05 and abs(_CHEAP_TF * S_RATIO - 48.95) < 0.1, \
    "「比值小于 3 全收下」对不上 128K 锚点：%.2f / %.2f" % (_CHEAP_GIB * S_RATIO, _CHEAP_TF * S_RATIO)
assert abs(_SEL_SAVE - 77.8) < 0.1, "选择性换掉池子的比例变了：%.1f%%" % _SEL_SAVE

# ⭐ §4.1 那张表跟散在正文里的那几个数必须对得上。
#   ⛔ 它们分处两节、相隔三千行 —— 正是「改了一处忘了另一处」的经典产地，
#      而这一讲自己已经因为讲义没跟着改而被评审抓到过一次。
assert _LED_B == 16, "每参数字节数不是 16 了：%d —— §3.1 的拆法改了？" % _LED_B
for _n, _v, _want in (("优化器状态", 12, 7.32), ("权重", 2, 1.22),
                      ("常驻小计", _LED_B, 9.76)):
    _tib = N_PARAM * _v / TIB
    assert abs(_tib - _want) < 0.005, \
        "§4.1 的表算出 %s ＝ %.3f TiB，可正文里写的是 %.2f" % (_n, _tib, _want)
assert "权重 1.22 ＋ 梯度 1.22 ＋ 优化器状态 <b>7.32</b>" in _html, \
    "§5.1 那句拆分被改过了 —— §4.1 的表要跟着核一遍"

# ⛔ 标签配对：全课落点那个 <h3> 的开标签曾经整个丢了，只剩 </h3>。
#   后果不是「样式不对」——是**全讲的落点渲染成裸正文、不进目录、锚点不存在**，
#   而页面照样构建通过、照样上线。四位评审各自看见它，我自己读了几十遍没看见。
#   ⭐ 判据：**会静默上线的错，必须让它在构建时炸。**
for _tag in ("h2", "h3", "section", "div"):
    _o = len(re.findall(r"<%s[ >]" % _tag, _html))
    _c = _html.count("</%s>" % _tag)
    assert _o == _c, (
        "<%s> 开 %d 个、闭 %d 个 —— 差 %d。"
        "少一个开标签的话，那一段会当成正文渲染，构建不会报错，但目录里它不存在。"
        % (_tag, _o, _c, _c - _o))

for ph, (what, figs, asks) in PLAN.items():
    assert ph in _html, "正文里没有 %s —— 章写完了要连这一行一起删" % ph
    _html = _html.replace(ph, todo(what, figs, asks))

# ⭐ 这一份是**要投屏讲的**，所以装折叠开关，默认只剩图。
_html = P.place_figs(_html, FIGS)

# ⛔⛔ place_figs 在上面那几轮替换**之后**才把图注注入，所以图注里的占位符
#   一轮都没被替换到 ——&#160;它们原样漏进了成品。
#   ⭐ 而原来的断言只查「替换前在不在」（`assert _ph in _html`），
#     **不查「替换后还剩没剩」** ——&#160;于是 __NPARAM_CN__ 在线上漏了半天没人发现。
#   （2026-09-20 第 3 轮开讲时撞到：图注里写「代价差 __NAIVE_YEARS_CN__年」。）
#   判据：**替换类操作要同时有前置断言和后置断言。** 只查前置，
#   等于只保证「我动手了」，不保证「动干净了」。
for _ph, _val in _SUBS.items():
    _html = _html.replace(_ph, _val)
_leak = sorted(set(re.findall(r"__[A-Z][A-Z_0-9]*__", _html)))
assert not _leak, "占位符没落地，漏进成品：%s" % "、".join(_leak)
_html = P.add_figonly_toggle(_html)
# ⛔⛔ 2026-09-20：T12 换主线时改了 SECTIONS 的标题，**却没改正文里的 <h2>** ——
#   而吸顶目录是从 <h2> 长出来的，于是目录上挂了一整套旧标题，一挂就是八轮。
#   ⭐ 判据：**同一个东西有两份写法时，必须断言它们一致** ——&#160;
#     「改一处忘另一处」不会报错，它只是静默地展示旧的那份。
_h2 = dict(re.findall(r'<section id="(s[^"]+)"><div class="wrap"><div class="stn">'
                      r'<span class="badge">[^<]*</span><h2>(.*?)</h2>', _html, re.S))
for _sid, _num, _title in SECTIONS:
    _strip = lambda t: re.sub(r"<[^>]+>", "", t).replace("&nbsp;", " ").strip()
    # ⭐ SECTIONS 里的标题还带着占位符（它在替换发生前就被 TOC 用掉了），
    #   所以比较前要对它做同一次替换 ——&#160;否则这条断言会把「已正确替换」误报成不一致。
    _want_raw = _title.replace("__NPARAM_CN__", _NPARAM_CN)
    _got, _want = _strip(_h2.get(_sid, "")), _strip(_want_raw)
    assert _got == _want, \
        "SECTIONS 跟正文 <h2> 对不上：%s\n  SECTIONS：%s\n  正文：%s" % (_sid, _want, _got)
print("   ✅ SECTIONS 与正文 %d 个 <h2> 逐字一致" % len(SECTIONS))

P.finish(_html, OUT, SECTIONS, "topic-04.html")

# ⛔ 站内锚点必须落地 —— **查的是写出去的那份成品**，不是中间变量。
#   小节 id 是 P.finish() 生成 tocbar 时才补上的，在它之前查会全军覆没。
#   ⭐ 这一条防的是：交叉引用指向一个**真实存在但内容不相干**的小节
#      （「6.4 那个清单」实际在 3.7、「1.2 那三条边界」实际在 1.7）。
#      死链会被发现，跳错不会 —— 所以它比死链更该由机器盯。
_final = io.open(OUT, encoding="utf-8").read()   # OUT 已是完整文件路径
_ids = set(re.findall(r'\bid="([^"]+)"', _final))
_bad = sorted({h for h in re.findall(r'href="#([^"]+)"', _final)
               if h and not h.startswith("!") and h not in _ids})
# ⭐⭐ 上面那段注释说这条断言「防的是跳错」——&#160;可它只查了 id 在不在，
#    跳错一个字都拦不住（2026-09-19 红队查出六处：五处「3.3」实际在 3.3b，
#    一处「2.6」实际在 2.5）。现在把那句话真的实现出来：
#    **凡是链接文字本身就是节号的，它必须落在同号的那一节上。**
_CN = dict(zip("零一二三四五六七八九", range(10)))
_titles = dict(re.findall(r'\bid="(s[^"]+)"[^>]*>(.{0,18})', _final))
_mis = []
for _href, _txt in re.findall(r'href="#(s[^"]+)"[^>]*>(.*?)</a>', _final, re.S):
    _t = re.sub(r"<[^>]+>", "", _txt).strip()
    if not re.fullmatch(r"\d+(?:\.\d+[a-z]?)?", _t):
        continue                                  # 「上面那张图」这类文字链不查
    _m = re.fullmatch(r"s(\d+)-(\d+[a-z]?)", _href)
    if _m:                                        # 指向小节 → 号必须一模一样
        if "%s.%s" % _m.groups() != _t:
            _mis.append("%s → #%s（同粒度但跳到了别的小节）" % (_t, _href))
    elif re.fullmatch(r"s[零一二三四五六七八九]", _href):
        if _CN[_href[1]] != int(_t.split(".")[0]):   # 指向整章 → 至少章号要对
            _mis.append("%s → #%s（连章都不对）" % (_t, _href))
assert not _mis, "节号链接跳错：\n  " + "\n  ".join(sorted(set(_mis)))

assert not _bad, (
    "这些站内锚点指向不存在的 id：%s\n"
    "⭐ 小节 id 形如 s3-7 / s6-6，节级形如 s六。" % "、".join(_bad))
print("   ✅ 站内锚点 %d 个全部落地" % len(re.findall(r'href="#', _final)))

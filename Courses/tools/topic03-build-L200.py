# -*- coding: utf-8 -*-
r"""专题三 · 注意力演进 —— **主线 L200**（要拿上讲台的那一份）。

════════════════════════════════════════════════════════════════════
⭐⭐⭐ 这一份跟 L300 的分工：**体裁不同，不是长短不同**
════════════════════════════════════════════════════════════════════
2026-09-14 现场原话（这段是这份文件的宪法，改任何东西之前先读一遍）：

  「把现在的这个专题三改成专题三的 L300。然后从 L300 里边一点一点地蒸馏，
    要这个最好最好的内容，要最精华的部分。然后写一个专题三。
    **重点就是要写一篇完整的故事**，不要像现在这种，就是东一句西一句，
    然后说的你确实是很重点，但是是这个**没有上下文**，对吧？
    你动不动搞了一句谁说的、很著名的一句话、一件事情，就**不是很连贯**。」

  「整个的故事就是从 RNN 开始讲起，然后变成了 Transformer，Transformer 又有
    什么问题。然后呢，之后又出现了长上下文的需求……**每一个阶段谈到某一个
    注意力的时候，就把它展开讲，就按时间线穿起来。**」

  「总的原则是**只要图，尽量少写文字**。」
  「一定要让大家懂得这里边的道理，**尽量要说人话，说大白话**，
    就像台大李宏毅老师那样说大白话。」
  「**这是一本书，是一本讲技术的小说一样。** 一开始就是要有文学素质，
    就好像一个写书写得很好的老师一样。」

⭐ 落成三条可执行的判据：

  ① **每一章只回答一个问题：上一章欠下了什么，这一章拿什么还。**
     ⛔ 一段话如果换个位置照样成立，它就不属于这份 —— 它属于 L300。
     ⭐ 反过来：一句话如果**只在「按时间读」时才成立**，它就是这一份的血肉。

  ② **先有图，再有字。** 写任何一段正文之前先问：这句话图里有没有？
     有就别写第二遍。图注和落点带**是图的一部分**。
     （这条是 2026-09-12 立的老规矩，L300 那边一字不改地照搬过来。）

  ③ **不要「名人名言式」的孤立事实。** 「某某论文说过某某话」这种句子，
     除非它正好是这一步的**动机**或**代价**，否则一律降到 L300 的出处折叠里。
     ⛔ 这正是现场点名的病：单看每句都很重点，连起来没有上下文。

════════════════════════════════════════════════════════════════════
📌 文件名与产物（2026-09-14 R62 定）
════════════════════════════════════════════════════════════════════
  · 本文件            →  `topic-03.html`          ← **正牌专题三，文件名不带 L200**
  · topic03-build-L300.py →  `topic-03-L300.html`  ← 档案版

⛔ 现场明确要求过：「那个文件名不用写 L200，就写那个正常的这个专题三就行。」

════════════════════════════════════════════════════════════════════
⭐ 图从哪来
════════════════════════════════════════════════════════════════════
L300 手上已经有 48 个 `topic03-fig-*.py`、58 张图。这一份**不是重画一套**，
是从那批里挑、并借这次机会**一张一张往精致里改**（现场：「那个图要越画越精致，
就是从现有的 L300 里边抽出来，然后放到 L200 里边，然后来借机优化一下」）。

⛔ 所以图脚本仍然只有一套，两页共用同一批 `fig3-*.svg`。
  改图时要意识到 **L300 那边也会跟着变** —— 这是有意的：
  同一张图在两页上长得不一样，是比「两页各有一张差不多的图」更糟的债。

════════════════════════════════════════════════════════════════════
🚧 当前状态：**骨架（R62）** —— 故事脊梁已立，九章待写
════════════════════════════════════════════════════════════════════
每一章下面现在只有两样东西：**这一章在故事里干什么**，和**它打算用哪几张图**。
⭐ 先把这个给现场看，是因为**脊梁错了，后面每一章都是白写的**。
⛔ 一章一章往里填；填完一章就把该章的「🚧 待写」块整段删掉。
"""
import os

import topic03_page as P
import topic03_family as FAM

HERE = P.HERE
WEB = P.WEB
OUT = os.path.join(WEB, "topic-03.html")

head = P.make_head(
    "专题三 · 注意力演进",
    "注意力演进 · 一条链，被剪断，又绕了回来",
    "从 RNN 讲到今天的混合配比 —— 按时间线穿起来的一个完整故事：每一步都只回答"
    "「上一步欠下了什么，这一步拿什么还」。多图、少字、说大白话。",
    "topic-03.html")

# ⭐ 节号与标题写死在这儿，跟正文里的 <section id> 对齐。
# ⛔ 加节 / 改标题时**两处一起改**。
# ⭐⭐ 标题要说这一节**在故事里干什么**，不是说它讲哪个名词 ——
#   这条是 2026-09-14 从 L300 §三 那个「已经是标配，所以这一讲不展开它」
#   学来的：一个承重的小节挂了一块「无事可看」的牌子。
SECTIONS = [
    # ⭐⭐ 2026-09-16 现场：「把注意力编年史挪到全篇的第 0 节去，
    #   要让大家先有一个概念，最后再收尾，这是头尾呼应用。」
    # ⛔ 图**本来就已经在开篇**（2026-09-14 挪的），缺的是**名分**：
    #   它待在一个没编号、没 badge、**不在目录里**的块里（原 id="x1"）。
    #   ⭐ 于是读者不知道「有这么一节」，也就不会在最后想起来要滚回去。
    # ⭐ 判据：**首尾呼应要成立，那个「首」得先是一个<u>能被指认</u>的位置。**
    ("s零", "零", "先把这八年整个摊开看一眼"),
    ("s一", "一", "一条链 —— 最早的模型是怎么记事的"),
    ("s二", "二", "2017 年那一刀 —— 把链剪断，换成一张表"),
    ("s三", "三", "账单到期 —— 上下文一长，那张表就付不起了"),
    ("s四", "四", "第一次省：把头砍掉 —— MQA 砍过了头，GQA 停在半路"),
    ("s五", "五", "第二次省：干脆不存 K 和 V —— MLA 存的是一份压缩件"),
    ("s六", "六", "换个方向：不是存得少，是别全读 —— 从滑窗到 DSA"),
    ("s七", "七", "绕回原点 —— 还是那块固定大小的记事板"),
    ("s八", "八", "谁也赢不了，那就都要 —— 混合配比"),
    ("s九", "九", "落到机器上 —— 以及那 512 倍是怎么换来的"),
]


def todo(what, figs, asks):
    """一章的「🚧 待写」块。⛔ 这一章写完就把对它的调用整段删掉。"""
    return ('<div class="note warn"><span class="t">🚧 本章待写 ——&nbsp;%s</span>'
            '<p><b>这一章在故事里干什么：</b>%s</p>'
            '<p><b>打算用的图</b>（从 L300 抽，逐张再精修）：<code>%s</code></p></div>'
            % (what, asks, "</code> · <code>".join(figs)))


BODY = '''<section id="s零"><div class="wrap"><div class="stn"><span class="badge">第 零 节</span><h2>先把这八年整个摊开看一眼</h2></div>

<!-- ⭐⭐⭐ 2026-09-16 现场定的全课主线：「人类的大脑怎么思考 —&#160;
     模型什么样、人脑什么样、差别在哪、从里面学到了什么、又产生了什么。
     RNN 比较像人脑的思考方式，Transformer 很像机器的思考方式；
     一个省电省地方，一个快、并行 ——&#160;这也正是机器和人的区别。」
     ⭐ 开场图就用这张，它把两边**摆在同一个机架上**。
     ⛔⛔ 而它最好的用处**不是那个笑点，是那三个数** ——&#160;
       三个里只有一个经得起查。开场第一分钟就把本课的规矩演一遍，
       比讲十句「要核实」都管用。 -->
<figure class="fbox fwide" id="fig-brain-rack">
  <img src="img/brain-rack.jpg" alt="一个数据中心的机架上躺满了人，屏幕显示 1 ExaFLOPS/Brain、MFU 19%、20W/Brain"
       style="width:100%;height:auto;border-radius:8px;display:block">
  <figcaption>⭐⭐ <b>这一讲会一直并排跟着两条线：<u>模型是怎么记事的</u>，和<u>人是怎么记事的</u>。</b>
    <em>——&nbsp;因为这一路省下来的每一招，最后都在向同一个方向靠。</em><br>
    ⛔ <b>而这张图上有三个数，只有一个经得起查 ——&nbsp;先把它们查一遍，这就是本课的规矩。</b></figcaption>
</figure>

<div class="note danger"><p>⛔⛔ <b>开场第一分钟，先把这三个数审一遍。</b></p>
<ul>
  <li><b>20 W／脑 ——&nbsp;<u>站得住</u>。</b><em>而且有生理学依据：
    大脑占体重约 <b>2%</b>，却占掉<b>两成</b>的代谢。<b>十几到二十瓦，是个能引用的数。</b></em></li>
  <li><b>1 ExaFLOPS／脑 ——&nbsp;<u>不能当数用</u>。</b><em>大脑「等效算力」的公开估计
    <b>横跨 10¹² 到 10²⁸</b>，<b>十六个数量级</b>。
    1 ExaFLOPS 只是这条巨大区间里的<b>一个点</b>，不是共识。</em></li>
  <li><b>MFU 19% ——&nbsp;<u>纯属玩笑</u>。</b><em>MFU 的分母是<b>芯片的标称峰值</b>，
    而<b>大脑没有 spec sheet</b>。这个数连定义都不成立。</em></li>
</ul>
<p>⭐⭐ <em>——&nbsp;<b>一张图上三个数，一个能引用、一个只能当量级、一个根本不成立。</b>
  <b>这一讲从头到尾都这么读数。</b></em></p></div>

<p class="landing">⭐⭐⭐ <b>而这张图真正要立的，是这一讲的主线：</b>
  <em><b>RNN 那条路，像人脑的思考方式；Transformer 那条路，像机器的思考方式。</b><br>
  一个<b>省电、省地方</b>，一个<b>快、能并行</b> ——&nbsp;
  <b>而这也正是人和机器的区别本身。</b></em></p>

<p><b>2020 年的 GPT-3，一次只能记住 2048 个 token。</b>那时候它是个很会接话的
  补全器 ——&nbsp;你给一段，它接一段，接得挺像样。</p>
<!-- ⛔ 这一句原来跟 fig-arc 落点带里那串活计逐字撞了（查重抓到）。
     ⭐ 图负责列清单，正文负责**定调子** ——&nbsp;两边不该长一个样。 -->
<p>今天你让它干的活完全变了：读完一整个代码库再回头改对一处 bug、
  一口气走完几十步的工具链、还得<b>记得住你半小时前改过主意</b>。
  <em>2K 的上下文，连一个文件都读不完。</em></p>

<div class="note ok"><p>⭐⭐ <b>上下文长度就是模型的工作记忆。</b>
  记不住，就什么都干不成。<br>
  ⛔ 而每记住一分，要付的钱是实打实的显存 ——&nbsp;
  模型每吐一个字都要回看前面所有字，于是把每个字算出来的 K、V 存着不重算，
  存下来的这一堆就叫 <b>KV cache</b>，它跟着上下文线性长。<br>
  ⭐ 所以这六年注意力的全部演化，只在做一件事：<b>让「记得住」这件事付得起。</b></p></div>

<!-- ⭐⭐⭐ 2026-09-16 现场：「我们之前准备了两道题，怎么给搞丢了？」
     ⭐ 查下来**没丢也没被删** ——&#160;它们一直在 L300 里；是
       `67f3626`「原稿降格为 L300、新起 L200 骨架」那次，**新骨架是空白起的，
       没把这两道题带过来**。现在原样搬回主线。
     ⛔ 判据（值得单记）：**改架构时最容易丢的不是代码，是那些不在主线叙事里、
       却单独有价值的小块。** git 里按那个 CSS 类名做 -S 搜索只命中一个 commit ——
       就是那次拆分。重起炉灶之前，先列一遍「旧稿里有什么是新骨架接不住的」。
     ⛔⛔ **位置必须在那张模型表「之前」** ——&#160;
       表里有 KV cache 那一列，摆在题前面等于直接把第一题的答案送出去。
     ⛔⛔ 2026-09-16 补：第一版**把绑定脚本落下了** ——&#160;
       它在原稿里是 `</details>` **之后**一行，而我按 `</details>` 切片就停了。
       后果是**选项点不动**（题目显示完全正常，所以肉眼看不出来）。
       ⭐ 更该记的是：我当时在这条注释里写了「脚本一起搬」——&#160;
       **那是没验证就写下的断言**，而 build 回执全绿。
       判据（原注释里早就有，我又犯一次）：**交互要点过才算验过。**
       .guess/.opts/.rev 的样式来自共用样式表，不用另加。
     📌 揭晓规则：**全对才开**；全选但没全对 → 报「N 对 M 错」，不揭晓；
       答案区里有「↺ 重来」。这三条的来由见脚本内注释。 -->
<details class="more preclass">
  <summary>⛔ 课前勿点 ——&nbsp;开讲前的两道热身题<span class="why">现场会一起做；提前看答案＝自己剧透，这两道题就废了</span></summary>
  <div class="body">

  <div class="guess">
    <h3>第一题 · 一个用户的 KV cache，到底有多大</h3>
    <p class="q"><b>DeepSeek V3</b>（671B，61 层，128 个注意力头，head_dim 128），
      <b>128K 上下文、单个用户、bf16 存</b>。<br>
      <span class="qs">同样这个形状，换四种注意力，KV cache 各是多大？
      <b>四行分开选，各选各的。</b></span></p>

    <div class="oplab">(a) 最朴素的 <b>MHA</b>（128 个 KV 头）</div>
    <div class="opts">
      <button data-g="0">61 GiB</button>
      <button data-g="1">122 GiB</button>
      <button data-g="2" data-right>488 GiB</button>
      <button data-g="3">976 GiB</button>
    </div>

    <div class="oplab">(b) <b>GQA-8</b>（KV 头砍到 8）</div>
    <div class="opts">
      <button data-g="0">3.8 GiB</button>
      <button data-g="1">8.6 GiB</button>
      <button data-g="2" data-right>30.5 GiB</button>
      <button data-g="3">61 GiB</button>
    </div>

    <div class="oplab">(c) <b>MQA</b>（KV 头砍到 1）</div>
    <div class="opts">
      <button data-g="0" data-right>3.8 GiB</button>
      <button data-g="1">8.6 GiB</button>
      <button data-g="2">15.3 GiB</button>
      <button data-g="3">30.5 GiB</button>
    </div>

    <div class="oplab">(d) <b>MLA</b>（V3 真实用的方案）</div>
    <div class="opts">
      <button data-g="0">3.8 GiB</button>
      <button data-g="1" data-right>8.6 GiB</button>
      <button data-g="2">30.5 GiB</button>
      <button data-g="3">61 GiB</button>
    </div>

    <div class="rev">
      <p><b>488　/　30.5　/　3.8　/　8.6　GiB。</b><br>
        <span class="qs">算法就一条：<b>每 token 每层要留下几个数</b>，
        乘 61 层、乘 2 字节、乘 131,072 个 token。<br>
        MHA ＝ 2×128×128 ＝ 32,768 →&nbsp;<b>488 GiB</b>；
        GQA-8 ＝ 2×8×128 ＝ 2,048 →&nbsp;<b>30.5</b>（16×）；
        MQA ＝ 2×1×128 ＝ 256 →&nbsp;<b>3.8</b>（128×）；
        MLA ＝ 压缩维 512 ＋ RoPE 64 ＝ 576 →&nbsp;<b>8.6</b>（56.9×）。<br>
        ⭐⭐ <b>488 GiB 到底是多大？</b>换算成机器就有感觉了：按专题二推的
        可分配口径（<b>94.74 GiB／device</b>）算，488 ÷ 94.74 ＝
        <b>5.2 个 device</b>，也就是<b>约 2.6 块 v7 芯片</b>。<br>
        <b>一个用户、一段输入，就要把两块半芯片的 HBM 整个拿来放 KV
        ——&nbsp;而模型权重还一个字节都没放进去。</b><br>
        📌 <b>RoPE</b>（旋转位置编码）后面会反复出现，这里先把它的职责摆正：
        <b>「谁在前谁在后」并不归它管。</b>那件事是<b>因果掩码</b>白送的
        ——&nbsp;每个位置只看得见自己左边，多堆几层就能数出自己前面有几个人
        （<b>NoPE</b>，arXiv 2305.19466，NeurIPS 2023 ——&nbsp;
        直接证明了 decoder-only 不加任何显式位置编码也学得会顺序）。<br>
        ⭐ RoPE 真正加进来的是另一样东西：<b>「我跟它差几格」</b>。
        给每个位置的 Q 和 K <b>按它的位置转一个角度</b>，点积时两个绝对角度相减，
        <b>相对距离就直接出现在打分里</b>——&nbsp;零参数，而且每一层都能用，
        不必靠堆层去数。
        ⭐ 它只改 Q/K，不改 V。<b>后面 §五会讲它给 MLA 惹的麻烦</b>——&nbsp;
        上面那 64 维的成本，买的就是这一件事。</span></p>
      <p>⭐⭐ 这道题真正的题眼在 <b>(c) 和 (d) 的大小关系</b>：
        <span class="qs">MQA 只要 3.8 GiB，比 MLA 的 8.6 还小 2.25 倍。
        <b>MLA 并不是最省的那个。</b><br>
        ⭐ 所以这一支的目标从来不是「谁存得最少」——&nbsp;
        MQA 早在 2019 年就把它压到头了，代价是<b>质量掉得厉害</b>。
        真正要比的是「同样一份字节，换回多少能力」。
        <em>这正是第五节要讲的那条线。</em></span></p>
      <p style="margin-bottom:0"><b>⚠️ 还有一个口径要说清：</b>
        <span class="qs">488 GiB 是「假如 V3 用 MHA」的<b>反事实</b>数字，
        不是 V3 的实测值 ——&nbsp;V3 从第一天就是 MLA。
        而且这里沿用了 V3 论文比较表的口径（K、V 都按 head_dim=128 算）；
        V3 真实的 K 每头是 128+64＝192 维，严格算这个基线还会更大一点。</span></p>
    </div>
  </div>

  <div class="guess" style="margin-top:22px">
    <h3>第二题 · 那条「算得过来还是搬得过来」的线</h3>
    <p class="q">上一讲那把尺子：<b>算力 ÷ 带宽</b> ——&nbsp;
      每从内存搬一个字节，这台机器配套能算多少次。<br>
      <span class="qs">在 <b>TPU v7</b> 上，<b>两层各是多少？两行分开选。</b></span></p>

    <div class="oplab">(a) 对 <b>HBM</b>（片外）</div>
    <div class="opts">
      <button data-g="0">78</button>
      <button data-g="1">156</button>
      <button data-g="2" data-right>约 313</button>
      <button data-g="3">约 626</button>
    </div>

    <div class="oplab">(b) 对 <b>VMEM</b>（片上）</div>
    <div class="opts">
      <button data-g="0">跟 HBM 一样，约 313</button>
      <button data-g="1">比 HBM <b>高</b>，约 3,000</button>
      <button data-g="2" data-right>比 HBM <b>低</b>一个量级 ——&nbsp;几十这一档</button>
      <button data-g="3">片上没有「屋脊点」这回事</button>
    </div>

    <div class="rev">
      <p><b>(a) 约 313。(b) 比 HBM 低一个量级，落在几十这一档。</b><br>
        <span class="qs">(a) ＝ 2,307 TFLOP/s ÷ 7.37 TB/s。
        这个数不是本讲新造的，它就是<a href="topic-02.html">专题二</a>整整一节在立的那条屋脊线。</span></p>
      <p><b>⭐ (b) 最容易选反，而选反的人通常是把「快」和「门槛高」搞混了：</b>
        <span class="qs">片上更快，所以<b>分母变大</b> ——&nbsp;
        同一个分子除以更大的分母，<b>商只会更小</b>。
        <b>越靠近计算，这条线越低。</b>
        <em>门槛低意味着：同一个算子挪到片上以后，更容易变成算力受限。</em></span></p>
      <p style="margin-bottom:0">⛔ 而 (b) 为什么只给量级、不给数 ——&nbsp;<b>这才是这道题最想教的一件事</b>：
        <span class="qs"><b>VMEM 的带宽官方没有公开。</b>
        而屋脊点乘以算力就等于带宽 ——&nbsp;给出一个精确的屋脊点，
        等于把那个没公开的数反推出来。所以我们到「几十这一档」为止，<b>不往下猜</b>。<br>
        ⭐ 这正是<a href="topic-02.html">专题二</a>第 6 节那四句问法里的一句：
        先问这个数的出处和口径，再用它。
        <em>而那四句不是拿来审别人材料的 ——&nbsp;是先拿来审自己的。</em></span></p>
    </div>
  </div>

  </div>
</details>
<script>
/* 与专题二同一份实现；三条坑（别用 id、等 DOMContentLoaded、按 .opts 分组）
   的来由写在上面那段注释里。 */
(function(){
  function bind(){
  document.querySelectorAll('.guess').forEach(function(box){
    var rev=box.querySelector('.rev'); if(!rev) return;
    var groups=box.querySelectorAll('.opts');
    var rst=document.createElement('div');
    rst.className='rst';
    rst.innerHTML='<button type="button">\u21ba 重来</button>';
    rst.firstChild.addEventListener('click', function(){
      box.querySelectorAll('button').forEach(function(x){x.classList.remove('picked','right');});
      rev.classList.remove('on');
      var hh=box.querySelector('.hint');
      if(hh){ hh.textContent=''; hh.classList.remove('on'); }   /* ⛔ 别忘了连提示一起清 */
      box.scrollIntoView({behavior:'smooth', block:'nearest'});
    });
    rev.insertBefore(rst, rev.firstChild);
    /* ⛔⛔ 2026-09-12 改揭晓规则（现场点的）：
         ① **全对才开**；② 全选了但没全对 → 报「N 对 M 错，继续努力」，不揭晓。
       ⭐ 连带必须改的一处：原来一点就给正确答案加 .right（绿框）——
         那等于**第一次点击就把答案送出去了**，「全对才开」根本无从谈起。
         现在 .right 只在**全对那一刻**才加。
       ⚠️ 判据：**改揭晓条件时要顺着查一遍「还有什么地方也在泄答案」** ——
         按钮配色、aria、title 都算。这里就差点漏掉 .right。 */
    var hint=document.createElement('div');
    hint.className='hint';
    box.insertBefore(hint, rev);
    function grade(){
      var done=true, ok=0;
      groups.forEach(function(gg){
        var p=gg.querySelector('.picked');
        if(!p){ done=false; return; }
        if(p.hasAttribute('data-right')) ok++;
      });
      if(!done){ hint.textContent=''; hint.classList.remove('on'); return; }
      var bad=groups.length-ok;
      if(bad===0){
        hint.textContent=''; hint.classList.remove('on');
        box.querySelectorAll('[data-right]').forEach(function(r){ r.classList.add('right'); });
        rev.classList.add('on');
        rev.scrollIntoView({behavior:'smooth', block:'nearest'});
      }else{
        hint.textContent='\u26a0\ufe0f ' + ok + ' 对 ' + bad + ' 错，继续努力';
        hint.classList.add('on');
        rev.classList.remove('on');
        box.querySelectorAll('.right').forEach(function(r){ r.classList.remove('right'); });
      }
    }
    groups.forEach(function(g){
      g.querySelectorAll('button').forEach(function(b){
        b.addEventListener('click', function(){
          g.querySelectorAll('button').forEach(function(x){x.classList.remove('picked','right');});
          b.classList.add('picked');
          grade();
        });
      });
    });
  });
  }
  if(document.readyState==='loading') addEventListener('DOMContentLoaded', bind);
  else bind();
})();
</script>

<!-- ⭐⭐ 2026-09-14 R12 现场要求：「那个注意力编年史能给我放在开篇的位置吗？」
     ⭐ 顺手做成**首尾呼应**：开篇摊开它（此刻看不懂是正常的），
       第九章收尾请读者滚回来再看一眼。
     ⛔ 判据：**同一张图放在开头和结尾，价值不一样** ——&nbsp;
       开头它是「领土」（这八年真实发生了什么），结尾它是「你现在看得懂了」。
       所以图只放一次，第二次用一句话把人送回来。 -->
<!-- ⭐⭐⭐ 2026-09-16 现场：「这一部分给我折叠起来，我光看后边那个
     有模型名称和年份的编年史就够了。」——&nbsp;指的就是这张横轴时间线。
     ⛔ **折叠不是删掉**：它是首尾呼应用的，第九章末尾还要把读者送回来，
       删了那句召回就落空了。所以做成默认收起、点一下就开。
     ⭐ 判据：**当两样东西讲同一段历史、而其中一样明显更好读时，
       差的那样不该跟好的那样抢同一屏** ——&nbsp;把它收起来，而不是删掉。 -->
<details class="foldfig"><summary><b>先看一眼这八年的形状</b>
  <span class="why">——&nbsp;四条支线各修各的毛病（横轴时间线）。
  可以不点：<b>下面那张按模型排的表，信息更全、也更好读。</b></span></summary>

<p class="landing">⭐ <b>这八年真实发生过的事 ——&nbsp;四条支线，各修各的毛病。</b></p>

__FIG_CHRONICLE__

<!-- ⛔ 2026-09-14 R14：这一块原来有三句，**两句是图注的改写**
     （「看不懂是正常的」＝图注「不用现在看懂」；
       「回来再看一眼、会是另一张图」＝图注「再回来看它，会看见完全不同的东西」）。
     ⭐ 只有「请记住你此刻看它的感觉」是图注没有的 ——&nbsp;它才是那个钩子。
     ⭐⭐ 判据：**图注说「这张图是什么」，正文说「你该拿它怎么办」。**
        两边都写「怎么办」，读者就在一屏之内看见了两遍。
     ⛔ 查重 lint 放过了它，因为是**改写**不是逐字 ——&nbsp;
        所以每加一段挨着图的话，仍然要人眼比一遍图注。 -->
<div class="note info"><p>⭐ <em><b>请记住你此刻看它的感觉。</b>
  读到<a href="#s九">最后一章</a>，我会请你滚回来。</em></p>
<p><span class="sub">⭐ <em>这一节<b>没有新知识</b>，它只是一张地图 ——&nbsp;
  所以它不编到正课里，叫<b>第零节</b>。
  <b>它唯一的作用，是让你在最后那一章有一个<u>回得去的地方</u>。</b></em></span></p></div>

</details>

<!-- ⭐⭐⭐ 2026-09-16 现场：「我说的注意力编年史不是横着那张时间线，
     是**按模型来捋的、所有著名开源模型按年代排的那个巨大表格**，
     还能切 highlight 和全量两种模式的那个。」——&#160;表从 8.6 挪到这里。
     ⭐ 两样东西并不重复，它们是**同一段历史的两种分辨率**：
       上面那张时间线看的是**四条支线各修各的毛病**（形状）；
       下面这张表看的是**每一个具体型号到底选了什么**（账）。
     ⛔ 开篇读不懂是正常的，而且是故意的 ——&#160;第八章末尾会请读者回来。 -->
<p class="landing">⭐ <b>具体的账 ——&nbsp;先看列，不用看懂行。</b>
  <em>一个循环里几层便宜的、上下文多长、KV cache 多大 ——&nbsp;
  这一讲结束时，每一行你都读得下来。</em></p>

__TABLE_MODELS__

<div class="note info"><p>⭐ <em><b>这张表有两种读法，右上角可以切：</b>
  <b>Highlight</b>（默认）只留撑起这段历史的那些行，备注写的是「它凭什么在这条线上」；
  <b>全部</b>是完整参照表，备注是这一行独有的那句话。<br>
  ⭐⭐ <b>表头还能点</b> ——&nbsp;按时间、厂商、配比、上下文、KV cache 排序。</em></p></div>

<p class="landing">⭐ 这一讲按时间顺序讲，一步一步走。
  每一步只回答两个问题：<b>上一步欠下了什么，这一步拿什么来还。</b>
  <em>——&nbsp;所以读的时候不用记名词，记「谁欠了谁」就够了。</em></p>
<!-- ⛔ 这一段不能并回上面那段：版面体检 W3 只看**紧挨 <figure> 的那一个 <p>**，
     并起来就是 89 汉字、超过 80 的线。⭐ 图前预告太长的毛病是真的 ——&nbsp;
     读者在还没看见图的时候就被灌了一段，等图出来已经没力气看了。 -->
<p><em>⚠️ 想看推导、消融表、我们自己在 v7 上的实测数据，去
  <a href="topic-03-L300.html">L300 完整版</a>；这一份只讲故事线。</em></p>

<!-- ⭐⭐⭐ 2026-09-16 现场：「这一部分内容对主线没有什么帮助，把它折叠起来，
     然后有时间的话，谁愿意自己读的话，可以自己读。」——&nbsp;指的就是这张路线图
     （六张卡片 ＋ 底下两张趋势图 ＋ 两条落点带）。
     ⭐ 这是本页第二处 .foldfig。判据跟第一处同一条：
       **对主线没帮助的东西，收起来，不是删掉。**
     ⛔ 为什么不删：底下那两张趋势图是**本课模型表 44 行现算出来的**，
       是全讲唯一一处「拿自己的数据说话」；而且第九章收尾要引它那句
       「两条曲线同时反着走」。删了，收尾就没有落脚点。
     ⚠️ 上面那两段（「按时间顺序讲、记谁欠了谁」和 L300 指路）**留在外面**：
       前者是主线本身，后者是导航 ——&nbsp;都不属于「可以不读」的部分。 -->
<details class="foldfig"><summary><b>整本书的路线图（六段）＋ 两张趋势图</b>
  <span class="why">——&nbsp;对主线没帮助，<b>可以直接跳过</b>。
  想自己读的话：底下那两张是<b>本课模型表 44 行现算</b>的
  ——&nbsp;上下文往上、同一长度的代价往下，六年就这两条线同时反着走。</span></summary>

__FIG_ARC__

</details>

</div></section>


<section id="s一"><div class="wrap"><div class="stn"><span class="badge">第 一 节</span><h2>一条链 ——&nbsp;最早的模型是怎么记事的</h2></div>

<p class="lead">这本书里后面出场的每一个名词，解的都是同一道题：
  <b>话是有先后的，模型总得有个地方放「刚才说了什么」。</b>
  放在哪儿、放多少、要不要每次全看一遍 ——&nbsp;三十年就吵这一件事。</p>

<h3>1.1　全部设计只有一句话：带一个盒子</h3>

<!-- ⭐⭐⭐ 2026-09-16 **整节重写，砍到三分之一。** 现场原话：
     「1.1 和 1.1b 这两部分，非常简单的事情画了太多的图，它们彼此之间讲述的
       内容是严重重叠的。你把它们给我蒸馏一下，变成 1/3 我觉得差不多，
       然后文字部分尽量都折叠起来。」
     ⛔ 原来是 **两个小节 ＋ 两张图（1054 ＋ 3186 px）＋ 七个追问的长正文**。
       1.1b 整节并进来了，标题不再单列 ——&#160;它本来就只是「把 1.1 说细」。
     ⭐ 蒸馏的判据（值得记住，后面还会用到）：
       ① **主线只留「读者不读就跟不上下一节」的那几句**，其余一律折叠；
       ② **要人「看」的留图，要人「读」的留字** ——&#160;
          原图里三条纯文字的带子挪进了下面的折叠块，图矮了一半多；
       ③ **两个视角讲同一件事，就合成一格** ——&#160;
          「为什么不糊」和「为什么会淡」是同一个动作的两面，分两格反而看不出来。
     ⛔ 一个字都没删掉：七个追问全在折叠块里，谁愿意读谁点开。 -->

<p>1990 年给出的答案朴素得近乎粗暴：<b>带一个盒子</b>。<br>
  读进一个字，就把盒子里的东西和这个字搅一搅，再写回盒子；读下一个字，再搅一次。
  读到最后，<b>盒子里剩下的，就是这段话在模型眼里的全部</b>。</p>
<!-- ⚠️ 「喂十个字还是十万个字，盒子一样大」这句，图里的落点带**也有一份**。
     ⭐ 这是**故意留的唯一一处重复** ——&nbsp;那份在 SVG 里是 11px 的小字，
       站在台下的人根本读不到，而这恰好是这一整章唯一要人记住的一句。
     ⛔ 判据：**「图里有」要看的是「台下看不看得见」，不是「文件里在不在」。** -->
<p>⭐ 而这里有一个决定了后面三十年的细节：<b>这个盒子从头到尾一样大。</b>
  <em>喂它十个字，盒子这么大；喂它十万个字，盒子还是这么大。</em></p>

__FIG_RNN_UNROLL__

<p class="lead">⛔ 不过「搅一搅」是个<b>会骗人的比喻</b> ——&nbsp;
  <em>它容易让人想成<b>搅匀</b>，而搅匀是会把东西毁掉的。
  所以把盒子打开看一眼：<b>它其实不是搅匀。</b></em></p>

__FIG_RNN_INSIDE__

<div class="note ok"><p>⭐⭐ <b>这一节只要记住三句。</b></p>
<ul>
  <li><b>怎么进去的？</b>——&nbsp;<em><b>不是搅匀，是叠上去</b>：
    老状态转个向，新字映到同一个空间，两个<b>相加</b>，再压一压。</em></li>
  <li><b>为什么不糊，又为什么会淡？</b>——&nbsp;<em><b>是同一个动作的两面</b>：
    维度高所以叠加能还原；可每步都要再乘一次，所以远处被压没了。</em></li>
  <li><b>KV 是这时候发明的吗？</b>——&nbsp;<em><b>不是。</b>
    <b>RNN 的状态是「压成一团」，KV cache 是「原样留着」</b> ——&nbsp;
    <b>后面每一章，都可以按「它站在这两端的哪儿」来读。</b></em></li>
</ul></div>

<details class="foldfig"><summary><b>这一节的七个追问 —— 现场问出来的，答案都在</b>
  <span class="why">——&nbsp;<b>跳过不影响往下读。</b>
  名字的来历 · 盒子多长 · 什么精度 · 权重在哪 · 真能用时多大 ·
  那几块矩阵在提取什么 · 什么时候变成二维的</span></summary>

<div class="note info"><p>📌 <b>① 名字拆开：RNN ＝ <u>R</u>ecurrent <u>N</u>eural
  <u>N</u>etwork，中文叫<b>循环神经网络</b>。</b></p>
<p>⭐ <b>那个 R，指的就是你刚看到的那条回线。</b>
  <em>盒子里的东西算完之后，<b>不是走了，是绕回去</b>当下一步的输入。<br>
  词源上很直白：recurrent ＝ <b>re-（回）＋ currere（跑）</b>，
  字面就是<b>「往回跑」</b>。</em></p>
<p>⭐⭐ <em>而这个词<b>不是数学家造的，是从解剖学借的</b> ——&nbsp;
  1901 年 Cajal 在小脑皮层观察到 recurrent semicircles，
  1933 年 Lorente de Nó 发现 recurrent, reciprocal connections。
  <b>在那之前，神经系统一直被当成纯前馈的。</b></em></p>
<p>⛔ <b>中文有个常见误译要避开：「递归神经网络」不是它。</b>
  <em>递归（Recursive）是<b>另一个东西</b>，沿<b>语法树</b>往下钻；
  RNN 是<b>循环</b>，沿<b>时间轴</b>转圈。<b>一个往深处钻，一个原地打转。</b></em></p></div>

<div class="note"><p><b>② 那个盒子到底多长？</b>
  <em>——&nbsp;<b>小得超乎想象。</b>图里给了最大的那个（150）；
  同一篇论文里字母预测那两个只有 <b>20</b>，最小的 XOR ——&nbsp;<b>2</b>。
  <b>整篇的区间就是「2 到 150」。</b></em></p>
<p><b>③ 什么精度？</b>
  <em>——&nbsp;<b>论文没写。而这正是答案的一部分</b>：那个年代精度还不是一个
  可以权衡的设计维度，它就是机器上的普通浮点数，没人把它当旋钮。<br>
  ⭐ <b>一个量什么时候开始被写进论文，本身就是个信号</b> ——&nbsp;
  说明它那时候才变成一件要权衡的事。</em></p>
<p><b>④ 真能用的时候多大？</b>
  <em>——&nbsp;<b>150 是玩具尺寸。</b>第一个真正能用的大 RNN 是 2014 年那篇
  seq2seq：<b>4 层、每层 1000 个 cell</b>。而原文紧接那句才是精华：
  「the deep LSTM uses <b>8000 real numbers</b> to represent a sentence」。</em></p>
<p class="landing">⛔⛔ <b>八千　→　两千六百二十亿</b>（开篇那 488 GiB 按 bf16 算）。
  <b><u>三千两百七十五万倍</u></b>。
  <em>——&nbsp;这就是「压成一团」和「原样留着」之间的真实距离。</em></p></div>

<div class="note"><p><b>⑤ 那几块矩阵各自在提取什么？——&nbsp;答案有点反直觉。</b></p>
<p><em>注意力那边 <b>Q / K / V</b> 的语义人人讲得清：
  <b>Q ＝ 我要找什么，K ＝ 我能回答什么，V ＝ 找到我能给什么。</b></em></p>
<p>⛔ <b>而 RNN 没有这种分工 ——&nbsp;<u>不是我们没研究清楚，是架构本身没把角色分开</u>。</b>
  <em>输入矩阵写进去的东西<b>既是内容、又是将来被检索的依据</b>；
  循环矩阵<b>既保留又变换</b>，既是记忆的保持器又是检索器。</em></p>
<p class="landing">⭐⭐⭐ <b>而这正好说出了注意力为什么是个突破：
  它把「记什么」和「怎么找」拆成了两个<u>独立的</u>参数化。</b>
  <em>在 RNN 里，这两件事是<b>同一个矩阵在兼职</b>。</em></p>
<p><span class="sub">⭐ <em>更准的说法是三档：<b>RNN 的状态是「没有索引的一团」，
  线性注意力的状态是「带索引的记忆」，KV cache 是「一条一条原样摆着」。</b>
  中间那一档到<a href="#s七">第七章</a>会回来。</em></span></p></div>

<div class="note"><p><b>⑥ x 是什么、y 是什么？它也是在预测下一个词吗？</b></p>
<p><em><b>x</b> ＝ 当前这<b>一个</b> token 的向量；<b>h</b> ＝ 读完前面所有词之后的状态；
  <b>y</b> ＝ 状态过输出矩阵，得到一个<b>跟词表一样长</b>的向量，
  softmax 之后就是「下一个词是每个词的概率」。</em></p>
<p>⭐⭐ <em><b>所以是的 ——&nbsp;它就是 next-token prediction。</b>
  原文写着：「The task on each input cycle was to predict the … next word
  in the sequence.」<b>一九九〇年，同一个训练目标，用了三十五年。</b></em></p>
<p><span class="sub">⚠️ 顺带交代那时候的「词向量」：Elman 给每个词一个
  <b>31 位、只有一位是 1</b> 的向量，<b>随机指定、不参与训练</b>。
  原文点明两个后果：<b>彼此完全正交，且不携带任何词性或词义</b>。<br>
  ⭐ <em>这恰好说出了 embedding 后来是干什么的：<b>当年是完全正交但零信息，
  今天是学出来的稠密向量 ——&nbsp;只做到「近似正交」，换来的是它开始携带意思。</b></em></span></p></div>

<div class="note"><p><b>⑦ 什么时候变成二维的（矩阵状态）？</b>
  <em>——&nbsp;<b>比多数人以为的早得多，而且它不是被发明的，是被重新发现的。</b></em></p>
<p><em>90 年代初 Schmidhuber 那一支的 Fast Weight Programmers，用的就已经是
  <b>外积累加出来的矩阵</b>。2021 年那篇把两者<b>形式等价</b>证了出来
  （arXiv 2102.11174，作者里有 Schmidhuber 本人）。</em></p>
<p>⭐⭐ <em>而摘要里那句最值钱：那些外积拼的是
  「self-invented activation patterns（<b>today called keys and values</b>）」
  ——&nbsp;<b>今天叫 key 和 value 的那两样东西，三十年前就以外积的形式出现过。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-16 现场追问（这一问问得最深）：
     「那个『转一下』到底在转啥？是要淡化记忆还是腾地方？
       所谓的提取是怎么发生的？所谓的融入又是怎么发生的？」
     ⛔ 本课到这里为止，「转一下」一直是个**只说了名字没说内容**的动作 ——&#160;
       而它占了全部参数的七成，是这个架构里最该讲清楚的一块。
     ⭐ 判据：**一个被反复提到、却从来没打开过的词，就是一处欠账。**
       （这已经是本章第二次 ——&#160;上一次是「搅一搅」。） -->
<div class="note"><p><b>⑧ 那个「转一下」到底在转什么？是淡化记忆，还是腾地方？</b>
  <em>——&nbsp;<b>两个都是，而且它们是同一件事。</b></em></p>

<p>⛔ <em>先别混：<b>那条回线固定 1.0、不训练，它只负责把状态搬到下一步</b>。
  真正在「转」的是那块 <b>150 × 150</b>，也就是占了七成参数的那一块。
  ——&nbsp;<b>搬运是免费的，收费的是搬过来之后怎么读它。</b></em></p>

<p>⭐ <b>「转一下」这个说法只说对了三分之一。</b>
  <em>任何一个方阵作用在向量上，一定是三件事叠在一起：
  <b>换个角度看 →&nbsp;各个方向按<u>各不相同</u>的倍率缩放 →&nbsp;再换个角度摆回去</b>。
  <b>要害全在中间那一步。</b></em></p>
<ul>
  <li><em>某个方向倍率 <b>&lt; 1</b> ——&nbsp;每步乘一次，那个方向的旧内容
    <b>指数衰减</b>。<b>这就是淡化。</b></em></li>
  <li><em>倍率 <b>≈ 1</b> ——&nbsp;东西能传很远。<b>这就是长期记忆通道。</b></em></li>
  <li><em>而淡化腾出来的，正是那个方向上的<b>可分辨度</b> ——&nbsp;
    新内容叠上去之后还认得出来。<b>所以「淡化」和「腾地方」是同一个动作的两种说法。</b></em></li>
</ul>
<p>⭐⭐ <em>最要紧的是：<b>它不是均匀地淡化，是挑着方向淡化的。</b>
  哪些方向快忘、哪些慢忘 ——&nbsp;<b>这就是那块矩阵真正在学的东西。</b></em></p>

<p><b>那「提取」是怎么发生的？</b>
  <em>——&nbsp;<b>RNN 里根本没有一个叫「提取」的步骤。它藏在乘法里。</b></em></p>
<p><em>把那块矩阵<b>按行看</b>：矩阵乘 h，第 i 个输出就是<b>第 i 行跟 h 做内积</b>。
  而内积就是提取 ——&nbsp;h 是一堆<a href="#fig-rnn-inside">近似垂直</a>的方向叠出来的，
  你拿一条向量去点它，<b>跟你对齐的分量被读出来，垂直的那些自动约掉</b>。</em></p>
<p>⭐⭐ <em>所以<b>每一行就是一把尺子、一个探针</b>。150 行就是 150 个探针，
  同时去量这块白板；量出来的 150 个数，重新摆成新的 h。<br>
  ——&nbsp;<b>「转一下」同时在做两件事：把旧状态读一遍，再把读到的重写成新状态。</b></em></p>

<p><b>「融入」呢？</b>
  <em>——&nbsp;<b>就是向量加法</b>，简单得多。新字被另一块矩阵映到同一个空间里的
  某个方向，然后加上去。<b>不是覆盖，是叠加</b>：旧方向一点没动，只是多了一个分量。</em></p>
<p>⛔ <em>但这里有一层前面没说透的：<b>叠上去之后，新的和旧的就分不清谁是谁了</b>
  ——&nbsp;<b>除非有探针能把它们分开。</b>而那个探针，就是下一步那块矩阵的某一行。</em></p>

<p class="landing">⭐⭐⭐ <b>于是整件事是个闭环：「映过来」那块决定<u>往哪个方向写</u>，
  「转一下」那块的行决定<u>从哪个方向读</u> ——&nbsp;这两头必须<u>对上暗号</u>。</b></p>
<p>⛔ <em>而它们隔着<b>多少步的连乘</b>？隔着 t 步。梯度要穿过那 t 次连乘，
  才能告诉这两块矩阵该怎么对齐。<br>
  ⭐⭐ <b>这才是长程依赖学不好的真正根因 ——&nbsp;不是「记不住」，
  是<u>读写两端对不上暗号，而且越远越对不上</u>。</b></em></p>
<p class="landing">⭐⭐⭐ <b>所以注意力的突破可以说得比「它把记什么和怎么找分开了」更准一层：
  <u>RNN 的读写要跨 t 步对齐；注意力的读写在同一步里直接点积。</u></b>
  <em>Q 和 K 当场碰一下，对不对得上立刻就知道，<b>不隔任何连乘</b>。<br>
  ——&nbsp;<b>它不是把对齐学得更好了，是把需要对齐的距离砍成了零。</b></em></p>
<p><span class="sub">📌 <b>口径。</b>「方阵 ＝ 旋转 × 各向缩放 × 旋转」是奇异值分解，
  数学事实；「谱决定衰减或爆炸」是梯度消失那套标准分析。
  <em>⚠️ 而<b>「读写要跨 t 步对齐」这个说法是本课的表述</b>，
  不是文献里的标准提法 ——&nbsp;它底下就是长程依赖那套分析。</em></span></p></div>

</details>

<div class="note ok"><p>⭐⭐ <b>把图上那个形状先记在心里。</b>
  ⛔ 它在<a href="#s二">第二章</a>会被整个扔掉；然后在<a href="#s七">第七章</a>
  <b>原样回来</b>。<br>
  <em>要绕完那一整圈，我们才会发现：当年扔掉的东西里，有一样是宝贝。</em></p></div>


<h3>1.2　第一个痛：盒子只有一个，所以只能一步一步来</h3>

<p>这个设计有一个今天看来仍然极其诱人的好处：<b>不管这段话多长，
  每走一步要搬的东西一样多</b> ——&nbsp;<em>成本跟长度无关。</em></p>
<p>坏处写在同一句话里：<b>盒子只有一个。</b>
  想更新第三个字的盒子，得先等第二个字更新完；一万个字，就老老实实排一万轮。</p>
<p>「排一万轮」听上去只是慢一点。<b>真正难受的是它落到机器上的样子</b> ——&nbsp;
  <em>每一轮都要把整个模型的权重从显存里重新搬一遍，然后只拿它算一丁点活。</em></p>

__FIG_RNN_HW__

<!-- ⛔⛔ 2026-09-14 截图审出来的：这里原来是一整块 note danger，标题写着
     「两头堵死」——&nbsp;而**图自己的落点带标题就叫「两头堵死」**，下面两行说的
     也是同一件事。渲染出来页面在原地结巴了一次。
     ⭐ 留下的只有图里没有的两样：**313 的出处（专题二那条屋脊线）**，
       和**一句大白话的后果**。⛔ 判据②在这里第一次被自己违反，靠截图才看见 ——
       所以下面给 place_figs 加了「图注 ↔ 图内文字」查重（见 topic03_page.py）。 -->
<div class="note danger"><p>⭐ 那个 313 不是这一讲新造的数 ——&nbsp;它就是<a href="topic-02.html">专题二</a>
  整整一节在立的那条<b>屋脊线</b>：<em>每从显存搬一个字节，这台机器配套能算 313 次。</em></p>
<p>⛔ 所以这一条不是「RNN 慢一点」，是<b>买了台很贵的机器，而它九成时间在等着搬东西</b>。</p></div>

<!-- ⭐⭐ 2026-09-14 R4 联网补料后加的一段。原稿读起来像「RNN 就这么被卡死了」，
     ——&nbsp;而真实的历史是**有人拼命修过**，只是修不动。
     ⛔ 判据：**写一个东西被淘汰，一定要写它挣扎过** ——&nbsp;
       否则读者会以为前人都很笨，而这是技术史最常见的一种误读。 -->
<div class="note info"><p>📌 <b>顺带说一句：当年不是没人想办法。</b></p>
<p>既然病根是「权重每一步都要重搬一遍」，那就<b>干脆别搬</b> ——&nbsp;
  把权重<b>钉死在芯片内部</b>，一步步往下走的时候它一直待在那儿不动。
  <em>这招叫 <b>persistent RNN</b>，2016 年百度硅谷 AI 实验室做的。</em></p>
<p>⭐ <b>记住这个念头 ——&nbsp;它后面还要再赢一次。</b>
  <em>七年后有人把同一句话用在了另一个算子上：「别让中间结果落回显存」。
  那个东西叫 <b>FlashAttention</b>，<a href="#s九">第九章</a>会讲到。</em></p>
<p>⛔ 可惜这一招救不了 RNN ——&nbsp;
  <em>能钉在片上的模型就那么大，而且它<b>只治了「搬得慢」，没治「必须排队」</b>。</em></p></div>

<h3>1.3　另外两个痛：传远了会淡，一整段塞不进一个盒子</h3>

<!-- ⛔ 这里原来有三段正文，把图上三格逐格复述了一遍 ——&nbsp;
     现场的原则是「只要图，尽量少写文字」，而那三段正是反面教材。
     ⭐ 只留一句领进去的话，剩下交给图。 -->
<p>第一个痛是<b>算不快</b>。另外两个更伤 ——&nbsp;<em>它们不是慢，是<b>真的学不会</b>。</em></p>

__FIG_RNN_PAIN__

<h3>1.4　本章落点：这本书的主角，是那个盒子</h3>

<div class="note ok"><p><b>这一章只留两句话。</b></p>
<p><b>① 盒子的好处</b>：每一步要搬的东西<b>不随长度变</b>。<br>
  <b>② 盒子的坏处</b>：盒子只有一个，所以<b>只能一步一步来</b>；而且它<b>装不下</b>一整段长话。</p>
<p>⭐ <a href="#s二">下一章</a>那一刀，冲的是 ②。<br>
  ⛔ <b>但它会把 ① 一起扔掉。</b>
  <em>这本书剩下的八章，讲的都是怎么为「扔掉 ①」付账。</em></p>
<!-- ⭐⭐ 2026-09-15 模拟上课第 2 轮：**第二章开口引的那个问句，第一章从没问过。**
     §二 的 lead 写着「上一章最后那个问句还悬着」——&nbsp;
     而那个问句只存在于 1.3 的**图注**里，1.4 恰恰用编号句把它替换掉了。
     读者顺着读会愣一下：**哪个问句？**
     ⭐ 判据：**「上一章说过」这种话，要去上一章核一遍是不是真说过** ——&nbsp;
        而且要核**正文**说没说过，图注不算：读者不一定读图注，讲师也不念它。 -->
<p>⭐⭐ <em>不过在那一刀落下来之前，先把这一章真正的那个问句问出来 ——&nbsp;
  <b>既然「回头看一眼」这个配件这么好使，那能不能把 RNN 整个扔掉，只留配件？</b></em></p></div>

</div></section>


<section id="s二"><div class="wrap"><div class="stn"><span class="badge">第 二 节</span><h2>2017 年那一刀 ——&nbsp;把链剪断，换成一张表</h2></div>

<!-- ⛔ 原稿这里把上一章图注那句问话**逐字**抄了一遍。跨章呼应是好事，
     但逐字重复读起来就是页面结巴 ——&nbsp;换成自己的话说一遍，还更冲。 -->
<p class="lead">上一章最后那个问句还悬着：<b>配件既然这么好用，RNN 还留着干什么？</b>
  <em>2017 年有人真就这么干了 ——&nbsp;而且论文标题就是这个意思。</em></p>
<!-- ⭐ 2026-09-15 现场试讲第二章时自然补的一句：台下默认「注意力＝2017」，
     不先把这个误解撬开，后面「那一刀砍的是链不是配件」就立不住。 -->
<p>⛔ <b>先纠正一件很多人搞混的事：注意力不是 2017 年发明的。</b>
  <em>它 <b>2014 年</b>就有了 ——&nbsp;而且一开始，
  <b>它是长在 RNN 身上的一个小配件</b>。</em><br>
  ⭐ 所以那一刀真正干的事，<b>不是发明注意力，是把长着配件的那条链整个扔掉，只留配件</b>。</p>

<h3>2.1　先看清那个配件是怎么被逼出来的</h3>

<!-- ⛔ 图前预告不许复述图注（体检 W4）。原稿这里把图注那句
     「每一步只修上一步的一个毛病」提前说了一遍 ——&nbsp;读者等于看了两次。
     ⭐ 领进去的话只负责**换一种情绪**，具体机制交给图。 -->
<p>动手之前先补一段：<b>注意力不是谁灵光一闪想出来的</b>。
  <em>它是被人一次次嫌弃出来的 ——&nbsp;每一版都有人指着上一版说「这儿不对」。</em></p>

__FIG_ATTN_INVENTED__

<!-- ⛔⛔ 这里原来把图里蓝框那段（论文原话 ＋「不是更准，是能变成矩阵乘」）
     **换了几个词又说了一遍**。⭐ 查重比不出来 ——&nbsp;它是改写不是照抄，
     字符串对不上。**这一处是靠截图用眼睛看出来的。**
     ⛔ 判据：查重只挡得住照抄；**改写只能靠眼睛，所以每章写完必须渲染出来看一遍。**
     ⭐ 留下来的只有图给不了的：**这条线后面会去哪儿。** -->
<div class="note ok"><p>⭐⭐ 图里蓝框那一句，先在这儿挂个号 ——&nbsp;
  <b>它是这本书的第一条暗线</b>。</p>
<p><em>同样的事后面还会发生三次，一次比一次露骨。</em>到<a href="#s六">第六章</a>它会变成：
  连「挑哪几个字来看」都得按块挑，因为机器讨厌零零散散地取数。</p>
<p>⛔ 这本书里不少公式长成那个样子，并不是数学上非如此不可 ——&nbsp;
  <em><b>而是机器只认那一种形状。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-14 R4：联网把「发明现场」找回来了。
     现场要的是「一本讲技术的小说」，而小说里最不能缺的就是**人**。
     这两段全是当事人的一手自述（Karpathy 去问 Bahdanau，Turing Post 又去问了 Bengio），
     ⛔ 不是二手转述，也不是「据说」。 -->
<h4>那第一格，是怎么想出来的</h4>

<p>Bahdanau 自己讲过这段。他当时是 Bengio 实验室的实习生，试过一个「两个游标」的方案，
  太复杂，做不出来；退而求其次写了个死板的对角线对齐，能跑，但不好看。
  <em>然后有一天 ——</em></p>

<blockquote><p>「我忽然想到，<b>要是能让解码器自己学会该把游标放在原文的哪个位置，那该多好。</b>
  这个念头多少是<b>中学学英文时做翻译练习</b>来的 ——&nbsp;
  <em>你翻译的时候，目光会在原文和译文之间来回移动。</em><br>
  我把这个『软搜索』写成了 softmax，再对双向 RNN 的状态做加权平均。
  <b>第一次跑就成了，我兴奋极了。</b>」</p>
<p class="sub">——&nbsp;Dzmitry Bahdanau，2022 年回复 Karpathy 的邮件</p></blockquote>

<p>他给这个架构起的名字叫 <b>RNNSearch</b>。他后来说：<em>「这名字不怎么样。」</em><br>
  ⭐ <b>「注意力」这三个字，是 Bengio 在最后几轮修改里加进去的。</b></p>

<!-- ⛔ 这段引文原来是一整块 188 字的段落墙（体检 W1）。⭐ 按语义切成三段 ——&nbsp;
     它本来就是三步：旧做法怎么错的 / 该怎么办 / 为什么必须是「软」的。 -->
<blockquote><p>「在我们提出注意力之前，做法是让一个循环网络<b>把整段原文读完</b>，
  然后生成译文。<em>可人根本不是这么翻译的。</em></p>
<p>这就好比<b>先把整本法文书从头读到尾，然后凭着脑子里的印象把英文版写出来</b> ——&nbsp;
  肯定会丢细节，因为我们的脑子没法一次装下那么多东西。</p>
<p>办法是：写每一个英文词的时候，<b>都允许回头去看法文书里的任意一处细节</b>。
  而为了能用反向传播训练，注意力必须是『<b>软</b>』的 ——&nbsp;
  <em>不是挑中某一个位置，而是有一份<b>总额固定</b>的注意力，可以分摊到所有位置上。</em>」</p>
<p class="sub">——&nbsp;Yoshua Bengio，被问到为什么用「attention」这个词时</p></blockquote>

<!-- ⛔ 这里原来写「下一节」，而 #s二 就是本章自己 ——&nbsp;指针指回了原地。
     ⭐ 跨节指针体检只管 §X.Y 这种写法，**中文的「下一节」它看不见**。
       判据：**方位词也是指针**，跟节号一样会指错，而且更不容易被发现。 -->
<div class="note ok"><p>⭐⭐ <b>最后那句话，顺手解释了这一讲后面所有的 softmax。</b></p>
<p><em>「总额固定、分摊到所有位置」——&nbsp;就是等一下 §2.4 那张图里
  那根<b>一直加到 100% 的百分比条</b>。
  它不是某个数学家的偏好，是<b>为了能被梯度下降训练而必须付的一个形状</b>。</em></p></div>

<p class="sub">📌 上面两段自述的出处：Karpathy 2022 年去信问 Bahdanau，
  <a href="https://www.turingpost.com/p/attention" target="_blank" rel="noopener">Turing Post</a>
  整理并另行采访了 Bengio。<em>本讲的译文按原文意思重述，不是逐字直译。</em></p>

<!-- ⛔ 这段原来挂着 class="landing"，于是折叠模式下也露着。
     ⭐ 但它是**读法说明**，不是主线钩子 ——&#160;降级成普通段落，跟着折叠走。
     判据：**landing / lead 是「一句话的钩子」，补充说明不该占这个位置。** -->
<p>📌 <b>顺带交代一下这本书往后的读法</b>：每讲到一个新名词，
  我都会先说它<b>在修上一版的哪个具体毛病</b>。
  <em>——&nbsp;「某某灵光一闪」这件事你学不会；「上一版这个地方不对」，你下次能照着用。</em></p>

<h3>2.2　那一刀砍下去，换掉的是什么</h3>

__FIG_MHA_SWAP__

<div class="note danger"><p>⛔ 图里那句「状态没了」，
  正是<a href="#s一">第一章</a>结尾<b>押下的那一笔</b>。</p>
<p>①「每步要搬的东西不随长度变」——&nbsp;<em>从这一刀落下的那一刻起，<b>不成立了</b>。</em></p>
<p>⭐ <em>换来的是并行；赔进去的，是那份「贵不贵跟话有多长没关系」的清白。</em></p></div>

<h3>2.3　那信息到底是怎么走过去的</h3>

<p>链剪断了，可话还是得连起来读。<b>不靠一格一格传，那靠什么？</b></p>

__FIG_MHA_FLOW__

<h3>2.4　拆开一层看：它在算什么</h3>

__FIG_MHA_QKV__

<div class="note info"><p>📌 <code>√d_k</code> <b>就是点积的标准差</b>：
  <em>q·k 是 d_k 个乘积之和，方差随维度线性增长 ——&nbsp;除掉它，
  把打分的尺度拉回跟维度无关的常数量级。</em></p>
<p>⛔ 别小看这一下 ——&nbsp;不除，整碗货会被一家吃掉；
  除了，才混得出一碗真正的混合物。</p></div>

<h3>2.5　多头在多什么</h3>

__FIG_MHA_HEADS__

<h3>2.6　本章落点：KV cache 就是在这里出生的</h3>

<!-- ⛔ 这句改过两版了：第一版抄了图的 header sub，第二版抄了 Ⓑ 的标题。
     判据⑩：挨着图的那一处赢 ——&#160;正文只留**图上没有的那个动作**（去图里找）。 -->
<p>前面讲的都是想法，现在看它落成什么。<b>Ⓑ 那三堆里，
  只有一堆会跟着对话一直变大</b> ——&nbsp;<em>那一堆就是 KV cache。</em></p>

__FIG_KV_BORN__

<!-- ⭐⭐⭐ 2026-09-16 现场：「这个小结里画的那个图是 Scaling Book 里的专家版，
     信息量太大了，我怕同学们理解不了，能不能画一个幼儿版的，简单点。」
     ⭐ 于是新画了 fig3-kv-born 顶上来，专家版**折起来、不删**。
     ⛔ 为什么不删：<b>它是全专题的主线图</b> ——&#160;后面好几章都要把它重画一遍、
       点亮被改动的那一处。删了后面接不上。
     ⭐ 判据（本页第三处折叠）：**看不懂 ≠ 没价值 ——&#160;收起来，不是删掉。** -->
<details class="foldfig"><summary><b>同一件事的专家版：一层 Transformer 的完整张量图</b>
  <span class="why">——&nbsp;沿用 How to Scale Your Model 的记号（BTD / BSKH …）。
  <b>第一次读可以完全跳过</b>；<b>但后面几章会反复回到它</b>，
  每讲一个新方案就把它重画一遍、只点亮被改动的那一处。</span></summary>

__FIG_TX_BASE__

</details>

<div class="note ok"><p>⭐⭐ <b>给它起个名字：这一讲的主角，就是那个 <code>S</code>。</b></p>
<p><em>后面每一章你其实只要盯着它问一句：这一招是让 S 前面的系数变小、
  让每步读到的 S 变少，还是干脆让 S 从形状里消失？</em></p>
<p>⭐ <b>名词有几十个，它们全都只在回答这三问中的一个。</b></p></div>

<div class="note info"><p>📌 <b>这里一定会想问：存不下，为什么不能每步重算？</b></p>
<p>因为重算的不是「再算一遍」，是每一步都把整段历史重算一遍。
  吐第 100 个字要把前 99 个重过一遍，吐第 101 个再重过一遍 ——&nbsp;
  <em>整段生成的总开销会从正比于 N² 变成正比于 <b>N³</b>。</em></p>
<p>⛔ 所以这不是「省一点」，是<b>能不能用</b>的分界线。
  <em>KV cache 是拿显存换时间 ——&nbsp;而这本书剩下的部分，
  全在算这笔交换到底有多贵。</em></p></div>

<div class="note ok"><p><b>这一章也只留两句话。</b></p>
<p><b>换来的</b>：任意两个字之间只隔一步，而且整段话可以一次算完 ——&nbsp;
  <em>训练终于能把机器喂饱了。</em><br>
  <b>欠下的</b>：要算的格子从 n 变成 n²；更要命的是那个盒子没了，
  换成一份<b>会随对话一直长下去</b>的 KV cache。</p>
<p>⭐ <a href="#s三">下一章</a>不讲新招，只做一件事：
  <b>把这份 KV cache 换算成实实在在的显存数字。</b>
  <em>数字出来之前，谁都不会觉得它是个事。</em></p></div>

<div class="note info"><p>📌 <b>这一章的结尾，留给发明注意力的那个人。</b></p>
<p><em>2017 年 Transformer 那篇出来的时候，Bahdanau 的反应是一句话：</em></p>
<p><b>「我当场就跟同组的人宣布：<em>RNN 死了。</em>」</b></p>
<p>⭐ <em>他说对了一半 ——&nbsp;<b>RNN 确实从训练里退场了。</b>
  但<a href="#s三">下一章</a>你就会看到，它在另一个地方好端端地活着；
  而到<a href="#s七">第七章</a>，还会有人正式把它请回来。</em></p></div>

</div></section>


<section id="s三"><div class="wrap"><div class="stn"><span class="badge">第 三 节</span><h2>账单到期 ——&nbsp;上下文一长，那张表就付不起了</h2></div>

<p class="lead">上一章欠下一份<b>会一直长下去</b>的 KV cache。
  <em>可这件事一点都不新鲜 ——&nbsp;那篇论文发表的那天，它就已经在那儿了。
  为什么隔了七年，才有人真的动手去改？</em></p>

<h3>3.1　先看一件反直觉的事：吐字的时候，它又变回了那条链</h3>

<p>动手之前得先看清楚：<b>这笔账到底是在什么时候付的</b>
  ——&nbsp;<em>而答案会把我们送回<a href="#s一">第一章</a>。</em></p>

__FIG_RNN_DECODE__

<div class="note danger"><p>⭐ 这不是打比方 ——&nbsp;<a href="#s一">第一章</a>那条链，
  <b>在这里原封不动地回来了一次</b>。</p>
<p><em>只不过这一次它是被迫的：没人想要它，它自己长回来了。</em></p>
<p>⭐⭐ <em>到<a href="#s七">第七章</a>我们还会再见到同一幕 ——&nbsp;
  那一次，是有人<b>主动</b>把它请回来的。</em></p></div>

<h3>3.2　那为什么偏偏是这两年</h3>

<p>⭐ 这个问题的答案<b>不在论文里，在机房里</b>。</p>

__FIG_FLIP__

<div class="note ok"><p>⭐⭐ <b>「突然」这两个字是怎么来的</b></p>
<p><em>一样东西翻十倍，你会看着它一点点变贵；可要是两样东西各翻了几十倍、
  而且都乘在同一项上 ——&nbsp;你看见的就不是变贵，
  是某一天早上它<b>忽然成了最大的一块</b>。</em></p>
<p>⛔ 技术没有变坏。<b>是我们换了一种用法去用它。</b></p></div>

<h3>3.3　「必须省」只是一半，还有另一条完全独立的线</h3>

<p>到这里为止，理由都是机器给的：装不下、读不动。
  <em>但如果只有这一条，这件事最多做成一堆将就的权宜之计。</em></p>
<p>真正让这么多人扑上来的，是<b>还有第二条线</b> ——&nbsp;
  <em>而且它跟显存一点关系都没有。</em></p>

__FIG_MOTIVES__

<!-- ⛔ 这段原来把图注那句「每出场一个新名词就拿它量两下」又说了一遍。
     ⭐ 换成图注给不了的：**这两问的先后顺序**。 -->
<div class="note ok"><p>⭐⭐ 右下角那把尺子，<b>是这本书后面五章唯一的评分表</b>。</p>
<p>⛔ <em>而且这两问的顺序不能反 ——&nbsp;先看「省了多少」，很容易被一个漂亮的
  倍数晃花眼；<b>真正决定一个方案有没有人用的，是第二问。</b></em></p>
<p>⭐ <b>名词会换，这两问不换。</b></p></div>

<h3>3.4　本章落点：它决定的不是装不装得下</h3>

<div class="note ok"><p><b>上一章那笔债，这一章换算成了看得见的东西。</b>
  <em>——&nbsp;这里没有新机制，只有定价。</em></p>
<!-- ⚠️ 这一句图里那行小字也有。⭐ 换了一种说法而不是照抄 ——&nbsp;
     它是这一章唯一必须带走的操作性结论，而图里那份是 11px，台下读不到。
     ⛔ 判据（第一章立的）：**「图里有」要看台下看不看得见**；
       但**换句话说**，不要逐字重复 ——&nbsp;逐字重复读起来是页面结巴。 -->
<p>⛔ 而最要命的那一条，值得换句话再说一遍：
  模型的权重，一台机器上只放一份，谁来都用它；
  <b>可那份 KV，来一个人就得单开一份</b>。<br>
  <em>所以它卡住的不是「这台机器装不装得下这个模型」，
  而是<b>这台机器同时伺候得了几个人</b>。</em></p>
<p>⛔ 而且这笔账<b>不是线性涨的，是一级一级往上跳</b>：
  <em>能一张卡装下的，就别跨卡；能一台机装下的，就别跨机 ——&nbsp;
  因为<b>卡里面的带宽 &gt; 卡跟卡之间 &gt; 机器跟机器之间</b>，
  每跨一级，整套推理就被最慢的那一段拖一次。</em></p>
<p>⭐ <a href="#s四">下一章</a>开始真的动手，第一刀也最直白：
  既然每个头都要自己的一份，<b>那让几个头合用一份，行不行？</b></p></div>
<!-- ⭐⭐ 2026-09-14 R14 章尾／章首分工（外部评审给的规则，值得当成通则）：
       **账记在上一章，还款写在下一章，不许两边各记一遍全账。**
     ⛔ 原来六处章尾都在预告下一章要干什么，而下一章开头又说一遍 ——
       中间只隔一个 </section>，读者在滚动条上只走了一屏。
     ⭐ 所以章尾留**钩子**（悬念，不剧透），章首才揭晓。此处是唯一做对的模板：
       这里问出问题，第四章开头**直接去试**，不重复那个问句。 -->

</div></section>


<section id="s四"><div class="wrap"><div class="stn"><span class="badge">第 四 节</span><h2>第一次省：把头砍掉 ——&nbsp;MQA 砍过了头，GQA 停在半路</h2></div>

<p class="lead">账摆在那儿了：<b>一人一份，而且随着话越说越长</b>。
  <em>上一章末尾已经把最直白的那个省法问出来了 ——&nbsp;<b>这一章就去试。</b></em></p>

<h3>4.1　先把四种存法摆在一起</h3>

<p>这一章和<a href="#s五">下一章</a>讲的是同一个旋钮上的四个位置。
  <b>先看全景，再逐个拆</b> ——&nbsp;<em>今天讲左边三家，最右边那家留给下一章。</em></p>

__FIG_WIRING__

<h3>4.2　一刀砍到底：MQA</h3>

<p>2019 年的做法简单粗暴：<b>所有头合用同一份 K 和 V</b>。
  <em>八个头本来要八份，现在只要一份 ——&nbsp;缓存直接除以八。</em></p>
<p>省得漂亮。<em>可它也就是从这儿开始变笨的。</em></p>

<!-- ⛔ 2026-09-15 模拟上课第 4 轮：全书 12 处「困惑度」，**一处定义都没有**，
     而它第一次出现是在下面这张图里。⭐ 判据（第 2、3 轮已立，这是第三次用）：
     **新名词第一次出现就给白话。**
     ⛔ 而且这里还缺一样更要紧的 ——&nbsp;**1.0 到底算大算小**。
       没有尺度感，这一章的全部论证对读者就是一个悬空的数。 -->
<p><span class="sub">📌 <b>下面这张图里会出现「困惑度」这个词，这里先交代一句</b>：
  你就把它理解成 ——&nbsp;<b>模型读下一个字的时候，心里大概在几个候选之间犹豫</b>，
  <b>数越小越好</b>。
  ⭐ 而且要有个尺度感：这一档模型在<b>三十上下</b>，<b>差 1.0 不是个小差距</b>
  ——&nbsp;后面你会看到有的方案改了一大堆东西，才换回零点几。</span></p>

__FIG_MQA_WHY__

<div class="note danger"><p>⛔ <b>「变笨」这件事有两张脸，而第二张更麻烦。</b></p>
<p>GQA 那篇论文开头就把话说全了：MQA 会导致
  <em>「quality degradation and <b>training instability</b>」</em>
  ——&nbsp;不只是掉点，<b>还训不稳</b>。</p>
<p>⭐ <em>掉点还能拿更多数据去补；训不稳是另一回事 ——&nbsp;
  它意味着这条路上你随时可能摔一跤，而且事先不知道会在哪儿摔。</em></p></div>

<!-- ⛔ 这块原来先把图里「贵的是通讯录不是查的人」那句用别的词说了一遍，
     再给判据 ——&nbsp;前半段是重复（改写型，查重比不出来，截图看出来的）。
     ⭐ 只留后半段：**把图里那句具体的话，抬成一条能反复用的判据。**
       这是图给不了的 ——&nbsp;图擅长讲这一次，抬成通则是正文的活。 -->
<div class="note ok"><p>⭐⭐ <b>把图里最后那句话抬一层，就是这本书后面一直在用的判据：</b></p>
<p><b>一个方案压掉的，是「体积」还是「自由度」？</b><br>
  <em>——&nbsp;用图上那个比喻说：<b>是通讯录变薄了，还是查的人变少了？</b></em><br>
  <em><b>通讯录薄一点通常还好；查的人少了，很贵。</b></em></p>
<p>⭐ <em>这条后面还要用四次 ——&nbsp;每一次都会有人想拿自由度去换体积，
  而每一次都要回来问这一句。</em></p></div>

<h3>4.3　本章落点：这一刀，其实只改了一块矩阵</h3>

<!-- ⭐⭐⭐ 2026-09-16 现场第二次点名：「这个小结没有用，给我收起来，折叠起来。」
     ⛔ 上一轮我留了三样并说明了理由（图被第五章接、章落点是固定结构、
       八组对八张卡要被第九章回指）。**他看过那条说明之后仍然说折** ——&#160;
       那就是决定，照办。
     ⭐ 折不是删：三样东西一个字没动，点开就在。
     ⚠️ 唯一要补的是<b>第五章开篇那句「上一章最后停在一块矩阵上」</b> ——&#160;
       它原来假设读者刚看过那张图。已在那句后面加了一个指回这里的链接，
       **免得折叠模式下它指向一片空白。**
     ⛔ 判据：**折叠一段内容之前，先搜一遍谁在「假设读者刚看过它」** ——&#160;
       断掉的不是链接，是叙述的连贯性，而那个不报错。 -->
<details class="foldfig"><summary><b>这一刀，其实只改了一块矩阵</b>
  <span class="why">——&nbsp;四种存法摆在同一块矩阵上比一次，外加这一章的
  「换来的 / 欠下的」两句。<b>可以直接跳过</b>；
  <b>下一章开篇说的「那块写死的矩阵」，指的就是里面那张图。</b></span></summary>


<!-- ⭐⭐⭐ 2026-09-16 现场：「4.3 和 4.4 这两节太扯了，什么也没讲明白，
     乱七八糟的都删了。」——&#160;照办：两节合成一节，删掉的东西包括 ——
       · GQA「为什么偏偏是 8」那三条理由（论文的分片复制论证、速度曲线、
         Llama-2-70B 的 config）
       · GQA 转换成本那一整块（按组求平均、5% 回炉、三种转换法排名）
       · 「把二选一变成可以拧的连续量」那条 landing
     ⛔ 只留下三样，理由逐条写在这里，免得以后有人以为是漏删：
       ① **那张图**（fig3-copy-matrix）——&#160;它不是「乱七八糟的字」，
          而且<b>第五章开篇直接接它</b>：「上一章最后停在一块矩阵上」。
       ② **「换来的 / 欠下的」两句** ——&#160;全书每一章都以这两句收尾，
          这是固定结构，删了第四章就没落点。
       ③ **「八组 ≈ 八张卡」那一句** ——&#160;<b>第九章收尾要回指它</b>
          （「硬件决定了一个超参」那条暗线）。留一句，不留论证。
     ⚠️ 要是这三样也该删，说一声 ——&#160;但②和③删了会让后面两处断掉。 -->

<p>2023 年的 GQA 给了个折中：<b>别全合，分组合</b> ——&nbsp;八个头分成几组，一组共用一份。
  <em>⭐ 顺带一句：大家不约而同都选了 <b>8 组</b>，
  而一台机器通常正好装 <b>8 张卡</b> ——&nbsp;
  <b>这个数字不是从模型里推出来的，是从机箱里数出来的。</b></em></p>

<p>而「共用」听上去是个操作，可把它摊开看，<b>它只是一次线性变换</b> ——&nbsp;
  <em>而线性变换，是可以画成一块矩阵的。</em></p>

__FIG_COPY_MATRIX__

<div class="note ok"><p><b>这一章的两句话。</b></p>
<p><b>换来的</b>：缓存除以 8，而且不用重训 ——&nbsp;
  <em>GQA 因此成了这些年的默认选项。</em><br>
  <b>欠下的</b>：省的办法，是让好几个头拿到一模一样的 K/V。
  <em>省得越狠，大家越像；到 MQA 那一步，八个人看的是同一本。</em></p>
<p>⭐⭐ <em>而图里已经把话说到头了：这一刀改的，就是那块矩阵里写了什么。
  <a href="#s五">下一章</a>做的事只有一件 ——&nbsp;
  <b>把那块写死的矩阵松开，让它自己学。</b></em></p></div>


</details>

</div></section>


<section id="s五"><div class="wrap"><div class="stn"><span class="badge">第 五 节</span><h2>第二次省：干脆不存 K 和 V ——&nbsp;MLA 存的是一份压缩件</h2></div>

<p class="lead">上一章最后停在一块矩阵上（<a href="#fig-copy-matrix">收在 4.3 里</a>）：GQA 的那块是写死的，格子里只有几个 1。
  <em>那把它松开、交给模型自己去填，会怎么样？<b>——&nbsp;那就是 MLA。</b>
  而它带来的后果，比「效果好一点」大得多。</em></p>

<!-- ⛔ 2026-09-15 现场试讲：讲到「把那块矩阵松开」，第一个问题就是
     「这一点都不相干啊」。⭐ 问得对 —— 读者还没有「四家是同一个形状」
     这个前提，所以那个动作是凭空的。前提补在这里，四句话。 -->
<div class="note info"><p>📌 <b>要看懂这一章，先接受一个前提：这四家其实是<u>同一个形状</u>。</b></p>
<p><b>仓库里存一份东西，再从这一份，做出每个头各自要用的 K 和 V。</b>
  <em>四家都是这个形状。区别<b>只有两件事</b>：那份有多宽，
  以及<b>「做出一百二十八份」的那个动作是什么动作</b>。</em></p>
<p><b>GQA</b>：仓库里存<b>八杯调好的果汁</b>。用的时候
  <b>照着每一杯复印出十六杯一模一样的</b>，凑够一百二十八杯。<br>
  ⭐ <em>那十六杯<b>一滴不差</b>，多出来的十五杯是<b>纯冗余</b>，不带任何新东西。</em><br>
  ⛔ <em>所以 GQA 省的是<b>仓库</b>，代价是<b>那十六个头被迫拿到同一杯</b>。</em></p>
<p><b>MQA</b>：只存一杯，复印一百二十八份 ——&nbsp;所有人喝同一杯。<br>
  <b>MHA</b>：老老实实存一百二十八杯，各不相同，一份都不用复印。最贵，也最自由。</p>
<p>⭐⭐ <b>而 MLA 存的不是果汁，是一包<u>浓缩粉</u>。</b>
  <em>一百二十八个头，<b>每人一张自己的配方</b>，从同一包粉里冲出
  一百二十八杯<b>各不相同</b>的。</em></p>
<p>⛔ <b>所以「松开」松开的是这个动作本身：从<u>复印</u>变成<u>冲</u>。</b>
  <em>复印只能得到一样的；冲可以得到不一样的 ——&nbsp;
  而两个动作花的力气差不多。<b>既然反正都要做这一步，为什么只许你复印？</b></em></p>
<p class="sub">⚠️ 口径：这不等于「MHA / GQA 是 MLA 的特例」——&nbsp;
  那是转述放大出来的说法。苏剑林的原话是「MLA 被视为 GQA 的一般化」。<br>
  ⭐ <em>他把「换掉那个动作」这件事写成了一句更准的话 ——&nbsp;
  <b>就在下面 5.x 那张矩阵图里</b>，这里不重复抄一遍。</em></p></div>

<h3>5.1　先说一件最容易听岔的事：箱子里那个，已经不是 K 和 V 了</h3>

<p>前三家存的都是 K 和 V 本身，区别只是存几份。
  <em>MLA 换了一条路：<b>它存的是一份压缩件</b> ——&nbsp;
  每个头真正要用的 K 和 V，是从这份压缩件现场展开出来的，用完就扔。</em></p>

__FIG_KNOB1__

<!-- ⛔ 这块原来把上一章那张矩阵图的落点带又说了一遍（查重抓到两段）。
     ⭐ 只留它给不了的：**把那句破除，接到这一章的新事实上。** -->
<div class="note warn"><p>⚠️ <b>「MLA 就是给 KV 做低秩分解」——&nbsp;这句话很顺口，
  可它把上一章那张矩阵图的结论说反了。</b></p>
<p><em>低秩这件事，<a href="#s四">上一章</a>那四块矩阵已经摆明了：
  <b>四家都在做</b>。所以它根本不是分界线。</em></p>
<p>⭐ 真正的分界线是<b>展开出来的东西一不一样</b>：
  <em>GQA 展开出来的几个头拿到的是同一份；
  <b>MLA 展开出来的每个头都不一样</b> ——&nbsp;这就是它凭什么能压得更狠还不变笨。</em></p></div>

<h3>5.2　马上会想到的一个问题：存的是压缩包，用的时候不还得拆开吗？</h3>

<p><b>不用拆。</b><em>而「不用拆」这件事，是 MLA 能不能省下来的前提 ——&nbsp;
  要是每生成一个字都得把仓库里所有压缩包拆一遍，那还不如一开始就别压。</em></p>

__FIG_ABSORB__

<!-- ⭐ 2026-09-15 现场原话：「涉及到这个交换，能省下算力吗？能省下什么东西？
     能省下显存吗？」—— 图画了「怎么挪」，可**没有一处回答「挪完省了什么」**。
     于是读者只记住一个巧妙的代数动作，不知道它为什么要紧。 -->
<div class="note info"><p>📌 <b>挪完之后，到底省下了什么？——&nbsp;答案跟多数人以为的不一样。</b></p>
<p><b>先看「不挪」有多贵。</b><em>仓库里躺着<b>十三万</b>个压缩件（128K 上下文）。
  每吐一个字，就得把它们<b>一个一个解压</b>成每个头要用的 K，再拿提问去比 ——&nbsp;
  <b>解压十三万次</b>。而下一个字<b>还得再来一遍</b>，因为解压出来的你不敢留：
  留了就等于又存了一份没压缩的，压缩白做了。</em></p>
<p><b>挪完之后</b>，先算的是「配方转置 × 提问」，而<b>提问只有一个</b> ——&nbsp;
  <em>这个动作<b>只做一次</b>。<b>解压次数：十三万次 →&nbsp;零次。</b></em></p>
<p>⛔ <b>但请分清它省的是哪一样（这正是第三章那把尺子）：</b></p>
<ul>
  <li>⛔ <b>它不省显存缓存。</b>仓库里还是每个 token 576 个数，挪不挪一个字节都不差。</li>
  <li>⭐ 省的是<b>算力</b> ——&nbsp;那个「跟历史一样长」的解压工作，
      变成了一个<b>跟历史无关的一次性动作</b>。历史越长，省得越多。</li>
  <li>⭐ 还省<b>带宽和临时显存</b> ——&nbsp;解压出来那一大坨要找地方放、
      还得写出去再读回来，现在它<b>压根没被造出来</b>。</li>
</ul>
<p>⭐⭐ <b>而这两件事是绑死的，这才是这一节真正的地基：</b>
  <em>挪不动括号 →&nbsp;每步解压十三万次太贵 →&nbsp;只能退回去直接存解压后的 K/V
  →&nbsp;压缩白做。</em><br>
  <b>所以：挪括号本身不省显存，但它是「存压缩件」这个选择能不能成立的前提。</b></p></div>

<!-- ⛔⛔ 2026-09-14 R15：这一段原来说「第二次见到了」，而第七章说「第三次」，
     图里又写「第三次出场」——&nbsp;**同一条线三种数法**。
     ⭐ 核完的结论：第二章那次**根本不是挪括号**。点积 vs 加性换的是打分函数，
        跟结合律没关系；把它算进来，这条线从第一站就是错的。
     ⭐⭐ 所以这里做两件事：**把它降为「第一次」**，
        同时**把第二章那一次归还给它真正所属的那条线**（硬件决定公式）。
     ⛔ 判据：一条线数不准的时候，通常不是漏了一次 ——&nbsp;
        是**把不同类的东西算进了同一条线**。 -->
<div class="note ok"><p>⭐⭐ <b>这是这本书第一次真的「挪括号」。</b>
  <em>——&nbsp;一共只有两次，另一次在<a href="#s七">第七章</a>，那一次更赚。</em></p>
<p><em>⛔ <a href="#s二">第二章</a>那次<b>不算</b>：点积赢加性，换的是<b>打分的公式</b>，
  跟结合律没关系。<b>那一次属于另一条线</b> ——&nbsp;
  「<b>能写成什么形状</b>」从那以后就一直在替数学做决定。</em></p>
<p>⭐ <em>两次是同一个恒等式，而且<b>两次都被同一类东西挡过</b>：
  <b>中间夹了个带下标的家伙。</b>这一次是 RoPE。</em></p></div>

<!-- ⛔⛔⛔ 2026-09-16 **整节删除。** 现场原话：
     「5.3 凭什么敢压 ——&#160;一半是白送的，一半是在赌。这一小节就是在胡说。
       它这个低秩压缩，就算你左边那个例子，它也不是白送的 ——&#160;
       它能表现的那个秩就降低了，能存的信息就更少了。你一个大的方阵变成
       两个窄条相乘的时候，就限制了这个方阵里边数与数之间的独立性。
       所以这一节你讲不明白就删了吧，咱不讲这个。」

     ⭐ **这个批评是对的，而且指出的是框架错，不是措辞错。** 记在这里：
       · 那张图（fig3-lowrank）本身**数学上没错**：左栏是一个**真·秩 2** 的
         矩阵用 rank=2 去拆，误差恒等于 0。
       · ⛔ 错的是**拿它来给 MLA 背书**。它成立的前提是「矩阵本来就低秩」，
         而 MLA 把 KV 压到 512 维**根本不是这种情况** ——&#160;
         那是「硬压」，是图里右栏那一格。**所以「一半是白送的」不成立。**
       · ⭐ MLA 里**真正白送的**是另一样东西：**吸收**（把升维矩阵折进 Q 和
         输出矩阵），那是精确的代数恒等、零损失 ——&#160;
         但它省的是**算力和带宽，不是 cache 大小**。
         **这一节把「吸收是白送的」和「低秩是白送的」混成了一件事。**

     ⛔ 判据：**一个恒等式成立的前提，比恒等式本身更重要。**
       「秩够的时候低秩分解无损」是真的，可一旦拿它去论证一个「秩不够」的
       场景，它就从证据变成了障眼法。

     ⚠️ 同一套论证在 **L300 也有一份**（那边也用 fig3-lowrank）。
       本次只按现场要求删了 L200 这一节，**L300 没动** ——&#160;要一起删说一声。 -->

<h3>5.3　那条看起来很别扭的窄轨 ——&nbsp;可能才是它好的真正原因</h3>

<p>MLA 有一处形状很不好看：<b>它每个 token 要存的那一份，是 512 ＋ 64。</b>
  <em>512 是那份压缩件；多出来的 64 维专门用来装位置信息，<b>单独走一路</b>。</em><br>
  <b>为什么非得这么别扭？——&nbsp;下面这张图三步说完，不用读公式。</b></p>

__FIG_TWO_LANES__

<!-- ⭐ 2026-09-15 现场原话：「64 个维度的旋转编码，为什么它就影响了矩阵运算？」
     —— 图和正文都直接说「它带下标所以挡住了」，可读者连 RoPE 在干什么都还没被告知，
     「带下标」这三个字接不住。先用一段说清它怎么工作，那道闸才立得起来。 -->
<div class="note warn"><p>⚠️ <b>那道闸凭什么挡得住？</b></p>
<p><b>「吸收」要求夹在 Q 和压缩件中间的那个东西是一张<u>固定</u>矩阵</b>
  ——&nbsp;<em>固定，才能提前折进上投影矩阵里，运行时不再出现。</em></p>
<p>⛔ <b>RoPE 不是。</b><em>它对 K 施加的旋转角取决于 query 与 key 的<b>相对</b>位置，
  每一对「谁问谁」对应一张不同的旋转矩阵 ——&nbsp;没有哪一张能提前折进去。
  <b>一折，absorption 当场失效。</b></em>
  <span class="sub">（RoPE 的职责 <a href="#s零">§0</a> 已经摆正过：顺序不归它管，
  它加进来的是「我跟它差几格」——&nbsp;而「相对」这三个字，正是这里挡住吸收的原因。）</span></p>
<p>⭐⭐ <b>MLA 的解法是解耦</b>：<em>另开一段 <b>64 维</b>专门承载位置信息
  ——&nbsp;<u>不走压缩、直接存</u>，而且跟压缩件一样<b>128 个头共享同一份</b>
  （所以是 ＋64，不是 ＋128×64）。于是每 token 每层要存的是：</em></p>
<p class="landing"><code>512（latent 内容） ＋ 64（共享 RoPE） ＝ 576</code></p></div>

<!-- ⭐⭐⭐ 2026-09-16 现场指着这一段说：「这一段对主线没有帮助，而且我们
     现在也讲不明白，把它折叠起来。」
     ⛔ 折的是**后半段**（那组受控实验：GQA2-128 → GQA1-256 → GQA1-256-PR
       的两级台阶，以及「这两样一样都不是低秩」那个减法）。
     ⭐ **前半段留着** ——&#160;那道 RoPE 闸「为什么挡得住」是主线，
       而且它回答的正是现场自己问过的那句「64 个维度的旋转编码，
       为什么它就影响了矩阵运算？」。
     ⚠️ 「讲不明白」这半句值得单独记：那张图的减法（两级台阶合计 −0.039，
       MLA 只好 −0.029）**是本课自己做的，不是原文的话** ——&#160;
       原文只说「增大 head_dims 收益最大，Partial RoPE 也有一定帮助」。
       ⛔ 判据：**自己从别人数据里做的减法，讲的时候要背两重责任**
       ——&#160;既要讲对数，又要讲清「这一步推理是我们加的」。
       两重都背不动的时候，**收起来比硬讲强。** -->
<details class="foldfig"><summary><b>那条「无奈」的窄轨，可能才是 MLA 好的原因</b>
  <span class="why">——&nbsp;一组受控实验：把 KV cache 钉死在 512，只改注意力的形状。
  <b>对主线没帮助，可以直接跳过</b>；想读的话，它的结论是
  「MLA 那条被逼出来的 64 维窄轨，恰好撞上了两件对的事」。</span></summary>

<p class="landing">📌 <b>读到这儿，很多人会觉得这是个妥协。</b>
  <em>——&nbsp;可有人做了一组受控实验，结论正好反过来。</em></p>

__FIG_MLA_CREDIT__

<div class="note ok"><p>⭐⭐ <b>苏剑林那组实验的三条结论，值得原样记住：</b></p>
<p><b>① 增大每个头的维度，收益最大</b> ——&nbsp;<em>比多分几组有效得多。</em><br>
  ② 只给一小部分维度加位置信息（<b>Partial RoPE</b>），对效果也有帮助。<br>
  ③ 让 K 和 V 共享，应该也有作用。</p>
<p>⛔ <em>而 MLA 那条「被逼出来」的 64 维窄轨，<b>恰好同时做到了 ① 和 ②</b>：
  它让吐字时每个头的宽度变成 512＋64，又顺手变成了「只给一小段加位置」。</em></p>
<p>⭐ <b>所以那不是妥协，是撞上了对的东西。</b></p></div>


</details>

<h3>5.4　本章落点：MLA 真正漂亮的地方，是它同时向两套账交了差</h3>

<p>还记得<a href="#s三">第三章</a>那件事吗 ——&nbsp;
  <b>同一个模型，读题的时候和吐字的时候，瓶颈根本不是一回事。</b>
  <em>把这句话往下推一层，就能看见 MLA 到底在解什么题。</em></p>

<div class="note info"><p>📌 <b>两边想要的东西，正好相反。</b></p>
<p><b>吐字那一边</b>卡在 KV cache 上。<em>所以给定一个缓存预算，
  理论上最好的做法是：把这个预算<b>全给一个头</b>，K 和 V 还共用
  ——&nbsp;因为不管 MHA 还是 GQA 都能被改写成这种形状，它是它们的超集。</em><br>
  ⭐ <b>为什么是超集？</b><em>——&nbsp;「一份大的、所有人共用」这种形状，
  <b>能表达</b>「切成 128 小份各带各的」；<b>反过来不行</b>。
  同样的字节数，它装得下最多的东西。</em></p>
<p><b>读题那一边</b>卡在算力上，而算力主要看头数 × 每头维度。
  <em>每头维度从 128 抬到 512，<b>计算量就是四倍</b>。
  所以这边最想要的，是老老实实的 MHA-128。</em></p>
<p>⛔ <b>一边要 512，一边要 128。这是个死结。</b></p></div>

<div class="note ok"><p>⭐⭐⭐ <b>MLA 的大招，就是把这个死结解开了。</b></p>
<p>它分两步投影：先把输入压成一个 512 维的向量，再从这个向量展开成多个 128 维的头。
  <em>然后靠上面那个「挪括号」的恒等变换 ——</em></p>
<!-- ⛔ 2026-09-15：原来写「吐字的时候是一个 MQA-512」——&nbsp;漏了那条窄轨。
     上一节刚说完「每个 token 要存的是 512 ＋ 64」，隔一段就写成 512，
     台下会问「那 64 去哪了」。⭐ 真正变成纯 512 要等到第八章的 NoPE。 -->
<p><b>训练和读题的时候，它是一个 MHA-128；吐字的时候，它是一个 MQA-576</b>
  <em>（512 的压缩件 ＋ 那条 64 维的窄轨 ——&nbsp;
  <b>到<a href="#s八">第八章</a>那个 NoPE，这 64 会整个消失，那时才是纯 512</b>）。</em><br>
  <em>同一套权重，两种算法，各自站在自己那一边的最优点上。</em></p>
<p>⭐ <em>苏剑林把这件事叫做「<b>Prefill 和 Decoding 的双向奔赴</b>」，
  并且给了一个很高的判断：<b>在相同的训练成本和推理成本下，
  MLA 可能是效果最好的完整注意力变体。</b></em><br>
  <span class="sub">⚠️ 这是他基于自己那组实验和一套简化假设下的推断，
  他本人也写明了「实际情况复杂，结论大概率会有偏差」。</span></p></div>

<div class="note danger"><p>⛔ <b>但它也有一处很实在的代价，而且正好长在它最漂亮的地方上。</b></p>
<p>GQA 的 KV 是按头切的，八张卡就一卡存八分之一，天然分得开。
  <em>而 MLA 的那一份压缩件<b>根本不按头分</b> ——&nbsp;
  于是多卡部署时要么每张卡各存一份（省下来的当场还回去几倍），
  要么把注意力那一段单独改成另一种并行方式。</em></p>
<p>⭐ <em>这又是本书那条暗线的反面：一个在单卡上极漂亮的数学结构，
  到了多卡上<b>恰恰因为「不按头分」而失去了最自然的切法</b>。</em></p></div>

<div class="note ok"><p><b>这一章的两句话。</b></p>
<p><b>换来的</b>：缓存压到几十分之一，<em>而且不是靠让大家看同一本 ——&nbsp;
  每个头拿到的 K/V 仍然各不相同。</em><br>
  <b>欠下的</b>：算力多花了一点，多卡切分变难了，
  <em>而且整套设计依赖一个<b>只在吐字时才成立</b>的代数把戏。</em></p>
<p>⭐⭐ <em>到这里，「<b>让每一份更小</b>」这条路基本走到头了。<br>
  <a href="#s六">下一章</a>连这个旋钮都不碰了 ——&nbsp;
  <b>KV 一个字节都不少存，照样能省。</b></em></p></div>

</div></section>


<section id="s六"><div class="wrap"><div class="stn"><span class="badge">第 六 节</span><h2>换个方向：不是存得少，是别全读 ——&nbsp;从滑窗到 DSA</h2></div>

<p class="lead">前两章一直在拧同一个旋钮：让每一份小一点。
  <em>这一章换一个完全没碰过的方向 ——&nbsp;每一份多大都不改，
  改的是<b>每走一步到底读进来几份</b>。</em><br>
  ⛔ <em>先把话说在前面：<b>这条路一个字节的显存都不省。</b></em></p>

<h3>6.1　先把这一支的全家福摆出来</h3>

<!-- ⭐ 2026-09-15 试讲补：这一节原来是**只有标题和一张图**。
     ⛔ 第一版我在这儿写了「五张一起看、骨架从摆在一起里浮出来」——&#160;
        **那是图注里已有的话**（查重没抓到，因为共有子串只有 6 字，不到 14）。
     判据⑩：两处说同一件事，挨着图的那一处赢。这里只留图注给不了的两样：
        **怎么排着读** 和 **骨架到底是哪一句**。 -->
<p>从左往右，<b>黑格子越来越少</b>：全都算 →&nbsp;只看身边 →&nbsp;只看身边再加开头几个
  →&nbsp;挑着看 →&nbsp;先压再挑。</p>

__FIG_KNOB2__

<div class="note ok"><p>⭐⭐ <b>五张图其实只是一句话 ——&nbsp;
  这一支从头到尾，都在回答「这一步，到底哪些格子<u>可以不算</u>」。</b></p>
<p><em>⭐ 记住这个问法。它跟前两章那个问法（「一份能做多小」）
  是<b>两个方向</b>，后面第八章要把这两个方向<b>同时</b>用上。</em></p></div>

<h3>6.2　最朴素的一招：只看身边那几个 ——&nbsp;以及它为什么会崩</h3>

<p>最容易想到的就是<b>滑动窗口</b>：每个字只看前面固定的几百个。
  <em>听上去很合理 ——&nbsp;说话本来就大多只跟附近有关。</em></p>
<p>可它有一个著名的崩法，而且<b>崩得毫无预兆</b>。</p>

__FIG_SWA_WHY__

<p>⛔ <b>这件事值得单拎出来讲，因为它是一整类 bug 的样板</b> ——&nbsp;
  <em>从公式上你看不出任何问题；是有人<b>把注意力矩阵画出来</b>才看见的。</em>
  <b>那下一张图就把「为什么」画出来。</b></p>

__FIG_SINK__

<div class="note ok"><p>⭐⭐ <b>这一条正好把<a href="#s二">第二章</a>那个伏笔收了。</b></p>
<p><em>当时 Bengio 说，注意力必须是「软」的 ——&nbsp;
  要有一份<b>总额固定</b>的注意力，可以分摊到所有位置上，这样才能训。</em></p>
<p>⛔ <b>「总额固定」是优点，也正是这里的病根。</b>
  <em>一个设计的副作用，十有八九跟它的优点是同一条性质 ——&nbsp;
  这条判据以后你会一直用。</em></p></div>

<h3>6.3　那就别只看身边，去把重要的挑出来 ——&nbsp;可这里有个死结</h3>

<!-- ⭐ 2026-09-15 试讲补：这一节原来是**只有标题和一张图**，正文是空的。
     图把死结的环画出来了，但下面这两句图上没有 ——&#160;
     一句是死结**为什么**是死结，一句是「两家独立做出来」这个事实**能推出什么**。
     判据①：讲台上说得出口、图上却找不到的那句话，必须留在正文里。 -->
<div class="note danger"><p>⛔ <b>这个死结之所以是死结，不在于绕圈，在于<u>钱</u>。</b></p>
<p><em>要挑出重要的，就得先知道谁重要；而「谁重要」这件事，
  <b>本身就是注意力分数</b>。<br>
  ⭐⭐ 所以等你挑得动的时候 ——&nbsp;<b>你想省的那一步，钱已经付过了。</b></em></p></div>

__FIG_CHICKEN__

<div class="note ok"><p>⭐ <b>图里那条「两家独立想到同一招」，值得比一个巧合多想一层。</b></p>
<p><em>一件事被<b>独立做对两次</b>，跟只有一家做过，分量不一样 ——&nbsp;
  那说明它不是谁的灵光一现，<b>是这个死结自己逼出来的解</b>。</em></p>
<p>⭐⭐ <em>看研究时这条可以一直用：<b>同一个坑上冒出两条互不知情的同形解法，
  往往说明约束比人聪明。</b></em></p></div>

<h3>6.4　DSA 是怎么破这个死结的 ——&nbsp;以及它一步到底在干什么</h3>

<p>DeepSeek 的破法是<b>师徒</b>：<em>让真的注意力当老师，
  另外训一个极便宜的学生，专门去学老师那张分布的<b>排序</b>。</em></p>

__FIG_DSA_WHY__

<!-- ⛔ 这块原来把图里那条落点带整段抄了一遍（查重抓到三段）。
     ⭐ 只留图给不了的：**这个套路在别处也长这个样子。** -->
<div class="note ok"><p>⭐ <b>图里那条套路，你在别处大概已经见过好几次了。</b></p>
<p><em>投机解码：让小模型先草拟，大模型只负责验收。<br>
  模型蒸馏：大模型的输出当标准答案，小模型照着学。<br>
  各种缓存预测器：真实访问序列当标准答案，训一个便宜的去猜。</em></p>
<p>⭐⭐ <b>同一个形状：贵的那个不用消失，它只要愿意当一次老师。</b></p></div>

<p class="landing">📌 <b>那 2048 个到底怎么挑出来的</b> ——&nbsp;
  <em>省了什么、没省什么、多花了什么，一张图答完。</em></p>

__FIG_DSA_FLOW__

<h3>6.4b　顺着这条路再走三步：NSA · CSA · HCA</h3>

<!-- ⭐⭐⭐ 2026-09-16 新增。原来 NSA / CSA 被明确标成「支线，当堂不讲」，只留 DSA。
     ⛔ 那个取舍成立于「一小时讲完」的前提，而这个前提已经没了。
     ⭐ 更要紧的理由：**这四个名字是同一条路上的四步**，拆开讲每一步都少一个
       对照组 ——&#160;尤其 DSA「为什么最碎」，不摆 NSA 的「挑块」在旁边说不清。
     ⛔ 用 6.4b 不新起 6.5：它们是 DSA 的兄弟，而 6.5 是整章落点，
       插在落点后面会把落点顶掉。
     ⛔⛔ 第一版正文把图里的话抄了一遍（查重当场 2 → 10）。
       判据⑩：挨着图的那一处赢。**正文只留图给不了的三样** ——&#160;
       四个名字各自一句的<u>索引</u>、出处与口径、以及它跟本讲判据的关系。 -->
<p class="lead">上一节那个 DSA，<b>不是这条路的终点，也不是起点。</b>
  <em>把它前后那三步一起摆出来，你会看到一条很干净的线。</em></p>

__FIG_DEEPSEEK_SPARSE__

<div class="note ok"><p>⭐⭐ <b>先给一份速查 ——&nbsp;四个名字，一人一句。</b>
  <em>（细节、两条轴、以及它们在 TPU 上的差别，全在上面那张图里。）</em></p>
<ul>
  <li><b>NSA</b>（2025-02）——&nbsp;<em>三条支路 ＋ 门控。<b>挑块</b>，训练时就稀疏。</em></li>
  <li><b>DSA</b> ——&nbsp;<em>轻量索引器打分，<b>挑散落的单 token</b>。更准，也最碎。</em></li>
  <li><b>CSA</b> ——&nbsp;<em>先压 4 倍<b>再挑</b>。要挑的池子小了四倍。</em></li>
  <li><b>HCA</b> ——&nbsp;<em>压 128 倍，<b>挑这一步整个取消</b>。</em></li>
</ul></div>

<!-- ⭐⭐⭐ 2026-09-16 试讲现场问出来的两条，原样收进来。
     ⛔ 两条都不是「讲得不够细」，是**图和正文都默认了一件读者没有的前提**：
       ① 默认读者知道压缩器和滑窗是两条并排的支路（图上画了，但没说它俩是加起来的）；
       ② 默认读者不会把这里的「压缩」跟 §五 MLA 的「压缩」当成同一件事 ——&#160;
          可它俩同名、又挨着讲，混起来太自然了。
     ⭐ 判据：**同一个词在两章里指不同的轴，必须在第二次出现的地方当场分开。**
     ⛔ 措辞上避开图里那句「远处压得很糊、近处一个不落」（判据⑩：挨着图的那一处赢），
       这里只写图给不了的：误解本身、两条支路的加法关系、以及跟 MLA 的轴对照。 -->
<div class="note"><p>⭐⭐ <b>这里有两个坑，几乎每个人第一次读都会掉进去。</b></p>

<p>❶ <b>「压缩难道还分远近吗？不是统一压的吗？」</b>——&nbsp;
  <em><b>是统一压的。</b>压缩器一视同仁，<b>不认远近</b>，每 128 个 token 合成一条。</em></p>
<p><em>近处之所以还清楚，<b>不是压缩器手下留情，是旁边那条支路给的</b> ——&nbsp;
  一个<b>局部滑窗</b>（默认 128）把身边那一段<b>原样留着、根本不进压缩器</b>。</em></p>
<!-- ⛔⛔ 这里原来写「两条支路的结果加起来，才是这一层的输出」——&#160;**是错的**。
     那是 NSA 的门控融合，被我顺手套到了 V4 头上。扒了 MaxText 源码才发现
     V4 走的是拼接：compressed_kv 直接 concat 到 kv 后面，一次 softmax 一起算。
     ⭐ 判据（又一次）：**「听起来像常识的架构关系」最危险** ——&#160;
       三条支路 → 门控融合，在 NSA 上成立，在 V4 上不成立。 -->
<p><em>⭐ 而两段<b>不是各算一遍再融合</b>（那是 NSA 的做法）——&nbsp;
  <b>压缩块直接拼在未压缩那段的后面，凑成一条更长的 KV 序列，一次注意力一起看。</b>
  <span class="sub">（源码：<code>jnp.concatenate([kv, compressed_kv], axis=1)</code>）</span></em></p>
<p><em>⭐⭐ 所以这个「越远越粗」是<b>两条支路拼出来的</b>，压缩本身没有这个概念。
  ——&nbsp;而这一手各位刚在 Ⓐ 见过：<b>NSA 的 ① 压缩管远、③ 滑窗管近。
  HCA 就是把 NSA 中间那条「选择」砍掉之后剩下的东西。</b></em></p>

<p>❷ <b>「用的时候要不要解压缩？还是像 MLA 那样把矩阵吸收了？」</b>——&nbsp;
  <em><b>不用解压。但原因跟 MLA 完全不是一回事 ——&nbsp;它俩压的根本不是同一个轴。</b></em></p>
<ul>
  <li><b><a href="#s五">§五</a> 的 MLA 压的是<u>维度</u>轴</b> ——&nbsp;
    <em>「<b>一个 token 有多宽</b>」。宽的 K/V 压成一条窄的潜向量。<br>
    ⛔ 维度方向压完，<b>逻辑上必须升回去</b>，否则点积对不上宽度 ——&nbsp;
    <b>「吸收」这一手就是为此而生的</b>：把升维矩阵提前乘进 Q 和输出矩阵，
    于是物理上不用真展开。</em></li>
  <li><b>CSA / HCA 压的是<u>序列</u>轴</b> ——&nbsp;
    <em>「<b>一共有多少个 token</b>」。128 行合成 1 行。<br>
    ⭐ 而压出来那一条，<b>本身就是要参与注意力的 K 和 V</b>，
    它不是谁的压缩包，<b>它就是本体</b> ——&nbsp;<b>所以没有「还原」这回事。</b></em></li>
</ul>
<p>⭐⭐⭐ <em>两句话摆一起，形状很清楚：<b>MLA 是「需要解压，但我们想办法不解了」；
  这一支是「压根没有解压这一步」。</b></em></p>
<p>⛔ <em><b>代价也跟着这两个轴分开走。</b>维度方向是<b>低秩近似</b>，有损，
  但<b>每个 token 都还在</b>；序列方向是把 128 个 token <b>合掉</b> ——&nbsp;
  它们各自的身份<b>永久没了，不可逆</b>。
  ⭐ <b>这才是那条滑窗支路非挂不可的真正理由</b> ——&nbsp;
  不挂它，连刚说完的上一句都是一团摘要，模型接不上话。</em></p></div>

<h3>6.4c　把 V4 的一条 KV 拆开看 —— 它跟 MLA 到底差在哪</h3>

<!-- ⭐⭐⭐ 2026-09-16 下午重写。第一版是**只读 MaxText 代码**写的，讲拧了三处：
     ① 说「两条支路的结果加起来」—— 其实是**拼接**（已在 6.4b 改掉）；
     ② 把它说成「简单 MQA、跟 MLA 没关系」—— **现场的直觉才是对的**：
        论文的正式名字是 Shared Key-Value Multi-Query Attention，
        它确实是「所有头共用一条压缩体」。错的是我把差别说成了「有没有压缩体」。
     ③ 完全没交代那 64 维 RoPE 去哪了。
     ⛔ 判据（第三次应验）：**读实现只能告诉你「怎么做的」，说不清「为什么这么做」。**
       口径与命名要回论文和官方文档，代码只做交叉验证。
     ⛔ 本节全部结论都带出处，写在图的「出处与口径」里，不要删。 -->
<p class="lead">上一节那四个名字讲完，最常被追问的是同一句：
  <b>「那它一条 KV 里到底存的是什么？跟 MLA 到底差在哪？」</b>
  <em>⭐ 这一节把 <b>DeepSeek-V4-Pro</b> 的一条 KV 整个拆开。</em></p>

__FIG_V4_ARCH__

<div class="note ok"><p>⭐⭐ <b>先说那个最自然的误会 —— 而它有一半是对的。</b></p>
<p><em>「看上去跟 MLA 一样，所有头最后变成一条 512 的压缩体」</em>
  —— <b>这个直觉是对的。</b><em>论文给它的正式名字就是
  <b>Shared Key-Value Multi-Query Attention</b>：<code>num_key_value_heads = 1</code>，
  128 个查询头<b>共读同一条</b>。</em></p>
<!-- ⛔ 这里原来把图 Ⓐ 最底下那句结论**原样抄了一遍**（查重 2 → 7）。
     判据⑩：挨着图的那一处赢。正文换一个图上没有的比方，别复述。 -->
<p>⛔ <em>差别用一个比方最清楚：<b><a href="#s五">§五</a> 的 MLA 存的是<u>原料</u>，
  V4 存的是<u>成品</u>。</b>原料下锅前得先加工一道，成品端上来就能吃。<br>
  ⭐ <b>「吸收」那一手，本质上是把那道加工工序提前折进别的矩阵里；
  而 V4 这边，<u>那道工序压根不存在</u>。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-16 现场追问追加的三块。原话：「你还没有说那个 Q 到底是
     1536 维度的 Q，还是按 MQA 的方式生成一个 512 的 Q」「SWA 也是用的 MQA 吗」
     ⭐ 以及那个**量化论证**（画进图 Ⓐb 的 band 了）：不以 MLA 为基础，
       光压 4:1 还不如 MLA ——&#160;这条是对的，而且是整套设计的命门。
     ⛔ 正文只写图上没有的：Q 的完整路径、与 V3 的对照、我自己算的账、SWA 的归属。 -->
<div class="note"><p>❓ <b>那 Q 呢？是一条 1536 的 latent，还是按 MQA 只生成一条 512？</b></p>
<p><em>—— <b>都不是，但更接近前者：Q 走 latent，而且最后是完整的多头。</b></em></p>
<ul>
  <li><em>hidden <b>下投</b>到 <code>q_lora_rank ＝ 1536</code> 的一条潜向量</em></li>
  <li><em>RMSNorm 之后<b>上投</b>成 <b>128 个查询头 × 每头 512 维</b>
    <span class="sub">（Pro 是 128 头；Flash 才是 64 头，别记混）</span></em></li>
  <li><em>再做 partial RoPE 与缩放，然后去和那<b>唯一一条</b> 512 点积</em></li>
</ul>
<p>⭐⭐ <em>值得注意的是：<b>这个 1536 跟 V3 / V3.2 的 <code>q_lora_rank</code>
  一模一样</b> ——&nbsp;<b>查询侧 V4 根本没动，沿用的就是 MLA 那一套。</b>
  改的全在 KV 侧。</em></p>
<p>⛔ <em>还有一条容易忽略的：<b>Q 不进 cache。</b>
  所以查询侧这个低秩压缩<b>省的是参数量和算力，不是显存</b> ——&nbsp;
  跟这一章讨论的那两个轴，不是一回事。</em></p></div>

<!-- ⭐⭐ 这一块原来是我自己按定义推的估算（11%–13%）。后来在 vLLM 那篇的
     **附录**里找到了他们自己的算术，逐项对上了 ——&#160;于是换成确切数字，
     并更正一处：CSA / HCA 的层数是 **30 / 31**，我原先写的 29 差了一层。
     ⭐ 判据：**自己推的数只是入场券，对上外部锚点之后才配写进教材。** -->
<div class="note ok"><p>⭐ <b>把账走一遍 ——&nbsp;这几个数能反过来验证整套分解。</b>
  <em>（1M 上下文，bf16，全部取自 vLLM 那篇的附录。）</em></p>
<ul>
  <li><b>V3.2</b>：<em>MLA 每 token 每层 <b>1152 字节</b>（576 × 2），
    索引器再加 <b>256 字节</b> ——&nbsp;合计 <b>1408</b>。
    61 层下来约 <b>83.9 GiB</b>。</em></li>
  <li><b>V4 的一条共享 KV</b>：<em><b>1024 字节</b>（512 × 2）。</em></li>
  <li><b>CSA 层</b>：<em>条数只有 1/4 ——&nbsp;主 KV 256 MiB ＋ 索引器 64 MiB
    ＝ <b>320 MiB</b>。</em></li>
  <li><b>HCA 层</b>：<em>条数只有 1/128 ——&nbsp;<b>8 MiB</b>。</em></li>
</ul>
<p>⭐⭐ <em><b>30 层 CSA ＋ 31 层 HCA，合计约 9.62 GiB</b> ——&nbsp;
  对 83.9 GiB 就是 <b>11.5%</b>。</em></p>
<p><span class="sub">📌 论文自己标的是 <b>10%</b>，差的那点应该是口径不同
  （论文还提到实跑用 FP4 存专家权重与索引器 QK）。
  <em>⛔ 逐项算术在图的「出处与口径」里。</em></span></p></div>

<div class="note"><p>❓ <b>那滑窗那 128 条，用的也是 MQA 吗？</b>
  <em>—— <b>是，而且是<u>同一条</u>，不是另起一套 KV。</b></em></p>
<p><em>官方文档里这个分支的名字就叫
  <b>Shared sliding-window K=V branch</b> ——&nbsp;
  它用的就是主路径那个投影：<b>1 个 KV 头 × 512 维，K 和 V 共用</b>。</em></p>
<p>⭐⭐ <em>所以这正好又是「<b>两个轴各管各的</b>」的一个实例：
  滑窗那一段<b>序列轴没压</b>（每个 token 各占一条，没被合并），
  但<b>宽度轴照样是压过的</b>（还是那条 512）。<br>
  ——&nbsp;<b>滑窗只决定「这一段不合并」，它不决定「每条多宽」。</b></em></p></div>

<h4>6.4c-1　为什么 2019 年被判「不行」的 MQA，现在又行了</h4>

<!-- ⭐⭐⭐ 2026-09-16 现场追问。原话：「他们为什么把这个 MQA 又给捞回来？
     这个 MQA 一开始不是证明他的能力不太行吗？什么时候 MQA 又好上了？」
     ⛔⛔ **论文没有正面回答。** §2.3.3 与 §2.3.4 都没有一段解释「共享 KV
       为什么不掉点」——&#160;§2.3.4 只在比 KV cache 大小时引了 GQA。
     ⭐ 所以本小节是**推导**，图 Ⓐc 上也标了。吸收恒等式本身是公开技巧
       （DeepSeek-V2 就有），「V4 ＝ MLA 吸收形态的原生化」是我们的判断。 -->
<p class="lead">这是个好问题，<b>而论文没有正面答它。</b>
  <em>⭐ 但答案能从架构本身推出来，而且推得很干净。</em></p>

<p class="landing">⭐ <em>恒等式、两边对照、以及「不是零损失」那一条，
  都在上面那张图的 <a href="#fig-v4-arch">Ⓐc 那一格</a>。</em></p>

<div class="note ok"><p>⭐⭐ <b>关键在于：它压根不是 2019 年那个 MQA。</b></p>
<p><em>图 Ⓐc 最上面那个恒等式说的是 ——&nbsp;<b>MLA 在「吸收」之后，
  本来就长成一个 head_dim ＝ 512 的 MQA。</b>
  所以 V4 不是「把 MQA 捞回来」，是<b>把 MLA 原本就存在的那个等价形态，
  直接拿来当架构</b>。</em></p>
<p>⭐ <em>而 2019 年那个之所以掉点，<b>毛病不在「共享」，在「删了不补」</b>：
  共享的那条只有一个头宽，query 侧也没加宽，
  <b>被删掉的每头视角没有任何东西接手。</b><br>
  V4 这边，<b>那份视角搬到了 query 侧和输出侧</b> ——&nbsp;它没消失。</em></p></div>

<!-- ⛔ 这一块原来把图 Ⓐc 那条 band 几乎整段复述（查重 2 → 5）。
     判据⑩：挨着图的那一处赢。正文只留图上没有的两样 ——
     ① 那个「原生」类比在本讲是第二次出现，② 它对应的判据。 -->
<div class="note danger"><p>⛔ <b>图 Ⓐc 底下那条 band 请一定读完 ——&nbsp;
  它讲的是这笔买卖的代价。</b><em>这里只补两件图上没写的。</em></p>
<p>❶ <em>那条里的第三样（<b>原生训练</b>）是<b>类比</b>得来的，<b>论文没这么说</b>。
  ——&nbsp;而各位应该觉得眼熟：<b><a href="#fig-deepseek-sparse">NSA 名字里那个
  Native</a></b>，讲的是同一件事。</em></p>
<p>❷ <em>所以这是本讲第二次撞上同一条判据：<b>一个结构「长在上面」，
  和「事后套上去」，是两回事。</b><br>
  ⭐⭐ <b>它值得单独记一笔</b> ——&nbsp;因为很多「某某方法没用」的旧结论，
  失效的原因恰恰是<b>当初只在「事后套」的设定下测过</b>。</em></p></div>

<div class="note"><p>❓ <b>那 64 维的 RoPE 去哪了？</b>
  <em>—— <b>它没被挪走，它长在那 512 里面。</b></em></p>
<ul>
  <li><b>V3.2 的 MLA</b>：<em>512 的潜向量 <b>＋</b> 64 的 RoPE 分量，
    <b>两块分开存</b> ＝ <b>576</b>。</em></li>
  <li><b>V4</b>：<em>一共就 <b>512</b>。<b>partial RoPE</b> 只旋转每个头
    <b>末尾的 64 个通道</b>，前面 448 个不带位置。<b>RoPE 不额外占地方。</b></em></li>
</ul>
<p>⭐⭐ <em>一句话记住：<b>V3.2 的 64 是加在 512 外面的，V4 的 64 是长在 512 里面的。</b></em></p></div>

<div class="note danger"><p>⛔⛔ <b>K 和 V 共用一条，是要还债的。</b></p>
<p><em>既然 K 和 V 是同一个张量，那 <b>V 也跟着被 RoPE 转过了</b> —— 
  而 V 本来不该带位置。</em></p>
<p>⭐ <em>所以算完注意力之后，<b>输出的 rope 那 64 维要用位置 <code>−i</code>
  再反向转一次</b>（论文 §2.3.3 式 26）。转回来之后，
  <b>每条 KV 的贡献才只跟它到查询的「相对距离」有关。</b></em></p>
<p><span class="sub">⚠️ <b>这一步很容易被漏掉</b>，但它是「存一条当两条用」这笔买卖的
  全部代价 —— <em>省了一半显存，多了一次逐元素的旋转。</em></span></p></div>

<div class="note"><p>⭐ <b>最后两个数，图上都有，但它们放在一起才有意思。</b></p>
<p><em>① <b>CSA 的 <code>index_topk</code> 是 1024，而 V3.2 的 DSA 是 2048</b>
  ——&nbsp;<b>挑得更少了。</b>不是因为变胆小了，是因为
  <b>池子已经先被压过一道</b> ——&nbsp;挑 1024 条摘要，
  背后是 4096 个 token。</em></p>
<p><em>② <b>61 层里只有最后 1 层是纯滑窗</b>（图 Ⓑ 有完整层表）
  ——&nbsp;<em>也就是说，<b>压缩不是“可选项”，是这个模型的常态。</b></em></em></p>
<p>⛔ <em>而图 Ⓑ 底下那段说清楚了滑窗为什么非挂不可。
  请把结论记住：<b>它不是精度选项，是可用性底线。</b></em></p></div>


<div class="note danger"><p>⛔ <b>为什么值得把这四步连起来看：它是本讲那条判据的又一次应验。</b></p>
<p><em>这四步不是「四种不同的稀疏」，是<b>同一个动作被一步步往前挪</b> ——&nbsp;
  从「跑起来才决定」，一路挪到「编译的时候就定死」。</em></p>
<p>⭐⭐ <em>而最后那一步，<b>不是把麻烦解决了，是让麻烦不再存在</b> ——&nbsp;
  跟 <a href="#s八">第八章</a>那条窄轨消失是同一个形状。</em></p>
<p><span class="sub">📌 <b>出处与口径。</b>NSA ＝ Native Sparse Attention，
  <b>arXiv 2502.11089</b>（DeepSeek，2025-02）。
  <b>CSA / HCA 的结构与默认参数取自 MaxText 的公开实现</b>（Apache-2.0）——&nbsp;
  两者同属一种「压缩注意力」，按压缩比分叉：<b>等于 4 走 CSA</b>（必须提供索引器的
  mask），<b>大于 4 走 HCA</b>（改用编译期静态 mask，默认压缩比 128、局部窗口 128）。
  <em>⛔ <b>本节不含任何性能数字</b> ——&nbsp;本课没有这四者的对照实测，画柱子就是编。</em></span></p></div>


<h3>6.5　本章落点：这一章最该记住的，是它<u>没</u>解决的那一栏</h3>

<div class="note danger"><p>⛔⛔ <b>「稀疏就能省显存」是个极自然、也极常见的误会 ——&nbsp;
  上面那张图的第三栏专门空着给它。</b></p>
<p>⭐ <em>所以这个旋钮跟前两章根本不是一回事：<b>前两章动的是显存，
  这一章动的是时间。</b></em></p>
<p>⛔ <em>而这一栏空着，也正是<a href="#s八">第八章</a>存在的理由 ——&nbsp;
  <b>既然一个旋钮补不齐，那就几个一起拧。</b></em></p></div>

<div class="note ok"><p><b>这一章的两句话。</b></p>
<p><b>换来的</b>：长上下文下那一段从「跟长度平方成正比」变成「跟长度成正比」，
  <em>而且上下文越长赚得越多。</em><br>
  <b>欠下的</b>：显存一分没省；索引器要扫全程；<em>挑中的那些散落在各处，
  <b>搬起来很碎</b> ——&nbsp;这一笔到<a href="#s九">第九章</a>会变成真正的麻烦。</em></p>
<!-- ⛔ 2026-09-14 R14：这里原来把第七章开头那两句**逐字**说了一遍
     （「最狠 ——&nbsp;它干脆不存」「那条路的终点，是第一章那个盒子」）。
     ⭐⭐ 后一句尤其不能说两遍 ——&nbsp;**它是全书最大那个呼应的揭晓时刻**，
        提前剧透一次，第七章开头那一下就没了力气。留悬念，让章首去揭。 -->
<p>⭐⭐ <em>而现在三条路都走过一遍了：<b>存得少、读得少</b>……
  <b>还有第三条，它比这两条都狠。</b></em></p></div>

</div></section>


<section id="s七"><div class="wrap"><div class="stn"><span class="badge">第 七 节</span><h2>绕回原点 ——&nbsp;还是那块固定大小的记事板</h2></div>

<p class="lead">三条路走到这儿：一条改「一份有多大」，一条改「每步读几份」。
  <em>这一条最狠 ——&nbsp;<b>它干脆不存。</b></em><br>
  ⭐ <em>而这条路的终点，是<a href="#s一">第一章</a>那个盒子 ——&nbsp;
  隔了五章，我们要把当年扔掉的东西<b>再捡回来</b>。</em></p>

<h3>7.1　它的立身之本：把括号再挪一次</h3>

<p>2020 年有人写下了一句在当时听起来相当刺耳的话：
  <em><b>真正卡住注意力的，其实是那个 softmax。</b></em></p>
<p>理由只有一行：<em>因为要对所有位置求和归一化，那张「谁对谁」的大表<b>躲不掉</b>，
  必须先整个造出来 ——&nbsp;而它就是那个平方。</em></p>
<p>⭐ 可要是把 softmax 拿掉呢？
  <em>剩下的就是三个矩阵连乘 ——&nbsp;<b>而矩阵乘法是可以挪括号的。</b></em></p>
<p><span class="sub">⚠️ <b>「把 softmax 拿掉」是这一节的简化说法</b> ——&nbsp;
  <em>严格讲拿掉的只是<b>指数</b>那一半，归一化的分母其实留着。
  这个区别在 <b>7.2 末尾</b>会用到（它正好回答「一直加会不会爆」），
  <b>这里先按简化版往下读不影响。</b></em></span></p>

__FIG_ASSOC__

<div class="note ok"><p>⭐⭐ <b>这是这本书第二次、也是最赚的一次「挪括号」。</b>
  <em>——&nbsp;一共就两次。</em></p>
<p><em><a href="#s五">第五章</a>：把括号挪到提问那侧，压缩包一个都不用拆。<br>
  这一次：括号一挪，那张句长 × 句长的大表<b>根本没被造出来</b>。</em></p>
<p>⛔ <em>两次都是同一个恒等式；而两次都被「中间夹了个带下标的东西」挡过 ——&nbsp;
  <b>上一次是 RoPE，这一次是 softmax 那个分母。</b></em></p></div>
<!-- ⭐⭐ 2026-09-14 R15：给这条线补一个落点。外部评审报的原话是
     「这条线在 7.1 讲完就没了，全书没有收口」——&nbsp;属实。
     ⭐ 而它的落点正好能跟第八章的 NoPE 接上，两条线在那里合流。 -->
<p class="landing">⭐⭐ <em>所以这条线真正的教训只有一句：
  <b>当你发现一个变换「挪不动」，先别改算法 ——&nbsp;先去看中间夹的是谁，它凭什么在那儿。</b></em><br>
  <em>⭐ <a href="#s八">第八章</a>那个 NoPE，就是照这句话做的：<b>它把夹在中间的那个直接请走了。</b></em></p>

<h3>7.2　挪完之后，它其实变成了什么</h3>

<p><b>一个固定大小的状态。</b>
  <em>每来一个字，把它揉进这个状态里；要输出的时候，从这个状态里取。</em></p>

<!-- ⭐⭐⭐ 2026-09-15 现场提问补：「那个『加』到底是什么意思？数学上成立吗？
     token 的向量是怎么加到板子上去的？」
     ⛔ 这三问全书一处都没答过 ——&#160;而「揉进去」「加一笔」这种说法**通篇在用**。
     ⭐ 不答清楚，后面「d 维最多 d 个正交方向」「叠糊了」「delta rule」
       三件事全都是悬空的断言。这一格是它们共同的地基。 -->
<div class="note info"><p>📌 <b>「揉进去」到底是怎么揉的？——&nbsp;这一格把它说死。</b></p>
<p>⛔ <b>先排掉一个很自然的误解：<u>不是</u>把这个字的向量直接加到板子上。</b>
  <em>维度都对不上 ——&nbsp;板子是一个 <b>d×d 的方块</b>，
  而一个字的向量只是<b>一条 d 长的线</b>。</em></p>
<p>⭐ <b>真实的动作分两步：</b></p>
<ol>
  <li><b>拆成一对。</b><em>这个字先被两个<b>学出来的</b>矩阵投影成
    <b>k</b>（钥匙）和 <b>v</b>（内容），各是一条 d 长的线。</em></li>
  <li><b>焊成一张卡片。</b><em>把 k 和 v 做<b>外积</b>（<code>v·kᵀ</code>）——&nbsp;
    两条 d 长的线相乘，得到一个 <b>d×d 的方块</b>。
    <b>板子上加的是这张卡片，不是零件。</b>现在维度对上了。</em></li>
</ol>
<p>⭐⭐ <b>那为什么「加」起来之后，还能把某一张单独取回来？</b>
  <em>关键在<b>取</b>的动作也是线性的：拿钥匙 <b>q</b> 去问，做的是
  <b>板子 × q</b>。<br>
  而板子是一堆卡片加起来的，所以这一乘就<b>自动散开</b>成：
  <b>每张卡片的内容 v，各自乘上「它那把钥匙和 q 有多像」。</b></em></p>
<p>⛔ <b>于是取出来的东西一定是两截：</b>
  <em><b>① 你要的那一张</b>（它的钥匙跟 q 完全一样）；
  <b>② 加上所有别的卡片漏过来的一点</b> ——&nbsp;
  <b>漏多少，取决于它们的钥匙跟 q 有多像。</b></em></p>
<p>⭐⭐⭐ <b>第二截就是「串音」，也就是后面 7.3 说的「叠糊了」。</b>
  <em>如果所有钥匙<b>两两垂直</b>，串音正好全是零 ——&nbsp;取回来完全干净。
  <b>而 d 维空间里最多只能有 d 个两两垂直的方向</b> ——&nbsp;
  <b>这就是那条「板子只放得下 d 条」的出处，它不是个比喻，是能验算的。</b></em></p>
<div class="note danger"><p>⛔⛔ <b>顺手记一条判据，它比这一节本身更通用：
  <u>「加法」之所以有意义，是因为「读法」是线性的。加和读必须配成一对。</u></b></p>
<p><em>softmax 那套读法是<b>非线性</b>的（分母要对所有位置求和），
  所以它<b>没法</b>用加法把历史压在一起 ——&nbsp;它只能把每一条<b>原样留着</b>。<br>
  ⭐ <b>回头看 7.1 就通了：先拿掉 softmax，不只是为了挪括号 ——&nbsp;
  是拿掉它之后，「加」这个动作才第一次变得可逆、可取回。</b></em></p></div></div>

<!-- ⭐⭐⭐ 2026-09-15 现场追问：「一直加不会爆炸吗？总得配个 norm 吧？」
     ⛔ 查证时发现**本讲 7.1 那句「把 softmax 拿掉」是简化说法** ——&#160;
        原论文公式 (5) 里**分母是在的**，被拿掉的只是 exp。
     ⭐ 这一条必须写出来，不能让简化说法在课里当真话用。
     出处：Katharopoulos 等, arXiv 2006.16236, 式 (5) 与式 (7)。 -->
<div class="note danger"><p>⛔⛔ <b>那「一直加」不会爆吗？——&nbsp;这一问逼出一个
  <u>本讲前面说漏了的事实</u>。</b></p>
<p>⭐ <b>先认一件事：本节 <b>7.1</b> 那句「把 softmax 拿掉」是<u>简化说法</u>。</b>
  <em>翻回原论文（Katharopoulos 等，<b>arXiv 2006.16236</b>）的式 (5)，
  <b>分母是在的</b> ——&nbsp;它算的是
  <b>「这一条的相似度 ÷ 所有条相似度之和」</b>，跟 softmax 的形状<b>一模一样</b>。</em></p>
<p>⛔ <b>所以被拿掉的<u>不是归一化，是 exp</u>。</b>
  <em>softmax ＝ <b>先取指数</b>，再<b>除以总和</b>。线性注意力扔掉的是前一半
  （那层非线性的壳，也正是它锁死乘法顺序的原因），
  <b>后一半原封不动地留着了</b>。而且分母自己也是个累加量，
  <b>用同一招挪括号就能顺带算出来</b>，不多花什么。</em></p>
<p>⭐⭐ <b>于是「爆不爆」就清楚了：<u>分子在涨，分母也在涨</u>。</b>
  <em>取出来的是个<b>加权平均</b>，不是一个越堆越大的和。<b>这就是原版的 norm，
  它从第一天起就在里面。</b></em></p>
<p><span class="sub">⭐ 顺带一个容易被跳过的细节，它解释了那个奇怪的
  <code>elu(x)+1</code>：<em>论文明说选它是为了让相似度<b>恒为正</b>
  （式 7）。<b>因为分母要是能过零，除法当场就炸。</b>
  至于为什么不用 relu ——&nbsp;原文的理由是 relu 在负半轴<b>梯度为 0</b>，训不动。</em></span></p>
<div class="note ok"><p>⭐⭐⭐ <b>但这一支后来<u>主流并不靠那个分母</u> ——&nbsp;
  它换了一条更狠的路，而那条路正好是<b>下一节（7.3）</b>整节的内容。</b></p>
<p><em>做法是：<b>每一步先把板子整体乘掉一点，再加新的。</b>
  于是「一直加」变成了「<b>一边加，一边一直在忘</b>」——&nbsp;
  加的是等比级数不是等差级数，<b>它自己就收敛，根本不需要谁来压。</b></em></p>
<p>⛔ <em>所以你的直觉对了一半：<b>确实得有东西管住它</b> ——&nbsp;
  但管住它的<b>主力不是外面那层 norm，是「会忘」这件事本身</b>。
  <b>外面通常还有一层 norm，那是兜底，不是主因。</b></em></p></div></div>
<p class="landing">📌 <em>线性注意力<b>本质上就是一个 RNN</b> ——&nbsp;
  只不过它的状态不是一个向量，是一个矩阵。</em></p>

__FIG_NOTEPAD__

<div class="note ok"><p>⭐⭐⭐ <b>值得停一下：这块板子，和<a href="#s一">第一章</a>那个盒子，是同一样东西。</b></p>
<p><em>形状一模一样：大小固定、一步一步往下传、<b>成本跟话有多长无关</b>。<br>
  差别只有一处 ——&nbsp;当年那个是个<b>向量</b>，这一块是个<b>矩阵</b>。</em></p>
<p>⭐ <em>而这一处差别正是三十年里最要紧的那一处：<b>板子变大了，而且是能变大的。</b>
  当年 RNN 的状态跟输入维度同量级，是因为更新一次太贵；
  换成外积累加之后，状态可以撑得很大，而一步的代价还是常数。</em></p></div>

<!-- ⭐ 2026-09-15 现场提问补：「它明明就是 RNN，为什么叫线性注意力？」
     这个问题每个听众都会冒出来，而课里之前**一个字都没交代**。
     答案不用猜 —— 那篇论文的标题和摘要第一句就把两层意思都写了。
     ⛔ 出处必须写死：arXiv 2006.16236，ICML 2020。 -->
<div class="note info"><p>📌 <b>顺手回答一个几乎人人会问的问题：
  它明明就是 RNN，为什么叫「线性注意力」？</b></p>
<p><em>先看命名它的那篇论文叫什么 ——&nbsp;
  <b>《Transformers are RNNs: Fast Autoregressive Transformers with
  Linear Attention》</b>（Katharopoulos 等，ICML 2020，arXiv <b>2006.16236</b>）。
  <b>「它是 RNN」这件事就写在标题的<u>正</u>标题里</b>，作者从第一天就知道。
  叫 linear attention 的是<b>副</b>标题里那个方法。</em></p>
<p>⭐⭐ <b>而「线性」这两个字，一个词管住了两件事，摘要里一句话说全：</b></p>
<ul>
  <li><b>① 那个<u>点积</u>是线性的。</b>原话是
    <em>“express the self-attention as a <b>linear dot-product of kernel
    feature maps</b>”</em>。<br>
    <!-- ⛔ 这里原来写的是「变成一个可以拆开的双线性形式」——&#160;
         **「双线性形式」是行话，两头都断着**。口头讲的时候我下意识换了说法，
         那个说法才是真解释。判据：讲技术要说人话。 -->
    ⭐ <b>说人话</b>：<em>softmax 是套在相似度<b>外面</b>的一层非线性的壳。
    把这层壳拿掉，剩下的相似度就是 ——&nbsp;<b>q 和 k 各自先过一道很轻的变换，
    然后直接点积。</b>没有壳了，也就<b>不用先凑齐一整行再算</b>。</em></li>
  <li><b>② 于是<u>复杂度</u>是线性的。</b>原话接着说：正因为如此，
    才能<em>“make use of the <b>associativity property of matrix products</b>”</em>，
    把 O(N²) 降到 <b>O(N)</b>。</li>
</ul>
<p>⛔ <b>这两件事不是并列的，是<u>因果</u>的。</b>
  <em>正因为相似度线性了，结合律才用得上；正因为结合律用得上，括号才挪得动；
  正因为括号挪得动，平方才掉成线性。<b>一个「线性」，一条因果链。</b></em></p>
<p>⭐ <em>那为什么不干脆叫它 RNN？——&nbsp;<b>名字记录的是它从哪来，不是它是什么。</b>
  它是从 attention 里<b>删</b>出来的，不是从 RNN 里<b>长</b>出来的；
  而且 2020 年的 RNN 刚被 Transformer 打败三年，那时候是个不利的名字 ——&nbsp;
  <b>给新方法起名叫 RNN，等于自己把自己埋了。</b></em></p>
<p>⭐ <em>还有一层是<b>写给谁看</b>：作者要说服的是<b>用 Transformer 的那群人</b>，
  就得在<b>他们的坐标系里</b>命名。<b>「这是一种注意力」他们会读下去；
  「这是一种 RNN」他们会划走。</b></em></p>
<p>⭐⭐ <em>这条判据后面还要再用一次：同一块板子，从信号处理那边推出来的叫
  <b>SSM / Mamba</b>，从 attention 这边删出来的叫 <b>线性注意力</b>，
  从 1990 年代 fast weight 那支接下来的叫 <b>delta rule</b>。
  <b>三个名字，一样东西 ——&nbsp;听到新名字先问它的血统，别问它的外形。</b></em></p>
<!-- ⭐ 2026-09-15 现场问完「RNN 的 R 在哪」之后冒出来的：两个名字放在一起
     正好构成一组对照，而这组对照本身就解释了「为什么这俩看着毫不相干」。 -->
<p>⭐⭐ <b>把这个名字和<a href="#s一">第一章</a>那个名字摆在一起，还能多读出一层。</b></p>
<p><em><b>RNN 的「R」，说的是它<u>长什么样</u></b> ——&nbsp;有一条绕回自己的边，
  是个形态描述。<br>
  <b>线性注意力的「线性」，说的是它<u>怎么被算出来</u></b> ——&nbsp;
  点积是线性的、于是能重排，是个数学性质。</em></p>
<p>⛔ <em><b>一个按形态命名，一个按性质命名 ——&nbsp;所以这两个名字听上去八竿子
  打不着，而它们指的是同一样东西。</b>这不是巧合，是命名这件事本身的毛病：
  <b>名字只记录起名那一刻最要紧的那一面。</b></em></p></div>

<h3>7.3　板子会写满 ——&nbsp;而这一支三十年的改进，改的一直是同一个动作</h3>

<p>固定大小，就意味着<b>装不下全部历史</b>。<em>那它到底是怎么坏的？</em></p>

<!-- ⭐ 2026-09-15 试讲补：这一节原来是直接给答案（「只加不擦」）。
     ⛔ 可这里有一个**几乎人人都会先想到的错答案**，不先摆出来，
        「不会擦」那个反转就是平的。同 6.2 / 6.3 的形状，这是第三处。 -->
<div class="note warn"><p>⚠️ <b>先摆一个几乎人人都会先想到的答案 ——&nbsp;它是错的。</b></p>
<p><em>大多数人第一反应是：<b>旧的东西会慢慢淡掉</b>吧。像磁带反复覆盖，
  越早的声音越模糊。</em></p>
<p>⛔ <b>不是。最朴素的那个线性注意力，根本不是这么坏的</b> ——&nbsp;
  <em>它<b>只会加，不会擦</b>。旧笔迹一笔都没走，没有任何东西在变淡。</em><br>
  ⭐ <em>所以它不是「淡掉」，是 <b>叠糊了</b>：
  <b>白板还是那块白板，墨也还都在 ——&nbsp;可你已经一条都读不出来了。</b></em></p></div>
<p>把它想成一本<b>联想笔记</b>：每来一对「钥匙 → 内容」，就往板子上加一笔；
  <em>取的时候拿钥匙去比对。</em><br>
  ⛔ 只要两把钥匙长得有点像，取出来的东西就会串 ——&nbsp;
  <em>而一块 d 维的板子上，<b>真正互不打架的钥匙最多只有 d 把</b>。</em></p>

<div class="note danger"><p>⛔ <b>但真正的病根还要再往下一层，而且它跟你的直觉相反。</b></p>
<p><em>最朴素的线性注意力<b>只会加，不会擦</b>。
  于是话越说越长，旧笔记一笔都没走，新笔记不停往上叠 ——&nbsp;
  取回的误差就是这样累起来的。</em></p>
<!-- ⛔ 图里已经引了 Eagleman 那句「记忆的敌人不是时间，是别的记忆」。
     ⭐ 正文不再抄一遍 ——&nbsp;正文负责的是**它跟第一章的关系**，那是图给不了的。 -->
<p>⭐⭐ <em>所以<a href="#s一">第一章</a>那个「传远了会淡」，在这里换了一张脸 ——&nbsp;
  <b>它不是被时间冲淡的，是被后来的记忆挤掉的。</b></em></p>
<p>⭐ <em>（下面那张图里引了一句神经科学家的话，把这件事说得比任何公式都准。
  那句话值得一字一句读。）</em></p></div>

<!-- ⭐⭐ 2026-09-15 课程作者现场补的直觉。它补的正好是这一节的软肋：
     前面说清了「是挤掉不是淡掉」，但**没说清「为什么装的东西越多越挤」**。
     ⛔ 边界写死：借的是**形状**，不是它在认知科学里的真值。 -->
<div class="note ok"><p>⭐⭐ <b>这件事有一个日常版本，而且它比白板那个比喻更准。</b></p>
<p><em>成年人常觉得自己记性不如小孩。<b>可未必是「记性」变差了 ——&nbsp;
  更可能是脑子里原有的东西太多了。</b><br>
  小孩脑子里<b>干扰项少</b>，一件新事进来，周围是空的，很容易跟别的区分开、单独记住。<br>
  成年人不一样：<b>得先找一块空地，或者找一个合理的位置把它插进去</b>，
  让它跟已有的知识互不冲突 ——&nbsp;<b>而这件事本身就很难。</b></em></p>
<p>⛔ <b>看出来了吗？这正是上面那个「d 维最多 d 个正交方向」在说的事。</b>
  <em>板子空的时候，新记录随便往哪儿放都不打架；板子满了，
  新记录<b>只能挤在别人旁边</b>，挤到谁就把谁弄脏。
  <b>「找一块空地」＝ 找一个还没被占用的正交方向。</b></em></p>
<p>⭐⭐⭐ <b>而这个直觉还会直接把下面 delta rule 那一招解释掉。</b>
  <em>「找个合理的位置插进去、不跟原有知识冲突」——&nbsp;
  翻成板子上的动作，就是：<b>先拿这把钥匙看看那儿原来写着什么，
  把旧的那一笔清掉，再写新的。</b><br>
  <b>成年人学东西要先「整理」一下才记得住，delta rule 干的就是这件事。</b></em></p>
<p>⚠️ <b>一句边界</b>：<em>我们借的是这个直觉的<b>形状</b>（干扰，而不是衰减），
  不是在断言人脑就是这么工作的 ——&nbsp;那是另一个领域的问题。
  <b>这一节真正扛事的仍然是那句可以验算的：d 维里最多 d 个互不打架的方向。</b></em></p></div>

__FIG_ERASE__

<!-- ⭐⭐⭐ 2026-09-15 现场追问：「残差网络的加为什么就能把信息融进去、不糊？」
     ⛔ 这个对照本来不在课里，但它**顺手就把 delta rule 解释掉了** ——&#160;
       所以位置必须在 delta rule **之前**，不能放在后面当补充。
     ⭐ ResNet 摘要的原话就是答案："learning residual functions
       **with reference to the layer inputs**"（arXiv 1512.03385）。 -->
<div class="note info"><p>📌 <b>一个很值得比的对照：<u>残差网络也是「加」，它为什么不糊？</u></b></p>
<p><em>残差块做的是 <b>输出 ＝ 输入 ＋ F(输入)</b>，也是往同一个地方加。
  可它叠一百多层都不糊，凭什么？</em></p>
<p>⛔ <b>区别只有一条，而且是决定性的：<u>它在加之前，先读了一遍。</u></b></p>
<ul>
  <li><b>最朴素的线性注意力是「盲加」</b>：<em>第 t 步往上加的那张卡片，
    只由<b>当前这个字</b>算出来 ——&nbsp;
    <b>它压根没看板子上已经写着什么。</b>写重了也不知道。</em></li>
  <li><b>残差块是「先读再加」</b>：<em>F 的输入<b>就是</b>当前这份表示。
    它<b>读得到</b>已经有什么，所以它写的是<b>「还差什么」</b>，
    不是「我以为该有什么」。</em></li>
</ul>
<p>⭐ <em>ResNet 原文（He 等，<b>arXiv 1512.03385</b>，2015）的用词就很准：
  <b>“learning residual functions <u>with reference to the layer inputs</u>”</b>
  ——&nbsp;<b>「有参照地」学</b>。这个 with reference to 就是不糊的全部原因。</em></p>
<p>⭐⭐⭐ <b>而下面 delta rule 干的，正好就是把这件事装到线性注意力身上。</b>
  <em>——&nbsp;<b>先拿钥匙去把板子上那儿的旧内容取出来看看，
  再按差去改。</b>换句话说：<b>delta rule ＝ 给「加」装上残差那种「先读再写」。</b></em></p>
<p><span class="sub">⚠️ 另外两条次要的、但也真在起作用：
  残差网络的层数是<b>固定的几十到一百多层</b>，不是几万步累加；
  层与层之间还有 LayerNorm 把尺度重新归一化。
  <em>（原论文的主要动机其实是<b>好优化</b>：加了恒等通路之后，
  梯度有一条不衰减的高速路，深层才训得动。）</em></span></p></div>

<div class="note ok"><p>⭐ <b>于是这一支所有新名字，其实都只在回答同一个问题：「怎么擦」。</b></p>
<p>⭐⭐ <em><b>这一句可以当工具用，不用背谱系</b> ——&nbsp;
  以后每碰到一个新名字，只问它一句：<b>「你是怎么擦的？」</b>
  擦多少、擦哪些、要不要每一步都擦、能不能让模型自己学着决定擦什么 ——&nbsp;
  <b>答案落在这四格里的某一格。</b></em></p>
<p><em>最漂亮的那一招叫 <b>delta rule</b>，它的直觉一句话就够：</em><br>
  <b>教小孩射箭，偏左就往右调一点，偏多少就调多少。</b></p>
<p>⭐⭐ <em>放到板子上：来了一对新的「钥匙 → 内容」，
  先拿这把钥匙去把板子上的旧内容取出来看看，
  再按「想要的」和「取到的」之间那点差去改那一笔。<br>
  ——&nbsp;<b>不是盲目覆盖，是有针对地修。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-16 现场追问：「那个『转一下』的矩阵，怎么看上去跟 DeltaNet
     的遗忘门差不多呢？长记忆的留着、短记忆的淡忘得快一点 ——&#160;
     这东西是不是一开始的 RNN 就该有的？」
     ⭐ 直觉方向完全对，而且正是这一章那条线的来处 ——&#160;**但认错了对象**：
       DeltaNet 不是遗忘门那一支。这个区分值得单独写清楚。
     ⛔ 判据（本讲第三次）：**听起来像同一类的东西，未必是同一类。**
       「都在让旧的变弱」这个表层相似，掩盖了「全体打折」和「定点改写」的区别。 -->
<div class="note info"><p>📌 <b>顺着这里把一条三十年的线串起来 ——&nbsp;
  顺便纠一个很容易混的说法。</b>
  <em>（现场原话：「那个『转一下』的矩阵，怎么看上去跟 DeltaNet 的遗忘门差不多？」）</em></p>

<p>⭐ <b>先说直觉对的那一半：<a href="#s一">第一章</a>那块「转一下」，
  确实就是遗忘门的祖先。</b>
  <em>它按方向打折 ——&nbsp;倍率小的方向快忘，接近 1 的方向能传远。</em></p>
<p>⛔ <em>但它是<b>静态</b>的：倍率烧死在权重里，
  <b>第 1 步和第 1 万步一样，读到「的」和读到一个关键人名也一样。</b></em></p>

<p>⭐⭐ <b>而「这东西是不是一开始就该有」——&nbsp;是的，而且很快补上了，
  但比多数人以为的<u>晚</u>，还是单独一篇论文。</b></p>
<ul>
  <li><b>1997 · LSTM 原版</b>：<em>只有<b>输入门和输出门</b>，
    <b>没有遗忘门</b>。</em></li>
  <li><b>1999 · 遗忘门</b>：<em>Gers、Schmidhuber、Cummins 加进去的，
    论文标题就叫 <b>《Learning to Forget》</b>。</em></li>
  <li><em>⭐ <b>「学会遗忘」值得单独写一篇 ——&nbsp;说明当时人们并不觉得它是显然的。</b>
    而它解决的问题很具体：那条 cell state 只会一直往上加，
    <b>跑连续不断的流式任务时会饱和、会爆</b>。<b>不是想更聪明，是不加它跑不下去。</b></em></li>
  <li><b>2023 之后 · GLA / Mamba 这一支</b>：<em>把衰减做成<b>跟当前输入有关</b>的，
    但为了能并行，<b>退回成对角</b>（每个通道一个数）。</em></li>
</ul>

<p>⛔⛔ <b>而 DeltaNet <u>不在</u>这条线上 ——&nbsp;这是要纠的那一点。</b></p>
<ul>
  <li><b>遗忘门</b>（GLA / Mamba）——&nbsp;<em><b>全局按通道打折</b>，
    所有东西一起淡一点。</em></li>
  <li><b>delta rule</b>（DeltaNet）——&nbsp;<em>就是上面那一招：
    <b>对准当前这一把钥匙的方向做定点改写，别的方向一根毫毛都不碰。</b>
    混合比例由一个动态系数控制 ——&nbsp;取 0 完全不变，取 1 完全替换。</em></li>
</ul>
<p>⭐ <em><b>一个是「全体淡一点」，一个是「只改这一条记录」。</b>
  ——&nbsp;而 <b>Gated DeltaNet 的名字里就写着：两样都要。</b></em></p>

<p class="landing">⭐⭐⭐ <b>最后是那个反转：1990 年那块「转一下」是<u>全矩阵</u>，
  比今天的对角遗忘门<u>更一般</u>。</b>
  <em>它输的从来不是表达力，<b>是它不随数据变。</b><br>
  ——&nbsp;<b>一般但静态　→　对角但动态　→　对角动态 ＋ 定点改写。</b>
  这就是那三十年。</em></p>
<p><span class="sub">📌 出处：Gers, Schmidhuber &amp; Cummins《Learning to Forget:
  Continual Prediction with LSTM》，1999 年提出，期刊版
  <b>Neural Computation 12(10):2451–2471, 2000</b>。
  DeltaNet 的更新式与「先取回旧值、再写新旧混合值」的解读，
  见本节末尾引的<b>杨松琳《DeltaNet Explained》</b>。
  <em>⚠️ 「Gated DeltaNet ＝ 两者都要」这句，依据是它的名字与构造本身，
  <b>本课没有去核它的论文</b>。</em></span></p></div>

<h3>7.4　可状态一回来，串行也跟着回来了</h3>

<p>这就尴尬了。<em><a href="#s一">第一章</a>那个盒子的三个痛里，第一条就是
  <b>只能一步一步来</b> ——&nbsp;现在盒子请回来了，那个痛是不是也回来了？</em></p>
<p><b>回来了。</b><em>这一支能站得住，全靠把它又解决了一次。</em></p>

__FIG_CHUNKWISE__

<!-- ⭐⭐⭐ 2026-09-16 现场：「chunk 化到底具体是怎么做的？GDN、KDA 怎么在硬件上
     加速并行？」⛔ 原来 7.4 只有比喻（排队、交接单），机制一个字没有。
     ⭐ 机制全部画进 fig3-chunkwise 的 ②b / ②c 两格了；
       正文只留图给不了的那一样：**这件事跟第一章那条硬件账是同一笔。** -->
<div class="note danger"><p>⛔⛔ <b>这一节最容易被跳过、却最该记住的一句：
  <u>「线性注意力更快」是有前提的。</u></b></p>
<p><em>上面图里 <b>②b</b> 讲的是块内怎么拆（两截，全是矩阵乘），
  <b>②c</b> 讲的是 delta rule 为什么还多一步。
  <b>那多出来的一步，不是实现细节 ——&nbsp;它决定了这条路能不能落地。</b></em></p>
<p>⭐⭐ <em>所以读这一节的时候，<b>别把「线性注意力更快」当成一条模型属性</b>。
  它<b>一半是模型的功劳，一半是 kernel 的功劳</b> ——&nbsp;
  <b>而后一半，是可以缺席的。</b></em></p>
<p>⭐ <em>——&nbsp;这也是<a href="#s九">最后一章</a>那条暗线的又一次应验：
  <b>纸面上省下的东西，要有人把它落到机器上，才算数。</b></em></p></div>

<h3>7.5　本章落点：兜了一整圈，我们对「状态」的看法变了</h3>

<!-- ⛔⛔ 2026-09-15 画完 fig3-gu 之后**立刻重写了这一节的正文**。
     原来三段引文是逐字写在正文里的；图一画出来，查重当场响了 **5 处**。
     ⭐ 判据⑩：两处说同一件事，**挨着图的那一处赢**。
     ⛔ 但不能简单删 ——&#160;正文要改成承担**图给不了的三样**：
        ① 说话的人是谁（图里只在折叠的出处里）
        ② 第一段引文里图没画的那半句（「每吐一个字就演化一次」）
        ③ 门牌（Mamba）＋ 本章落点
     判据：**查重响了不等于该删字，多半是该换一批字。** -->
<p class="landing">📌 <b>兜完一圈再看：KV cache 不是「状态」的对立面，
  它本身就是状态的一种</b> ——&nbsp;<em>一种不压缩、按长度线性增长的状态。</em></p>

__FIG_GU__

<div class="note ok"><p>⭐⭐⭐ <b>先交代说话的人：这几句出自
  <a href="https://goombalab.github.io/blog/2025/tradeoffs" target="_blank" rel="noopener">Albert
  Gu</a> ——&nbsp;Mamba 的作者之一，卡内基梅隆大学。</b></p>
<p>⭐ <em>他那个框架的起点，是图里 Ⓐ <b>没画出来</b>的半句：每一个自回归模型
  <b>在内存里都持有一个状态，每吐一个字就演化一次</b>。</em></p>
<p>⛔ <em>请注意这句话的<b>覆盖面</b> ——&nbsp;它说的不是「有一类模型是状态模型」，
  是<b>全都是</b>。于是前面七章那些看着毫不相干的名字，
  <b>第一次被放进了同一个框子</b>，剩下的分歧只有一个：
  <b>那个状态，长什么样。</b></em></p></div>

<div class="note info"><p>📌 <b>顺带认个门牌：这一支在外面最响的名字叫
  <u>Mamba</u></b>（2023）。</p>
<p><em>它走的是<b>同一块板子、同一套「擦了再写」</b> ——&nbsp;
  只是从<b>信号处理</b>那边推出来的，术语完全不一样，所以听上去像另一门功夫。</em></p>
<p>⭐ <em>——&nbsp;这正是本章 <b>7.2</b> 那条判据的又一次应验：
  <b>名字记录的是血统，不是形态。</b></em></p></div>

<div class="note ok"><p><b>这一章的两句话。</b></p>
<p><b>换来的</b>：<em>KV cache 从形状里彻底消失了 ——&nbsp;
  <b>每步要搬的东西，终于又跟话有多长无关了。</b>
  那正是<a href="#s一">第一章</a>①那条被扔掉的好处，绕了五章，捡回来了。</em><br>
  <b>欠下的</b>：<em>板子就那么大，<b>精确检索是它的软肋</b> ——&nbsp;
  而且这是压缩的直接后果，不是实现不好。</em></p>
<p>⭐⭐ <em>于是局面变成了：<b>三条路，三块啃不动的骨头。</b>
  而全行业给出的答案，<b>出奇地一致。</b></em></p></div>

</div></section>


<section id="s八"><div class="wrap"><div class="stn"><span class="badge">第 八 节</span><h2>谁也赢不了，那就都要 ——&nbsp;混合配比</h2></div>

<p class="lead">三条路都走完了，局面是这样的：<b>每一条都有一块自己啃不动的骨头。</b>
  <em>于是今天几乎所有人的答案都一样 ——&nbsp;<b>不选了，掺着用。</b></em><br>
  ⭐ <em>几层便宜的，配一层贵的。<b>可这件事远比「省钱」有意思。</b></em></p>

<h3>8.1　先证明一件事：混合不是「拿便宜的换点钱」</h3>

<p>如果掺便宜的层只是为了省，那道理很简单：<b>全用贵的，效果应该最好。</b>
  <em>省不省是另一回事，但「最好」总归该归全贵的那一头。</em></p>
<p>⛔ <b>可实测不是这样。</b></p>

__FIG_HYBRID__

<div class="note ok"><p>⭐⭐ <b>这件事有一手的、来自 Mamba 作者本人的说法。</b></p>
<p><em>他写道：把一个纯 Transformer 里的一部分、甚至大部分层换成状态类的层，
  <b>效率和效果会同时变好</b>。而那个最优配比大致在 <b>3:1 到 10:1</b> 之间 ——&nbsp;
  这一点<b>已经被几十个研究组各自独立地验证过了</b>。</em></p>
<p>⭐ <em>换句话说：<b>掺进来的那些便宜层，不是妥协，是它自己带来了好处。</b></em><br>
  <span class="sub">⚠️ 他这句话说的是<b>建模能力</b>（困惑度），没有把计算成本算进去 ——&nbsp;
  也就是说「省钱」是白捡的，不是理由。</span></p>
<!-- ⭐⭐⭐ 2026-09-15 核 Gu 原文时捡到的：**他自己在同一篇里把第七章和第八章
     接成了一条线**，而本课这两章原来是断开的。
     7.5 收尾那个反问（「我的记忆和我的研究笔记，哪个更好」）在这里拿到答案。
     ⛔ 不接上，第八章只是一章新内容；接上之后，它是**上一章那个问题的回答**。 -->
<p>⭐⭐ <b>而他紧接着那一句，正好把 <a href="#s七">7.5</a> 那个反问接住了。</b></p>
<p><em>原文的意思是：<b>就像人的智能，是靠随手的草稿纸和外部的参考资料撑起来的</b>
  ——&nbsp;语言模型也一样，把状态层和注意力层掺在一起就会变好。</em></p>
<p>⛔ <em>所以上一节结尾<b>那个反问</b>（在 fig3-gu 最下面那条带子里），
  <b>答案根本不是二选一</b> ——&nbsp;</em></p>
<p class="landing">⭐⭐⭐ <b>答案是：你两个都有。<br>
  一个大脑，外加一份可以随手翻的笔记 ——&nbsp;这就是混合架构。</b></p>
<p><span class="sub">⭐ 「几十个研究组」不是虚指，他原文点了名：
  <b>H3 / Jamba / Zamba / Samba</b>，以及后面跟上的一长串。
  <em>各家从不同起点出发，最后落在同一个区间 ——&nbsp;
  这比任何一家自称「我们这个配比最好」都有分量。</em></span></p></div>

<!-- ⛔⛔ 2026-09-14 R15：这段原来**不在 L200 里，只在 L300 里**。
     而 8.4 那张图明明白白引着它、还标着「本课自己写过」，然后把它推翻。
     ⭐ 于是 L200 的读者是这样的：先看见一句「本课写过 X」——&nbsp;他从没读过 X；
        再看见「X 被否掉了」——&nbsp;**推翻落了空**。
     ⭐⭐ 判据：**要推翻一个说法，得先让读者真的相信过它。**
        被推翻的那一句不是冗余，它是论证的前半截。 -->
<p>⭐ <em>那贵的层要配多少呢？<b>一个很自然的想法是：不用多。</b></em></p>
<p class="landing"><b>只要有几层能把全部历史查一遍，查到的东西就能传给后面所有层用。</b>
  <em>——&nbsp;所以全局层<b>不需要很多</b>。</em></p>
<p><span class="sub">📌 「传给后面所有层」靠的是<b>残差流</b>：
  每一层算完不是把上一层的结果替换掉，而是<b>加</b>在它上面往下传 ——&nbsp;
  像一条一路往下、谁都可以往里添东西的主干道。所以一层查到的东西，
  后面的层不用自己再查一遍，<b>顺着这条道就拿到了</b>。</span></p>
<p>⛔ <em>这个说法<b>八成是对的，但它有一个字说错了</b> ——&nbsp;
  错在哪，<b>本章后面</b>会用十四个模型的真实配置来判。</em></p>

<h3>8.2　那为什么不能全用便宜的？——&nbsp;这里有一条能证伪的规律</h3>

<p>一个很自然的反问：既然便宜的层这么好，<b>干脆全用它不行吗？</b>
  <em><a href="#s七">上一章</a>给的答案是「板子会写满」——&nbsp;
  但那还只是一句直觉。</em></p>
<p>⭐ <b>这件事其实有一条能算的规律。</b></p>

__FIG_INFO_LAW__

<!-- ⛔ 这一块原来把图里那两段落点带（供给 vs 需求、改常数 vs 改阶）又说了一遍。
     ⭐ 只留图给不了的：**它改掉了第三章那句话的一半。** -->
<div class="note ok"><p>⭐⭐ <b>顺带说一句：这一张把<a href="#s三">第三章</a>那句话改掉了一半。</b></p>
<p><em>第三章讲「信息账」的时候说过「远处可以少看」。<b>那句话对，但不完整</b> ——&nbsp;
  ⛔ <b>它不等于「远处不重要」。</b></em></p>
<p>⭐ <em>一本书前半和后半之间的那点关联，<b>是随着书变厚一直在涨的</b>，
  只是涨得慢。<b>慢，不是零。</b></em></p></div>

<h3>8.3　那 3:1 是怎么定出来的 ——&nbsp;以及这张表<u>问不出</u>什么</h3>

__FIG_RATIO_GRID__

<div class="note danger"><p>⛔ <b>这张表里最该被注意的，是两件「跟直觉相反」的事。</b></p>
<p><b>① 纯全注意力那一头不是最差的。</b>
  <em>——&nbsp;<b>这张表一共五个配比，它排第四。</b>
  <b>五个里的第四不是中游，是倒数第二</b>；最差的反而是另一头那个极端。</em></p>
<p><span class="sub">⛔ <b>名次必须带分母。</b><em>「排第四」单独拿出来听着像中游 ——&nbsp;
  说了「五个里」才知道它其实垫在倒数第二。
  这跟「样本量不等于去重后的个数」是同一族毛病：<b>一个数字看着精确，
  就没人再问它是几分之几。</b></em></span></p>
<p><b>② 这张表答不了「为什么是 3 不是 4」。</b>
  <em>因为消融模型只有 16 层，能摆得匀的配比就那么几个 ——&nbsp;
  <b>中间那一段根本没被试过。</b></em></p>
<p>⭐ <em>——&nbsp;这也是读任何一张消融表时该问的一句：
  <b>它到底扫过了哪些点，没扫过哪些点？</b></em></p></div>

<h3>8.4　守恒的到底是比值，还是全注意力的层数</h3>

<p>这是一个看起来吹毛求疵、其实很要命的问题：
  <em>模型从<b>二十来层</b>长到<b>九十多层</b>，该保住的是那个「几比几」，
  <b>还是那几层的绝对个数</b>？</em></p>
<!-- ⛔⛔ 2026-09-15 现场核数时发现的读者陷阱：**这一章里有两个「十四」，
     而它们不是同一批模型。** 8.1 那张配比点阵是「十四个已公开配比的模型族」
     （含滑窗族，最高 7:1）；这一节是「十四个能拉到 layer_types 配置文件的模型」
     （含 Granite 40/4 ＝ 9:1、Bamba 32/3 ≈ 9.7:1，**都超过 7:1**）。
     ⭐ 两边不矛盾 ——&#160;超过 7:1 的那两家**根本不在前一张图里**。
     ⛔ 但交叉读的人会以为两张图在打架，所以两张图的口径都加了限定词，
       这里再明说一次。
     判据：**同一章里出现两个同样大小的样本数，必须说清是不是同一批。** -->
<p><span class="sub">⚠️ <b>这里的「十四个」跟 8.1 那张配比点阵的「十四个」<u>不是同一批</u>。</b>
  <em>那边数的是<b>已公开配比的模型族</b>（含滑窗族，最高 7:1）；
  这边数的是<b>能拉到 <code>layer_types</code> 配置文件的模型</b> ——&nbsp;
  里面有 Granite（40 层 4 条 ＝ 9:1）和 Bamba（32 层 3 条 ≈ 9.7:1），
  <b>它们超过 7:1，但它们不在那张点阵里</b>，两边并不打架。</em></span></p>
<p>⭐ <b>这个问题不适合讲道理，适合去数。</b></p>

__FIG_RATIO_OR_COUNT__

<h3>8.5　混合之后，还白捡了一样东西</h3>

<p>还记得<a href="#s五">第五章</a>那条很别扭的 64 维窄轨吗？
  <em>它之所以非有不可，是因为<b>模型里没有任何一层天生知道先后</b> ——&nbsp;
  于是人人都得自己带着位置信息。</em></p>
<p>⭐ <b>那要是有一类层，天生就知道先后呢？</b></p>

__FIG_NOPE__

<div class="note ok"><p>⭐⭐ <b>状态类的层是「一步一步」走的，先后顺序本来就写在它的走法里。</b></p>
<p><em>于是可以做一件很干净的事：<b>把位置信息的全部责任，交给那些便宜的层</b>，
  <em>剩下的全注意力层就不用带位置编码了。</em></em></p>
<p>⭐ <em>——&nbsp;<a href="#s五">第五章</a>那条被逼出来的窄轨，在混合架构里可以不要了。
  <b>一个在单一架构里绕不过去的麻烦，换个结构之后自己消失了。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-15 现场提问补：「线性层不需要 RoPE 我懂了，
     可混合里那层 MLA，总还是需要 RoPE 的吧？」
     ⛔ 这个疑问**几乎人人都会冒出来**，而上面那段只轻描淡写说了一句
     「剩下的全注意力层就不用带位置编码了」——&#160;读者读完照样会问。
     ⭐ 判据：**一句结论如果会引出一个明显的反问，那这个反问就是正文的一部分。**
       不替读者问出来，他就只能自己带着疑问往下读。
     出处：Kimi Linear 技术报告 arXiv 2510.26692，正文有一整段小标题就叫
     "No Position Encoding (NoPE) for MLA Layers"。 -->
<div class="note danger"><p>⛔⛔ <b>这里一定会冒出一个反问：
  <u>那层全注意力（MLA）总还是得知道位置吧？它不留 RoPE 怎么行？</u></b></p>
<p>⭐ <b>不留。而且这不是我们的推论，是论文里一整段的小标题。</b>
  <em>Kimi Linear 技术报告（arXiv <b>2510.26692</b>）原话：
  <b>“we apply NoPE to <u>all</u> full attention (MLA) layers”</b> ——&nbsp;
  <b>所有</b> MLA 层，一个不留。</em></p>
<p>⛔ <b>那它怎么知道位置？——&nbsp;位置没有消失，是<u>送达方式</u>换了。</b></p>
<ul>
  <li><b>原来：每一层自己盖戳。</b><em>RoPE 是在<b>这一层自己的 key 上</b>
    按位置转一下 ——&nbsp;层层都得来一遍，所以层层都被那个旋转挡着。</em></li>
  <li><b>现在：下面几层已经揉好了，顺着<u>残差流</u>送上来。</b>
    <em>MLA 层读到的那个向量<b>本身就带着位置</b>，
    它不需要再在自己的 key 上做任何事。</em></li>
</ul>
<p>⭐⭐ <b>那「多远」这件事，具体是谁编码的？——&nbsp;<u>衰减</u>。</b>
  <em>论文说 KDA 接过去的是 <b>“positional information <u>and recency bias</u>”</b>，
  并把它定性为 <b>“the primary position-aware operator”</b>。<br>
  <b>那块板子每走一步都要先乘一个衰减因子</b> ——&nbsp;
  所以一样东西<b>被乘过几次衰减，就说明它离现在有多远</b>。
  <b>衰减本身就是那把尺子。</b></em></p>
<p>⭐ <em>——&nbsp;回头看本讲反复用的那条分工：<b>「顺序」归因果掩码，
  「距离」原来归 RoPE</b>。<b>在混合架构里，「距离」这一项从 RoPE 手里
  交给了衰减</b>，于是 RoPE 整个没有了存在的理由。</em></p>
<p><span class="sub">⚠️ <b>别推广成「混合就一定 NoPE」</b>：<em>这是 Kimi Linear
  的设计选择，论文说它跟另外几家的结果一致，但<b>不是所有混合模型都这么做</b>。
  ⛔ 前提也很硬 ——&nbsp;<b>得有一类天然带时序的层，而且要摆在全注意力层下面</b>，
  位置才来得及被揉进去。</em></span></p></div>

<h3>8.6　对号入座：现在回第零节那张表，你能一行一行读下来了</h3>

<!-- ⭐⭐⭐ 2026-09-16 现场纠正：之前说「把注意力编年史挪到第零节」，
     指的是**这张按模型排的大表**，不是那张横向四支线时间线。表已挪到第零节。
     ⛔ 这里**不要再放一遍表** ——&#160;判据跟 fig3-chronicle 那次一样：
       **同一样东西放在开头和结尾，价值不一样**。开头它是「领土」，
       结尾它是「你现在读得懂了」。所以东西只放一次，第二次用一句话把人送回去。 -->
<p class="landing">📌 <b>回到<a href="#s零">第零节</a>那张表。</b>
  <em>——&nbsp;<b>开篇它是一墙陌生的型号；现在每一列你都认得：
  一个循环里几层便宜的、上下文多长、KV cache 多大、以及它凭什么在这条线上。</b></em></p>
<p><span class="sub">⭐ <em>那张表默认开在 <b>Highlight</b> 模式（撑起这段历史的那些行）；
  <b>切到「全部」</b>就是完整的参照表 ——&nbsp;每一行都标了出处，
  多数可以在公开配置里当场核。<b>表头可以点，按时间／厂商／配比／上下文／KV 排序。</b></em></span></p>

<div class="note ok"><p><b>这一章的两句话。</b></p>
<p><b>换来的</b>：<em>同时拿到两种记忆 ——&nbsp;一个能精确翻查，一个能长久兜住。
  <b>而且实测上，它比任何一头单用都更好。</b></em><br>
  <b>欠下的</b>：<em>多了一个要调的东西（配比），
  而它为什么是这个数，<b>目前没有一篇公开消融真正回答过</b>。</em></p>
<p>⭐⭐ <em>到这里，这本书的「想法」部分讲完了。
  <a href="#s九">最后一章</a>要做的事只有一件 ——&nbsp;
  <b>把这些聪明办法真的搬到机器上，看看哪些能兑现，哪些兑现不了。</b></em></p></div>

</div></section>


<section id="s九"><div class="wrap"><div class="stn"><span class="badge">第 九 节</span><h2>落到机器上 ——&nbsp;以及那 512 倍是怎么换来的</h2></div>

<p class="lead">八章的想法讲完了。<b>最后一件事：它们真搬到机器上，兑现了多少？</b><br>
  ⭐ <em>这一章给三样东西：一张换算成毫秒的账、一场还没打完的仗，
  以及开场那把枪的答案。</em></p>

<h3>9.1　先把「省了 N 倍」换算成毫秒</h3>

<p>前面每一章都在说「省了多少倍」。
  <em>可倍数是个相对量，它不告诉你那一步到底要多久。</em></p>

__FIG_PERSTEP__

<h3>9.2　这些招落到 TPU 上，为什么格外难</h3>

<p>要命的是：<b>这些聪明办法绝大多数是在 GPU 上长出来的。</b>
  <em>而一台机器擅长什么，是写在它的出厂设计里的 ——&nbsp;换一台，前提就换了。</em></p>

__FIG_TPU_GAP__

<div class="note danger"><p>⛔⛔ <b>有人真的动手试过</b>，而他撞到的东西值得一字一句地看。</p>
<p><em>2025 年有人把 NSA 移植到 TPU 上，写了一份工作日志。他的结论是一句话：
  <b>动态稀疏在这套栈的每一层都难</b> ——</em></p>
<p>· <em>编译器不喜欢运行时才知道的变量和分支，
  <b>而「挑哪几块」恰恰是运行时才知道的</b>；</em><br>
  · <em>写 kernel 的框架强制按顺序一格一格走，而 top-K 挑出来的块<b>是跳着的</b>
  ——&nbsp;像 [7, 6, 1, 2] 这种顺序，它直接不让你走；</em><br>
  <!-- ⛔ 2026-09-15：原来写「跑在芯片里**最不擅长这件事的那个部件**上」——&nbsp;
       指代含糊，而**最自然的误读是错的**：读者脑子里浮现的是那块只会做矩阵乘的
       大方阵，实际上原文说的是**向量单元**。
       ⭐ 而且「最不擅长」这个形容也不对 ——&nbsp;向量单元恰恰是这块芯片上
       **最灵活**的算术部件；它不是「最差的那个」，是「**不该干这活的那个**」。
       真正该干这活的，是芯片上那套专门搬数据的硬件。 -->
  · <em>而那个「把散落的块取回来」的活，<b>落到了一个专管算数的部件头上</b>
    ——&nbsp;<b>而不是那套专门搬东西的硬件</b>。</em><br>
  　<em>它干活的方式是「同一个动作，对一排几百个数一起做」；
    你让它一个一个去不同地方捡，<b>一排几百个位子只用得上一个，其余全在干等</b>。</em></p>
<p>⭐ <em>光是把索引改写成编译器友好的形式，他就快了 <b>286 倍</b>
  ——&nbsp;<b>算法一个字没改。</b></em></p></div>

<div class="note danger"><p>⛔⛔⛔ 但真正值得记住的是下面这一条 ——&nbsp;
  <b>它是这本书那条暗线的最后一击。</b></p>
<p><em>为了对齐芯片里那块矩阵乘单元的形状，
  <b>他不得不把 NSA 论文里的块大小从 32 改成 64</b>。</em></p>
<p>⭐ <em>而他自己在日志里写了一句：<b>这可能会完全改变训练动态</b> ——&nbsp;
  块大了，挑中的那一块里会夹进更多无关的东西，
  模型在「大海捞针」这类细活上可能会变差。</em></p>
<!-- ⛔ 2026-09-14 R15：这个总结原来只点了**三站**，而书里实际走了**五站** ——
     而且漏掉的恰恰是写得最好的两站（§4.3 从机箱里数出 8、§5.5 硬件否决数学）。
     ⭐ 外部评审的原话：「作者自己给自己的暗线少算了 40%。」
     ⭐⭐ 判据：**一条线的收尾要把每一站都点名**，不能只点你写的时候还记得的那几站。
        少点一站，那一章读者当时的「啊」就白攒了。 -->
<p>⛔⛔ <em>读懂这一条，这本书那条暗线就走完了全程 ——&nbsp;
  <b>注意它管的事，一路在往「模型聪不聪明」靠：</b><br>
  <a href="#s二">第二章</a>，硬件决定了用哪个<b>公式</b>（点积赢加性，因为它能变成矩阵乘）；<br>
  <a href="#s四">第四章</a>，硬件决定了一个<b>超参</b>（g=8，因为一台机器装 8 张卡）；<br>
  <a href="#s五">第五章</a>，硬件<b>否决</b>了一个漂亮的数学（压缩件不按头分，于是多卡切不开）；<br>
  <a href="#s六">第六章</a>，硬件决定了<b>怎么挑</b>（散着取不行，只能整块取）；<br>
  到这里，<b>硬件直接改了训练动态 ——&nbsp;而它改的那个数，会影响模型聪不聪明。</b></em></p></div>

<!-- ⭐⭐⭐ 2026-09-14 R15：还第一章那笔债。
     ⛔ §1.2 写着「记住这个念头 ——&nbsp;它后面还要再赢一次……那个东西叫
        FlashAttention，第九章会讲到」，**还挂了一条指向第九章的超链接**。
        而第九章原来 FlashAttention 出现 **0 次** ——&nbsp;
        全书唯一一处**带链接的失信**：读者真的会点过来找。
     ⭐ 顺手把第六章欠的那笔（「挑中的那些散落在各处，搬起来很碎，
        这一笔到第九章会变成真正的麻烦」）一起认领了 ——&nbsp;
        它跟这一条本来就是同一件事的两面。 -->
<div class="note ok"><p>⭐⭐ <b>到这里，<a href="#s一">第一章</a>那个念头终于赢了第二次。</b></p>
<p><em>当时说的是 persistent RNN：<b>别让权重反复落回显存。</b>
  七年后同一句话被用在另一个算子上 ——&nbsp;<b>别让中间结果落回显存</b>，
  那就是 <b>FlashAttention</b>。<em>它今天是所有注意力实现的默认底座。</em></em></p>
<!-- ⛔⛔ 2026-09-15：这一段我第一版**写错了因果**，而且错得很像真的。
     原话是「它快，是因为它按整块连着搬 ——&nbsp;而稀疏挑出来的块是散的」
     「它让那个最快的底座失效了」。⛔ 两处都不成立：
     ① FlashAttention 快的主因就是上一句已经说对的那个（大表不落回显存）；
        「按块搬 K/V」是它能这么干的**手段**，不是它快的**原因**。
     ② **稀疏和它根本不冲突** ——&nbsp;FlashAttention 原论文自己就带一个
        block-sparse 版本（摘要原话 "We also extend FlashAttention to
        block-sparse attention"），而 GPU 上的稀疏注意力几乎全是拿它改的
        （Block-Sparse-Attention、FlexAttention 生成的 Triton kernel、
        NSA 的 Triton 实现、cuDNN 的 NSA kernel）。
     ⭐⭐ 我把「**这台机器**搬东西的方式跟不上」写成了「**这个算法**天生接不住」——
        顺手把第六章的主角 DSA（它就是散着挑单个 token 的，而且上线跑生产）踩掉了。
     ⭐ 判据（本课第一原则的又一次）：**越是顺口的因果越要核。**
        「A 快是因为 B」这种句子，B 常常只是 A 的手段或伴随现象。 -->
<p>⛔ <em>可这一章讲的这些麻烦，恰恰是它<b>在这台机器上</b>接不住的那一类。</em></p>
<p><em>它<b>不挑</b>你每步去读哪几块历史 ——&nbsp;连着读能用，跳着读也能用；
  <b>GPU 上那些「只读挑中的几块」的做法，几乎全是拿它改出来的。</b></em></p>
<p>⭐⭐ <em><a href="#s六">第六章</a>末尾欠的那一笔，在这里到期了。
  当时那句话是「挑中的那些散落在各处，搬起来很碎」——&nbsp;
  <b>「很碎」翻译成机器听得懂的话是两件事</b>：<b>一趟大搬运被拆成很多趟小的</b>，
  每趟都要付一次起步钱；而且<b>要等挑完才知道去哪儿拿</b> ——&nbsp;
  <em>推车没法像平时那样提前一两趟就把下一车备好。</em></em></p></div>

<h3>9.3　那怎么克服</h3>

__FIG_TPU_FIX__

<h3>9.4　全讲落点：这是一部反复搬家的历史</h3>

<p>把九章摆在一起，会看见一个出乎意料干净的形状。</p>

__FIG_LANDING__

<h3>9.5　把开场那把枪打响</h3>

<p>封面上挂着一把枪：<b>六年，五百一十二倍。</b>
  <em>现在这笔账算得完了。</em></p>
<p class="landing">📌 <em>这五百多倍里，<b>真正靠「买更好的硬件」换来的占几成？</b></em></p>

__FIG_GUN__

<h3>9.6　收尾：一个完整的圆</h3>

<!-- ⭐⭐ 2026-09-14 R14 外部评审在这里报了四条，全部证实，一起修：
     ⛔ ① 链接指的是 #s零（章标题），不是那张图 ——&nbsp;跳过去图还在四段之下。
     ⛔ ② 「图里那句话」在 1.1 MB 之外，读者滚不回来就读不懂这一段的主语。
     ⛔ ③ 「变的是你」只说了「四条支线你都认得」，**没有逐条点名** ——&nbsp;
        空口说「你变了」是要读者自己举证；点名才是把证据摆给他。
     ⛔ ④ 第九章全章 RNN 0 次、盒子 0 次，而这一节标题叫「一个完整的圆」——
        那个圆在第七章就画完了，这里宣布闭合，闭的其实是另一个东西。 -->
<p class="landing">⭐⭐ <b>现在，请滚回<a href="#fig-chronicle">开篇那张编年史</a>，再看它一眼。</b></p>

<p><em>两小时前它是一团噪音。现在从上往下数 ——&nbsp;</em><br>
  <em>最上面那条灰的，是<b>那个盒子</b>，和 2014 年挂在它身上的那个配件；</em><br>
  <em>蓝色那条①，是<a href="#s四">第四</a>、<a href="#s五">五章</a>：<b>让每一份更小</b>；</em><br>
  <em>橙色那条②，是<a href="#s六">第六章</a>：<b>每步只读一部分</b>；</em><br>
  <em>紫色那条③，是<a href="#s七">第七章</a>：<b>换回一块固定大小的板子</b>；</em><br>
  <em>最底下那三行公司，是<a href="#s八">第八章</a>：<b>同一批旋钮，各拧各的</b>。</em></p>
<p><em><b>那张图一个像素都没变。变的是你。</b></em></p>

<div class="note ok"><p>⭐⭐⭐ <b>图顶上那句话，现在可以原样读一遍了：</b></p>
<blockquote><p>「Transformer 当初做的交易是<b>用平方的计算量，买来完全的并行度</b>
  ——&nbsp;而这段历史，是在把那笔交易<b>往回赎</b>，但不能把并行度还回去。」</p></blockquote>
<p><em><b>而每一章都是它的一个注脚：</b></em></p>
<p><em>砍头、压成压缩件、只读一部分 ——&nbsp;赎回的是<b>那个平方项前面的系数</b>；<br>
  线性注意力要赎的是<b>那个平方本身</b>，代价是把串行请了回来；<br>
  于是分块并行又出现了 ——&nbsp;<b>为了把并行度再找回来。</b></em></p>
<p>⭐⭐ <em>所以这一讲从头到尾其实只讲了一件事：
  <b>三十年里，人们一直在同一个三角里挪动</b> ——&nbsp;
  记得住、算得快、付得起。<b>哪一角都不能真的放手。</b></em></p></div>

<!-- ⭐⭐⭐ 把暗线③（那个盒子）请回来当最后一个画面。
     ⛔ 原来第九章一次都没提它 ——&nbsp;而全书最大那个呼应就是它。
        第七章兑现了「捡回来」，但**兑现不等于收尾**：
        那时读者刚看见它回来，还没看见它回来之后长成了什么。 -->
<p class="landing">⭐⭐⭐ <b>但真正走完一圈的，不是那张图，是<a href="#s一">第一章</a>那个盒子。</b></p>
<p><em>赎到最后，桌上摆着的东西，跟 1990 年那个盒子<b>只差一处</b> ——&nbsp;
  <b>它是个矩阵，而且会擦。</b></em><br>
  <em>就这一处差别，<b>撑了三十年。</b></em></p>

<h3>9.7　想往下读的人：这些人讲得比我好</h3>

<div class="note info"><p>📌 这一讲里凡是讲得漂亮的地方，多半不是我想出来的。
  <em><b>下面这些人把同一件事讲得更透，值得直接去读原文。</b></em></p>
<p><b>说大白话的</b><br>
  · <a href="https://speech.ee.ntu.edu.tw/~hylee/ml/ml2021-course-data/self_v7.pdf" target="_blank" rel="noopener">李宏毅《自注意力机制》讲义</a>
  ——&nbsp;<em>中文圈讲自注意力最清楚的一份，图少而准。</em><br>
  · <a href="https://jalammar.github.io/illustrated-transformer" target="_blank" rel="noopener">Jay Alammar《The Illustrated Transformer》</a>
  ——&nbsp;<em>2018 年的老文章，至今没有被超越的图解。</em><br>
  · <a href="https://bbycroft.net/llm" target="_blank" rel="noopener">bbycroft 的 3D 交互可视化</a>
  ——&nbsp;<em>把一个 GPT 整个摊在三维空间里，可以一层一层点进去看。</em></p>
<p><b>把机制讲到骨头里的</b><br>
  · <a href="https://kexue.fm/archives/10091" target="_blank" rel="noopener">苏剑林《从 MHA、MQA、GQA 到 MLA》</a>
  与 <a href="https://kexue.fm/archives/10907" target="_blank" rel="noopener">《MLA 好在哪里》上下两篇</a>
  ——&nbsp;<em><a href="#s四">第四章</a>和<a href="#s五">第五章</a>大半的骨架来自这里。</em><br>
  · <a href="https://goombalab.github.io/blog/2025/tradeoffs" target="_blank" rel="noopener">Albert Gu《On the Tradeoffs of SSMs and Transformers》</a>
  ——&nbsp;<em><a href="#s七">第七章</a>那个「数据库 vs 大脑」的框架出自这里。</em><br>
  · <a href="https://sustcsonglin.github.io/blog/2024/deltanet-1" target="_blank" rel="noopener">杨松琳《DeltaNet Explained》三篇</a>
  ——&nbsp;<em>线性注意力这一支写得最清楚的一份中文作者英文博客。</em><br>
  · <a href="https://magazine.sebastianraschka.com/p/visual-attention-variants" target="_blank" rel="noopener">Sebastian Raschka《A Visual Guide to Attention Variants》</a>
  ——&nbsp;<em>要一张图看完所有变体，看这篇。</em></p>
<p><b>真的动手去撞硬件的</b><br>
  · <a href="https://henryhmko.github.io/posts/nsa_tpu/nsa_tpu.html" target="_blank" rel="noopener">Optimizing NSA for TPUs（Kernel Worklog）</a>
  ——&nbsp;<em>上面那条「块大小从 32 改成 64」就出自这里。
  全网少见的、把「落不落得下去」一路写到 kernel 那一层的记录。</em></p>
<p>⭐ <em>另外，本讲的<b>全部推导、消融表、一手出处，以及我们自己在 v7 上的实测</b>，
  都在<a href="topic-03-L300.html">完整版 L300</a> 里。</em></p></div>

</div></section>
'''

# ── 九章的「这一章干什么 / 用哪些图」───────────────────────────────
# ⭐ 这张表就是**故事脊梁本身**，现场先看它。⛔ 章写完了就把对应这行删掉。
PLAN = {
    # ✅ 第一章已写（2026-09-14 R62 R1）——&nbsp;原来这一行的「待写」块已删。
    # ✅ 第二章已写（2026-09-14 R62 R2）。
    # ⛔ 两张原计划的图**没有进 L200**，是有意的：
    #   · fig3-why-softmax（为什么非 softmax 不可）
    #   · fig3-capacity（128 维凭什么装得下上万个词）
    #   ⭐ 判据①：这两段换个位置照样成立 ——&nbsp;它们是「摊开算才站得住」的东西，
    #     属于 L300。故事线这一章要的是「换来了什么 / 欠下了什么」，不是旁支。
    # ✅ 第三章已写（2026-09-14 R62 R3）。
    # ⛔ fig3-info-law（互信息幂律 ＋ L2M 下界）**挪到第八章**：
    #   它在这儿只是「信息账」的严格版，属于旁支；
    #   ⭐ 而到第八章要回答「为什么非混着用不可」时，它是那条**理论下界**，
    #     是承重的。判据①：同一张图放在不同章，价值差一个数量级。
    # ✅ 第四章已写（2026-09-14 R62 R5）。
    # ⛔ fig3-knob1（四种存法的散点图）挪到第五章：它的题眼是
    #   「MQA 比 MLA 还小，却更差」——&nbsp;而第四章还没讲 MLA，那个反差成立不了。
    # ✅ 第五章已写（2026-09-14 R62 R6）。
    # ⛔ 两张原计划的图没进 L200：
    #   · fig3-rope（RoPE 三问：职责 / 为什么点积自带相对距离 / 凭什么外推 1M）
    #     ——&nbsp;那是一整套 RoPE 教学，在这一章是岔路。本章只需要一句话：
    #     **它带下标，所以挡住了挪括号。** 这句话 fig3-knob1 的第三格已经画了。
    #   · fig3-mla-why（事后缩印 vs 生来就窄）——&nbsp;它的判据跟 fig3-lowrank 同源，
    #     而 lowrank 带真数字。留一张就够。
    # ✅ 第六章已写（2026-09-14 R62 R7）。
    # ⭐⭐ 现场点名的那四问（top-2048 怎么挑 / 省不省显存 / trade-off /
    #   多花的算力与不规整访存）——&nbsp;**新画了一张 fig3-dsa-flow 专门答**，
    #   四问四块画面，一一对上。⛔ 答案里最重要的一条是「显存不省」。
    # ⛔ 没进 L200 的三张：fig3-nsa-why（NSA 三条路是被什么逼出来的）、
    #   fig3-csa-why（V4 的 CSA/HCA）、fig3-overmix（sink 口径修正）——&nbsp;
    #   它们是这一支的**支线**，主线这一章只要「滑窗为什么崩 → 死结 → DSA」。
    # ✅ 第七章已写（2026-09-14 R62 R8）。全书的回旋点。
    # ⛔ 三张没进 L200：fig3-at-gallery（八家的 A 矩阵长什么样）、
    #   fig3-duality（SSD 对偶）、fig3-two-brackets（同一张矩阵两种括号）——&nbsp;
    #   它们都是「摊开算才站得住」的东西，归 L300。
    # ⭐ 这一章的关键不在多一个名词，在**看法变了**：
    #   KV cache 才是状态的一种，而不是反过来。（Albert Gu 的框架，出处见正文）
    # ✅ 第八章已写（2026-09-14 R62 R9）。
    # ⭐ fig3-info-law 是从第三章挪过来的：在那儿它只是「信息账」的严格版（旁支），
    #   在这儿它是「为什么纯线性一定兜不住」的**理论下界**，承重。
    # ✅ 第九章已写（2026-09-14 R62 R10）。九章全部成稿。
}

# ⭐ 图注是**图的一部分**，不是正文的摘要。写图注前先问：
#   这句话图里已经有了吗？有就别写第二遍；图里没有的**故事接口**才写在这儿。
# ⛔ 三张 RNN 图跟 L300 共用同一份 `topic03-fig-rnn.py` ——&nbsp;改图两页一起变，
#   这是有意的（见文件头「图从哪来」）。改的时候记得去 L300 看一眼图注还合不合。
FIGS = {
    "__FIG_RNN_UNROLL__": ("fig-rnn-unroll", "fig3-rnn-unroll.svg",
        'topic03-fig-rnn.py',
        '⭐ 左边是 1990 年的原始画法，右边把它<b>沿时间摊开</b> ——&nbsp;'
        '<em>一条链的样子，得摊开才看得见。</em><br>'
        '看完只要记住一件事：<b>那条红线是横着走的。</b>'
        '<em>这本书接下来所有的招，都是在想办法<b>别沿着它走</b>。</em>'),

    "__FIG_KV_BORN__": ("fig-kv-born", "fig3-kv-born.svg",
        'topic03-fig-kv-born.py',
        '⭐⭐ <b>Ⓑ 那三堆是这张图的全部</b> ——&nbsp;'
        '<em>权重本来就在那儿，中间量算完就扔 ——&nbsp;'
        '<b>三堆里只有一堆过不了这一关。</b></em><br>'
        '⭐ <em>而 Ⓒ 把「必须留」翻译成了画面：<b>每来一个字就多一格，一格都不能扔。</b>'
        '——&nbsp;<b>后面七章全在拿这条格子开刀。</b></em>'),

    "__FIG_RNN_INSIDE__": ("fig-rnn-inside", "fig3-rnn-inside.svg",
        'topic03-fig-rnn-inside.py',
        '⭐⭐ <b>Ⓑ 是这张图的重心</b> ——&nbsp;'
        '<em>「叠加为什么不毁掉信息」不是个说法，<b>是个能算的数</b>：'
        '2 维里两个随机方向只差 50 度，1024 维里差 88.6 度。'
        '<b>叠加能还原，是维度买来的。</b></em><br>'
        '⭐ <em>而 Ⓑ 真正的巧处，是它把<b>好处和坏处画在了同一格</b>：'
        '「为什么不糊」和「为什么会淡」，<b>是同一个「每步再乘一次」的两面。</b><br>'
        '⭐ 底下那条落点带回答「KV 是不是这时候发明的」——&nbsp;'
        '<b>不是，而且这个「不是」正是全书的骨架。</b></em>'),


    "__FIG_RNN_HW__": ("fig-rnn-hw", "fig3-rnn-hw.svg", 'topic03-fig-rnn.py',
        '⭐ <b>Ⓑ 那一行现在只要记个印象就够了</b> ——&nbsp;'
        '<em>它是这张图里唯一没在讲 RNN 的一格，而<b>下一章那一刀，'
        '就是照着它来的</b>。</em>'),

    "__FIG_RNN_PAIN__": ("fig-rnn-pain", "fig3-rnn-pain.svg",
        'topic03-fig-rnn.py',
        '⭐ <b>下半截那三个盒子，就是这本书剩下八章的全部内容。</b><br>'
        '<em>其中最右边那一条最先发生 ——&nbsp;早在 2017 年之前，'
        '注意力就已经出现了，当时它<b>只是挂在 RNN 身上的一个配件</b>，'
        '专治「装不下」。</em><br>'
        '⭐⭐ <b>下一章讲的，就是有人突然问了一句：'
        '既然这个配件这么好使，能不能把 RNN 整个扔掉，只留配件？</b>'),

    # ── 第九章 ────────────────────────────────────────────────────
    "__FIG_PERSTEP__": ("fig-perstep", "fig3-perstep.svg",
        'topic03-fig-perstep.py',
        '⭐ <b>倍数是相对的，毫秒是绝对的 ——&nbsp;这一张把前八章全部换算成后者。</b><br>'
        '<em>⭐⭐ 看第二格里灰色那一段：<b>五个方案完全一样</b>。'
        '差别全在红的那一段上 ——&nbsp;这才是「KV cache 是瓶颈」这句话真正的样子。</em><br>'
        '⛔ <b>先看清这张图量的是「搬多少字节」，不是「算多少」</b> ——&nbsp;'
        '<em>灰色是<b>权重</b>（所有人共享，每步整个搬一遍，所以五根一样高），'
        '红色是<b>你一个人的 KV</b>。'
        '⭐ 那根灰的就是<a href="#s一">第一章</a>那句「每走一步都要把全部权重'
        '从显存搬一遍」——&nbsp;<b>它一个字都没过期。</b></em>'),

    "__FIG_TPU_GAP__": ("fig-tpu-gap", "fig3-tpu-gap.svg",
        'topic03-fig-tpu-gap.py',
        '⭐ <b>整张图的题眼是那个厨房比喻，值得慢慢看完。</b><br>'
        '<em>⛔ 特别注意中间那段引文的<b>日期</b> ——&nbsp;它不是五年前的旧话。</em>'),

    "__FIG_TPU_FIX__": ("fig-tpu-fix", "fig3-tpu-fix.svg",
        'topic03-fig-tpu-fix.py',
        '⭐ <b>三招的形状跟上一张那三个难处一一对上</b> ——&nbsp;'
        '<em>可以把两张图叠着读：<b>左边是病，右边是药。</b></em>'),

    "__FIG_LANDING__": ("fig-landing", "fig3-landing.svg",
        'topic03-fig-landing.py',
        '⭐⭐ <b>先看最下面那一行小字，它解释了整个顺序为什么是这样。</b><br>'
        '<em>能拿尺子量的先被搬走，量不出来的留到最后 ——&nbsp;'
        '<b>这不是研究者偷懒，这是任何一个领域都会有的走法。</b></em>'),

    "__FIG_GUN__": ("fig-gun", "fig3-gun.svg", 'topic03-fig-gun.py',
        '⭐⭐⭐ <b>这是整本书的收据。</b>'
        '<em>开场挂在墙上那把枪，在这里打响 ——&nbsp;'
        '而它打出来的答案，多半跟你开讲前猜的不一样。</em><br>'
        '⚠️ <em>两根条<b>不是一本账</b>，图里专门写了一句提醒 ——&nbsp;别加起来。</em>'),

    # ⭐ 2026-09-14 R12 挪到开篇。图注也跟着换角色：
    #   在开篇它是「领土」，任务是**让人看见有多乱**，不是让人看懂。
    "__FIG_CHRONICLE__": ("fig-chronicle", "fig3-chronicle.svg",
        'topic03-fig-chronicle.py',
        '⭐ <b>这是从 2014 年到今天真实发生过的事，四条支线各修各的毛病。</b>'
        '<em>不用现在看懂 ——&nbsp;先看清一件事：<b>它有多乱。</b></em><br>'
        '⭐⭐ <em>这一讲要做的，就是把这团乱麻理成<b>一条能从头读到尾的线</b>；'
        '而读完之后你再回来看它，<b>会看见完全不同的东西。</b></em>'),

    # ── 第八章 ────────────────────────────────────────────────────
    "__FIG_HYBRID__": ("fig-hybrid", "fig3-hybrid.svg", 'topic03-fig-hybrid.py',
        '⭐⭐ <b>先看左边那一格 ——&nbsp;它是这一整章的地基。</b>'
        '<em>如果掺便宜的层只是为了省钱，那一格的结果就不该长成那样。</em><br>'
        '⭐ <em>右边那把尺子上，每一个点都是一个真模型。'
        '<b>「大家都在哪」不用数，看那一摞堆在哪儿就行。</b></em>'),

    "__FIG_INFO_LAW__": ("fig-info-law", "fig3-info-law.svg",
        'topic03-fig-info-law.py',
        '⚠️ <b>先看清纵横轴都是对数的 ——&nbsp;这张图不能当普通折线读。</b><br>'
        '⭐⭐ <em>它真正的价值在于：<b>它把「纯线性到底行不行」从一句嘴仗，'
        '变成了一条能被证伪的规律。</b>——&nbsp;能被证伪，才值得当理由用。</em>'),

    "__FIG_RATIO_GRID__": ("fig-ratio-grid", "fig3-ratio-grid.svg",
        'topic03-fig-ratio-grid.py',
        '⭐ <b>数值全公开，两张图的纵轴跨度也特意做成一样 ——&nbsp;所以斜率可以直接比。</b><br>'
        '<em>⛔ 而这张图最值钱的一句，是它<b>老老实实标出了自己问不出什么</b>。'
        '——&nbsp;一张敢写「这我答不了」的表，比一张全是箭头的表可信得多。</em>'),

    "__FIG_RATIO_OR_COUNT__": ("fig-ratio-or-count", "fig3-ratio-or-count.svg",
        'topic03-fig-ratio-or-count.py',
        '⭐ <b>这一张的做法本身就是个示范：不猜，去数。</b>'
        '<em>十四个模型的层类型逐个拉下来，一行一行摆出来。</em><br>'
        '⭐⭐ <em>而数完之后会发现，<b>两派都真实存在</b> ——&nbsp;'
        '所以「守恒的是比值」这句常见说法，只说对了一半。</em>'),

    "__FIG_V4_ARCH__": ("fig-v4-arch", "fig3-v4-arch.svg",
        'topic03-fig-v4-arch.py',
        '⭐ <b>Ⓐ 那两条色带是这张图的全部</b> ——&nbsp;'
        '<em>V3.2 的 64 挂在 512 <b>外面</b>，V4 的 64 长在 512 <b>里面</b>。'
        '一眼就看出「576 变 512」省在哪。</em><br>'
        '⭐⭐ <em>而 Ⓓ 那个 ④ 是最容易漏掉的一步：'
        '<b>K 和 V 共用一条，所以 V 也被转过了，得按负位置转回来。</b>'
        '——&nbsp;省下来的那一半，代价就写在这一步上。</em>'),

    "__FIG_NOPE__": ("fig-nope", "fig3-nope.svg", 'topic03-fig-nope.py',
        '⭐ <b>看①和②的对照就够了：同一件事，谁来负责。</b><br>'
        '<em>⭐⭐ 这是这本书里很少见的一种好事 ——&nbsp;'
        '<b>一个麻烦不是被解决的，是因为换了结构而不再存在。</b></em>'),

    # ── 第七章 ────────────────────────────────────────────────────
    "__FIG_ASSOC__": ("fig-assoc", "fig3-assoc.svg", 'topic03-fig-assoc.py',
        '⭐ <b>两块矩阵是按真实比例画的，所以这一张不用读，用看的。</b><br>'
        '⛔ <em>盯着右边那一格里的形状 ——&nbsp;<b>句长这一维整个不见了。</b>'
        '这就是这一支敢说「跟话有多长无关」的全部底气。</em>'),

    "__FIG_NOTEPAD__": ("fig-notepad", "fig3-notepad.svg",
        'topic03-fig-notepad.py',
        '⭐⭐ <b>看这张图的时候，请把<a href="#s一">第一章</a>那个盒子放在旁边一起看。</b><br>'
        '<em>它们是同一个东西 ——&nbsp;<b>只是这一块大得多，而且会擦。</b></em>'),

    "__FIG_ERASE__": ("fig-erase", "fig3-erase.svg", 'topic03-fig-erase.py',
        '⭐ <b>三代模型读的方式几乎没动过，全部差别都在「写之前怎么处理旧的」。</b><br>'
        '<em>⭐⭐ 所以以后碰到这一支的任何一个新名字，你只要问一句：'
        '<b>它的「擦」是怎么决定的？</b>——&nbsp;剩下的部分，多半跟上一代一样。</em>'),

    "__FIG_CHUNKWISE__": ("fig-chunkwise", "fig3-chunkwise.svg",
        'topic03-fig-chunkwise.py',
        '⭐⭐ <b>这一张最该带走的不是那个名词，是它证明了一件事：</b>'
        '<em>「串行」有时候不是数学性质，<b>只是算的顺序没安排好</b>。</em><br>'
        '⭐ <em>同一个式子，一个字没改，并行度就从 1 变成了一批 ——&nbsp;'
        '<a href="#s一">第一章</a>那个卡死人的痛，在这里第二次被解开。</em>'),

    # ── 第六章 ────────────────────────────────────────────────────
    # ── 第七章 ────────────────────────────────────────────────────
    "__FIG_GU__": ("fig-gu", "fig3-gu.svg", 'topic03-fig-gu.py',
        '⭐⭐ <b>Ⓑ 那两个圈值得多看两秒</b> ——&nbsp;'
        '<em>左右用的是<b>同样两个词</b>，只是<b>谁装着谁换了个位置</b>。'
        '这件事用嘴说，听的人要在脑子里自己转一次；<b>画出来是一眼的事。</b></em><br>'
        '⭐ <em>Ⓐ 那条轴是<b>全课的地图</b>：前面六章都在左半边挪，'
        '<b>只有这一章真的走到了右端</b>。</em>'),


    "__FIG_KNOB2__": ("fig-knob2", "fig3-knob2.svg", 'topic03-fig-knob2.py',
        '⭐ <b>别一张一张看，要五张一起看</b> ——&nbsp;'
        '<em>这一支的骨架是从「五张摆在一起」里浮出来的，单看任何一张都看不见。</em><br>'
        '⭐⭐ <em>看完你会得到一把尺子：以后碰到一个没听过的稀疏方案，'
        '<b>先把它按到这五张里去比一比，比记住它叫什么名字有用得多。</b></em>'),

    "__FIG_SWA_WHY__": ("fig-swa-why", "fig3-swa-why.svg",
        'topic03-fig-swa-why.py',
        '⛔⛔ <b>下面那三根柱子是按对数画的</b> ——&nbsp;'
        '<em>5158 和 5.40 之间差的是<b>三个数量级</b>，不是三倍。</em><br>'
        '⭐ <em>一个模型从「能用」到「彻底胡说」，中间只隔着<b>四个 token</b>。</em>'),

    "__FIG_SINK__": ("fig-sink", "fig3-sink.svg", 'topic03-fig-sink.py',
        '⭐ <b>上一张停在「它崩了」，这一张回答「凭什么会崩」。</b><br>'
        '⛔⛔ <em>读 ② 那一格的时候请<b>先不要看数字，先看两个方框的形状</b> ——&nbsp;'
        '这一格的标题里「<b>看起来</b>」三个字，是要用眼睛验的。</em><br>'
        '⭐⭐ <em>验完再看数字，你会得到本讲最便宜也最贵的一条经验：'
        '<b>两个长得一样的方案，结果可以差一千倍。</b></em>'),

    "__FIG_DEEPSEEK_SPARSE__": ("fig-deepseek-sparse", "fig3-deepseek-sparse.svg",
        'topic03-fig-deepseek-sparse.py',
        '⭐⭐ <b>Ⓑ 那两条轴是这张图的全部</b> ——&nbsp;'
        '<em>横着是「压多狠」，竖着是「要不要运行时挑」。'
        '四个名字一摆上去，<b>那条对角线自己就出来了</b>。</em><br>'
        '⭐ <em>Ⓐ 里记一条对照：<b>NSA 挑的是块，DSA 挑的是散落的单 token</b> ——&nbsp;'
        '这一条决定了它们在硬件上的命。</em>'),


    "__FIG_CHICKEN__": ("fig-chicken", "fig3-chicken.svg",
        'topic03-fig-chicken.py',
        '⭐ <b>左边那个圈绕不过去，只能破 ——&nbsp;而破法有三条，不是一条。</b><br>'
        '<em>⭐⭐ 三条各自聪明在不同地方，值得一条一条读：'
        '<b>一条是找人替考，一条是废物利用，一条干脆把考场缩小了。</b></em><br>'
        '⭐⭐ <em>读到最下面那条带子 ——&nbsp;<b>第三条有两家不同的团队，'
        '各自摸到了同一个解。</b>一件事被独立做对两次，分量跟只有一家做过是不一样的。</em>'),

    "__FIG_DSA_WHY__": ("fig-dsa-why", "fig3-dsa-why.svg",
        'topic03-fig-dsa-why.py',
        '⭐ <b>看中间那一格就够了：它怎么知道徒弟学到位了没有。</b><br>'
        '<em>⚠️ 至于 2048 这个数是怎么定下来的 ——&nbsp;图里说了老实话，'
        '<b>论文没给消融</b>。能查到的只有它占多大比例，'
        '而两个公开口径在这个比例上给出的答案<b>差着一倍多</b>。</em>'),

    "__FIG_DSA_FLOW__": ("fig-dsa-flow", "fig3-dsa-flow.svg",
        'topic03-fig-dsa-flow.py',
        '⭐⭐⭐ <b>这一张是为现场那四个问题专门画的，四问四块，一一对上。</b><br>'
        '<em>要是只看一眼，就看第 ② 块那条按真比例画的带子 ——&nbsp;'
        '<b>橙色那一小条是这一步读进来的，灰色那一大片全都还在显存里躺着。</b></em>'),

    # ── 第五章 ────────────────────────────────────────────────────
    "__FIG_KNOB1__": ("fig-knob1", "fig3-knob1.svg", 'topic03-fig-knob1.py',
        '⭐⭐ <b>横轴是「一份占多少地方」，越往左越省。</b>'
        '<em>那为什么最左边那个不是赢家？</em><br>'
        '⛔ <b>因为这张图比的从来不是「谁存得最少」</b> ——&nbsp;'
        '<em>它比的是<a href="#s四">上一章</a>那个问题的答案：'
        '省下来的，是拿体积换的，还是拿自由度换的。</em>'),

    "__FIG_TWO_LANES__": ("fig-two-lanes", "fig3-two-lanes.svg",
        'topic03-fig-two-lanes.py',
        '⭐⭐ <b>三步，从上往下看就行 ——&nbsp;不用读那两行公式。</b>'
        '<em>紫色的方块本来能从仓库那侧搬到提问那侧；'
        'RoPE 在中间立了一道闸，搬不过去；于是把一条轨劈成两条，'
        '让闸只站在窄的那条上。</em><br>'
        '⛔ <b>中间那个黄框别跳过，它是这张图真正的机关。</b>'
        '<em>左右两边画的东西几乎一样，差的只有一个角标 ——&nbsp;'
        '<b>而 MLA 那个 512 ＋ 64 的怪形状，整个是被那个角标逼出来的。</b></em>'),

    "__FIG_ABSORB__": ("fig-absorb", "fig3-absorb.svg", 'topic03-fig-absorb.py',
        '⭐ <b>看①和②的区别就够了 ——&nbsp;算的是同一个东西，只是括号站的位置不同。</b>'
        '<em>而括号换个位置，仓库里那一万个压缩包就一个都不用动。</em><br>'
        '⛔ <b>这一步要是不成立，MLA 整套设计当场作废</b> ——&nbsp;'
        '<em>所以它不是一个优化技巧，是这一章的地基。</em>'),

    "__FIG_MLA_CREDIT__": ("fig-mla-credit", "fig3-mla-credit.svg",
        'topic03-fig-mla-credit.py',
        '⭐⭐ <b>这组实验最狠的一点：KV cache 全程钉死不动。</b>'
        '<em>所以每一级台阶量的都不是「多花了多少」，'
        '而是<b>同样的代价，能换回多少</b>。</em><br>'
        '⭐ <em>这也是看任何一篇「我们又省了 N 倍」时该先问的一句：'
        '<b>你把代价那一头按住了吗？</b></em>'),

    # ── 第四章 ────────────────────────────────────────────────────
    "__FIG_WIRING__": ("fig-wiring", "fig3-wiring.svg", 'topic03-fig-wiring.py',
        '⭐ <b>零公式、零数字，只回答一件事：那个「一份」到底是什么。</b><br>'
        '<em>四家该做的乘加几乎一样多 ——&nbsp;'
        '所以下面两章跟你算的每一笔，算的都不是算力。</em><br>'
        '⛔ <b>最右边那一格今天先不看</b>（它根本没在共用 K/V），'
        '<em>留到<a href="#s五">下一章</a>。</em>'),

    "__FIG_MQA_WHY__": ("fig-mqa-why", "fig3-mqa-why.svg",
        'topic03-fig-mqa-why.py',
        '⭐⭐ <b>整章的证据都在中间那三行困惑度上，值得盯久一点。</b><br>'
        '<em>它们回答的不是「MQA 掉了多少点」，而是一个更有用的问题：'
        '<b>同样只剩一份，为什么少了几个「问的人」就差这么多。</b></em>'),

    "__FIG_COPY_MATRIX__": ("fig-copy-matrix", "fig3-copy-matrix.svg",
        'topic03-fig-copy-matrix.py',
        '⭐⭐ <b>这四块矩阵，是这本书里信息密度最高的一张图。</b>'
        '<em>四个名词第一次被摆在同一个位置上，比的只有一件事：'
        '<b>同一块矩阵里写了什么。</b></em><br>'
        '⭐ <em>看完就会明白，为什么<a href="#s五">下一章</a>不是「又一个新方法」，'
        '而只是<b>把其中一块的格子解开、交给模型去填</b>。</em>'),

    # ── 第三章 ────────────────────────────────────────────────────
    "__FIG_RNN_DECODE__": ("fig-rnn-decode", "fig3-rnn-decode.svg",
        'topic03-fig-rnn.py',
        '⭐ <b>三行用的是同一把尺子，所以可以直接叠着看。</b>'
        '<em>Ⓐ 是开篇那条老链，Ⓑ 是它当年赢下来的地方，'
        'Ⓒ 是你此刻正在用的那个东西。</em><br>'
        '⛔⛔ <b>赢下来的是 Ⓑ ——&nbsp;可你每天在用的是 Ⓒ。</b>'),

    "__FIG_FLIP__": ("fig-flip", "fig3-flip.svg", 'topic03-fig-flip.py',
        '⭐⭐ <b>这张图最该带走的，是它什么都没证明「技术变差了」。</b>'
        '<em>同一个模型、同一份权重，只是被拿去干了另一种活，'
        '瓶颈就自己换了个位置。</em><br>'
        '⛔ <b>所以这本书后面所有的招，修的都不是模型</b> ——&nbsp;'
        '<em>修的是「我们想怎么用它」和「机器给得起什么」之间的那道缝。</em>'),

    "__FIG_MOTIVES__": ("fig-motives", "fig3-motives.svg",
        'topic03-fig-motives.py',
        '⭐ <b>左右两半是各长各的，读的时候别把它们并成一句话。</b>'
        '<em>一半是机器逼出来的，一半是数据自己露出来的破绽。</em><br>'
        '⭐⭐ <b>右下角那把尺子先记一下</b> ——&nbsp;'
        '<em>从下一章起，每出场一个新名词，我们都只拿它量两下。</em>'),

    # ── 第二章 ────────────────────────────────────────────────────
    "__FIG_ATTN_INVENTED__": ("fig-attn-invented", "fig3-attn-invented.svg",
        'topic03-fig-attn-invented.py',
        '⭐ <b>三步，每一步只修上一步的一个毛病</b> ——&nbsp;'
        '<em>顺着读一遍，你会发现没有哪一步需要天才，只需要有人肯说「这儿不对」。</em>'),

    "__FIG_MHA_SWAP__": ("fig-mha-swap", "fig3-mha-swap.svg",
        'topic03-fig-mha.py',
        '⭐ <b>左边是上一章那条链，右边是它的替代品。</b>'
        '<em>横着那根箭头没了，取而代之的是一张人人对人人的表 ——&nbsp;'
        '<b>这张表，就是后面整本书都在跟它较劲的那个「注意力矩阵」本人。</b></em>'),

    "__FIG_MHA_FLOW__": ("fig-mha-flow", "fig3-mha-flow.svg",
        'topic03-fig-mha.py',
        '⭐ 跟着最后那个字走一遍，你就懂了 ——&nbsp;<b>它不用等谁传话，'
        '直接一嗓子问全场，然后按各家报的价把货收回来。</b><br>'
        '<em>⭐⭐ 而且这不是轮流来：<b>六个位置同时在做同一件事</b>。'
        '——&nbsp;上一章卡死人的那个「只能一步一步来」，到这里彻底没有了。</em>'),

    "__FIG_MHA_QKV__": ("fig-mha-qkv", "fig3-mha-qkv.svg", 'topic03-fig-mha.py',
        '⭐⭐ <b>这里最容易听岔</b>：注意力<b>不是「挑出最像的那一个」</b>，'
        '<em>而是<b>按百分比把几家的货混成一碗</b>。</em><br>'
        '⛔ 而混完之后，<b>牌子和货（K 和 V）不能扔 ——&nbsp;下一个字还要用</b>。'
        '<em>留下来的这两份，就是这本书的反派：<b>KV cache</b>。</em>'),

    "__FIG_MHA_HEADS__": ("fig-mha-heads", "fig3-mha-heads.svg",
        'topic03-fig-mha.py',
        # ⛔ 原稿这条图注把绿带那两句（「切开不是加倍」「要留下来的乘了 8」）
        #   换了几个词又说一遍 ——&nbsp;查重比不出来（改写），是截图看出来的。
        # ⭐ 换成图给不了的：**把这一格的代价，摊开成后面四章的地图。**
        '⭐⭐ <b>这一格里藏着后面四章的全部导火索。</b>'
        '<em>为了看得更清楚，一个头被切成了八个 ——&nbsp;'
        '而这一刀的代价，绿带里已经替你算完了。</em><br>'
        '⛔ <b>从下一章起，所有人都在跟这个代价讨价还价</b>：'
        '<em><a href="#s四">第四章</a>直接砍它、'
        '<a href="#s五">第五章</a>把它压成一份压缩件、'
        '<a href="#s六">第六章</a>照存但不全读、'
        '<a href="#s七">第七章</a>干脆不存。</em>'),

    "__FIG_TX_BASE__": ("fig-tx-base", "fig3-tx-base.svg",
        'topic03-fig-transformer.py',
        # ⛔ 原稿这里复述了图自己的标题、还把图里已有的致谢又写了一遍 ——
        #   新上的图注查重当场抓到两处。⭐ 改成只交代**读者现在该干什么**。
        '⭐ <b>别急着看懂整张 ——&nbsp;现在只做一件事</b>：'
        '<em>在这一堆字母里，把那个<b>会随对话越变越长</b>的找出来。</em><br>'
        '⭐⭐ 找到它，你就找到了后面<b>所有招数共同的靶子</b> ——&nbsp;'
        '<em>剩下七章，全是在想办法拿它开刀。</em>'),

    "__FIG_ARC__": ("fig-arc", "fig3-arc.svg", 'topic03-fig-arc.py',
        # ⭐ 新上的「图注 ↔ 图内文字」查重当场抓到这一句照抄了图里那半句，
        #   顺手改成图给不了的东西：**为什么两条必须同时发生。**
        '⭐ <b>这就是整本书的路线图。</b>六段，每段只问三件事：图啥、'
        '带来了什么、欠下了什么。<br>'
        '<b>底下那两条数才是落点，而且缺一不可</b> ——&nbsp;'
        '<em>只把长度做上去、价钱降不下来，没人用得起；'
        '价钱降下来了、长度还是 2K，那它仍旧只是个接话的补全器。</em>'),
}

out = [head, '''
</head>
<body>

<!-- ⛔ 这个文件由 Courses/tools/topic03-build-L200.py 生成。
     **正文写在那个脚本的 BODY 常量里** —— 改内容改那里，别改这个产物。 -->

<div class="hero"><div class="wrap">
  <div class="crumb"><a href="index.html">加速器系统课程</a> ／ 主线 ／ 专题三
    ／ <b>注意力演进</b></div>
  <h1>注意力演进</h1>
  <div class="en">A Chain, Cut Loose, and Coming Back Around</div>
  <div class="hook">
    2020 年的 GPT-3 只记得住 <b>2048</b> 个 token；今天的模型记 <b>100 万</b>。<br>
    <em>——&nbsp;这六年的注意力演进，讲的就是这 <b>512 倍</b> 是怎么换来的。</em>
  </div>
  <p style="max-width:820px;color:var(--gray)">
    这一讲<b>按时间顺序讲一个完整的故事</b>：一条链怎么被剪断、
    剪断之后欠下了什么账、这笔账后来被四拨人用四种办法还、
    以及为什么最后<b>又绕回了那块固定大小的小本子</b>。
    <em>多图、少字、说大白话。</em>
  </p>
  <div class="chips">
    <span class="chip">前置 <b>专题一 · 专题二</b></span>
    <span class="chip">读法 <b>从头顺着读</b></span>
    <span class="chip">要推导与实测 <b>看 L300</b></span>
  </div>
  <p class="author">课程作者　<b>Chris Yang</b><span class="sep">·</span>Google Cloud
    AI Infra 架构师</p>
</div></div>

<div class="wrap">''' + FAM.nav("topic-03.html") + '''</div>

''', BODY, '''

<div class="wrap" style="padding:32px 0 64px">
  <p style="color:var(--gray)">
    ← 回 <a href="index.html">课程总纲</a>　·
    硬件背景在 <a href="topic-02.html">专题二 · TPU 与 GPU</a>　·
    推导 · 消融 · 一手出处在 <a href="topic-03-L300.html">本讲 L300 完整版</a><br>
    本页由 <code>Courses/tools/topic03-build-L200.py</code> 生成 ——&nbsp;
    <b>正文写在那个脚本里</b>。
  </p>
</div>

</body></html>''']

_html = "\n".join(out)

for ph, (what, figs, asks) in PLAN.items():
    assert ph in _html, "正文里没有 %s —— 章写完了要连这一行一起删" % ph
    _html = _html.replace(ph, todo(what, figs, asks))

_html = P.place_table(_html)
_html = P.place_figs(_html, FIGS)
# ⭐ 2026-09-16：这一份是**要投屏讲的**，所以装上折叠开关，默认只剩图。
#   ⛔ L300 不装 ——&#160;那一份是拿来读的。
_html = P.add_figonly_toggle(_html)
P.finish(_html, OUT, SECTIONS, "topic-03.html")
